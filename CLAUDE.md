# bionic-centers-manager — مرجع للمساعد الذكي

> هذا الملف يحوي خلاصة المشروع وآخر ما تمّ عليه. اقرأه أولاً قبل أي تعديل
> أو إجابة لتفهم السياق الكامل.
>
> **وابدأ بالقسم ٠ أدناه**: هو وحدَه يقول أين وصل العمل اليوم، وما هو
> مفتوحٌ، وما هو مؤجَّلٌ بقرار المالك، وخطُّ الأساس الذي تُقاس عليه أيّ
> دعوى كسر.

---

## 0. حالة المشروع الآن — ابدأ من هنا

> **آخرُ تحديثٍ لهذا القسم: ٢٠٢٦-٠٩-٢٥.**
>
> بقيّةُ هذا الملفّ تقول **«كيف يعمل النظام»** — وهي مرجعٌ دائم. وهذا القسمُ
> وحدَه يقول **«أين وصلنا اليوم»**. **ومَن أنهى عملاً يحدّثه قبل أن يغلق
> مهمّته**: قسمُ حالةٍ لا يُحدَّث يشيخ فيصير يكذب، وقسمٌ كاذبٌ أسوأُ من
> غيابه.

### آخرُ ما دُمج
**سكربتا استرجاع الأعذار التي مُحيت قبل ٤٠٣ — جاهزان، والكتابةُ تنتظر المالك** (§4.ao،
آخرُ القسم): قاعدةُ المالك ٢٠٢٦-٠٩-٢٥ — «الخبير الذي يكتب عذراً في أيّ موضعٍ خاصٍّ للعذر
يجب ألّا يكون مريضه أحمر وإنما أصفر». **فُحص الجديدُ أوّلاً فوُجد سليماً**: كلُّ شاشةٍ تعرض
التأخيرَ تقرأ العذر، ولا كاتبَ يمحوه من أمرٍ حيٍّ بعد ٤٠٣. **وبقيت أوامرُ مُحي عذرُها قبله
حمراء** — والعذرُ لم يضع، فسطرُه في الخطّ الزمني باقٍ بصيغةٍ ثابتة منذ وُلد. فصار في جذر
الريبو ملفّان يلصقهما المالكُ في Neon: **معاينةٌ للقراءة وحدها** تعرض كلَّ أمرٍ حيٍّ بلا عذرٍ
وفي سجلّه عذرٌ كتبه خبيرٌ بيده (العذرُ الذي سيعود، ومَن كتبه ومتى)، ثمّ **سكربتُ كتابة** يُعيد
**آخرَ** عذرٍ مكتوب باستبعادٍ يحدّده المالك، ويكتب سطراً في الخطّ الزمني والتدقيق لكلّ أمر،
ويتراجع كلُّه عند أيّ اختلاف. **ليسا ترحيلاً — ولم تُشغَّل الكتابةُ بعد.** **بلا ترحيلٍ ولا
حرفٍ في شيفرة التطبيق.**
وقبله **بطاقةُ فرعٍ آخر في «مراجعة الحركة» تُقرأ ولا يُؤشَّر عليها** (§4.t، آخرُ القسم): قرارُ
المالك ٢٠٢٦-٠٩-٢٥ — «فيجب ان ترى الفروع المشتركة لمريض واحد ترى كل شيء يحدث لهذا
المريض سواء بفرعهم ام بغيره ليعلموا مافعل اما القرارات فبالتاكيد تحصر لصاحب الفرع هو
من يقرر». الطابورُ يعرض بطاقاتِ الفرع الآخر للعلم (قرار ٢٢/٩)، **وكان يُظهر عليها أزرارَ
التأشير بقدرة المستخدم العامّة** — فيضغط مديرُ بغداد «تمت المراجعة» على بطاقة ذي قار
فيُردّ «غير مصرح لك بهذا الفرع» (مُعادٌ حيّاً بالأزرار الثلاثة، بصفر كتابة). فصار الخادمُ
يرسل على كلّ صفٍّ `branchInScope` **من الدالّة نفسِها التي يردّ بها التأشير**
(`requestBranchInScope`)، وبطاقةُ غير فرعك بلا أزرار: سطرُ «هذه الحركة لفرع ذي قار —
يراجعها ذلك الفرع» و«فتح ملف المريض». **بلا ترحيلٍ ولا صلاحيةٍ تتّسع أو تضيق** — الحارسُ
مطابقٌ بحرفه.
وقبله **القرارُ لمن أرسل المريض، والمنتظرُ لفرعه وحده** (§4.t، آخرُ القسم): قرارُ المالك
٢٠٢٦-٠٩-٢٥ — «إن احتاج معاينةً إضافية أو أُرسل للمعاينة وهو متاحٌ لعدّة أفرع… يعود
القرارُ لهذا الفرع حصراً… لأن الفرعَ الآخر لم ولن يعرف أن اشترى المريضُ أو لم يشترِ».
أمران، **مُعادٌ كلاهما حيّاً قبل أيّ تعديل**: (١) **معاينةٌ بلا جهاز** كان قرارُها
يُفتَح في فرع الحالة ولو أرسلت المريضَ بغداد، فتحسمه ذي قار (٢٠٠) وتُردّ بغدادُ
المُرسِلة ٤٠٣. صار **لفرع أوّل طلبٍ أغلقته المعاينة** (الطلبُ الذي يحمله صفُّ
«معايناتي») ما دام يصل الملفّ، ومعه ما يلزمه ليُباع هناك: خبيرُه يُتحقَّق منه بفرع
القرار، وجهازُه الأوّل يُفتَح فيه — فالأمرُ والكلفةُ في بغداد. **والمعاينةُ نفسُها على
فرعها، وطلبُ الجهاز كما كان.** (٢) **القرارُ المنتظر في ملفّ المريض لفرعه وللمسؤول
وحدهما**، والمحسومُ تاريخٌ للجميع — وبه زال بندُ «إتمام البيع على قرار فرعٍ آخر» من
«ما هو مفتوح». **بلا ترحيلٍ ولا حرفٍ في الواجهة ولا في القاعدة.**
وقبله **التراجعُ عن ٤٠٩ و٤١٠ — توسّعٌ لم يطلبه المالك، وإصلاحان ضيّقان لا ينقلان شيئاً**
(§4.t، آخرُ القسم): حكمُ المالك بعدهما: «المريضُ الذي يُشارَك مع فرعٍ آخر لا يُنقَل
ملفُّه — بدّل قالباً في بغداد بعد أن اشترى من ذي قار، ثمّ عاد لذي قار واشترى قدماً…
ممنوعٌ أن تُنقَل أموالُه من فرعه القديم… **ممنوعٌ التوسّعُ والافتراضُ والتخمين**».
**وهو محقّ**: ٤٠٩ نقل قرارَ ما بعد المعاينة وجهازَه إلى فرع الخبير المختار، وجعل
قائمةَ الخبراء بملفّ المريض، وفتح أبوابَ القرار لكلّ فرعٍ يصل الملفّ، ونسب المعاينةَ
بلا جهاز لمن أرسل المريض — **كلُّها من عندي لا من طلبه** — و٤١٠ بنى فوقه. **وقولي
إن المسؤولَ يرى خبراءَ ذي قار وحدها كان بلا تحقّق**: رأى فيها عناد وهو من بغداد.
فأُرجعت الملفّاتُ كلُّها **إلى ما قبل ٤٠٩ بحرفها** (الشجرةُ مطابقةٌ آلياً قبل أيّ
سطرٍ جديد)، ثمّ أمران لا ينقلان شيئاً: (١) **جهازٌ فتحه الفرعُ المُتاح يُباع في
فرعه** — كان بيعُه **لا يعود بردٍّ إطلاقاً** على نسخة ما قبل ٤٠٩ (مُعادٌ حيّاً):
صار الأمرُ والكلفةُ والدفعةُ في فرع الجهاز كما فُتح؛ و(٢) **نافذةُ المعاينة تفتح على
ما سجّله الاستقبال** (شكوى المالك الثانية): كانت تقرؤه من ملفّ المريض، وذلك البابُ
يردّ **صامتاً** طبيباً بلا «عرض المرضى» (٤٠٣) وطبيباً يعمل في فرعين والمريضُ في غير
فرع جلسته (٤٠٤)، فتفتح فارغةً من نوع البتر ونوع المسند — صارت تقرؤه من باب المعاينة.
**ومثالُ المالك بعينه مُختبَرٌ حيّاً**: كلُّ دفعةٍ في قاصة فرعها، والأولى بحرفها.
**بلا ترحيلٍ ولا حرفٍ في القاعدة.** وزهراء وأيوب **ينتظران بيانات الإنتاج** (أدناه).
وقبله **تصحيحُ مراجعة Codex على ٤٠٧ — طلبُ الجهاز يُلغيه فرعُه، وسطرُ التدقيق في فرع
الحركة** (§4.t، آخرُ القسم): ٤٠٧ فتح إلغاءَ طلب الجهاز لكلّ فرعٍ يصل الملفّ، **فمريضٌ
أُتيح لبغداد وكربلاء معاً كانت كربلاء تُلغي طلبَ جهازٍ فتحته بغداد** (٢٠٠، ويُسحب
معه طلبُ مراجعته المعلَّق)، ويُلغيه فرعُ التسجيل كذلك — مُعادٌ حيّاً قبل أيّ تعديل.
فصار **فرعُ الحلقة نفسِها** يُحكَم عليه **تحت قفلها** (`actorBranchScope`) — قاعدةُ
«عاد للشراء» بحرفها — ⟵ ٤٠٣ تسمّي الفرعَ المالك، بصفر كتابة. ومعه **سطرُ تدقيق
«إضافة نوع حالة»** صار في فرع الحركة كالحالة التي يصفها (كان فرعَ التسجيل). **بلا
ترحيلٍ ولا حرفٍ في الواجهة ولا في القاعدة.**
وقبله **الفرعُ المُتاحُ له الملفّ يُرسل للطبيب ويفتح طلبَ الجهاز كفرع التسجيل** (§4.t،
آخرُ القسم): شكوى المالك — «زهراء واثق مناحي ضيول» أُتيحت لبغداد مع ذي قار، فدخل
عليها استقبالُ بغداد فظهر «غير مصرح لك بهذا الفرع». **والسببُ نواةٌ واحدة**:
`createReviewRequestTx` كانت تقيس بفرع **التسجيل** وحده، وهي البابُ الواحد لكلّ
طلبِ مراجعة. **مُعادٌ حيّاً قبل أيّ تعديل**: «إرسال لمراجعة الطبيب» ٤٠٣ ·
«إضافة نوع حالة» ٥٠٠ بالرسالة نفسِها **بعد** أن أُضيفت الحالةُ وزيارتُها · «طلب
جهاز» ٥٠٠ «تعذّر بدء الجهاز» **بعد** أن فُتحت الحلقة · «زيارةُ جهاز» بلا ردٍّ
إطلاقاً · وأجهزةُ المريض والملخّصُ الماليّ مردودان. فصار الفحصُ
`scopeReachesPatient`، **والطلبُ يُنسَب لفرع الحركة** (ولعمليته في «عاد للشراء»)،
**والحالةُ الجديدة** من «إضافة نوع حالة» في فرع الحركة كذلك، والقراءتان وإلغاءُ
طلب الجهاز بالقاعدة نفسِها. **ومعه ثغرةٌ أقدم**: `POST /api/visits` لم يكن يفحص
الفرعَ إطلاقاً — صار يردّ مَن لا يصل الملفّ **قبل أيّ كتابة**. **بلا ترحيلٍ ولا
حرفٍ في الواجهة.**
وقبله **تصحيحُ مراجعة Codex على ٤٠٥ — إذنُ الخبير هو الإسنادُ نفسُه، ويُحكَم عليه تحت
القفل** (§4.ao، آخرُ القسم): `loadWritable` تأذن للخبير لأن الأمرَ مسنَدٌ إليه —
**وتقرأ ذلك قبل القفل**، ولم يكن كاتبٌ يُعيد فحصَه. فإسنادٌ إلى خبيرٍ آخر يلتزم بين
الفحص والقفل كان يترك طلبَ الأوّل يكتب **بعد أن فقد الأمر**. سمّت المراجعةُ
`documentHoldReason` وحدَه، **والعطبُ صنفٌ لا حالة**: مُعادٌ حتميّاً على `main` قبل
أيّ تعديل في **الأبواب الستّة** المتاحة للخبير — كلُّها ردّت ٢٠٠ وكتبت: أوقف وأرجع
بمرحلة واستأنف ونقل والتزم بموعدٍ لأمرٍ لم يعد أمرَه. فصار **أساسُ الإذن**
(`WriteAuthority`: `role` أو `assignment`) يُبنى في `loadWritable` وحدها ويُمرَّر
**مطلوباً** إلى الكتّاب السبعة، ويُحكَم **أوّلَ ما بعد القفل** (`assertWriteAuthority`)
⟵ ٤٠٣ «أُسنِد هذا الأمرُ إلى خبيرٍ آخر أثناء طلبك — لم يُكتب شيء.» **والمديرُ
والمسؤولُ لا يُردّان لأن الخبيرَ تغيّر** (مُثبَتٌ ومُتحقَّقٌ بالعكس). **بلا ترحيلٍ ولا
حرفٍ في الواجهة ولا في القاعدة.**
وقبله **تصحيحُ مراجعة Codex على ٤٠٤ — سببُ التوقّف القائم يُكتب، ولا يُرجِع الأمرَ
بمرحلة** (§4.ao، آخرُ القسم): ٤٠٤ فتح «توقّف / مشكلة» للمتوقّف الموروث بلا سبب،
لكنّ نافذتَه **توقّفٌ جديد** — و«إعادة عمل فني» فيها لا تُكتب إلّا برجوعٍ إلى مرحلةٍ
سابقة. فإعادةُ عملٍ موروثة (رجعت فعلاً في النظام القديم) **كانت تُرجَع مرّةً ثانية
ويُسجَّل لها صفُّ إعادة عملٍ ثانٍ وحدثُ مرحلةٍ يصل المريض** لمجرّد كتابة سببها،
وفي أوّل المراحل لا يُكتب السببُ أصلاً. **مُعادٌ حيّاً قبل أيّ تعديل.** فصار لسبب
التوقّف القائم بابُه: `POST …/hold-reason` (`documentHoldReason` — **النوعُ نوعُه
والمرحلةُ مرحلتُه**، يُحكَم تحت القفل، وسطرٌ `hold_reason` واحد)، ونافذتُه تُختار
بـ`holdDialogKind` لحظةَ الضغط. **و`/hold` و`reworkToStage` بحرفهما** — إعادةُ
العمل الثانية مشروعةٌ ومُختبَرة. **بلا ترحيلٍ ولا عمودٍ ولا قيد.**
وقبله **تصحيحُ مراجعة Codex على ٤٠٣ — المتوقّفُ بلا سببٍ مكتوب ليس معذوراً، وصفحتُه
لا تصمت** (§4.ao، آخرُ القسم): ترحيلا ٠٤٥ و٠٤٦ حوّلا حالاتِ التوقّف القديمة
**بلا تعبئة السبب**، فأمرٌ متوقّفٌ متأخّرٌ بلا عذرٍ مكتوب تعدّه اللوحةُ «متأخر
بدون عذر» بالأحمر **بينما صفحتُه صامتة**: التنبيهُ يُسقَط لأنه «متوقّف»،
وبطاقةُ التوقّف تُخفى لأنه «بلا سبب»، و«توقّف / مشكلة» يُخفى لأنه متوقّف — فلا
بابَ لعذره. **مُعادٌ إنتاجُه حيّاً قبل أيّ تعديل.** فصار الشرطُ واحداً للبطاقة
وللتنبيه (`heldExcuseOf`) — أحدُهما بالضبط لكلّ متوقّفٍ متأخّر — و«توقّف /
مشكلة» يظهر لكلّ أمرٍ حيٍّ لا عذرَ مكتوباً لتوقّفه (`holdButtonShown`)، والخادمُ
يقبل كتابةَ السبب على المتوقّف أصلاً. **بلا ترحيلٍ ولا حرفٍ في الخادم ولا في
القاعدة** — ولا يُعبَّأ سببٌ للأوامر الموروثة بأثرٍ رجعيّ.
وقبله **العذرُ المكتوب لا يضيع، ويُكتب في مكانٍ واحد** (§4.ao، آخرُ القسم): شكوى
المالك في أمر «وهج» — كتب الخبيرُ عذرَها فلم يظهر. **والسببُ من الشيفرة لا
تخمين**: التطبيقُ يقرأ العذرَ من «توقّف / مشكلة» وحدَه، **ثمّ يمحوه «إلغاء
التوقّف ومتابعة العمل»** (ويمحوه التقدّمُ كذلك) فيعود الأمرُ المتأخّر أحمر؛
ومعه موضعان يكتب فيهما الخبيرُ نصّاً لا يُقرأ عذراً (ملاحظاتٌ فنّية · سببُ
تغيير الموعد، ومثالُه داخل الحقل نصُّ عذر). وقرارُ المالك: «لا يضيع عذرٌ… أو
تُلزم الخبيرَ بمكانٍ واحد وتجعل التطبيقَ يقرأ منه فقط». **فالعذرُ يبقى** حتى
التسليم أو الإلغاء أو عذرٍ أحدث — لا يمحوه استئنافٌ ولا تقدّمٌ ولا تغييرُ موعد —
**والمكانُ واحد** تقوله الصفحةُ صراحةً: تنبيهٌ على كلّ متأخّر (بعذره أو يدلّ على
«توقّف / مشكلة»)، وجملةٌ بجانب الموضعين الآخرين أنهما ليسا عذراً. والأصفرُ
صار لعذرٍ **يعمل الآن** (متوقّفٌ أو متأخّر)، وعاملٌ في موعده بعذرٍ باقٍ بلا لون.
**بلا ترحيلٍ ولا حرفٍ في القاعدة** — وما مُحي قبل النشر لم يُسترجَع (**سكربتا
استرجاعه من الخطّ الزمني جاهزان** — أوّلُ هذا القسم).
وقبله **تصحيحُ مراجعة Codex على ٤٠١ — دليلُ لوحة التصنيع الذي يقرؤه المساعدُ يقول
الانقسامَ نفسَه** (§4.ao، آخرُ القسم): كان ما زال يصف شريطاً واحداً «متأخرون»
ومربّعاً واحداً «متأخر»، فيشرح المساعدُ عدّاداً لم يعد موجوداً. صار يقول
القسمين والفارقَ بينهما (عذرٌ مكتوب) ولونَ الصفّ وأن الشريطَ يُضغَط فيُرشِّح،
**وحارسٌ يقرأ الأسماءَ من الشاشة نفسِها** (`BUCKET_DEFS` · `Manufacturing.tsx` ·
`rowToneOf`) فلا يعود الدليلُ يخالفها بصمت. **بلا ترحيلٍ ولا حرفٍ في الواجهة
ولا في الخادم** — المُعدَّلُ نصُّ الدليل واختبارُه.
وقبله **«متأخرون بدون عذر» و«متأخرون بعذر» — الأحمرُ لمن لا عذرَ له وحدَه، في
كلّ عدّاد** (§4.ao، آخرُ القسم): ضغط المالكُ «متأخرون ١٨» فخرجت بطاقاتٌ كهرمانيّة
كتب خبراؤها عذرَها. وقرارُه: «لا تحسبهم متأخرون، وإنما نقول عنهم متأخرون
بعذر، فلا يُعرَضون بالمتأخرون بدون عذر. **الأحمرُ فقط وفقط لمن متأخرٌ وليس
لديه عذر**». **والعدُّ القديم كان في أربعة مواضع لا واحد** — الشريط · مربّعُ
«متأخرة» · عمودُ الخبراء · سطرُ الفروع — فانقسم كلٌّ اثنين على **تعريفٍ
واحد** في `shared/manufacturing.ts` (`latenessOf`) يقرؤه الخادمُ والشاشةُ معاً،
**ومجموعُ الاثنين هو الرقمُ القديم بالضبط** فلا صفَّ يضيع. وشريطٌ كهرمانيٌّ
جديد «متأخرون بعذر»، والشارةُ تقول الكلمتين. **بلا ترحيلٍ ولا حرفٍ في
القاعدة**؛ و`getOverview` صار يقرأ العذرَ ويُرسل `overdueExcused` بجوار
`overdue` (وهذا صار بدون عذرٍ وحدَه).
وقبله **تصحيحُ قياسٍ — لا عطبَ في خلفيّة الكهرمانيّ، في الشاشتين** (§4.ao،
آخرُ القسم): أبلغتُ في الطلب ٣٩٩ أن خلفيّةَ «متأخّرٌ بعذر» تخسر أمام `bg-card`
فتخرج بيضاء — في لوحة التصنيع **وفي شاشة التنبيهات** — ووضعتُ علامةَ أولويةٍ
على خلفيّات اللوحة. **ولم يكن ذلك صحيحاً**: `Card` تمرّر صنفَها عبر `cn`
(`twMerge`) فتُسقط `bg-card` حين يصلها لونٌ آخر، فلا يجتمع الصنفان على العنصر
أصلاً. والقياسُ الخاطئ جرى على **صفحةٍ كُتبت باليد** جمعتهما متخطّيةً `cn`.
**مقيسٌ الآن على الصفحتين الحقيقيتين** في متصفّح: الألوانُ كلُّها ممتلئة
و`bg-card` غائبٌ عن كلّ بطاقةٍ ملوّنة. **فلم تُمَسّ شاشةُ التنبيهات** (وصورةُ
المالك نفسُها — المستطيلُ الأصفرُ الكامل — كانت منها)، **وأُزيلت العلامةُ
الزائدة من اللوحة** فصارت الشاشتان على سلسلةٍ واحدة حرفاً بحرف، **والحارسُ
صار يختبر الآليّةَ الحقيقيّة**: `cn` الحقيقيّة على قاعدة `Card` الحقيقيّة.
**بلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة ولا في `Notifications.tsx`.**
وقبله **صفُّ لوحة التصنيع يقول حالتَه بلونه ويعرض عذرَه كاملاً** (§4.ao، تكملتُه):
كان اللونُ حاشيةً حمراء رفيعة للمتأخّر وحده، **فلا يفرّق بين متأخّرٍ كتب
الخبيرُ عذرَه ومتأخّرٍ لم يكتب** — والأوّلُ معذورٌ والثاني هو الذي يُسأل عنه.
فصار الصفُّ **بطاقةً ممتلئة**: **أحمرُ** لمتأخّرٍ **بلا عذرٍ مكتوب** ·
**أصفرُ** لمن له عذر (متأخّراً كان أو في موعده — «أو أيّ عذر») · **أخضرُ**
للمكتمل. **والعذرُ واقعةٌ في القاعدة لا استنتاج**: `holdReasonCode` إلزاميٌّ
على كلّ توقّف، يرسله الخادمُ منذ ٢٠٢٦-٠٨-٣١ **وكانت الشاشةُ تُسقطه**. ومعه
**السببُ كاملاً** في الصفّ («سببُ التأخير: تأخّر وصول مكوّن — المفصل من
تركيا»)، **والاسمُ سطرٌ قائمٌ بذاته** لا ينضغط بين الشارات (٢٢٠ ⟶ ٢٧٤ بكسل،
مقيسٌ بمتصفّحٍ حقيقيّ). **بلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة.**
وقبله **شرائطُ لوحة التصنيع تُضغَط فتُرشِّح** (§4.ao): تسعةُ تصنيفاتٍ كانت **تعدّ
ولا تُضغَط** — يقرأ المالكُ «متأخرون ٢١» ولا يجد باباً يفتحهم، فيقلّب الصفحةَ
بيده. صارت **أزراراً ملوّنةً بمعنى حالتها**، **تتركّب فوق مرشِّحات الخادم**:
«عناد» ثمّ «متأخرون» ⟶ متأخرو عناد وحدهم — بلا معامِلٍ جديد في الخادم، لأن
نقطةَ الأوامر بلا حدٍّ فالقائمةُ التي بين يدي الشاشة هي المجموعةُ الكاملة بعد
الترشيح. **والعددُ المعروضُ هو عددُ ما يظهر بالضبط** (مصدرٌ واحد). والقرارُ
في دالّةٍ خالصة تُختبَر دخلاً وخرجاً. **بلا ترحيلٍ ولا حرفٍ في الخادم ولا في
القاعدة.**
وقبله **مراجعةُ ٣٩٦ — اسمُ المُنادى لا تُقتطَع منه ناقلة** (§4.an، آخرُ القسم):
بقعةٌ سادسة في الحارس، وهي **انحدارُ ٣٩٦ نفسِه** وأثرُها **إفلاتٌ** يُعيد
الحلقةَ صامتة. الناقلةُ الاختياريةُ المنفردة كانت تبتلع أوّلَ اسم المُنادى حين
لا يسبقه فاصل: `queryFn:()=>setTimeout(saveThing)` تُقرأ ناقلتُها `setTimeou`
ومُنادَاها `t` — **والشكلُ يكتبه كلُّ مُنسِّقٍ يضغط المسافات**. **والعطبُ صنفٌ
لا حالة**: المساعِدُ المحلّيُّ `runIt(saveThing)` يفلت بالعلّة عينها (**وُجد
بالفحص لا ببلاغ**)، ومقيسٌ أن ماسحَ ما قبل ٣٩٦ يُمسك الشكلين. **والاقتراحُ
الحرفيُّ مرفوضٌ بالقياس**: ربطُ الناقلة بالنقطة في مجموعةٍ واحدة يُصلح الشكلين
**ويكسر السلسلة** (`‎.then` و`rows.filter(x).map` ناقلتُهما قوسٌ لا اسم، فتُقرأ
الطريقةُ نداءً مجرَّداً فيفلت ردُّها) — إسقاطُه الحيُّ **خمسةُ بنود** منها شكلُ
٣٩٢ بعينه. فصار **الاسمُ يُقرأ أوّلاً بتعبير ما قبل ٣٩٦ بحرفه**، والناقلةُ
**خطوةً ثانية بعد النقطة** فلا تُقتطَع من الاسم ولا تُقرأ في نداءٍ مجرَّد.
**وحدُّه مقالٌ لا مخبوء**: الجسمُ بأقواس يفلت قبل وبعد سواء (قرارُ ٣٩٠ المقيس).
**والتغطيةُ على المستودع لم تتغيّر بحرف**. **بلا ترحيلٍ ولا حرفٍ في شيفرة
التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٩٥ — الموضعُ جزءٌ من العقد، والساكنةُ بناقلتها** (§4.an، آخرُ
القسم): بقعتان في الحارس، كلتاهما **اتّهامٌ باطل** لا إفلات — وهو الأخطر،
فحارسٌ يُسقط الحزمةَ بلا مخالفةٍ يُعطَّل فلا يحرس شيئاً. **والموضعُ كان
مُهمَلاً**: كلُّ وسيطٍ مجرَّدٍ لطريقةٍ في المجموعة يُقرأ رَدَّ نداءٍ مهما كان
موضعُه — و**كلُّ** طريقةٍ معياريةٍ فيها موضعُ ردٍّ معلوم وما عداه بيانات، فكانت
`rows.map(pick, holder)` و`rows.reduce(combine, seedFns)` تُتَّهمان بكتابةِ
`thisArg` وقيمةٍ ابتدائية لا تُنفَّذان (مقيسٌ حيّاً، **وهاتان وُجدتا بالفحص
لا ببلاغ**). **و`groupBy` ليست في `Array.prototype` إطلاقاً**: بل
`Object.groupBy(items, cb)` و`Map.groupBy(items, cb)` — **ساكنتان ردُّهما في
الموضع الثاني والأوّلُ بيانات** — ففِكستشرُ ٣٩٤ كان يختبر طريقةً لا وجودَ لها.
فصارت المجموعةُ **خريطةَ مواضع** (و`‎.then` وحدَها تنادي موضعين)، والساكنةُ
**بناقلتها** لأن اسمَ الطريقة وحدَه ليس عقداً. **وبالناقلة زال سببُ استثناء
`Array.from`** فدخلت، **والمُلتبِسُ يبقى خارجاً بشرطٍ مُعلَن**: موضعٌ يقبل غيرَ
الدالّة (`JSON.stringify` · `String.replace`) لا يُلاحَق. **والتغطيةُ على
المستودع لم تتغيّر بحرف**، وتصحيحاتُ ٣٩١–٣٩٤ تبقى بحرفها. **بلا ترحيلٍ ولا حرفٍ
في شيفرة التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٩٤ — المعامِلُ الاختياريّ، والصنفُ المغلق** (§4.an):
بقعتان في الحارس، كلتاهما **إفلاتٌ** لا اتّهام. **و`?` علامةُ اختيارٍ لا فاصلَ
اسم**: `const runIt = (f?: …) => { if (f) return f(); }` كان يُقرأ معامِلُه
**فراغاً**، فلا يُطابَق الوسيطُ في موضعه ولا يُلاحَق ما يكتبه — **والفارقُ عن
الشكل المُمسَك محرفٌ واحد** (مقيسٌ حيّاً). **ونقصانُ طريقةٍ من الصنف مخرجٌ
صامت**: `findLast` و`findLastIndex` تنادِيان وسيطَهما بحكم عقدهما **وكانتا
تُمسَكان قبل ٣٩٣**، فأسقطهما تضييقُه بلا قصد. فأُكملت المجموعةُ على **الصنف
كلِّه** (ومعهما `toSorted` ومؤقّتان). **وتضييقا ٣٩٣ يبقيان بحرفهما** (الموضعُ
يحكم، والمُخزَّنُ صامت)، **والتغطيةُ على المستودع لم تتغيّر بحرف**. **بلا
ترحيلٍ ولا حرفٍ في شيفرة التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٩٣ — الوسيطُ المجرَّد ليس رَدَّ نداءٍ بذاته** (§4.an):
توسيعُ ٣٩٢ إلى الوسيط المجرَّد كان صحيحاً في مبدئه وواسعاً في تطبيقه — قرأ
**كلَّ** وسيطٍ مجرَّدٍ نداءً، **ومَن يستقبله قد يخزّنه أو يقارنه أو يُرجعه ولا
يناديه**: `queryFn: () => cacheValue(saveThing)` كان يُقرأ كتابةً والاستعلامُ
لا يكتب شيئاً — **اتّهامٌ باطل، وهو صنفُ عطب ٣٩١ نفسُه** (مقيسٌ حيّاً). فصار
**دليلُ نداءٍ لا احتمالُه**: طريقةٌ تنادي بحكم عقدها (`‎.then` وأخواتُها)، **أو
تعريفٌ في الملفّ يُثبت أن معامِلَه في ذلك الموضع يُنادى** — والمطابقةُ بموضع
الوسيط لا بوجوده. وما لا يُعرَف (طريقةٌ ليست منها · دالّةٌ مستورَدة · معامِلٌ
لا يُنادى) **لا يُلاحَق**. **ورأسُ السهم صار يوازن الأقواس**: `\([^)]*\)` كانت
تبتر قائمةَ معامِلاتٍ تحمل نوعاً بأقواس فيُقرأ جسمُ المساعِد خطأً — شرطُ صحّةٍ
للقاعدة لا تحسين. **والتغطيةُ على المستودع لم تتغيّر بحرف**، وتصحيحا ٣٩١ و٣٩٢
يبقيان بحرفهما. **بلا ترحيلٍ ولا حرفٍ في شيفرة التطبيق** — المُعدَّلُ ملفُّ
الاختبار وحده.
وقبله **مراجعةُ ٣٩٢ — الوسيطُ يُنادِيه مُستقبِلُه** (§4.an): تضييقُ ٣٩١
إلى «ما يُنادى» كان صحيحاً في مبدئه وضيّقاً في تطبيقه — قصرَ الملاحقةَ على
**رأس النداء** وحدَه، فـ`queryFn: () => Promise.resolve().then(saveThing)`
يُرجع `["resolve","then"]` ولا يُحَلّ اسمُ ما يكتب: **صفرُ مخالفات والحلقةُ
تعود صامتة** (مقيسٌ حيّاً). **والوسيطُ المجرَّد موضعُ استدعاءٍ أيضاً** — مَن
يستقبله يناديه — فصارت **ثلاثةَ مواضع**: رأسُ النداء · والوسيطُ المجرَّد ·
والقيمةُ التي هي اسمٌ وحدَه. **وتصحيحا ٣٩١ يبقيان بحرفهما**: الوسيطُ يُقرأ
على مستوى قائمته، فكائنُ الإعداد يبقى خاملاً. **والقوسُ الذي لا يسبقه نداءٌ
ليس قائمةَ وسائط** — شرطُ صحّةٍ مقيس: بلاه يُحَلّ **معامِلُ سهمٍ** إلى مساعِدٍ
يشاركه الاسمَ فيُتَّهم بريء. **والتغطيةُ على المستودع لم تتغيّر بحرف.**
**بلا ترحيلٍ ولا حرفٍ في شيفرة التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٩١ — الحارسُ كان يتّهم بريئاً** (§4.an، آخرُ القسم): البقعتان
السابقتان كانتا تُفلتان مخالفاً، وهذه في الاتّجاه المعاكس — ملاحقةُ الأسماء
تَحُلّ **كلَّ اسمٍ يُذكَر** في تعبير الاستعلام لا ما يُنادى منه، فاسمٌ يُمرَّر
**قيمةً** (`() => ({ saveThing })` أو `{ onDone: saveThing }`) يُقرأ كتابةً
يفعلها الاستعلامُ وهو لا يفعلها. **وكشف الفحصُ عيباً ثانياً**: قيمةُ التعريف
لا تقف عند فاصلتها المنقوطة فتبتلع أسطرَ جيرانها، **فيُتَّهم استعلامٌ مستثنىً
حقيقيّ بعنوان دالّةٍ مجاورة** (مقيسٌ حيّاً). **والاتّهامُ الباطلُ أخطرُ من
الإفلات**: حارسٌ يُسقط الحزمةَ بلا مخالفةٍ يُعطَّل فلا يحرس شيئاً. فصارت
الملاحقةُ **لما يُنادى وحدَه** (والاسمُ المجرَّدُ في موضع `queryFn` مُنادىً
بحكم موضعه فيُستثنى)، والتعبيرُ يقف عند `;`. **وكلاهما تضييقٌ لا توسيع**
والتغطيةُ على المستودع لم تتغيّر بحرف. **بلا ترحيلٍ ولا حرفٍ في شيفرة
التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٩٠ — المساعِدُ كان يُقرأ من أوّلِ قوسٍ بعده** (§4.an):
الحارسُ الذي يمنع عودةَ حلقةِ `POST` كان يَحُلّ اسمَ الدالّة المساعِدة ثمّ
يأخذ **أوّلَ `{` بعد اسمها** — فمساعِدٌ **بسهمٍ بلا أقواس**
(`const h = (id) => apiRequest("POST", …, { id })`، أشيعُ أشكاله) يجعله يقرأ
**كائنَ الحمولة** `{ id }` بدل مُهيّئه: **لا فعلَ كتابةٍ يُمسَح ولا عنوان**.
فصارت **قاعدةٌ واحدة تقرأ القيمةَ في الطرفين** — جسمٌ بأقواس أو تعبير —
ومعها جسمُ `function` **بعد قائمة معامِلاتها** ونوعٌ مكتوبٌ لا تُقرأ `=>`
فيه إسناداً، وسلسلةُ التعابير تُلاحَق. **وحدُّه مقيس**: ملاحقةُ الأجسام
بأقواس أيضاً أنتجت **٩٢ اتّهاماً باطلاً** في مستودعٍ نظيف فتُركت.
**بلا ترحيلٍ ولا حرفٍ في شيفرة التطبيق** — المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٨٩ — الحارسُ المعماريُّ كان له بقعةٌ عمياء** (§4.an، آخرُ القسم):
الفحصُ الذي يمنع عودةَ حلقةِ `POST` كان يقرّر «أهذا سهمٌ بجسم؟» بوجود `=>` في
الجوار ثمّ يأخذ **أوّلَ `{` بعده** — فسهمٌ **بتعبيرٍ بلا أقواس**
(`queryFn: () => fetchX(id)`، وهو شكلٌ قائمٌ في المستودع) يجعله يقرأ كائناً لا
علاقةَ له بالاستعلام **فلا يُحَلّ اسمُ الدالّة أبداً**، ولو كتبت تلك الدالّةُ
بـ`POST` يوماً لمرّت وعادت الحلقة. **وبندُ «لا مخالفةَ في المستودع» يبقى
أخضرَ والماسحُ أعمى**. فصار الرأسُ يُعرَف من **أوّل القيمة**، ويُقرأ التعبيرُ
نفسُه وتُحَلّ أسماؤه، **وستّةُ بنودٍ تعرض عليه الأشكالَ صراحةً** فلا يُقاس
على مستودعٍ نظيفٍ اليوم. **بلا ترحيلٍ ولا حرفٍ في شيفرة التطبيق** —
المُعدَّلُ ملفُّ الاختبار وحده.
وقبله **مراجعةُ ٣٨٧ — القراءةُ بـ`POST` كانت حلقةً لا تنتهي** (§4.an، آخرُ القسم):
نقطتان تُنادَيان بـ`POST` وهما **قراءةٌ محضة** وتُستعملان `queryFn` لاستعلامٍ
فعّال (تلميحاتُ نموذج المصروف · وأثرُ التصحيح الإداريّ قبل وقوعه) — فالتحديثُ
يُبطلهما فيُعاد جلبُهما فيُنادَيان فيجدولان تحديثاً… **بلا نهاية كلَّ ٥٠ مللي
ثانية**، وفتحُ نافذة المصروف وحدَه يُطلقها. مُعادٌ إنتاجُه حيّاً: ٢٤ طلباً في
١٢٠٠ مللي ثانية. فاستُثنيتا بعد فحصهما في الخادم، **والقائمةُ محروسةٌ بفحصٍ
معماريّ** يمسح مصادرَ الواجهة فيُفشل الحزمةَ عند أوّل نقطةٍ جديدة — فلا تعود
صامتة. ومعها **التركيزُ صار `"always"`**: `true` تُعيد جلبَ القديم وحده
و`staleTime` دقيقة، فعودتُك خلالها لا تُظهر عملَ زميلك. **بلا ترحيل.**
وقبله **التحديثُ الحيّ — قانونٌ واحد بدل ٢٣٤ قائمة** (§4.an): كلُّ كتابةٍ
كانت تحمل **قائمةَ مفاتيحَ مكتوبةً بيد مَن كتبها**، ومَن نسي مفتاحاً بقيت
شاشتُه قديمة حتى يُحدَّث المتصفّحُ يدوياً — عطبٌ مسجَّلٌ في هذا الملفّ **ثلاث
مرّات** (كلفةٌ لم تظهر فأُدخلت الخدمةُ مرّتين · مريضٌ محذوفٌ بقي ظاهراً · مفتاحُ
٣٨٤)، وثلاثتُها أُصلحت **بقائمةٍ رابعة**. فصار الحارسُ عند **`fetch` نفسِه**
— نقطةِ الخنق التي تنتهي إليها أشكالُ الكتابة الثلاثة — فأيُّ كتابةٍ تنجح
تُحدِّث كلَّ شاشةٍ مفتوحة، وتعودُ إلى النافذة فترى عملَ زملائك. **بلا ترحيلٍ
ولا حرفٍ في الخادم.**
وقبله **التصحيحُ الإداريُّ لا يسحب مالَ جهازٍ لم يُلغَ** (§4.am): مريضٌ
بجهازين على خيطٍ واحد، خفّض المسؤولُ دفاترَه يدوياً لأن أحدهما مكرَّر — فصار
حدُّ العكس (وهو مجموعُ المريض وكلفةُ خيطه) يقرأ مالَ **الجهاز الباقي** كأنّه
مالُ الملغى فيعكسه كلَّه: المجموعُ صفرٌ، والمريضُ يقرأ رصيداً بمقدار ما دفع،
والجهازُ الحيُّ قيد التصنيع بلا كلفة. فصارت **أرضيّةٌ ثالثة**: لا تنزل
الدفاتر تحت **ما قيّده كلُّ جهازٍ باقٍ في `cost_entries`** — أثرٌ مكتوبٌ لا
سعرٌ على الحلقة (فالحلقاتُ التاريخية تحمل أسعاراً لم تُقيَّد قطّ، ٠٥٠/٠٦٣).
**بلا ترحيلٍ ولا حرفٍ في القاعدة.**
وقبله **مراجعةٌ آلية على ٣٨٤ — خمسُ ملاحظاتٍ ثبتت كلُّها** (§4.al):
الذاكرةُ المخبَّأة كانت **تُبقي مبلغَ الجهاز معروضاً بعد سحب `canViewPayments`**
— مفتاحُ الأوامر يحمل رقمَ المريض في **جسمه**، فلا بادئةَ تصيبه ولم يكن في
قائمة `App.tsx`. فصار الإبطالُ **بمُسنِد** (إبطالٌ لا نزع، ومصوَّبٌ لا كاسح).
وسكربتُ التراجع صار **يقف عند ما لا يحمله التدقيق** بدل أن يُصفّر وقتَ
إغلاقٍ أو سببَ إلغاءٍ سابقين، ويحذف شهادةَ المعاينة **بعلامة التدقيق** لا بنصّ
السبب، **ويَنسب الفعلَ لمن نفّذه** لا لمن أوقع الإلغاء. **بلا ترحيلٍ ولا حرفٍ
في القاعدة.**
وقبله **ملفُّ المريض يقول أيُّ عمليةٍ وأيُّ مال** (§4.al): بطاقتا تصنيعٍ بنفس
النوع والخبير كانتا لا تُميَّزان، وجدولُ الدفعات لا يقول لأيّ جهازٍ كلُّ
صفّ، ونافذةُ التصحيح تعرض الكلفةَ ولا تعرض المدفوع — **وهو الرقمُ الذي
يقرّر سؤالَ إرجاع المبلغ**. فصارت البطاقةُ تحمل ترتيبَ الجهاز ورقمَ الأمر
ومالَه، والدفعةُ تقول جهازَها، والنافذةُ تقول المدفوعَ قبل الاختيار.
**عرضٌ فقط — بلا ترحيلٍ ولا حرفٍ في أيّ حساب.**
وقبله **طلبُ الدمج ٣٨٣** — **إرشادُ رسالة التكرار محايدٌ** (§4.ab، آخرُ القسم):
مراجعةٌ آلية على ٣٨٢ أمسكت أن «راجع المسؤول لإتاحة ملفّه لفرعك» خاطئةٌ في
أشيع حالاتها — المريضُ في **فرع السائل نفسِه** غالباً، فلا إتاحةَ تلزم؛ وكذلك
ملفٌّ أُتيح سلفاً، وكذلك المسؤولُ نفسُه. فصارت «ابحث عنه في سجل المرضى، وإن
لم يظهر لك فراجع المسؤول». **والشرطُ المانعُ لم يتغيّر بحرف، وبلا ترحيل.**
وقبله **طلبُ الدمج ٣٨٢** — **رسالةُ تكرار الاسم تقول الحقيقةَ بدل أن تدلّ على
التكرار** (§4.ab، آخرُ القسم): موظّفةُ الموصل حُجبت عن تسجيل مريضةٍ عائدةٍ من
بغداد برسالةٍ تأمرها بـ«إكمال كتابة الاسم» — **والاسمُ الأطول مقبولٌ بالقاعدة**
— فالرسالةُ كانت تدلّ على التكرار بدل أن تمنعه. **والمنعُ لم يتغيّر بحرف**؛
المطابقةُ التامّة صارت تقول «مسجَّل مسبقاً في فرع كذا»، والبادئةُ تبقى على
رسالتها. (وإرشادُها عُدِّل في ٣٨٣ أعلاه.) **بلا ترحيلٍ ولا حرفٍ في الواجهة.**
وقبله **طلبُ الدمج ٣٨١** — **ترويساتُ الجسم لا تُورَّث في نداء القدرة** (§4.ak،
آخرُ القسم): أوّلُ اختبارٍ على الإنتاج ردّ «تأخّر الخادمُ في الاستجابة» —
والشاشةُ وُجدت والفهرسُ عمل، والنداءُ وحده انهار. الطلبُ الصناعيُّ كان يرث
`content-type` و`content-length` من طلب المساعد، فيسلّمه `express.json()`
إلى قارئ الجسم فينهار على كائنٍ ليس مجرى قراءة. **بلا ترحيل.**
وقبله **طلبُ الدمج ٣٨٠** — **قدراتُ القراءة: المساعدُ يسأل التطبيقَ بجلسة
السائل** (§4.ak): كان يرى ثماني نوافذ من أصل ١٣٢ نقطةَ قراءة، فيعتذر عن كلّ
سؤالٍ خارجها ولو كان جوابُه محسوباً في القاعدة (شكوى المالك: «أيُّ خبيرٍ
أكثرُ متأخّرين بالأحمر؟»). فصار الفهرسُ **يُشتَقّ من جدول المسارات** وتُنفَّذ
النقطةُ نفسُها بجلسة السائل — فالشمولُ والحدُّ من آليةٍ واحدة، **وبلا سطرِ
صلاحيةٍ جديد**. **بلا ترحيل.**
وقبله **طلبُ الدمج ٣٧٩** — **إكمالُ إتاحة الفرع: صفحةُ أمر التصنيع ومراجعةُ
الحركة** (§4.t، آخرُ القسم): ٣٧٨ وسّع **القائمة** ولم يوسّع **صفحةَ الأمر**،
فكان مديرُ الفرع المضاف يرى الصفَّ ويُردّ ٤٠٣ على الضغطة (شكوى المالك بعد
النشر). ومعه طابورُ «مراجعة حركة مرضى الأطراف والمساند» وتاريخُ طلباتها في
ملفّ المريض صارا يتبعان الإتاحة، ولكلّ صفٍّ اسمُ فرعه. **بلا ترحيل.**
وقبله **طلبُ الدمج ٣٧٨** — **لوحةُ التصنيع تعرض المريضَ المُتاحَ لفرعها**: كان
الترشيحُ بفرع الأمر وحده، فمريضٌ أُتيح ملفُّه لفرعٍ آخر لا تظهر عمليتُه في
لوحته. **بلا ترحيل.** وقبله **طلبُ الدمج ٣٧٧** — **سطرُ حالة واتساب عند الإقلاع** (§4.c، القسمُ الأخير
فيه): كان يُطبَع نصّان مكتوبان باليد لا يسمّيان الناقص، ودالّةُ السطر
المكتوبة لذلك ميّتةٌ بلا مُنادٍ. **بلا ترحيلٍ ولا حرفٍ في القاعدة**. وقبله
`dfe105a` — **§4.ai** (الجلسةُ المُهداة تُحتسب والمالُ لا يتحرّك، **ترحيل
٠٨٧**) و**§4.aj** (تحريرُ كلفة الحالة على الهاتف).

**وآخرُ ترحيل: ٠٨٧** — فالتالي **٠٨٨**، ولا يُشغَّل ما لم يُسجَّل في
`server/migrations/runner.ts` (استيرادٌ **ومصفوفة** `migrations`، الاثنان معاً).

### ما هو مفتوحٌ الآن
لا فرعَ عملٍ قائم، ولا PR مفتوح، ولا مهمّةً نصفَ منجَزة.

**زهراء وأيوب — حسمها المالكُ بنفسه** (٢٠٢٦-٠٩-٢٥): الاستعلامُ أثبت أن حسابَ أيوب
بلا ذي قار (`branch_ids` = بغداد، الموصل، كركوك) وحسابَ عناد بلا الموصل، فأعاد المالكُ
فروعَهما وحوّل أمرَ زهراء من زياد إلى أيوب. **والمالكُ متأكّدٌ أنه لم يُزِل تلك الفروع،
والسببُ لم يُثبَت**: الكاتبُ الوحيد لـ`branch_ids` نافذةُ تعديل المستخدم
(`PATCH /api/admin/users/:id`، للمسؤول وحده) **ولا تكتب سطرَ تدقيق**، فلا أثرَ يقول
مَن ومتى (آخرُ حفظٍ لأيوب ٢٠٢٦-٠٧-١٦ ٠٦:٠٢، ولعناد ٢٠٢٦-٠٨-٢٢ ١٦:٣٦). وتدقيقُها لم
يُطلَب.

**الأعذارُ الممحوّة قبل ٤٠٣ — السكربتان جاهزان وينتظران المالك** (§4.ao، آخرُ القسم):
يلصق `restore_erased_excuses_preview.sql` في Neon (قراءةٌ فقط) ويراجع القائمة، ثمّ
`restore_erased_excuses_apply.sql` بأرقام ما يستبعده في سطره المعلَّم. **لم تُشغَّل الكتابةُ
على الإنتاج بعد**، ولا يُشغّلها المساعدُ أبداً. **ونصُّهما يُسلَّم له داخل صندوق شيفرةٍ في
المحادثة، لا ملفّاً مرفقاً** (§8).

**ومُلاحَظٌ لم يُصلَح** (٢٠٢٦-٠٩-٢٥): مربّعاتُ لوحة التصنيع (`getOverview`) تعدّ أوامرَ مرضى
السلّة، والشرائطُ والقائمةُ والتنبيهاتُ لا تعدّها — فقد يزيد المربّعُ على الشريط بعددهم.
سابقٌ، ولا يُصلَح بلا طلب.

**واعتمده المالكُ ولم يُبدأ بعد** — ردُّه ٢٠٢٦-٠٩-٢٤ «نعمل بمقترحك» على الأسئلة
المعلَّقة. **والقراءةُ مكتوبةٌ له في المحادثة قبل التنفيذ**، فإن صحّحها
يُعمَل بتصحيحه:
- **أسبابُ الأوامر الموروثة**: لا تُعبَّأ (يكتبها الخبيرُ من «توقّف / مشكلة»).
- **شاشةُ التنبيهات**: تنقسم «متأخرة بدون عذر» و«متأخرة بعذر» كلوحة التصنيع.
- **عمودا «إعادة قالب/سوكت»** الفارغان دائماً: يحلّ محلَّهما عمودٌ واحد «إعادات
  العمل» من `reworks` المحسوب أصلاً.

**وقرارُ المالك ٢٠٢٦-٠٩-٢٣ — لا استطلاعَ دوريّ**: سُئل أيريد أن يظهر عملُ
زميله على شاشته وهو واقفٌ أمامها بلا أن يلمس شيئاً، فأجاب أنّ ذلك يعجبه
**لكن إن أثقل النظامَ فلا**. فبقي التحديثُ عند الكتابة وعند العودة إلى
النافذة (§4.an)، **ولا `refetchInterval` ولا ويب سوكِت** — ولا يُضاف بلا
طلبٍ صريح.

### وينتظر المالكَ لا المساعد — واتساب المرضى
الشيفرةُ كاملةٌ ومدموجة منذ ٢٠٢٦-٠٨-٢٢، **ولم تُرسَل رسالةٌ واحدة قطّ**:
بيئةُ Render لا تحمل أيّاً من متغيّرات `PATIENT_WHATSAPP_*`. والقالبان
معتمَدان في Meta (`bionic_patient_welcome` · `bionic_patient_update`،
والثاني بمعامِلين — مطابقٌ لعقد الشيفرة)، والمتبقّي خطوةُ `Create app`
لأخذ `Phone Number ID` والتوكن الدائم. **التفصيلُ كلُّه في §4.c**، ومنه
الأربعةُ التي تُضبَط في حفظةٍ واحدة. **ولا يُطلَب التوكنُ في محادثةٍ
أبداً** — يُلصَق في Render مباشرةً.

### المؤجَّلُ بقرار المالك — لا يُبدَأ بلا طلبٍ صريح منه
- **`PUT /api/patients/:id` لا تكتب سطرَ `audit_log` واحداً**: تغييرُ كلفة
  مريضٍ لا يخلّف أثراً يقول مَن ومتى. قرارُ المالك ٢٠٢٦-٠٩-٢١: «لا حاجة…
  لا أحد يعدّلها إلّا أنا المدير المسؤول».
- **المراحلُ ٤–١٠ من قطار إصلاح تدقيق ٢٠٢٦-٠٩-١٢**: لم تُبدأ. ومنها **INT-05**
  الموصوفُ بتفصيله في آخر **§4.q** — انقلابُ ترتيب القفل بين إلغاء المعاينة
  وإتمام البيع. أثرُه **ردٌّ خاطئ وخروجُ عملية، لا فسادَ بيانات** (مقيسٌ
  حيّاً)، وهو **سابقٌ** لتلك المرحلة لا ناتجٌ عنها.

### ما أُنجز ولا يُعاد فتحه
**الملفُّ المكرَّر WB-01686** — دُمج في **WB-01688** (انتصار حبيب محمد) يوم
٢٠٢٦-٠٩-٢١ بنجاح: الثابتان المحاسبيّان متطابقان (١,٨٥٠,٠٠٠ = مساند
١,٧٥٠,٠٠٠ + علاج طبيعي ١٠٠,٠٠٠)، والرمزُ القديم محفوظٌ اسماً بديلاً،
وعدّادُ جلساتها ٨ كما كان، **ولا شارةَ انتظارٍ ظهرت** (حلقتُها `delivered`
فالخيطُ مستقرٌّ بحكم حارس `noEpisodes`). **لا عملَ باقٍ عليه.**

**والمتراكمُ الأوّل لواتساب** — ٨٤٦ صفّاً `pending` صارت `skipped` يوم
٢٠٢٦-٠٩-٢٢ بقرار المالك الصريح («نتخطّاهم ونبدأ بالجدد»): ٤٢١ ترحيباً
قديماً و٤٢٥ تحديثَ تصنيعٍ كانت ستصل خارجَ تسلسلها. `UPDATE 846` ثمّ
`بقي_معلقا = 0` ثمّ `COMMIT`، بصورةٍ طبق الأصل من `markSkipped` ولا صفَّ
`sent` ولا ديناراً مُسّ. **و`skipped` طرفيّةٌ لا تُحجَز ثانيةً** —
**ولا تُعاد هذه الصفوفُ إلى `pending` بحال**، فذاك يُطلق بالضبط ما قرّر
المالكُ تفاديه. (§4.c)

### خطُّ الأساس — قِسْ عليه قبل أن تدّعي كسراً
| الأمر | المتوقَّع |
|---|---|
| `npx tsc --noEmit` | **٣٥ خطأً معروفاً** — قارن بالعدد لا بالصفر |
| `npm run check:tests` | ٠ (وهو **لا** يُغطّيه `npm run check`) |
| `npm run build` | ينجح |

**وإخفاقاتُ الاختبار المسبَقة مسجَّلةٌ في آخر §4.ai** — كلُّها عيوبُ فِكستشر
لا سلوكِ إنتاج، ومُثبَتةٌ متطابقةً على `main`. **ولا تُصلَح بتخفيف اختبارٍ
ولا بتعطيله.**

**والمقارنةُ بـ`main` تكون بشجرة عملٍ حقيقية** — `git worktree add /tmp/mainwt main`
— **لا بـ`git stash`**: تلك تقارن بـ`HEAD` الذي يحمل التزاماتِك أنت، فتُريك
عطبَك «مسبَقاً» وهو منك. (وقع فعلاً وصُحِّح.)

**والقياسُ في المتصفّح يكون على المكوّن الحقيقيّ لا على صفحةٍ تُكتب باليد** —
ما يصل العنصرَ هو ما تُخرجه دوالُّ المكوّن (`Card` ⟵ `cn` ⟵ `twMerge`)،
وصفحةٌ تجمع الأصنافَ باليد تقيس شيئاً لا تُنتجه الشاشة. (وقع فعلاً وصُحِّح،
§4.ao.)

### وحارسُ فهرس القدرات
`npm run test:ai-capabilities` يُفشل الحزمةَ حين تُضاف **نقطةُ قراءةٍ جديدة**
لم يُقرَّر فيها: توصَف في `DESCRIBED` أو تُمنَع في `BLOCKED` بسببٍ مكتوب
(§4.ak). فنقطةٌ جديدة لا تنفتح صامتةً ولا تختفي صامتة.

### ولا CI في هذا الريبو
لا ملفَّ workflow واحد. المراجعةُ الآلية الوحيدة **Codex Code Review** —
بلغت حدَّ استعمالها يوم ٢٠٢٦-٠٩-٢١ ثمّ **عادت تعمل في ٢٠٢٦-٠٩-٢٢**
(راجعت الطلب ٣٨١ وخرجت بلا ملاحظة)، **ثمّ بلغته ثانيةً يوم ٢٠٢٦-٠٩-٢٥** فلم تراجع
الطلب ٤١٢. **فالتحقّقُ مسؤوليتُك
أنت**: اختبارٌ حيٌّ على Postgres محلّي على **النقاط الحقيقية**، ومعه
**مراجعةٌ عكسية لكلّ حارسٍ جديد** — أزِله فتسقط تأكيداتُه **وحدها**، ثمّ
أعِده. وحارسٌ لا يُسقط شيئاً بإزالته لا يحرس شيئاً.

### قاعدةُ الاختبار المحلّية
تذهب مع إعادة تشغيل الحاوية، وتُبنى من الصفر: أنشئ قاعدةً ⟵
`DATABASE_URL=… npm run db:push` ⟵ شغّل الخادمَ مرّةً ليُطبِّق الترحيلات ⟵
ثمّ استعملها **قالباً** (`CREATE DATABASE x TEMPLATE …`) لكلّ حزمة اختبار.
**ولا تُلمَس قاعدةُ الإنتاج من بيئة التطوير إطلاقاً** (القسم ٨).

**وعلى قاعدةٍ فارغة يتعثّر `db:push` مرّتين، والمخرجُ معروف** (٢٠٢٦-٠٩-٢٤):
(١) `function patient_search_norm(text) does not exist` — فهرسٌ تعبيريٌّ يسبق
الدالّة، فأنشئ الدالّتين (`patient_search_norm` و`patient_digits_only`) من SQL
الترحيل ٠٥٤ أوّلاً؛ (٢) `no unique constraint matching given keys for
referenced table "patient_device_episodes"` — المفاتيحُ المركّبة تسبق قيدَي
`uq_pde_id_patient` و`uq_patient_cases_id_patient`، **والإعادةُ لا تُصلحه**:
خذ جملَ `drizzle-kit push --force --verbose` وطبّق ما فيه `UNIQUE(` قبل
غيره. ثمّ `runMigrations()` مرّةً (سكربتٌ صغير يستورد
`server/migrations/runner.ts`) — فتُسجَّل الترحيلاتُ كلُّها إلّا
`backfill_all_cases_v4` الذي يكتبه إقلاعُ الخادم وحده ولا تحتاجه الاختبارات.

---

## 1. ما هو المشروع

نظام إدارة مراكز الأطراف الصناعية والعلاج الطبيعي (Bionic Centers Manager).
- المالك: Dr Yasir (yasir.s81@gmail.com)
- اللغة الأساسية للواجهة: العربية (RTL).
- المنطقة الزمنية: بغداد (UTC+3).
- المستخدمون الفعليون: موظفو استقبال، مدراء فروع، ومسؤول واحد (admin).

### الوحدات الأساسية
- **المرضى** (`patients`) — بيانات شخصية، نوع الإصابة، الفرع، إجمالي التكلفة.
- **الزيارات** (`visits`) — كل جلسة علاج. مرتبطة بمريض وفرع. تحوي
  `treatment_type`، `session_count`، `cost`، `shift`، `created_by`.
- **الدفعات** (`payments`) — مدفوعات المريض، مع
  `payment_treatment_type` و `session_count`.
- **الفواتير** (`invoices`) + `invoice_items` — للجانب المحاسبي.
- **خطط التقسيط** (`installment_plans`).
- **المستخدمون** (`system_users`) — حسابات الموظفين بأدوار:
  `reception` / `branch_manager` / `admin`. كل حساب يحمل أعلام صلاحية
  دقيقة (مثل `can_delete_visits`, `can_edit_visits`, `can_delete_patients`،
  وغيرها).
- **سجل التدقيق** (`audit_log`) — يستقبل أحداث (create / update / delete)
  مع `entity_type`، `entity_id`، `user_id`، `ip_address`، `old_values`،
  `new_values`. ليست كل النقاط النهائية تكتب فيه — راجع "ثغرات معروفة".
- **تتبّع الجلسات اليومية** (`daily_sessions`, `session_counts`,
  `monthly_targets`) — إحصاءات الأجهزة الـ15 في كل فرع/شفت/يوم.
- **الكشف عن الشذوذ** (`anomaly_decisions` + `server/anomalies/detector.ts`)
  — يكشف فواتير متأخّرة، مرضى بلا دفعات، إلخ. قراءة فقط.
- **النسخ الاحتياطي البريدي** (`server/backup.ts`) — كرون يومي 23:55
  بغداد يرسل
  Excel بكل البيانات لبريد المالك.
- **تنبيهات تلغرام** (`server/notifications/telegram.ts`) — رسالة فورية
  للمالك عند تسجيل كل مريض جديد (الاسم/الفرع/النوع/المسجِّل، بلا هاتف).
  التوكن و chat id في `system_settings` تُدار من لوحة المسؤول (بطاقة
  «تنبيهات تلغرام» + زر رسالة تجريبية يستخرج chat id تلقائياً من
  getUpdates). الإرسال «أطلق وانسَ»: فشله لا يعطّل تسجيل المريض أبداً.
  **وهذا شأنٌ داخليّ للإدارة لا علاقة له بتواصل المرضى** (القسم 4.c):
  تلغرامُ المرضى تقاعد، وهذه باقيةٌ كما هي.
- **عملياتُ «بلا معاينة»** (`server/pending_charges/`, ترحيل ٠٦٧) —
  الاستقبالُ يُنجز البيعَ أو الصيانةَ **ويقيّد مبلغَها معها بحفظٍ واحد**
  (٠٦٩/٠٧٠)، **وصفرُ سلطةٍ للطبيب**: لا معاينةَ يُوقّعها، ولا مراجعةَ
  إشرافيةً تصله عن أيٍّ منهما بعد اليوم (كانت تصله استرجاعياً، تقاعدت
  للصيانة في القسم 4.j ولبيع الجزء في القسم 4.k). وجدولُ
  `pending_service_charges` **تاريخٌ لا مسار**: لا صفَّ جديد يدخله — يبقى
  مقروءاً وقابلاً للحسم بمساره القائم وحده. التفصيل في الأقسام 4.e، 4.j، 4.k.
- **سلّة المرضى** (`server/patients/`, ترحيل ٠٦٨) — **الحذفُ العاديّ لم يعد
  يهدم شيئاً**: حالةُ حذفٍ على صفّ المريض ومهلةُ استعادةٍ ثلاثين يوماً، ولا
  صفَّ تابعاً يُمَسّ. والكاسكيدُ الهادمُ بابُه الوحيد «حذف نهائي» للمسؤول
  العام. التفصيل في القسم 4.g.
- **تواصل المريض** (`server/patient_whatsapp/`) — **واتساب حصراً**: حفظُ
  المريض يستحقّ رسالةَ ترحيبٍ على رقمه المسجَّل، وتحديثاتُ التصنيع تتبعها.
  التفصيل في القسم 4.c.

### حماية بيانات الزيارات (مهمّة بعد حادثة 2026-06-08)
- **حذف ناعم**: `visits.deleted_at`. كل القراءات تصفّي
  `IS NULL`. `storage.deleteVisit` يحدّث العمود بدل
  `DELETE FROM`، فالضغطة الخاطئة لا تمحو الصف.
- **جدول جنائي**: `visits_forensic_log` يستقبل صورة كاملة من كل صف
  يُحذف فعلياً من `visits` (الكاسكيد عند حذف مريض مثلاً) مع معلومات
  جلسة Postgres (current_user, application_name, client_addr).
- **ترِكر**: `trg_log_visit_delete BEFORE DELETE ON visits` يكتب في
  الجدول الجنائي مهما كان مصدر الحذف (تطبيق، SQL مباشر من Neon Console،
  أي شيء).
- **تسجيل تدقيق لحذف المريض**: `DELETE /api/patients/:id` يكتب الآن في
  `audit_log` قبل تنفيذ الكاسكيد. قبل اليوم كان الحذف صامتاً تماماً.

---

## 2. البنية التقنية

- **الخلفية**: Node 20 + Express + TypeScript + Drizzle ORM.
- **قاعدة البيانات**: PostgreSQL على Neon (production branch).
- **الواجهة**: React + Vite + TanStack Query + Radix UI + Tailwind.
- **النشر**: Render Web Service (auto-deploy على كل دمج إلى `main`).
- **التحقّق من المخطّط**: Drizzle Kit (`npm run db:push` للتطوير).
- **الـ migrations**: ملفات في `server/migrations/`، يشغّلها
  `server/migrations/runner.ts` عند بدء الخادم. كل migration **يجب**
  أن يكون idempotent (يستخدم `IF NOT EXISTS` ونحوه).
- **الجلسات**: `connect-pg-simple` على نفس قاعدة البيانات
  (`sessions` table).
- **المصادقة**: جلسة `branchSession` على `req.session` تحوي
  `userId`, `branchId`, `displayName`, `isAdmin`, `permissions`.

### بيئة عمل وحيدة
- Render خلف Cloudflare، لذلك `req.ip` يُظهر حافة Cloudflare لا IP
  المستخدم الحقيقي. (لم نفعّل قراءة `X-Forwarded-For` بعد).
- ريبلت سابقاً كان يستضيف المشروع، الآن **متوقّف** (يتطلّب تجديد).
  ملف `.replit` ومجلّد `server/replit_integrations/auth/` لا يزالان
  في الريبو لأن المصادقة بُنيت فوقهما؛ يمكن إعادة تسميتها لاحقاً.

---

## 3. أحداث جلسة 2026-06-08

في هذا اليوم تمّ دمج 4 PRs ومعالجة حادثة فقدان بيانات.

### الجدول الزمني للنشر (Baghdad)
| الوقت | الحدث |
|---|---|
| 13:26 | بدء نشر PR #72 |
| 13:28 | PR #72 live (audit + confirm delete) |
| 13:44 | بدء نشر PR #73 |
| 13:46 | PR #73 live (لا منح ضمني لمدراء الفروع) |
| 18:02 | بدء نشر PR #74 |
| 18:04 | PR #74 live (soft delete + forensic trigger) |

### PR #71 — Stop global error handler from crashing the process
**الخلفية**: معالج أخطاء express كان يعيد `throw err` بعد ردّ JSON،
ما يسقط العملية على كل خطأ في طلب — ومنه ضياع طلبات بشكل عشوائي
على الإنتاج.
**التغيير**: حذف `throw err`. الإجراء يبقى آمناً لأن JSON قد أُرسل
للعميل، وحدّ Sentry/تسجيل الأخطاء يكفي.

### PR #72 — Protect visits: confirm-to-delete + audit edit/delete
**الخلفية**: المريض حميد ذياب سبع (ID 1473) فقد زياراته بشكل صامت.
لا يوجد دليل من سجلّ التطبيق.
**التغيير**:
- نافذة تأكيد (`AlertDialog`) قبل حذف زيارة في الواجهة.
- استدعاء `logAudit` في كلٍّ من `PATCH /api/visits/:id` و
  `DELETE /api/visits/:id` مع `oldValues` + `newValues` + IP +
  user_agent.

### PR #73 — Stop auto-granting visit-delete to branch_manager role
**الخلفية**: منطق تسجيل الدخول كان يمنح `canDeleteVisits=true` لكل
من دوره `branch_manager` بغضّ النظر عن العمود في صفّه. الواجهة لا
تكشف هذا المنح الضمني.
**التغيير**: في `server/routes.ts` تحوّل
```ts
canDeleteVisits: grantAll || systemUser.canDeleteVisits
```
إلى
```ts
canDeleteVisits: Boolean(systemUser.canDeleteVisits)
```
بقي `canEditVisits` على المنح الضمني لأن التعديل قابل للاسترجاع.

### PR #74 — Protect visits with soft delete + Postgres-level forensic trigger
**الخلفية**: تحقيق ميداني كامل بالكود وبيانات Neon لم يجد مساراً
يفسّر فقدان زيارات حميد ذياب. مفصّل في القسم 4 أدناه.
**التغيير** (3 طبقات حماية مستقلّة):
1. `migration 011_visit_soft_delete_and_forensic.ts`:
   - عمود `deleted_at TIMESTAMPTZ` + partial indexes.
   - جدول `visits_forensic_log` بـ 16 عمود.
   - ترِكر `trg_log_visit_delete BEFORE DELETE` على visits.
2. الحذف الناعم في كل أنحاء التطبيق:
   - `storage.deleteVisit` يضع `deleted_at` بدل `DELETE`.
   - كل القراءات (`getVisitsByPatientId / ByPatientIds / ByBranch /
     getAllVisits` + raw SQL الإحصائيات + التقرير اليومي + كاشف
     الشذوذ + القراءات الإنلاين في handlers) تصفّي
     `deleted_at IS NULL`.
3. `DELETE /api/patients/:id` يستدعي `logAudit` قبل الكاسكيد.

---

## 4. حادثة فقدان زيارات حميد ذياب سبع

### الوقائع
- المريض: **حميد ذياب سبع** = ID **1473**.
- في صبيحة 2026-06-08 كان لديه 4 زيارات ظاهرة في الواجهة:
  - 03/06 الساعة 20:06 بغداد
  - 04/06 الساعة 13:53 بغداد
  - 07/06 الساعة 17:20 بغداد
  - 08/06 الساعة 12:08 بغداد (أنشأتها ريام)
- بعد ظهر 08/06، **اختفت الزيارات الأربع دفعة واحدة**. بقيت فقط
  زيارة id=7909 (الساعة 13:07 بغداد، أنشأتها ريام أيضاً، **بعد**
  حدث الاختفاء).
- ريام (user_id=7، role=reception، `can_delete_visits=true`)
  هي المستخدم الوحيد الذي دخل اليوم.
- المالك يؤكّد أن ريام لم تحذف ولا أحد دخل بصلاحية حذف عمداً.
- قبل الاختفاء، ريام لاحظت أن حميد ذياب لديه "ناقص 1 جلسة" — أي
  عدد زياراته أكبر من جلساته المدفوعة.
- **`audit_log` لا يحوي أي قيد حذف زيارة** قبل النشر أو بعده.
  (PR #72 الذي يضيف audit للحذف نُشر 13:28؛ الاختفاء وقع بعد ذلك
  وفقاً للمالك، ومع ذلك لا سجل).
- **دفعات حميد ذياب الخمس سليمة**. صفّه في `patients` سليم.

### ما استبعدته المراجعة الكاملة للكود
| المحتمل | الحكم |
|---|---|
| `DELETE /api/visits/:id` | يسجّل تدقيقاً منذ PR #72؛ لا سجلّات |
| `deletePatient(1473)` cascade | كان سيمسح الدفعات. سليمة. |
| FK CASCADE | كل FKs على visits = NO ACTION |
| Triggers قديمة | لا توجد (`information_schema.triggers` فارغ قبل PR #74) |
| حذف مستخدم نظام/فرع اليوم | لا |
| حذف مريض اليوم | لا (لا فجوات في patient_ids) |
| كاشف الشذوذ، sessions module، backup cron، treatment plans | كلها قراءة فقط أو لا تلمس visits |
| PATCH /api/visits/:id | يحدّث 6 حقول فقط، لا يحذف ولا يعيد إسناد |
| Frontend hooks / useEffect | لا حلقات حذف، لا تأثيرات تلقائية |
| ريبلت يعمل ويصل لنفس DB | متوقّف (متأكّد) |
| Neon Backup & Restore | لم تحدث عملية اليوم |
| استدعاء SQL مباشر | لم يحدث (متأكّد) |

### النتيجة المعلنة
**لا يمكنني تحديد السبب من الكود وحده**. النمط الذي رأيناه (4 زيارات
محدّدة محذوفة + 6 زيارات لمرضى آخرين أنشأتها ريام اليوم بـ IDs
7900-7906 ما عدا 7903، مع إبقاء صفّ المريض ودفعاته وبدون سجل تدقيق)
لا تنتجه أي شيفرة في الإنتاج اليوم. الحماية في PR #74 وُضعت تحديداً
لضمان أن أي تكرار سيخلّف أدلة قاطعة.

### ما يجب على المالك فعله
1. **إعادة إدخال زيارات حميد ذياب الأربع المفقودة** يدوياً من
   السجلات الورقية (المالك يحتفظ بها).
2. **مراقبة دورية**:
   ```sql
   SELECT * FROM visits_forensic_log ORDER BY logged_at DESC LIMIT 20;
   ```
   المعتاد أن يبقى الجدول فارغاً. أي صفّ فيه = إشارة فورية.

---

## 4.b معاينة الطبيب (migration 028)

**سجلّ سريري موقّع ومقفل.** يُبنى فوق الاختصاصات الثلاثة الموجودة أصلاً
في `patient_cases.case_type`: `prosthetic` (أطراف) · `medical_support`
(مساند) · `physiotherapy` (علاج طبيعي) — لا تصنيف موازٍ.

### الجداول
- `medical_exams` — المعاينة. `doctor_name` **لقطة** لا join، ليبقى
  السجل مقروءاً بعد تغيير اسم الحساب أو حذفه.
- `medical_exam_addenda` — الملاحق. التصحيح يُضاف ولا يمسح الأصل.
- `medical_exams_forensic_log` — لقطة كاملة لأي صفّ يُحذف فعلياً.

### مَن يكتب
مساران، بنفس تناظر خبير الأطراف تماماً:
- **دور `doctor`** — مهنته الطبّ. يحمل القدرة ضمناً كما يحمل
  `prosthetics_expert` قدرة الخبير.
- **قدرة `can_write_medical_exam`** — لمن دوره الأساسي شيء آخر
  (مدير فرع طبيب مثلاً).

وفي الحالتين `medical_specialties` (مصفوفة jsonb) **إلزامية**: كلمة
«طبيب» تحدّد المهنة لا القسم. الخادم والواجهة يرفضان حفظ حساب بدور
`doctor` بلا اختصاص واحد على الأقلّ.

**لا تُمنح تلقائياً لمدير ولا لمسؤول.** المنح **يُقرأ من القاعدة عند كل
كتابة** لا من الجلسة، فسحب الصلاحية يسري فوراً لا عند الدخول التالي.

### صلاحيات دور الطبيب
المرضى (عرض/إضافة/تعديل) + الزيارات + خطط العلاج + المعاينة.
**بلا وصول مالي** كخبير الأطراف: نقطة `/api/patients/:id/cases` تُرجع
له الحالات **منزوعة الكلفة والمدفوع والمتبقّي** — لا 403، لأنه يحتاج
الحالات ليعرف اختصاصات المريض، والحجب يكون في المصدر لا في الواجهة.

### التحرير مع حفظ كامل النسخ (migration 030)
المعاينة **تُحرَّر** الآن، لكن **لا شيء يُمحى**: كل نسخة سابقة تُصوَّر في
`medical_exam_revisions` قبل استبدالها، مع مَن عدّلها ومتى. الواجهة تُظهر
«النسخة ٢ — عدّلها فلان» بضغطة تفتح النصّ القديم كاملاً.

**مَن يحرّر**: الطبيب **صاحب المعاينة** حصراً، أو **المدير المسؤول**
(admin / branch_manager). طبيب آخر — ولو بنفس الاختصاص — لا يعيد كتابة
توقيع زميله، بل يضيف ملحقاً.

**الترِكر باقٍ كما هو**: يرفض أي `UPDATE` لم يفتح الباب المراقَب
`app.allow_exam_edit`. فمسار التطبيق المدقَّق يفتحه صراحةً، وأي تعديل
شارد من Console أو سكربت أو خطأ برمجي يبقى مرفوضاً. القفل لم يضعف —
اكتسب باباً واحداً تحت الرقابة. وجدول النسخ نفسه مقفل تماماً.

> **⚠ لم يعد الطبيبُ يكتب هذا الاقتراح من نافذة التوقيع** (القسم 4.h،
> 2026-08-28 — معاينةٌ طبّيةٌ محضة). العمودان `medical_exams.device_cost`
> و`proposed_expert_user_id` والمنطقُ الموصوف في هذين القسمين الفرعيين
> يبقيان في القاعدة ويُقرآن للمعاينات القديمة، لكنّهما لا يُكتَبان بعد
> اليوم عند التوقيع — فقط بتصحيحٍ إداريّ صريح عبر `PATCH` على معاينةٍ
> تحمل قيمةً سابقة. اقرأ القسم 4.h.

### كلفة الجهاز — مقترح الطبيب، واعتماد الاستعلامات (migrations 030+032)
`medical_exams.device_cost` — **للأطراف والمساند فقط**. الطبيب يقترح
الكلفة لأنه مَن يحدّد الجهاز، لكنها **لا تدخل الحسابات عند التوقيع**:
تبقى على المعاينة كمقترح، وتظهر لموظف الاستعلامات في «تخصيص وإسناد
خبير» **مملوءة مسبقاً** مع سطر مصدرها. **حفظ التخصيص هو نقطة الاعتماد
الوحيدة** — يكتب كلفة الحالة **ويرفع `patients.total_cost`** (وهو ما
يجمعه `totalRevenue`). فمريض خرج ولم يدفع أو غيّر رأيه — ولو بعد سنة —
لا أثر له في الأرقام حتى يُؤكَّد.

**لماذا**: الكتابة المباشرة على الحالة (التنفيذ الأول) كانت تُدخل المبلغ
في تقسيم الأقسام دون `totalRevenue`، ثم «تخصيص» يضيف الفرق فقط — فتأكيد
نفس السعر أضاف صفراً والبيع اختفى من الأرقام نهائياً. `migration 032`
تعيد الكلف غير المؤكَّدة (بلا أمر تصنيع ولا دفعات) إلى صفر.

**العلاج الطبيعي بلا تغيير إطلاقاً**: كلفته تبقى في «الكلفة والجلسات»
تُحسب بالجلسة. والخادم يُسقط أي كلفة تصل على معاينة علاج طبيعي.

### الخبير المقترح من الطبيب (migration 035)
`medical_exams.proposed_expert_user_id` — **للأطراف والمساند فقط**، بنفس
منطق الكلفة المقترحة: الطبيب **يقترح** الخبير (اختياري)، فتفتح نافذة
«تخصيص وإسناد خبير» مملوءة به مع سطر «اقترحه د. فلان»، وللاستعلامات
إبقاؤه أو تغييره — ولا يُسنَد شيء إلا بحفظ التخصيص. وإن تركه الطبيب
فارغاً يختاره الموظف كالمعتاد. «بدء التصنيع» في صفحة المريض يُملأ منه
أيضاً فلا يُتَّخذ القرار مرتين.

**الخادم يتحقّق**: الاقتراح يُقبل فقط إن كان خبيراً فعّالاً في **فرع
المريض** (نفس قائمة الاستعلامات)؛ وإلا يُسقَط بصمت — اقتراحٌ لا يصلح
لا يجوز أن يُفشل توقيع سجلّ سريري. والعلاج الطبيعي يُسقِط الحقل دائماً.

**بلا مفتاح أجنبي — وهذا مُثبَت لا مفترَض**: أول تنفيذ استعمل
`REFERENCES ... ON DELETE SET NULL`، فصار حذف خبير مقترَح **يفشل**، لأن
SET NULL تعديلٌ على `medical_exams` وترِكر الختم (028) يرفض أي تعديل لم
يفتح الباب المراقَب. فالعمود لقطة للرقم كما أن `doctor_name` لقطة
للاسم: حساب يُحذف يترك رقماً لا يُفسَّر، فيظهر بلا اسم ويختار الموظف من
القائمة الحيّة. لا شيء ينكسر ولا الختم يضعف.

### باني نوع البتر في الوصفة
حالات البتر الثلاث من نموذج التسجيل نُقلت حرفياً إلى وصفة الطبيب:
**احادي** (الطرف/الجهة/المستوى) · **ثنائي** (علوي/سفلي/علوي وسفلي
بتفاصيل كل جهة) · **اطراف سليكونية تعويضية** (النوع/الجهة عدا الأنف/
ملاحظات). القوائم والتركيب في `shared/case_fields.ts`
(`buildAmputationSite`)، والسلسلة المركّبة **مطابقة بايتاً** لصيغة
النموذج القديم فيقرأها EditPatient وصفحة أمر التصنيع بلا تغيير.
اختبار دائم: `npm run test:amputation-site` (١٥ حالة، بلا قاعدة بيانات).

### تبديل النوع لا إضافته
قرار الطبيب **يستبدل** تحديد الاستعلامات ولا يضيف حالة ثانية — وإلا بقيت
الحالة الأولى «بانتظار معاينة» للأبد (حدث فعلياً).

**القاعدة الدقيقة**: التبديل فقط حين كان للمريض **حالة جهاز واحدة** وهي
الأخرى. أما مَن يحمل **الاثنين** (تركيبة مشروعة يضيفها الاستعلامات) فلا
يُمسّ — الطبيب يوثّق أحد خيطين حقيقيين. اختبار دائم:
`npm run test:switch-case` (١١ حالة، بلا قاعدة بيانات).

التنفيذ يعيد استخدام `storage.deleteCaseType` بحُرّاسه: يرفض إن وُجد أمر
تصنيع أو دفعات موسومة، وينقل الزيارات والدفعات للحالة الباقية. وحين
يرفض، تبقى الحالتان ويُبلَّغ الطبيب برسالة صريحة.

### ثلاث طبقات حماية
1. **ترِكر `BEFORE UPDATE`** على الجدولين — لا تعديل بلا الباب المراقَب.
   مخرج طوارئ للمالك داخل معاملة:
   ```sql
   BEGIN; SET LOCAL app.allow_exam_edit = 'on';
   UPDATE medical_exams SET ... WHERE id = ...; COMMIT;
   ```
2. **ترِكر `BEFORE DELETE`** يصوّر الصفّ في الجدول الجنائي مع
   `current_user` و `client_addr`. لا توجد نقطة REST للحذف إطلاقاً؛
   المسار الوحيد هو كاسكيد حذف المريض.
3. `logAudit` بـ `entity_type = 'medical_exam'` و
   `'medical_exam_addendum'`.

### عزل المساند عن الأطراف (migration 031)
**كانت `syncPatientCases` تخترع حالة أطراف من عمود متروك.** القاعدة القديمة
تعتبر `amputation_site` أو `prosthetic_type` دليلاً على البتر. ونموذج
التسجيل **يبقي قيم الخدمة السابقة** عند تبديل النوع (باني `amputationSite`
يخرج مبكّراً حين لا يكون النوع «أطراف» فلا يمسح ما كتبه)، **ويفتح على
الأطراف افتراضياً** في أغلب الفروع. فمريض مساند فقط كان يحمل
`amputation_site` متروكاً ⇒ حالة أطراف وهمية ⇒ «بانتظار معاينة أطراف
صناعية» لطرفٍ لا يحتاجه (حدث فعلاً مع المريض جواد نعمة ثامر).

**القاعدة الآن** في `shared/case_signals.ts` (اختبار: `npm run
test:case-signals`، ٢٠ حالة بلا قاعدة بيانات):
- **إشارات صارمة** تُحتسب دائماً: العلم، أمر التصنيع، دفعة موسومة.
- **عمود التفاصيل إشارة ناعمة**: تُحتسب **فقط** لمريض بلا أي تصنيف — وهو
  إنقاذ السجلات القديمة الذي كُتبت لأجله. أما المصنَّف صراحةً فالعمود
  المتروك ضجيج لا دليل.

**والنموذج يمسح** حقول الخدمات الأخرى عند تبديل النوع، فلا تُرسل أصلاً.

**الترحيل 031** يحذف الحالات الوهمية الموجودة بحُرّاس صارمة: العلم false،
والمريض مصنَّف بغيرها، والكلفة صفر، ولا زيارة ولا دفعة ولا أمر تصنيع ولا
معاينة. ثم يمسح الأعمدة المتروكة كي لا تُعيد تصنيعها. أي حالة فيها أثر
حقيقي **لا تُمَسّ**.

### وسم «بانتظار معاينة»
`GET /api/medical/pending` يرجع كل **حالة نشطة بلا معاينة**، صفّ لكل
(مريض، اختصاص). يظهر شارة كهرمانية في قائمة المرضى وصفحة المريض، فيرى
كل طبيب مَن ينتظره في اختصاصه. الحالات `status='closed'` مستثناة.

### قائمة عمل الطبيب — `/my-exams`
صفحة **هبوط الطبيب** بعد الدخول (كخبير الأطراف مع `/manufacturing`).
تُظهر كل حالة نشطة بلا معاينة **في اختصاصاته هو**، مجمّعة بالاختصاص
ومرتّبة بالأقدم انتظاراً، مع زرّ «كتابة معاينة» مباشر. الفلترة
بالاختصاص تتمّ **في الخادم** فلا يُعرض على طبيب علاج طبيعي حالةُ أطراف.

هذا نمط الـ Worklist في الأنظمة السريرية: العمل يذهب للطبيب لا العكس.

زرّ «كتابة معاينة» يظهر أيضاً في **صفّ المريض داخل سجلّ المرضى**، لكن
فقط حين يكون للمريض حالة منتظرة **ضمن اختصاص هذا الطبيب**، ويفتح
النموذج مضبوطاً على ذلك الاختصاص.

### الوصفة — قرار الطبيب (migration 029)

**الطبيب يقرّر، والاستعلامات تسعّر وتقبض.** قبل هذا كان الطبيب يكتب
التشخيص نصّاً ثم **يعيد موظف الاستعلامات إدخال القرار يدوياً** في
«تخصيص» أو «الكلفة والجلسات» — فالقرار للطبيب والإدخال (وأخطاؤه) لغيره.

- عمود `medical_exams.prescription` (jsonb) يحمل القرار **داخل الصفّ
  المقفل**، فلا يُعدَّل بعد التوقيع كبقية المعاينة.
- `shared/case_fields.ts` — **مصدر حقيقة واحد** للحقول والقوائم، منقولة
  حرفياً من نموذج المريض ونافذة «تخصيص» (كانت مكرّرة في ٦ ملفات):
  `PROSTHETIC_SPECS` (٧ حقول) · `SUPPORT_SPECS` · `INJURY_TYPE_OPTIONS`
  (٣٤ قيمة، الاتحاد الأشمل) · `INJURY_AREA_OPTIONS` (٢٣) ·
  `INJURY_SIDE_OPTIONS` · `serializeInjuries` / `parseInjuries`.
- `store.applyPrescription` يكتب **نفس أعمدة `patients` القديمة** التي
  يكتبها الاستعلامات، ثم `syncPatientCases` يشتقّ `details` — المسار
  المُجرَّب نفسه، فلا شيء في المصنع أو التقارير يحتاج تغييراً.
- **لا يمسح شيئاً**: الحقل الفارغ في الوصفة يعني «غير محدَّد» لا «امسح».
- **بلا مال**: أنواع العلاج وعدد الجلسات يقرّرها الطبيب، لكن الكلفة لا
  تظهر له إطلاقاً.

**الطرف الآخر**: «تخصيص» و«الكلفة والجلسات» تُفتحان **مملوءتين** من
المعاينة الموقّعة مع سطر «من معاينة د. فلان» — فيؤكّد الموظف بدل أن
يعيد الإدخال، ويبقى له الكلفة والخبير والقبض.

اختبار دائم: `npm run test:prescription` (١٤ حالة، بلا قاعدة بيانات).

**تعبئة مسبقة (2026-07-28)**: نموذج معاينة **العلاج الطبيعي** يفتح
مملوءاً بالتشخيص والإصابات المسجّلة على ملف المريض (من الاستعلامات أو
ملف قديم) — الطبيب يكمل ويعدّل بدل إعادة الإدخال، وما يوقّعه هو
المعتمد. الحقول التي لمسها الطبيب لا تُستبدل، والتحرير (نسخة ثانية)
يفتح وصفة المعاينة نفسها لا بيانات الملف.

**ومصدرُ التعبئة بابُ المعاينة لا ملفُّ المريض (٢٠٢٦-٠٩-٢٤)**: الأطرافُ (نوعُ
البتر) والمساندُ (نوعُ المسند وجهتُه) تفتح كذلك على ما سجّله الاستقبال (قرارا
المالك ٢٠٢٦-٠٧-٣١ و٢٠٢٦-٠٨-٠٦). وكانت النافذةُ تقرؤه من `GET /api/patients/:id`،
وذلك البابُ يردّ **صامتاً** طبيباً لا يحمل «عرض المرضى» (٤٠٣، منذ ٢٠٢٦-٠٩-٠٣)
وطبيباً يعمل في فرعين والمريضُ في غير فرع جلسته (٤٠٤) — فتفتح فارغة (شكوى المالك،
مُعادٌ حيّاً). فصار يصل في `registration` من `GET /api/medical/patients/:id/exams`
(`registrationClinicalOf`) **بنطاق قائمة الطبيب نفسِه**: سبعةُ حقولٍ سريرية لا غير،
بلا مالٍ ولا هاتف. **وتُجلَب لكلّ معاينةٍ جديدة** ولو وصل الجهازُ جاهزاً من
«معايناتي» — والجهازُ الثابت لا يُقرَّر بها ولا يُنتظَر لها الحفظ.

### نقاط REST (`server/medical/routes.ts`)
| النقطة | الوصف |
|---|---|
| `GET /api/medical/me` | صلاحية الطبيب واختصاصاته |
| `GET /api/medical/patients/:id/exams` | التسلسل + الملاحق + الانتظار |
| `POST /api/medical/patients/:id/exams` | توقيع معاينة |
| `POST /api/medical/exams/:id/addenda` | إضافة ملحق |
| `GET /api/medical/pending` | خريطة الانتظار للقائمة |
| `GET /api/medical/worklist` | قائمة عمل الطبيب (اختصاصه فقط) |
| `PATCH /api/medical/exams/:id` | تحرير — لصاحبها أو للمدير، مع حفظ النسخة |

**القراءة للجميع** ضمن نطاق الفرع — السجل السريري يراه فريق العلاج كلّه،
لكن **كلفة الجهاز تُحجب عن خبير الأطراف** (محجوب مالياً في كل النظام) فلا
تصير المعاينة قناةً جانبية للأسعار.

### كلٌّ يرى أدوات عمله فقط
- **نموذج التسجيل**: الحقول السريرية **للأطراف والمساند** تظهر لـ**مدير
  الفرع والطبيب والمسؤول العام فقط** — باني نوع البتر (أطراف) وجهة
  الإصابة (مساند)، مع ملاحظة «يحدّدها الطبيب في المعاينة» لكل منهما.
  الحجب مفروض **في الخادم أيضاً**: نقطة إنشاء المريض تُسقط هذه الحقول
  من جلسة غير مخوَّلة. **العلاج الطبيعي (عُدّل قرار 2026-07-26 في
  2026-07-28)**: «تشخيص الحالة / نوع المرض» صار للطبيب وحده — محجوب عن
  الاستقبال في التسجيل (واجهةً وخادماً، مع العبارة الخضراء «يحدّده
  الطبيب في المعاينة») ويكتبه الطبيب في وصفة المعاينة فتُطبَّق على صفّ
  المريض. أما الإصابات وتاريخها وسببها وتصنيف المريض والملاحظات فتبقى
  للاستقبال كما كانت.
- **«تخصيص وإسناد خبير»**: ما حدّده الطبيب في المعاينة يظهر **للقراءة
  فقط** حقلاً بحقل، والفارغ يبقى قابلاً للإكمال (مخرج المرضى القدامى بلا
  معاينة). والخادم يعيد فرض مواصفات الطبيب فوق أي قيمة في الطلب
  (`prescribedSpecs`).
- **«تعديل مريض»** يبقى كاملاً — مخرج التصحيح للإدارة.

### «خدمة جديدة» بعد التنظيف (2026-07-28)
«إضافة نوع حالة» هيكلية (فتح خيط اختصاص، بلا مال) و«خدمة جديدة» مالية
(قيد على خيط قائم) — لا تكرار بينهما بعد الآن. «خدمة جديدة» اقتصرت على
ما لا مسار له غيرها: **جلسات علاج إضافية · استشارة · خدمة أخرى**.
خيارات صيانة/طرف جديد/تعديل حُذفت من الواجهة **ويرفضها الخادم** برسالة
تدلّ على المسار الصحيح (الصيانة من نافذة الزيارة، والطرف عبر المعاينة ثم
التخصيص). النقطة `POST /api/patients/:id/new-service` صارت تتطلّب
`canAddPatients` (أو مدير/مسؤول)، تثبّت الفرع من صفّ المريض لا من جسم
الطلب، وتكتب في `audit_log`. مكوّن `VisitOrServiceModal` الميت حُذف.

### دفتر قيود الكلف المؤرَّخ (migration 033)
`patients.total_cost` رقم متراكم بلا تاريخ، فكان التقرير اليومي يزوّر
«التكاليف» من كلف مسجَّلي اليوم (متضخّمة رجعياً وعمياء عن كلف المرضى
القدامى) ويطرح منها مدفوعات الجميع فيخرج «متبقي −500,000» أحمر بلا معنى
(اكتشفه المالك 2026-07-28). **الحل**: جدول `cost_entries`
(مريض/فرع/مبلغ/مصدر/تاريخ) تكتب فيه **كل** نقطة تحرّك `total_cost` نفس
الدلتا: التسجيل، «تخصيص» (بالسالب عند التخفيض)، «الكلفة والجلسات»، أجور
الصيانة، «خدمة جديدة»، «إضافة نوع حالة»، الزيارة بكلفة، التعديل الإداري
(نقطة الخنق في `storage.updatePatient`)، وسحب الحالة (سالب). الدمج ينقل
القيود للملف الهدف، وحذف المريض يحذفها (مُضافة للكاسكيد ومُختبرة محلياً
وفق القاعدة الملزمة). **الثابت**: مجموع قيود المريض = `total_cost` — ويحرسه إنذار دائم في
«تنبيهات» المحاسبة (`cost_ledger_mismatch`, شدّة high) يسمّي المريض
والفرق بالدينار متى انحرف الرقمان، فلا يمرّ تحرّك مال بلا قيد صامتاً
(أُضيف بعد ملاحظة المالك 2026-07-29 على المريض كاظم ستار: كلفته ظهرت
٢,٩٩٩,٨٨٩ بدل ٣,٠٠٠,٠٠٠ لحظةً ثم عادت).
**القيد الافتتاحي**: عند أول تشغيل يُقيَّد لكل مريض قيد واحد بكامل كلفته
بتاريخ تسجيله — الأيام قبل النشر تقريبية، والدقة الكاملة تبدأ بعده.
**التوزيع التاريخي (migration 034)**: دفعات جلسات العلاج الطبيعي قبل
التفعيل تُقيَّد كلفها بتاريخ الدفعة (الجلسة تُدفع لحظتها بنفس المبلغ)
ويُخصم مجموعها من القيد الافتتاحي — المجموع لا يتغير، والتوزيع يصير
حقيقياً. الأطراف والمساند لا تُمسّ (أقساط ≠ كلفة يومها)، ومريض أرقامه
لا تنطبق يُترك كما هو.

**عرض التقرير اليومي (قرار المالك 2026-07-28 — البساطة)**: رأس اليوم
ثلاثة أرقام فقط: **الوارد** (كل المقبوض) · **المصاريف** (مصروفات الفرع
ذلك اليوم) · **الصافي** = وارد − مصاريف. والمدفوعات جدولان: «دفعات
خدمات اليوم» (لمريض له قيد كلفة موجب أو سُجّل ذلك اليوم) و«تسديد ديون
سابقة» مع عمود «الباقي عليه» (كلفته − مدفوعه مدى الحياة). لا مصطلحات
محاسبية في الواجهة (لا «فائض تحصيل» ولا «قيود» ولا «افتتاحي») — الدفتر
يعمل تحت السطح فقط، مع سطر صغير «خدمات جديدة سُعّرت اليوم بقيمة X».

### النظام المحاسبي موحَّد مع دفتر الكلف (2026-07-28)
`getAccountingSummary` كان «كوهورت»: الإيرادات = كلف مسجَّلي الفترة،
والمدفوع = كل مدفوعاتهم مدى الحياة — واختيار يوم واحد يعطي **أصفاراً**
لأن نهاية الفترة كانت تُفسَّر منتصف ليل بدايتها (بينما المصاريف مقارنة
نصية شاملة فتظهر — وهذا ما حيّر المالك). **الآن تدفّق نقدي موحَّد** مع
التقرير اليومي: الوارد = مدفوعات الفترة · المبيعات = قيود كلف الفترة ·
الصافي = وارد − مصاريف · **الديون المستحقة ونسبة التحصيل أرقام إجمالية
(حتى الآن) لا تتأثر بالفترة**. تقسيم الأقسام: الوارد المقبوض عبر وسم
الحالة، والمبيعات عبر مصدر القيد (تخصيص/صيانة = أجهزة، تسعير/جلسات =
علاج طبيعي، والغامض يبقى «غير مبوّب» لا يُخمَّن). ترتيب البطاقات: وارد،
مصاريف، صافي أولاً. وأزرار «اليوم» و◀▶ تزحزح الفترة بطولها للتنقل
اليومي بلا إدخال يدوي.

### الصيانة: الكلفة عند الفتح، وتغيير الموعد بسبب جازم
- **أجور الصيانة** تُدخَل في نافذة فتح الصيانة (استقبال/مدير/مسؤول —
  نفس مَن تقبلهم النقطة أصلاً)، **ويجب أن تكون مبلغاً موجباً**.

  **الصفر وحده لا يعني «مجّاناً» أبداً (قرار المالك 2026-08-20 — يُلغي
  ويستبدل قرار 2026-07-26 «المبلغ قرار الموظف — صفر أو غيره»)**. المجّانيّ
  قرارٌ يُعلَن ويُعتمَد، لا رقمٌ يُترك صفراً: الخادم يردّ `cost <= 0` على
  `POST /api/manufacturing/maintenance-visit` برسالة تدلّ على الباب الصحيح،
  والواجهة تمنع الإرسال قبله. **فالصفر لم يعد باباً خلفياً للتبرّع.**

  المسارات الثلاثة، وهي كلّ ما هو مسموح:
  - **السعر الكامل الموجب** ⟶ **تنفيذٌ فوري**: يُقيَّد داخل نفس المعاملة
    على كلفة حالة الجهاز (`manual`) **وعلى `patients.total_cost`** فيدخل
    `totalRevenue` وتقسيم الأقسام والمتبقّي، ثم يُسجَّل الدفع كالمعتاد.
  - **بخصم** ⟶ **طلبٌ معلَّق عبر نظام الخصم الموحَّد** نفسه (لا مسار
    موازٍ للصيانة).
  - **مجّاني** ⟶ يُختار **صراحةً** «مجاني (تبرع من دكتور ياسر)» **من سعرٍ
    أصليٍّ موجب** ⟶ طلبٌ معلَّق بقواعد الاعتماد القائمة.

  **ولا أثر مالياً قبل الاعتماد** حين يكون الاعتماد مطلوباً: لا أمر تصنيع
  ولا زيارة ولا قيد كلفة ولا سطر يومية ولا دينار. الطلب المعلَّق يحمل
  `originalPrice` الموجب و`contextRef = maintenanceDiscountRef(serviceType)`
  ولا سعرَ في حمولته؛ والاعتماد وحده ينادي
  `createMaintenanceOrderWithVisit` — **الدالّة القانونية نفسها** التي
  تنادِيها الصيانةُ كاملةُ السعر، فلا نسخة ثانية من محاسبة الصيانة تنحرف.

  ملاحظة الزيارة تتجنّب صيغة «تكلفة:» عمداً كي لا يلتقطها محلّل الـ
  markers في `syncPatientCases`.
- **تغيير موعد التسليم** متاح لكل كاتب على الأمر — **الخبير المسنَد
  ضمناً** — لكن بسبب **إلزامي** عند تغيير موعد قائم (أول تحديد بلا سبب).
  السبب يُحفظ في سجل الأمر (`date_change`) وفي `audit_log`، **ويظهر في
  بطاقة التصنيع بصفحة المريض للجميع** (`dateChanges` من
  `getAllOrdersForPatient`).

### الاستقبال يُدخل الاتفاق، والمشرف يعتمد الاستثناء (2026-08-21)

**الموظّفُ هو مَن يكلّم المريض**، وهو وحده يعرف على كم اتّفقا: ٢٥,٠٠٠ أو
٢٠,٠٠٠ أو ١٢,٥٠٠ أو ٥,٠٠٠ أو مجّاناً. وقبل هذا لم يكن له حقلٌ يقوله فيه في
«جلسات علاج إضافية» — تُنفَّذ الخدمةُ بسعرها الكامل، **ثم يخمّن مديرُ الفرع
الاتفاقَ لاحقاً**. فصار له الحقل، وصار الطابورُ للاستثناء وحده.

**القاعدة:**
- **السعرُ الكامل يمضي فوراً بلا اعتماد** — خمسون مريضاً في اليوم لا يمرّون
  بطابور، وطابورٌ يمتلئ بالعاديّ لا يُقرأ. `hasDiscount` تفرّق: المساواةُ
  ليست خصماً.
- **بخصم** ⟶ طلبٌ معلَّق في `service_discount_requests` نفسه (لا نظامَ
  اعتمادٍ موازٍ ولا صفحةَ ثانية).
- **مجّاني** ⟶ يُختار صراحةً «مجاني (تبرع من دكتور ياسر)» **من سعرٍ أصليٍّ
  موجب**. **والصفرُ وحده لا يعني «مجّاناً» أبداً** — نفسُ قاعدة الصيانة.
- **ولا أثرَ مالياً قبل الاعتماد**: لا كلفةَ مريض، ولا قيدَ دفتر، ولا دفعة،
  ولا جلسةَ مشتراة، ولا زيارة، ولا سطرَ يومية. الصفُّ المعلَّق هو الأثرُ
  الوحيد.

**والسعرُ الأصليُّ يُحسب في الخادم** (`physioOriginalPrice` من جدول الأسعار
المشترك) لا يُقبل من العميل: أصلٌ ملفَّقٌ منخفض يجعل الخصمَ يبدو صغيراً
فيمرّ على المعتمِد ما لم يكن ليمرّ.

**والمرجعُ رمزُ الإرسالة نفسه** (`newServiceDiscountRef`): ضغطةٌ واحدة =
طلبٌ واحد، ولا يرى المديرُ طلبين متطابقين من إعادة إرسالٍ بعد انقطاع شبكة.

**والاعتمادُ ينادي الدالّة القانونية نفسها.** `server/new_service/store.ts:
executeNewService` هي **الكتابةُ الواحدة** التي يناديها البابان: النقطةُ
بالسعر الكامل، والاعتمادُ بالسعر المعتمد. نُقلت من جسم `POST
/api/patients/:id/new-service` كما هي حرفياً، وتقبل `tx` فتنضمّ إلى معاملة
الاعتماد — فالقرارُ والخدمةُ يقعان معاً أو لا يقع شيء. **ولا نسخةَ ثانية من
محاسبة الخدمة في `discounts/store.ts`** (وهو ما انحرفت به الصيانةُ قبل ٠٦٠).

**والمجّانيُّ خدمةٌ حقيقية بقيمةٍ صفر**: الجلسةُ تُسجَّل بدفعةٍ مبلغُها صفر
موسومةٍ `is_free_sessions` — المسارُ القائم نفسه للجلسة المجّانية من نقطة
الدفعات. فلا دينارَ ملفَّق، ولا جلسةٌ تضيع من عدّاد مريض المفرد.

**وبطاقةُ الاعتماد تقول ما اشتراه المريض**: نوعُ الخدمة وأنواعُ العلاج
وأعدادُ الجلسات، مع الأصليّ والنهائيّ والنسبة والسبب وطالبِها. و«اعتماد»
و«رفض» أوّليّان، و«تعديل واعتماد» ثانويٌّ للاستثناء — **ولا يُطلَب من المدير
أن يُدخل السعرَ المتّفق عليه ابتداءً**.

### «تمت المراجعة» اعترافٌ لا موافقةٌ طبية (2026-08-21)

صفحةُ `/medical-review` صارت **«مراجعة حركة مرضى الأطراف والمساند»**: مَن
جاء · وماذا جرى · ومتى · ومَن تولّاه.

**لماذا**: الطلبُ السريع يُنشَأ **بعد** أن تقع الخدمةُ فعلاً (تُفتَح صيانةٌ
أو تُسجَّل زيارةٌ ثمّ يُوجَّه الطلب)، فتسميةُ الفعل «موافقة طبية» وصفٌ لشيءٍ
لم يحدث: مَن يقرأ «موافقة» يظنّ أن الخدمة كانت تنتظره، ومَن لا يقرؤها يظنّ
أنه أوقف شيئاً.

**القيمةُ `approved` باقيةٌ في القاعدة كما هي** — تسميتُها لا تستحقّ ترحيلاً
(**ولا ترحيلَ في هذه التمريرة إطلاقاً**) — **لكن كلَّ ما يقرؤه إنسان يقول
«تمت المراجعة»**: الزرّ، والحالة، وسطرُ التدقيق، والتعليقات، وهذا الملفّ.

**والبطاقةُ خفيفة عن قصد**: كانت تسكب ملفَّ المريض كلَّه (مواصفات الجهاز،
مرحلة الأمر، تشخيصَ آخر معاينةٍ وخطّتَها)، ومَن يمرّ على ثلاثين بطاقةً كهذه
لا يقرأ شيئاً. فبقي ما يلزم للتأشير، والتفصيلُ خلف «فتح ملف المريض».
**ولا حالاتٍ داخلية خام** (`awaiting_exam`) تُعرَض. **واليومُ افتراضاً**
(تقويم بغداد) مع بابٍ واحد «غير مراجعة سابقة».

**وبوّابةُ المعاينة الكاملة لم تُمَسّ**: الجهازُ الجديد يستوجب معاينةً موقّعة
كما كان، ولا شيءَ هنا يُضعف ذلك الشرط.

#### قدرتان لا واحدة
- **`canSuperviseReview`** (جديدة) — مسؤولٌ، أو **مديرُ فرعٍ في نطاقه**، أو
  طبيبٌ مخوَّل. تفتح «تمت المراجعة» و«إرجاع للاستعلامات».
- **`canDecideReview`** (كما هي) — طبيبٌ مخوَّل وحده. تفتح «يتطلّب معاينة
  كاملة»، وهو **قرارٌ سريريّ** لا إشرافيّ.
- **و`canWriteMedicalExam` لم تُمَسّ بحرف**: مديرُ الفرع يراجع إشرافياً ولا
  يوقّع معاينةً أبداً. الاعترافُ الإشرافيُّ والتوقيعُ السريريُّ قدرتان
  مختلفتان — ولو وُسِّعت الأولى لصار المديرُ يوقّع سجلّاً سريرياً باسمه.
- **والاستقبالُ العاديُّ ليس منهما.**

#### الإرجاع إلى الاستعلامات
**بسببٍ إلزاميّ** في المسارين (مثال: «جهة البتر غير صحيحة — عدّلها وأعد
إرسال الطلب»). ويُحفظ مَن أرجع ومتى ولماذا.

- **الطلبُ السريع**: `return_to_reception` القائم، بسببٍ صار مطلوباً.
- **طلبُ المعاينة الكاملة** (جديد): `POST /api/medical-review/requests/:id/
  return` — بابُ خروجٍ نظيف لطلبٍ أُرسل ببيانٍ خاطئ. كان عالقاً إلى الأبد في
  «معايناتي»: لا الطبيبُ يوقّع على خطأ، ولا أحدَ يسحبه — فكانت الحيلةُ
  الوحيدة أن يوقّع معاينةً يعرف أنها خطأ ليُخرجها من قائمته.
  زرُّه في «معايناتي»، ويرسل الخادمُ `returnableRequestId` لمن يملك القدرة
  وحده.

**وما لا يفعله الإرجاع**: لا يُنشئ معاينةً ولا يحذفها ولا يمسّ نسخةً ولا
ملحقاً · ولا يمسّ حلقةَ الجهاز ولا أمرَ التصنيع ولا ديناراً — الخدمةُ التي
وقعت وقعت، والمُرجَعُ هو **طلبُ المراجعة** لا الحدثُ الذي وثّقه · ولا يُرجَع
ما وُقّعت له معاينةٌ بعده (٤٠٩) · **ولا يُرجِع المرءُ طلبَ نفسه** ولو ملك
القدرة، وإلّا صار البابُ طريقاً لمحو الأثر.

**ولا ترحيلَ لأجله**: فهارسُ التفرّد في ٠٥٥ مشروطةٌ بـ`WHERE status =
'pending'` أصلاً، فالمُرجَعُ يحرّر المرساةَ ويرسل الاستعلاماتُ بديلاً
مصحَّحاً بلا ٤٠٩.

اختبارٌ دائم: `npm run test:reception-agreement` (١٢ سيناريو حيّاً) و
`npm run test:supervisor-ui` (عقدُ الشاشات، بلا قاعدة بيانات).

### «حذف» المعاينة الموقّعة = إلغاءٌ مؤرشف (migration 061)

**زرُّ «حذف» في الشاشة لا يمحو شيئاً أبداً.** الموظّف يفهم «حذف»، والنظام
ينفّذ **إلغاءً**: صفٌّ في `medical_exam_cancellations` (فريدٌ على `exam_id`)
يقول مَن ألغى ومتى ولماذا. والمعاينةُ الأصلية ونسخُها وملاحقُها وتوقيعُ
الطبيب وأختامُه الزمنية **تبقى كلُّها كما هي بايتاً بايت** — وختمُ ٠٢٨ لم
يضعف بحرف.

**لماذا أصلاً**: معاينةٌ تُكتب للمريض الخطأ أو على اختصاصٍ غير صحيح كانت
تبقى قائمةً إلى الأبد، فيقرأ الخبيرُ مواصفاتِ مريضٍ آخر ويقف الجهازُ على
سعرٍ خاطئ. ولم يكن للطبيب مخرجٌ إلّا أن يوقّع معاينةً يعرف أنها خطأ.

**تعريفٌ واحد لا يتكرّر**: `server/medical/active_exam.ts` يقول ما هي
«المعاينة الفعّالة» = صفٌّ **بلا شاهدة إلغاء**. ويُستورَد في كلّ قارئٍ
تشغيليّ (~١٥ موضعاً عبر خمسة ملفّات). فالملغاة **لا** تفتح بوّابة التصنيع،
ولا تعطي سعراً ولا مواصفات، ولا شارةَ «تم تحديد»، ولا تُرضي طلبَ مراجعةٍ
كاملة، ولا تُخرج المريضَ من طابور الطبيب، ولا تُحتسب حضوراً في «مرضى
اليوم». **والقراءةُ التدقيقية تبقى كاملة** — الإلغاء يرفع السلطة لا الشهادة.

**قبل البيع**: إلغاءُ معاينةِ جهازٍ **يعيد الحلقةَ نفسَها** من `examined`
إلى `awaiting_exam`. الطلبُ باقٍ — المريضُ ما زال يريد الجهاز — والمعاينةُ
وحدها سقطت. فتُوقَّع مصحَّحةٌ تطالب **الحلقة نفسَها** وتعيدها `examined`.
والمتابعةُ المولودة عنها تتقاعد بحالةٍ طرفيّةٍ ثالثة `closed_exam_cancelled`
(**لا** `closed_without_purchase`: المريض لم يرفض الشراء). وفهرسا التفرّد
في ٠٥٣ وُسِّعا ليستثنياها، وإلّا منعت المتقاعدةُ متابعةَ المعاينة المصحَّحة.

**بعد البيع أو التصنيع**: يُردّ الإلغاء **٤٠٩** — حلقةٌ `in_manufacturing`
أو `delivered`، أو أمرُ تصنيعٍ أوّليّ قائم، أو متابعةٌ `converted`، أو طلبُ
خصمٍ/سعرٍ معلَّق **بمرجع هذا الجهاز بعينه** (خصمُ جهازٍ آخر للمريض نفسه لا
يمنع). ولا يُحذَف أمرٌ ولا تُعكَس دفعةٌ ولا قيدٌ — لكلٍّ مسارُ إلغائه.

**ولا تراجعَ عن حقول المريض**: التوقيع يكتب الوصفةَ على صفّ المريض، ولا
لقطةَ لما كان قبله. فتخمينُ «ما كان» يكتب ماضياً لم يقع. الحمايةُ الحقيقية
أن ترجع الحلقةُ `awaiting_exam` فلا يُصنَّع شيءٌ حتى تُوقَّع مصحَّحةٌ تكتب
إسقاطَها بالمسار نفسه.

**والصلاحية بلا دورٍ جديد**: نفسُ طرفَي «تعديل» حرفياً — صاحبُ المعاينة أو
المدير المسؤول (مسؤول/مدير فرع)، ضمن الفرع. وطبيبٌ آخر ولو بنفس الاختصاص
**لا يسحب توقيع زميله**. والاستقبالُ والمحاسبُ والخبير ليسوا منهم.
والملغاةُ **لا تُعدَّل ولا يُضاف إليها ملحق** (٤٠٩) — التصحيحُ معاينةٌ جديدة.

**وصاحبُها يبقى صاحبَها ما بقي اختصاصُها له**: الشرطُ ليس «أيملك أيَّ اختصاصٍ
طبّي؟» بل **اختصاصُ هذه المعاينة بعينها**، يُقرأ من القاعدة عند كلّ طلب لا من
الجلسة. فطبيبٌ سُحب منه اختصاصُ الأطراف وبقي له العلاجُ الطبيعي **لا يسحب
توقيعاً لم يعد يملك أن يكتب مثلَه** — سحبُ المنح يسري فوراً في الاتجاهين معاً.
والمديرُ المسؤول خارجَ الشرط: إذنُه إداريٌّ لا سريريّ، فلا يُطلَب منه اختصاصٌ
طبّيٌّ (وهو لا يوقّع معاينةً أصلاً). **والشاشةُ تقرأ القاعدةَ نفسَها**: `canCancel`
يخرج من الدالّة التي تحرس النقطة وباختصاصات الطالب المقروءة مرّةً واحدة —
بلا استعلامٍ لكلّ معاينة — فلا يظهر زرُّ «حذف» حيث كان الخادمُ سيردّ ٤٠٣.

**والمتابعةُ المتقاعدة تقول سببَها بالعربية**: `closed_exam_cancelled` مفتاحٌ
داخليّ (وكذلك `exam_cancelled` في عمود السبب)، و`shared/followup_events.ts`
يترجمه «أُغلقت المتابعة بسبب إلغاء المعاينة» ويُخرج معه **سببَ مَن ألغى** كما
كتبه. بلا ذلك كان الموظّفُ يقرأ العبارةَ العامّة «إجراء مسجَّل على الملف».

اختبارٌ دائم: `npm run test:exam-cancellation` (١٤ سيناريو حيّاً، منها
إثباتُ أن لا صفَّ يُحذف وأن حذف المريض الكامل ما زال يعمل، وأن سحبَ الاختصاص
يُغلق البابين معاً)، و`npm run test:followup-events` لعرض حدث الإغلاق.

### الملفات القديمة مستثناة من المعاينة (قرار المالك 2026-07-29)
«القديم» = المسجَّل **قبل لحظة تطبيق migration 028 في قاعدة البيانات
نفسها** (`_migrations.applied_at` — حتمي، ولا يمكن لمريض جديد أن
«يشيخ» إلى الاستثناء)، **أو** مَن صنّفه الاستعلامات «مريض قديم»
(`patient_classification = 'past'`) — لأن العائد القديم كثيراً ما
يُنشأ ملفه في النظام اليوم رغم أن ملفه الورقي قديم (لاحظه المالك مع
المريض نعمه محمد الحمزه). للقديم: تخصيص/بدء تصنيع بلا معاينة، الاستعلامات
تدخل المواصفات والكلفة مباشرة (كالمسار القديم)، ويختفي من قوائم
الانتظار وقائمة عمل الطبيب — فتصفو للجدد. المعاينة تبقى **ممكنة** له
(الاستثناء يرفع الإلزام لا الإمكانية): نقطة `/api/medical/pending`
تُرجع خريطتين — `pending` (إلزامي، شارة كهرمانية) و`optional` (قدامى
بلا معاينة: بلا شارة، لكن زرّ «كتابة معاينة» يظهر للطبيب)، وإن وُجدت معاينة طوعية فقرارها
يسود. الجديد: معاينة إلزامية **لكل اختصاص** عند أول طلب له (اختصاص
جديد = مريض جديد لذلك الاختصاص). المنطق في
`medical/store.ts: examSystemActivatedAt / isLegacyPatient`، والخادم
والواجهة يطبّقانه معاً (`legacyExempt` في نقطة المعاينات و`activatedAt`
في `/api/medical/pending`).

> **⚠ ضاق نطاقُ هذه القاعدة في ترحيل ٠٦٥ — اقرأ القسم 4.d.** التسجيلُ لم
> يعد يسأل «جديد/قديم» (الخادمُ يختمه `'new'`)، والقرارُ لعمليةٍ **لها
> `service_path`** يُقرأ من مسارها هي. وما وُصف أعلاه يبقى **حرفاً بحرف**
> لخيطٍ بلا حلقة ولحلقةِ ما قبل ٠٦٥ (`service_path IS NULL`)، ولمَن
> صنّفته الإدارةُ «قديماً» من «تعديل مريض».

### خطة الجلسات المشتراة (migration 036)
عدّاد الجلسات كان يقرأ «المشتراة» من `payments.session_count` — وهذا صحيح
في المسار القديم حيث كل بيع جلسات = دفعة. لكن بعد أن صار التسجيل بلا
كلفة، صارت «الكلفة والجلسات» تحسب الأعداد **وتنساها**: تكتب المال واسم
النوع فقط. فالمريض المسعَّر لعشر جلسات كان يقرأ «صفر مشتراة» وعدّاده
يصير سالباً مع كل زيارة (لاحظه المالك على منتهى خميس وادي ومصطفى حيدر).

`patients.physio_plan` (jsonb: نوع + عدد لكل نوع) هي الذاكرة المفقودة:
تكتبها «الكلفة والجلسات» وتزيد عليها «جلسات علاج إضافية»، والعدّاد
يفضّلها على الدفعات. **المرضى القدامى (بلا خطة) يبقون على الدفعات كما
هم** — لا مصدرين معاً أبداً وإلا احتُسب الشراء مرّتين. ونصّ الخطة صار
يحمل الأعداد («روبوت (10 جلسات)») فيُقرأ من الملف مباشرة.

**الاحتساب يُشتق حيّاً لا بالترحيل وحده** (`resolvePurchasedSessions` في
`shared/pricing.ts`، اختبار: `npm run test:physio-sessions`، ١٥ حالة).
الترتيب — الأصرح يسبق، **ولا يُجمع مصدران أبداً**:
١) الخطة المحفوظة · ٢) الأعداد المكتوبة في النص «روبوت (10 جلسات)» ·
٣) **الاشتقاق من الكلفة**: نوع واحد معروف السعر وكلفة تقسم تماماً ·
٤) جلسات الدفعات (المسار القديم).

المصدر الثالث هو المهمّ عملياً: منتهى خميس وادي سُعِّر ٢٧٥,٠٠٠ لـ«أجهزة
علاج طبيعي» = **١١ جلسة**، بينما دفعاته تحمل ٤ فقط؛ ومصطفى حيدر ١,٠٠٠,٠٠٠
= **٤٠ جلسة** ودفعاته ١١. قراءة الدفعات هي ما جعل العدّاد يخالف الكلفة.

**حماية مريض المفرد (حادثة ذي قار 2026-07-29)**: مَن يدفع جلسة بجلسة
تاريخُه كلّه على دفعاته. لذلك: (١) «خدمة جديدة» **لا تنشئ خطة** لمريض
بلا خطة — تُنشئ الدفعات الحاملة للجلسات فقط، وتزيد الخطة **إن وُجدت**
أصلاً؛ إنشاؤها من جلسة واحدة كان يطمس تاريخ ١٥ جلسة ويقلب العدّاد
سالباً لحظة الدفع. (٢) الاشتقاق من الكلفة يشترط أن يكون ناتجه **أكبر**
من جلسات الدفعات — فالجلسات المجانية بلا كلفة، والكلفة وحدها تُنقِص
عدّ المفرد. (٣) ترحيل 037 صفّر الخطط المسمومة (خطة بلا قيد تسعير
وجلسات دفعاتها أكثر) فأعاد أصحابها لعدّ الدفعات.
ولذلك **لا يعتمد الإصلاح على الترحيل**: أول شرط في ترحيل 036 كان «بلا
دفعات تحمل جلسات» فتخطّى هذين تحديداً — والاشتقاق الحيّ يصلحهما بلا
انتظار. والترحيل يبقى لأنه يثبّت الخطة صراحةً لمن ينطبق عليه.

**النافذة تعدّل الأعداد لا المال**: نقطة `PUT /api/patients/:id/physio-plan`
لا تلمس `total_cost` ولا دفتر القيود إطلاقاً — تصحيح عدّاد يجب ألا يحرّك
ديناراً. وبطاقة «ملخص الجلسات» صارت تظهر حتى عند الصفر مع سطر يشرح
كيفية الإصلاح، فالمشكلة وعلاجها في المكان نفسه.

### الجلسات الموصوفة تُكتب على الملف فور التوقيع (2026-07-29)
شكوى الأطباء: عدد الجلسات في وصفة العلاج الطبيعي لم يكن يظهر في سجل
المريض إلا بعد تسعير الاستعلامات، فيبدو الملف بلا خطة والمريض ينتظر.
الآن `applyPrescription` يكتب `patients.treatmentType` بنص الخطة مع
الأعداد («روبوت (10 جلسات)، أبر صينية (5 جلسات)») لحظة التوقيع —
والتسعير يستبدله لاحقاً بخطته المؤكدة عبر نفس العمود. المال لا يتحرك
عند التوقيع إطلاقاً.

### المراجعة التدقيقية الشاملة للحسابات (2026-07-29, migration 038)
مراجعة عميقة بطلب المالك («لا تفترض ولا تتوقع — اسألني»). النظيف المؤكَّد:
كل نقاط تحريك `total_cost` العشر مقيَّدة بالدفتر، المصاريف مقيَّدة
ومدقَّقة، إنشاء/حذف الدفعات كذلك، حارس «لا دفعة فوق المتبقي»، النسخة
الليلية متّسقة. الثغرات الأربع المكتشفة عولجت بقرار المالك الصريح:

1. **ربط دفعة الزيارة بزيارتها** — `payments.visit_id` (migration 038،
   **عدد صحيح بلا FK عمداً**: كاسكيد `deletePatient` يحذف الفواتير قبل
   الدفعات وFK كان سيكسره — نفس درس `proposed_expert_user_id`).
   الزيارة المدفوعة الجديدة تكتب الرابط **وتُنشئ قيد يومية للدفعة**
   (كانت الوحيدة غير المقيَّدة في اليومية).
2. **تعديل كلفة الزيارة يصحّح المال كله (قرار المالك: ب)** —
   `PATCH /api/visits/:id` عند تغيّر الكلفة: يحرّك `total_cost`
   بالدلتا (مصدر `visit` في الدفتر)، يجد الدفعة المرتبطة (بالرابط، أو
   للقديم: تطابق واحد حصري على المريض+المبلغ+«دفعة زيارة») ويعدّلها
   ويعكس/يعيد قيدها اليومي مع تدقيق كامل. إن لم يجدها قطعاً يرجع
   `moneyNote` تحذيري للواجهة (toast أحمر) والمال لا يُلمس.
3. **حذف الزيارة المدفوعة يحذف كل شيء (قرار المالك)** — الكلفة تُخصم
   والدفعة المرتبطة تُحذف مع عكس قيدها وتدقيقها؛ الغامض يبقى بمال
   سليم + `moneyNote`. (الاستجابة صارت JSON لا 204 لحمل الملاحظة.)
4. **القبض على الفواتير له باب واحد (قرار المالك: نعم يُقبض مال فعلي)**
   — نموذج الفاتورة **بلا حقل مدفوع** والخادم يُسقط `paidAmount` من
   `PATCH /api/invoices/:id`. الباب الوحيد
   `POST /api/invoices/:id/collect`: ينشئ **صف دفعة حقيقياً**
   (`invoice_id` مربوط، ملاحظة «قبض فاتورة رقم X») فيراه «الوارد»
   والتقرير اليومي، يحدّث `paidAmount` والحالة (partial/paid)، يرفض
   ما يفوق المتبقي، مع قيد يومية وتدقيق. زر «قبض» في صفحة المحاسبة.
   (الرصيد التلقائي من دفعات سابقة عند **إنشاء** الفاتورة باقٍ كما هو.)
5. **تعديل الدفعة صار مدقَّقاً ومقيَّداً** — `PATCH /api/payments/:id`
   يكتب `audit_log` بالقيم القديمة/الجديدة ويعكس القيد اليومي القديم
   وينشئ الجديد؛ و`session-info` (إعادة الوسم) يدقَّق أيضاً لأنه ينقل
   المال بين الأقسام.

مثبَّت حياً على Postgres محلي: السيناريوهات السبعة (إنشاء/تعديل/حذف
زيارة مدفوعة، القديم المطابق، الغامض، القبض الجزئي/الكامل/المرفوض،
تعديل دفعة) ومجموع قيود الدفتر = `total_cost` في كل خطوة.
ملاحظة معلومة بلا إصلاح: تغيير تاريخ تسجيل مريض لا يحرّك تاريخ قيده
الافتتاحي (نادر، عرضه فقط).

### تصحيحُ سعر جهازٍ بعد البيع (2026-08-22)

**سعرُ جهازٍ بِيع ليس ثابتاً لمجرّد أن التصنيع بدأ. ما دام السعرُ التجاريُّ
أصلُه المعاينة، فتصحيحُ محرِّرٍ مخوَّلٍ ينتقل ذرّياً إلى الحلقة بعينها
وإلى المجاميع المالية. أمّا التجاوزاتُ والخصوماتُ التجارية الصريحة فتبقى
هي صاحبةَ السلطة.**

**الواقعة**: وقّع الطبيبُ ١,٧٠٠,٠٠٠ وهو يقصد ١,٧٥٠,٠٠٠؛ اشترى المريضُ وبدأ
التصنيع، ثمّ اكتُشف الخطأ فوجد الحقلَ مقفلاً. فصُحّح الرقمُ من شاشة تعديل
المريض العامّة: تحرّك `patients.total_cost` وحدَه وبقيت المتابعةُ والحلقةُ
وكلفةُ الخيط على الخطأ. **بابٌ مقفلٌ لم يمنع التصحيح — منع أن يقع في مكانٍ
واحد.**

**والسؤالُ صار «ممّن يأتي الرقم؟» لا «أبِيع أم لا؟»**:
- `price_source = 'exam'` ⟶ يُصحَّح ويُنزَل.
- `manager_set` / `approved_change` ⟶ تُصحَّح المعاينةُ وحدها، مع عبارةٍ
  صريحة: «تم تصحيح سعر المعاينة، لكن السعر التجاري المعتمد بقي كما هو لأنه
  عُدّل بقرار تجاري مستقل». **ولا يُظنّ أن التجاريَّ تحرّك وهو لم يتحرّك.**

**وستّةُ مواضعَ تتحرّك في معاملةٍ واحدة** (`applyExamPriceCorrectionAfterSale`)
ومعها نسخةُ المعاينة وتدقيقُها: `post_exam_followups.approved_price` ·
`patient_device_episodes.agreed_cost` (**بلا مسّ الحالة** — المسلَّمُ يبقى
مسلَّماً ولا يعود إلى المصنع، ولذلك حُقنةٌ ضيّقة `correctEpisodeAgreedCost`
لا `markEpisodeInManufacturing`) · `patient_cases.cost` و`patients.total_cost`
**بالجمع لا بالكتابة فوق** · قيدُ `cost_entries` واحدٌ موقَّعٌ مؤرَّخ
(`source = 'exam_price_correction'`، **والأصلُ لا يُعاد كتابتُه ولا يُحذف**) ·
وحدثُ `exam_price_corrected` موسوماً `afterSale` بالسبب.

**والهويّةُ من السلسلة**: معاينة ⟶ متابعة ⟶ حلقة ⟶ خيط. **لا بـ(مريض +
قسم) أبداً** — العائدُ يملك أكثر من جهاز.

**وما لا يُمَسّ**: الدفعاتُ لا تُعاد كتابتُها في أيّ اتجاه (لا ردَّ ولا حذف) ·
وأمرُ التصنيع كما هو (لا أمرَ جديد ولا مرحلةَ ولا موعدَ تسليمٍ يُصفَّر ولا
خبيرَ يُعاد إسنادُه).

**والحُرّاسُ تحت القفل** (٤٠٩ ولا نصفَ كتابة): المتابعةُ لهذه المعاينة بعينها ·
المصدرُ ما زال `exam` · الحلقةُ موجودةٌ ولهذا المريض وخيطِه وحالتُها
`in_manufacturing` أو `delivered` · **ولا طلبَ خصمٍ معلَّقٌ بمرجع هذا الجهاز**
(`deviceDiscountRefs` — ومعلَّقُ جهازٍ آخر لا يمنع) · **وإن اختلف
`approved_price` عن `agreed_cost` أصلاً** فالملفُّ يحتاج مراجعةً إدارية ويُردّ:
إضافةُ فرقٍ فوق خللٍ تُنتج رقماً ثالثاً لا يفسّره أحد.

**ولا يُقارَن `total_cost` بشيء**: المالكُ صحّح ملفّاتٍ يدوياً من الشاشة
العامّة، فمريضٌ صحيحُ الحلقة لا يُمنَع لأن مجموعَه لا يطابق فرضاً — **يُضاف
الفرقُ ولا يُدّعى علمٌ بالماضي، ولا شيفرةَ إصلاحٍ تخمينية.**

**وبيعٌ بلا هويّة جهاز** (متابعةٌ قديمة بلا `device_episode_id`) **يبقى
مجمَّداً بصدق**: لا مكانَ نظيفاً يُنزَل عليه الفرق.

**والسببُ إلزاميّ** (`priceCorrectionReason`) لتصحيحِ ما بعد البيع —
**يفرضه الخادم** لا الشاشةُ وحدها. **والصلاحيةُ يومَ كُتب هذا القسم**: نفسُ
طرفَي «تعديل المعاينة» — صاحبُها أو المدير المسؤول؛ وطبيبٌ آخر بنفس الاختصاص
والاستقبالُ والمحاسبُ والخبير يُردّون.

> **⚠ (٢٠٢٦-٠٨-٢٨) — ضاق الطرفُ الأول**: صاحبُ المعاينة لم يعد أحدَ طرفَي
> **الحقل التجاريّ** بعينه (القسم 4.h، تنقيحٌ ثانٍ): يبقى يملك تعديلَ
> معاينته سريرياً، لكنّ `deviceCost`/`proposedExpertUserId`/
> `priceCorrectionReason` التي يرسلها تُتجاهَل بصمت — لا تُطبَّق ولا
> تُزامَن، ولا تُقفَل الكتابةُ السريرية بسببها. **الطرفُ الفعليّ الآن
> لهذا الحقل بعينه: المدير المسؤول وحده** (مسؤولٌ عامّ أو مديرُ فرع) —
> وهذا ما تثبته أمثلةُ هذا القسم بعد تحديثها؛ لم يعد أيٌّ منها يستعمل
> حساب الطبيب لتنفيذ تصحيحٍ فعليّ.

**والشاشة**: نافذةُ «تصحيح سعر بعد البيع» **حُذفت بالكامل من `NewExamDialog`**
(القسم 4.h) مع كلّ حقلٍ تجاريّ آخر — لا قفلَ عامّ بالبيع يُعرَض ولا حقلَ سعرٍ
يُفتَح من هناك بعد اليوم. البابُ الوحيد الباقي نقطةُ `PATCH` مباشرةً،
والمانعُ الحقيقيُّ الوحيد صار طلبَ خصمٍ معلَّقاً (٤٠٩) لا قفلاً واجهياً.

**بلا ترحيل**: `cost_entries.source` عمودُ نصٍّ بلا قيد، فالمصدرُ الجديد لا
يحتاج شيئاً.

اختبارٌ دائم: `npm run test:exam-price-correction` (٧١ تأكيداً حيّاً على
Postgres وعلى النقطة الحقيقية).

### التصحيحُ الإداريّ للعمليات الخاطئة (ترحيل ٠٦٤ — 2026-08-23)

**الأخطاءُ التشغيلية تُصحَّح بتراجعٍ إداريٍّ مدقَّق، لا بحذفٍ مدمّر. المسؤولُ
العامّ يستطيع دائماً تصحيحَ عمليةٍ خاطئة؛ والوقائعُ الطبيةُ والتصنيعيةُ
والماليةُ والتاريخيةُ الحقيقية تبقى محفوظة. والآثارُ المالية تُعكَس بقيودٍ
معاكسة.**

**الواقعة (المريض «علي نزار»)**: مريضُ أطرافٍ قديم عاد ليشتري **قالباً** —
جزءاً لا طرفاً كاملاً. فرُدّ أوّلاً لنقصٍ في ملفّه (ردٌّ صحيح، لكنّه ظهر
`400 missing:["amputationLevel"]` خاماً)، ثمّ تعجّل الموظّفُ وضغط «تم الشراء»
قبل اكتمال المسار. فوُلدت حالةٌ تجاريةٌ وتصنيعية، ورُدّ إلغاءُ المعاينة لأن
بيعاً وقع — **وبقي الملفُّ محبوساً بلا مخرج**.

**وضعان لا ثالثَ لهما** (`/api/admin/operation-reversal/{preview,execute}`):
- **«تراجع عن الشراء فقط»**: الطلبُ والمعاينةُ صحيحان والخطأُ الضغطة. يُبطَل
  الأمرُ إدارياً · تُعكَس الكلفة · تعود الحلقةُ `examined` **بسعرها صفراً**
  · وتعود المتابعةُ `awaiting_patient_decision` بلا رابط شراء · **والمعاينةُ
  تبقى فعّالة** · ويُشترى الطلبُ نفسُه بعدها بشكل صحيح بلا حلقةٍ ثانية.
  (وإرجاعُ الحالة دون السعر كان يجعل الشراءَ التالي يُحسب بفرقٍ صفريّ فلا
  يعود المالُ أبداً — أمسكه الاختبار.)
- **«إلغاء العملية بالكامل»**: العمليةُ كلُّها خاطئة. يُبطَل الأمر · تُعكَس
  الكلفة · تُلغى الحلقة · تصير المتابعةُ `closed_admin_void` · **وتُسحب
  سلطةُ المعاينة بشاهدة ٠٦١ نفسِها** · ثمّ يُفتَح الطلبُ الصحيح فوراً.

**والترتيبُ مقصود**: تُعكَس العواقبُ التجارية **أوّلاً** ثمّ تُسحَب السلطةُ
السريرية — فلا تبقى لحظةٌ تكون فيها المعاينةُ ملغاةً والمالُ قائماً.

**وما لا يُمَسّ أبداً**: **الدفعات** (نقدٌ قُبض؛ يُعكَس البيعُ ويبقى المدفوع،
فيصير للمريض رصيدٌ يُوسَم `requires_financial_settlement` — **ولا ردَّ
تلقائيّ، ولا دفعةَ سالبة**) · **التسليم** (جهازٌ سُلِّم واقعةٌ فيزيائية: يبقى
`delivered`/`completed` بختمه ويخرج من السلطة **بوسم** `admin_void_reversal_id`
لا بتغيير حالته) · **سجلُّ التصنيع** بمراحله وموادّه وخبيره · **المعاينةُ
ونسخُها وملاحقُها** · **والقيدُ الأصليّ** — يبقى ويُضاف معاكسُه.

**والهويّةُ من السلسلة**: متابعة ⟶ معاينة · حلقة · أمر. **لا بـ(مريض + قسم)**
— فمريضٌ بجهازين يُصحَّح المقصودُ وحده.

**والسلطةُ إدارية لا سريرية**: المسؤولُ في كلّ الفروع بلا قيد (لا يُقال له
«لا يمكنك» ولو بدأ التصنيعُ أو سُلِّم الجهاز)، ومديرُ الفرع في فرعه.
**والطبيبُ الموقِّع ليس منهم**: له تصحيحُه السريريّ بقواعده، ولا تُمنَح له
سلطةٌ تجارية لأنه وقّع. والاستقبالُ والمحاسبُ والخبير يُردّون ٤٠٣.

**والذرّيةُ والتكرار**: كلُّ شيءٍ في معاملةٍ واحدة تحت قفل المتابعة ·
والمعاينةُ المسبقة تُعيد **ختمَ حالة** يُقارَن عند التنفيذ فيُردّ البائتُ
٤٠٩ («تغيّرت العملية… حدّث الصفحة»).

**والتفرّدُ للصفقة لا للمتابعة مدى الحياة.** تصحيحُ بيعٍ ينتمي إلى **أمر
التصنيع بعينه**: `uq_aor_work_order_once` (فهرسٌ جزئيّ على `work_order_id`)
يمنع تصحيحين وقيداً سالباً ثانياً على الصفقة نفسِها. **وطلبٌ لم يُبَع قطّ**
لا أمرَ له، فإبطالُه الكامل حدثٌ طرفيٌّ يقع مرّةً: `uq_aor_unsold_followup_void`
على `followup_id WHERE work_order_id IS NULL AND mode = 'full_operation'`.
**ولا تفرّدَ على `followup_id` مدى الحياة**: كان يحبس الطلبَ للأبد — قالبٌ
اشتُري بالخطأ، فتُراجَع الضغطةُ وتعود المتابعةُ `awaiting_patient_decision`،
ثمّ يُشترى صحيحاً بأمرٍ ثانٍ **لا يقبل تصحيحاً أبداً**. و`alreadyReversed`
تعني «العمليةُ القائمة الآن صُحِّحت»: تُقرأ بأمرِ المتابعة الحاليّ
(`converted_work_order_id`)، فتصحيحُ أمرٍ سابق لا يجعل التاليَ يبدو مُصحَّحاً.

**و«تراجعٌ عن الشراء» يُعرَض حين يمكن فعلاً** (`purchaseOnlyPossible`): حلقةٌ
موجودة `in_manufacturing` · وأمرٌ ليس `completed` ولا `cancelled` · ولا واحدَ
منهما مُبطَلٌ سلفاً. أمّا **المسلَّم فـ«إلغاءٌ كامل» وحده**: إعادةُ حلقةٍ
`delivered` إلى `examined` تدّعي أن التسليمَ لم يقع. **والخادمُ يفرضها**
فيردّ ٤٠٩ على طلبٍ ملفَّق ولو لم تعرضها الشاشةُ قطّ. **والسلطةُ لم تُقيَّد**:
المسؤولُ يصحّح دائماً — المرفوضُ وضعٌ متناقضٌ لا تصحيحٌ.

**ونتائجُ الكتابة تُقرأ لا تُهمَل**: `revertEpisodeToExamined` و
`voidOrderAdministratively` تعودان بحقيقةِ ما وقع، فإن لم تُرجَع الحلقةُ
المتوقَّعة بعينها أو صار الأمرُ طرفياً بين المعاينة والقفل ⟶ ٤٠٩ **وتراجعٌ
كامل**، ولا نصفُ تصحيح.

**والإذنُ يُفحَص تحت القفل**: فحصُ الفرع قبل فتح المعاملة نافذةٌ مفتوحة —
قد يُنقَل المريضُ بينهما. فيُمرَّر نطاقُ الجلسة الحيّ إلى المنسّق، ويُقرأ فرعُ
المريض من صفّه **المقفول** داخل المعاملة نفسِها: مسؤولٌ ⟶ يمضي · مديرُ فرعٍ
خرج المريضُ من نطاقه ⟶ ٤٠٣ بلا كتابة. والفحصُ في النقطة يبقى ردّاً مبكّراً
للاستقبال والمحاسب لا الحارسَ الأخير. **و`stateStamp` إلزاميّ**: طلبٌ بلا ختمٍ
يُردّ ٤٠٠ («أعد فتح نافذة التصحيح لمراجعة الأثر قبل التنفيذ») — فالعقدُ
مراجعةُ الأثر ثمّ تنفيذُ **ذلك الأثر بعينه**، ولا يلتفّ عليه طلبٌ ملفَّق.

**والبابُ الطبيعيُّ لإلغاء المعاينة لم يُمَسّ**: يبقى يردّ ٤٠٩ متى وقع بيع.
والمُصحِّحُ يشارك كتابتَه عبر `writeExamCancellation` الداخلية — **ولا راية
`skipSoldGuard` عامّة**، فشرطُها المكتوب أن العواقبَ التجارية عُكست سلفاً في
المعاملة نفسِها.

**وبياناتُ المريض الإلزامية لم تُضعَف بحرف**: الحراسةُ في الخادم كما هي.
الذي تغيّر أن الشاشة تترجم الردَّ المنظَّم إلى عملٍ ممكن — نافذةُ «ملف المريض
يحتاج استكمال» تسمّي الناقصَ بالعربية من `FIELD_LABELS`، وتفتح تعديلَ
المريض بضغطة، **وتحفظ اختيار الموظّف** ليعود إليه بلا أن يبدأ من أوّله.

**والاختيارُ يعيش فوق مَن يُفكَّك.** «إكمال البيانات الآن» يفتح المسارَ القائم
`/patients/:id/edit`، وتغيُّرُ المسار **يفكّك الصفحةَ والموزِّعَ والنافذةَ
معاً** — فأيُّ `useState` يموت، والوعدُ بحفظ الاختيار كان يموت معه. فاللقطة
`{serviceType, requestedItem}` في `sessionStorage` (`device_flow_resume.ts`)
يحفظها **الموزِّع** قبل المغادرة، ويقرؤها عند التركيب فيعيد فتح النافذة
مضبوطةً على «قالب» كما تُركت. **وتُستهلَك مرّةً واحدة**: القراءةُ تمسح دائماً،
ولا تُعطى لمريضٍ آخر، ولا تلاحق الموظّفَ في تحميلٍ لاحق. **ولا تُسقط طلبَ
جهاز**: تخزينٌ ممنوع (تصفّحٌ خاصّ) يعني «بلا استئناف» لا تعطُّلاً.

**والجزءُ ليس طرفاً ثانياً** (٠٦٠ محفوظ): `socket` حلقةٌ جديدة بما طُلب،
والجهازُ المسلَّم القديم **تاريخٌ لا حاجز**. والطلبُ القائم يُردّ بسياقٍ
منظَّم (`active_device_operation` + معرّفَي الحلقة والأمر) فتبني الشاشةُ عليه
«فتح العملية الحالية» و«تصحيح / إلغاء العملية».

**و«فتح العملية الحالية» يفتحها فعلاً**: أمرُ تصنيعٍ قائم ⟶ **صفحةُ الأمر
نفسِها** `/manufacturing/orders/:id`؛ وحلقةٌ بلا أمرٍ بعد ⟶ بطاقةُ «قرار
المريض بعد المعاينة» في الصفحة نفسِها بمرساتها. **ولا عارضَ ثانٍ للعملية** —
الوجهتان قائمتان قبل هذه التمريرة، والقرارُ بينهما دالّةٌ خالصةٌ مُختبَرة.

**والأبوابُ الثلاثة كاملة، ونافذةٌ واحدة**: بطاقةُ المعاينة · بطاقةُ قرار ما
بعد المعاينة · بطاقةُ التصنيع. كلُّها تفتح `AdministrativeReversalDialog`
نفسَها، **ولا واحدةٌ منها تنادي نقطةً** — النافذةُ وحدها تنادي `preview`
و`execute`. والهويّةُ دقيقةٌ في كلٍّ: متابعتُها · أمرُ الصفّ المضغوط بعينه ·
ومتابعةُ تلك المعاينة. **وبطاقةُ المعاينة لا تُظهره إلّا لمعاينةٍ لها عمليةُ
جهاز**: الخادمُ يرسل `reversalFollowupId` من `post_exam_followups.
medical_exam_id` — الرابطُ الذي كتبه التوقيع — فمعاينةُ علاجٍ طبيعي تصل
بـ`null` ولا زرَّ عليها. **والأمرُ المُبطَل إدارياً** يُعرَض بوسم «ملغاة
إدارياً» ولا يُفتَح عليه بابٌ ثانٍ.

**والرصيدُ السالب يُقال رصيداً**: «رصيد للمريض: X د.ع» بالأخضر لا «المتبقي:
−X» بالأحمر.

#### النيّةُ بدل الوضع، والاستبدالُ ذرّياً (2026-08-24)

**الموظّفُ يصف الخطأ، والخادمُ يختار آليّةَ العكس.** كانت النافذةُ تسأل
سؤالين — «ما الخطأ الذي وقع؟» ثمّ «نوع التصحيح؟» — فتطلب منه أن يترجم خطأً
يعرفه إلى آليّةٍ لا يعرفها، ثمّ تعاقبه إن أخطأ الترجمة. فصار سؤالاً واحداً
بأربع نوايا في `shared/administrative_reversal.ts`، و`CORRECTION_INTENT_MODE`
و`CORRECTION_INTENT_REASON` **خريطتان واحدتان يقرؤهما الطرفان** — لا سلسلةَ
`?:` في النقطة تنحرف عن الشاشة يوماً.

**والعنوانُ يقول الأثرَ ولا يجمّله**: «تم إنشاء أمر التصنيع بالخطأ» وحدها
كانت تُوهم أن الأمرَ وحده يُلغى، **ولا مسارَ ضيّقٌ كهذا في النظام**. فصارت
تقول «— تُلغى العملية بالكامل»، ويقول أثرُها صراحةً «لا أمر التصنيع وحده».

**والنوايا المتاحة من الخادم لا من قائمةٍ ثابتة**: `purchase_mistake` حين
يمكن التراجعُ فعلاً · `replace_requested_item` حين توجد حلقةٌ وخيطٌ وبديل ·
`work_order_mistake` حين يوجد أمرٌ أصلاً · و`cancel_operation` دائماً.
**وخيارٌ يعرضه الخادمُ ثمّ يردّه هو نفسُه ليس خياراً بل فخّ** — فالدالّةُ
التي تقرّر العرضَ (`replacementPossible`) هي بعينها التي تحرس التنفيذ.

**والاستبدالُ: طرفٌ كامل ⟶ قالب.** يُبطَل الخاطئ وتُعكَس كلفتُه، **ثمّ
تُفتَح حلقةٌ جديدة** بالمطلوب الصحيح `awaiting_exam` بكلفةٍ صفر — **ولا
يُعدَّل الصفُّ القديم أبداً**: هو يقول بصدقٍ إن طرفاً كاملاً طُلب يوماً.
والجديدُ **لا يرث شيئاً**: لا سعرَ ولا خبيرَ ولا معاينةَ ولا دفعةَ ولا أمرَ
تصنيع، ويمرّ بدورة المعاينة من أوّلها.

**والحلقةُ تُنشأ في طبقتها** — `createReplacementEpisodeTx` في
`server/device_episodes/store.ts`، تأخذ **معاملةَ المُستدعي** وتحمل ثوابتَ
`startDeviceEpisode` نفسَها: المريضُ موجود · الخيطُ لهذا المريض ومن هذا
النوع · قفلُ صفّ الخيط · لا حلقةَ مفتوحةٌ ثانية · لا أمرَ بناءٍ قديمٍ نشط ·
التصنيفُ واشتقاقُ الجزء من الملفّ المشترك · والتسلسلُ يُحسَب تحت القفل.
**وتُنادى بعد الإبطال لا قبله** — وذاك شرطُ صحّةٍ لا ترتيب. **وفشلُها يُرجع
كلَّ شيء**: صفَّ التصحيح والقيدَ المعاكس وإبطالَ الأمر وإغلاقَ المتابعة
وإلغاءَ المعاينة معاً. ولا `INSERT INTO patient_device_episodes` في منسّق
التصحيح — يحرسه `test:device-episodes` و`test:correction-ux` معاً.

**والحاضرُ والتاريخُ يُقرآن معاً لا أحدُهما**: بطاقةُ «طلب جديد» (المطلوب ·
بانتظار المعاينة · بلا كلفة) فوق بطاقةِ «عملية ملغاة إدارياً» الرمادية
(الطلبُ السابق · السعرُ المعكوس · أمرُ التصنيع السابق · السببُ ومَن ومتى).

**وهويّةُ الطلب الجديد بالمعرّف لا بالترشيح**: المريضُ يملك خيوطاً متوازية —
أطرافٌ ومساند — فـ«أوّلُ حلقةٍ مفتوحة» كانت تعرض **مسندَه** بوصفه الطلبَ الذي
وُلد عن تصحيح طرفه. والهويّةُ الدقيقة كتبها التصحيحُ في حمولة حدثه داخل
معاملته: `replacementEpisodeId`، يقرؤها `replacementEpisodeIdOf` في
`shared/followup_events.ts` وتُطابَق بالمعرّف على نقطة الحلقات القانونية.
**ولا يُخمَّن بديلٌ حين لا يوجد**: إلغاءٌ كاملٌ بلا استبدال ⟶ البطاقةُ
التاريخية وحدها، ولو كان للمريض حلقةٌ مفتوحةٌ أخرى. **والصمتُ أصدقُ من
ترشيح.** والبديلُ المُعايَن لاحقاً يبقى هو بعينه — الهويّةُ بالمعرّف لا
بالحالة.

**والتصحيحُ الأخير هو القائم**: `getEvents` تُرجع `ORDER BY id DESC` —
الأحدثُ أوّلاً — فالحسمُ **بأكبر `id`** لا بترتيب المصفوفة (مسحٌ يكتب فوق
نتيجته كان يُبقي **الأقدمَ** فائزاً، عكسَ المكتوب). **وإلغاءٌ كاملٌ بلا
استبدالٍ يتلو استبدالاً ⟶ `null`**: حلقةٌ أبطلها التصحيحُ التالي لا تُعرَض
«طلباً جديداً».
**والملغاةُ لا تُقرأ «تم الشراء» أبداً**: `purchasePresentation` صارت تسبق
الحالةَ الطرفيّة على رقم الأمر التاريخيّ، فتقول «عملية ملغاة إدارياً» و«المعاينة
ملغاة» و«أُغلقت بدون شراء» — والحيّةُ المحوَّلة كما كانت بحرفها.

**والتاريخُ يُقرأ ولا يُفتَح**: البطاقةُ التاريخية **بلا نافذةِ تصحيح** —
التصحيحُ تمّ، وفتحُ نموذجِ تنفيذٍ عليه يَعِد بفعلٍ يردّه الخادمُ بـ«ملغاة
إدارياً بالفعل». فالتفصيلُ سجلُّ إجراءاتٍ مقروء.

**وحقائقُ الحدث إنسانية**: «الطلب السابق: طرف صناعي كامل · الطلب الجديد:
القالب · أمر التصنيع السابق: #٢٤٣ · سبب التصحيح» — من `requestedItemLabel`
القائمة لا من معجمٍ ثانٍ. والحمولةُ اكتسبت `previousRequestedItem` و
`serviceType` (jsonb — **بلا ترحيل**)، ورقمُ الحلقة الداخليّ بقي فيها
للتدقيق ولا يُعرَض.

**والملخّصُ يُقرأ والتفصيلُ يُطوى**: «سيتم: ✓ إلغاء العملية الخاطئة ✓ عكس
كلفتها ✓ الاحتفاظ بالسجل السابق ✓ فتح طلب جديد: القالب» ظاهرٌ قبل التأكيد،
وتفصيلُ الأسطر كلِّه تحت «تفاصيل ما سيحدث». **والعقدُ لم يضعف**: معاينةٌ ⟶
`stateStamp` ⟶ تنفيذُ ذلك الأثر بعينه، والخادمُ يعيد الفحصَ تحت القفل.

اختبارٌ دائم: `npm run test:administrative-reversal` (١٧٥ تأكيداً حيّاً، منها
شكلُ «علي نزار» كاملاً حتى فتحِ طلب القالب، والدفعةُ المحفوظة، والمسلَّم،
وعزلُ الجهاز، والضغطةُ المزدوجة، والختمُ البائت، ومصفوفةُ الصلاحية، وكاسكيد
حذف المريض، وبابُ بطاقة المعاينة بطرفَيه الموجب والسالب، والاستبدالُ كاملاً
بطهارة الحلقة الجديدة وتاريخِ التصنيع والمعاينة، والتزامنُ بأربعة أشكال،
وتراجعُ المعاملة كاملاً حين يُردّ فتحُ البديل، وهويّةُ البديل بالمعرّف مع
خيطٍ موازٍ مفتوح وبلا استبدالٍ وبحلقةٍ مفقودة وبعد المعاينة)، و
`npm run test:correction-ux`
(حالةُ العرض الأربع · حقائقُ الحدث الإنسانية · خريطةُ النيّة · والحارسُ
المعماريّ: لا SQL حلقاتٍ في منسّق التصحيح — بلا قاعدة بيانات)، و
`npm run test:device-flow-resume` (لقطةُ الاستئناف ووجهةُ «فتح العملية
الحالية» وعقدُ الأبواب الثلاثة، بلا قاعدة بيانات).

### هويّةُ الجهاز الأول تُماديَ عند البيع (ترحيل ٠٦٣ — 2026-08-22)

**كلُّ بيعِ جهازٍ يُنشئه التطبيقُ الحيّ يخرج من معاملته بحلقةٍ دقيقة.** لا
أمرَ بناءٍ أوليّ يغادر معاملتَه بـ`device_episode_id = NULL` لمريضٍ له خيطُ
أطرافٍ أو مساند.

**الثغرة (المريض WB-02243)**: ترحيلُ ٠٥٠ ملأ أوامرَ البناء التاريخية
بحلقاتها — **لكنه جرى مرّةً واحدة**. والمسارُ الحيّ ظلّ يبيع **الجهازَ
الأول** بلا حلقة حين لا يفتح له الاستعلاماتُ «طلبَ جهاز» صراحةً. فمريضٌ
جديد سُجّل بعد ٠٥٠، عُويِن واشترى وبدأ تصنيعُه — **وُلد صفُّه «تاريخياً»
لحظةَ ولادته**، فرُدّ عليه تصحيحُ السعر بعد البيع بحجّة «مسارٌ قديم بلا
هويّة جهاز». **ترحيلٌ يجري مرّةً لا يحرس ثابتاً يتجدّد.**

**الإصلاحُ في المصدر**: `ensureFirstDeviceEpisodeForSale` (في
`device_episodes/store.ts` كبقيّة كتابات الحلقة) يُنادى من `confirmPurchase`
**داخل معاملة البيع** — أضيقُ نقطةٍ يَعلم فيها النظامُ يقيناً أن هذه
المتابعةَ بعينها تتحوّل إلى هذا البيع بعينه. **ولا تُنشأ لمجرّد أن المريضَ
مبتور**: الرايةُ نيّةٌ لا طلب. وحالتُها `examined` لا `awaiting_exam` —
المتابعةُ لا توجد إلّا عن معاينةٍ موقّعة. ثمّ يمرّ البيعُ بـ
`assignManufacturing` **نفسِها بلا مسارٍ ثانٍ**، فيرث الأمرُ وقيدُ الكلفة
الهويّةَ نفسَها. **والعائدُ لا يُمَسّ**: متابعتُه تدخل `confirmPurchase`
وهي تحمل حلقتَها، فلا تمرّ بالدالّة أصلاً. والقفلُ على صفّ الخيط +
`uq_pde_case_open` يمنعان حلقتين من ضغطتين.

**وحلقةٌ مفتوحةٌ غيرُ مرتبطةٍ تُردّ ٤٠٩ ولا تُختطَف**: متابعةٌ بلا هويّة +
حلقةٌ مفتوحةٌ على الخيط = **التباس** لا دليلَ فيه — قد تكون طلبَ جهازٍ آخر
أحدثَ، أو تكراراً تشغيلياً، أو حالةً تاريخيةً غيرَ متّسقة. والتقاطُها كان
سيثبّت سعرَ هذه المتابعة على جهازٍ لم يُقصَد ويَنسب أمرَ تصنيعه إليه. **ولا
تُستعمَل «حلقةٌ موجودة» وسيلةَ عدم-تكرارٍ لمتابعةٍ مجهولة الهويّة** — تلك
تُمرّر الالتباسَ صامتاً. الذرّيةُ من قفل المتابعة وقفلِ الخيط والفهارس.

**والمعاينةُ المختومة لا تُفتح لهويّةٍ إدارية**: بدلاً من ملء
`medical_exams.device_episode_id`، صار البحثُ عن المتابعة **بأقوى هويّة**
(`followupForExamPrice`): ① `medical_exam_id` — الرابطُ الذي كتبه التوقيع
② `device_episode_id` ③ ثمّ مخرجُ القديم الآمن. فمعاينةٌ لا تعرف جهازَها
تجد متابعتَها ولو أُصلحت بحلقةٍ بعد ختمها. **ولا يُختار «آخرُ متابعةٍ
للمريض والخدمة» ما دام الرابطُ الدقيق موجوداً.**

**ترحيل ٠٦٣ — هويّةٌ فقط**: يصلح ما وقع قبل الإغلاق بهويّةٍ **حتميّة
واحدة**: `converted_work_order_id ⟶ الأمر` — رابطٌ كتبه البيعُ في معاملته،
لا قرينةَ زمنٍ ولا مبلغ. `agreed_cost = approved_price` (السعرُ الدقيق على
المتابعة التي أنشأت الأمر، لا مجموعُ المريض المتراكم الذي تحفّظ منه ٠٥٠)،
والحالةُ من دورة حياة الأمر بخريطة ٠٥٠ نفسِها، والتسليمُ من ختمٍ حقيقيّ أو
لا يُكتب. والإدراجُ والربطُ **جملةٌ واحدة بـ`RETURNING`** فلا مطابقةَ بختم.
**ولا يتغيّر**: `total_cost` · `patient_cases.cost` · الدفعات · مبالغُ
القيود · مراحلُ التصنيع · **ولا `medical_exams`** إطلاقاً.

**وحارسُ التسلسل — لا يُقلَب تاريخُ الأجهزة**: «MAX + ROW_NUMBER» صحيحٌ فقط
إن كان المفقودُ متأخّراً عن كلّ حلقةٍ قائمة. وإلّا وقعت الحالةُ المعاكسة:
جهازٌ أولُ بِيع في الفجوة بلا حلقة واكتمل، ثمّ فُتح ثانٍ بالمسار الصريح فأخذ
التسلسل ١ — فيصير الأقدمُ «الثاني» والأحدثُ «الأول». فالقاعدة: **لا حلقةَ
على الخيط ⟶ تسلسل ١** · **كلُّها أقدمُ ⟶ MAX+١** · **أيٌّ منها أحدثُ أو
مساويةٌ أو مجهولةُ الختم ⟶ لا يُنشَأ شيء** ويبقى الصفُّ للمراجعة اليدوية.
**ولا تُعاد ترقيمُ حلقةٍ قائمة إطلاقاً** — إصلاحٌ يعيد كتابة تاريخٍ صحيح
ليس إصلاحاً.

**والحالاتُ الجزئية** (لا تُنشئ تسلسلاً فلا تحتاج الحارس): المتابعةُ بلا
حلقة والأمرُ يحملها ⟶ تُربَط · العكسُ ⟶ يُربَط · متطابقان ⟶ لا شيء ·
**مختلفان ⟶ لا يُمَسّ أحدُهما ولا يُخمَّن**.

**والملتبسُ يبقى ملتبساً** (مبدأ ٠٥٠ محفوظ): بلا `converted_work_order_id`
لا حلقةَ تُخترَع، وتصحيحُ ما بعد البيع يبقى يردّ ٤٠٩ بصدق.

اختبارٌ دائم: `npm run test:first-device-identity` (٦٠ تأكيداً حيّاً، منها
شكلُ الإنتاج بالضبط، وحارسُ التسلسل في الاتجاهين، والحلقةُ غيرُ المرتبطة،
وتشغيلُ الترحيل مرّتين، والضغطةُ المزدوجة).

### لا تخصيص قبل المعاينة
**البناء الأول** لأمر تصنيع (تخصيص من السجل أو «بدء التصنيع» من صفحة
المريض) **مقفل حتى توجد معاينة موقّعة** لنفس الاختصاص — يفرضه الخادم
(`hasSignedExam`) في نقطتَي `assign-manufacturing` و`POST
/api/manufacturing/orders`، والواجهة تخفي الأزرار وتشرح السبب.
**الصيانة مستثناة** بطبيعتها: تمرّ عبر نقطتها المستقلة
`maintenance-visit` (جهاز موجود سبق وصفه). فمريض قديم بلا معاينة يحتاج
تخصيصاً ⇒ يفحصه الطبيب أولاً — وهذا هو المسار المصمَّم.

### وسم «تم تحديد …»
شارة خضراء مقابلة للكهرمانية: بمجرّد وجود معاينة موقّعة لاختصاص، يقرأ
الاستعلامات «تم تحديد مسند» فيعرف أن دوره جاء (إسناد خبير + قبض).
مشتقّة من وجود المعاينة لا مخزّنة، فلا مصدر حقيقة ثانٍ يمكن أن ينحرف.

### تحقّق ميداني
شُغّلت الـ migration على Postgres 16 محلي مرّتين (idempotent)، وثُبّت
عملياً: رفض التعديل، بقاء النص الأصلي، نجاح مخرج الطوارئ، وكتابة
الصفّ الجنائي عند الحذف.

---

## 4.c تواصل المريض — **واتساب، وحدها، وتلقائياً** (2026-08-22)

**حفظُ المريض = واتساب جاهزة.** لا ربط، ولا رمز QR، ولا تذكرة، ولا رسالةٌ
ينتظرها المركزُ من المريض. الاستقبالُ يُدخل البيانات والرقم ويحفظ — فتُستحقّ
رسالةُ الترحيب في المعاملة نفسها، ويرسلها العاملُ على الرقم المسجَّل.

السلسلةُ المعمارية لم تتغيّر، وهي ملزمة:

```
حدثُ مريض (أو حفظُ ملفّ) ⟶ صندوقُ صادرٍ دائم ⟶ renderNotification ⟶ العامل ⟶ Meta
```

`patient_events` مصدرُ الواقعة · `patient_notification_deliveries` الصندوق ·
`patient_notifications/render.ts` **كتالوجُ الرسائل الوحيد** ·
`patient_notifications/registration.ts` جهةُ الاتصال وترحيبُ التسجيل ·
`patient_contacts` طبقةُ الهوية.

### تلغرامُ المرضى تقاعد — وتنبيهاتُ المالك **لم تُمَسّ**
شأنان مختلفان تماماً، والخلطُ بينهما كان سيُسكت المالك عن مركزه:

- **تلغرامُ المرضى** (`server/patient_telegram/*`، نقطة الـwebhook، الربط،
  أمر `/id`، الناقل) — **أُزيل بالكامل**. لا جهةَ تُنشأ به، ولا صفَّ يُستحقّ
  له، ولا نقطةَ تستقبله. وصفوفُه التاريخية تبقى في الجدول **خاملة** تُقرأ
  للتدقيق. **ولا ترحيلَ حذفٍ** — جدولٌ خاملٌ أهونُ من ترحيلٍ مدمّر.
- **تنبيهاتُ الإدارة** (`server/notifications/telegram.ts`) — **باقيةٌ كما
  هي بحرفها**: رسالةُ المالك الفورية عند تسجيل كل مريض ما زالت تُرسَل.

### الرقمُ المسجَّل هو الوجهة — **قرارُ منتجٍ صريح**
`patients.phone` هو مصدرُ رقم المستقبِل. ولا يُنتظَر إثباتٌ من المزوّد، ولا
تُصدَر تذكرة. ومعنى صفّ الجهة **بصدق**: «هذا هو الرقمُ الذي أعطاه المريضُ
للاستقبال» — **لا** «هويّةٌ موثَّقة». ولا يُبنى عليها إذنٌ ولا وصول.

**والتطبيعُ من مطبِّع المستودع وحده** (`shared/phone.ts: normalizePhone`،
نقطةُ الخنق القائمة): `07701234567` و`+9647701234567` و`٠٧٧٠…` و`00964…`
كلُّها ⟶ `+9647701234567`، ثم `whatsappDestination` **تنزع علامة الزائد
فقط** ⟶ `9647701234567`. **ولا خوارزميةَ ثانية**، وإلّا صار للمريض رقمان لا
يتطابقان وهما واحد. ورقمٌ لا يُطبَّع ⟶ **لا جهةَ ولا رسالة**، ولا تخمين.

### الموافقة — مربّعٌ واحد، بلا اعتماد (ترحيل ٠٦٢)
`patients.whatsapp_notifications_enabled` (+ `whatsapp_consent_at` و
`whatsapp_consent_by_user_id`). في نموذج التسجيل تحت حقل الهاتف مباشرةً:
«إرسال إشعارات وتحديثات المركز عبر واتساب على الرقم المسجل» — **مرفوعٌ
افتراضاً**، مع سطر «بحفظ الملف سيُستخدم الرقم لإرسال رسائل الخدمة
وتحديثاتها». والختمُ والمُقرِّر **يكتبهما الخادم** لا العميل.

**وافتراضُ القاعدة `FALSE`**: المرضى القدامى **لا يستيقظون على رسالة** بعد
النشر. ولا حلقةَ ترحيل، ولا مسحَ إقلاع، ولا بثَّ جماعيّ — النموذجُ الجديد
وحده يرسل `true`.

### تعديلُ الرقم والراية — نقطةُ خنقٍ واحدة
كلُّها في `storage.updatePatient`، فلا مسارَ يُنسى:
- **تغيّر الرقم والرايةُ مرفوعة** ⟶ القديمةُ تُختَم والجديدةُ تُنشأ،
  **وبلا ترحيبٍ ثانٍ**: تعديلُ رقمٍ ليس تسجيلاً جديداً.
- **أُطفئت الراية** ⟶ تُسحَب الجهاتُ النشطة، فلا يُستحقّ لها شيء.
- **أُعيدت** ⟶ جهةٌ من الرقم الحالي، **وبلا بثٍّ رجعيّ**: أحداثُ الأمس لم
  تُستحقّ لها صفوفٌ أصلاً — والغيابُ أمتنُ من الترشيح.

### قالبان معتمَدان — ولا نصَّ حرّ
كلُّ رسالةٍ **يبدأها المركز**، فلا نافذةَ محادثةٍ تُفترَض. قالبان، ولكلٍّ
عددُ معامِلاته الخاصّ به:
- **الترحيب** — معامِلٌ واحد `{{1}}` = **رمزُ المريض القانونيّ وحده**.
  والصياغةُ الثابتة في Meta: «أهلاً وسهلاً بكم في مجموعة مراكز الدكتور ياسر
  الساعدي (الوارث وبايونك) / تم تسجيل ملفكم بنجاح في نظام المراكز الموحد. /
  سنبقيكم على اطلاع عبر هذا الرقم على المواعيد ومراحل الخدمة الخاصة بكم. /
  رقم ملفكم في نظام المراكز: {{1}} / نتمنى لكم دوام الصحة والعافية».
  **والاسمُ لا يُرسَل**: رسالةٌ تصل رقماً خاطئاً بالاسم تكشف مريضاً، وبالرمز
  وحده لا تكشف أحداً.
- **التحديث** — معامِلان لا واحد: `{{1}}` = **رمزُ المريض القانونيّ**، يُقرأ
  حيّاً بـ`patientCodeOf` وقت الإرسال في `dispatcher.ts` لا من حمولة الحدث؛
  و`{{2}}` = **نصُّ `renderNotification` نفسه**. فالقالبُ صار مربوطاً بملفٍّ
  بعينه لا برسالةٍ عائمة بلا هويّة.

جسمُ قالب التحديث المعتمَد في Meta:
```
تحديث بخصوص الخدمة المسجلة في ملفكم رقم {{1}}.
الحالة الحالية:
{{2}}
هذه رسالة خدمية تخص الخدمة المسجلة مسبقاً.
```
فالكتالوجُ يبقى في الشيفرة لا في لوحة Meta، ولا تُكتب صياغةُ المراحل مرّتين.

**وقالبٌ غير مُعدّ ليس فشلاً**: الصفُّ **لا يُحجَز أصلاً** — لا نداءَ، ولا
عدَّ محاولة، ولا رمزَ خطأ. يبقى `pending` بـ٠ محاولات، وحين يُضبَط الاسمُ
يُرسَل **الصفُّ نفسُه** بلا تدخّل. أما رفضُ Meta لقالبٍ **مُعدٍّ فعلاً** فهو
`whatsapp_template_error` حقيقيّ ويتبع الإعادةَ كغيره.

**والتصفيةُ في SQL قبل `LIMIT` لا بعد الحجز** — وإلّا وقع تجويعٌ حقيقيّ:
الدفعةُ عشرون والترتيبُ بالأقدم، فمركزٌ لم يعتمد قالبَ ترحيبه وعنده ثلاثون
ترحيباً مستحقّاً كان يحجز العشرين الأولى ويعيدها كلَّ دورة — **وتحديثُ
التصنيع خلفها لا يبلغ الدفعةَ أبداً**. والتصفيةُ تُشتقّ من
`WELCOME_NOTIFICATION_TYPES` وحدها (فئةُ «التحديث» مفتوحة فتُوصَف بالنفي)،
ولا كتالوجَ ثانٍ. و`markPendingAgain` بقيت **حزامَ سباقٍ** لتغيّرِ إعدادٍ
بين الحجز والإرسال، لا مساراً عادياً.

### حدُّ المعاملة — **عطلان لا عطلٌ واحد**
- **عطلُ المزوّد** (Meta ساقطة · الشبكة · التوكن · قالبٌ لم يُعتمَد) **لا
  يتراجع عن شيء أبداً**: لا نداءَ شبكةٍ داخل أي معاملة عمل، والإرسالُ كلُّه
  بعد `COMMIT` في العامل. فتسجيلُ المريض ودفعتُه وتصنيعُه لا تُمَسّ.
- **عطلُ القاعدة نفسِها** ليس عطلَ واتساب، **ولا يُبتلَع**: ردٌّ بنجاحٍ على
  مريضٍ رايتُه مرفوعة بلا جهةٍ وبلا ترحيب وعدٌ ضاع بلا أن يعلم أحد.

`storage.createPatient` — **معاملةٌ واحدة**: صفُّ المريض · جهةُ الاتصال ·
صفُّ الترحيب. الثلاثةُ معاً أو لا شيء. ثم `syncPatientCases` وقيدُ الكلفة
كما كانا، ثم `nudgeDispatcher()` «أطلق وانسَ» بعد الحفظ.

`storage.updatePatient` — حين يمسّ التعديلُ الرقمَ أو الراية ولم يمرّر
المنادي معاملةً، تُفتح معاملةٌ تشمل **الصفَّ ومزامنةَ الجهة معاً**. فالحالةُ
المستحيلة — رقمٌ جديد مع جهةٍ قديمة نشطة تذهب إليها تحديثاتُ جهازه —
لا تقع: إمّا الاثنان وإمّا يبقى الملفُّ على حالته القديمة المتّسقة.

**والفشلُ يُقال لا يُترك معلَّقاً**: نقطتا الإنشاء والتعديل تردّان ٥٠٠
برسالةٍ عربية بدل `throw` كان يصير رفضاً غير ملتقَط — فيبقى الطلبُ بلا ردٍّ
وتنتظر موظّفةُ الاستقبال دوّارةً لا تعرف أحُفظ الملفُّ أم لا.

**والترحيبُ مرّةً واحدة بحكم القاعدة**: الفهرسُ الجزئيّ
`uq_pnd_contact_link_type (contact, type) WHERE patient_event_id IS NULL` —
فإعادةُ الطلب، أو تكرارُ النداء، أو إطفاءُ الراية ثم رفعُها، لا تُنتج ثانياً.

### الواجهة
بطاقةُ «تواصل المريض» صارت **سطرَ حالةٍ يُقرأ**: «واتساب — ✓ الإشعارات
مفعلة على الرقم المسجل» أو «الإشعارات متوقفة». بلا QR ولا رابط ولا منتقي
صلة ولا استطلاع ولا زرّ.

**والإدارةُ من بابٍ واحد**: مربّعُ الموافقة نفسُه في **نموذج التسجيل**
(مرفوعٌ افتراضاً) و**نموذج تعديل المريض** (قيمتُه الابتدائية من صفّ المريض،
لا افتراضاً — فملفٌّ قديمٌ مطفأ يبقى مطفأً حتى يؤشّره الموظّف). وكلاهما
بجوار حقل الهاتف، فيُقرَّر الأمران معاً.

### الإعداد — بيئةٌ فقط، **ولا سرَّ في القاعدة**
`PATIENT_WHATSAPP_ACCESS_TOKEN` · `PATIENT_WHATSAPP_PHONE_NUMBER_ID` ·
`PATIENT_WHATSAPP_GRAPH_VERSION` · `PATIENT_WHATSAPP_WELCOME_TEMPLATE_NAME` ·
`PATIENT_WHATSAPP_UPDATE_TEMPLATE_NAME` · `PATIENT_WHATSAPP_TEMPLATE_LANGUAGE`.
**أسماءٌ لا قيم** — والنسخةُ الاحتياطية اليومية تُرسَل بالبريد، فسرٌّ في جدول
هو سرٌّ في صندوق بريد. الأوّلان إلزاميان، ونقصانُهما **يعطّل التكامل نظيفاً
ولا يُسقط التطبيق**. **ولا `VERIFY_TOKEN` ولا `APP_SECRET`**: لا webhook ولا
واردَ بعد اليوم — وما لا يُستعمَل لا يُحفَظ إعدادُه.
ولا يُطبع توكنٌ ولا جسمُ ردٍّ خام: أعمدةُ الخطأ رموزٌ محدودة
(`whatsapp_timeout` · `whatsapp_network` · `whatsapp_api_error` ·
`whatsapp_disabled` · `whatsapp_template_error`)، ورموزُ تلغرام تبقى في
النوع للصفوف التاريخية وحدها.

### الترحيل
**٠٦٢ فقط** — إضافيّ، idempotent: ثلاثةُ أعمدةٍ على `patients` وفهرسٌ جزئيّ.
بلا حذفِ جدولٍ ولا بثٍّ جماعيّ. و`patient_link_tokens` يبقى **خاملاً** بلا
مسارٍ حيّ يصل إليه — جدولٌ غيرُ مستعمَل أهونُ من ترحيلٍ مدمّر.

اختبارٌ دائم: `npm run test:whatsapp` (١٠٢ تأكيداً حيّاً على Postgres وعلى
نقطة التسجيل الحقيقية، **بلا نداءِ شبكةٍ إلى Meta**).

### حالةُ التكامل على الإنتاج — والمتراكمُ تُخطّي بقرار المالك (٢٠٢٦-٠٩-٢٢)

**الشيفرةُ كاملةٌ ومدموجة منذ ٢٠٢٦-٠٨-٢٢** (الطلبات ٢٣٨ و٢٦٨ و٢٧٧)،
**ولم تُرسَل رسالةُ واتساب واحدة قطّ** إلى يوم كتابة هذا السطر. وكلُّ صفٍّ
`sent` في `patient_notification_deliveries` من حقبة تلغرام (أنواعُ `link.*`).

**والسببُ ليس عطباً في الكود**: بيئةُ Render لا تحمل **أيّاً** من متغيّرات
`PATIENT_WHATSAPP_*` الستّة، فيُعطَّل التكاملُ نظيفاً كما صُمِّم —
`dispatchOnce` تعود قبل `claimDue`، فالصفوفُ **لا تُحجَز أصلاً**: تبقى
`pending` بـ`attempt_count = 0` و`last_error_code = null`. (وهذا بعينه ما
تقوله البياناتُ: ٨٤٦ صفّاً بلا رمز خطأ واحد.)

**وحالةُ Meta**: الرقمُ **+964 783 311 1555**، والقالبان **معتمَدان** باسمَي
`bionic_patient_welcome` و`bionic_patient_update`، وقالبُ التحديث بمعامِلين
اثنين — **مطابقٌ لعقد الشيفرة حرفاً** (§4.c أعلاه). والمتبقّي خطوةُ
`Create app` في Meta (ردّت بحظرٍ مؤقّت)، ومنها يُؤخذ `Phone Number ID`
والتوكنُ الدائم. **والتوكنُ يُلصَق في Render مباشرةً ولا يُكتب في محادثةٍ
أبداً** — نسخةٌ في سجلّ محادثة تبقى فيه.

**والأربعةُ تُضبَط في حفظةٍ واحدة**: `PATIENT_WHATSAPP_ACCESS_TOKEN` ·
`PATIENT_WHATSAPP_PHONE_NUMBER_ID` ·
`PATIENT_WHATSAPP_WELCOME_TEMPLATE_NAME=bionic_patient_welcome` ·
`PATIENT_WHATSAPP_UPDATE_TEMPLATE_NAME=bionic_patient_update`. (والإصدارُ
واللغةُ لهما افتراض.)

#### والمتراكمُ الأوّل خُطّي عمداً — ولا يُعاد إرساله
**قرارُ المالك ٢٠٢٦-٠٩-٢٢**: «لا فائدة من إرسال شيءٍ الآن للقدامى، والأفضل
أن نتخطّاهم ونبدأ بالجدد». فـ**٨٤٦ صفّاً** كانت `pending` صارت `skipped`:
٤٢١ ترحيباً لتسجيلاتٍ مضى عليها أسابيع، و٤٢٥ تحديثَ تصنيعٍ كانت ستصل
**خارج تسلسلها** (مريضٌ استلم جهازَه يقرأ «دخل التصنيع» ثمّ «جاهز» ثمّ
«سُلِّم» في دقائق)، ومعها خطرُ تقييم الجودة وحدّ المعدّل عند Meta لرقمٍ
جديد يبدأ بثمانمئة رسالة.

والكتابةُ **صورةٌ طبق الأصل من `markSkipped`** (`status='skipped'`,
`locked_at=NULL`, `updated_at=NOW()`)، مقصورةٌ على
`channel='whatsapp' AND status='pending'`. **ولا صفَّ `sent` ولا تاريخَ
تلغرام ولا مريضَ ولا ديناراً مُسّ.** نُفِّذت في معاملةٍ واحدة: `UPDATE 846`
ثمّ `بقي_معلقا = 0` ثمّ `COMMIT`.

**و`skipped` طرفيّةٌ لا تُحجَز ثانيةً أبداً** — فتشغيلُ التكامل لاحقاً
يبدأ من الجديد وحده. **ولا تُعاد هذه الصفوفُ إلى `pending` بحال**: ذاك
يُطلق بالضبط ما قرّر المالكُ تفاديه.

**وما يتراكم بعد اليوم يُرسَل**: قرابة **١٥ صفّاً يومياً** (مقيسٌ من ٤٢١
ترحيباً على ثلاثين يوماً). فتأخيرُ أيّامٍ لا يضرّ، وشهرٌ يبني متراكماً
ثانياً يحتاج القرارَ نفسَه.

#### وسطرُ الإقلاع كان ميّتاً — فلم يقل شيئاً من هذا (٢٠٢٦-٠٩-٢٢)
`patientWhatsappStatusLine` و`missingPatientWhatsappEnv` كُتبتا لتقولا
**أسماءَ** الناقص عند الإقلاع، **وبقيتا بلا مُنادٍ واحد في المستودع كلِّه**.
وكان `startNotificationDispatcher` يطبع نصّين مكتوبين باليد بدلهما:
«dispatcher idle — WhatsApp not configured» **بلا أن يسمّي المتغيّرَ
الناقص**، و«dispatcher started» **ولو لم يُضبَط اسمُ قالبٍ واحد** — فحالةٌ
لا تُرسِل حرفاً تُقرأ في سجلّ Render سليمةً تماماً.

**فصار سطراً واحداً** يُطبَع **قبل** بوّابة التعطيل (وإلّا لم تُطبع أسماءُ
الناقص أبداً، وهي أوّلُ ما يلزم)، بثلاثة أشكالٍ تقول حالةَ العامل كاملةً:

| الشكل | ما يعنيه |
|---|---|
| `[patient-whatsapp] disabled — missing env: …` | لا مؤقّتَ يبدأ، **والناقصُ مسمّى** |
| `[patient-whatsapp] enabled — templates pending: …` | المؤقّتُ يبدأ، والقالبُ الناقصُ مسمّى؛ وكلُّ دورةٍ تعيد قراءةَ القوالب فيبدأ الإرسالُ من تلقائه |
| `[patient-whatsapp] enabled` | مضبوطٌ بالكامل |

**وأسماءٌ لا قيم** — التوكنُ سرٌّ وسجلُّ Render يُقرأ ويُحفَظ؛ ولا اسمَ
قالبٍ يُطبَع حين يكون مضبوطاً: السطرُ تشخيصٌ لا تفريغُ إعداد.

**وبلا ترحيل** — ولا حرفَ في القاعدة ولا في الطابور ولا في `dispatchOnce`
ولا في `claimDue` ولا في عقد القوالب.

اختبارٌ دائم: `npm run test:whatsapp-status` (٣٠ تأكيداً **بلا قاعدة بيانات
ولا شبكة**: الأشكالُ الثلاثة بحالاتها الثمانية · والناقصُ الإلزاميُّ يعلو
على القوالب · وأسماءٌ لا قيم بثلاث بيئات · والبياضُ يُقرأ غياباً ·
**وعقدُ الإقلاع**: النداءُ واقعٌ فعلاً وقبل البوّابة، والنصّان المكتوبان
باليد زالا، وسطرٌ واحد لا سطران، و`server/index.ts` ينادي الدالّةَ فعلاً ·
ونقاءُ `config.ts` من كلّ استيراد). **ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ
آليّةٍ على حدة**: حذفُ النداء يُسقط **٣ بالضبط** (هـ٢ · هـ٣ · هـ٥) ·
ونقلُه بعد البوّابة يُسقط **هـ٣ وحدَه** · وإعادةُ النصّين يُسقط **هـ٤
وهـ٥**. ومعه **إثباتٌ حيّ عبر `startNotificationDispatcher` نفسِها** في
خمس بيئات: الأسطرُ الخمسة تُطبَع كما في الجدول أعلاه، **ولا قيمةَ توكنٍ
فيها**.

---

## 4.d مسارُ العملية — والسؤالُ الذي كان يُجاب عنه بالخطأ (ترحيل ٠٦٥ — 2026-08-24)

**«أتحتاج هذه العمليةُ معاينةَ طبيب؟» سؤالٌ عن العملية، لا عن المريض.**

### الواقعة
`patients.patient_classification` («جديد» / «قديم») كان يُجيب عن سؤالين لا
رابطَ بينهما: **بُعدٌ تقريريّ** في ظاهره («ملفُّه الورقيّ قديم»)، و**قرارُ
إعفاءٍ سريريّ** في باطنه — `isLegacyPatient` تقرؤه فتُسقط عن المريض شرطَ
المعاينة. فموظّفةُ الاستقبال تُسأل عن ملفٍّ وتُجيب بصدقٍ إداريّ، فيمضي
مريضٌ إلى التصنيع **بلا أن يراه طبيب** — أثرٌ لم تقصده ولم تُخبَر به. وحقلٌ
واحد لا يُجاب به سؤالان.

### فصارا اثنين
- **`patients.had_prior_center_history`** — «سبق أن تعامل أو اشترى من المركز
  قبل تسجيله في النظام». **واقعةٌ إدارية وحدها**: لا تُعفي من معاينة · ولا
  تقول إن للمريض جهازاً · ولا حلقةً مسلَّمة · ولا شراءً مؤكَّداً · ولا أيَّ
  استثناءٍ في مسار العمل. **ولا يُشتقّ منها صفٌّ واحد في أيّ جدولٍ آخر.**
  مربّعٌ صريح بجوار حقل الهاتف في التسجيل و«تعديل مريض» معاً — رايةٌ بلا
  بابِ تصحيحٍ فخّ.

  **وثلاثيةٌ بالمبدأ نفسِه الذي في `service_path`**: `NULL` = **لم يُسأل**
  (صفٌّ وُلد قبل ٠٦٥) · `TRUE`/`FALSE` = جوابٌ صريحٌ أعطاه الموظّف. و`NOT
  NULL DEFAULT FALSE` كانت أوّلَ صياغةٍ ثمّ صُحّحت: `FALSE` على ملفٍّ قديم
  تقول «نعلم أنه لم يتعامل معنا»، والحقيقةُ «لا نعلم — لم نسأله».

  **والتسجيلُ الجديد يكتب بولياناً صريحاً دائماً** (لا `NULL` صامتة):
  المربّعُ ظاهرٌ في النموذج، فحفظُه بلا تأشيرٍ جوابٌ حقيقيّ «لا».

  **والتعديلُ لا يكتب إلّا بلمسةٍ**: «تعديل مريض» يعيد إرسال كلّ حقوله في
  كلّ حفظ، فمربّعٌ منعكسٌ من صفٍّ فارغ كان سيكتب `FALSE` عند تصحيح رقم
  هاتف. فالحقلُ **خارجَ النموذج** ومعه رايةُ لمس: لم يُلمَس ⟶ لا يُرسَل ·
  لُمِس ⟶ بوليانٌ صريح. **والخادمُ يُسقط ما ليس بولياناً** (`null`
  والنصوص) فلا تُكتب `FALSE` من طريقٍ آخر. والشاشةُ تقول للصفّ الذي لم
  يُسأل: **«غير محدد — سجل قديم»**.
- **`patient_device_episodes.service_path`** — `exam` / `no_exam`، يُسأل
  **مرّةً لكلّ عملية** عند فتح طلب الجهاز. **وإلزاميّ على الكتابة الجديدة**:
  الغيابُ يُردّ ٤٠٠ ولا يُقرأ افتراضاً في أيّ اتجاه — «معاينة» افتراضاً
  تسوق للطبيب مَن لا يحتاجه، و«بلا معاينة» افتراضاً تُسقط شرطاً سريرياً
  بصمت.

### و`NULL` غيابُ سؤالٍ لا قيمةٌ ثالثة
حلقةٌ وُلدت قبل ٠٦٥ لم يُسأل عنها أحد، ومسارُها الحقيقيّ **غيرُ معلوم**.
فكتابةُ `'exam'` عليها ادّعاءُ علمٍ بالماضي، و`'no_exam'` أسوأ: تُسقط عن
أجهزةٍ حيّةٍ شرطاً وُلدت تحته. فتبقى `NULL` وتُقرأ **بالقاعدة القديمة حرفاً
بحرف**. **ولا كتابةَ جماعية على صفٍّ قائم، ولا `DEFAULT` يكتب على القديم.**
والقارئُ في SQL يستعمل `IS DISTINCT FROM 'no_exam'` لا `<>` — الثانيةُ
تُقيَّم `NULL` فتُعفي بصمتٍ ما لم يُعفِه أحد.

### القرارُ في مكانٍ واحد
`shared/service_path.ts: operationNeedsExam({ servicePath, legacyRuleRequiresExam })`
— ثلاثُ حالاتٍ صريحة (`required` · `not_required` · `legacy_rule`) فلا يختلط
«لا يحتاج» بـ«لا نعلم». **والمسارُ المسجَّل يعلو على تصنيف المريض في
الاتجاهين معاً**: «قديم» بمسار `exam` **يُفحَص**، و«جديد» بمسار `no_exam`
لا يُساق إلى الطابور.

### التصنيفُ يبقى في القاعدة، ويختفي من الشاشات
لا يُحذَف العمود (تقاريرُ الصفوف القائمة تقرؤه)، ولا يُترك فارغاً (تعود
الفئةُ الثالثة بحكم الأمر الواقع). ونقطةُ الإنشاء **تختمه `'new'` ولا تقبله
من العميل** في أيّ اتجاه: قيمةٌ في جسم الطلب تدّعي أن هذا الملفَّ «قديم»
تدّعي معها إعفاءً لم يقرّره أحد. و`'new'` هي الصدقُ (الصفُّ أُنشئ اليوم)
والأمانُ معاً.

**ولا «جديد/قديم» في أيّ شاشةِ عمل** — لا في التسجيل ولا في «تعديل مريض».
إبقاؤه في الثانية كان يُبقي التناقضَ قائماً: الحقلُ نفسُه يقول شيئاً ويفعل
آخر، يراه الموظّفُ نفسُه في بابٍ ثانٍ. **والقيمُ التاريخية تبقى مقروءةً في
الخادم والتقارير كما هي** — لا تُمحى ولا تُعاد كتابتُها. ونقطةُ
`PUT /api/patients/:id` ما زالت تقبل قيمةً صحيحة (توافقاً مع أيّ عميلٍ
قائم) لكن **لا شاشةَ تُرسلها**. ولم تُخترَع في هذه الدفعة أداةُ تصحيحٍ
إداريةٌ بديلة.

### العلاجُ الطبيعي معزولٌ تماماً
الحلقاتُ للأطراف والمساند وحدها (`DEVICE_SERVICE_TYPES`)، فلا مسارَ يُسأل
عنه في العلاج الطبيعي ولا حرفَ تغيّر في تسعيره ولا جلساته ولا خططه.
ويحرسه اختبارٌ معماريّ: لا ذكرَ لـ`servicePath` في `PhysioPlanDialog` ولا
`PhysioPricingDialog` ولا `shared/pricing.ts` ولا `server/new_service/store.ts`.

### الحدُّ المؤقّت لمسار «بلا معاينة» — **رُفع في ترحيل ٠٦٧، اقرأ القسم 4.e**
الطلبُ يُفتَح ويُعرَف ويُوثَّق، **ولا يظهر في طوابير الطبيب ولا يُنشأ له
طلبُ مراجعة** — وهذا باقٍ حرفاً بحرف. أمّا **تسعيرُه واعتمادُه المالي**
فطبقةٌ لم تكن قد بُنيت حين كُتب هذا القسم، **وقد بُنيت في ٠٦٧**: العمليةُ
تُسجَّل ويُنجَز العمل **ويُقيَّد مبلغُه لحظتَه** من الاستعلامات (قرارُ المالك
2026-08-26 — يُلغي اعتمادَ الطبيب الماليّ). والطبيبُ يرى الحركةَ بعد وقوعها
في المراجعة الإشرافية فقط.

**و«تخصيص وإسناد خبير» و«أمر التصنيع» يبقيان يردّان ٤٠٩** كما كانا — لأنهما
يُدخلان المالَ المحاسبةَ لحظتَهما — لكنّ `NO_EXAM_PENDING_BOUNDARY` صارت تدلّ
على البابِ الحقيقيّ («بيع بلا معاينة») بدل «لم يُفعَّل بعد». **ولا التفافَ
عبر إعفاء «المريض القديم»**، ولا أمرَ تصنيعٍ ولا قيدَ كلفةٍ ولا دينارَ يُكتب
لتبدو الشاشةُ مكتملة.

اختبارٌ دائم: `npm run test:service-path` (٦٠+ تأكيداً حيّاً على Postgres وعلى
النقاط الحقيقية، ومعه عقدُ الشاشات وحارسُ عزل العلاج الطبيعي).

### الترحيل
**٠٦٥ فقط** — إضافيّ، idempotent: عمودان و`CHECK` وفهرسٌ جزئيّ. بلا `DROP`
ولا `DELETE` ولا مسٍّ لترحيلاتٍ من ٠٠١ إلى ٠٦٤.

---

## 4.f المرحلةُ الثانية — تفاصيلُ البيع ومالكُ كلّ حقل (ترحيل ٠٦٦ — 2026-08-24)

**معاينة ⟶ تفاصيلُ البيع ⟶ قرارٌ واحد: اشترى / لم يشترِ.**
وللأطراف والمساند **على مسار المعاينة** (`service_path = 'exam'`) وحدها.

> **⚠ «عند التوقيع» هنا وصفٌ تاريخيّ — لم يعد الطبيبُ يُدخل هذه الحقولَ
> الثلاثة من نافذة التوقيع بعد 2026-08-28** (القسم 4.h: معاينةٌ طبّيةٌ
> محضة). **والبابُ `POST /api/followups/:id/commercial` ومالكيةُ الحقول
> الموصوفة أدناه لم يُمَسّا بحرف** — يبقيان حيّين تماماً، تستعملهما بطاقةُ
> المريض اليوم. لكن كونَ الباب يمنح الطبيبَ (`asDoctor`) ملكيةَ الحقل إن
> ناداه مباشرةً **بقاءٌ انتقاليٌّ لا تصميمٌ نهائيّ**: لم يُعَد بناء هذا
> الباب في مرحلة 4.h عمداً (ليُعاد استعمالُه لاحقاً)، لا لأن الطبيب سيظلّ
> يملك دوراً تجارياً. **الهدفُ النهائيّ (4.h): لا دورَ تجاريّاً للطبيب
> إطلاقاً** — الاستعلاماتُ وحدها تملك الخبيرَ والسعرَ الأصليّ ومقدارَ
> الخصم وإتمامَ الشراء و«لم يشترِ» — وهذا البابُ سيُقيَّد أو يُزال حين
> تُبنى مرحلةُ تبسيط مبيعات الاستعلامات.
>
> **⚠ (٢٠٢٦-٠٨-٢٨) — وصلت، القسم 4.i.** هذا البابُ بعينه صار متقاعداً ٤٠٩
> على مسار المعاينة **للجميع بلا استثناءٍ للدور** — لا الطبيب وحده، بل
> الاستقبالُ والمحاسبُ ومديرُ الفرع والمسؤولُ العام سواء (`retiredOnExamPath`،
> حارسُ عقدِ مسارٍ لا قيدَ صلاحية). **البابُ نفسُه لم يُقيَّد بحذف كودٍ
> منه، بل بحارسٍ قبله**، فبقيت آليّتُه (المالكية، الذرّية) كما هي **لصفٍّ
> موروث** — حلقةٌ بلا `service_path`، لا يزال الطبيبُ يستعملها هناك كما
> كان. والبابُ الجديد الوحيد على مسار المعاينة
> `POST /api/followups/:id/complete-sale`، وله **الاستقبالُ والمحاسبُ**
> ومديرُ الفرع والمسؤولُ العام — لا الطبيب.

### الواقعة
كان المسارُ بعد المعاينة يسأل الموظّفَ أسئلةً كثيرة حول واقعةٍ واحدة يعرفها
يقيناً: أيرغب بالشراء؟ أنؤجّله (بأحدَ عشر سبباً)؟ أنعتمد سعرَه؟ أنعتمد
خصمَه؟ أنعتمد الشراءَ نفسَه؟ وكلُّ خطوةٍ طابورٌ ينتظر أحداً.

### فصار السؤالُ واحداً ومعه ثلاثةُ حقول
① **السعر** — عاديّ · بخصم · مجّانيّ. ② **الخبير**. ③ **القرار** — اشترى /
لم يشترِ. **وكلُّها اختيارية عند التوقيع**: السجلُّ السريريُّ يُوقَّع ولو
تُركت فارغةً كلُّها، والاستقبالُ يُكملها.

### ولكلّ حقلٍ **مالك** — محفوظٌ صريحاً لا مُستنتَج
`price_owner` · `expert_owner` · `purchase_decision_owner`، ومعها رقمُ صاحبها
ولقطةُ اسمه. **و«القيمةُ غير فارغة» لا تقول مَن كتبها**: سعرٌ أدخله
الاستقبالُ كان سيُقرأ سعرَ طبيبٍ فيُقفَل عليه.

- **مالكُه الطبيب** ⟶ يعدّله **صاحبُه** أو **المسؤولُ العام** حصراً.
  **ومديرُ الفرع ليس منهم** — قرارُ مالكٍ صريح يضيّق سلطتَه القائمة: توقيعُ
  الطبيب على رقمٍ ليس مسوّدةً يصحّحها مَن يدير الفرع.
- **مالكُه الموظّفون** ⟶ يديره أيُّ مخوَّل بالعمل التجاريّ.
- **فارغٌ** ⟶ يُكمله مَن حضر.

**والمالكيةُ تُكتب مرّةً واحدة**: تصحيحُ المسؤولِ لسعرٍ يملكه الطبيبُ **لا
يحوّله إلى `staff`** — وإلّا صار كلُّ تصحيحٍ إداريّ باباً يُسلّم القرارَ
السريريَّ للاستقبال بلا أن ينتبه أحد. والمُصحِّحُ مسجَّلٌ في الحدث.

**و«بوصف الطبيب» شرطُه أن يكون طبيبَ هذه المعاينة بعينها** (`medical_exams.
doctor_id`) — فطبيبٌ آخر ولو بنفس الاختصاص يكتب بوصفه موظّفاً، ولا يُقفِل
حقلاً على صاحب المعاينة.

**والحراسةُ في المخزن لا في النقطة**: `setCommercialPrice` و`selectExpert`
و`setInitialCommercialPrice` و`closeWithoutPurchase` — الأبوابُ القائمة قبل
هذه المرحلة — تُلزَم بـ`session` في توقيعها، فلا يسقط الحارسُ بالسهو ولا
تبقى نافذةٌ مفتوحةٌ بجوار نافذةٍ تُغلَق.

### «لم يُسعَّر» ≠ «مجّانيّ» — والصفرُ كان يقول الاثنين
`price_kind` هو الدليلُ لا الرقم: `NULL` = لم يُسعَّر · `free` = مجّانيٌّ
صريحٌ بـ`approved_price = 0` و`original_price > 0` (فالتبرّعُ يُقاس بقيمته).
و`setInitialCommercialPrice` كانت تقرأ الصفرَ «غير مسعَّر» فتسمح لأيّ موظّفٍ
أن يكتب فوق تبرّعٍ قرّره غيرُه رقماً موجباً — والشرطُ صار على النوع.

**و`approved_price` يبقى مصدرَ الحقيقة الوحيد للمال** كما كان: هو ما يقيّده
`assignManufacturing`. والجديدُ يقول **ممّ تكوّن** ذلك الرقم — لا سعرٌ ثانٍ.

### ولا اعتمادَ لهذا المسار
سعرٌ أو خصمٌ أو مجّانيّةٌ يُدخلها مخوَّلٌ **نافذةٌ فوراً**: لا صفَّ
`service_discount_requests` جديد، ولا طابورَ «بانتظار اعتماد الخصم».
**والطلباتُ القديمة المعلَّقة تبقى مقروءةً وقابلةً للحسم بمسارها** — وهذا
تقاعدٌ مرحليّ: العلاجُ الطبيعي و«خدمة جديدة» والصيانةُ على نظامها حتى
تُراجَع كلٌّ بأمانها.

### «اشترى» تُحفَظ ولو نقص السعرُ أو الخبير
القرارُ **واقعةٌ مسجَّلة لا حالةٌ في الآلة**، فلا يُسأل المريضُ مرّتين.
والصفُّ يبقى حيّاً ويُشتقّ عرضُه: «اشترى — بانتظار استكمال بيانات البيع —
الخبير». **ولا حالةَ جديدة في `status`** فلا ينكسر قارئٌ قديم.

**وإكمالُ آخرِ ناقصٍ يُتمّ البيعَ في المعاملة نفسِها** — بلا سؤالٍ ثانٍ،
وبالدالّة القانونية نفسِها (`confirmPurchase` ⟶ `assignManufacturing`).
وضغطتان متزامنتان تُنتجان **تحويلاً واحداً وقيدَ كلفةٍ واحداً**: قفلُ
المتابعة + شرطُ الحالة + `uq_pef_active_episode`.

**والبابُ واحد**: `POST /api/followups/:id/commercial` يقفل الصفَّ مرّةً،
ويفحص مالكيةَ كلّ حقلٍ يُلمَس، ويكتب، ثمّ يُتمّ. وثلاثُ نقاطٍ منفصلة كانت
تجعل «آخرَ ناقصٍ يُتمّ البيع» **مستحيلاً ذرّياً** — الجهوزيّةُ تُقاس بين
معاملتين لا داخل واحدة.

### «لم يشترِ» — سببٌ حرٌّ إلزاميّ
`not_bought_reason_text` نصٌّ يُكتب كما قاله المريض. والقائمةُ القديمة
(أحدَ عشر رمزاً) كانت تُجبر الموظّفَ على ترجمةٍ لا تصفه فيختار «سبب آخر».
**والرمزُ الموروث يبقى في `closed_reason`** بالقيمة المحايدة `other` — لا
يدّعي سبباً لم يُقَل، والقاعدةُ والتقاريرُ تقرؤه كما كانت.
ولا تصنيعَ ولا كلفةَ ولا دينار.

### الأفعالُ المتقاعدة — على المسار الجديد وحده
`defer` · `accept-price` · `purchase-interest` تُردّ ٤٠٩ برسالةٍ تدلّ على
البابِ الواحد. **والصفُّ الموروث لا يُحبَس**: حلقةٌ بلا مسار (ما قبل ٠٦٥) أو
متابعةٌ بلا حلقة تبقى على أفعالها كلِّها حتى تنفد.

### وما لم يُمَسّ
- **ختمُ المعاينة**: التفاصيلُ التجارية على **المتابعة** لا على
  `medical_exams` — والوصفةُ السريرية تبقى مختومةً كما هي.
- **تصحيحُ السعر بعد البيع** (٢٣٩) و**التصحيحُ الإداريّ** (٢٤١/٢٤٢): بعد
  البيع تُردّ نقطةُ التفاصيل ٤٠٩ وتدلّ على بابهما — **ولا مسارَ تراجعٍ ثانٍ**.
- **حدُّ ٠٦٥ لمسار «بلا معاينة»**: كما هو، بلا مالٍ ولا تصنيع.
- **العلاجُ الطبيعي**: الحقولُ تُسقَط من جسم الطلب إسقاطاً — لا تُعرَض ولا
  تُقبَل، ولا حرفَ تغيّر في تسعيره ولا جلساته ولا خططه.
- **واتساب وتلغرام**: بلا تغيير.

### الترحيل
**٠٦٦ فقط** — إضافيّ، idempotent: ١٤ عموداً على `post_exam_followups` و
سبعةُ قيود وفهرسٌ جزئيّ. كلُّها تولد `NULL` على الصفوف القائمة (وهو الصدق:
لم تُسجَّل لها هذه الوقائع). بلا `DROP` ولا `DELETE` ولا مسٍّ لترحيلٍ من
٠٠١ إلى ٠٦٥.

اختبارٌ دائم: `npm run test:exam-commercial` (٦٠ بنداً حيّاً على Postgres
وعلى النقاط الحقيقية، ومعه عقدُ الشاشات).

---

## 4.e مسارُ «بلا معاينة» — العمليةُ والمالُ من الاستعلامات (ترحيل ٠٦٧ — 2026-08-24، والقاعدةُ صُحِّحت 2026-08-26)

**القاعدةُ الحاكمة (قرارُ المالك 2026-08-26 — يُلغي ويستبدل ما قبله):
العمليةُ والمالُ يمضيان من الاستعلامات، والطبيبُ يراجع الحركةَ إشرافياً
فقط.**

> **⚠ القاعدةُ القديمة «العمليةُ تمضي والمالُ ينتظر» لم تعد سارية.** وما
> يلي يصف السلوكَ القائم اليوم؛ وحيثما ذُكر الاعتمادُ الطبّيُّ للمال فهو
> **تاريخٌ يُقرأ لا عقدٌ يُنفَّذ**.

### الواقعة
ترحيلُ ٠٦٧ جعل الاستقبالَ يُنجز العملَ ويبقى **المبلغُ** في صفٍّ معلَّق
خارج المحاسبة حتى يعتمده طبيبٌ مخوَّل. وأثرُها العمليّ أن **مالاً مشروعاً
وقع فعلاً** — قالبٌ بِيع أو ركبةٌ صُلّحت — يبقى خارج الدفتر والتقارير
والمتبقّي إلى أن يفرغ الطبيبُ لشاشةٍ ماليّةٍ ليست من عمله. فالطابورُ يمتلئ
بما لا قرارَ سريرياً فيه، والمريضُ يقرأ رصيداً لا يطابق ما دفع.

**ومَن يعرف السعرَ هو مَن اتّفق عليه**: الاستقبالُ يكلّم المريض ويقول له كم.
فصار المبلغُ الذي يُدخله **نهائياً لحظتَه**.

### فالمالُ يُقيَّد في معاملة العملية نفسِها
**ولا صفَّ معلَّقٌ جديد يُنشأ إطلاقاً.** والكُتّابُ القانونيّون القائمون
أنفسُهم، بلا نسخةٍ ثانية من المحاسبة:

- **بيعُ جزء** ⟶ `startDeviceSaleOperationallyTx` تفتح الأمرَ وتنقل الحلقة،
  ثمّ `applyDeviceSaleFinancialsTx` **في المعاملة عينها** تقيّد كلفةَ
  المريض والحالةِ والحلقةِ وقيدَ الدفتر. **وهما نصفا `assignManufacturing`
  اللذان يستعملهما مسارُ المعاينة بحرفهما وترتيبهما.**
- **صيانة** ⟶ `createMaintenanceOrderWithVisit` بأجرها الحقيقيّ، وهي تنادي
  `postMaintenanceFee` داخل معاملتها — **الكاتبُ عينه** الذي تناديه الصيانةُ
  كاملةُ الأجر من نقطتها القائمة.

**ومرّةً واحدة بالضبط**: المالُ داخل معاملة فتح الأمر، فحُرّاسُ الأمر النشط
وقفلُ الخيط و`uq_pde_case_open` و`uq_pwo_one_open_maint_per_episode` — التي
تمنع أمراً ثانياً — تمنع قيداً ثانياً معه. ضغطتان متزامنتان ⟶ الثانيةُ ترتدّ
قبل أن تكتب ديناراً، **ولا نصفَ كتابة**.

**و«بلا أجور» كما هي**: لا قيدَ ولا كلفةَ ولا دينار — والواقعةُ محفوظةٌ
صراحةً على أمر العمل (`no_exam_no_charge`) لا يُستدَلّ عليها بغياب صفّ.

### والطبيبُ يرى الحركةَ **بعد** وقوعها
يُوجَّه إلى **«مراجعة حركة مرضى الأطراف والمساند»** القائمة سجلٌّ
**استرجاعيّ**، بعد أن تصير العمليةُ صحيحةً مالياً وتشغيلياً:

- **صيانة** ⟶ `reviewKind = maintenance` · `requestedPath = quick`.
- **بيعُ جزء** ⟶ `reviewKind = other` — **ولا تصنيفَ سريريٌّ يُخترَع**: ليس
  «جهازاً جديداً» (ذاك يستوجب معاينةً كاملة) ولا صيانةً ولا تعديلاً.
  والعمليةُ تُكتب بالعربية في `receptionNote` من العناوين المشتركة:
  القسمُ · الجزءُ أو المُباع · منشأُ الجهاز · الخبيرُ · المبلغُ أو «بلا أجور».

**ويُنادى بعد `COMMIT`** — فلا يصير بوّابةً تحجب عملاً مشروعاً ومالاً
مستحقّاً. **وهو إخبارٌ لا إذن**: لا يحجب عمليةً ولا تصنيعاً ولا كلفةً ولا
محاسبةً ولا تقريراً.

**ويُعاد مرّةً واحدة، ثمّ يُقال الفشلُ للموظّف — ولا يُبتلَع صامتاً:**

- **محاولتان لا أكثر**: انقطاعُ شبكةٍ عابرٌ لا يستحقّ سجلّاً ضائعاً، وحلقةُ
  إعادةٍ مفتوحة تحبس الردَّ عن موظّفٍ ينتظر.
- **والإعادةُ لا تُنتج صفّاً ثانياً**: تمرّ بـ`ensureReviewRouting` نفسِها،
  وهي تُمسك ٤٠٩ وتُرجع الطلبَ المعلَّق القائم (فهارسُ التفرّد الجزئية هي
  الحارس). فمحاولةٌ أولى نجحت ثمّ ضاع ردُّها ⟶ الثانيةُ تقرأ الأولى.
- **ولا يُعاد شيءٌ ماليٌّ أو تشغيليّ**: لا أمرَ تصنيعٍ ولا قيدَ ولا دينار —
  المُعادُ نداءُ السجلّ وحده.
- **وإن فشلت الاثنتان**: العمليةُ والمالُ **نهائيّان كما هما**، ويُرفَع
  `reviewRouted: false` فتُظهر الشاشةُ **تحذيراً ظاهراً** بصياغةٍ تتبع ما
  وقع — «تم حفظ العملية والمبلغ بنجاح…» أو «تم حفظ العملية بلا أجور
  بنجاح…» — ولا تَعِد بمالٍ لم يقع.
- **ويُقال للموظّف صراحةً: «لا تُعِد تسجيل العملية»** — تكرارُها يُنتج ٤٠٩
  في أحسن الأحوال، وعمليةً ثانية في أسوئها. والمخرجُ إبلاغُ المسؤول.
- **ونجاحٌ لا يُميَّز عن نجاح** كان يُخفي عن الموظّف أن حركةً لن تصل شاشةَ
  الطبيب — فلا يبلّغ أحداً، ولا يعرف أحدٌ أنها ضاعت.

**و«تمت المراجعة» اعترافٌ لا موافقة** (القسم 4.ب): لا تُحرّك ديناراً ولا
أمرَ تصنيع. وإن أحال الطبيبُ لاحقاً إلى معاينةٍ كاملة فذاك **قرارٌ سريريّ
مستقبليّ** لا يعكس مبلغاً قُيِّد ولا أمراً فُتح ولا تصنيعاً جرى ولا واقعةً
تاريخية. **ولا معاينةَ تُنشأ** بسبب هذا السطر، ولا `service_path` يتغيّر.

### والصفوفُ المعلَّقة تاريخٌ لا مسار
`pending_service_charges` وأحداثُها **تبقى بصفوفها كما هي بايتاً بايت**:
لا تُحذَف ولا تُعتمَد تلقائياً ولا يُقيَّد مالُها بترحيل. **ولا ترحيلَ
لأجلها إطلاقاً.**

- **والمفتوحُ منها** (`pending_review` / `returned`) **لم يعد ينتظر طبيباً**
  — وإلّا بقي معلَّقاً إلى الأبد بعد أن خرج من هذا المسار.
- **ويُنهيه الاستقبالُ ومديرُ الفرع والمسؤول** ضمن نطاق فروعهم، بالبوّابة
  نفسِها التي تفتح الإنشاء (`canOperateNoExam`) — ولا بوّابةَ ثالثة.
- **والآليّةُ هي نفسُها المُختبَرة**: `approveCharge` بقفلها وشرطِ حالتها
  و`applied_at` حارساً، تنادي الكاتبَ القانونيَّ عينه. والإعادةُ والتصحيحُ
  وإعادةُ الإرسال كما كانت.
- **والقائمةُ بنطاق الفرع وحده** — لا فلترةَ باختصاصٍ طبّيّ، فقصرُها على
  اختصاصات الطبيب كان سيُخفي صفوفاً عمّن صار يملك إنهاءها.

### الطابورُ الموروث — وليس مراجعةً طبية
`/no-exam-review` صار **«مبالغ سابقة بانتظار الإكمال»**، وأُزيل من قائمة
الطبيب: يراه الاستقبالُ ومديرُ الفرع والمسؤول. وفعلان لا ثالث: **إكمال
وتسجيل المبلغ** · **إعادة للتصحيح بسببٍ حرٍّ إلزاميّ**. والقيمةُ `approved`
باقيةٌ في القاعدة كما هي — تسميتُها لا تستحقّ ترحيلاً.

### وليست معاينةً طبية
لا `medical_exams` تُنشأ · ولا تشخيصَ يُوقَّع · ولا `service_path` يتغيّر ·
ولا تصير العمليةُ عمليةَ مسارِ معاينة. **يحرسه اختبارٌ معماريّ** على منسّق
المبالغ نفسِه.

### العملُ يبدأ لحظتَه — البيعُ كالصيانة
**العمليةُ تمضي** حرفياً: بيعُ الجزء يفتح **أمرَ تصنيعه الآن** بخبيره وحلقته
وسجلِّه، فالخبيرُ يبدأ والمريضُ لا ينتظر قرارَ طبيبٍ لم يُطلَب منه أصلاً.
والصيانةُ كذلك: أمرُها وزيارتُها فوراً. **والمعلَّقُ هو المبلغُ وحده.**

**ولا سعرَ يُخترَع**: الأمرُ يُفتَح **بلا سعرٍ إطلاقاً** — لا بسعر صفر —
و`patient_device_episodes.agreed_cost` معناه الدقيق **«كم قُيِّد من هذه
الحلقة في المحاسبة»** لا «بكم اتُّفق»، فيبقى صفراً صادقاً حتى يقع القيد.
فلا التباسَ بين «مجّانيّ» و«لم يُدخَل بعد» (دلالةُ المرحلة الثانية محفوظة).

### والبيعُ نصفان في `storage.ts` — ولا محاسبةَ ثانية
كانت `assignManufacturing` تجمع فعلين وقعا دائماً معاً فبَدَوا واحداً:

- **`startDeviceSaleOperationallyTx`** — حارسُ الأمر النشط · قفلُ الخيط ·
  هويّةُ الحلقة وحالتُها ومسارُها · مواصفاتُ الجهاز وتفاصيلُ الحالة ·
  انتقالُ الحلقة إلى التصنيع · الأمرُ وسجلُّه وحدثُه. **بلا دينار.**
- **`applyDeviceSaleFinancialsTx`** — `patients.total_cost` · كلفةُ الحالة ·
  قيدُ الدفتر · `agreed_cost`. **بلا عمل.**
- **`loadDeviceSaleOperationTx`** — تقرأ عمليةً قائمةً بهويّتها كاملة فتُنتج
  الشكلَ نفسَه، فيأخذه النصفُ الماليُّ بلا أن يعرف من أين جاء.

**ومسارُ المعاينة لم يتغيّر بحرف**: `assignManufacturing` تركّب النصفين في
المعاملة نفسِها بالترتيب نفسِه. **ومصدرُ الحقيقة واحد** لحارسِ الأمر النشط ·
انتقالِ الحلقة · إنشاءِ الأمر وسجلِّه · حسابِ الفرق · مجموعِ المريض · كلفةِ
الحالة · قيدِ الدفتر.

### الإكمالُ الموروث ينادي الكاتبَ القانونيّ — ولا أمرَ ثانٍ

> **تاريخٌ يُقرأ**: ما يلي كان مسارَ **اعتماد الطبيب**، وهو اليوم مسارُ
> **إكمالِ صفٍّ موروث** يقوم به الاستقبالُ أو مديرُ الفرع أو المسؤول.
> والآليّةُ لم تتغيّر بحرف — تغيّر **مَن يضغط**.

- **صيانة** ⟶ `postMaintenanceFee` — الكتابةُ نفسُها التي تناديها الصيانةُ
  كاملةُ الأجر: كلفةُ الحالة · `patients.total_cost` · قيدٌ واحد بمصدر
  `maintenance`.
- **بيعُ جزء** ⟶ `applyDeviceSaleFinancialsTx` **على أمر العمل القائم** —
  لا `assignManufacturing` التي كانت ستفتح أمراً ثانياً وحلقةً ثانية.

**ولا `INSERT INTO cost_entries` ولا لمسَ `total_cost` في منسّق المبالغ** —
يحرسه اختبارٌ معماريّ، فلا تنحرف نسختان يوماً.

### والإكمالُ يعيد القراءة **تحت القفل**
بين الإنشاء والإكمال قد يمرّ يوم: يُنقَل المريضُ فرعاً · يُوقَف خبيرٌ أو
يخرج من الفرع · يُلغى أمرٌ إدارياً · تتغيّر الحلقة. **فلا لقطةَ قبل القفل
تُقبَل سلطةً**: المريضُ والفرعُ والخدمةُ والحلقةُ ومسارُها وخيطُها وما طُلب
وأمرُ التصنيع **تُقرأ الآن وتُطابَق**. والتناقضُ يُردّ ٤٠٩ **بلا دينار ولا
نصفِ كتابة**.

**والحلقةُ تُقرأ بمعرّفها** (`lockCaseAndReadExactEpisode`) لا «الحلقةَ
المفتوحة»: تلك تستثني `delivered` عمداً، فكانت تجعل الجهازَ المسلَّمَ بحقٍّ
غيرَ قابلٍ للإكمال أبداً. والمقبولةُ `in_manufacturing` و`delivered`،
**والملغاةُ والمُبطَلةُ إدارياً تُردّ** وبابُها التصحيحُ لا الإكمال.

**وترتيبُ القفل واحد لا اثنان**: الخيطُ أوّلاً ثمّ الحلقة — في التحميل
الدقيق وفي `lockCaseAndReadOpenEpisode` و`startDeviceEpisode` معاً.

**والخبيرُ الحاليُّ على أمرِ العمل هو السلطة** لا لقطةُ الإنشاء
(`sale_expert_user_id` تبقى للتدقيق): إعادةُ إسنادٍ مشروعة **يجب ألّا تُفشل
المال**. ويُعاد التحقّق بفرع أمر العمل (`validateExpertForBranchTx`).

**والصيانةُ تُطابَق كذلك قبل الدينار**: `postMaintenanceFee` كاتبٌ مطيع
يقيّد ما يُعطى — وهذا صوابُه — فتُقرأ هويّةُ أمرها مقفولةً أوّلاً: الغرضُ
والمريضُ والفرعُ والخدمةُ والجزءُ والمنشأُ والحلقة.

### ولا أمرَ في فرعٍ وحلقتُه في آخر
مريضٌ نُقل بعد أن فُتح طلبُه: الأمرُ كان يُنشأ بفرعه **الحاليّ** بينما
الحلقةُ من فرعه القديم — فيصير للعملية فرعان، ويُنسب مالُها بعد شهرٍ إلى
فرعٍ لم يعمل فيها. **فيُردّ ٤٠٩ قبل أن يُفتَح شيء** — لا أمرَ ولا صفَّ ولا
دينار — وبابُه نقلُ العملية إدارياً.

### والإعادةُ للتصحيح لا توقف العمل
المُراجَعُ هو **المبلغ**، فإعادتُه لا تُلغي أمرَ التصنيع ولا تُرجع الحلقة:
الخبيرُ يواصل، والصفُّ الماليُّ وحده يعود إلى الاستقبال. ويُصحَّح **الصفُّ
نفسُه** ويُعاد إرسالُه، ويُقيَّد عند الإكمال **المبلغُ الحاليُّ** —
والقديمُ لا يُقيَّد أبداً.

### والتطبيقُ مرّةً واحدة بالضبط
قفلُ الصفّ (`FOR UPDATE`) · ثمّ شرطُ الحالة في `UPDATE` · ثمّ `applied_at`
حارساً. وفهرسان جزئيّان (`uq_psc_open_work_order` · `uq_psc_open_episode_sale`)
يمنعان صفَّين معلَّقين على العملية نفسِها. **فضغطتان متزامنتان تُنتجان قيدَ
كلفةٍ واحداً وأمرَ تصنيعٍ واحداً**، والثانيةُ تُردّ برسالةٍ تقول ما جرى.

### والصفرُ لا يعني «مجّاناً» — القاعدةُ نفسُها من ٠٦٦
`charged: false` ⟶ **لا مبلغَ ولا قيدَ ولا دينار**: العمليةُ بلا أجر تُحفَظ
وتنتهي، ولا التباسَ بين «مجّانيّ» و«لم يُدخَل بعد». والمبلغُ الحاضر **موجبٌ
دائماً** — يفرضه `psc_amount_check` في القاعدة للصفوف الموروثة، ويفرضه
`parsePendingAmount` على كلّ عمليةٍ جديدة.

### الصفوفُ الموروثة — العقدُ كما هو، بمَن يضغط اليوم
- **الإكمالُ أو الإعادة** — طرفان لا ثالث.
- **وبسببٍ حرٍّ إلزاميّ للإعادة** — يفرضه الخادمُ (٤٠٠) والقاعدةُ
  (`psc_returned_shape_check`) والشاشةُ معاً.
- **والصفُّ لا يُحذَف**: يبقى بمبلغه، وتُقرأ عليه رحلتُه كاملةً.
- **وأثرُه الماليُّ صفرٌ ما لم يُكمَل** — لا كلفةَ ولا قيدَ ولا دفعة.
- **ويعود إلى استقبال فرعه** — الفرعُ يُقرأ من صفّ العملية لا من الطلب.
- **وطابورٌ دائم `/returned-charges` بشارةِ عددٍ على القائمة**، وسببُ
  الإعادة **أوّلُ ما يُقرأ** في الصفّ.
- **والرؤيةُ على مستوى الفرع**: `branch` هو الرقمُ الحاكم، و`mine` تبليغٌ
  شخصيٌّ لمن أنشأها — **ولا يقفل التصحيحَ عليه**.
- **والتصحيحُ على الصفّ نفسِه** — لا صفَّ ثانٍ يُستنسَخ فيُحسَب البيعُ مرّتين.
- **وكلُّ ذلك مدقَّق**: `audit_log` بـ`entity_type = 'pending_service_charge'`
  للصفوف الموروثة، و`'no_exam_operation'` للعمليات الجديدة، ومعه جدولُ
  الأحداث بسببِ كلّ إعادةٍ ومبلغِ كلّ تصحيح.
- **وتبليغٌ داخل التطبيق وحده** — ولا واتساب ولا تلغرام ولا بريد ولا دفع.

### العملُ المؤهَّل — من القائمة القائمة وحدها
`shared/prosthetic_parts.ts` (ترحيل ٠٦٠) هو مصدرُ الحقيقة: الأجزاءُ الثمانية
للأطراف، و`full_device` للجهاز الكامل. **ولا قائمةَ ثانية ولا حقلٌ حرّ**،
وقائمتان كانتا ستنحرفان: يُضاف «الأدابتر» إلى إحداهما فتُصان قطعةٌ لا تُباع.
**والمساندُ الطبية بلا أجزاء** — لا تُخترَع لها قائمةٌ لم يقلها أحد، وطلبُ
«ركبة» على مسندٍ **يُردّ لا يُصحَّح**.

### منشأُ الجهاز — **ثلاثُ حقائق لا اثنتان**
`shared/device_origin.ts`: `registered` (له حلقتُه المسلَّمة) ·
`center_unrecorded` (**صنعناه نحن** قبل التسجيل في هذا النظام) · `external`
(صُنع **خارج** المركز).

**والدمجُ كان كذباً في العمود نفسه**: خيارٌ واحد اسمُه «خارج المركز **أو**
غير مسجَّل» يجعل جهازاً صنعناه قبل النظام يُقرأ في كلّ تقريرِ ضمانٍ أو
جودةٍ لاحق على أنه ليس عملَنا.

**ومكانُه أمرُ التصنيع** (`prosthetic_work_orders.device_origin`) لا صفُّ
المبلغ: الصيانةُ بلا أجرٍ لا تُنشئ صفّاً معلَّقاً أصلاً، فلو عاشت الواقعةُ
هناك وحدها لاختفت كلّما كانت الخدمةُ مجّانية. والصفُّ يأخذ **لقطةً** منها
للعرض في الطابور ولا يصير مصدرَ حقيقةٍ ثانياً.

**ولا يُستنتَج من `had_prior_center_history`**: تلك عن **المريض** وهذه عن
**الجهاز الذي بين يدي الخبير الآن** — فيُسأل الموظّف ولا يُخمَّن عنه.
**والمجهولُ يُردّ ٤٠٠ لا يُصحَّح بصمت.** ويمرّ الاثنان بلا حلقة تشغيلياً
بمخرج «جهازٌ قديم غير مسجَّل» (`legacyUnrecordedDevice`) القائم — **فلا
يُخترَع أمرُ تصنيعٍ تاريخيّ ولا حلقةٌ مسلَّمة عندنا لم تقع.**

### والطرفُ الصناعيُّ الكاملُ لا يُباع بلا معاينة
الجزءُ بديلٌ لقطعةٍ وُصفت يوماً على جهازٍ قائم — قالبٌ يبلى أو ركبةٌ تنكسر —
فبيعُه قرارٌ تشغيليّ. أمّا **الطرفُ الكاملُ فقرارٌ سريريٌّ من أوّله**:
مستوى البتر والمفصلُ والقدمُ والمقاس.

**فيُردّ في الخادم** (`NO_EXAM_FULL_PROSTHESIS_REFUSAL`) عند فتح الطلب
(٤٠٠) وعند بيعه (٤٠٩) — **ولا يُعرَض في الشاشة أصلاً**. وحلقةٌ موروثة فُتحت
قبل هذا الحارس **لا يُغيَّر مسارُها بصمت**: تُردّ برسالةٍ تدلّ على مسار
المعاينة أو على التصحيح الإداريّ. **والمساندُ الطبية مختلفة**: لا قائمةَ
أجزاءٍ قانونيةً لها بعد، فالمسندُ الكاملُ يبقى مؤهَّلاً — **ولا تُخترَع له
أجزاءٌ لم يقلها أحد.**

### والصفرُ لا يعني «مجّاناً» — والعملُ يكتمل مع ذلك
`charged: false` ⟶ **لا صفَّ معلَّقاً**، لكنّ **العملَ يبدأ ويكتمل تسجيله**:
أمرُ التصنيع مفتوحٌ والحلقةُ في التصنيع، ولا مبلغَ ولا مراجعة. فلا عمليةٌ
تبقى معلَّقةً بلا سبب، ولا اعتمادَ مسرحيٌّ لصفر.

**و«بلا أجر» واقعةٌ تُحفَظ لا غيابُ صفّ**: `prosthetic_work_orders.
no_exam_no_charge` يُكتب في **المعاملة نفسِها** التي تبدأ العمل. فغيابُ
الصفّ لا يفرّق بين «قُرِّر مجّاناً صراحةً» و«لم يُسجَّل مبلغُه بعد».
ثلاثيةٌ صادقة: `NULL` لم يُسأل (قديمٌ أو خارج المسار) · `TRUE` بلا أجرٍ
صراحةً · `FALSE` بأجرٍ يمرّ بالمراجعة. **ولا قيمةَ افتراضية** تكتب معنىً
على أمرٍ قديم.

### الهويّةُ دقيقةٌ ولا مفاتيحَ بلا معنى
`device_episode_id` و`work_order_id` و`sale_expert_user_id` **لقطاتُ أرقامٍ
بلا مفتاحٍ أجنبيّ** — درسُ `proposed_expert_user_id` (٠٣٥) و`payments.visit_id`
(٠٣٨). أمّا `patient_id` و`case_id` فمفتاحان حقيقيّان، وقد أُضيف الجدولان إلى
كاسكيد `deletePatient` وإلى قرارات `mergePatients` (ومعه إعادةُ إسناد
`case_id`) كما توجب القاعدةُ الملزمة، **ومُختبَران حيّاً**.

**والخبيرُ يختاره الاستقبالُ لحظةَ العمل لا الطبيبُ لحظةَ المراجعة**: الطبيبُ
يراجع **المبلغ** لا مَن ينفّذ. ويُتحقَّق أنه فعّالٌ في فرع المريض بنفس قائمة
«تخصيص وإسناد خبير»، ويفرضه `psc_sale_expert_check` في القاعدة.

### والبابان الماليّان يبقيان مغلقين على هذا المسار
«تخصيص وإسناد خبير» و«بدء التصنيع» يردّان ٤٠٩ كما كانا — **لأنهما يُدخلان
المالَ المحاسبةَ لحظتَهما**، وذاك ما لا يجوز هنا قبل المراجعة. **والذي تغيّر
الرسالةُ**: صارت تدلّ على البابِ الحقيقيّ («سجّل العملية من بيع بلا معاينة،
ويُفتَح أمرُ التصنيع فور اعتماد المبلغ») بدل «لم يُفعَّل بعد». **ولا التفافَ
بإعفاء «المريض القديم»** كما كان.

### العلاجُ الطبيعي معزولٌ تماماً
`psc_service_check` في القاعدة يقصره على `prosthetic` و`medical_support`،
والنقطتان تردّان العلاجَ الطبيعي ٤٠٠. **ولا حرفَ تغيّر** في تسعيره ولا
جلساته ولا خططه — ويحرسه اختبارٌ معماريّ على `shared/pricing.ts` و
`server/new_service/store.ts`.

### وما لم يُمَسّ
ختمُ المعاينة · إلغاءُ المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) · تصحيحُ
السعر بعد البيع · دلالاتُ ٠٦٦ التجارية · واتساب وتلغرام · وترحيلاتٌ من ٠٠١
إلى ٠٦٦. **والصيانةُ كاملةُ الأجر بنقطتها القائمة كما هي** — هذا بابٌ ثالثٌ
بجوارها لا بديلٌ عنها.

### الترحيل
**٠٦٧ فقط** — إضافيّ، idempotent: جدولان وتسعةُ قيود وفهرسان فريدان جزئيّان
وأربعةُ فهارس، ومعها **عمودُ `device_origin` على `prosthetic_work_orders`**
بقيده (`NULL` للقديم وللبناء الأوليّ — صدقٌ لا نقص) **وعمودُ
`no_exam_no_charge` بلا قيمةٍ افتراضية**. **بلا `DROP` ولا `DELETE` ولا
`TRUNCATE`**، ولا مسٍّ لتاريخٍ ماليٍّ قائم ولا لترحيلٍ من ٠٠١ إلى ٠٦٦.

اختبارٌ دائم: `npm run test:pending-charge` (٢٦٢ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية — منها **أن أمرَ التصنيع قائمٌ قبل الاعتماد** والمالُ
صفرٌ، وأن الاعتمادَ لا يُنشئ أمراً ثانياً، والثابتُ المحاسبيُّ قبله وبعده،
وحارسُ الطرف الكامل بالحلقة الموروثة، وافتراقُ المنشأَين الثلاثة بعد إعادة
القراءة ومنشأُ الخدمة المجّانية، وإعادةُ التحقّق من الخبير والفرع والهويّة
لحظةَ الاعتماد، ومصفوفةُ الصلاحية اختصاصاً وفرعاً، والتزامنُ بثلاثة أشكال،
وحذفُ المريض الكامل ودمجُ ملفَّين، وعزلُ العلاج الطبيعي، وعقدُ الشاشات،
والحارسُ المعماريّ — **ومنها اعتمادُ المال بعد التسليم والاكتمال**، وسلطةُ
الخبير الحاليّ على الأمر بعد إعادة الإسناد، ورفضُ الملغى والمُبطَل إدارياً،
ومطابقةُ هويّة الصيانة بستّة أوجه، و«بلا أجر» محفوظةً على الأمر، وحلقةُ
فرعٍ آخر تُردّ قبل أن يُفتَح شيء، وتطابقُ القائمة مع الخادم لكلّ جلسة).

---

## 4.g سلّةُ المرضى واستعادتُهم خلال ثلاثين يوماً (ترحيل ٠٦٨ — 2026-08-24)

**الحذفُ العاديُّ لم يعد يهدم شيئاً.**

### الواقعة
`DELETE /api/patients/:id` كان ينادي كاسكيدَ `storage.deletePatient` مباشرةً:
ضغطةٌ واحدة تمحو الملفَّ وزياراتِه ودفعاتِه وفواتيرَه ومعايناتِه وأوامرَ
تصنيعه وقيودَ كلفه **بلا رجعة**. والسجلُّ الوحيد سطرُ تدقيقٍ يقول «حُذف» —
لا يُعيد شيئاً.

### فصار الحذفُ حالةً على الملفّ
`patients.deleted_at` وعشرةُ أعمدةٍ معها (مَن حذف باسمه ودوره · السبب ·
`restore_until` · لقطةُ الحال المالي · أَلَزِم المسؤولُ العام؟).

**ولا صفَّ تابعاً واحداً يُمَسّ**: المعايناتُ بأختامها · أوامرُ التصنيع
بسجلّها ومراحلها · الدفعاتُ وقيودُ الكلف · الفواتيرُ والأقساط · المبالغُ
المعلَّقة · الحلقاتُ والمتابعات · جهاتُ الاتصال ورمزُ المريض. تخرج من
النظام الفعّال **لأن صاحبَها خرج، لا لأنها تغيّرت**.

**ولا جدولَ أرشيفٍ ثانٍ**: الصفوفُ الأصلية هي الحقيقة، وجدولٌ يُعيد بناءَ
الملفّ يصير مصدرَ حقيقةٍ ثانياً ينحرف ثمّ لا يُعرَف أيُّهما الصحيح. ولا
جدولَ تدقيقٍ ثانٍ: `audit_log` يسع الحذفَ والاستعادةَ والهدم.

### تعريفٌ واحد لا يتكرّر
`server/patients/active_patient.ts` يقول ما هو «المريضُ الفعّال» =
`deleted_at IS NULL`، ويُستورَد في كلّ قارئٍ تشغيليّ (عشرون ملفّاً). ونقطةُ
الخنق `storage.getPatient` صارت **تُرجع الفعّالَ وحده**، فإحدى وثلاثون نقطةً
تنادِيها تُغلق دفعةً واحدة. ومَن أراد الصفَّ مهما كانت حالته ينادي
`getPatientAnyState` **صراحةً** — فالاستثناءُ يُطلَب باسمه ولا يُنال بالسهو.

**ولا نسخةَ ثانية من حالة الحذف تنتشر في الجداول**: الدفعاتُ والزياراتُ
وقيودُ الكلف تُصفّى **بانضمامها إلى `patients`** (`belongsToActivePatientSql`)
لا بعمودٍ يُكرَّر فيها فينحرف.

### المالُ يخرج من الحساب الفعّال ولا يُلمَس صفٌّ منه
`totalRevenue` وتقسيمُ الأقسام و«الوارد» و«المبيعات» و«الديون المستحقّة»
و«نسبة التحصيل» والتقريرُ اليوميُّ ومجاميعُ الفروع والنسخةُ البريدية —
كلُّها تصفّي المحذوف. **ولا دفعةَ سالبةً تُختَرع ولا قيدَ عكسيّ ولا شطبَ
دَين**: الصفوفُ باقيةٌ بمبالغها، والقراءةُ وحدها تغيّرت. والاستعادةُ تعيد
المالَ إلى المجاميع **كما كان بالضبط**.

### مَن يحذف ومَن يستعيد ومَن يمحو
- **الحذفُ والاستعادة**: المسؤولُ العام (كلُّ الفروع) · مديرُ الفرع (نطاقه) ·
  **الطبيب** (نطاقه). ولا استقبالَ ولا محاسبَ ولا خبيرَ ولا معالجَ ولا مسّاح.
- **ولا يُشترَط أن يملك الطبيبُ معاينةَ المريض ولا اختصاصَه** — هذه سلطةُ
  إدارةِ ملفّ منحتها السياسة، لا قرارٌ سريريّ يُقاس بالاختصاص.
- **ولا تُفتَح بـ`canWriteMedicalExam`** — تلك صلاحيةُ كتابةِ سجلٍّ سريريّ.
- **والعَلَمُ القديم `can_delete_patients` لم يعد بابَ هذا المسار** (قرارُ
  توسيعٍ صريح): ذاك عَلَمُ **هدمٍ لا رجعةَ فيه**، وهذا فعلٌ يُرَدّ بضغطة
  ويُسجَّل بسببه. والعمودُ يبقى في القاعدة كما هو ولم يُمَسّ.
- **والحذفُ النهائيُّ للمسؤول العام وحده** — الفعلُ الوحيدُ الذي لا رجعةَ فيه.

### والالتزامُ الماليُّ القائم يرفع القرارَ إلى المسؤول وحده
رصيدٌ غيرُ صفريّ (**دَيناً كان أو رصيداً للمريض** — كلاهما التزام) ·
مبلغُ «بلا معاينة» معلَّقٌ أو مُعاد · طلبُ خصمٍ معلَّق · طلبُ سعرٍ معلَّق ·
متابعةُ بيعٍ لم يُحسَم قرارُها · تسويةٌ ماليةٌ بعد تصحيحٍ إداريّ.

**والحسابُ هو الحسابُ القائم في النظام** — `total_cost` ناقصَ مجموعِ
الدفعات — لا صيغةٌ تُخترَع لهذه المرحلة. **والمسؤولُ لا يُمنَع أبداً**: هو
المخرجُ لا الحاجز. والشاشةُ تقول **لماذا** بالدينار لا «غير مصرح» عارية.

### والعملُ السريريُّ والتصنيعيُّ لا يمنع الحذف
أمرُ تصنيعٍ قيد التنفيذ أو معاينةٌ موقّعة **لا يوقفان الحذف** ولا يُلغَيان
به: الأمرُ يبقى `in_progress` بمرحلته، والحلقةُ بحالتها، والمعاينةُ بختمها.
يختفي **العرضُ** وحده، ويعود كلُّه بالاستعادة.

### معاملةٌ واحدة، وسببٌ إلزاميّ
قفلُ صفّ المريض (`FOR UPDATE`) · تأكيدُ أنه فعّال · حسابُ اللقطة **تحت
القفل** · قرارُ «أيلزم المسؤول؟» · إعادةُ فحص الفرع **من الصفّ المقفول**
(قد يُنقَل المريضُ بين المعاينة والتنفيذ) · الكتابةُ بشرطِ حالةٍ في
`UPDATE` · التدقيق. **فضغطتان متزامنتان تُنتجان حذفاً واحداً**، والثانيةُ
تُردّ ٤٠٩ برسالةٍ تقول ما جرى.

**والأرقامُ من الخادم**: نقطةُ `delete-preview` تقرأ الحالَ الماليَّ الحيّ،
والتنفيذُ **يعيد حسابَه** ولا يقبل رقماً من العميل أبداً.

**والمهلةُ يولّدها الخادم** من ختم الحذف وتُقارَن بـ`NOW()` **في القاعدة** —
فساعةُ المتصفّح لا تُستعاد بها ملفّ ولا يُمنَع بها.

### الاستعادةُ لا تبني شيئاً
تمسح حالةَ الحذف فحسب: المعرّفُ نفسُه · **رمزُ المريض نفسُه** (محجوزٌ على
صفّه، لا يُعاد استعمالُه ولا يصير اسماً بديلاً) · الحالاتُ والمعايناتُ
وأوامرُ التصنيع والحلقاتُ والدفعاتُ وقيودُ الكلف والفواتيرُ واليوميةُ
**كلُّها الصفوفُ نفسُها** — لم تُلمَس أصلاً. وسطرُ التدقيق يحمل **سياقَ
الحذف السابق** (مَن ولماذا ومتى) وإلّا ضاع بمسحِ الأعمدة.

### ولا يُمحى شيءٌ في اليوم الثلاثين
تسقط **الاستعادةُ وحدها** (٤٠٩ «انتهت مدة استعادة هذا المريض»)، والصفُّ باقٍ
كما هو. **ولا كرون محوٍ ولا حذفٌ آليّ**: محوُ سجلٍّ طبيٍّ وماليّ لا رجعةَ
فيه لا يقع بلا أن يقرّره إنسان. و«حذف نهائي» بعدها قرارٌ صريحٌ للمسؤول
العام: من داخل السلّة · بتأكيدٍ ثانٍ · وبسببٍ مكتوب · **والتدقيقُ يُكتب قبل
الهدم** لأن الكاسكيد يمحو كلَّ ما يمكن أن يُقرأ بعده · ثمّ يُنادى
**`storage.deletePatient` المُختبَرة نفسُها بلا حرفٍ يتغيّر**.

**والحذفُ النهائيُّ من السلّة فقط**: ملفٌّ فعّال يُردّ ٤٠٩ — بابُ الإخراج
هو السلّة ثمّ الهدمُ بقرارٍ ثانٍ منفصل، فلا ضغطةٌ واحدة تمحو ملفّاً حيّاً.

### والأبوابُ التشغيلية تُردّ برسالتها الصريحة
طلبُ جهازٍ جديد · دفعةٌ · زيارةٌ · معاينةٌ · صفحةُ أمرِ تصنيعه — كلُّها تُردّ
«هذا الملف في المحذوفات — استعده أولاً»، **لا «غير موجود»**: الملفُّ موجودٌ
وصفوفُه قائمة، وإنكارُ وجوده يرسل الموظّفَ يبحث عن خطأٍ ليس هناك.

**والدمجُ يرفض ملفّاً في السلّة في الطرفين معاً**، **ولا يستعيده ضمناً**:
مصدراً يسحب مالَ ملفٍّ أُخرج من النظام إلى ملفٍّ حيّ، وهدفاً يسكب بياناتٍ
حيّةً في ملفٍّ سيختفي.

### وتكرارُ التسجيل يُنبَّه عليه ولا يُكشَف
المحذوفُ غائبٌ عن السجلّ والبحث، فيفتح الاستقبالُ **ملفّاً ثانياً لنفس
الشخص** بحسن نيّة ثمّ يُستعاد الأوّل. فنقطةُ `lookup-by-name` صارت تفصل
المطابقاتِ الحيّةَ عن التي في السلّة: **مَن يملك السلّة** يقرأ «المريض موجود
في المحذوفات» ويرى الصفَّ ليقرّر · **ومَن لا يملكها** يقرأ «يوجد ملف مطابق
يحتاج مراجعة الإدارة قبل التسجيل» **بلا اسمٍ ولا رقمٍ ولا فرعٍ ولا ذكرِ
سلّةٍ أصلاً**.

### ولا رسالةَ واتساب لمحذوف
`claimDue` تصفّي المحذوفَ **في SQL قبل `LIMIT`** لا بعد الحجز — نفسُ درسِ
القالب غير المعتمَد (٤.ج): التصفيةُ بعد الحجز كانت ستُجوّع الدفعة. والصفُّ
يبقى `pending` بـ٠ محاولات وبلا رمزِ خطأ، فإن استُعيد الملفُّ أُرسل **الصفُّ
نفسُه** بلا تدخّل. ولا بثَّ رجعيّ ولا واتساب ولا تلغرام تغيّر حرفاً.

### والجنائيُّ لا يُستثار إلّا بالهدم الحقيقيّ
`visits_forensic_log` و`medical_exams_forensic_log` مرتبطان بـ`BEFORE DELETE`،
والحذفُ الناعم لا يمسّ صفّاً تابعاً — **فلا سطرَ جنائيٌّ يُكتب**، ولا يفقد
الجدولان معناهما إنذاراً. والحذفُ النهائيُّ يستثيرهما كما كان بالضبط.

### الشاشات
- **«المحذوفات»** (`/patient-trash`) — بشارةِ عددٍ على القائمة كي لا تنقضي
  مهلةُ ملفٍّ لأن أحداً لم يفتح الصفحة. الأحدثُ حذفاً أوّلاً، **والسببُ أوّلُ
  ما يُقرأ**، ومعه مَن حذف ومتى وكم بقي واللقطةُ المالية. وبحثٌ **داخل
  السلّة وحدها** — لا يُخلَط بالبحث العاديّ وإلّا عاد المحذوفُ إلى الشاشة
  التي أُخرج منها.
- **نافذةُ الحذف** في صفحة المريض — تقول ما سيحدث **قبل** أن يحدث: يختفي من
  القوائم والبحث والطوابير والمحاسبة · **ولا يُحذف منه شيء** · ويُستعاد خلال
  ثلاثين يوماً · والسببُ إلزاميّ. وتعرض الكلفةَ والمدفوعَ والمتبقّي من
  الخادم، وتشرح لمديرِ الفرع والطبيب **لماذا** حين يلزم المسؤول.
- **وتنظيفُ الذاكرة نقطةُ خنقٍ واحدة** (`invalidateAfterPatientTrashChange`)
  يناديها الحذفُ والاستعادةُ والهدم — فلا قائمةٌ تُبطَل هنا وتُنسى هناك.

### الترحيل
**٠٦٨ فقط** — إضافيّ، idempotent: أحدَ عشرَ عموداً على `patients` وقيدان
وأربعةُ فهارسَ جزئية. **بلا `DEFAULT`** يكتب معنىً على ملفٍّ لم يُحذف، وبلا
`DROP` ولا `DELETE` ولا `TRUNCATE`، ولا مسٍّ لترحيلٍ من ٠٠١ إلى ٠٦٧. والقيدان
يمنعان الحالةَ المستحيلة من الوجود: صفٌّ محذوفٌ بلا سببٍ أو بلا مهلة، ومهلةُ
استعادةٍ على ملفٍّ فعّال.

اختبارٌ دائم: `npm run test:patient-trash` (١٧٢ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية — منها **بصمةُ كلّ صفٍّ تابعٍ قبل الحذف وبعده وبعد
الاستعادة**، وستّةَ عشرَ سطحاً يختفي منها، والمالُ يخرج بمقداره ويعود،
ومصفوفةُ الصلاحية بالأدوار والنطاق، والالتزاماتُ المالية الستّة، وانقضاءُ
المهلة، والضغطةُ المزدوجة حذفاً واستعادةً، والدمجُ في الطرفين، وتكرارُ
التسجيل بوجهيه، والإشعارُ المحجوز، والجنائيُّ قبل الهدم وبعده، وعقدُ الشاشات
والحارسُ المعماريّ: لا كاسكيدَ هادمٌ في نقطة الحذف العاديّ).

**وتغطيةُ الكاسكيد لم تنقص**: كلُّ اختبارٍ كان يثبت حذفَ مريضٍ كامل صار
ينفّذ **الخطوتين** — نقلاً إلى السلّة ثمّ حذفاً نهائياً — فالقاعدةُ الملزمة
في القسم ٨ محفوظةٌ بحرفها.

---

## 4.h معاينةٌ طبّيةٌ محضة — بلا مسؤوليةٍ تجارية على الطبيب (2026-08-28)

**الطبيبُ يشخّص ويصف ويوقّع فقط. لا سعرَ ولا خصمَ ولا خبيرَ ولا قرارَ شراء
يصل الخادمَ من نافذته بعد اليوم.** هذه **المرحلةُ الأولى** من فصل الشقّ
الطبّيّ عن الشقّ التجاريّ؛ **مرحلةُ الاستعلامات القادمة** ستبني فوقها.

### الواقعة
كانت `NewExamDialog.tsx` تحمل **نظامين تجاريَّين طبقة فوق طبقة**: (١)
`medical_exams.device_cost`/`proposed_expert_user_id` (مقترحُ الطبيب،
migrations 030/032/035 — المفصَّل في القسم 4.b) و(٢) «تفاصيل البيع» عند
التوقيع (السعرُ ونوعُه والخبيرُ وقرارُ الشراء، migration 066 — المفصَّل في
القسم 4.f) بما فيها نافذةُ «تصحيح سعر بعد البيع». فالطبيبُ كان يقرّر
أرقاماً وأشخاصاً وقراراتٍ تجارية — وهذا عبءٌ ليس من عمله، وقرارُ المالك
إزالتُه كاملاً بلا استثناء.

### ما تغيّر
- **`NewExamDialog.tsx`**: حُذف كلُّ حقلٍ تجاريّ — كلفةُ الجهاز، الخبيرُ
  المقترَح، نوعُ السعر، قرارُ الشراء، سببُ عدم الشراء، ونافذةُ «تصحيح سعر
  بعد البيع» بكاملها (القفل والتحذير وحقل السبب). **الباقي سريريٌّ محضٌ**:
  الاختصاص، الوصفةُ (`PrescriptionFields`)، والحقولُ السرديّة الخمسة. وجسمُ
  الحفظ صار ثلاثةَ حقول: `caseType` + الحقولُ السرديّة + `prescription`.
- **`POST /api/medical/patients/:id/exams`**: لا يقرأ `deviceCost` ولا
  `proposedExpertUserId` ولا `commercial` من الجسم إطلاقاً — ولو وصلت من
  عميلٍ قديم، **تُتجاهَل تماماً** (لا تُقرأ، لا تُتحقَّق، لا تُكتب، ولا
  تُسقط المعاينةَ إن كانت فاسدة). المعاينةُ تُحفَظ **دائماً** بنجاح ما دام
  محتواها السريريّ سليماً.
- **`PATCH /api/medical/exams/:examId`** (تعديل معاينةٍ موجودة): **لم يُغلَق**
  — يبقى البابَ القانونيَّ الوحيد لتصحيح سعرٍ/خبيرٍ على معاينةٍ **قديمة**
  ما زالت تحمله من قبل هذا التبسيط، ومنطقُه (`classifyExamPriceChange`
  وما بعدها) **لم يُعَد بناؤه بحرف** — جاهزٌ لمرحلة الاستعلامات القادمة.
  **وغيابُ الحقل من الجسم صار يعني «لم يُلمَس» لا «امحُه»**: الشاشةُ
  الجديدة لا ترسله أبداً، فحقلُ الخادم يُبقي القيمةَ الحالية للمعاينة حين
  يغيب الحقلُ من الطلب، بدل أن يمحوها بصمت — وإلا كان كلُّ تعديلٍ نصّيّ
  بسيط (تصحيحُ فقرةٍ في التشخيص) يمسح كلفةَ جهازٍ حقيقية كتبها طبيبٌ قبل
  هذا التبسيط.

  **(٢٠٢٦-٠٨-٢٨، تنقيحٌ ثانٍ) — والحضورُ لا ينقذ طبيباً عادياً**: الحرسُ
  أعلاه يمنع مَحواً غير مقصود من عميلٍ نسي الحقل؛ وهذا حرسٌ ثانٍ **بالهويّة
  لا بالحضور**. صاحبُ التوقيع الذي لا يحمل صفةً إدارية وقتَ الطلب
  (`isNormalDoctorAuthor = isAuthor && !isResponsibleManager`) يبقى
  `deviceCost`/`proposedExpertUserId` عنده على القيمة المخزَّنة **حتماً**،
  حتى لو أرسلهما صراحةً بقيمةٍ صالحة — فالطبيبُ العاديّ **سلطتُه السريرية
  كاملة وسلطتُه التجارية صفر**، ولا يُردّ الطلبُ لهذا السبب: الشقُّ
  السريريُّ من نفس الطلب ينجح دائماً. **والمسؤولُ العام (`session.isAdmin`)
  ومديرُ الفرع كما كانا تماماً** — الأوّل يحتفظ بسلطته القانونية الوحيدة
  للتصحيح الإداريّ التاريخيّ، والثاني بما كان يملكه أصلاً؛ لا توسيعَ ولا
  تضييقَ على أيٍّ منهما في هذه المهمّة. اختبارٌ دائم يثبت الاثني عشر
  سيناريو (سريريّ كامل للطبيب، تجاريّ محجوب عنه، تاريخٌ محفوظ، مسؤولٌ
  ومديرُ فرعٍ بلا تغيير) في `server/exam_edit_commercial.test.ts`
  (`npm run test:exam-edit-commercial`).

### ما لم يتغيّر
- **`POST /api/followups/:id/commercial`** (`server/followup/routes.ts` ⟶
  `setCommercialFields`) — البابُ القانونيّ الباقي لكلّ تفصيلٍ تجاريّ،
  **لم يُمَسّ بحرف**، وأحكامُ المالكية (`asDoctor` من `isExamDoctorOf`)
  تعمل بصرف النظر عن الباب الذي وصلتَ منه. بطاقةُ المريض
  (`PostExamDecisionCard.tsx`) تبقى كما هي.

  **وهذا بقاءٌ انتقاليّ، لا تصميمٌ نهائيّ.** بقي هذا البابُ بأحكام مالكيته
  كما هي **لأن هذه المرحلةَ لم تمسّ سير عمل ما بعد المعاينة أصلاً** — ليس
  لأن الطبيب سيظلّ يملك قراراً تجارياً. فمَن اتّصل بهذا الباب مباشرةً (لا
  عبر نافذة التوقيع المحذوفة) وكان هو طبيبَ المعاينة، ما زال **تقنياً**
  يُمنَح `asDoctor` ويستطيع كتابة السعر أو الخبير أو القرار مملوكاً له —
  **هذه حقيقةٌ تقنية قائمة اليوم، لا الهدف**. الهدفُ النهائيّ: **الاستعلاماتُ
  وحدها** تملك اختيارَ الخبير، والسعرَ الأصليّ، ومقدارَ الخصم، وإتمامَ
  الشراء، و«لم يشترِ» — **بلا أيّ دورٍ تجاريّ للطبيب**. ومرحلةُ تبسيط
  مبيعات الاستعلامات القادمة ستُقيِّد أو تُزيل قدرةَ `asDoctor` هذه من
  الباب نفسِه، لا مسارٌ موازٍ يُبنى من الصفر.

  **⚠ (٢٠٢٦-٠٨-٢٨) — القسم 4.i: وصلت مرحلةُ التبسيط، وتصحيحُها اللاحقُ
  في اليوم نفسه.** `setCommercialFields` نفسُها **لم تُمَسّ بحرف** كما وُصف
  أعلاه — القيدُ حارسٌ جديد **قبل** النقطة (`retiredOnExamPath`) لا تعديلٌ
  فيها ولا في `asDoctor`. **والحارسُ عقدُ مسارٍ شامل لا حظرَ طبيبٍ فحسب**:
  `/commercial` (ومعها `/expert`/`/commercial-price`/`/confirm-purchase`/
  `/approve-purchase`/`/close`) تُردّ ٤٠٩ عن **الجميع** على مسار المعاينة
  — الاستقبالُ والمحاسبُ ومديرُ الفرع والمسؤولُ العام أيضاً، لا الطبيبَ
  وحده (تصميمٌ أوّلُ استثناهم كان خطأً صُحِّح). وصفٌّ موروثٌ يملك حقلاً
  مملوكاً للطبيب من قبل هذه المرحلة يبقى محفوظاً — لا يُمحى ولا يُعاد
  كتابتُه — بنفس حارس المالكية القديم بحرفه. والاستعلاماتُ (استقبالٌ أو
  محاسبٌ أو مديرُ فرع أو مسؤول) صار لها بابٌ جديدٌ مخصَّص على مسار المعاينة:
  `POST /api/followups/:id/complete-sale`.
- **`ensureFollowupForSignedExam`** (`server/followup/store.ts`) ما زالت
  تفتح متابعةً عند التوقيع كالمعتاد — **بلا سعرٍ ولا خبير** الآن دائماً
  (`deviceCost`/`proposedExpertUserId` يصلانها `null` من نقطة التوقيع)،
  فتصل الاستعلاماتِ فارغةً تماماً بانتظارهم، لا فرق بين مريضٍ كان الطبيبُ
  ليقترح له سعراً يوماً ومريضٍ آخر.
- الأعمدة `medical_exams.device_cost`/`proposed_expert_user_id` **باقيةٌ في
  القاعدة**، تُقرأ للمعاينات التاريخية (`PatientMedicalExams.tsx` يعرض
  «الكلفة المقترحة» لمعايناتٍ قديمة) وتُكتب فقط عبر `PATCH` الصريح. **بلا
  ترحيل** لهذه المرحلة.

### الاختبارات
`server/exam_commercial.test.ts` (١٥٢ تأكيداً) صار يمرّ بمسارَين: القسمُ
الجديد (ي‌ب) يثبت أن حمولةً تجاريةً كاملة (سعر+خبير+قرار «اشترى») مُرسَلةً
مباشرةً إلى نقطة التوقيع **لا تُنشئ أمرَ تصنيعٍ ولا تكتب شيئاً** والمعاينةُ
تُوقَّع رغم ذلك؛ وبقيّةُ الأقسام (أ–ك) تثبت أن `setCommercialFields` نفسَها
تعمل كما كانت **عبر البابِ الباقي**. `client/src/components/money_input.test.ts`
و`client/src/components/add_case_type_ui.test.ts` صارا يثبتان **غياب**
الحقول التجارية من `NewExamDialog.tsx` بدل حضورها. وسبعةُ ملفّاتِ اختبارٍ
أخرى كانت تُثبِّت سعراً على معاينةٍ **عبر جسم التوقيع** كتفصيلِ إعداد لا
كموضوع اختبار (`exam_price_correction`, `device_episode_flow`,
`device_episode_integration`, `exam_cancellation`, `post_exam_followup`,
`purchase_dialog`, `service_discount`, `administrative_reversal`,
`first_device_identity`) — عُدِّلت لتُثبِّته بعد التوقيع مباشرةً على الصفَّين
اللذين كانت `ensureFollowupForSignedExam` تكتبهما معاً وقت التوقيع، بلا لمس
مئات مواضع الاستدعاء ولا انحرافٍ عن موضوع كلّ ملفّ.

---

## 4.i تبسيطُ بيع الاستعلامات — بابٌ واحد «إتمام البيع» (2026-08-28)

**خبيرٌ + سعرٌ أصليّ + مقدارُ خصم = بيعٌ كامل، بحفظٍ واحد.** هذه هي
«مرحلةُ تبسيط مبيعات الاستعلامات» التي أشارت إليها الأقسام 4.f و4.h — وصلت.
فرعُ `simplify-reception-sale-flow`.

### الواقعة
كانت بطاقةُ المريض تعرض على مسار المعاينة ثلاثةَ أزرار: «تفاصيل البيع»
(نافذةٌ بمحدِّد نوع سعرٍ وحقلِ سعرٍ نهائيٍّ يكتبه الموظّف يدوياً) ثمّ «اشترى»
منفصلاً — خطوتان لواقعةٍ واحدة، وحسابٌ ذهنيٌّ (السعر الأصلي ناقص الخصم) لا
تُجريه الشاشة. **والطبيبُ** كان لا يزال تقنياً يستطيع كتابة السعر أو الخبير
أو القرار عبر البابِ نفسه (`POST /api/followups/:id/commercial`) إن ناداه
مباشرةً وكان صاحبَ المعاينة — القسم 4.h وصفه صراحةً «بقاءٌ انتقاليّ لا
تصميمٌ نهائيّ».

### القاعدةُ الجديدة: الاستقبالُ يُدخل الخصمَ لا السعرَ النهائي
الموظّفُ يعرف اتفاقاً واحداً: كم السعرُ المعلَن، وكم الخصمُ عنه. فصار
المُدخَلان `originalPrice` و`discountAmount` فقط، والباقي **يُشتقّ في
الخادم**:

```
discountAmount === 0                    ⟶ normal  (نهائيّ = أصليّ)
0 < discountAmount < originalPrice      ⟶ discount (نهائيّ = أصليّ − خصم)
discountAmount === originalPrice (>٠)   ⟶ free    (نهائيّ = صفر، والأصليّ يبقى محفوظاً)
```

`shared/commercial.ts: deriveOfferFromDiscount` تشتقّ النوعَ من الخصم ثمّ
تُسلِّم الأمرَ لـ`computeCommercialOffer` نفسِها (الثوابتُ الآمنة من ترحيل
٠٦٦ لا تتكرّر). **والعميلُ لا يُرسل سعراً نهائياً ولا نوعَ سعرٍ ولا قرارَ
شراء أبداً** — ولو أرسلهم عميلٌ بائتٌ أو خبيث، **تُتجاهَل تماماً**: الخادمُ
يشتقّ الحقيقةَ الوحيدة من `originalPrice`/`discountAmount` فقط.

### البابان الجديدان (`server/followup/routes.ts`)
| النقطة | الجسم | الأثر |
|---|---|---|
| `POST /api/followups/:id/complete-sale` | `{originalPrice, discountAmount, expertUserId, note?}` | يشتقّ العرضَ، يتحقّق من الخبير، **يكتب القرارَ «اشترى» ويبدأ التصنيعَ في نداءٍ واحد** |
| `POST /api/followups/:id/not-bought` | `{reason, note?}` | يُغلق الملفَّ بسببٍ حرٍّ إلزاميّ — بلا تصنيعٍ ولا كلفةٍ ولا دينار |

**لا `decision: 'bought'` يُرسَل ولا يُطلَب** — الحفظُ **هو** القرار. ولا
خطوةَ تأكيدٍ ثانية، ولا نافذةَ اعتمادٍ، ولا صفَّ `service_discount_requests`
جديداً لهذا المسار (الخصمُ يُدخَل مباشرةً لا يُطلَب اعتمادُه — نفسُ منطق
٠٦٦: «سعرٌ أو خصمٌ أو مجّانيّةٌ يُدخلها مخوَّلٌ نافذةٌ فوراً»).

### التنفيذ — غلافان رقيقان لا محاسبةٌ ثانية
`server/followup/store.ts: completeReceptionSale` تشتقّ العرضَ ثمّ تُسلِّم
الأمرَ لـ`setCommercialFields` القانونية (`patch: {price, expertUserId,
decision:"bought"}, asDoctor: false`) — **نفسُ الدالّة** التي كتبت المسارَ
منذ ٠٦٦، تقفل الصفَّ وتفحص المالكية وتكتب وتُتمّ البيعَ عبر
`confirmPurchase` ⟶ `storage.assignManufacturing` كما كانت. وبما أنّ الحقول
الثلاثة تُلمَس معاً دائماً، تكتمل الجهوزيّةُ بهذا الحفظ نفسِه حتماً — وإلّا
رُدّت رسالةٌ صريحة (تناقضٌ منطقيّ لا حالةٌ متوقَّعة).

`completeReceptionNotBought` تتحقّق من السبب ثمّ تُسلِّم لـ`closeWithoutPurchase`
القانونية (الرمزُ الموروث المحايد `other` في `closed_reason`، والواقعةُ
الحقيقية في `not_bought_reason_text` — نفسُ نمط ٠٦٦). **ومالكيةُ القرار
تُحفَظ إن وُجدت سلفاً** (صفٌّ نادرٌ من قبل هذه المرحلة) بدل الكتابة فوقها.

**فلا نسخةَ ثانية من المحاسبة**: `storage.assignManufacturing` يبقى البابَ
الوحيدَ للتصنيع، و`setCommercialFields`/`closeWithoutPurchase` البابَين
الوحيدَين لكتابة الحقول التجارية — تماماً كما كانا قبل هذه المرحلة.

### الصلاحيات — الاستقبالُ والمحاسبُ ومديرُ الفرع والمسؤول، لا الطبيبُ إطلاقاً

> **⚠ تصحيحٌ لاحقٌ في اليوم نفسه (2026-08-28).** التصميمُ الأوّل لهذا القسم
> استبعد المحاسبَ قياساً على «تحديد السعر التجاري» القديم (مديرُ الفرع/
> المسؤول حصراً) — **قياسٌ خاطئ**: ذاك بابٌ مختلف، وهذا بيعٌ يقرّره مَن
> يكلّم المريض، والمحاسبُ يفعل ذلك يومياً تماماً كالاستقبال. وما يلي هو
> **القاعدةُ النهائية** بعد التصحيح.

`shared/commercial.ts: canCompleteReceptionSale(session)` — `isAdmin`
تمرّ أوّلاً وبلا قيد (بسلطتها لا بدور صاحبها)، وإلّا `role ∈
{reception, accountant, branch_manager}`. **هذه الدالّةُ الواحدة** تحرس
البابين الجديدين، **وتحرس أيضاً القرارَ بين الأبواب القديمة والجديدة على
مسار المعاينة** (التالي) — مصدرُ حقيقةٍ واحد لـ«مَن يبيع على المسار
المبسّط»، والاسمُ بقي `canCompleteReceptionSale` رغم مشاركة المحاسب — لا
داعي لتغييرٍ يُشتّت مواضع الاستدعاء.

**والطبيبُ لا يشمله الشرط إطلاقاً — لا سلطةَ تجاريةً له مهما كانت الصفة**:
لا دورَ خاصّاً اسمُه «الطبيبُ المسؤول» في هذه الدالّة ولا في أيّ مكانٍ آخر.
**والطبيبُ المسؤولُ الفعليّ في النظام القائم يحمل `isAdmin`**، فسلطتُه
التجاريةُ الكاملة تمرّ من هنا — لا من دورٍ طبّيٍّ خاص، ولا من هويّةٍ مكتوبةٍ
في الكود (لا اسمَ ولا بريدَ ولا معرّفَ مستخدمٍ ثابتاً في أيّ ملفّ من هذه
المرحلة).

### الأبوابُ القديمة على مسار المعاينة — عقدُ مسارٍ شامل، لا حارسان مختلفان

> **⚠ تصحيحٌ لاحقٌ في اليوم نفسه.** التصميمُ الأوّل هنا كان حارسين
> منفصلين: `retiredOnExamPath` (عامّةٌ، ٤٠٩) لـ`/defer`/`/accept-price`/
> `/purchase-interest`/`/close`، وحارسٌ **دوريّ** جديد `blockedOnNewExamPath`
> (٤٠٣) لـ`/expert`/`/commercial`/`confirmPurchaseHandler` يستثني
> الاستقبالَ ومديرَ الفرع والمسؤولَ من التقاعد ويحجب الطبيبَ وحده. **وهذا
> خطأٌ صُحِّح صراحةً**: البابُ الجديد هو البابُ الوحيدُ الحيّ على مسار
> المعاينة — للجميع بلا استثناءٍ للدور، وليس للطبيب وحده. `blockedOnNewExamPath`
> **أُزيلت بالكامل**.

**حارسٌ واحد الآن**: `retiredOnExamPath` (`server/followup/routes.ts`) —
موجودةٌ منذ ٠٦٦ لـ`/defer`/`/accept-price`/`/purchase-interest`، **وصارت
تحرس أيضاً** `/expert`، `/commercial`، `/commercial-price`،
`confirmPurchaseHandler` (يخدم `/confirm-purchase` و`/approve-purchase`
معاً)، و`/close`. إن كانت المتابعةُ على مسار المعاينة
(`store.isExamPathFollowup`) ⟶ **٤٠٩ للجميع بلا استثناء** — الاستقبالُ
والمحاسبُ ومديرُ الفرع والمسؤولُ العام والطبيب سواء، **مهما ملكوا من
صلاحيةٍ على البابِ القديم أو من حقولٍ مملوكةٍ لهم من قبل هذه المرحلة**.
**وهذا عقدُ مسارٍ لا قيدُ صلاحية**: الردُّ ٤٠٩ لا ٤٠٣، لأن المشكلة «هذا
البابُ ليس بابَ هذه العملية» لا «أنت لا تملك صلاحية». ومَن يملك الصلاحية
فعلاً يستعمل `/complete-sale`/`/not-bought` بدلاً منه.

**وبعضُ هذه الأبواب محروسةٌ بدورٍ من الأصل** (`/close` بـ`canActCommercially`
تستبعد المحاسب، و`/commercial-price` بـ`canSetCommercialPrice` تقصرها على
مديرِ الفرع/المسؤول) — فمَن لم يكن مخوَّلاً بالباب أصلاً يبقى يُردّ ٤٠٣
كما كان قبل هذا التصحيح، وهذا **قيدُ دورٍ سابقٌ لا علاقةَ له بمسار
المعاينة**؛ ومَن كان مخوَّلاً بالباب القديم يُردّ الآن ٤٠٩ بدل أن يُقبَل.

**والصفوفُ الموروثة لا تُمَسّ إطلاقاً**: متابعةٌ بلا حلقة أو حلقةٌ بلا
مسار (`service_path IS NULL`) — `isExamPathFollowup` تُرجع `false` لها —
تبقى على كامل صلاحياتها القديمة **بحرفها**، **بما فيها الطبيبُ**: `/expert`،
`/commercial`، `/commercial-price`، `/confirm-purchase`، `/close` كلُّها
تعمل له كما كانت (مثبَتٌ حيّاً في `post_exam_followup.test.ts` قسم
«مشاركةُ الطبيب»، بلا تعديل — وفي `server/exam_commercial.test.ts` الذي
صار يستعمل الصفوفَ الموروثة عمداً ليبقى يختبر آليّةَ `setCommercialFields`
عبر بابها الأصليّ). وحقلٌ مملوكٌ للطبيب من قبل هذه المرحلة على صفٍّ موروث
**يبقى محفوظاً ومحروساً** بنفس حارس المالكية (`canOverwriteCommercialField`)
— صاحبُه أو المسؤولُ العام، كما كان قبل هذه المرحلة بحرفه.

**والخادمُ يقول الحقيقةَ لا الشاشةُ وحدها**: نقطةُ `GET
/api/followups/patient/:patientId` كانت تشتقّ `actions` (وبها أزرار
الشاشة) من `canConfirmPurchase` القديمة — التي تشمل الطبيبَ والمحاسبَ على
كلّ متابعة. فصارت تُشتقّ من `canCompleteReceptionSale` **حصراً على مسار
المعاينة**، فلا يظهر للطبيب زرٌّ سيردّه الخادمُ ٤٠٩ لو ضغطه.

### الواجهة (`client/src/components/PostExamDecisionCard.tsx`)
زرّان لا ثلاثة: **«إتمام البيع»** يفتح نافذةً بأربعة عناصر فقط — الخبير،
السعر الأصلي، مقدار الخصم، **والسعر النهائي للقراءة فقط** (معاينةٌ حيّة
بـ`deriveOfferFromDiscount` نفسِها، لا تُرسَل في الطلب — الخادمُ يشتقّه
ويعتمده وحده). ومجّانيّةٌ صريحة («مجاني — ٠ د.ع») حين يساوي الخصمُ الأصلَ.
زرُّ الحفظ «حفظ البيع وبدء التصنيع» ينادي `/complete-sale`. **و«لم يشترِ»**
بقي فعلاً منفصلاً بحقل سببٍ حرٍّ إلزاميّ، ينادي `/not-bought` الآن بدل
`/commercial`. وبعد الشراء تظهر السعرُ الأصليّ والخصمُ والنهائيّ والخبير
صراحةً تحت «تم الشراء — بدأ التصنيع» — **لأيّ متابعةٍ تحوّلت، بصرف النظر
عن رايةِ «يرغب بالشراء» القديمة** (`purchaseInterestAt`؛ تصحيحٌ لاحقٌ في
اليوم نفسه: كان الملخّصُ يُخفى إن حملت المتابعةُ تلك الرايةَ من مسارٍ قديم،
فيختفي بيعٌ حقيقيٌّ عن القراءة). بطاقةُ الرايةِ التاريخية تبقى تُعرَض
بجانبه لا بدلاً عنه. **ولا محدِّدَ نوع سعرٍ ولا حقلَ سعرٍ نهائيٍّ قابلاً
للتحرير بعد اليوم**، ولا عرضَ مالكيةٍ حقلاً حقلاً (كان موجوداً، حُذف مع
الأزرار الثلاثة القديمة — الطبيبُ لم يعد يكتب حيّاً على هذا المسار فلا
حاجةَ لعرض «أدخله د. فلان» فيه).

### ملاحظاتُ الطبيب من المعاينة — سياقٌ للبائع، لا حقيقةٌ مالية

الحقلُ السرديُّ الحرّ `medical_exams.notes` (موجودٌ من قبل هذه المرحلة،
القسم 4.b) صار يصل بطاقةَ البيع **للقراءة فقط**، قبل حقول إتمام البيع
مباشرةً وحين يكون غيرَ فارغ: «ملاحظاتُ الطبيب من المعاينة» ثمّ النصُّ كما
كُتب، ثمّ سطرٌ «للاطلاع فقط — السعر المعتمد هو المسجل في إتمام البيع.»

**السببُ العمليّ**: الطبيبُ يكتب أحياناً في الملاحظات سعراً تقريبياً
ناقشه المريضُ، أو تفضيلَ دفعٍ، أو مَن يريد المريضُ أن يتولّى جهازه — سياقٌ
مفيدٌ للبائع لا قرارٌ تجاريّ. **ولا يُقرأ برمجياً أبداً**: لا يُشتقّ منه
سعرٌ ولا خصمٌ ولا خبيرٌ ولا قرارُ شراء، والبيعُ الفعليُّ من حقول
`/complete-sale` الصريحة وحدها — لو ذكرت الملاحظةُ ١,٥٠٠,٠٠٠ وأدخل
الموظّفُ ١,٣٠٠,٠٠٠ صراحةً، فالحقيقةُ المالية ١,٣٠٠,٠٠٠ والملاحظةُ تبقى
كما كُتبت بحرفها.

**والهويّةُ دقيقة**: النصُّ يصل عبر `post_exam_followups.medical_exam_id
→ medical_exams.id` — علاقةُ التوقيع القانونية نفسُها، لا «آخرُ معاينةٍ
للمريض». مريضٌ بجهازين (أطرافٌ ومساند) له معاينتان ومتابعتان، ولكلٍّ
ملاحظتُها هي فقط. **ولا عمودَ جديداً ولا جدولَ جديداً ولا تعديلَ على سلوك
المعاينة الطبّية** — `getFollowupsForPatient` (`server/followup/store.ts`)
يقرأ العمودَ الموجود عبر `JOIN` قائم أصلاً ويضيفه إلى الاستجابة
(`examNotes`)، لا أكثر.

**ومَن يراها**: مَن يصل بطاقةَ البيع — الاستقبالُ والمحاسبُ ومديرُ الفرع
والمسؤولُ العام، ضمن نطاق الفرع كالسجلّ السريريّ عموماً. الطبيبُ لا يرى
أزرارَ البيع أصلاً فلا يظهر له هذا العرض في هذا الموضع (يقرأ ملاحظاته
كالمعتاد في سجلّ المعاينات نفسه).

### التزامن
ضغطتان متزامنتان على `/complete-sale` لنفس المتابعة: قفلُ الصفّ في
`setCommercialFields` يجعل الثانية تنتظر الأولى ثمّ تقرأ `converted`
فتُردّ — **أمرُ تصنيعٍ واحد وقيدُ كلفةٍ واحد بالضبط**، ولا نصفَ كتابة.

### وما لم يتغيّر
`/commercial-price` **على الصفوف الموروثة** (مديرُ الفرع/المسؤول فقط، لم
يكن الطبيبُ مخوَّلاً به أصلاً — وعلى مسار المعاينة صار متقاعداً ٤٠٩ كبقيّة
القائمة، القسم أعلاه) · محاسبةُ العلاج الطبيعي وجلساته وخططه
(`shared/pricing.ts` بلا لمسة، ويحرسه اختبارٌ معماريّ) · طابورُ الخصومات
القديم (`service_discount_requests` يبقى مقروءاً وقابلاً للحسم بمسارِه) ·
إلغاءُ المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) · تصحيحُ السعر بعد البيع ·
حذفُ المريض وسلّتُه (٠٦٨).

### الترحيل
**بلا ترحيل** — لا عمودَ جديداً ولا جدولَ جديداً. البابان الجديدان
يشتقّان من `originalPrice`/`discountAmount` في الطلب ويكتبان الأعمدةَ
القائمة نفسَها (`approved_price`, `original_price`, `price_kind`) عبر
الكاتبين القانونيَّين القائمين.

اختبارٌ دائم: `npm run test:reception-sale` (يثبت الصلاحياتِ الأربع
— الاستقبال والمحاسب ومديرَ الفرع والمسؤولَ، بالتكافؤ الكامل بين الاستقبال
والمحاسب في كلّ فعل — ورفضَ الطبيب المطلَق، وتطابقَ الأفعال المُرسَلة من
الخادم للمحاسب والاستقبال وخلوَّها للطبيب، وملاحظاتِ الطبيب من المعاينة
(الربطُ بالمتابعة الصحيحة، العزلُ بين جهازين، غيابُها الصريح، عدمُ تحليلها
مالياً)، اشتقاقَ السعر بأنواعه الثلاثة، التحقّقَ قبل الكتابة، تجاهلَ القيم
المحسوبة من العميل، غيابَ `service_discount_requests`، الحفظَ الواحد،
التزامنَ، «لم يشترِ» بأركانها، **التقاعدَ الشاملَ لكلّ الأبواب القديمة على
مسار المعاينة (٦ أبوابٍ × ٥ فاعلين) وبقاءَها كاملةً على الصفوف الموروثة**،
رفضَ البابين الجديدين للصفوف الموروثة بصفرِ كتابة، وعدمَ محو السجلّات
التاريخية) و`npm run test:exam-commercial` (مُحدَّثٌ ليثبت **التقاعدَ
الشاملَ** — لا الطبيبَ وحده — على مسار المعاينة، وآليّةَ `setCommercialFields`
عبر الصفوف الموروثة عمداً حيث بابُها القديم لا يزال حيّاً: المالكيةُ
للموظّفين وللطبيب صاحبِ المعاينة، الحفظُ الجزئيّ ثمّ الإكمال، التزامنُ،
المجّانيّةُ الصريحة، العزلُ، الحذفُ الكامل، عقدُ الشاشة) و
`npm run test:exam-edit-commercial` (سلامةُ المرحلة الأولى، بلا تغيير).

---

## 4.j تبسيطُ الصيانة — عمليةٌ وحيدة، حفظٌ وحيد، بلا طبيب (ترحيل ٠٦٩ — 2026-08-28)

**فتحُ صيانةٍ = جهازٌ + قطعةٌ إن لزمت + خبيرٌ + سعرٌ أصليّ + خصمٌ، بحفظٍ واحد
يفتح أمرَ العمل ويقيّد الكلفة معاً.** فرعُ `simplify-maintenance-flow`، ولا
علاقةَ له بالقسم 4.i سوى تكرار مبدئه على بابٍ آخر: **الاستقبالُ يبيع، لا
يُراجَع**.

### الواقعة
كانت الصيانةُ (كالبيع بلا معاينة، القسم 4.e، قبل قرار المالك 2026-08-26)
تفتح البابَ `POST /api/manufacturing/maintenance-visit` بعقدٍ قديم: مبلغٌ أو
علمُ «بلا أجور»، وسؤالُ منشأ الجهاز (مسجَّل/غير مسجَّل)، ومساران خلفَه —
مبلغٌ كاملٌ ينفَّذ فوراً، أو خصمٌ يذهب لطابور `service_discount_requests`
بانتظار اعتماد مديرٍ، **وصيانةٌ تُنشئ أيضاً طلبَ مراجعةٍ طبّية استرجاعية**
(`reviewKind = maintenance`، القسم 4.e). **ولم يبقَ لهذا كلِّه مبرِّرٌ** بعد
أن صار مبدأ «الموظّفُ يُدخل الاتفاق، لا يُخمَّن لاحقاً، ولا شيءَ ينتظر
طبيباً» راسخاً في القسمين 4.e (بعد قرار 2026-08-26) و4.i: لماذا يُدخل
الاستقبالُ سعراً نهائياً للصيانة بينما يُدخل خصماً صريحاً للجلسات الإضافية
وبيع الأجهزة بلا معاينة، ولماذا تبقى الصيانةُ وحدها تُزعج طابورَ الطبيب
بعملٍ لا قرارَ سريريّاً فيه؟ **فوُحِّد النمطُ الثالث معهما، وسقط طابورُ
المراجعة عن الصيانة تحديداً.**

### القاعدة: originalPrice − discountAmount = finalPrice، يشتقّها الخادم
نفسُ اشتقاق القسم 4.i حرفياً، معادَ تصديره لا معاداً كتابتُه:
`shared/maintenance.ts: deriveMaintenanceOffer` هو
`shared/commercial.ts: deriveOfferFromDiscount` بذاته (إعادةُ تصديرٍ، لا
نسخةٌ ثانية) — `discountAmount=0 ⟶ عاديّ` · `0<discount<original ⟶ خصم` ·
`discount=original(>٠) ⟶ مجّانيّ بسعرٍ أصليٍّ محفوظ`. **والعميلُ لا يُرسل
سعراً نهائياً ولا نوعَ سعرٍ أبداً** — الخادمُ يشتقّهما ويعتمدهما وحده.

### قدرةٌ جديدة، لا إعادةَ استعمال
`shared/maintenance.ts: canCompleteMaintenance(session)` —
`isAdmin ⟶ يمضي` وإلّا `role ∈ {reception, accountant, branch_manager} ⟶
يمضي` وإلّا يُرفَض. **قدرةٌ قائمةٌ بذاتها لا اسماً بديلاً**: لم تُستعمل
`canOperateNoExam` (القسم 4.e) لأنها تعتمد على `canAddPatients` — تبعيةٌ
غيرُ نقية لا يحتاجها هذا الباب. ولم تُستعمل `canCompleteReceptionSale`
(القسم 4.i) رغم تطابق المنطق **حرفاً بحرف** — الدالّتان مستقلّتان عمداً
فيتطوّر كلُّ مسارٍ (بيعُ ما بعد المعاينة، وصيانةُ الأجهزة) بلا أن يخشى
تعديلُ أحدهما كسرَ الآخر. **الطبيبُ العاديّ خارجَ الشرط كلّياً ولو نادى
النقطةَ مباشرةً**؛ وسلطةُ الطبيب المسؤول تمرّ من `isAdmin` فقط كبقيّة
القسم 4.i — لا دورَ خاصّاً باسمه في أيّ مكان.

### الباب: `POST /api/no-exam/maintenance` — نفسُه، بعقدٍ جديد
لا بابَ جديداً، **العقدُ تبدّل**: `{patientId, serviceType, deviceEpisodeId?,
legacyUnrecordedDevice?, expertUserId, maintenanceComponent?, originalPrice,
discountAmount, note?}`. **وحقولُ العقد القديم تُرفَض صراحةً** — `charged`
أو `amount` أو `deviceOrigin` في الجسم ⟶ ٤٠٠ برسالةٍ تدلّ على العقد الجديد،
لا تجاهلٌ صامت يُحسَب على الخادم قيمةً افتراضية لم يقصدها أحد. والخادمُ
يتحقّق أن الحالة/المريض يملكان الخدمةَ المطلوبة كما كان. **وسطرُ
`audit_log` يحمل السياقَ الهيكليّ كاملاً**: هويّةَ الجهاز أو التصريحَ
الصريح بعدم التسجيل، القطعةَ، الخبيرَ، الأصليَّ والخصمَ والنهائيَّ والنوعَ،
ومَن ومتى ولماذا — لا نصّاً حرّاً وحده.

### هويّةُ الجهاز — نيّةٌ صريحة، لا سؤالُ منشأ
`shared/maintenance.ts: parseMaintenanceDeviceTarget` — تحقّقٌ نقيّ: واحدٌ
فقط من `deviceEpisodeId` موجب أو `legacyUnrecordedDevice === true`، وإلّا
٤٠٠. **سؤالُ منشأ الجهاز (مسجَّل/غير مسجَّل/خارجيّ) حُذف تماماً من هذا
المسار** — لم يعد الموظّفُ يُصنِّف الجهاز، بل يقول فقط أهو جهازٌ **له حلقةٌ
في النظام** أم لا. والحلّالُ القانونيّ القائم `resolveDeviceTargetTx`
(القسم 4.e) هو مَن يحسم الهويّة لجهازٍ مسجَّل — **بلا حلقةٍ تُخترَع** لجهازٍ
غير مسجَّل. `prosthetic_work_orders.device_origin` **يبقى `NULL` دائماً**
على كلّ أمرِ صيانةٍ جديد من هذا الباب — العمودُ باقٍ في القاعدة للقراءة
التاريخية، و`shared/device_origin.ts` يبقى لذلك وحده.

**واجهةُ الاختيار مبنيّةٌ على أفكار PR #254 غيرِ المدموج** (المنطقُ لا
الكود — الـPR نفسُه لم يُمَسّ ولم يُدمَج): `client/src/components/
maintenance_device_target.ts` يفرّق حالاتِ استعلام React Query الأربع —
تحميل/إعادةُ جلب/خطأ/نجاحٌ مستقرّ بصفر نتائج — فلا يُقرَأ التحميلُ أو الخطأُ
خطأً بوصفه «لا أجهزة مسجَّلة» (الالتباسُ الذي كان في التصميم الأصل). الخيارُ
«جهاز غير مسجَّل في النظام» **صريحٌ في القائمة دائماً** بجانب الأجهزة
المسلَّمة الفعلية، لا حقلاً منفصلاً.

### القطعة — للأطراف فقط، كما كانت
`maintenanceComponent` عبر `parseComponent` القائمة (القسم 4.e) تُشترَط
لـ`prosthetic` ولا تُسأل ولا تُقبَل لـ`medical_support` — بلا تغييرٍ في
قائمة الأجزاء الثمانية.

### الخبيرُ من فرع العملية الفعليّ
قائمةُ الخبراء تُجلَب بفرع العملية الحقيقيّ (`GET /api/manufacturing/experts
?branchId=...`) لا فرع جلسة الموظّف المخمَّن — نفسُ إصلاح فرعٍ آخر من مهمّةٍ
سابقة على هذا الفرع نفسه. والحفظُ **يُعيد التحقّق** عبر
`validateExpertForBranch` فلا تُقبَل قائمةٌ بائتة.

### الكاتبُ القانونيّ يُعاد استعماله، لا يُستنسَخ
`createMaintenanceOrderWithVisit` (موجودةٌ من قبل) **مُدَّدَت لا استُبدلت**:
معاملٌ اختياريّ جديد `commercialTerms: {originalPrice, kind} | null`، حين
يصل يكتب `maintenance_original_price`/`maintenance_final_price`/
`maintenance_price_kind` في نفس إدراج أمر العمل؛ غيابُه (كلُّ نداءات طابور
الخصم القديم في `server/discounts/store.ts`) يُبقي الأعمدةَ الثلاثةَ
`NULL` كما كانت. **الكاتبُ الداخليّ `postMaintenanceFee` لم يُمَسّ بحرف** —
هو مَن يقيّد كلفةَ الحالة و`patients.total_cost` وقيدَ الدفتر، ويُنادى من
داخل `createMaintenanceOrderWithVisit` كما كان دائماً. **حفظٌ واحد ⟶ كتابةٌ
واحدة ذرّية**: أمرُ عمل + زيارة + قيدُ كلفة، أو لا شيء.

### مجّانيّةٌ حقيقية، لا غيابَ سعر
`priceKind === "free"` عمليةٌ حقيقية بأمر عملٍ وزيارةٍ وكلفةِ صفر — تُكتب
صراحةً لا تُستدَلّ عليها من غياب مبلغ. والعلمُ الموروث `no_exam_no_charge`
يبقى للتوافق: يُكتب `true` للجديد المجّانيّ صراحةً و`false` للمدفوع أو
المخصوم، **ولا يُعاد كتابتُه أبداً على صفٍّ قديم**.

### الترحيل ٠٦٩ — أعمدةٌ إضافية، لا جدولَ ثانياً
فُحص المخطّطُ أوّلاً: `prosthetic_work_orders` لم يكن يحمل تخزيناً هيكلياً
لأصليّ/نهائيّ/نوعٍ للصيانة تحديداً (خلافاً لـ`post_exam_followups` التي
تحمل مثيلاتٍ لمسار المعاينة منذ ٠٦٦) — فلا ازدواجَ حقيقة. **ثلاثةُ أعمدة
NULLable** على `prosthetic_work_orders`: `maintenance_original_price
INTEGER` · `maintenance_final_price INTEGER` · `maintenance_price_kind
TEXT`، **بلا عمود خصمٍ رابع** (يُشتقّ: أصليّ − نهائيّ). الصفوفُ التاريخية
تبقى `NULL` **بلا تعبئة رجعية** — لم تُسجَّل لها هذه الوقائع، وكتابةُ رقمٍ
عليها ادّعاءُ علمٍ بالماضي (نفسُ درس ٠٣٨/٠٣٥). وأربعةُ قيود `CHECK`:
تكامل الثلاثي (معاً أو لا شيء) · `purpose='maintenance'` حين تحضر ·
أصليّ>٠ ونهائيّ∈[٠,أصليّ] · واتّساق النوع مع الأرقام (عاديّ⟺نهائي=أصليّ،
خصمٌ⟺٠<نهائي<أصليّ، مجّانيّ⟺نهائي=٠). طُبِّق محلياً وأُعيد **idempotent**،
وأُثبتت القيودُ الأربعة حيّةً بمصفوفة SAVEPOINT قبل تشغيل حزمة الاختبارات
الآلية.

### البابُ القديم يتقاعد — لا يُحذف كاتبُه
`POST /api/manufacturing/maintenance-visit` **يبقى موجوداً بفحص صلاحيته
الأصليّ حرفياً**، ثمّ يردّ **٤٠٩** لكلّ مَن يجتازه — نفسُ مبدأ «تقاعدُ
البابِ لا حذفُ الكاتب» من القسم 4.i. `createMaintenanceOrderWithVisit`
نفسُها **تبقى** لأن `server/discounts/store.ts` يناديها مباشرةً (لا عبر
هذا الباب) لتنفيذ طلبات الخصم **القديمة** المعلَّقة من قبل هذه المرحلة —
اعتمادُها يعمل كما كان بلا حرفٍ يتغيّر، وطابورُها يبقى مقروءاً وقابلاً
للحسم بمساره القائم.

### صفرُ صفوفٍ في الطوابير القديمة، وصفرُ مراجعةٍ طبية
الصيانةُ الجديدة **لا تُنشئ** صفّاً في `pending_service_charges` ولا في
`service_discount_requests` ولا في طابور المراجعة الطبية — ولا تُنادي
`routeRetrospectiveReview` ولا `routeServiceToDoctorReview` إطلاقاً. الطبيبُ
لا يرى الصيانةَ في أيّ طابورٍ بعد اليوم — لا معاينةً ولا مراجعةً استرجاعية.
(بيعُ الجهاز بلا معاينة، القسم 4.e، **خارج نطاق هذه المرحلة**: يبقى يمرّ
بمراجعة الطبيب الاسترجاعية كما هو تماماً.)

### الواجهة (`NoExamOperationDialog.tsx`) — فرعُ الصيانة وحده
فرعُ `device_sale` **لم يُمَسّ بحرف**. فرعُ `maintenance` أُعيد بناؤه: جهازٌ
(بحالاته الأربع: تحميل/خطأ+إعادة/فارغٌ للقراءة فقط/قائمةٌ باختيار) ⟵ قطعةٌ
(للأطراف فقط) ⟵ خبير ⟵ سعرٌ أصليّ ⟵ خصمٌ ⟵ **نهائيّ للقراءة فقط** (محسوبٌ
حيّاً بنفس دالّة الخادم) ⟵ ملاحظة. **حُذف من هذا الفرع**: سؤالُ منشأ
الجهاز، مربّعُ «بلا أجور»، حقلُ المبلغ النهائيّ القابل للتحرير، سببُ
الخصم، وأيُّ صياغةِ اعتمادٍ أو مراجعةِ طبيب. رسالةُ النجاح **«تم تسجيل
الصيانة وفتح أمر العمل»** حصراً — لا صياغةَ اعتمادٍ ولا مراجعةَ طبيب
تظهر لهذا الفرع أبداً.

### مَن يرى خيار «صيانة» في مُشغِّل الخدمة
`reception_routing.ts` صار يقبل `session` اختيارياً ويُخفي خيارَ الصيانة
عمّن يفشل `canCompleteMaintenance` — فلا يظهر للطبيب في قائمة «ما سبب
حضور المريض اليوم؟» أصلاً، مع بقاء الخادم الحارسَ الحقيقيّ بصرف النظر عن
الواجهة. `PatientServiceLauncher.tsx` يمرّر جلسةَ الفرع الحيّة
(`useBranchSession`، نمطٌ قائم يستعمله الشريط الجانبيّ أصلاً) لا جلسةً
مخمَّنة.

### التزامن
ضغطتان متزامنتان على نفس الهدف: حُرّاسُ العملية النشطة وقفلُ الحلقة/الخيط
القائمة أصلاً (القسم 4.e) تمنع أمرَي عملٍ أو قيدَي كلفة — لا إضعافَ لأيّ
حارس قائم.

### وما لم يتغيّر
بيعُ الجهاز بلا معاينة ومراجعتُه الاسترجاعية (القسم 4.e) · القسم 4.i
كاملاً · القبضُ والفواتير والإيرادُ الحيّ · تسعيرُ العلاج الطبيعي وجلساته ·
سجلُّ المعاينة الطبّية · سلّةُ المرضى (٠٦٨) · PR #254 (غيرُ مدموج، لم
يُعدَّل، وأفكارُه فقط استُلهمت لا كودُه).

### الترحيل
**٠٦٩ فقط** — إضافيّ، idempotent: ثلاثةُ أعمدةٍ على
`prosthetic_work_orders` وأربعةُ قيود `CHECK`. بلا `DROP` ولا `DELETE` ولا
مسٍّ لترحيلٍ من ٠٠١ إلى ٠٦٨.

اختبارٌ دائم: `npm run test:maintenance-simplify` (بعد التصحيح أدناه: ١١٤
تأكيداً حيّاً على Postgres وعلى النقاط الحقيقية)، ومعه مراجعةُ ارتداد كاملة
على كلّ ملفّ اختبارٍ قديم كان يستدعي البابَ المتقاعد (`pending_charge.test.ts` ·
`manufacturing/maintenance_service_type.test.ts` · `departments.test.ts` ·
`medical_review.test.ts` · `device_episode_integration.test.ts` ·
`returning_patient.test.ts` · `patient_service_launcher.test.ts` ·
`reception_routing.test.ts`) — كلُّها محدَّثةٌ لتُثبت السلوكَ الجديد بدل
القديم، وتمرّ كاملةً.

### ⚠ تصحيحٌ لاحقٌ في اليوم نفسه — حارسان معامَليّان في `createMaintenanceOperation`
مراجعةٌ حيّة كشفت ثغرتين هادئتين على هذا المسار تحديداً، ومعهما إصلاحاً
مستقلّاً في اختيار الجهاز — الثلاثةُ في PR #257 نفسِه، بلا فرعٍ جديد.

**١) الجهازُ — إعادةُ جلبٍ خلفية كانت تُقرأ نجاحاً مستقرّاً.**
`client/src/components/maintenance_device_target.ts: devicePhaseOf` كانت
تحسم `choose`/`none` من `isSuccess` وحدها، فإعادةُ جلبٍ خلفية
(`isSuccess: true` مع `isFetching: true`) كانت تُبقي الحفظَ مفتوحاً فوق
بياناتٍ قد تكون شاخت تلك اللحظة. صار الشرطُ `isSuccess && !isFetching` —
أيّ إعادة جلبٍ تبقى `loading` وتمنع الحفظ. اختبارٌ دائم جديد ومباشر (لا
مصدرَ شيفرةٍ يُقرَأ فقط): `npm run test:maintenance-device-target`
(`client/src/components/maintenance_device_target.test.ts`، ١٦ تأكيداً).

**٢) الحالةُ الحقيقية — لا عَلَمُ المريض وحده.** `patients.is_amputee`/
`is_medical_support` مجرّد عَلَمَين تقترحان `serviceType` مبكّراً في النقطة،
وقد ينحرفان عن `patient_cases` الفعلية (ملفٌّ قديم، أو حالةٌ سُحبت بـ
`deleteCaseType` وبقي العَلَمُ). ولو وصل الانحرافُ إلى
`createMaintenanceOrderWithVisit` بلا حارسٍ هنا، كان مخرجُه الموروث
(حالةٌ مطابقة ⟵ فيزيو ⟵ أوّل حالة) يُسند الزيارةَ لحالةٍ لا تخصّ هذه
الصيانةَ فعلاً — **والمخرجُ الموروث نفسُه لم يُمَسّ** (لا يزال يخدم الصيانةَ
الكاملةَ الأجر واعتمادَ الخصم الموروث كما هو)؛ الحارسُ الجديد **قبله**
خاصٌّ بالمرحلة الثالثة وحدها.

**٣) الخبيرُ تحت القفل — لا لقطةَ النقطة.** النقطةُ تتحقّق من الخبير قبل
فتح المعاملة لرسالةِ خطأٍ مبكرة، وتلك قراءةٌ خارج القفل. فصار يُعاد التحقّق
داخل معاملة `createMaintenanceOperation` نفسِها، بـ`validateExpertForBranchTx`
القانونية (نفسُها التي يستعملها اعتمادُ بيع الجزء الموروث) — لا نسخةَ ثانية.

**والحارسان معاً قبل أيّ نداءٍ لـ`createMaintenanceOrderWithVisit`**: رفضٌ
هنا يعني صفرَ كتابة — لا أمرَ ولا زيارةَ ولا قيدَ ولا لمسَ كلفة. مكانُهما
`server/pending_charges/store.ts: createMaintenanceOperation` وحدها — لا
تعديلَ في `createMaintenanceOrderWithVisit` ولا `postMaintenanceFee` ولا
مسارِ الصيانة الموروث ولا اعتمادِ الخصم الموروث، وكلاهما محفوظان بحرفهما
لعملائهما الأصليّين. **وبلا ترحيل**: لا عمودَ جديداً ولا تعديلَ في ترحيل
٠٦٩ — الحارسان يقرآن `patient_cases`/`system_users` القائمتين فقط.

خمسةُ سيناريوهاتٍ جديدة أُضيفت إلى `npm run test:maintenance-simplify`
(قسم «س»): عَلَمٌ بلا حالةٍ إطلاقاً ⟶ ٤٠٠ بصفر كتابة · حالةُ فيزيو فقط
لصاحب عَلَم «أطراف» أو «مساند» ⟶ ٤٠٠ ولا إسنادَ لها (مرّتان) · الحالةُ
الصحيحة موجودة ⟶ ٢٠١ وكلُّ الأرقام تتطابق (بسعرٍ موجب ومجّانيّاً معاً) ·
وخبيرٌ غيرُ صالحٍ لفرع العملية يُرفَض **من داخل المعاملة نفسِها** بنداءٍ
مباشر للمخزن يتجاوز فحصَ النقطة المبكّر تماماً. ومعها إصلاحُ فِكستشرَي
`manufacturing/maintenance_service_type.test.ts` (إضافةُ صفوف `patient_cases`
المطابقة التي كان الاختبارُ يفترض عَلَماً وحده يكفي بدلها).

---

## 4.k تبسيطُ بيع الجزء — حفظٌ واحد يفتح الحلقةَ ويبيعها معاً، بلا طبيب (ترحيل ٠٧٠ — 2026-08-28)

**جزءٌ + خبيرٌ + سعرٌ أصليّ + خصمٌ = بيعٌ كامل، بحفظٍ واحد يفتح الحلقةَ ويقيّد
المبلغ معاً.** تكرارُ مبدأ القسمين 4.i و4.j على البابِ الثالث الأخير، وبها
يكتمل تفريغُ الطبيب من كلّ سلطةٍ تجارية على عمليات «بلا معاينة» الثلاث
(جلساتٌ إضافية · صيانة · بيعُ جزء). فرعُ `simplify-component-sale-flow`،
ولا علاقةَ له بـPR #254 (غيرُ مدموج، لم يُمَسّ).

### الواقعة
كان بيعُ جزءٍ من طرفٍ صناعي بلا معاينة (ترحيل ٠٦٧، القسم 4.e) **نداءين
منفصلين**: `POST /api/patients/:id/device-episodes` يفتح الحلقة، ثمّ
`POST /api/no-exam/device-sale` (بعقده القديم: `charged`/`amount`) يبيعها.
فينجح الأوّل ويفشل الثاني — انقطاعُ شبكة، تبويبٌ أُغلق، ضغطةٌ لم تكتمل —
فتبقى **حلقةٌ يتيمة** `awaiting_exam` بلا أمرٍ ولا مال، لا الموظّفُ يعرف
أكمِلت العمليةُ أم لا ولا نظامٌ يذكّره. وحتى حين ينجح النداءان معاً، كان
البيعُ **آخرَ** بابٍ لا يزال يُنشئ سجلَّ مراجعةٍ للطبيب (`routeRetrospectiveReview`)
— الصيانةُ توقّفت عن ذلك في القسم 4.j، وبيعُ الجزء بقي وحده.

### القاعدة: نداءٌ واحد، لا اثنان
حفظٌ واحد **يفتح الحلقةَ (أو يستأنف حلقةً موروثة يتيمة بمعرّفها) ويبيعها
ويقيّد كلفتها معاً في معاملةٍ واحدة** — فلا حالةٌ وسيطة ممكنة: إمّا العمليةُ
كاملةً أو لا شيء. والسعرُ **يُشتقّ في الخادم** بنفس معادلة القسمين 4.i
و4.j حرفياً — لا حسابَ ثالث:
```
discountAmount === 0                    ⟶ normal   (نهائيّ = أصليّ)
0 < discountAmount < originalPrice      ⟶ discount (نهائيّ = أصليّ − خصم)
discountAmount === originalPrice (>٠)   ⟶ free     (نهائيّ = صفر، والأصليّ يبقى محفوظاً)
```
`shared/component_sale.ts: deriveComponentSaleOffer` هو
`shared/commercial.ts: deriveOfferFromDiscount` بذاته (إعادةُ تصديرٍ، لا
نسخةٌ ثالثة). **والعميلُ لا يُرسل سعراً نهائياً ولا نوعَ سعرٍ أبداً** — وإن
أرسلهما عميلٌ بائتٌ أو خبيث، يُتجاهَلان تماماً.

### قدرةٌ رابعة مستقلّة، لا إعادةَ استعمال
`shared/component_sale.ts: canCompleteComponentSale(session)` —
`isAdmin ⟶ يمضي` وإلّا `role ∈ {reception, accountant, branch_manager} ⟶
يمضي` وإلّا يُرفَض. **دالّةٌ رابعةٌ قائمةٌ بذاتها**، لا `canOperateNoExam`
(٠٦٧، تتبع `canAddPatients`) ولا `canCompleteReceptionSale` (٠٦٦) ولا
`canCompleteMaintenance` (٠٦٩) — أربعتُها بجسدٍ واحد **عمداً**: كلُّ بابٍ
يتطوّر بلا أن يخشى تعديلُه كسرَ الأبواب الثلاثة الأخرى. **والطبيبُ خارجَ
الشرط كلّياً** ولو نادى النقطةَ مباشرةً؛ وسلطةُ الطبيب المسؤول تمرّ من
`isAdmin` وحدها كبقيّة الأبواب — لا دورَ خاصّاً باسمه في أيّ مكان.

### الباب: `POST /api/no-exam/device-sale` — نفسُه، بعقدٍ جديد
لا بابَ جديداً — **العقدُ تبدّل بالكامل**:
`{patientId, expertUserId, originalPrice, discountAmount, note?}` مع إمّا
`component` (حلقةٌ جديدة، من `PROSTHETIC_COMPONENTS` القانونية) أو
`existingEpisodeId` (استئنافُ حلقةٍ يتيمة) — **لا ثالث ولا كلاهما معاً**.
**والعقدُ القديم مرفوضٌ صراحةً**: `charged` أو `amount` في الجسم ⟶ ٤٠٠
برسالةٍ تدلّ على العقد الجديد، لا تجاهلٌ صامت. **ولا `deviceOrigin`
إطلاقاً** — الحلقةُ دائماً بناءٌ جديد يفتحه هذا الباب بنفسه، فلا التباسَ
«مسجَّل/غير مسجَّل» يُسأل عنه أصلاً (خلافاً للصيانة التي تخدم جهازاً قائماً
قد يسبق تسجيلَه في النظام).

### استئنافُ حلقةٍ يتيمة — بهويّتها لا بثقة الطلب
`existingEpisodeId` هو **مخرجُ الشكل القديم ذي النداءين حين ينجح الأوّل
ويفشل الثاني** — النافذةُ الجديدة لا تُرسله إطلاقاً عند فتح طلبٍ جديد.
الحلقةُ تُقرأ بمعرّفها **تحت القفل القانونيّ** (`lockCaseAndReadExactEpisode`
من ٠٦٧) لا بثقة العميل: تخصّ هذا المريضَ ولهذه الخدمة · حالتُها
`awaiting_exam` بعد · مسارُها `no_exam` · **والجزءُ المطلوب يُشتقّ من الحلقة
المقفولة نفسِها لا من الطلب** — فعميلٌ يرسل جزءاً مغايراً مع
`existingEpisodeId` لا يبدّل ما طُلب فعلاً، الحلقةُ تقول الحقيقة. وجهازٌ
كاملٌ ادّعته حلقةٌ موروثة **يُردّ لا يُصحَّح بصمت**: بابُه المعاينةُ أو
التصحيحُ الإداريّ.

### التنفيذ — تركيبٌ واحد، لا محاسبةٌ ثانية
`server/pending_charges/store.ts: createComponentSaleOperation` معاملةٌ
واحدة بالترتيب التالي بالضبط:
١) حلقةٌ جديدة عبر `startDeviceEpisodeTx` **القانونية نفسِها** (لا SQL
   حلقاتٍ منسوخ)، أو استئنافُ اليتيمة أعلاه. ٢) `mfg.validateExpertForBranchTx`
   — إعادةُ تحقّقٍ **تحت القفل**، لا لقطةَ فحصِ النقطة المبكّر (نفسُ حارس
   الصيانة المبسّطة). ٣) `startDeviceSaleOperationallyTx` — الأمرُ والحلقةُ
   والسجلّ، بلا دينار. ٤) `applyDeviceSaleFinancialsTx` — المجموعُ والكلفةُ
   والقيد، بلا عمل. **وهما نصفا `assignManufacturing` نفساهما** اللذان
   يستعملهما مسارُ المعاينة وطابورُ إكمال الصيانة الموروث. ٥)
   `setEpisodeComponentSaleTermsTx` (جديدة) — تكتب الحقيقةَ التجارية
   المُهيكَلة على الحلقة.

`startDeviceEpisode` (القديمة) انقسمت إلى `startDeviceEpisodeTx(tx, params)`
**القانونية** + غلافٌ رقيق `startDeviceEpisode(params)` يفتح معاملته
الخاصّة — نفسُ نمط `Tx` القائم في كامل الملفّ (`assignManufacturing` وأخواتها)،
فيصير فتحُ الحلقة قابلاً للانضمام إلى معاملة `createComponentSaleOperation`
بدل معاملته المستقلّة السابقة. **وهذا ما يُغلق فجوة اليتيمة**: فتحُ الحلقة
وبيعُها في نفس القفل، فإمّا ينجح الاثنان معاً أو يتراجع الاثنان معاً — لا
نجاحٌ جزئي ممكن بعد اليوم لطلبٍ جديد (اليتيمةُ القديمة تبقى ممكنة الحدوث
تاريخياً فقط، ولذلك بقي مخرجُ `existingEpisodeId`).

### الحقيقةُ التجارية مُهيكَلة (ترحيل ٠٧٠)
`patient_device_episodes.component_sale_original_price`/`component_sale_price_kind`
— نفسُ نمط `maintenance_original_price`/`maintenance_price_kind` (٠٦٩)
**بلا عمود خصمٍ ثالث** (يُشتقّ: أصليّ − نهائيّ) **وبلا عمود نهائيٍّ رابع**:
`agreed_cost` القائم أصلاً على الحلقة يبقى مصدرَ الحقيقة الوحيد للمبلغ
الفعليّ المقيَّد — لا عمودَ ثانياً له. أربعةُ قيود `CHECK`: تكامل الثنائي
(معاً أو لا شيء) · أهليّةٌ (`service_path='no_exam'` حين يحضران) · أصليّ>٠
واتّساقُ النوع مع `agreed_cost` (عاديّ⟺نهائي=أصليّ، خصمٌ⟺٠<نهائي<أصليّ،
مجّانيّ⟺نهائي=٠). الصفوفُ التاريخية تبقى `NULL` **بلا تعبئة رجعية** — لم
تُسجَّل لها هذه الوقائع (نفسُ درس ٠٣٨/٠٣٥/٠٦٩)، وطُبِّقت القيودُ الأربعةُ
حيّةً بمصفوفة SAVEPOINT محلياً قبل حزمة الاختبارات.

### مجّانيّةٌ حقيقية، لا غيابَ سعر
`priceKind === "free"` عمليةٌ حقيقية بحلقةٍ وأمرِ عملٍ وكلفةِ صفر — تُكتب
صراحةً `no_exam_no_charge = true` على أمر العمل (العلمُ الموروث نفسُه من
٠٦٧/٠٦٩)، لا تُستدَلّ عليها من غياب مبلغ. ولا فرقَ في مسار الكتابة بين
مجّانيّ ومدفوع — كلاهما يمرّ بنفس الخطوات الخمس أعلاه، والفرقُ رقمٌ لا مسار.

### صفرُ سلطةٍ للطبيب — الآخِرُ يُغلَق
بيعُ الجزء **لا** يستدعي `routeRetrospectiveReview` بعد اليوم — الدالّةُ لم
تعد مستوردةً ولا منادَاةً من نقاط بيع الجزء إطلاقاً (لا وجودَ حيّاً، لا
شيفرةً ميتة). وبهذا **يكتمل** ما بدأته الصيانةُ المبسّطة (٠٦٩، القسم 4.j):
لا صيانةٌ ولا بيعُ جزءٍ يصلان طابورَ الطبيب بعد اليوم — بطاقةُ الإشراف
تعرض غيابَهما معاً، لا أحدَهما. **وصفرُ `pending_service_charges`**: هذا
البابُ لا يكتب في ذلك الجدول إطلاقاً — الصفوفُ الموروثةُ فيه (طابورُ
«مبالغ سابقة بانتظار الإكمال»، القسم 4.e) تبقى مقروءةً وقابلةً للحسم بمسارها
القائم، ولا صفَّ جديداً يُضاف إليها من هذا الباب بعد اليوم.

### الواجهة (`NoExamOperationDialog.tsx`) — فرعُ بيع الجزء وحده
فرعُ `maintenance` **لم يُمَسّ بحرف** (مُثبَتٌ بمطابقةٍ حرفية في الاختبار).
فرعُ `device_sale` أُعيد بناؤه على العقد الجديد: جزءٌ (من القائمة القانونية،
أو مقروءٌ للعرض فقط حين `existingEpisodeId` — لا يُعاد اختياره) ⟵ خبيرٌ ⟵
سعرٌ أصليّ ⟵ خصمٌ ⟵ **نهائيٌّ للقراءة فقط** (محسوبٌ حيّاً بنفس دالّة الخادم)
⟵ ملاحظة. **حُذف**: محدِّدُ «شراء/صيانة» المزدوج القديم، وأيُّ نداءِ فتحِ
حلقةٍ منفصل قبل الحفظ، وأيُّ صياغةِ مراجعةٍ طبية. زرُّ حفظٍ واحد. رسالةُ
النجاح من `COMPONENT_SALE_SUCCESS_MESSAGE` المشتركة — لا نصّاً منسوخاً، ولا
أثرَ لصياغات «بلا أجور»/«بانتظار الاعتماد» القديمة في هذا الملفّ بعد اليوم.

### مَن يرى خيار «بيع جزء» في مُشغِّل الخدمة
`reception_routing.ts` اكتسب بوّابةً ثانية: خيارُ `device_sale` يظهر الآن
لمن يجتاز **كِلا** الشرطين — `noExamSaleServiceTypes` (كان موجوداً، يستبعد
المساندَ لأنها بلا قائمة أجزاء) **و**`canCompleteComponentSale(session)`
(جديد). فالطبيبُ (أو جلسةٌ غائبة) لا يرى خيارَي «بيع جزء» و«صيانة» معاً في
مُوجِّه «ما سبب حضور المريض اليوم؟» — بابانِ مستقلّانِ اتّفقا في حجب الطبيب
**صدفةً** بصلاحيّتين مستقلّتين، لا لأنهما بابٌ واحد. والخادمُ يبقى الحارسَ
الحقيقيَّ بصرف النظر عن الواجهة.

### التزامن
ضغطتان متزامنتان على `existingEpisodeId` نفسِه: قفلُ صفّ الحلقة يجعل
الثانية تنتظر الأولى ثمّ تقرأ حالةً لم تعد `awaiting_exam` فتُردّ ٤٠٩. وعلى
حلقةٍ جديدة: حارسُ الأمر النشط وقفلُ الخيط و`uq_pde_case_open` تمنع حلقتين
أو قيدين معاً — أمرُ تصنيعٍ واحد وقيدُ كلفةٍ واحد بالضبط.

### وما لم يتغيّر
البابُ القديم `POST /api/patients/:patientId/device-episodes` **يتقاعد فقط
للشكل الذي كان ينجح فعلاً هنا**: جزءٌ حقيقيّ على مسار `no_exam` ⟶ ٤٠٩ يدلّ
على الباب الموحَّد. **وكلُّ ما عداه بحرفه**: مسارُ المعاينة (`servicePath
= "exam"`) يفتح الحلقةَ كما كان تماماً (`NewDeviceEpisodeModal`، القسم 4.f)
· رفضُ الجهاز الكامل والمساند الطبي على مسار «بلا معاينة» (`noExamSaleRefusal`،
٤٠٠) يسبق فحصَ التقاعد فيبقى كما هو حرفياً · الصيانةُ المبسّطة (٠٦٩، القسم
4.j) · القسم 4.i كاملاً · إلغاءُ المعاينة (٠٦١) والتصحيحُ الإداريّ (٠٦٤)
وتصحيحُ السعر بعد البيع · طابورُ «مبالغ سابقة بانتظار الإكمال» الموروث
ومَن يُنهيه · العلاجُ الطبيعي (لا `servicePath` يُسأل عنه، ولا حرفَ تغيّر
في تسعيره) · سلّةُ المرضى (٠٦٨) · واتساب وتلغرام · PR #254 (غيرُ مدموج، لم
يُعدَّل).

### الترحيل
**٠٧٠ فقط** — إضافيّ، idempotent: عمودان على `patient_device_episodes`
وأربعةُ قيود `CHECK`. بلا `DROP` ولا `DELETE` ولا مسٍّ لترحيلٍ من ٠٠١ إلى
٠٦٩.

اختبارٌ دائم: `npm run test:component-sale` (يثبت الصلاحياتِ الأربع بالتكافؤ
الكامل، رفضَ الطبيب المطلَق، الحفظَ الواحدَ الذرّيّ، اشتقاقَ السعر بأنواعه
الثلاثة وتجاهلَ القيم المحسوبة من العميل، رفضَ العقد القديم `charged`/
`amount`، الاستئنافَ بهويّة الحلقة المقفولة لا بثقة الطلب، حارسَ الجهاز
الكامل على الحلقة الموروثة، إعادةَ تحقّق الخبير تحت القفل، صفرَ
`pending_service_charges` وصفرَ سجلّات مراجعةٍ طبية، الحقيقةَ التجارية
المُهيكَلة، المجّانيّةَ الصريحة، التزامنَ، تقاعدَ الباب القديم للشكل
الناجح وحده مع بقاء مسار المعاينة والرفض الأصليّ كما هما، عزلَ العلاج
الطبيعي، وعقدَ الشاشة). ومعه مراجعةُ ارتداد كاملة على كلّ ملفّ اختبارٍ
كان يفترض العقدَ القديم أو مسارَ المراجعة الطبية القديم لبيع الجزء
(`pending_charge.test.ts` · `service_path.test.ts` · `exam_commercial.test.ts` ·
`simplified_maintenance.test.ts` · `reception_routing.test.ts`) — كلُّها
محدَّثةٌ لتُثبت السلوكَ الجديد بدل القديم، وتمرّ كاملةً مع كامل حزمة
اختبارات الأجهزة والتصنيع والمراحل السابقة (٠٦٠–٠٦٩) بلا انكسار.

---

## 4.l طابورُ «بانتظار الحسم / تم الحسم» — تبسيطُ الشريط الجانبي (٢٠٢٦-٠٨-٢٨)

**لا مريضَ مُعايَناً يُفقَد بين المعاينة والحسم.** فرعُ
`simplify-decision-queue-sidebar`، ولا علاقةَ له بـPR #254 (غيرُ مدموج، لم
يُمَسّ).

### الواقعة
الطبيبُ يوقّع المعاينة فيُنشئ ذلك متابعةً (٠٥٣)، لكنّ لا شيءَ كان يقود
الاستعلاماتِ إليها مباشرةً: «متابعة ما بعد المعاينة» شاشةُ آلة حالاتٍ
كاملة — مرشِّحاتٌ، بطاقاتُ اعتماد سعرٍ موروثة، ألوانُ حالاتٍ، و«فتح الملف»
فعلاً أساسياً. **فمريضٌ عايَنه الطبيب كان يضيع فعلياً حتى يتذكّره أحد** —
لا الشريطُ الجانبيُّ يقول «هنا مَن ينتظرك»، ولا الشاشةُ تفصل بين مَن ينتظر
حسماً ومَن حُسم بالفعل.

### القاعدة: تبويبان لا حالةٌ جديدة
`shared/decision_queue.ts` — **قراءةٌ فقط فوق `FollowupStatus` القائمة**:

```
بانتظار الحسم = ! isTerminal(status)                              (shared/followup.ts)
تم الحسم       = status ∈ {converted, closed_without_purchase}    فقط
```

**و`closed_exam_cancelled`/`closed_admin_void` مُستبعَدتان من كلا التبويبين
عمداً**: طرفيّتان فعلاً لكنّهما واقعتان تاريخيّة/إدارية لا قرارَ شراءٍ من
المريض — عرضُهما في «تم الحسم» يوهم بقرارٍ لم يقع. **ولا `isResolved` ولا
حالةَ طابورٍ جديدة تُخترَع** — الفرزُ كلُّه مُشتقٌّ حيّاً من الحالة الحالية،
فصفٌّ أُعيد فتحه بعد «لم يشترِ» يعود «بانتظار الحسم» تلقائياً بلا زرّ
«تحديد كمحسوم».

**والحالةُ الوسيطة الموروثة `price_approval_pending` كانت محبوسة فعلياً**:
`COMMERCIAL_EDITABLE` تقبلها فتدخل نقطةَ `/complete-sale`، لكنّ `CONFIRMABLE`
في `confirmPurchase` (`server/followup/store.ts`) كانت تنساها فتُردّ ٤٠٩ —
فصفٌّ حيٌّ فعلاً كان يظهر في الطابور بلا زرٍّ يعمل. **إصلاحٌ ضيّقٌ واحد**:
أُضيفت إلى `CONFIRMABLE` (توافقٌ رجعيّ، مثل `purchase_approval_pending`
قبلها) — بلا توسيعٍ آخر في نقطة التوقيع القانونية نفسِها.

### الأبوابُ الجديدة — قراءةٌ فقط (`server/followup/decision_queue_store.ts`)
| النقطة | الوصف |
|---|---|
| `GET /api/followups/decision-queue?state=waiting\|resolved` | الصفوف، بفلترةِ `branchId`/`serviceType` اختياريّة و`{rows, total}` — `total` من `COUNT(*)` حيّ لا من طول القائمة |
| `GET /api/followups/decision-queue/count` | شارةُ الشريط الجانبيّ — عددُ «بانتظار الحسم» ضمن نطاق الجلسة **كاملاً**، بلا فلترةٍ محليّة |

**بلا حقيقةٍ ماليةٍ جديدة**: الملفّ **لا يكتب شيئاً**. الكتابةُ الوحيدة
تبقى عبر `/complete-sale`/`/not-bought` القائمتين (المرحلة الثانية) — هذا
نموذجُ قراءةٍ فوق أثرهما فقط.

**والبوّابةُ `canCompleteReceptionSale` نفسُها** (`shared/commercial.ts`) —
مَن يقدر أن يحسم هو مَن يرى الطابور: الاستعلامات والمحاسب ومديرُ الفرع
والمسؤولُ العام، **ولا الطبيبُ إطلاقاً** (٤٠٣ لا قائمةً فارغة).

**ومسارُ المعاينة وحده — `JOIN` داخليّ لا `LEFT`**: نفسُ حدّ
`isExamPathFollowup` بالضبط (`de.service_path = 'exam'`)، فمتابعةٌ بلا
حلقةٍ (`device_episode_id IS NULL`، ممكنةٌ فعلاً) **لا** تظهر — لأنّ
`/complete-sale`/`/not-bought` القانونيَّين لا يقدران أصلاً على حسمها
(`isExamPathFollowup` تردّه `false` لهما أيضاً). **لا يُعرَض صفٌّ كإجراءٍ
ممكن إن كانت نقطتُه القانونية ستردّه** — صفٌّ كهذا يبقى مرئياً من ملفّ
المريض مباشرةً كما كان.

**والنطاقُ مفروضٌ خادمياً**: `branchScope(req)` كما في كلّ نقطةٍ سابقة —
فلترةٌ صريحة بفرعٍ خارج النطاق تُردّ ٤٠٣ لا تُوسَّع صامتاً. ومسؤولٌ يفلتر
الصفحةَ بفرعٍ واحد **تبقى شارتُه كلَّ الفروع** — الشارةُ لا تقبل فلتراً
إطلاقاً.

### هويّةُ الحاسم — من الحدث الأخير لا من عمود المالكية
بطاقةُ «تم الحسم» تعرض **مَن حسم ومتى وبأيّ صفة** — و**ليس** من
`purchase_decision_owner`/`purchase_decision_name` (تُكتَب مرّةً واحدة بحكم
القاعدة، ٠٦٦، ولا تتحرّك عند إعادة فتحٍ وحسمٍ ثانٍ فتكذب على القارئ). بل من
**آخر حدثٍ في `post_exam_followup_events` يطابق نوعُه الحالةَ الحالية**
حرفياً (`event_type = f.status` — `'converted'`/`'closed_without_purchase'`
نصّان متطابقان مع الحالة نفسِها، فلا حاجةَ لخريطة تحويل):

```sql
LEFT JOIN LATERAL (
  SELECT actor_name, payload, created_at FROM post_exam_followup_events
   WHERE followup_id = f.id AND event_type = f.status
   ORDER BY id DESC LIMIT 1
) ev ON TRUE
```

فإعادةُ فتحٍ وحسمٍ من فاعلٍ آخر تُظهر **الأحدثَ لا الأقدم**، والحدثُ الأوّل
يبقى في `post_exam_followup_events` كاملاً (لا محوَ ولا كتابةَ فوق) —
مُثبَتٌ حيّاً في `server/decision_queue.test.ts` (القسم «ز»).

### لقطةُ الدور — تُكتب لحظةَ الحسم، لا تُخمَّن لاحقاً
`Actor` في `server/followup/store.ts` اكتسبت حقلاً اختيارياً `role?: string
| null`. عند إلحاق حدث `converted`/`closed_without_purchase` يُكتب
`actorRole` داخل حمولة **ذلك الحدث بعينه** — لا عمودَ جديداً، `payload`
جدولٌ `jsonb` قائمٌ أصلاً. `actorRoleSnapshotOf` (`shared/decision_queue.ts`)
تفحص `isAdmin` أوّلاً وبلا شرط — مسؤولٌ يحمل دوراً آخر (`role: "admin"` في
الجلسة الخام) يُختَم `global_admin` **بسلطته لا بدوره الخام**؛ وإلّا الدورُ
الحرفيّ إن كان أحد الأربعة المعروفة، وإلّا `null`. **ولماذا لقطة لا اشتقاقاً
لاحقاً**: محاسبٌ يصير مديرَ فرع بعد أشهر، فقراءةُ `system_users.role`
الحاليّ لحدثٍ قديم تكذب. حدثٌ سابقٌ للمرحلة الخامسة بلا لقطة ⟶ «—»
(`UNKNOWN_ROLE_LABEL`) — لا تخمين. خريطةُ التسميات: استقبال→الاستعلامات ·
محاسب→المحاسب · مديرُ فرع→مدير الفرع · مسؤول→المسؤول العام.

### المكوّنُ المشترك — `ExamPathDecisionActions.tsx`
كتلةُ «إتمام البيع»/«لم يشترِ» (الأزرارُ، ملاحظةُ الطبيب، نافذتا الحوار،
معاينةُ السعر الحيّة بـ`deriveOfferFromDiscount`، مطابقةُ استعلام الخبراء
بفرع العملية) **استُخلصت من `PostExamDecisionCard.tsx` حرفياً** إلى مكوّنٍ
مستقلّ يستهلكه الطرفان: بطاقةُ المريض **وطابورُ «بانتظار الحسم» الجديد**.
**بلا نسخةٍ ثانية من المنطق** — الأبوابُ المنادَاة هي `/complete-sale`
و`/not-bought` القانونيّتان بلا تغيير، والمكوّنُ لا يكتب شيئاً بنفسه.

بعد النجاح: إبطالٌ لمفتاح `["/api/followups/decision-queue"]` (يطابق
جزئياً قوائمَ الحالتين **وشارةَ الشريط الجانبيّ** معاً — الأخيرةُ مفتاحُها
`[..., "count"]`) بجانب مفاتيح البطاقة القديمة (`/api/followups/patient/…`،
`/api/followups`، `/api/discounts`، …) — فالصفحتان تتحدّثان من نداءٍ واحد.

### الصفحةُ المبسَّطة — `PostExamFollowups.tsx`
فقدت آلةَ الحالات القديمة (مرشِّحاتٌ بعدد الحالات، بطاقاتُ اعتماد سعرٍ
موروثة، جدولُ ألوانٍ لكلّ حالة، «فتح الملف» فعلاً أساسياً) لصالح تبويبين:
**«بانتظار الحسم»** (افتراضيّ، الأقدمُ توقيعاً أوّلاً) و**«تم الحسم»**
(الأحدثُ حسماً أوّلاً). كلُّ صفٍّ بانتظارٍ يحمل رمزَ المريض واسمَه وفرعَه
وتصنيفَه (طرف صناعي/مسند طبي، فلترةٌ ثلاثية) وتاريخَ المعاينة وطبيبَها
وملاحظتَه (تُخفى إن فرغت) **وأزرارَ الحسم مباشرةً من الصفّ نفسه** —
لا بحث ولا فتح ملفّ للحسم العاديّ. وصفُّ «تم الحسم» يحمل النتيجةَ ومَن حسم
وصفتَه ووقتَه، ولتفاصيل الشراء: الأصليّ والخصمَ والنهائيَّ والخبير؛ ولعدم
الشراء: السببَ كما قاله المريض.

> **⚠ تصحيحُ توثيقٍ (مراجعةٌ لاحقة)**: كان هذا القسم يقول إن بطاقة «طلبات
> سعرٍ قديمة معلَّقة» (بقايا المسار الأقدم من ٠٥٣) بقيت **فوق التبويبين
> بحرفها** — وهذا لم يكن صحيحاً؛ الشاشةُ المبسَّطة **حذفتها فعلياً**، ولم
> تُعِدها هذه المراجعة. الصفحةُ الطبيعية تبقى بسيطةً: تبويبان لا أكثر.
> **والتاريخُ محفوظ لا محذوف**: `GET /api/followups/approvals` والصفوفُ
> نفسُها (`price_change_requests`) باقيةٌ كما هي، مقروءةً وقابلةً للحسم من
> بطاقة المريض عبر مسارها القديم — لا واجهةَ لها في طابور «بانتظار الحسم»
> الجديد بعد اليوم.

**بلا مصطلحاتٍ تقنية** («followup»، «converted»، «purchase_approval_pending»)
تصل الشاشة — والصفوفُ الموروثة (خارج مسار المعاينة) تبقى مقروءةً وقابلةً
للحسم من ملفّ المريض مباشرةً كما كانت، لا من هذه الصفحة.

### الشريط الجانبيّ — ثلاثةُ تعديلات، واثنان أُبقيا بعد تدقيق
- **«متابعة ما بعد المعاينة» ⟶ «بانتظار الحسم»** (نفسُ الرابط
  `/post-exam-followups`): القائمةُ ضاقت من `[reception, branch_manager,
  doctor, accountant]` إلى `[reception, branch_manager, accountant]` —
  **لا الطبيب** — مع شارةِ عدد «بانتظار الحسم» (لا تختفي الصفّة عند الصفر،
  فالتبويبُ الثاني يبقى مفيداً حتى بلا انتظار).
- **الطابورُ الموروث** (`/no-exam-review`): شارةُ عددٍ جديدة
  (`GET /api/no-exam/review/count`، نفسُ نمط `returnedCounts`) + **يختفي
  الصفُّ كلُّه عند الصفر** — لا الشارةُ وحدها.
- **«مُعادة للتصحيح»** (`/returned-charges`): الشارةُ قائمةٌ من قبل؛
  أُضيف **إخفاءُ الصفّ عند الصفر** (كان يبقى ظاهراً بشارةٍ غائبة فقط).
- **اعتمادُ الخصومات** و**مراجعةُ الطبيب** — **أُبقيا بلا تغيير** بعد تدقيق
  حيّ للكود يثبت منتجين فعليَّين لكلٍّ: اعتمادُ الخصومات له أربعةُ مواضع
  نداءٍ حيّة (خصمُ العلاج الطبيعي في «خدمة جديدة»، تسعيرُ العلاج الطبيعي
  الأوّل، اعتمادُ بيع جهازٍ (`assignManufacturing`)، وتأكيدُ شراءٍ موروث)؛
  ومراجعةُ الطبيب لها أربعةٌ أيضاً (فتحُ حلقة جهازٍ جديدة على مسار المعاينة،
  إضافةُ نوع حالة، تسجيلُ زيارةٍ عامّة لمريض جهاز، وزرُّ «إرسال لمراجعة
  الطبيب» اليدويُّ الصريح). **كلاهما طابوران حيّان فعلياً، لا بقايا ميتة.**
- **وكلُّ ما عداها لم يُمَسّ**: لوحة التحكّم، سجلّ المرضى، التقارير،
  المحاسبة، الفروع، الإحصاءات، الاستطلاعات، تتبّع الجلسات، التصنيع،
  التنبيهات، المحذوفات، الإعدادات، الصلاحيات.

### وما لم يتغيّر
`/complete-sale`/`/not-bought` (المرحلة الثانية) بحرفهما · `setCommercialFields`
ومالكيةُ الحقول (٠٦٦) · إلغاءُ المعاينة (٠٦١) والتصحيحُ الإداريّ (٠٦٤) ·
تصحيحُ السعر بعد البيع · مسارُ الصيانة وبيعُ الجزء (٤.ي، ٤.ك) · العلاجُ
الطبيعي (لا `servicePath` يُسأل عنه) · سلّةُ المرضى (٠٦٨) · واتساب وتلغرام ·
Daily Review (المرحلةُ التالية، لم تبدأ بعد) · PR #254 (غيرُ مدموج، لم
يُمَسّ).

### الترحيل
**بلا ترحيل** — `payload` على `post_exam_followup_events` جدولٌ `jsonb`
قائمٌ أصلاً يقبل `actorRole` بلا تعديل مخطّط، ولا عمودَ ولا جدولَ جديداً
لأيّ جزءٍ آخر من هذه المرحلة.

اختبارٌ دائم: `npm run test:decision-queue` (٣٦ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية — الصلاحياتُ الأربع ورفضُ الطبيب، عضويّةُ «بانتظار
الحسم» (مسارُ المعاينة وحده، استبعادُ الصفّ الموروث، إصلاحُ الحالة الوسيطة
`price_approval_pending`)، ملاحظةُ الطبيب بهويّتها الدقيقة وغيابِها الصريح،
نطاقُ الفرع الأربعة أشكال (مديرٌ يرى فرعه، فلترةٌ خارج النطاق تُرَدّ، مسؤولٌ
يرى الفروع كلَّها، شارتُه لا تتأثّر بفلترة الصفحة)، عضويّةُ «تم الحسم»
وفرزُها، هويّةُ الحاسم ولقطةُ دوره بأربعة فاعلين ومنهم `global_admin`،
إعادةُ الفتح والحسم من فاعلٍ آخر مع تاريخٍ كامل محفوظ، استبعادُ الإلغاء
والإبطال الإداريّ من كلا التبويبين، التزامنُ، والتصنيفُ ودقّةُ العدّ بلا
`LIMIT` صامت) — ومعه مراجعةُ ارتداد على كلّ ملفّ اختبارٍ لمسته إعادةُ
الاستخلاص (`reception_sale.test.ts`، `exam_commercial.test.ts`،
`shared/followup_events.test.ts`) لتُثبت عقدَ الشاشة على المكوّن المشترك
بدل البطاقة وحدها، مع إثباتِ إعادة الاستعمال صراحةً (البطاقةُ والطابورُ
يستوردان `ExamPathDecisionActions` نفسَه)، وتمرّ كاملةً مع كامل حزمة
اختبارات المتابعة والمعاينة والاستعلامات السابقة (`test:followup` ٢٠٠٤
سطراً، `test:exam-edit-commercial`، `test:exam-price-correction`،
`test:exam-cancellation`، `test:reception-agreement`،
`test:administrative-reversal`، `test:component-sale`,
`test:maintenance-simplify`) بلا انكسار.

### تصحيحٌ لاحق — أربع ثغراتٍ دقيقة (مراجعةٌ حيّة، ٢٠٢٦-٠٨-٢٨)

المعماريّةُ ثابتة؛ أربعُ ثغراتٍ ضيّقة كشفتها مراجعةٌ حيّة، وعولجت في الفرع
نفسِه بلا فتح فرعٍ ثانٍ.

**١) `/complete-sale` المباشر كان يترك طلبَ سعرٍ موروثاً `pending`.**
`CONFIRMABLE` قبلت `price_approval_pending` فصار الإتمامُ المباشر ممكناً
لصفٍّ قد يحمل صفَّ `price_change_requests` حقيقياً لم يُحسَم — فيتحوّل
الملفُّ و**يبقى طلبُ سعره ينتظر اعتماداً** في `listPendingApprovals`: تاريخٌ
مستحيل. الحلّ: دالّةٌ مشتركة `cancelPendingPriceRequestsTx` (استُخلصت من
`closeWithoutPurchase` بحرفها، لا نسخةً ثانية) تكتب `status='cancelled'`
**لا `rejected`** (لم يرفض أحدٌ سعراً — الطلبُ صار بلا موضوع) مع حدث
`price_request_cancelled`. و`completeReceptionSale` صارت تفتح معاملتها
الخاصّة (كانت تُسلِّم لـ`setCommercialFields` بلا تغليفٍ صريح) فتستدعي
الإلغاءَ **بعد** أن يثبت `result.converted === true` **وفي المعاملة
نفسِها** — فشلُ البيع يُسقط الإلغاءَ معه، ولا نصفَ أثر. `closeWithoutPurchase`
(ومنها «لم يشترِ») تستعمل الدالّةَ المشتركةَ ذاتها بحرفها كما كانت.

**٢) `complete_sale` كان يظهر لصفوفٍ سيردّها الخادمُ ٤٠٣.**
`setCommercialFields` تكتب السعرَ والخبيرَ والقرارَ معاً، لكنّ `examPathActions`
كانت تفحص مالكيةَ القرار وحده. فصفٌّ موروثٌ سعرُه أو خبيرُه مملوكٌ للطبيب
(قرارُه فارغٌ يُكمله مَن حضر) كان يعرض زرَّ «إتمام البيع» ويردّه الخادمُ.
الإصلاح: `examPathActions` تقبل الآن `priceField`/`expertField` أيضاً؛
`not_bought` يحتاج القرارَ وحده، و`complete_sale` يحتاج الحقولَ الثلاثة
معاً. فقد تُعرَض `["not_bought"]` وحدها لصفٍّ موروث — **ولا يُخفى الصفُّ**
عن «بانتظار الحسم» لهذا السبب: يبقى ظاهراً، وملاحظةُ الطبيب تبقى مقروءة،
ورسالةٌ إنسانية (`examPathBlockedMessage`، بلا رمزِ مالكيةٍ ولا اسمِ حالة)
تشرح أن الحسمَ الكامل يحتاج المسؤولَ العام. كلا نقطتَي القراءة
(`/api/followups/patient/:patientId` و`/api/followups/decision-queue`)
تُمرِّران الحقولَ الثلاثة من مصدرٍ واحد — لا حسابَ ثانياً في أيّ منهما.
`ExamPathDecisionActions` لم تعد تُخفي نفسَها عند `actions.length === 0`
(ذاك كان يُخفي ملاحظةَ الطبيب أيضاً على صفٍّ **حيّ** محجوب) — التمييزُ بين
«منتهٍ» و«حيٌّ محجوب» صار عند المنادي عبر `isTerminal(status)` لا طول
المصفوفة.

**٣) تعارضٌ (٤٠٩) كان لا يُحدِّث الطابور.**
موظّفٌ يحسم صفّاً حسمه زميلٌ للتوّ يرى رسالة الخطأ الصحيحة، لكنّ الصفَّ
الباطل يبقى ظاهراً حتى تحديثٍ لاحق. صار `onError` في `ExamPathDecisionActions`
يُبطل نفسَ مفاتيح `onSuccess` (`decision-queue`، `followups/patient/:id`،
وبقيّة المجموعة) — إبطالٌ على **كلّ** خطأ (لا تحليلَ رمز حالةٍ هشّاً عبر
`apiRequest`)، فلا حالةٌ محليّةٌ تبقى تُصدَّق بعد فشل.

**٤) الشريط الجانبيّ كان يعيد كتابة `canCompleteReceptionSale` يدوياً.**
شرطٌ مطابقٌ حرفياً (`isAdmin || role∈{...}`) لكنّه نسخةٌ ثانية تنحرف صامتة
لو تغيّرت القاعدةُ القانونية. صار `Sidebar.tsx` يستورد
`canCompleteReceptionSale` من `shared/commercial.ts` مباشرة، وعنصرُ
«بانتظار الحسم» يحمل محمولاً عاماً جديداً `eligible: boolean` بدل `roles`
— أوّلُ استعمالٍ لهذا النمط في الملفّ، وبقيّةُ العناصر لم تُمَسّ.

اختبارٌ دائم مُحدَّث ضمن `npm run test:decision-queue` (٦١ تأكيداً بعد
الإضافة، مقابل ٣٦ قبلها): إلغاءُ طلبِ سعرٍ حقيقيّ حيّاً (إنشاءً حقيقياً لا لقطة حالةٍ فقط)
مع إثبات التراجع الكامل عند فشل البيع، مصفوفةُ الحجب بأشكالها الثلاثة
(سعرٌ محجوب/خبيرٌ محجوب/قرارٌ محجوب) للاستقبال وللمسؤول العام معاً، بقاءُ
الصفّ في «بانتظار الحسم» وظهورُ ملاحظة الطبيب على صفٍّ محجوب، وفحصٌ معماريّ
على مصدر `Sidebar.tsx` يثبت استيراد الدالّة القانونية وغيابَ قائمة أدوارٍ
ثانية.

---

## 4.m المراجعةُ اليومية — سردٌ إشرافيٌّ مُشتَقّ، لا مصدرَ حقيقةٍ جديد (٢٠٢٦-٠٨-٣٠)

**«المراجعة اليومية» (`/daily-review`) للمسؤول العام حصراً: ما جرى فعلياً في
الأطراف والمساند اليوم، عبر كل الفروع، الأحدثُ أوّلاً. قراءةٌ فقط — لا زرَّ
فعلٍ فيها إطلاقاً.** فرعُ `daily-review`.

### وليست `patient_events`
فُحص `patient_events` تحديداً قبل البناء ورُفض مصدراً: من أربعٍ وعشرين نوع
حدثٍ معلَن (`shared/patient_events.ts`) **خمسةٌ فقط موصولة فعلياً** — مراحلُ
تصنيع البناء الأوّلي وحدها (`recordStageEvent`/`recordOrderCreatedEvent` في
`server/manufacturing/events.ts`، مشروطةٌ بـ`purpose='initial_build'`) —
بحمولةٍ لا تحمل مالاً ولا فاعلاً ولا طبيباً ولا خبيراً. لا تسجيلَ مريض، لا
توقيعَ معاينة، لا حسمَ ما بعد معاينة، ولا صيانةً على الإطلاق (مستبعَدةٌ
بالـ`purpose` صراحةً). المراجعةُ اليومية تقرأ **الجداولَ التجارية القائمة
نفسَها** مباشرةً — سبعُ أسرٍ، كلٌّ من مصدره الوحيد:

| الأسرة | المصدر | زمنُها الموثوق |
|---|---|---|
| تسجيل مريض | `patients` + `audit_log` (`entity_type='patient', action='create'`) | `audit_log.created_at` — لا `patients.created_at` التي قد تكون تاريخاً تجارياً مُدخَلاً بأثرٍ رجعي |
| معاينة طبية | `medical_exams` (+ `medical_exam_cancellations` للوسم لا الإخفاء) | `signed_at` |
| حسمُ ما بعد المعاينة | `listDecisionQueueResolved` القانونية (`server/followup/decision_queue_store.ts`) — **بلا نسخةٍ ثانية من منطقها** | حدثُ الحسم الفعليّ، لا تحديثٍ لاحق |
| فتحُ صيانة | `prosthetic_work_orders` (`purpose='maintenance'`) | سطرُ `prosthetic_work_history` بـ`action_type='created'` |
| بيعُ جزءٍ بلا معاينة | `patient_device_episodes` (`service_path='no_exam'`) | نفسُ سطر «الفتح» |
| حركةُ تصنيعٍ لاحقة | `prosthetic_work_history` — **`action_type <> 'created'` وحدها**، فلا يُزدوَج مع الأسرتين قبلها | `created_at` لكلّ حركة |
| دفعةُ جهاز | `payments` مربوطةٌ بحالةٍ (`case_id`) نوعُها أطرافٌ أو مساند **صراحةً** — دفعةٌ بلا حالةٍ محدَّدة أو بحالة علاج طبيعي **تُستبعَد كلياً، لا تُخمَّن** | `audit_log` (`entity_type='payment'`) حين يوجد مباشرةً؛ وإلّا `payments.date` |

### حقيقةٌ مالية، لا حسابٌ محاسبيّ
الصفحةُ **لا تحسب شيئاً**: `actual paid = payments.amount` فقط، `discount
= original − final` فقط حين يُعرَف الاثنان معاً، و`legacyUnrecorded: true`
(لا صفر) لصفٍّ سابقٍ على ٠٦٩/٠٧٠ بلا حقولٍ تجارية مُهيكَلة. المجّانيُّ
يُقرأ من `priceKind='free'` صراحةً لا من رقمٍ صفريّ. ولا يُخمَّن قسمٌ (أطراف/
مساند) لدفعةٍ غامضة، ولا فاعلٌ لدفعةٍ بلا تدقيقٍ مباشر عليها.

### «سببُ الحضور» — أولويةٌ صريحة
مصدرُ كلّ أسرةٍ الخاصّ أوّلاً (`chiefComplaint` للمعاينة، `requestedItem`
لبيع الجزء، `maintenanceComponent` للصيانة) — **بلا** تخمينٍ من أعلام
تصنيف المريض (`isAmputee`/…) حين يغيب، خلافاً لمسارٍ قديمٍ آخر يفعل ذلك
(`server/routes.ts` ~٤١٠٠). الغيابُ يُقال «غير محدد» بصدق.

### العلاجُ الطبيعي خارج النطاق كلياً
كلُّ استعلامٍ يستثنيه بنيوياً — لا `servicePath` له، ولا حضور له في أيّ
أسرة.

### الملفّات
`shared/daily_review.ts` (العقد + اشتقاقُ الحقيقة المالية + تسميات الحركات
العربية) · `server/daily_review/store.ts` (سبعُ دوالّ قراءةٍ مستقلّة،
`Promise.all` ثم دمجٌ وفرزٌ في الذاكرة — بلا `UNION` لاختلاف الأشكال جداً) ·
`server/daily_review/routes.ts` (`GET /api/daily-review` وحدها، حارسُ
`isAdmin`) · `client/src/pages/DailyReview.tsx`.

اختبارٌ دائم: `npm run test:daily-review` (٧٣ تأكيداً حيّاً على Postgres
وعلى النقطة الحقيقية — الصلاحية، الزمنُ الموثوق مقابل التاريخ التجاريّ
البائت، فاعلٌ معروفٌ ومجهول («سجلٌّ قديم» بلا تخمين)، صفرُ تسرّبٍ للعلاج
الطبيعي، الفرعُ والخدمة، الترتيبُ تنازلياً، الأسرُ السبع كاملةً بأموالها
الثلاثة وحالاتها الحدّية، غيابُ الازدواج بين فتح الصيانة/بيع الجزء وحركة
«created»، ودفعةٌ غامضةٌ لا تُخمَّن قسماً).

---

## 4.n المساعد الذكي — نسخةٌ ثانية موثوقة: معرفةٌ يديرها المسؤول، وتقاريرُ
مُحسَّبة في الخادم (٢٠٢٦-٠٩-٠٩)

**الطبقةُ الأولى** (`server/ai/provider.ts`/`access.ts`/`chat.ts`/`tools/registry.ts`،
بُنيت ٢٠٢٦-٠٨) بقيت **بحرفها**: وضعان بنيويّان (عامّ/ماليّ)، حلقةُ أدواتٍ
بسقف ثلاث جولات، أربعُ أدواتٍ قراءةٍ فقط. هذه المرحلةُ **تمتدّ** لا
تُعيد بناء: طبقةُ معرفةٍ موثوقة، ثلاثُ أدواتٍ جديدة، وواجهةُ اقتراح/اعتماد.
**بلا أداة كتابةٍ واحدة جديدة** — لا تزال كلُّ أدوات المساعد قراءةً فقط.

### المعرفةُ الموثوقة — لا تعليمات مكتوبة في الشيفرة تشيخ بصمت
كان `GENERAL_SYSTEM_PROMPT` يحمل شرح المسارات (تسجيل، معاينة، تصنيع…)
حرفياً في الكود — نافعٌ لحظة كتابته، شائخٌ مع أوّل تطويرٍ لاحق. صار شرحُ
المسارات يُقرأ من جدولٍ يديره المسؤول العام بلا نشر كود:

- **`ai_knowledge_articles`** (ترحيل ٠٧٥) — المعرفةُ الفعّالة المعتمَدة.
  **لا حذفَ ولا كتابةَ فوق صفٍّ قائم أبداً**: تعديلٌ ينشئ نسخةً جديدة
  (`supersedesId` يشير للقديمة) ويُطفئ القديمة (`isActive=false`) — نفسُ
  مبدأ ٠٦١/٠٦٨ («لا شيء يُمحى، الحالة تتغيّر»). ولا يمكن تفعيل نسخةٍ قديمة
  ما دامت نسخةٌ أحدث منها فعّالة — يُمنع صراحةً، فلا نسختان فعّالتان من
  نفس السلسلة معاً. سبعُ فئات: `general`/`reception`/`medical`/
  `manufacturing`/`physiotherapy`/`finance`/`administration`، وفرعٌ
  اختياريّ (`branchId` فارغ = كلّ الفروع). زُرعت اثنتا عشرة مقالةً
  أوّلية بالترحيل نفسِه (`ON CONFLICT (seed_key) DO NOTHING` — idempotent)،
  كلُّها مُتحقَّقةٌ من الكود الفعليّ لحظةَ كتابة الترحيل لا منقولةً حرفياً
  عن توثيقٍ قد يكون شاخ.
- **`ai_knowledge_suggestions`** — اقتراحُ تصحيحٍ من موظّف. **لا يمسّ
  المعرفةَ الفعّالة بذاته أبداً** — يبقى `pending` حتى يقرّر المسؤولُ العام
  صراحةً (لا `branch_manager`، ولو كان مسؤولاً إدارياً في نطاقاتٍ أخرى —
  قرارُ معرفة المساعد للمسؤول العام حصراً)، ولا يُحذَف الصفّ في أيّ اتّجاه
  (موافقةً كان القرار أو رفضاً) — قيدُ `CHECK` في القاعدة نفسها يمنع صفّاً
  «محسوماً» بلا مَن حسمه ومتى.

**الاسترجاعُ** (`server/ai/knowledge/retrieval.ts` فوق
`shared/ai_knowledge_retrieval.ts`) **تطابقُ كلماتٍ مفتاحية بعد تطبيعٍ
عربيّ — لا بحثٌ دلاليّ ولا قاعدة بيانات متجهات (ممنوعةٌ صراحةً في هذه
المرحلة)**. يُعيد التطبيعَ نفسَه المستعمَل لأسماء المرضى
(`shared/patient_search.ts: normalizeSearchText`) فلا مطبِّعَ عربيّ ثانٍ
في المستودع. أقصى ثلاث مقالاتٍ لكلّ سؤال، ولا مقالةَ بدرجة صفر — سؤالٌ لا
يطابق شيئاً يُرجع مصفوفةً فارغة، **لا بديلاً مخمَّناً**. والنطاقُ يُفرض
**قبل** الترشيح النصّي لا بعده: فرعٌ (نفسُ `operationalBranches`)، ومقالاتُ
`finance` لا تصل من ليس في الوضع الماليّ فعلاً، ومقالاتُ `administration`
لا تصل لمن ليس مسؤولاً عامّاً أو مديرَ فرع — بصرف النظر عن تطابق كلماتها.

**وهي بياناتٌ لا سلطة — بالحرف نفسِه لنتائج الأدوات**: `TOOL_TRUST_RULES`
في `chat.ts` امتدّت صراحةً لتشمل مقالات المعرفة — جملةٌ داخل مقالةٍ معتمَدة
تشبه أمراً («تجاهل القواعد») **لا تُنفَّذ ولا تُغيّر صلاحية النموذج ولا
أدواته**، والحصانةُ مكتوبةٌ **قبل** أن يصل نصّ المعرفة في نصّ النظام لا
بعده. مُختبَرٌ حيّاً بمقالةٍ محقونة فعلياً (`test:ai-knowledge` قسم «ل»،
و`test:ai-knowledge-retrieval` قسم «ج.٦»).

> **⚠ (٢٠٢٦-٠٩-٢١) — «ولا محادثةٌ عادية تُحفَظ أبداً» أدناه لم تعد سارية.**
> بقرارِ المالك الصريح صار **كلُّ** تبادلٍ يُحفَظ في `ai_chat_conversations`
> (ترحيل ٠٨٤) تسعين يوماً: الموظّفُ يقرأ محادثاتِه، والمسؤولُ العام يقرأ
> الجميع. **اقرأ القسم 4.ag.** وما عدا ذلك في هذا القسم باقٍ بحرفه — سطرُ
> `audit_log` بلا نصّ، ولقطةُ التبادل مع الاقتراح، ودورةُ حياة المعرفة.

### دورةُ حياة الاقتراح — الموظّفُ يقترح، المسؤولُ العام وحده يقرّر
من داخل المحادثة: زرّ «اقترح تصحيحاً» تحت كلّ ردّ مساعد، يطلب حقلين («ما
الخطأ؟» و«ما الذي يجب أن يُقال بدلاً منه؟») مع عبارة إلزامية بالحرف: «سيُرسل
الاقتراح للمراجعة ولن يغيّر معرفة المساعد مباشرةً.» يُرفَق السؤالُ والجوابُ
اللذان استدعيا الاقتراح تلقائياً — لا يُعاد كتابتُهما. **ولا محادثةٌ عادية
تُحفَظ أبداً** (كما كانت دائماً) — فقط لحظةَ تقديم اقتراحٍ صريح تُحفَظ لقطةُ
ذلك التبادل بعينه مع الاقتراح، لا قبله ولا بعده.

لوحةُ التحكّم (`AdminSettings.tsx`، تبويب «معرفة المساعد» — نفسُ نمط تبويب
«ذاكرة الذكاء» المجاور له بالحرف) للمسؤول العام حصراً: قائمةُ الاقتراحات
المعلَّقة بزرَّي اعتماد/رفض، وسجلُّ القرارات، وإدارةُ المقالات (إنشاء،
تحرير-كنسخةٍ-جديدة، تفعيل/تعطيل — **لا حذفَ فعليّاً من الواجهة إطلاقاً**).
الاعتمادُ يقرّر مسؤولٌ صراحةً: مقالةٌ جديدة كلّياً، أو نسخةٌ جديدة لمقالةٍ
قائمة — وكلاهما وقرارُ الاقتراح نفسِه يقعان في **معاملةٍ واحدة**
(`server/ai/knowledge/store.ts: approveSuggestion`).

نقاطُ REST (`server/ai/knowledge/routes.ts`، مسجَّلةٌ في `routes.ts` عبر
`registerAiKnowledgeRoutes`):

| النقطة | مَن يفتحها |
|---|---|
| `POST /api/ai/knowledge/suggestions` | أيّ موظّفٍ مصادَق |
| `GET /api/ai/knowledge/suggestions` | المسؤول العام حصراً |
| `PATCH .../suggestions/:id/approve` | المسؤول العام حصراً |
| `PATCH .../suggestions/:id/reject` | المسؤول العام حصراً |
| `GET/POST /api/ai/knowledge/articles` | المسؤول العام حصراً |
| `PATCH .../articles/:id` (نسخةٌ جديدة) | المسؤول العام حصراً |
| `PATCH .../articles/:id/active` | المسؤول العام حصراً |

كلُّ فعلٍ إداريّ (تقديم، اعتماد، رفض، إنشاء، تحرير، تفعيل، تعطيل) يكتب في
`audit_log` (`entity_type = 'ai_knowledge_article' | 'ai_knowledge_suggestion'`)
— بلا نصّ السؤال/الجواب/الاقتراح في سطر التدقيق نفسِه (ذاك محفوظٌ كاملاً
في جدول الاقتراح نفسِه الذي لا يُحذَف أبداً؛ سطرُ التدقيق للمساءلة لا
للأرشفة، نفسُ مبدأ سطر `ai_chat` القائم أصلاً).

### ثلاثُ أدواتٍ قراءةٍ جديدة — نفسُ عقد الأربع القديمة بالحرف
`server/ai/tools/registry.ts` صار سبع أدوات (`TOOL_NAMES`)، والثلاثُ
الجديدة في نفس السجلّ المقفَل (`Object.create(null)`، بلا أداة SQL ولا
جدولٍ عامّ ولا تنفيذ أمر):

- **`patient_search`** — بحثٌ بالاسم/الهاتف يُرجع أقصى خمسة مرشَّحين
  (رمزٌ علنيّ + اسمٌ + فرعٌ فقط، بلا رقمٍ داخليّ). **خلافاً لبقيّة الأدوات
  لا تُعرَض للجميع** — تُشترَط `canViewPatients` بالحرف (نفسُ بوّابة `GET
  /api/patients/registry`)، فخبيرٌ صِرف (بلا هذه الصلاحية افتراضاً) لا
  يصير له دليلُ مرضى بديل عبر المساعد. تُعيد استعمال `buildPatientSearch`
  (`server/patient_search/sql.ts`) — نفسَ محرّك البحث الحقيقيّ حرفياً، لا
  خوارزميةَ بحثٍ ثانية — والفرعُ يُفرض **داخل شرط SQL قبل الترتيب والحدّ**
  لا بترشيحٍ لاحق.
- **`operational_summary`** — ملخّصٌ تشغيليّ لفترة (مرضى جدد، زيارات،
  جلسات علاج طبيعي، طابور المعاينة الآن، أوامر التصنيع النشطة الآن) ضمن
  نطاق فروع المستخدم. **مقفلةٌ خلف صلاحية `canViewReports` الحقيقية**
  (نفسُ عَلَمٍ يحرس كلّ نقطة تقرير في `server/routes.ts`) — لا للجميع
  ولا بافتراض دور: `offeredTo` تشترطها، والمنفِّذ يفحصها ثانيةً دفاعاً
  في العمق (نفسُ نمط `patient_finance`). بلا اسمَ مريضٍ ولا مبلغ، أرقامٌ
  مجمَّعة فقط.
- **`financial_summary`** — ملخّصٌ ماليّ لفترة، بأربعة حقول لا تتبادل:
  `salesValue` (قيمةُ المبيعات — قيودُ الكلفة المسجَّلة في الفترة، ولو لم
  تُقبض بعد) و`revenue` (الإيرادُ الفعليّ — النقدُ المقبوضُ فعلاً) و
  `expenses` و`net`. **المبيعات ليست إيراداً حتى تُقبض**: `revenue` هنا
  مأخوذٌ من `s.totalPaid` لا `s.totalRevenue` — الاسمُ الموروث المضلِّل
  في `storage.getAccountingSummary` («totalRevenue» = مبيعات فعلياً،
  راجع تعليقه هناك) لا يُعاد تسميته هناك (خارج نطاق هذه الإضافة)، لكنّ
  محوِّل المساعد في `reports.ts` يُخرِج بأسماءَ صادقة. مع مقارنةٍ اختيارية
  بالفترة السابقة بنفس الطول. مقفلةٌ خلف الوضع الماليّ (نفسُ حارس
  `patient_finance` بالحرف: `offeredTo` وفحصٌ ثانٍ داخل المنفِّذ). **تنادي
  `storage.getAccountingSummary` مباشرة — بلا حسابٍ مالي ثانٍ يخالف الصفحة
  الحقيقية**، مُثبَتٌ بمطابقةٍ حرفية (وبمبلغين مختلفين فعلياً لا صفرَين
  متساويين) في الاختبار الحيّ.

  **والتفصيلُ بالأقسام يصل النموذجَ (٢٠٢٦-٠٩-٢١)**: `byDepartment` كان
  يُحسَب في `getAccountingSummary` **ويرميه المحوِّل**، فسؤالٌ مشروع («كم
  إيراد العلاج الطبيعي في كربلاء في آب؟») لا جواب له إلّا أن يخترعه النموذج
  أو يعتذر عن رقمٍ موجودٍ في القاعدة. فصار يمرّ مع `current` ومع **كلّ**
  صفٍّ في `byBranch`: خمسةُ دلاءٍ (`prosthetic` · `medical_support` ·
  `physiotherapy` · `legacyDevicesUnsplit` · `unclassified`) لكلٍّ `label`
  عربيّ من `REPORT_ROW_LABELS` القانونية — **لا معجمَ ثانٍ** — و`salesValue`
  و`revenue` **بنفس إعادة التسمية أعلاه بالحرف**، ومطابقتُها تقع في **دالّةٍ
  واحدة** (`departmentMoney`) فقَلبُها يقلب الأقسامَ كلَّها معاً فيُمسَك، ولا
  يُقلَب قسمٌ بالسهو. **و`revenue` تصل `null` للقديم غير المقسَّم**: مبيعاتٌ
  فقط بحكم مصدرها، وصفرٌ كان سيدّعي قياساً لم يقع. **و`devicesCombined`
  خارجَ الدلاء عمداً** — تجميعٌ مشتقّ من `rollups` القانونية لا دلوٌ سادس،
  ولو وُضع بينها لجمعها النموذجُ ستّاً فحسب مالَ الأجهزة مرّتين. **ولا يدخل
  `comparison`** (تفصيلٌ لخمسة أقسامٍ في مقارنةٍ تاريخية ضجيجٌ لا يُقرأ)،
  **ولا مصاريفَ ولا صافيَ لقسمٍ** (المصاريفُ إجماليةٌ للفترة، فلا يُقال إن
  قسماً رابحٌ أو خاسر). **وبلا حرفٍ في `storage.ts`** ولا في القاعدة ولا
  ترحيل — قراءةٌ لرقمٍ محسوبٍ أصلاً.

**والحدُّ المشترك للأداتين الأخيرتين** (`server/ai/tools/reports.ts`):
فرعٌ من مدخل النموذج **لا يُعتمَد إلا للمسؤول العام** (نفسُ
`enforceBranchAccess` تماماً) — غيرُه يبقى على نطاقه بصرف النظر عمّا
طلب. مدىً زمنيّ أطول من الحدّ (٩٢ يوماً للتشغيليّ، سنةٌ للماليّ) **يُرفَض
صراحةً لا يُقصَّر صامتاً** — فلا يبدو تقريرٌ جزئيّ كاملاً. والمسؤولُ بلا
فرعٍ محدَّد يحصل على تفصيلٍ لكلّ فرع؛ بفرعٍ محدَّد لا يحصل عليه.
**والحساباتُ كلُّها في الخادم** — النموذج يُلخِّص أرقاماً جاهزة فقط، ولا
يُطلَب منه جمعٌ ولا طرحٌ ولا مقارنةُ فترتين (`TOOL_TRUST_RULES` تنصّ على
هذا صراحةً الآن).

### المفرداتُ التجارية — `server/ai/semantics.ts`
تركيبٌ رقيق فوق خرائط تسمياتٍ قائمة أصلاً (`shared/manufacturing.ts:
STAGE_LABELS/STATUS_LABELS/PURPOSE_LABELS/SERVICE_TYPE_LABELS`،
`shared/medical.ts: specialtyLabel`) — **لا تسميةَ جديدة ولا قاعدةَ عملٍ
ثانية**، تستعمله الأدواتُ الجديدة لترجمة الحقول الخام للنموذج بدل تسريب
رموزٍ إنجليزية داخلية.

### الواجهة
بطاقةُ المساعد (`AiChatDrawer.tsx`) صارت تعرض تحت كلّ ردٍّ سطرَ تزويدٍ
خفيفاً «اعتمدتُ على: …» (عناوينُ المقالات فقط، **بلا رقمٍ داخليّ للمستخدم
أبداً**، مُشتقٌّ من `ChatOutcome.knowledge` الذي أضافه `chat.ts`)، وزرَّ
«اقترح تصحيحاً». إغلاقُ النافذة يمسح حالة نموذج الاقتراح أيضاً — نفسُ
مبدأ مسح المحادثة القائم («لا يبقى نصفُ اقتراحٍ معلَّقاً بعد إعادة فتح»).

### الأمان — الثوابتُ الخمسة من الطبقة الأولى، بلا استثناء
الصلاحيةُ من الجلسة وحدها (لا من جسم الطلب ولا نصّ الرسالة) · عزلُ الفرع ·
عزلُ المال (المخوَّلُ ماليّاً وحده تصله أرقامٌ مالية — من الأداة **أو من
المعرفة**) · نتيجةُ الأداة **والمعرفة** بياناتٌ لا تعليمات · بلا أداةٍ
عامّة (SQL/جدول/أمر). وأُضيف: **المعرفةُ المعلَّقة أو المرفوضة لا تصل
الاسترجاع أبداً** (اختبارٌ صريح)، و**غيابُ سلطةٍ للمعرفة**: مقالةٌ معتمَدة
مهما بلغت درجتها لا تفتح أداةً ولا توسّع نطاقاً — الاسترجاعُ استعلامٌ عن
نصٍّ، لا فحصُ صلاحية.

### الترحيل
**٠٧٥ فقط** — إضافيّ، idempotent: جدولان (`ai_knowledge_articles`،
`ai_knowledge_suggestions`) وقيودُهما وزرعُ اثنتي عشرة مقالة بمفتاحٍ ثابت
(`ON CONFLICT (seed_key) DO NOTHING`). بلا `DROP` ولا `DELETE` ولا مسٍّ
لأيّ ترحيلٍ من ٠٠١ إلى ٠٧٤. **مستقلٌّ تماماً عن `ai_memory_notes`**
(ترحيل ٠٠٤) — ذاك سياقٌ لمفسِّر الشذوذ الإحصائي وحده، تحقَّق من هذا
بقراءة الكود الفعليّ قبل الترحيل لا افتراضاً.

اختبارٌ دائمٌ للتفصيل بالأقسام: القسم **ج.١٩** في `npm run
test:ai-tools-reports` (٣٧ تأكيداً حيّاً — المطابقةُ الحرفية بالدلاء الخمسة
بعد إعادة التسمية، وأرقامٌ مختلفة لكلّ دلو فلا يمرّ انعكاسُ قسمين، و`null`
لا صفراً، ومصالحةُ الخمسة مع الإجماليّ، و`devicesCombined` من `rollups`
وخارجَ الدلاء، والتسمياتُ من المعجم القانونيّ، و`byBranch`، وغيابُه عن
`comparison`، ووصفُ الأداة يقول ذلك كلَّه للنموذج).

اختبارٌ دائم: `npm run test:ai-knowledge` (دورةُ حياة الاقتراح والمقالة
كاملةً على نقاطٍ حقيقية عبر Express حقيقيّ، بما فيها حجبُ `branch_manager`
عن دور المسؤول العام لمعرفة المساعد تحديداً)، `npm run
test:ai-knowledge-retrieval` (الترشيحُ الخالص، بلا قاعدة بيانات)، `npm run
test:ai-tools-reports` (الأدواتُ الثلاث الجديدة، بما فيها مطابقةٌ حرفية مع
`getAccountingSummary`)، `npm run test:ai-knowledge-ui` (موضعُ التبويب
داخل بوّابة المسؤول، بلا قاعدة بيانات). ومُحدَّثةٌ لتشمل هذه المرحلة:
`npm run test:ai-live` (المعرفةُ تصل نصّ النظام فعلياً، والأدواتُ الجديدة
بحسب الدور)، `npm run test:ai-tools` (السجلُّ المغلَق صار سبعاً)، `npm run
test:ai-ui` (بطاقةُ «اقترح تصحيحاً» ومسحُها عند الإغلاق). و`npm run
test:ai-access`/`test:ai-confidentiality` تعملان بلا تعديل — الطبقةُ
الأولى لم تُمَسّ بحرف.

> **⚠ ملاحظةٌ لمن يشغّل `test:ai-confidentiality`**: عند كتابة هذا القسم
> وُجد فشلٌ **مسبَق** (٥ تأكيدات من ~٣٠) في عزل الفرع للمحاسب، مُثبَتٌ
> بمقارنةٍ نظيفة أنه موجودٌ في `main` قبل أي تعديلٍ من هذه المرحلة —
> غالباً لأن الترقيةَ الحيّة للصلاحيات (`server/routes.ts`، منتصف الطلب
> «تحديثُ الصلاحيات حيّاً») تعيد بناء صلاحيات مستخدمي هذا الاختبار من
> عمود `system_users.can_manage_accounting` **الحقيقيّ في القاعدة**، بينما
> إدراجُ المستخدمَين التجريبيَّين في الملف لا يضبط ذلك العمود صراحةً. لم
> يُصلَح هنا (خارج نطاق هذه المهمّة) — يستحقّ تدقيقاً منفصلاً.

### تصحيحٌ لاحق — مراجعةٌ حيّة على PR #281 (٢٠٢٦-٠٩-٠٩)

مراجعةٌ حيّة مستقلّة قبل الدمج كشفت سبعَ ثغراتٍ دقيقة، عولجت كلُّها في الفرع
نفسِه بلا فتح فرعٍ ثانٍ. المعماريّةُ أعلاه ثابتة؛ ما يلي تصحيحاتٌ ضيّقة.

**١) `operational_summary` كانت مفتوحةً للجميع** — `offeredTo: () => true` رغم
أن التقارير صلاحيةٌ حقيقية (`canViewReports`، تحرس كلّ نقطة تقرير في
`server/routes.ts`). صارت تشترط `isAdmin || permissions.canViewReports === true`
في `offeredTo` **والمنفِّذ نفسِه** (دفاعٌ في العمق، نفسُ نمط `patient_finance`) —
`server/ai/tools/registry.ts`. اختبارٌ حيّ شامل: `test:ai-tools-reports` قسم ب
و`test:ai-live` قسمَي ب/ط (حجبٌ بلا العَلَم، نداءٌ مباشرٌ مرفوض، منحٌ صريح،
سلطة المسؤول، وتجاهُل `branchId` من غير المسؤول).

**٢) خلطُ «المبيعات» بـ«الإيراد» في محوِّل `financial_summary`** — الحقلُ
`revenue` كان يحمل قيمة `s.totalRevenue` (وهي فعلياً **مبيعاتٌ/كلفةٌ مسجَّلة**
بحسب تعليق `storage.ts` نفسِه)، يخالف قاعدة «لا إيراد بلا قبضٍ فعليّ» التي
يطبّقها التقريرُ اليوميّ أصلاً. صار المخرَج: `salesValue = totalRevenue` (مبيعات)
و`revenue = totalPaid` (نقدٌ مقبوضٌ فعلاً) — حقلان لا يتبادلان، بلا مسّ
لـ`storage.getAccountingSummary` نفسِها (الاسمُ الموروث فيها يبقى كما هو، خارج
نطاق هذه المهمّة). وصفُ الأداة والتعليمات في `TOOL_TRUST_RULES` تعلّمان النموذج
الفرق صراحةً. اختبارٌ حيّ بمبلغين مختلفين عمداً (٥٠٠,٠٠٠ مبيعات مقابل ٢٠٠,٠٠٠
مقبوض) — `test:ai-tools-reports` قسم ج، يفضح أيّ انعكاسٍ مستقبليّ لا يكفيه
تساوي صفرَين.

**٣) ست مقالاتٍ معرفةٍ مزروعة كانت مخالفةً للكود الحاليّ**، صُحِّحت مباشرةً في
`server/migrations/075_ai_knowledge.ts` (لم تُدمَج بعد، فلا حاجة لترحيلٍ ثانٍ):
`reception_new_patient_registration` كانت تقول إن الاستقبال لا يُدخل تفاصيل
البتر/المسند/تشخيص العلاج الطبيعي — بينما `CreatePatient.tsx` (`canEditAmputationBuilder
= true`، تعليقٌ مؤرَّخ ٢٠٢٦-٠٧-٣١) و`server/routes.ts` (`mayWriteClinical`، قائمةُ
الحقول المحجوبة) يثبتان أن الثلاثة عادت للاستقبال (قرارات المالك ٢٠٢٦-٠٧-٣١،
٢٠٢٦-٠٨-٠١، ٢٠٢٦-٠٨-٠٦) — تبقى الاستثناء **مواصفاتُ الجهاز الفنية** وحدها
(نوع الطرف، القدم، مفصل الركبة) حكراً على الإدارة والطبيب. `manufacturing_stages`
كانت تقول إن الصيانة وبيع الجزء يراجعهما الطبيب استرجاعياً — تقاعد هذا في
القسمين 4.j و4.k؛ صحِّحت لتقول صراحةً «بلا أي دور للطبيب». `patient_journey_overview`
عُدِّلت لتصف مسار «بلا معاينة» كمسارٍ معتاد لا استثناءً نادراً. `financial_correction_request`
كانت تصف طلبَ تصحيحٍ ينتظر دائماً — بينما `applyPaymentCorrectionDirect`
تطبِّقه فوراً للمسؤول العام تحديداً؛ صُحِّحت لتفرّق الحالتين.
`administrative_reversal` كانت تقول إن التصحيح لا يعمل بعد التسليم — والصحيحُ
العكس: «إلغاء العملية بالكامل» يبقى متاحاً بعد التسليم («والسلطةُ لم تُقيَّد:
المسؤولُ يصحّح دائماً» كما يقول القسم أعلاه بالحرف)، «تراجع عن الشراء فقط»
وحده يتوقّف. `patient_trash_restore` ضاقت عبارتها من «التزام مالي غير صفري»
إلى «أيّ التزامٍ أو قرارٍ معلَّق» (طلب خصم/سعر معلَّق يرفع القرار للمسؤول
حتى لو كان الرصيد صفراً). الستّةُ الباقية (`medical_exam_workflow`،
`maintenance_flow`، `component_sale_flow`، `physio_session_plan`،
`cost_vs_payment`، `return_to_purchase`) رُوجعت وتُركت كما هي — مطابقةٌ للكود
الحاليّ بعد التحقّق المباشر.

**٤) `server/ai/semantics.ts` كانت شيفرةً ميتة** — موجودةٌ، غيرُ مُستهلَكة من
أي أداة. صارت مُستوردةً في `server/ai/tools/registry.ts` وتُلحق تسميةً عربية
مرافقةً (لا بديلة) لكلّ رمزٍ خامٍ من عائلة التصنيع/الاختصاص يصل النموذج:
`activeOrders`/`myManufacturingOrders` (`serviceTypeLabel`، `purposeLabel`،
`currentStageLabel`/`stageLabel`، `statusLabel`)، و`latestExam`/`exams`/
`awaitingMyExam`/`awaitingExpertAssignment`/`deviceEpisodes`/`awaitingExam`/
`decidedExam` (`specialtyLabel`). **استثناءٌ متعمَّد**: حالةُ الحلقة
(`patientDeviceEpisodes.status`) بقيت رمزاً خاماً — لا تسميةَ قانونية لها في
`shared/`، وتطبيقُ `STATUS_LABELS`/`STAGE_LABELS` عليها كان يكون تسميةً
مخترَعة. و`doctorSpecialties` (مصفوفةُ رموزٍ مسطَّحة) بقيت بشكلها — عقدٌ
يعتمده اختبارٌ حيّ بمساواةٍ تامّة، فلا تُعاد بنيتُه لأجل تسميةٍ وحدها.

**٥) لا تزويدَ للبيانات الحيّة في الواجهة** — كانت تعرض عناوين المعرفة وحدها.
أُضيف `ChatOutcome.toolsUsed` (`server/ai/chat.ts`): أسماءُ الأدوات المنفَّذة
مُترجَمةً عبر خريطةٍ عربية جديدة `TOOL_PROVENANCE_LABELS`/`toolProvenanceLabels`
(`server/ai/semantics.ts`) — بلا تكرار، وبعبارةٍ عامّة صادقة لأداةٍ نُسي وسمُها.
**والاسمُ الخامّ لا يصل العميل إطلاقاً بعد اليوم**: نقطة `POST /api/ai/chat`
(`server/routes.ts`) تبني ردّها من حقولٍ صريحة تُسقط `tools` (الخام، للتدقيق
في الخادم وحده) قبل الإرسال — لا `res.json(result.value)` مباشرةً كما كانت
(كان `tools.names` يصل الشبكةَ فعلياً بلا أن تعرضه الواجهة). `AiChatDrawer.tsx`
يدمج `toolsUsed` مع عناوين المعرفة في سطرٍ واحد «اعتمدتُ على: …».

**٦) مدخلاتُ نقاط إدارة المعرفة كانت تفشل مفتوحةً** — `Boolean(req.body.active)`
تُقيِّم `"false"` (نصّاً) إلى `true`؛ و`branchId`/`targetArticleId` مشوَّهان
(`NaN`، سالب، كسريّ) كانا يُقرآن صمتاً «نطاقاً عامّاً»/«مقالةً جديدة» بدل رفضٍ
صريح. صار `active` يُشترَط بوليان حقيقيّ (`typeof === "boolean"`) وإلا ٤٠٠؛
و`branchId` (`resolveBranchIdField`) يُرفَض شكلاً (غير رقمٍ صحيح موجب) **أو
وجوداً** (تحقّقٌ فعليّ من `storage.getBranches()` قبل أيّ كتابة) — والغيابُ/
الفراغُ الصريح يبقيان نطاقاً عامّاً صحيحاً بلا استعلام. و`targetArticleId`
مشوَّهٌ يُرفَض ٤٠٠ بدل أن يُقرأ «مقالةً جديدة» خطأً (`server/ai/knowledge/routes.ts`).
اختبارٌ حيّ: `test:ai-knowledge` قسم ن (٩ تأكيدات، منها مطابقةُ نجاح الغياب
الصريح وسلامة الاقتراح بعد الرفض المشوَّه).

**٧) حدودُ اليوم في `operational_summary` كانت بتوقيت UTC لا بغداد** —
`patients.created_at`/`visits.visit_date` أعمدةُ `TIMESTAMP WITHOUT TIME ZONE`
تُكتَب بـ`NOW()` على جلسةٍ توقيتُها `UTC` (لا `SET timezone` في `server/db.ts`،
مؤكَّدٌ حيّاً)، فمقارنتُها بـ`'YYYY-MM-DD'::date` خاماً كانت تحسب اليومَ بتوقيت
UTC — فرقُ ٣ ساعات عن بغداد. أُضيفت `baghdadRangeBounds` (`server/ai/tools/reports.ts`)
— **نفسُ إزاحة `storage.getAccountingSummary` بالحرف** (`baghdadDays: true`)،
لا حسابٌ ثانٍ — وطُبِّقت على الاستعلامات الخمسة كلِّها (مرضى جدد، زيارات،
جلساتُ علاجٍ طبيعي، وتفصيلا الفرعين). مُثبَتٌ حيّاً بمريضٍ سُجّل عند ٠٠:٣٠
بتوقيت بغداد (= ٢١:٣٠ UTC اليوم السابق): كان يُحسَب ليوم UTC الخطأ، وصار
يُحسَب ليوم بغداد الصحيح — `test:ai-tools-reports` قسم ب.١٤.

اختبارٌ دائمٌ محدَّث بعد هذه المراجعة: `test:ai-tools-reports` (١٠٤+ سطراً
جديداً)، `test:ai-live` (تصحيحُ أقسام ب/ط بالكامل + قسمٌ جديد لرفض النداء
المخترَع)، `test:ai-tools` (تسميةٌ عربية على أمر تصنيع)، `test:ai-knowledge`
(قسمٌ جديد ن)، `test:ai-ui` (تزويدُ الأدوات الحيّة). كلُّها تمرّ كاملةً على
Postgres محلّي طازج (بناءٌ من الصفر عبر `db:push` + كامل الترحيلات) —
`test:permission-authority` و`test:ai-access` و`test:ai-knowledge-retrieval`
و`test:ai-knowledge-ui` بلا تغييرٍ وتمرّ كاملةً أيضاً. وأُعيد تشغيلُ
`test:ai-confidentiality` بعد التصحيحات: الفشلُ الخمسةُ نفسُه أعلاه، بلا زيادةٍ
ولا نقصان — لم تلمس هذه المراجعةُ الكودَ الذي يسبّبه.

### تصحيحٌ ثانٍ — مراجعةٌ حيّة مستقلّة أخرى على PR #281 (٢٠٢٦-٠٩-٠٩)

مراجعةٌ حيّة **مستقلّة عن سابقتها** كشفت أن بيانات المريض نفسَها — لا التقارير
وحدها — كانت بابَ التفافٍ على `canViewPatients`. عولجت خمسُ ثغراتٍ في الفرع
نفسِه.

**١) `patient_lookup`/`patient_clinical_summary` كانتا `offeredTo: () => true`**
— بيانات مريضٍ حيّة (اسمٌ، عمرٌ، هاتفٌ، حالتُه السريرية) لكلّ جلسةٍ مصادَقة
بصرف النظر عن `canViewPatients`. صار الحارسُ `canViewPatientRecords(access)`
(`isAdmin || canViewPatients === true`) — نفسُ حارس `patient_search` القائم
أصلاً، أُعيد استعمالُه لا نُسِخ — يفرض في `offeredTo` **وفي أوّل سطرٍ من كلّ
دالّة** (دفاعٌ في العمق، نفسُ نمط `patient_finance`).

**٢) `my_worklist` كانت تفتح طوابيرَ الفرع بأسماء مرضى** (بانتظار معاينة ·
بانتظار تخصيص خبير · علاجٌ طبيعيّ نشط) لمجرّد `role === branch_manager` أو
`canAddPatients === true` أو (للعلاج الطبيعي تحديداً) `canEnterSessions ===
true` — وثلاثتُها **لا تمنح رؤية سجلّ مريضٍ في التطبيق الحيّ**، تحقّقاً لا
افتراضاً: `GET /api/patients` وGET `/api/patients/:id/cases` وGET
`/api/follow-ups` تحرسها `canViewPatients` وحدها **بلا استثناءٍ من الدور**
بالحرف («لا منحَ دورٍ إضافي» — إصلاحاتٌ 2026-09-01/02/03 موجودةٌ في تعليقات
تلك النقاط نفسِها)، و`canAddPatients` تسجّل مريضاً جديداً ولا تفتح قائمة
الموجودين، و`canEnterSessions` تفتح فقط إدخال عدّادات أجهزةٍ **مجهولة
الهويّة** في `server/sessions_module/routes.ts` ولا تمنح اسمَ مريضٍ واحداً.
فصار الحارسُ الوحيد لكتلة طوابير الفرع كلِّها `canViewPatientRecords(access)`
نفسَها. **والاستثناءان الشرعيّان بقيا بلا مسّ**: عملُ الطبيب الخاصّ
(باختصاصاته هو، مطابقٌ لـGET `/api/medical/worklist` — `isAuthenticated` فقط
بلا `canViewPatients`، لأن النطاق عملُ صاحب الجلسة لا سجلّاً عامّاً) وأوامرُ
الخبير الخاصّة (بمعرّفه هو، مطابقٌ لـGET `/api/manufacturing/my-orders`
بنفس المبدأ).

**٣) `activePhysiotherapyPatientsNow` — أُكمِلت المهمّةُ الأصلية** (كانت
تطلبها ولم تُبنَ بعد): عدّادٌ حيّ لمرضى العلاج الطبيعي ذوي الحالة النشطة
**الآن**، بنفس حقيقة `my_worklist` بالحرف (`patients.is_physiotherapy` +
`patient_cases` بنوعٍ physiotherapy وحالة active) — لا تعريفٌ ثانٍ. **حالةٌ
الآن لا مقياسَ فترة**، فلا تدخل حساب `compare` أدناه بحال.

**٤) `compare: boolean` على `operational_summary`** — يقارن **مقاييسَ الفترة
الثلاثة فقط** (newPatients/visits/physiotherapySessions) بالفترة السابقة
بنفس الطول (`previousPeriod` القائمة أصلاً)، في حقلٍ منفصل `comparison`
بتاريخه الخاصّ. **ولا `awaitingExamNow`/`manufacturingNow`/
`activePhysiotherapyPatientsNow` في `comparison` بحال** — إدراجُ حالةٍ الآن
في مقارنةٍ تاريخية يوهم بأنها قيست في الماضي وهي لم تُقَس. الحسابُ عبر
دالّةٍ واحدة مشتركة `countPeriodMetrics` تُنادى مرّتين (الحاضرة والسابقة) —
لا نسخةَ SQL ثانية تنحرف.

**٥) `byBranch` (visits) — تدقيقٌ مستقلّ أثبت أنها كانت صحيحةً أصلاً**: طلبت
المراجعةُ الحيّةُ التأكّدَ أن تفصيل الفرع للمسؤول يستبعد الزياراتِ المحذوفةَ
ناعماً (`deleted_at IS NULL`) مطابقةً للعدّ الرئيسيّ — **وقراءةُ الكود قبل أيّ
تعديل وجدت الشرط موجوداً بالفعل** في `server/ai/tools/reports.ts` منذ
المراجعة السابقة. لم يكن ثمّة عطبٌ يُصلَح، فلم يتغيّر السطر — لكنّ الاختبار
المطلوب أُضيف فعلياً (زيارةٌ محذوفةٌ تُدرَج ثمّ يُقارَن تفصيلُ الفرع قبلها
وبعدها) ليقفل الضمانةَ ثابتةً بدل أن تبقى ادّعاءً غيرَ مُختبَر.

**٦) branchId فاشلٌ مغلَقاً للمسؤول** — نفسُ حارس ٦.n السابق (`resolveBranchIdField`
لنقاط إدارة المعرفة) يتكرّر هنا بدالّةٍ مستقلّة `resolveAdminRequestedBranchId`
(`server/ai/tools/registry.ts`)، لأن `operational_summary`/`financial_summary`
كانتا تقرآن `Number(input?.branchId)` مباشرةً: نصٌّ غيرُ رقميّ ⟶ `NaN` ⟶ غيرُ
منتهٍ ⟶ يسقط بصمتٍ إلى `null` («كلّ الفروع») بدل أن يُرفَض، وصفرٌ أو رقمٌ
سالبٌ أو كسريّ كان يمرّ كفرعٍ صالح فيُنتج تقريراً بفرعٍ لا وجود له (أصفارٌ
مضلِّلة لا خطأٌ صريح). صار: الغيابُ صحيحٌ (كلّ الفروع) · الحضورُ يُشترَط
شكلاً (رقمٌ صحيحٌ موجب — لا بوليان ولا كائن ولا مصفوفة) **ووجوداً** (تحقّقٌ
فعليّ من `storage.getBranches()`) وإلّا خطأٌ صريح. **وغيرُ المسؤول لا يُفحَص
هنا إطلاقاً** — `branchId` من طلبه ليس سلطةً بحالٍ فيُتجاهَل كأيّ قيمةٍ أخرى،
مطابقةً للقاعدة القائمة (`enforceBranchAccess`).

اختبارٌ دائمٌ محدَّث: `test:ai-tools` (ثلاثةُ فاعلين جدد يثبتون أن لا شيءَ
غير `canViewPatients` يفتح طوابيرَ الفرع — لا الدور ولا `canAddPatients` ولا
`canEnterSessions`)، `test:ai-tools-reports` (أقسامٌ جديدة: activePhysiotherapyPatientsNow
· شكلُ compare وعزلُه عن حالة الآن · الزيارةُ المحذوفة في byBranch · ثمانيةُ
أشكال branchId فاشلة للتقرير التشغيليّ وستّةٌ للماليّ، ونجاحُ الغياب/الصحيح
بعدها، وتجاهلُ غيرِ المسؤول لبranchId مشوَّه بلا رفض). `test:ai-live` مرّت
كاملةً بلا تعديل (فِكستشراتها الأصلية تحمل `canViewPatients: true` أصلاً).
كلُّها على Postgres محلّي طازج؛ `test:permission-authority` كاملةً بلا فشل؛
وأُعيد تشغيلُ `test:ai-confidentiality`: الفشلُ الخمسةُ المعروف نفسُه فقط
(اختلافٌ صفريّ عن المراجعة السابقة) — لم يمسّه هذا التصحيحُ أيضاً، والملفّان
الوحيدان المتغيّران (`server/ai/tools/registry.ts`، `server/ai/tools/reports.ts`)
لا علاقةَ لهما بمساره.

### تصحيحٌ ثالث — تمريرةٌ ضيّقة رابعة على PR #281 (٢٠٢٦-٠٩-٠٩)

أربعُ ثغراتٍ دقيقة أخرى، أضيقُ نطاقاً من التمريرتين السابقتين، عولجت في
الفرع نفسِه.

**١) `resolveDateRange` كانت تقرأ تاريخاً مشوَّهاً **مُرسَلاً صراحةً** كأنه
غيابٌ للمفتاح** — `typeof v === "string" && DATE_RE.test(v)` الفاشلة كانت
تسقط صمتاً إلى `null` فيحلّ محلَّها «اليوم» أو التاريخ الآخر، فطلبٌ لفترةٍ
محدَّدة (بصيغةٍ خاطئة، أو `2026-02-31` — شكلاً صحيحة لكن ٣١ شباط لا يوجد)
كان يتحوّل بصمتٍ إلى فترةٍ لم تُطلَب. صارت الدالّةُ تفرّق **الغيابَ**
(`undefined`/`null` صريحة فقط ⟶ السلوكُ الافتراضيّ كما كان) عن **الحضور
المشوَّه** (أيّ قيمةٍ أخرى — سلسلةٌ فارغة، رقمٌ، بوليان، كائن، أو نصٌّ
لا يصلح تاريخاً حقيقياً بالتقويم لا الصيغة فقط ⟶ خطأٌ صريح). `isRealCalendarDate`
تبني `Date.UTC` من المكوّنات ثمّ تقارن — ٣١ شباط يفيض صامتاً إلى آذار
فتكشفه المقارنةُ. تاريخٌ صحيحٌ واحد ⟶ يومٌ واحد كالمعتاد، وحدودُ بغداد
والحدُّ الأقصى لم يتغيّرا بحرف.

**٢) `resolveAdminRequestedBranchId` كانت تقرأ `branchId: ""` مثل الغياب
تماماً** — سلسلةٌ فارغة أو بياضٌ **وصلت صراحةً** وليست غياباً للمفتاح.
صار الفراغُ الصريح يُرفَض كأيّ قيمةٍ أخرى لا تصلح رقم فرع؛ **الغيابُ
الحقيقيّ وحده** (`undefined`/`null`) يبقى «كلّ الفروع» الصحيحة. وصفُ
الأداتين حُدِّث ليقول «غيابُه (لا يُرسَل) وحده يعني كلّ الفروع؛ فارغٌ أو
غير صحيح يُرفَض».

**٣) `comparison` في `financial_summary` كانت تحمل `outstandingLifetime`/
`collectionRateLifetime`** — و`getAccountingSummary` يعرّفهما صراحةً أرقاماً
**مدى الحياة حتى الآن**، لا مقياسَ فترة. فمقارنةٌ بفترةٍ سابقة تحمل «رصيداً
مستحقّاً» أو «نسبةَ تحصيل» توهم أنهما قيسا في تلك الفترة، وهما لم يُقاسا
هناك أصلاً. صار الشكلُ اثنين: `FinancialPeriodMetrics` (ستّةُ حقول: start،
end، salesValue، revenue، expenses، net) لـ`comparison` وحدها، و
`FinancialPeriodFigures` (تمتدّ منه + الحقلين مدى الحياة) لـ`current`/
`byBranch` كما كانا — لم يُحذَف الحقلان من هناك، الحالةُ الحاضرةُ تحتاجهما
فعلاً. `periodMetricsOnly` تُسقِط الحقلين صراحةً (لا تكتفي بعدم قراءتهما)
قبل أن يدخلا `comparison`.

**٤) نقاطُ `:id` في `server/ai/knowledge/routes.ts` الأربع كانت تستعمل
`parseInt(String(req.params.id))`** — و`parseInt("12abc")` يُرجع **١٢
صامتاً** (يقرأ حتى أوّل حرفٍ غيرِ رقميّ ثمّ يتوقّف)، فطلبٌ بمعرّفٍ مشوَّه
كان يُصيب معرّفاً حقيقياً بالخطأ بدل أن يُرفَض. صارت الأربعةُ (اعتمادُ
اقتراح، رفضُه، تعديلُ مقالة، تفعيل/تعطيل) تستعمل `parsePositiveIntId`
نفسَها المستعملة أصلاً لـ`targetArticleId` — لا مِحلَّلاً جديداً. مُثبَتٌ
حيّاً بمقالةٍ حقيقية: طلبٌ بمعرّفها ملصوقاً بحرفٍ (`"${id}abc"`) يُردّ ٤٠٠
**والمقالةُ الحقيقية تبقى بلا مسّ** — لا اسمُها ولا حالةُ تفعيلها تتغيّر.

اختبارٌ دائمٌ محدَّث: `test:ai-tools-reports` (ثلاثةَ عشرَ شكل تاريخٍ فاشل
لكلا الأداتين، فراغُ branchId صريحاً، وشكلُ `comparison` الستّةِ حقولٍ بلا
الحقلين مدى الحياة بينما `current`/`byBranch` يحتفظان بهما)، `test:ai-knowledge`
(قسمٌ جديد ن.١٠: عشرةُ تأكيداتٍ على المعرّفات الأربعة، منها إثباتُ أن معرّفاً
«قريباً» مشوَّهاً لا يُصيب مقالةً أو اقتراحاً حقيقيَّين). كلُّها على Postgres
محلّي طازج؛ `test:ai-tools`/`test:ai-live`/`test:ai-access`/
`test:permission-authority` كاملةً بلا فشل. وأُعيد تشغيلُ
`test:ai-confidentiality`: الفشلُ الخمسةُ المعروف نفسُه فقط، بلا زيادةٍ ولا
نقصان — الملفّاتُ الخمسةُ المتغيّرة في هذه التمريرة لا علاقةَ لها بمساره.

---

## 4.o التحقّق الإنتاجيُّ الأوّل لِـ AI Assistant v2 — أربعةُ إصلاحات (٢٠٢٦-٠٩-٠٩)

مراجعةٌ إنتاجية على PR #281 بعد دمجه كشفت أربعةَ عيوبٍ حقيقية في الاستعمال
الفعليّ، لا في التصميم. فرعُ `ai-assistant-v2-validation-fixes`. **بلا إعادةِ
تصميم** — أربعةُ إصلاحاتٍ ضيّقة فوق البنية القائمة بحرفها.

### ١) الخلاصةُ السريرية كانت تتجاهل ملفّ المريض نفسَه
مريضُ علاجٍ طبيعي حمل «التشخيص / الحالة = شلل نصفي» ظاهراً في ملفّه، فأجاب
المساعد «آخر تشخيص: لا يوجد» — لأنّ `patient_clinical_summary` كانت تقرأ
`medical_exams` (المعاينة الموقّعة) وحدها، ولا تلمس حقول ملفّ المريض نفسِها
(`patients.disease_type`/`amputation_site`/`injury_side`/`support_type`
ومواصفات الجهاز الفنية) التي تعرضها `CaseDetailSections.tsx` بصرف النظر عن
وجود معاينة. **«لا توجد معاينة موقّعة» ليست «لا يوجد تشخيص»** — حقيقتان
مستقلّتان.

الحلّ: `patientClinicalSummary` (`server/ai/tools/registry.ts`) تقرأ الآن
`patients` أيضاً وتبني `patientFileClinicalFacts` لكلّ اختصاصٍ يحمله
المريض فعلاً — العلاج الطبيعي (التشخيص/الحالة، الإصاباتُ عبر `parseInjuries`
من `shared/case_fields.ts`، نوعُ العلاج)، الأطراف (موقعُ البتر بوصفه
«التشخيص/الحالة» كما تفعل الصفحةُ نفسُها، جهةُ الإصابة، ومواصفاتُ الجهاز
الفنية عبر `specsForSpecialty` **القانونية نفسِها** — لا قائمةً ثانية)،
والمساند (نوعُ المسند وجهةُ الإصابة). **بلا مبلغٍ واحد** في أيٍّ من الأقسام.
ومعها `hasSignedExam` صريحةٌ ثلاثيّة (`true`/`false`/`null` بحسب حمل
الاختصاص) — لا استنتاجاً من غياب سطرٍ في `exams`، فالنموذج لا يُترَك يخمّن.
ونصّ الأداة وقواعدُ التوجيه في `chat.ts` (كلا الوضعين) صارا يُلزمان النموذج
بمناداة هذه الأداة لسؤال التشخيص/الحالة، ونقلِ الحقيقتين معاً دون خلط.

اختبارٌ حيّ جديد ضمن `test:ai-tools` (قسم «ي»): مريضُ علاجٍ طبيعي بتشخيصٍ
مسجَّل وبلا معاينة موقّعة ⟶ الحقلان يصلان متمايزين؛ ومقارنةً بمريضٍ بمعاينة
موقّعة فعلاً؛ وأقسامُ الأطراف والمساند بحقولهما الفنية؛ وغيابُ أيّ مبلغ.

### ٢) النموذج كان يحسب فروقاً ونسباً بنفسه
`compare` في `operational_summary`/`financial_summary` كانت تُرجع رقمَي
الفترتين خاماً (الحاليّ والسابق)، فيطرحهما النموذج ويحسب النسبة بنفسه —
مخالفةً صريحة لقاعدة «لا تحسب رقماً بنفسك».

الحلّ: `computeComparison` (`server/ai/tools/reports.ts`، مُصدَّرة) تبني
كائناً واحداً لكلّ مقياس: `currentValue`/`previousValue`/`delta`
(=الفرقُ الجاهز) و`percentChange` (النسبةُ الجاهزة بمنزلةٍ عشرية واحدة، أو
`null` صراحةً حين `previousValue=0` — لا نسبةَ ذاتَ معنى بلا مقام). والقسمةُ
على **القيمة المطلَقة** للسابقة عمداً: صافٍ يتحسّن من ‎-50,000‎ إلى ‎-20,000‎
(خسارةٌ أصغر) يُقرأ ‎+60%‎ لا ‎-60%‎ — وإلّا بدا التحسّنُ الحقيقيّ تراجعاً.
`comparison.metrics.{newPatients,visits,physiotherapySessions}` (تشغيليّ)
و`comparison.metrics.{salesValue,revenue,expenses,net}` (ماليّ) — كلٌّ
منها كائنٌ محسوبٌ كامل. **بلا** `outstandingLifetime`/`collectionRateLifetime`
في `comparison` (باقيةٌ في `current`/`byBranch` وحدهما، كما كانت). ووصفُ
الأداتين و«قواعد الأدوات والمعرفة» في `chat.ts` صارا يُلزمان النموذج بترحيل
`delta`/`percentChange` حرفياً، وعدمِ اختراع نسبةٍ حين `percentChange` تصل
`null`.

اختبارٌ دائمٌ ضمن `test:ai-tools-reports` (قسم «د» جديد): أربعُ حالاتٍ
حسابية دقيقة (زيادةٌ موجبة، نقصان، قيمةٌ سابقة صفر، صافٍ سالبٌ في الاتجاهين)
+ إثباتُ أن الأرقام الحيّة من الأداتين تطابق `computeComparison` حرفياً، لا
حساباً ثانياً منفصلاً داخلهما.

### ٣) الواجهة كانت تعرض صياغة Markdown خامّاً
`AiChatDrawer.tsx` كانت تعرض `{m.content}` نصّاً عادياً، فتظهر `**نصّ**` و
`##` و`|` حرفياً للموظّف. **بلا مكتبة Markdown في الريبو أصلاً**، فمُحلِّلٌ
صغيرٌ خاصّ (`client/src/components/assistant_markdown.ts`) أخفّ من سلسلة
unified/remark/rehype الكاملة، وأضمن أمناً **بالبناء لا بالتصفية**: يبني
شجرة كتلٍ مطبوعة (عناوين # ## ###، عريضٌ `**نصّ**`، قوائمُ نقاطٍ/أرقام،
جداولُ GFM بسيطة، أسطرٌ جديدة) لا تعرف صياغةَ الروابط ولا الصور ولا HTML
الخام إطلاقاً — فلا تدخل شجرةَ العناصر أصلاً، بلا حاجة لمعقِّمٍ يلاحقها.
الرسمُ (`AssistantMarkdown.tsx`) يبني عناصر React مباشرةً من الشجرة —
**بلا `dangerouslySetInnerHTML` في أيّ من الملفّين إطلاقاً** (مُختبَرٌ
معمارياً). رسالةُ المستخدم تبقى نصّاً عادياً كما كانت. والجدولُ داخل حاويةٍ
`overflow-x-auto` بخطٍّ مضغوط (`text-xs`) فلا ينكسر عرضُ الدرج الضيّق ولا
اتجاهُ RTL.

اختبارٌ دائمٌ جديد `npm run test:assistant-markdown`
(`client/src/components/assistant_markdown.test.ts`): كلَّ نوع كتلة على
حدة، ورسالةٍ واقعية الشكل تجمع عناصر متعدّدة مع إثباتٍ مباشر أن `**`/`##`/`|`
لا تظهر حرفياً في أيّ عقدةٍ بعد التحليل مع بقاء المحتوى نفسِه، ومحتوًى
معاديًا (`<script>`) يبقى نصّاً حرفياً لا يُفسَّر بنيوياً، وفحصٌ معماريّ على
مصدر `AiChatDrawer.tsx` يثبت الوصلَ الفعليّ (الاستيراد + شرط الرسم) لا
مجرّد وجود محلِّلٍ معزول.

### ٤) معرفةُ مسارِ العمل كانت تصل أسئلةً عن بياناتٍ حيّة
سؤالٌ عن مريضٍ بعينه («ما حالة WB-02119؟») كان يستدرج مقالاتِ مسارِ عملٍ
عامّة («تسجيل مريض جديد»، «مراحل التصنيع») إلى تزويد الردّ — لا صلةَ فعلية،
وخلطٌ بين نثر العمل العامّ وحقائق السجلّ.

الحلّ: `isLiveDataOnlyQuestion` (`shared/ai_knowledge_retrieval.ts`) —
بوّابةٌ حتميّة **قبل** `retrieveKnowledge`، لا سؤالَ للنموذج. تكشف رمزَ
مريضٍ صريحاً (`WB-\d{3,}`) أو إشارةَ تقرير/طابور عملٍ حاليّ، **وتفحص في
نفس الرسالة** غيابَ إشارة مسارِ عملٍ/سياسة صريحة (كيف/خطوات/طريقة/صلاحية/
سياسة/لماذا...) — فإن وُجدت الإشارتان معاً (سؤالٌ مختلط) يبقى الاسترجاعُ
مفتوحاً. ومطبِّعٌ أخفّ من `tokenize()` القائمة عمداً (`normalizeSearchText`
وحدها، بلا إسقاط كلمات الاستفهام) لأن «كيف»/«متى» بالذات هي إشارةُ مسارِ
العمل التي تحتاجها هذه البوّابة، وكانت `tokenize()` تُسقطها. الافتراضُ
الآمن حين لا يقين: الاسترجاعُ يمضي كالمعتاد — **لا إضعافَ لسؤالٍ إرشاديّ
حقيقيّ**. `resolveKnowledge` في `chat.ts` (الوضعان معاً) تستدعي البوّابةَ
قبل `retrieveKnowledge`.

اختبارٌ خالصٌ ضمن `test:ai-knowledge-retrieval` (قسم «د» جديد): الحالاتُ
الأربع المطلوبة بالضبط («ما حالة/تشخيص WB-xxxxx؟» ⟶ بلا معرفة، طلبُ تقريرٍ
ماليّ ⟶ بلا معرفة، «كيف أسجل مريض طرف صناعي جديد؟» ⟶ المعرفةُ تبقى مفتوحة)
+ رسالةٌ مختلطة (رمزٌ مع «لماذا») ⟶ تبقى مفتوحة أيضاً + حالاتٌ حدّية.
وإثباتٌ حيّ إضافيّ ضمن `test:ai-live` (قسم «ح.٧»): مقالةُ معرفةٍ تشارك
الكلمةَ المفتاحية «حالة» تظهر فعلاً لسؤال مسارِ عملٍ يشاركها («ما هي خطوات
تحديث حالة الطلب؟») **ولا تظهر** لسؤالٍ عن مريضٍ بعينه («ما حالة WB-xxxxx؟»)
رغم التطابق النصّيّ الحقيقيّ المُثبَت لتوّه — الغيابُ سببُه البوّابة لا عدمُ
التطابق.

### الاختبار والفحص
`npx tsc --noEmit`: ٣٩ خطأً قبل وبعد هذه التمريرة على التمام (فرقٌ وحيدٌ
تجميليّ بترتيب خصائص كائنٍ في رسالة خطأٍ غير متعلّق إطلاقاً بهذه المهمّة،
في `AdminSettings.tsx`، غيرُ مُمَسّ). `npm run build`: العميل والخادم كلاهما
ينجحان. `git diff --check`: نظيف. ثمانيةُ حزم اختبارٍ كاملة على Postgres
محلّي طازج بلا فشل: `test:ai-tools`، `test:ai-live`، `test:ai-ui`،
`test:ai-tools-reports`، `test:ai-knowledge-retrieval`، `test:ai-access`،
`test:permission-authority`، و`test:assistant-markdown` الجديد.

### ما لم يُمَسّ
واتساب · توسيعُ مراجعة العلاج الطبيعي الطبية · محاسبةُ `storage.ts` نفسُها
(المحوِّلُ في `reports.ts` وحده تغيّر) · تكرارُ المرضى · بياناتُ الإنتاج ·
الأسرار. كلُّ العمل والاختبار جرى على قاعدة Postgres محلّية.

---

## 4.p هويّةُ الجهاز في توقيع المعاينة — المرحلة الأولى من قطار الإصلاح (ترحيل ٠٧٧ — ٢٠٢٦-٠٩-١٢)

**المعاينةُ تُوقَّع على جهازٍ بعينه — لا على «أيّ حلقةٍ منتظرة صادفها ترتيبُ
الصفوف».** فرعُ `claude/repair-p1-exact-device-identity`، المرحلةُ الأولى من
قطار إصلاحٍ مبنيٍّ على تدقيق ٢٠٢٦-٠٩-١٢ (INT-02 / RTP-2 / MULTI-1 / MULTI-3 /
RTP-3 / RTP-1).

### الواقعة
ترحيلُ ٠٧٣ أسقط `uq_pde_case_open` فصار للخيط الواحد أكثرُ من حلقةٍ
`awaiting_exam` قانونياً — لكنّ مسارَ التوقيع بقي يفترض حلقةً واحدة:
`claimAwaitingEpisodeForExam` كانت `LIMIT 1` **بلا ترتيبٍ ولا معرّف** فتربط
المعاينةَ بأيّ حلقةٍ صادفها ترتيبُ الصفوف الفيزيائيّ (و`UPDATE` «عاد للشراء»
ينقل الصفَّ العائد خلف الأحدث فتُوقَّع معاينةُ العائد على جهازٍ آخر —
والربطُ **مختومٌ بترِكر ٠٢٨** فلا يُصحَّح). ثمّ `closeRequestsAwaitingExam`
كانت تُغلق **كلَّ** طلبات (مريض، اختصاص) بمعاينةٍ فحصت جهازاً واحداً. وقائمةُ
«معايناتي» كانت تكرّر المريضَ بلا هويّة حلقة وتضع **أقدمَ** طلبٍ على كلّ صفّ
(`DISTINCT ON`) فتختفي شارةُ «عاد للشراء» ويُرجِع زرُّ الصفّ B طلبَ A. وكان
الانتظارُ يُقرأ من `created_at` الحلقة فيظهر العائدُ «منذ ٦٠ يوماً» ويُدفَن
آخرَ الصفحات بترتيب «الأحدث أولاً» — **وهذا أبسطُ تفسيرٍ لشكوى «عاد للشراء لا
يصل الطبيب»**. أُعيد إنتاجُ الثلاثة باستقلال على HEAD `64a8bcbd` قبل أيّ
تعديل، للأطراف والمساند معاً.

### الثابتُ الآن
```
صفُّ «معايناتي» ⟶ طلبُ مراجعةٍ بعينه ⟶ حلقةٌ بعينها ⟶ النافذة ⟶ POST
⟶ قفلُ الحلقة بعينها ⟶ معاينةٌ مختومة عليها ⟶ إغلاقُ طلبها هي ⟶ متابعتُها هي
```
- **`POST /api/medical/patients/:id/exams` يقبل `deviceEpisodeId`** (اختياريٌّ
  للتوافق؛ مشوَّهٌ ⟵ ٤٠٠ `device_episode_invalid`). الحَكَمُ الأخير
  `claimAwaitingEpisodeForExam` **تحت القفل**: معرّفٌ صريح ⟵ تلك الحلقةُ بعينها
  ويُتحقَّق أنها لهذا المريض وهذا الخيط وما زالت `awaiting_exam` **وليست على
  مسار «بلا معاينة»**، وإلّا ٤٠٩ `device_episode_stale`؛ بلا معرّف ⟵
  `resolveExamEpisode` **قبل أيّ كتابة**: صفرٌ = معاينةٌ بلا جهاز (الموروث)،
  واحدةٌ = هي (توافقٌ رجعيّ)، **أكثرُ من واحدة ⟵ ٤٠٩ `device_episode_ambiguous`
  بالمرشَّحين** — لا `LIMIT 1` ولا أوّل ولا أحدث ولا (مريض + خدمة). **وحلقةُ
  «بلا معاينة» ليست مرشَّحاً أصلاً** (لا تُعرَض ولا تُعدّ في الالتباس ولا
  تُوقَّع عليها ولو بمعرّفها الصريح). وتحت القفل بلا معرّف: ظهورُ حلقةٍ ثانية
  بين الفحص والقفل ⟵ `device_episode_stale` («حدّث الصفحة وحدّد الجهاز»)،
  وإن كان مفتاحُ التطابق نفسُه قد سبق ووُقّع ⟵ يُعاد الفائزُ (٠٧٤).
- **وصفرُ كتابةٍ عند الرفض — بترتيب الكتابة لا بالفحص المسبق وحده**:
  `createExam` (الادّعاءُ المقفول) تسبق `applyDecision` (التي تكتب الوصفةَ على
  ملفّ المريض) حين يكون الخيطُ قائماً؛ فالمردودُ — التباساً أو بياتاً أو
  خاسرَ سباقٍ — **لم يكتب حرفاً** في الملفّ ولا الحلقات ولا الطلبات (مُثبَتٌ
  بلقطةٍ قبل/بعد، وبأنّ ملفَّ المريض بعد سباقِ طبيبين يحمل وصفةَ الفائز وحده).
  ومفتاحُ التطابق (٠٧٤) صار يشمل `deviceEpisodeId`: المفتاحُ نفسُه بجهازٍ آخر
  ليس إعادةً بل تناقضاً يُردّ.
- **`closeRequestsAwaitingExam` داخل معاملة التوقيع، وبقاعدة «مستوى
  الاختصاص»** (`specialtyLevelRequestSql` — تعريفٌ واحد تستورده القائمةُ
  وخريطتا الانتظار والإغلاقُ والإرجاع): الطلبُ **العاري** (`device_episode_id
  IS NULL`) أو **مرساتُه جهازٌ حيّ لم يعد ينتظر** (`examined`/`in_manufacturing`/
  `delivered` — زيارةُ متابعةٍ على مسلَّم أُحيلت إلى معاينةٍ كاملة) يُدرج
  المريضَ في القائمة صفّاً بلا حلقة ويُغلقه **أيُّ** توقيعٍ للاختصاص بعده؛
  أمّا المرساةُ إلى حلقةٍ **منتظرة** فلا يُغلقها إلّا توقيعُ حلقتها هي —
  توقيعُ A **لا يُغلق طلبَ B أبداً**؛ والمرساةُ إلى حلقةٍ **ملغاة** لا تُعرَض
  ولا تُغلَق (إغلاقُها من شأن المرحلة الثالثة). وحارسُ «وُقّعت معاينةٌ بعد
  الطلب» (`examSignedAfterRequestSql`) بالقاعدة نفسِها في
  `pendingFullRequestsFor` و`listPendingFullRequests` و
  `returnFullRequestToReception`: معاينةُ A لا تحبس إرجاعَ طلب B.
- **قائمةُ العمل صفٌّ لكلّ حلقةٍ منتظرة** (`episodeId`, `requestedItem`,
  `sequenceNumber`) — مريضٌ بجهازين يظهر مرّتين بجهازيه، ومفتاحُ React
  `${patientId}-${caseType}-${episodeId}` **والنافذةُ تُعاد تركيبُها لكلّ صفّ**،
  وكلُّ صفٍّ يحمل **طلبَ مراجعته هو**: المطابقةُ بالحلقة لصفّ الحلقة، وبالثنائيّة
  للطلب على مستوى الاختصاص (`specialtyLevel` من `pendingFullRequestsFor` —
  لا يُقرَض طلبُ جهازٍ منتظر لصفٍّ غيره، **والأقدمُ أوّلاً**)، والعدّادُ يعدّ
  الأجهزة. **وطلبٌ مرساتُه حلقةٌ أُلغيت لا يُبقي وحده المريضَ في القائمة** —
  لا صفَّ شبحاً.
- **`awaiting_since` (ترحيل ٠٧٧)**: آخرُ دخولٍ إلى `awaiting_exam` — يُكتب في
  `revertEpisodeToAwaitingExam` (البابُ الواحد لـ«عاد للشراء» وإلغاء
  المعاينة) وبافتراض القاعدة عند الإنشاء. و**`created_at` لا يُعاد كتابتُه
  أبداً**. القارئُ `COALESCE(awaiting_since, created_at, pc.created_at)`. الترحيلُ
  إضافيٌّ idempotent: العمودُ يُضاف **بلا افتراضٍ** أوّلاً (وإلّا امتلأ القائمُ
  بلحظة الترحيل)، ثمّ `SET DEFAULT NOW()` للجديد، وتعبئةٌ **حتميّةٌ** واحدة:
  حلقةٌ منتظرة ولها طلبُ «عاد للشراء» معلَّق ⟵ لحظةُ الطلب. لا `DROP` ولا
  `DELETE`.
- **النافذة** (`NewExamDialog`): من «معايناتي» يصل الجهازُ جاهزاً ويُعرَض
  «الجهاز: #٢ · قالب»؛ ومن صفحة المريض/السجلّ تقرأ `awaitingEpisodes` من
  `GET /api/medical/patients/:id/exams`: واحدةٌ تُنتقى تلقائياً، وأكثرُ منتقٍ
  إلزاميّ (زرُّ الحفظ معطَّل حتى الاختيار). و٤٠٩ التباس/بيات يُحدِّث القائمةَ
  والطابورَ بدل إعادة إرسالٍ عمياء.

### ما لم يتغيّر
ختمُ المعاينة (٠٢٨) · مسارُ «بلا معاينة» (حلقتُه ليست مرشَّحاً للتوقيع بحال —
لا في القائمة ولا في المنتقي ولا في الالتباس؛ والصريحُ عليها ٤٠٩) · العلاجُ
الطبيعي (معرّفُ جهازٍ عليه ⟵ ٤٠٩) · عزلُ الفرع (٤٠٣ قبل أيّ هويّة) · بوّاباتُ
البيع/التصنيع (القسم 1.7 من التدقيق) · التصحيحُ الإداريّ · الدفعات.

**مُرجَأٌ صراحةً** (مراجعةُ المرحلة الأولى بستّة عدساتٍ مستقلّة): وصفةُ الجهاز
لكلّ حلقة (MULTI-2 — المرحلة الثانية) · إغلاقُ طلب المراجعة عند إلغاء الحلقة
(المرحلة الثالثة) · هويّةُ الجهاز عبر نقل الفرع (المرحلة الخامسة).

اختبارٌ دائم: `npm run test:exam-identity` (٢١٩ تأكيداً حيّاً للأطراف والمساند
معاً: حلقةٌ واحدة، حلقتان، عودةٌ + منتظرة بالترتيبين، عودةٌ + مُعايَنة، عودةٌ
+ قيد التصنيع، فجوةُ ٤٠٠ يوم، بلا معرّفٍ وواحدة، بلا معرّفٍ واثنتان ⟵ ٤٠٩
وصفرُ كتابة، سباقُ الحلقة الواحدة **وملفُّ الفائز وحده**، توقيعان متزامنان على
حلقتين، شاشةٌ بائتة، طلبٌ مُرجَع، حلقةٌ ملغاة، مريضٌ قديم، عزلُ الفرع، منتقي
النافذة، **طلبٌ على مستوى الاختصاص: مرساتُه مسلَّمٌ، وعارٍ مُحال مع جهازٍ
يُفتَح بعده**، مسارُ «بلا معاينة»، العلاجُ الطبيعي)، و`npm run
test:exam-identity-ui` (عقدُ النافذة و«معايناتي»: المعرّفُ في الحمولة، لا حفظَ
مع أكثرَ من مرشَّح أو أثناء تحميلهم، الرمزان يُلتقَطان، النافذةُ تُعاد تركيبُها
لكلّ صفّ)، و`test:return-to-purchase` (القسم د صار يثبت هويّةَ الصفّ ولحظةَ
انتظاره)، و`test:exam-idempotency` (المفتاحُ نفسُه بجهازٍ مختلف ⟵ تناقض).

---

## 4.q مواصفاتُ كلّ جهازٍ من معاينته هو، وإلغاءُ القديمة لا يُرجع جهازاً تحكمه أحدث — المرحلة الثانية من قطار الإصلاح (٢٠٢٦-٠٩-١٢)

فرعُ `claude/repair-p2-per-device-prescription` (فوق المرحلة الأولى)، يغلق
**MULTI-2** و**INT-01** من تدقيق ٢٠٢٦-٠٩-١٢. **وترحيلُ ٠٧٨ فهرسٌ واحد** لا أكثر.

### الواقعتان (أُعيد إنتاجُهما على HEAD `64a8bcbd` قبل أيّ تعديل، للأطراف والمساند)
- **MULTI-2**: وصفةُ الطبيب تُكتب عند كلّ توقيعٍ على **أعمدة صفّ المريض
  الواحدة** (`applyPrescription` ⟶ `prosthetic_type`/`foot_type`/`injury_side`/
  `amputation_site`…)، وكانت كلُّ قراءةٍ تصنيعية تقرأ تلك الأعمدة: بعد معاينة
  الجهاز B تعرض صفحةُ أمر A نوعَ B وجهتَه، وقائمةُ الأوامر «نوع-B» على أمر A،
  ومعاينةُ **مسندٍ** تقلب جهةَ أمر الطرف وتُظهر نوعَ مسندٍ عليه، وبيعُ B يكتب
  مواصفاتِ B في `patient_cases.details`. والحقيقةُ لكلّ جهازٍ كانت موجودةً
  ومختومة في `medical_exams.prescription` بـ`device_episode_id` — **ولا قارئَ
  تصنيعيّ يسألها**.
- **INT-01**: «لم يشترِ» ⟵ «عاد للشراء» ⟵ معاينةٌ ثانية على الحلقة نفسِها
  ومتابعتُها حيّة. إلغاءُ **الأولى** كان يردّ ٢٠٠ ويُرجع الحلقةَ «بانتظار
  المعاينة» والثانيةُ حيّة (الطبيبُ يراه منتظراً فحصاً والبائعُ يرى «إتمام
  البيع» — ويُردّ ٤٠٩)، ويعيد كتابةَ متابعة «لم يشترِ» إلى «أُغلقت بسبب
  إلغاء المعاينة» — ماضٍ لم يقع. فالإلغاءُ القديم كان **يهدم حاضراً صحيحاً
  ويكتب ماضياً كاذباً** بضغطةٍ واحدة، بلا أن يقول للطبيب شيئاً.

### الثابتُ الآن — MULTI-2
`server/medical/episode_prescription.ts` **تعريفٌ واحد**: أحدثُ معاينةٍ فعّالة
(`activeExamSql`) على الحلقة بعينها ⟶ `deviceSpecsFromPrescription` (مفاتيحُ
`PROSTHETIC_SPECS`/`SUPPORT_SPECS` القانونية + الجهةُ + موقعُ البتر بـ
`buildAmputationSite` بالصيغة البايتية نفسِها).

**ووصفةٌ لا تقول عن الجهاز شيئاً ليست حقيقةً عنه** (`hasAnySpec`): معاينةٌ
فعّالة بوصفةٍ خاليةٍ من كلّ مفاتيح الاختصاص (تشخيصٌ نصّيٌّ وحده) ⟶ المصدرُ
**«ملفّ المريض» بصدق**. وإلّا لأُخفيت أعمدةُ الملفّ وظهرت البطاقةُ كلُّها «—»
ولخرجت لقطةُ البيع فارغة — **محوٌ لما كان يُقرأ، لا تصحيحُ هويّة**.
**ومفتاحٌ واحدٌ يكفي** ليصير الجهازُ هو مصدرَ **بطاقته**. والقاعدةُ في مكانٍ
واحد يستورده القرّاءُ الثلاثة — **ولا تُكتَب بـSQL ثانيةً** فتنحرف عنه.

**ولا يُخفى عن الخبير ما كان يقرؤه** (مراجعةٌ حيّة، تصحيحٌ لاحق): موقعُ البتر
وجهةُ الإصابة **ليسا من مفاتيح الوصفة**، وبانيَ البتر اختياريٌّ في نافذة
التوقيع — فطبيبٌ يكتب نوعَ الطرف ولا يلمس البانيَ كان يترك شاشةَ الخبير بلا
موقعِ بترٍ إطلاقاً، وهو أحملُ حقيقةٍ في بناء طرف. فأعمدةُ الملفّ السريرية
**معروضةٌ دائماً** تحت بطاقة الجهاز، وفوقها سطرٌ يقول من أين هي وأنّ المعتمَدَ
لهذا الجهاز بطاقتُه. والبطاقةُ نفسُها تبقى وصفتَه وحدها — **لا تستعير**.

**ولقطةُ البيع تُكمَل من الملفّ حين لا لبسَ وحده**: خيطٌ بحلقةٍ **واحدة** ⟶
عمودُ الملفّ لا يمكن أن يصف جهازاً غيرَها (كتبه التسجيلُ أو هذه المعاينةُ
نفسُها) فيُكمِل ما سكتت عنه الوصفة. وبأكثرَ من حلقةٍ ⟶ **يبقى فارغاً**:
الصمتُ أصدقُ من استعارةِ مواصفةِ جهازٍ آخر. والوصفةُ تعلو على الاثنين دائماً.

يستورده ثلاثةُ قرّاء:
- **`getOrderDetail`** يرسل `deviceSpecs` **بمصدرٍ مُعلَن**: `{source:"exam",
  examId, doctorName, signedAt, specs}` أو `{source:"patient_file"}` للأمر
  الموروث بلا هويّة جهاز — لا خلطٌ صامت. ومعه `deviceEpisodeId`/`requestedItem`/
  `sequenceNumber`. والشاشةُ (`ManufacturingOrder.tsx`) تعرض بطاقةَ «مواصفات
  هذا الجهاز — من معاينته» وتُخفي أعمدةَ المريض بوصفها مواصفاتٍ حين تُعرَف
  المعاينة، وتقول للموروث «من ملف المريض كما كان».
- **`listOrders`** (`/orders` و`my-orders`): `itemType` من وصفة حلقة الأمر حين
  لها معاينةٌ فعّالة **تقول شيئاً** (فارغُها يبقى فارغاً لا مقترَضاً)، ومن عمود
  المريض للموروث كما كان. والاستعلامُ يجلب **الوصفةَ كاملةً** لا حقلاً منها،
  فالقاعدةُ تُقرأ من الملفّ المشترك لا تُكرَّر في SQL.
- **`startDeviceSaleOperationallyTx`**: تفاصيلُ الحالة لحظةَ البيع من معاينة
  الحلقة هي (+ ما أدخله هذا البيعُ صراحةً في `fields`، ووقائعُ المريض: سببُ
  الإصابة وتاريخُها ونوعُها) — **لا من عمودٍ قد يخصّ جهازاً آخر**. وبلا
  حلقةٍ أو معاينة: الأعمدةُ كما كانت. **و`patient_cases.details` يبقى صفّاً
  واحداً للخيط** — لقطةُ آخر بيعٍ **من معاينة جهازه هو**؛ الحقيقةُ لكلّ جهازٍ
  على أمره وحلقته لا فيه.

  **وحدُّ ذلك صريحٌ ومُثبَتٌ حيّاً** (دحّاضٌ عدائيّ): بيعُ الجهاز B **يكتب فوق**
  لقطةِ A في الصفّ نفسِه — فقارئا `patient_cases.details` (لوحةُ حالات المريض،
  وعمودُ `case_details` في بطاقة المراجعة) يعرضان مواصفاتِ B على مريضٍ أمرُ A
  ما زال قيد التصنيع. **وهذا سابقٌ لهذه المرحلة لا ناتجٌ عنها** — على الأساس
  كانت لقطةُ A تصير مواصفاتِ B **لحظةَ توقيع معاينة B** لا لحظةَ بيعه، فالمرحلةُ
  تضيّق الضررَ ولا تُزيله. وإزالتُه تحتاج تخزيناً لكلّ جهازٍ على حدة (مرحلةٌ
  لاحقة)، و**بطاقةُ الأمر وقائمتُه صحيحتان لكلّ جهازٍ اليوم** فلا يُبنى عليه قرار.
- التصحيحُ السريريُّ اللاحق (`PATCH`) يكتب على صفّ المعاينة نفسِه، فيتبعه أمرُ
  جهازه — وجهازٌ آخر لا يُمَسّ. **ولم يتغيّر** `applyPrescription` ولا أعمدةُ
  المريض (ملفُّ المريض يبقى «آخرَ ما وُصف» كما كان — لأن شاشاتٍ كثيرة تقرؤه).

### الثابتُ الآن — INT-01 (`server/medical/cancel_exam.ts`)
- تُقاس **أحدثُ معاينةٍ فعّالة على الحلقة** (بترتيب `signed_at` ثمّ `id`). إن
  وُجدت أحدثُ من المُلغاة ⟶ **٤٠٩ وصفرُ كتابة**: لا شاهدةَ إلغاء، ولا إرجاعَ
  للحلقة، ولا مسَّ لأيّ متابعة، **ولا سطرَ تدقيق**. والرسالةُ **تسمّي الأحدثَ
  بعينها** (`#id`) فيعرف الطبيبُ ما الذي يحكم الجهازَ الآن.
- **والحجبُ مؤقّتٌ لا دائم**: المعايناتُ تُسحَب على الجهاز **من الأحدث إلى
  الأقدم**. فمَن أراد سحبَ توقيعه عن قديمةٍ خاطئة يُلغي ما بعدها أوّلاً، ثمّ
  تُلغى القديمةُ فعلاً — فتبقى السلسلةُ متّسقةً في كلّ لحظة، ولا يقع رفضٌ
  ضمنيٌّ صامت ولا بابٌ مسدود.
- وإن كانت المُلغاة هي الأحدث ⟶ كما كان دائماً: الحلقةُ تعود «بانتظار
  المعاينة» ومتابعتُها **الحيّة** تتقاعد `closed_exam_cancelled`.
- **والمتابعةُ المنتهية لا يُعاد كتابةُ تاريخها أبداً** (`isTerminal`): «لم
  يشترِ» تبقى «لم يشترِ» بسببها كما قاله المريض — حتى في الإلغاء العاديّ
  لمعاينةٍ وحيدة.
- الحُرّاسُ التجارية كما هي وتسبق: تصنيعٌ/تسليم/بيعٌ معتمَد/طلبٌ معلَّق ⟶ ٤٠٩
  برسالتها هي.
- **والرسالةُ تدلّ على مخرجٍ ممكن فعلاً**: إلغاءُ الأحدث لصاحبها أو للمدير
  المسؤول، فتقول صراحةً «أو راجع المدير المسؤول إن كانت لطبيبٍ آخر» — وإلّا
  أرسلت طبيبةً إلى بابٍ يردّها ٤٠٣ (أمسكته المراجعةُ الحيّة).

### الترحيل — ٠٧٨ فهرسٌ واحد
صارت بطاقةُ كلّ أمرٍ تقرأ وصفةَ حلقتها، فدخل إلى `listOrders` استعلامٌ مترابط
لكلّ صفّ — و`medical_exams` **بلا فهرسٍ على `device_episode_id` إطلاقاً**،
فصار المخطَّطُ `Seq Scan` مرّةً لكلّ أمر. قيس حيّاً (٤٠٠ أمرٍ، ٨٠٠٠ معاينة)
على **صفحة هبوط الخبير**: ٣٣٠ مللي ثانية ⟵ ١٩ بالفهرس.
`idx_medical_exams_episode (device_episode_id, signed_at DESC, id DESC)
WHERE device_episode_id IS NOT NULL` — **جزئيٌّ** (لا موضعَ فيه للعلاج الطبيعي
ولا للموروث بلا هويّة)، وترتيبُه **ترتيبُ القارئ نفسُه** فيُخدَم `LIMIT 1` بلا
فرز. إضافيّ، idempotent، بلا `DROP` ولا `DELETE` ولا مسٍّ لترحيلٍ من ٠٠١ إلى
٠٧٧. (أمسكته المراجعةُ الحيّة بقياسٍ فعليّ، لا بالتقدير.)

### وما لم يتغيّر
ختمُ المعاينة (٠٢٨) · مسارُ التوقيع وهويّةُ الجهاز (4.p) · محاسبةُ البيع
(`assignManufacturing` ونصفاها) · `applyPrescription` وأعمدةُ المريض ·
`AssignExpertDialog` (كانت تقرأ `prescribedSpecsForEpisode` أصلاً) · العلاجُ
الطبيعي · واتساب وتلغرام.

**مُلاحَظٌ للمرحلة الرابعة (لم يُصلَح هنا — سابقٌ لهذه المرحلة، مُثبَتٌ)**:

**انقلابُ ترتيب القفل بين إلغاء المعاينة وإتمام البيع.** `cancelExam` يقفل
**معاينة ⟶ حلقة ⟶ متابعة**، و`completeReceptionSale` ⟶ `setCommercialFields`
يقفل **متابعة ⟶ (خيط ⟶ حلقة)** عبر `assignManufacturing`. فضغطتان متزامنتان
على المعاينة والبيع تُنتجان **جموداً حقيقياً** (`deadlock detected`) بعد مهلة
`deadlock_timeout`:
- في الشكل الحرّ يخسر **الإلغاء** فيُردّ **٥٠٠** «تعذّر إلغاء المعاينة» بدل
  ٤٠٩ الصحيحة — لأن مصيدةَ المسار تترجم `ExamCancelError` وحدها.
- وفي الشكل المُجبَر يخسر **البيع**، فيهرب `DeviceEpisodeError` رفضاً **غيرَ
  ملتقَط** من `fail()` (التي تعالج `FollowupError` وحدها) — فلا يصل الطلبَ ردٌّ
  إطلاقاً، **وبلا معالجٍ لـ`unhandledRejection` تخرج العمليةُ نفسُها**.

**ولا حالةَ ممزّقة في أيّ شكل**: كلُّ جولةٍ تنتهي إلى «بِيع كاملاً» أو «لم
يُبَع إطلاقاً» — أمرٌ وقيدُ كلفةٍ ومتابعةٌ وحلقةٌ متّسقة. والعيبُ **رَدٌّ خاطئ
وخروجُ عملية، لا فسادُ بيانات**.

**وسابقٌ لهذه المرحلة بالقياس**: نفسُ السكربت (١٢ جولة) على شجرة المرحلة
الأولى وعلى هذا الفرع سواء — `deadlocks Δ=١١`، و١١ ردَّ ٥٠٠، وصفرَ ٤٠٩، في
الاثنين. استعلامُ «أحدثُ معاينة» يوسّع نافذةَ حَمْل قفل الحلقة رحلةً واحدة
**ولا يغيّر النتيجةَ قياساً**. صنفُ INT-05، وجردُ المرحلة الرابعة.

اختبارٌ دائم: `npm run test:device-prescription` (٤٣ تأكيداً حيّاً: بِيع A ثمّ
عُوين B، بيعُ A بعد معاينة B (الالتقاطُ من معاينة A لا من الأعمدة)، عبر
الاختصاصين، التصحيحُ اللاحق يتبعه أمرُه وحده، الأمرُ الموروث بمصدرٍ مُعلَن،
الخبيرُ يقرأ بلا مال، المساند، **ووصفةٌ بلا مواصفاتٍ ⟶ ملفُّ المريض لا شاشةٌ
فارغة، ومفتاحٌ واحدٌ يكفي وبطاقتُه لا تستعير، وأعمدةُ الملفّ تبقى على الشاشة، ولقطةُ البيع تُكمَل من الملفّ بحلقةٍ واحدة ولا تُكمَل بحلقتين**) · `npm run test:exam-cancel-safety` (٦٦
تأكيداً للأطراف والمساند معاً: شكلُ INT-01 ⟵ ٤٠٩ برسالةٍ تسمّي الأحدث وصفرُ
كتابة — لقطةٌ مطابقة، ولا شاهدةَ، ولا سطرَ تدقيق — مع إثبات أن إتمام البيع
يمضي بعدها؛ والمخرجُ «الأحدثُ أوّلاً ثمّ القديمة» يعمل فعلاً؛ وإلغاءُ الأحدث
يُرجع ويُقاعد متابعتَه هو وحده؛ ووحيدةٌ بمتابعةٍ منتهية/حيّة؛ وبعد البيع ٤٠٩)
· `npm run test:manufacturing-order-ui` (عقدُ الشاشة، ٨ بنود — منها أن أعمدةَ
الملفّ معروضةٌ دائماً وأن سطرَ مصدرها حاضر).

---

## 4.r السقالةُ تُسحَب والتاريخُ يُحمى — المرحلة الثالثة من قطار الإصلاح (ترحيل ٠٧٩ — ٢٠٢٦-٠٩-١٣)

**خطأُ إدخالٍ في ثانيةٍ لا يصير خلوداً؛ وتاريخٌ حقيقيٌّ لا يُهدَم أبداً.**
فرعُ `claude/repair-p3-case-disposal`، يغلق **CASEDEL-01/02/03/05/06** و
**INT-06 = RTP-5** من تدقيق ٢٠٢٦-٠٩-١٢.

### الواقعةُ المُعادُ إنتاجُها (على HEAD `bab05821` قبل أيّ تعديل، للنوعين معاً)
موظّفٌ يفتح «أطراف صناعية» أو «مساند» بالخطأ. والتطبيقُ **يفتح تلقائياً بلا
قرارِ إنسان** حلقةَ جهاز (`awaiting_exam`) وطلبَ مراجعةٍ للطبيب وزيارةَ علامة.
ثمّ يضغط المسؤولُ سلّةَ بطاقة الحالة فيُردّ:
- **«يوجد سجل أجهزة لهذا النوع — لا يمكن حذفه»** — `caseHasEpisodes` كانت
  `SELECT 1 … WHERE case_id = ?` **بلا شرطِ حالةٍ أو تاريخ**، فحتى حلقةٌ
  `cancelled` فارغةٌ تحبس الخيطَ إلى الأبد؛
- **أو نصَّ Postgres خامّاً**: `violates foreign key constraint
  medical_review_requests_case_id_…` — مفتاحٌ `NO ACTION` لم يتعلّمه أحد.

**ولا مخرجَ ثالث**: إلغاءُ الحلقة لا يفكّ الحبس (الملغاةُ صفٌّ أيضاً) **ويترك
طلبَه `pending` شبحاً** ومتابعتَه حيّةً، والتصحيحُ الإداريّ يردّ ٤٠٤ لحلقةٍ لم
تُبَع، و«تعديل مريض» يُطفئ العَلَمَ فيترك حالةً شبحاً. فصار الخطأُ **غيرَ قابلٍ
للتراجع إلى الأبد**.

### القاعدةُ الواحدة — `server/patient_cases/disposal.ts`
**الخلودُ يُكتسَب بالتاريخ لا بوجود الصفّ.** `classifyCaseDisposal` تُنادى
**داخل معاملة الكتابة وتحت قفل المريض** (`pg_advisory_xact_lock(919, …)`) —
**حالةُ الشاشة ليست حدَّ أمان**: بين العرض والضغطة قد يوقّع طبيبٌ معاينةً أو
يُفتَح أمرُ تصنيع. وتُرجع إمّا **سقالةً بأرقامها** وإمّا **حاجزاً برمزه وسببه
وبابِه**.

**ثمانيةُ حواجزَ، لكلٍّ رمزٌ وبابُ خروج** (٤٠٩ + `code` + `remedy`):
| الرمز | ما يمنع |
|---|---|
| `signed_exam` | معاينةٌ موقّعة — **ولو أُلغيت لاحقاً** (ختمُ ٠٢٨ وشاهدةُ ٠٦١) |
| `followup` | متابعةُ قرارِ مريضٍ بعد المعاينة |
| `work_order` | أمرُ تصنيعٍ أو صيانة (الحارسُ القديم بنصّه «سجل تصنيع») |
| `tagged_payment` | دفعاتٌ موسومة بهذا النوع — مالٌ قُبض |
| `pending_charge` | عمليةُ «بلا معاينة» **بأيّ حالة**: معلَّقةً تنتظر، أو مُكتمَلةً قُيِّد مبلغُها |
| `discount_request` | طلبُ خصمٍ معلَّق لم يُحسَم |
| `live_episode` | جهازٌ تجاوز مرحلةَ الطلب — **ونصُّه يبقي «سجل أجهزة»** حيث كان صادقاً |
| `reviewed_request` | طلبُ مراجعةٍ حسمه الطبيب — قرارٌ وقع |

**والسقالةُ تُعرَّف بأن لا شيءَ يشير إليها** — لا بالنيّة: حلقةٌ
`awaiting_exam` أو `cancelled` **وبلا** معاينة · متابعة · أمرِ عمل · مبلغٍ
معلَّق · **قيدِ كلفة · دفعةٍ · زيارةٍ** · طلبِ مراجعةٍ حسمه إنسان. **وكلُّ
مفتاحٍ أجنبيٍّ يشير إليها يُفحَص، لا بعضُه** — مرجعٌ غيرُ مفحوصٍ يعني إمّا
٢٣٥٠٣ خامّاً على وجه المستخدم (وهو العطبُ نفسُه) وإمّا هدمَ تاريخٍ حقيقيّ.

### ما يقع عند السحب — ذرّياً في معاملةٍ واحدة
① طلباتُ المراجعة السقالية **تُسحَب ولا تُمحى**: `cancelled` بلا `decision`
(لم يقرّر طبيبٌ شيئاً) مع مَن سحب ومتى والسبب. **ومرساتاها تُحرَّران والحقيقةُ
تُنقَل إلى الملاحظة** («سُحبت الحالة #١٢ وطلبُ الجهاز #٨٣») — الصفُّ يبقى يقول
عن أيّ حالةٍ وأيّ طلبٍ كان. ② زيارةُ «إضافة نوع حالة» تُحذَف **ناعماً
وتُفصَل** — فلا يقرأ أحدٌ على خيط العلاج الطبيعي زيارةً تقول إن نوعاً أُضيف وقد
سُحب، **ولا سطرَ جنائيّ يُستثار** (ذاك للهدم الحقيقيّ). ③ الحلقاتُ السقالية
تُحذَف فيزيائياً — صفٌّ فتحه التطبيقُ ولم يستعمله أحدٌ ليس تاريخاً يُحفَظ.
④ ثمّ الزياراتُ والدفعاتُ تُنقَل للحالة الباقية والصفُّ يُحذَف والأعلامُ
وأعمدةُ التفاصيل تُصفَّر — **كما كان بحرفه**. ⑤ **وسطرُ التدقيق يسمّي ما
أُزيل بأرقامه** ومعه سببُ السحب.

**وكلفةُ الحالة لم تتغيّر بحرف**: `manual` تُخصَم من `total_cost` بقيد
`case_retired`، و`auto` تُنقَل للحالة الباقية — المنطقُ القائم نفسُه.

### إلغاءُ الحلقة يُنظّف أثرَه (INT-06 = RTP-5)
`cancelPreManufacturingDeviceEpisode` صارت تسحب — **في معاملتها نفسِها**:
- **طلبَ المراجعة المرساةَ إليها** ⟶ `cancelled`. وبلا ذلك كان يبقى `pending`
  إلى الأبد: يقرأ الطبيبُ جهازاً سُحب، **ويشغل مرساةَ التفرّد** (`uq_mrr_pending_*`
  مشروطةٌ بـ`status='pending'`) فيُردّ الطلبُ الصحيح التالي ٤٠٩ بلا سبب مفهوم.
- **والمتابعةَ الحيّة** ⟶ الطرفيّةُ الخامسة `closed_request_cancelled`.
  **ولا واحدةٌ من الأربع تصفها**: المريضُ لم يرفض الشراء، ولا سقطت معاينةٌ
  سريرياً، ولا بطلت صفقةٌ بقرارٍ إداريٍّ بعد بيع — بل سُحب **الطلبُ نفسُه**.
  **والمنتهيةُ لا يُعاد كتابةُ تاريخها** (درسُ INT-01 بالحرف): «لم يشترِ» تبقى
  كما قالها المريض، ولا حدثَ زائفٌ يُكتب عليها.

`TERMINAL_STATUS_SQL_LIST` (في `shared/followup.ts`) **مصدرٌ واحد** للقوائم
المكتوبة يدوياً في SQL — نسيانُ طرفيّةٍ في موضعٍ يجعل متقاعدةً تُحسَب حيّةً
فيُقفل الفهرسُ الفريدُ الملفَّ إلى الأبد (درسُ ٠٦١ ثمّ ٠٦٤).

### إطفاءُ العَلَم لا يُيتّم حالةً (CASEDEL-06 = INT-15)
الأعلامُ و`patient_cases` مصدرا حقيقةٍ بلا مزامنة (`syncPatientCases` **إنشاءٌ
فقط**). فإطفاءُ عَلَمٍ من «تعديل مريض» وللمريض حالةٌ من نوعه: **يُسقَط الحقلُ
ويُبلَّغ البابُ الصحيح** (`caseFlagNote`) — لا حذفٌ ضمنيٌّ من بابٍ ثانٍ، ولا
شبحٌ يُترَك. **والانتقالُ `true ⟶ false` وحده يُمسَك** (النموذجُ يرسل الكائنَ
كاملاً في كلّ حفظ، فلا يُقاس الحضور)، والرفعُ حرٌّ كما كان.

### والشاشة — نفسُ زرّ السلّة، بلا زرٍّ ثانٍ
حقلُ **سببٍ اختياريّ** (الخادمُ يضع نصّاً افتراضياً صادقاً حين يُترَك فارغاً،
فلا يوقف حقلٌ إضافيٌّ تصحيحَ خطأ إدخال) · ونصُّ النافذة يقول ما يُزال وما
يمنع · **والتوستُ يعرض `remedy`** مع السبب: ما يمنع، ثمّ الخطوةُ التي تفكّه.

### وما لم يُمَسّ
ختمُ المعاينة (٠٢٨) وإلغاؤها (٠٦١) · التصحيحُ الإداريّ (٠٦٤) · تصحيحُ السعر
بعد البيع · محاسبةُ البيع والصيانة · سلّةُ المرضى (٠٦٨) · كاسكيدُ
`deletePatient` · مسارُ التوقيع وهويّةُ الجهاز (٤.p) · وصفةُ كلّ حلقة (٤.q) ·
العلاجُ الطبيعي · واتساب وتلغرام. **والصلاحيةُ كما كانت**: سحبُ نوع الحالة
للمسؤول العام حصراً — لا مديرَ فرعٍ ولا استقبالَ ولا طبيب.

### الترحيل
**٠٧٩ فقط** — إضافيّ، idempotent، **توسيعُ قيودٍ إلى مجموعةٍ أوسع** فكلُّ صفٍّ
قائم يجتازها كما هو: حالةُ `cancelled` لطلب المراجعة وشكلُها · والطرفيّةُ
الخامسة للمتابعة · وفهرسا «المتابعة الحيّة الواحدة» يعرفانها. **بلا عمودٍ
جديد، ولا `DROP TABLE`، ولا `DELETE`، ولا `UPDATE` على أيّ صفّ**، ولا مسٍّ
لترحيلٍ من ٠٠١ إلى ٠٧٨.

### تصحيحٌ لاحق — سبعُ ثغراتٍ أمسكها مراجعٌ مستقلّ قبل الدمج
**١) والمسحوبُ سقالةٌ لا تاريخ (P1 — العطبُ بعينه عائداً).** `cancelled` لم
تكن في مفردات السقالة، وهذا المسارُ نفسُه يكتبها عند إلغاء الحلقة **ويُبقي**
مرساتَها. فكانت «افتح ⟶ ألغِ ⟶ اسحب» تُردّ `live_episode` برسالةٍ كاذبة
(«مُعايَن أو قيد التصنيع») وبابٍ لا يوجد — الحبسُ الأبديُّ نفسُه. **٢)** وطلبٌ
مسحوبٌ وحدَه كان يُقرأ «حسمه الطبيب». **٣) والمرساتان معاً لا إحداهما**:
الحلقةُ تُعَدّ سقالةً بشرطٍ على `device_episode_id` والصفُّ كان يُختار
بـ`case_id` — فطلبٌ بلا حالةٍ يُترَك فينفجر حذفُ الحلقة بـ٢٣٥٠٣ خامّاً.
**٤) وقرارُ الطبيب لا يُمحى**: طلبٌ `returned` يحمل قرارَه وسببَه الإلزاميّ
ومَن قرّره، وكانت الكتابةُ تمحو الثلاثة. فصارت **تُلحِق ولا تستبدل**، والقيدُ
يشترط `decided_at` وحدها — الشهادةُ لا تُهدَم لتناسب قيداً. **٥) وأربعةُ
مواضعَ** كانت تقرأ ثلاثَ طرفيّاتٍ فقط (قفلُ سعر الجهاز، المتابعةُ الحيّة،
وتصادمُ الدمج بطرفيه) — فالمتقاعدةُ بالسحب تُحسَب حيّةً ويبقى `total_cost`
مقفلاً بلا متابعةٍ ولا سعرٍ معتمَد. **٦) و`caseFlagNote`** كانت تُرسَل ولا
تُعرَض — الإسقاطُ الصامتُ الذي وُضعت لمنعه — ويُسقَط الحقلُ من `req.body`
أيضاً كي لا يُقاس تصحيحُ هاتفٍ على أنه تغييرُ تصنيف. **٧) و٢٣٥٠٣** من فتحِ
طلبٍ متزامن (لا يأخذ قفلَ المريض) صار يُترجَم «تغيّرت حالة الملف — حدّث
الصفحة»؛ المعاملةُ كانت تتراجع كاملةً أصلاً، والرسالةُ وحدها تغيّرت.
ومعها: مفرداتُ `shared/schema.ts` لحقت الترحيلاتِ الثلاثة (كانت تقف عند
`converted` فتُنتج قيداً وفهرسين خاطئين لبيئةٍ تُبنى بـ`db:push`)، ومطابقةُ
زيارة العلامة ضاقت إلى `details` وحده (لا `notes LIKE`)، **وقسمُ «م» في
`test:episode-manufacturing` حُدِّث** ليُثبت القرارَ الجديد (السقالةُ تُسحَب،
والمسلَّمُ يمنع كما كان) بدل القديم الذي انعكس.

اختبارٌ دائم: `npm run test:case-disposal` (٣٠٣ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية، **للأطراف والمساند معاً**: السقالةُ الكاملة بالمسار
الطبيعيّ · الملغاةُ الفارغة مقابل الملغاة ذاتِ المعاينة · إلغاءُ الحلقة
بطلبِه ومتابعتِه والمرساةِ المحرَّرة · الحواجزُ الثمانية بصفرِ كتابةٍ لكلّ
واحد · الخيطُ الموازي والخدمةُ الأخرى لا يُمَسّان · إعادةُ القرار تحت القفل ·
الضغطتان المتزامنتان · المزامنةُ لا تُعيد البناء · إطفاءُ العَلَم · الصلاحيةُ
بثلاثة أدوار · عزلُ العلاج الطبيعي · وحرّاسٌ معماريّون: لا `caseHasEpisodes`
تعود، ولا قائمةَ طرفيّاتٍ ثانية، ولا هدمَ تاريخٍ في قاعدة السحب).

### تكملةُ المرحلة الثالثة — الحالةُ ذاتُ التاريخ **تُغلَق** ومالُها يُردّ بقيدٍ معاكس

**الحاجزُ وحده كان نصفَ مخرج.** «لا يُحذَف» صحيحة، لكنّها لا تعني أن الحالة
تبقى في طوابير العمل إلى الأبد: مريضٌ أنهى علاجَه، أو جهازٌ سُلِّم وأُغلق
ملفُّه، كان يبقى «نشطاً» في قائمة الطبيب وعدّادات الفرع بلا بابٍ لإخراجه.
**فصار للسحب بابان يقرّر الخادمُ بينهما**: الهدمُ للسقالة وحدها كما هو،
و**الإغلاق** لكلّ ما له تاريخ.

**والمفردةُ موجودةٌ سلفاً بلا كاتب — بلا ترحيل**: `patient_cases.status`
يُقرأ `= 'active'` في سبعة مواضع حيّة (خريطتا انتظار المعاينة · قائمةُ عمل
الطبيب · عدّادُ العلاج الطبيعي · أدواتُ المساعد · مزامنةُ الكلفة) **ولم يكن
في المستودع كاتبٌ واحد يضع `'closed'`**. فالإغلاقُ يستعمل الطبقةَ كما
صُمِّمت: تختفي الحالةُ من كلّ طابور، **وتبقى في ملفّ المريض** بحالتها
(`getCasesByPatientId` تُرجع كلّ الحالات)، و`syncPatientCases` لا تُعيد
بناءها (فحصُها `caseType` لا الحالة).

**ولا صفَّ تاريخيٍّ يُمَسّ**: المعايناتُ ونسخُها وملاحقُها · أوامرُ التصنيع
وسجلُّها ومراحلُها · التسليمُ بختمه · الصيانةُ · الحلقاتُ · **الدفعاتُ
الأصلية** · قيودُ الكلف · `patients.total_cost` · `patient_cases.cost`.
الإغلاقُ يغيّر **حالةَ الصفّ وحدها**.

**والمالُ يُردّ حركةً مستقلّة لا تعديلاً على دفعة**: صفُّ `payments` **بالسالب**
موسومٌ بقسمه ومربوطٌ بالحالة، ومعه **قيدُ يوميةٍ مرآة** (مدينٌ للإيراد · دائنٌ
للصندوق) **في المعاملة نفسِها**. ولا `reverseJournalEntryTx`: تلك تعكس قيداً
بكامله وتَسِمه `reversed` فتكتب على دفعةٍ صحيحةٍ وقعت فعلاً ماضياً لم يقع —
والمستردُّ قد يكون **جزءاً** من مجموع دفعاتٍ كثيرة. و**الوارد والإيراد ينقصان
تلقائياً** لأن `totalPaid = SUM(payments.amount)`.

**وصافي المقبوض يُقرأ من القاعدة تحت القفل** (`SUM` يشمل الاستردادات السابقة
فيخصم نفسَه) — لا من الشاشة ولا من عدّادٍ مخزَّن: فلا يُردّ المبلغُ مرّتين،
و**ردٌّ يفوق الصافي يُردّ ٤٠٠** برسالةٍ تسمّي الرقمين. والأسبابُ إلزامية:
سببُ الإغلاق دائماً · سببُ الاسترجاع متى رُدّ شيء · **وسببُ الاحتفاظ متى بقي
شيء** (جزئيٌّ أو صفرُ ردّ سواء). والترتيبُ مقصود — قفلٌ ⟶ إعادةُ قراءة ⟶
تحقّقٌ كامل ⟶ **ثمّ** أيُّ كتابة — فرفضٌ لأيّ سبب **يترك القاعدة كما وجدها**.

**والصلاحية للمسؤول العام وحده** (نفسُ سلطة السحب، بلا توسيع)، والبابان
`GET /api/patients/:id/case-type/:caseType/removal-preview` و
`POST .../close`، والتدقيقُ (`patient_case`/`update`) **في المعاملة نفسِها**
بسطرٍ يقول الأرقامَ والأسباب لا «أُغلقت» عارية.

**وثغرةٌ سابقة على `main` أُغلقت معها**: `deleteCaseType` كان يلفّ فحصَي
التصنيع والدفعات في `if (caseType !== "physiotherapy")` — فحالةُ علاجٍ طبيعي
**قُبض عليها مالٌ فعلاً** تُقرأ «سقالة» وتُهدَم وتُعاد دفعاتُها إلى حالةٍ
أخرى أو إلى `NULL`. ولا يُصلحه توسيعُ الوسم: **دفعةُ العلاج الطبيعي تُوسَم
بنوع الجلسة** («روبوت»، «أبر صينية») لا بعبارة «علاج طبيعي». فأُضيف حارسٌ
بالرابط `payments.case_id` **للأقسام الثلاثة** — وهو بعينه ما يحسب به الإغلاقُ
الصافي — والوسمُ على مستوى المريض يبقى للجهازين بنصّه (يمسك دفعةً لم تُربَط
بحالةٍ قطّ).

**ونقصُ إعدادٍ لا يحبس مالَ مريض**: `createJournalForPayment` — بابُ **كلّ**
دفعةِ مريض — يتخطّى القيدَ بتحذيرٍ حين لا يكون للفرع صندوقٌ في دليل الحسابات،
**والدفعةُ تُحفَظ**. فلو رفض الردُّ لنفس السبب لصار المالُ يدخل ذلك الفرعَ ولا
يخرج منه. فالنقصُ **المُعرَّف** (صندوقٌ أو حسابُ إيرادٍ غيرُ موجود) يُتخطّى
**ويُقال**: `journalPosted: false` في الردّ وسطرٌ صريح في التدقيق — **وأيُّ
فشلٍ آخر يصعد** فيُسقط المعاملةَ كلَّها (لا `try` حول الكتابة).

**والرسائلُ لا تَعِد بإعادةِ محاولةٍ لا تعمل**: الحواجزُ الثمانية أُعيدت
صياغةُ مخارجها كلُّها — خمسةٌ منها تقول «أغلِق الحالة بدل حذفها» وتسمّي ما
يبقى، والمعلَّقُ منها (مبلغٌ ينتظر · طلبُ خصمٍ لم يُحسَم) يدلّ على الطابور
الذي يُنهيه. ولم يبقَ في مسار السحب وعدُ إعادةٍ إلّا حيث تعمل فعلاً: سباقُ
٢٣٥٠٣ («حدّث الصفحة») وفشلُ جلبِ المعاينة.

**والشاشةُ زرُّ السلّة نفسُه** — لا زرَّ ثانياً: تقرأ `mode` من الخادم فتعرض
إمّا تأكيدَ السحب القديم وإمّا بطاقةَ المال وحقولَ الردّ. والصافي **يُقرأ** من
الردّ ولا يُرسَل إليه.

**بلا ترحيل** — لا عمودَ جديداً ولا جدولَ جديداً.

اختبارٌ دائم: `npm run test:case-closure` (على Postgres وعلى النقاط الحقيقية:
حسابُ الردّ خالصاً · اختيارُ البابِ في الخادم · **ردٌّ كامل/جزئيّ/صفر** ·
**تجاوزُ المقبوض بصفر كتابة** · لا يُردّ المبلغُ مرّتين · **بصمةُ التاريخ
مطابقةٌ قبل وبعد** · الوارد ينقص بالمقدار بالضبط · قيدُ اليومية بسطريه ·
**الاختفاءُ من خريطة الانتظار وقائمة الطبيب مع البقاء في ملفّ المريض** ·
الصلاحيةُ بأربعة أدوار · التزامنُ · العلاجُ الطبيعي · **وصندوقُ فرعٍ غيرُ
مُعَدٍّ يُقال ولا يحبس** · وحرّاسٌ معماريّون: لا `DELETE FROM` ولا
`UPDATE payments` ولا لمسَ كلفةٍ أو دفترِ قيودٍ في مسار الإغلاق).

### إصلاحٌ ضيّق — «عاد للشراء» لقرارٍ سابقٍ **بلا حلقة جهاز**

**العطبُ المُبلَّغ من الإنتاج**: مريضٌ عاد فعلاً ليشتري، والشاشةُ تقول «لا
توجد عملية سابقة مؤهلة للعودة للشراء لهذا المريض».

**السببُ الجذريّ، مُعادُ إنتاجُه على النقاط الحقيقية**: توقيعُ معاينةٍ **قبل
أن يفتح الاستقبالُ طلبَ جهاز** مسارٌ قائمٌ ومدعوم (٤.p — `resolveExamEpisode`:
«صفرٌ = معاينةٌ بلا جهاز (الموروث)»)، فتُولَد المتابعةُ بـ`device_episode_id
IS NULL`. ثمّ يسجّل الاستقبالُ «لم يشترِ» — والبابُ المبسَّط `/not-bought`
يردّه ٤٠٩ (ليس على مسارٍ بحلقة) فيُستعمَل البابُ الموروث `/close`. فيصير على
الملفّ **قرارُ رفضٍ حقيقيّ**. و`listEligibleReturnToPurchase` تبدأ
`FROM patient_device_episodes` وحدَها، فلا تراه أبداً: صفرُ صفوف ⟶ الرسالة.

**والإصلاحُ لا يخترع ولا يمحو**: مصدرٌ ثانٍ في الأهليّة — **المتابعةُ نفسُها**
هويّةً (`episodeId: null` + `followupId`) — وتنفيذٌ يُنشئ **طلبَ المراجعة
الكاملة وحدَه** بمرساةٍ على مستوى الاختصاص (`device_episode_id = NULL`)، وهو
الشكلُ الذي تعرفه قائمةُ عمل الطبيب أصلاً (٤.p). **ولا حلقةَ تُفتَح، ولا
متابعةَ تُعدَّل، ولا معاينةَ تُمَسّ** — مُثبَتٌ ببصمةِ الثلاثة قبل وبعد.

**وليست توسعةً عمياء** — أربعةُ شروطٍ تقابل شروطَ المسار المرساة واحداً بواحد:
القرارُ `closed_without_purchase` **وهو الأحدثُ** بين متابعات هذا (المريض،
الاختصاص) التي بلا حلقة · **ولا حلقةَ حيّةً** على مسار المعاينة لهذا الاختصاص
(وجودُها يعني أن المرساةَ هي البابُ الدقيق، فلا عرضان متنافسان) · **ولا طلبَ
مراجعةٍ معلَّقاً** لهذا (المريض، الاختصاص) — و`uq_mrr_pending_bare` هو الحارسُ
في القاعدة · والفرعُ في النطاق. وكلُّها **يُعاد قراءتُها تحت `FOR UPDATE`**
لحظةَ التنفيذ لا من قائمةٍ عُرضت قبل ثوانٍ.

**والشاشةُ تُبدِّل مفتاحَها من الحلقة إلى المتابعة** (`followupId` موجودٌ على
الطرفين)، وترسل `deviceEpisodeId` **فقط حين يوجد** — فالخادمُ يحسم بالمرساة
لا بقيمةٍ فارغة. **وقيمةٌ مشوَّهة في `deviceEpisodeId` تُردّ ٤٠٠** ولا تُقرأ
«بلا جهاز» بصمت.

**بلا ترحيل** — القاعدةُ تقبل `return_to_purchase` بلا مرساة أصلاً (٠٧٢ لا
يشترط حلقة)، ولا عمودَ ولا قيدَ جديد.

اختبارٌ دائم: قسما «١٣» و«١٤» في `npm run test:return-to-purchase` (شكلُ
العطب حيّاً من المعاينة بلا طلبٍ حتى الرسالة · الأهليّةُ تراه · الأثرُ الوحيد
طلبُ مراجعةٍ بلا مرساة · بصمةُ الحلقات والمتابعات والمعاينات مطابقةٌ قبل وبعد ·
الضغطةُ المزدوجة ٤٠٩ وطلبٌ واحد · وحلقةٌ حيّةٌ تُسقِط العرضَ عرضاً وتنفيذاً).

### إصلاحان ضيّقان أخيران

**١) الحالةُ المغلقة تُفتَح حين تُستأنَف الخدمة — بالصفّ نفسِه.**
الإغلاق يُخرج الحالةَ من سبعة قرّاءٍ يشترطون `= 'active'`، **ولم يكن بابٌ
يعيدها**: `syncPatientCases` تفحص **النوعَ لا الحالة** (`has(t)`) فلا تُنشئ
شيئاً، و`uq_patient_cases_patient_type` يمنع صفّاً ثانياً — **وهذا هو
الصواب**: الصفُّ الواحد هو ما تشير إليه المعايناتُ والحلقاتُ والدفعاتُ
وقيودُ الكلف. وفتحُ طلبِ جهازٍ كان يقفل الصفَّ **ولا يقرأ حالتَه**، فتُولَد
حلقةٌ حيّةٌ على حالةٍ مغلقة: عملٌ قائمٌ لا يراه أحد.

`server/patient_cases/reopen.ts: reopenClosedCaseTx` — كاتبٌ واحد يُنادى من
البابين اللذين يستأنفان خدمةً فعلاً: `syncPatientCases` (حين تعود الخدمةُ
مطلوبةً والصفُّ مغلق) و`startDeviceEpisodeTx` (فتحُ الطلب **هو** الاستئناف،
تحت القفل المأخوذ لتوّه). **ويغيّر الحالةَ وحدَها**: لا كلفةَ ولا
`cost_source` ولا `details` ولا `branch_id` ولا `created_at` ولا معرّفٌ
جديد — فكلُّ التاريخ يعود مربوطاً كما كان. والشرطُ `status = 'closed'` في
`UPDATE` نفسِه، فالنشطةُ لا تُلمَس ولا يتحرّك ختمُها بلا سبب.

**٢) ولا يُقرأ فرعُ الحالة `NULL` صفراً.** `patient_cases.branch_id` يقبل
`NULL`، و`Number(null)` تساوي **صفراً** لا `NaN`. فكانت دفعةُ الاسترجاع
تُكتب على `branch_id = 0`: لا فرعَ به، فيرتدّ المفتاحُ الأجنبيّ بنصّ
Postgres خامٍّ وتسقط المعاملةُ كلُّها؛ ولو وُجد فرعٌ بالرقم يوماً لكان أسوأ —
مالٌ يُنسَب صامتاً إلى فرعٍ لم يعمل فيه. و`resolveBranchId` ترتّب المصادر
ولا تخترع: فرعُ الحالة · وإلّا **فرعُ المريض** (`NOT NULL`، وهو الاحتياطُ
الذي تستعمله `startDeviceEpisodeTx` أصلاً) · وإلّا **يُردّ الطلبُ صراحةً**
بلا كتابةِ دينار.

**وحارسٌ معماريٌّ أُعيد إلى الخضرة**: `test:device-episodes` يشترط أن تمرّ
**كلُّ كتابةٍ حيّةٍ على الحلقات من طبقتها**، وكان سحبُ نوع الحالة (٤.r)
يحذف حلقاتِ السقالة من `disposal.ts` مباشرةً فيُفشله. فانتقلت الكتابةُ إلى
`device_episodes/store.ts: deleteScaffoldingEpisodesTx` وبقي **القرارُ** في
`classifyCaseDisposal` — لا توسيعَ لقائمة الاستثناءات، بل احترامُ القاعدة
التي وُضع الحارسُ لها.

اختبارٌ دائم: قسما «ن» و«س» في `npm run test:case-closure` (حالةٌ بلا فرع
تُغلَق بردٍّ ولا صفرَ في فرع الحركة بل فرعُ المريض · والمغلقةُ تُفتَح بفتح
طلبِ جهاز، بصفٍّ واحدٍ بمعرّفه وكلفتِه وتفاصيلِه وفرعِه وتاريخِه كما هي،
وتاريخُها كلُّه ما زال مربوطاً بها، وتعود إلى خريطة الانتظار، والنشطةُ لا
يتحرّك ختمُها). وكلا القسمين **يفشل بإزالة إصلاحه** — مُتحقَّقٌ بالعكس.

> **مُرجَأٌ صراحةً — عطبُ تبديل نوع الحالة في المعاينة** (سُجِّل هنا ولم
> يُصلَح بطلب المالك): حين يوقّع الطبيبُ معاينةً بنوعٍ مخالفٍ لما فتحه
> الاستقبال (٤.b «تبديل النوع لا إضافته»)، يُسحب الخيطُ القديم فتُحذَف
> حلقتُه سقالةً، وتُولَد المتابعةُ بـ`device_episode_id = NULL` — فيردّ
> `/not-bought` عليها ٤٠٩ ويُستعمَل البابُ الموروث. مُعادُ إنتاجُه حيّاً على
> النقاط الحقيقية (الاستقبالُ «أطراف» ⟵ الطبيبُ «مساند»). يستحقّ تمريرةً
> مستقلّة.

---

## 4.s «إلغاء المعاينة» — طلبٌ لم تبدأ معاينتُه يُسحَب من «معايناتي» (٢٠٢٦-٠٩-١٣)

**زرٌّ ثالث في «معايناتي» بجانب «كتابة معاينة» و«الملف».** إن لم تبدأ معاينةٌ:
يُسحَب طلبُ المراجعة وتُلغى سقالةُ الجهاز غير المستعمَلة، فيخرج الصفُّ من
قائمة الطبيب ويبقى المريضُ في سجلّ المرضى بملفّه كاملاً — **بلا سجلّ معاينةٍ
ملغاة وبلا أشباح**. فرعُ `claude/patient-event-communication-plan-y0x9e6`.
**بلا ترحيل.**

### الواقعة
الاستقبالُ يفتح «طلب جهاز» أو يضيف نوعَ حالةٍ بالخطأ، أو يعود المريضُ فلا
يريد شيئاً — فيظهر في قائمة عمل الطبيب صفٌّ لا معاينةَ فيه ولا عمل. ولم يكن
له إلّا بابان: أن **يوقّع معاينةً يعرف أنها لا لزوم لها** ليُخرجها من قائمته
(توقيعٌ يصف ما لم يحدث — وهو بعينه ما جاءت §4.b لتمنعه)، أو «إرجاع
للاستعلامات» الذي يقول «صحّح البيانات وأعد الإرسال» — وصفٌ لشيءٍ آخر.

### القاعدة: الفعلُ على **صفٍّ بعينه** لا على المريض كلِّه
هويّةُ الجهاز من المرحلة الأولى (§4.p) تحكم هنا أيضاً:
- **صفٌّ بحلقة** ⟶ تُلغى **حلقتُه هي** ويُسحَب طلبُ مراجعتها معها.
- **صفٌّ بلا حلقة** ⟶ يُسحَب **الطلبُ على مستوى الاختصاص** الذي يُبقيه.
- **وحلقةٌ أخرى منتظرةٌ على الخيط نفسِه لا تُمَسّ**: هي صفٌّ آخر بزرّه
  (مُثبَتٌ حيّاً: تُلغى الأولى فتبقى الثانية بحلقتها وطلبها).

**والإلغاءُ لا الحذف — وهذا قياسٌ لا ذوق.** أُعيد إنتاجُه حيّاً على `491b174`:
حذفُ حلقةٍ من خيطٍ **سابقٍ لحقبة المسار** (ترحيل ٠٦٥) يُرجع المريضَ إلى
القائمة فوراً عبر القاعدة القديمة (`noEpisodes` تصير صادقة) — **شبحٌ حقيقيّ**.
والحلقةُ الملغاة تمنع ذلك في كلّ تركيبة، وتبقى **سقالةً** بحكم §4.r فيسحبها
المسؤولُ كاملةً من بطاقة الحالة متى شاء. فلا شيءَ صار نهائياً.

### وما لا يفعله
**لا يُنشئ معاينةً ولا يُلغي واحدة**: صفرُ `medical_exams` وصفرُ
`medical_exam_cancellations` — المعاينةُ لم تُكتب أصلاً، وتلفيقُ سجلٍّ سريريٍّ
لإغلاق طابورٍ يكتب ماضياً لم يقع. **ولا يمسّ**: صفَّ الحالة (بابُه §4.r) ·
صفَّ المريض · ديناراً واحداً · أمرَ تصنيع · دفعةً · قيداً. مُثبَتٌ ببصمةٍ
كاملة قبل/بعد، وبحارسٍ معماريّ على مصدر الملفّ نفسِه.

### والكتابةُ بالدالّات القانونية وحدها
- الحلقةُ بـ`cancelPreManufacturingDeviceEpisodeTx` — **الدالّةُ نفسُها** التي
  تناديها نقطةُ إلغاء الجهاز، بحرّاسها كاملةً (`in_manufacturing` مرفوضة)
  وبسحبِ طلبِ مراجعتها وتقاعدِ متابعتها الحيّة (INT-06). انقسمت إلى `Tx`
  وغلافٍ رقيق **بنفس نمط `startDeviceEpisodeTx` بحرفه** — بلا تغييرٍ لأيّ
  مُستدعٍ قائم.
- الطلبُ بـ`cancelScaffoldRequestsById` — غلافٌ رقيق فوق كاتبِ «يضيف ولا
  يمحو» نفسِه (§4.r)، بـ`releaseAnchors: false` لأن لا صفَّ يُحذف.
- واختيارُ الطلبات بـ`lockSpecialtyLevelQueueRequestsTx` في
  `medical_review/store.ts` — **بنفس شروط القائمة بالحرف** (`specialtyLevelRequestSql`
  و`examSignedAfterRequestSql` و`scopeClause`)، لا نسخةٌ ثانية تنحرف عنها.

**وكلُّه في معاملةٍ واحدة تحت قفل المريض** (`pg_advisory_xact_lock(919, …)`)،
والقراراتُ كلُّها قبل أيّ كتابة: **الرفضُ يعني صفرَ كتابة، ولا نصفَ إلغاء.**

### والحُرّاس
| الحالة | الردّ |
|---|---|
| الحلقةُ لم تعد `awaiting_exam` (وُقّعت معاينتُها للتوّ) | ٤٠٩ `device_episode_stale` |
| الحلقةُ على مسار «بلا معاينة» (٠٦٥) | ٤٠٩ `no_exam_path` — بابُه التصحيحُ الإداريّ |
| طلبٌ `escalated` — **إحالةُ طبيبٍ وقعت** | ٤٠٩ `request_escalated`، ويدلّ على «إرجاع للاستعلامات» |
| لا حلقةَ ولا طلبَ على الصفّ | ٤٠٩ `nothing_to_cancel` |
| الملفُّ في السلّة (٠٦٨) | ٤٠٩ `patient_in_trash` |
| الحالةُ مغلقة (§4.r) | ٤٠٩ `case_not_active` |
| سببٌ فارغ · اختصاصٌ مجهول · معرّفُ جهازٍ مشوَّه | ٤٠٠ |

**والمشوَّهُ لا يسقط صامتاً إلى «بلا جهاز»** — قاعدةُ §4.p بحرفها.

### الصلاحية — الطابورُ نفسُه
`doctorSpecialties(userId)` **تُقرأ من القاعدة في كلّ طلب** (لا من الجلسة،
فسحبُ الاختصاص يسري فوراً)، وتشترط حساباً فعّالاً دورُه `doctor` أو يحمل
`canWriteMedicalExam`؛ **والاختصاصُ شرطٌ لا زينة** — طبيبُ العلاج الطبيعي لا
يُلغي طلبَ أطراف. ونطاقُ الفرع يُعاد فحصُه **من صفّ المريض المقفول** لا من
لقطةٍ قبل القفل. **والاستقبالُ والمحاسبُ والخبيرُ ومديرُ الفرع يُردّون ٤٠٣**:
مَن يستطيع أن **يوقّع** على هذا الصفّ يستطيع أن يقول «لا معاينةَ له» — والثاني
أقلُّ أثراً من الأوّل.

### ويُقال ما جرى لا ما وُعد به
خيطُ خدمةٍ **سابقٌ لحقبة المسار** يبقى «ينتظر معاينة» بحكم القاعدة القديمة ولو
سُحب طلبُه كلُّه — وبابُه الإدارة لا هذا الزرّ. فبدل أن تَعِد الرسالةُ بخروجٍ
لم يقع، تُسأل **`getWorklist` نفسُها** بعد الكتابة (لا نسخةٌ ثانية من قاعدة
العضوية) ويُرفَع `stillListed`، فتقول الشاشةُ الحقيقةَ في الحالتين.

### والعلاجُ الطبيعي معزولٌ — ولا زرَّ له أصلاً
`reviewServiceOfCaseType` تُرجع `null` له، فلا حلقاتِ أجهزةٍ له ولا طلباتِ
مراجعة: صفُّه يقف على القاعدة القديمة وحدها ولا شيءَ فيه يُلغى. فالشاشةُ تُخفي
الزرَّ عنه بـ`isDeviceServiceKind` **المشتركة** (لا قائمةٌ مكتوبةٌ في الواجهة)،
والخادمُ يردّه `nothing_to_cancel` طبيعياً بلا شيفرةٍ خاصّةٍ به.

### إصلاحٌ عابر
ملاحظةُ سحبِ الطلب كانت تُقرأ **«أُلغي طلبُ الجهاز: أُلغي طلبُ الجهاز: …»** —
المُنادي والكاتبُ يصدّران العبارةَ نفسَها (عيبٌ سابقٌ من §4.r، مُثبَتٌ حيّاً).
**والعبارةُ يملكها الكاتب**، فحُذف تصديرُ المُنادي.

### الترحيل
**بلا ترحيل** — لا عمودَ جديداً ولا جدولَ جديداً ولا قيدَ جديداً.

اختبارٌ دائم: `npm run test:exam-request-cancel` (حيّاً على Postgres وعلى
النقاط الحقيقية، **للأطراف والمساند معاً**: المسارُ الطبيعيّ · صفرُ معاينةٍ
وصفرُ شاهدةِ إلغاء · الصفُّ بلا حلقة · اختفاءُ الشارة الكهرمانية وتحرُّرُ
المرساة فيقبل الخادمُ طلباً جديداً بعده · رفضُ ما بدأت معاينتُه بصفر كتابة ·
عزلُ الحلقة الأخرى · بصمةُ التاريخ مطابقةً بايتاً بعد معاينةٍ وأمرٍ ودفعة ·
رفضُ الإحالة ودلالتُها على بابها · مصفوفةُ الصلاحية بستّة فاعلين واختصاصاً
وفرعاً · الضغطتان المتزامنتان · الحُرّاسُ السبعة · `stillListed` صادقاً على
خيطٍ سابقٍ للحقبة · عزلُ العلاج الطبيعي · وعقدُ الشاشة والحارسُ المعماريّ).
وكلُّ حارسٍ جديد **مُتحقَّقٌ بالعكس**: أُزيل فسقطت تأكيداتُه، ثمّ أُعيد.

### تصحيحٌ لاحق — ثغرتان دقيقتان (مراجعةٌ آلية بعد الدمج، ٢٠٢٦-٠٩-١٣)

المعماريّةُ أعلاه ثابتة؛ ثغرتان ضيّقتان في الكود المدموج، عولجتا بلا ترحيلٍ
ولا تغييرٍ في العقد.

**١) صفٌّ ظاهرٌ لا يُلغى — المريضُ المنقولُ بين فرعين.**
`lockSpecialtyLevelQueueRequestsTx` كانت ترشّح **بفرع الطلب**
(`r.branch_id`)، بينما `getWorklist` ترشّح **بفرع المريض/الحالة**
(`COALESCE(pc.branch_id, p.branch_id)`) **ولا تفلتر فرعَ الطلب إطلاقاً**.
و`transferPatientToBranch` تنقل المريضَ وحالاتِه وزياراتِه ودفعاتِه **وتترك
طلبَ المراجعة بفرعه الأصليّ عمداً** (لا يُعاد كتابةُ تاريخٍ قديم). فطلبٌ
فُتح في الفرع ١ ثمّ نُقل مريضُه إلى الفرع ٢: يرى طبيبُ الفرع ٢ الصفَّ ويُردّ
زرُّه `nothing_to_cancel` **إلى الأبد**. مُعادٌ إنتاجُه حيّاً على النقاط
الحقيقية قبل أيّ تعديل.

**والحلُّ إسقاطُ الفلترة لا إضافةُ استثناء**: نطاقُ الجلسة يُفرَض **مرّةً
واحدة على صفّ المريض المقفول** داخل `cancelExamRequest` (٤٠٣ وإلّا) قبل أن
تُنادى هذه، والاستعلامُ مقصورٌ على `patient_id` واحد — **فالعزلُ لم يضعف
بحرف** وصار حدُّ الإلغاء مطابقاً لحدّ القائمة تماماً. والمعامِلُ `branchIds`
أُزيل من توقيعها: معامِلٌ يبدو حارسَ صلاحيةٍ ولا يحرس شيئاً يضلّل مَن يقرأ.

**٢) سطرُ تدقيقٍ نوعُه لا يطابق معرّفَه.** كان يُكتب
`entityType = "medical_review_request"` مع **معرّف الحلقة** حين تُلغى حلقة —
فيُنسَب الحدثُ إلى طلبِ مراجعةٍ لا علاقةَ له به (القراءةُ ترشّح بالثنائيّة
نوع/رقم) **ويغيب عن تاريخ الحلقة التي أُلغيت فعلاً**. صار النوعُ يتبع
معرّفَه: حلقةٌ ⟶ `patient_device_episode` بمعرّفها — **نفسُ عقد نقطة إلغاء
الجهاز بحرفه** — وصفٌّ بلا حلقة ⟶ `medical_review_request` بمعرّف طلبه،
وأرقامُ الطلبات المسحوبة في الملاحظة كما تفعل تلك النقطة. **والاحتياطيُّ
بمعرّف المريض أُزيل**: كان أسوأَ الثلاثة.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:exam-request-cancel` (قسما «ع» و«ف»):
النقلُ بين فرعين للنوعين معاً حتى الإلغاء الناجح وخروج الصفّ · **وبقاءُ
العزل**: طبيبُ الفرع ٢ يُردّ ٤٠٣ عن مريض الفرع ١ بصفر كتابة · وهويّةُ سطر
التدقيق في الشكلين · **وثابتٌ عامّ**: كلُّ سطرٍ تكتبه النقطةُ يشير إلى صفٍّ
موجودٍ **من نوعه هو**، ولا سطرَ «طلب مراجعة» يحمل رقمَ حلقةٍ مُلغاة.
وكلا الإصلاحين **مُتحقَّقٌ بالعكس**: أُزيل فسقطت تأكيداتُه، ثمّ أُعيد.

---

## 4.t إتاحةُ الملفّ لفروعٍ إضافية — بديلُ «نقل المريض» (ترحيل ٠٨٠ — ٢٠٢٦-٠٩-١٤)

**فرعُ التسجيل لا يتغيّر أبداً، ولا صفٌّ تاريخيٌّ يُعاد كتابةُ فرعه. ويُمنَح
فرعٌ إضافيّ حقَّ رؤية الملفّ كاملاً، وكلُّ حركةٍ جديدة تُنسَب للفرع الذي حدثت
فيه.** فرعُ `claude/patient-branch-sharing`.

### الواقعة
`transferPatientToBranch` كان **ينقل**: يعيد كتابة `patients.branch_id` و
`visits.branch_id` و`payments.branch_id` و`patient_cases.branch_id` **كلَّها**
إلى الفرع الجديد. فمريضٌ دفع في كربلاء ثمّ جاء ذي قار كان مالُه **يُنزَع من
حسابات كربلاء بأثرٍ رجعيّ** ويُنسَب إلى فرعٍ لم يقبضه — تقريرُ كربلاء عن أمسٍ
مضى يتغيّر اليوم، والفرقُ لا يفسّره أحد.

### القاعدة
`patient_branch_access` (صفٌّ لكلّ مريض/فرعٍ إضافيّ) يقول **مَن يرى الملفَّ
أيضاً**. و`server/patients/branch_access.ts` **تعريفٌ واحد** يستورده كلُّ
قارئٍ وحارس:
- `scopeReachesPatient` / `patientVisibleToScopeSql` — فرعُ التسجيل **أو**
  فرعٌ أُتيح له. **ولا يُكتب `allowed.includes(patient.branchId)` بعد اليوم**:
  تلك تقرأ فرعَ التسجيل وحده فتحجب فرعاً مُنح الرؤيةَ صراحةً.
- `resolveActingBranchId` — **الفرعُ الذي حدثت فيه الحركة**: فرعُ جلسة
  الموظّف إن كان يصل الملفَّ، وإلّا فرعُ التسجيل (السلوكُ القائم بحرفه).
  **ورقمٌ من جسم الطلب ليس سلطةً**: يُفحَص أنه يصل الملفَّ وفي نطاق الفاعل
  وإلّا يُتجاهَل.

**والرؤيةُ كاملةٌ بلا تفرقةِ قسم** — أطرافٌ ومساندُ وعلاجٌ طبيعي سواء.

### ما يُنسَب لفرع الحركة
الدفعة · الزيارة · «خدمة جديدة» وقيدُ كلفتها · حلقةُ الجهاز · أمرُ التصنيع ·
الحالةُ الجديدة إن لم تكن. **وما لا يُمَسّ**: `patients.branch_id` · أيُّ
دفعةٍ أو زيارةٍ أو قيدِ كلفةٍ سابق · `patient_cases.branch_id` القائم · أيُّ
عمليةٍ تاريخية (أمرٌ منتهٍ أو حلقةٌ مسلَّمة أو ملغاة).

### العمليةُ المفتوحة — سؤالٌ إلزاميّ
حين توجد حلقةٌ حيّة أو أمرُ عملٍ حيّ، **الصمتُ يُردّ ٤٠٠** ولا يُقرأ «لا»:
- **«لا»** ⟶ الإتاحةُ وحدها؛ العمليةُ وخبيرُها **كما هما بالضبط**.
- **«نعم»** ⟶ تنتقل الحلقةُ الحيّةُ وأمرُ العمل الحيّ إلى الفرع الجديد،
  **ومعها قرارُ خبيرٍ إلزاميّ**: إبقاءُ الحالي، أو خبيرٌ من الفرع الجديد
  يُتحقَّق منه **تحت القفل** بـ`validateExpertForBranchTx` القانونية.
  وسجلُّ الأمر يقول ما جرى.

**والمنتهي لا يُسأل عنه ولا يُمَسّ** — الشرطُ في `UPDATE` نفسِه.

**والأمرُ الحيُّ عمليةٌ مفتوحة مهما كانت حلقتُه**: البدءُ من الحلقة وحدها كان
يُعمي عن شكلين حقيقيَّين — **صيانةُ جهازٍ مسلَّم** (حلقتُها `delivered`)،
و**أمرٌ بلا حلقةٍ إطلاقاً** (`device_episode_id IS NULL`، الموروثُ من قبل حقبة
الحلقات) — كلاهما خبيرٌ يعمل اليوم وكلاهما كان يمضي بلا سؤال. فصارت
`listOpenOperations` **اتّحادَ** الأوامر الحيّة والحلقاتِ الحيّةِ التي لا أمرَ
لها، **بلا ازدواج** (حلقةٌ حيّةٌ بأمرها الحيّ صفٌّ واحد). **والمسلَّمةُ لا
تُنقَل** حين يقول المسؤولُ «نعم»: جهازٌ سُلِّم واقعةٌ وقعت في فرعها، وأمرُ
صيانته اليوم هو الذي ينتقل وحده.

### ولا سحبَ عن فرعٍ يملك عملاً حيّاً
سحبُ الإتاحة يُردّ **٤٠٩ بصفر كتابة** حين يملك **هذا الفرعُ بعينه** أمرَ عملٍ
حيّاً أو حلقةً حيّةً لم يُفتَح أمرُها: الخبيرُ يصنع الجهازَ اليوم، وسحبُ الرؤية
يُعميه عن ملفّ مَن يصنع له. والمخرجُ إكمالُ العملية أو نقلُ مسؤوليتها إلى فرعٍ
آخر. **وعمليةٌ في فرعٍ ثالث لا تمنع** — ولا العملُ الباقي في فرع التسجيل.

### والتدقيقُ في المعاملة نفسِها
سطرُ `audit_log` للمنح وللسحب يُكتب **داخل معاملة المخزن** بـ`tx`، لا بعدها في
النقطة: سطرٌ يُكتب خارجها يبقى شاهداً على إتاحةٍ تراجعت، و`logAudit` بلا `tx`
يبتلع خطأه فتمضي الإتاحةُ بلا شاهد. **ولا `logAudit` في ملفّ النقاط إطلاقاً**
— يحرسه فحصٌ معماريّ.

### الصلاحية
**المسؤولُ العام وحده** يمنح ويسحب (قرارُ المالك: «فروع إضافية يحددها
المسؤول»). والقراءةُ لمن يصل الملفَّ أصلاً. **وتضييقٌ مقصود**: مديرُ الفرع
كان يملك «النقل»، لكنّ النقلَ يُخرج ملفّاً من فرعه، والإتاحةُ تُدخل فرعاً
غيرَه إلى ملفٍّ ليس له.

### البابُ القديم والكاتبُ الهادم
`POST /api/patients/:id/transfer` **يتقاعد ٤٠٩** بفحص صلاحيته الأصليّ حرفياً
ويدلّ على البديل. و`storage.transferPatientToBranch` **أُزيل** (لا تقاعد):
لا مُستدعيَ مشروعاً له، وإبقاءُ كاتبٍ يعيد كتابة التاريخ الماليّ يعني أنه
سيُنادى يوماً بالسهو.

### وثابتٌ قائم صُحّح معه
حارسُ «لا أمرَ في فرعٍ وحلقتُه في آخر» (٤.e) كان يقارن فرعَ الحلقة بفرع
**تسجيل المريض**. صار يقارنه **بفرع العملية** — الثابتُ المحفوظ هو اتّحادُ
فرعِ الأمر وفرعِ حلقته، لا انتماؤهما لفرع التسجيل.

### الكتابةُ في طبقتها
`moveLiveEpisodeToBranchTx` في `device_episodes/store.ts` — القرارُ في
منسّق الإتاحة والكتابةُ في طبقة الحلقات، كما يوجب الحارسُ المعماريّ في
`test:device-episodes` (نفسُ درس `deleteScaffoldingEpisodesTx` في ٤.r).

### الترحيل
**٠٨٠ فقط** — إضافيّ، idempotent: جدولٌ واحد وثلاثةُ فهارس. **بلا `UPDATE`
ولا `DELETE` ولا `DROP` ولا تعبئةٍ رجعية**: مريضٌ نُقل قبل هذا الترحيل لا
نعرف فرعَه الأصليّ (كُتب فوقه)، واختراعُ صفّ إتاحةٍ له ادّعاءُ علمٍ بالماضي.
والجدولُ **في كاسكيد `deletePatient` صراحةً** وفي قرارات `mergePatients`
(اتّحادٌ لا استبدال) كما توجب القاعدة الملزمة (القسم ٨) — **ومُختبَران حيّاً**.

### تصحيحٌ لاحق — الفرعُ المضاف يرى الصفَّ ولا يفتح الملفّ (٢٠٢٦-٠٩-١٦)

**العطبُ المُبلَّغ من الإنتاج**: موظّفُ الفرع المضاف يرى المريضَ في سجلّ
المرضى، فيضغط صفَّه ⟶ **٤٠٤**.

**والسببُ نمطٌ يمنعه `branch_access.ts` صراحةً**: `GET /api/patients/:id`
(`server/routes.ts`) كانت تقيس بـ`patient?.branchId === ctx.branchId` —
**فرعُ التسجيل وحده**. بينما `/api/patients/registry` يستعمل
`patientVisibleToScopeSql` منذ ٠٨٠ فيُظهر الصفَّ. فالبابان يقيسان بمقياسين.

**والإصلاحُ تعبيرٌ واحد** — لا صلاحيةَ تتّسع ولا تضيق: النطاقُ يُبنى بالشكل
نفسِه الذي كان يفتح البابَ قبل اليوم (**مسؤولٌ عامّ** أو **موظّفٌ بلا فرعٍ
بعد** ⟶ `null`، وإلّا فرعُ جلسته وحده) ثمّ يُسأل `scopeReachesPatient`
القانونية — فرعُ التسجيل **أو** إتاحةٌ صريحة. فالطرقُ الثلاث القديمة تبقى
بحرفها، وتُضاف الرابعةُ وحدها.

**ولا شيءَ غيرها**: حارسُ `canViewPatients` كما هو · ولا واجهةَ ولا
`shared/schema.ts` ولا ترحيل · ولا فرعَ تسجيلٍ يتغيّر (مُثبَتٌ بعد الفتح).
**والعزلُ لم يضعف**: الفرعُ نفسُه **قبل** الإتاحة يُردّ ٤٠٤، و**بعد السحب**
يعود ٤٠٤ — فالمنعُ أصلٌ والإتاحةُ استثناءٌ صريح.

اختبارٌ دائم: القسمُ «ي» (٦٥–٧٤) في `npm run test:patient-branch-access` —
سيناريو المالك بالحرف: مريضُ **ذي قار** يُتاح لـ**كربلاء**، فموظّفُ كربلاء
يراه في السجلّ **ويفتح ملفَّه الكامل** بدفعاته وزياراته ومستنداته.
**ومُتحقَّقٌ بالعكس**: أُعيد الشرطُ القديم فسقطت **ثلاثُ تأكيداتٍ وحدها**
(٦٩ ومرفقُه و٧٠) ولا شيءَ غيرها.

### تحسينٌ لاحق — قرارُ كلّ عمليةٍ مفتوحة على حدة (٢٠٢٦-٠٩-١٦)

**العمليتان المتعامدتان صارتا قرارين لكلّ عملية**: تبقى في فرعها أم تنتقل ·
ويبقى خبيرُها أم يتغيّر. **ولا قرارَ واحدٌ يُطبَّق على الجميع بعد اليوم.**

### الواقعة
كان السؤالُ واحداً للمريض كلِّه — «أتُنقَل مسؤوليةُ العمليات؟» — وخبيرٌ واحد
يُطبَّق على كلّ أمرِ عملٍ حيّ. فنقصان اثنان:
- **لا سبيلَ إلى تغيير خبير عمليةٍ تبقى في فرعها**: «لا» كانت تعني حرفياً
  «العمليةُ وخبيرُها كما هما». وخبيرٌ يُجاز أو يمرض فيُسلَّم جهازُه لزميلٍ
  **في الفرع نفسِه** واقعةٌ لا تمرّ من هذا الباب.
- **ومريضٌ بعمليتين لا يُوصَف قرارُه بنعم/لا واحدة**: طرفٌ يُصنَع في كربلاء
  ومسندٌ يُصنَع فيها أيضاً، ينتقل أحدُهما ويبقى الآخر — فإمّا نُقلت عمليةٌ لم
  يُقصَد نقلُها، وإمّا بقيت عمليةٌ كان يجب أن تنتقل.

### القاعدة: صفٌّ لكلّ عملية، بمفتاحها
`shared/branch_access_operations.ts` — **منطقٌ خالص يقرؤه الطرفان**: الشاشةُ
لتعطيل الحفظ، والخادمُ ليحرس ما يُكتب. `openOperationKey` هويّةٌ فريدة (أمرُ
العمل أوّلاً · ثمّ الحلقة · ثمّ المتابعة — وصفوفُ `listOpenOperations`
متقاطعةُ الاستبعاد بحكم استعلامها). و`resolveOperationDecisions` تحسم أو
تردّ بسببٍ يُقرأ.

**والفرعُ الذي يُتحقَّق الخبيرُ تجاهه يتبع القرار لا المنح**: المنتقلةُ ⟶
**الفرعُ المضاف**، والباقيةُ ⟶ **فرعُها هي**. ولو تحقّقنا دائماً تجاه المضاف
لصار تغييرُ خبيرِ عمليةٍ باقيةٍ في كربلاء يطلب خبيرَ ذي قار — فيُسنَد جهازُ
كربلاء إلى مَن لا يعمل فيها.

**و«إبقاء» وحدها تتخطّى التحقّق**: خبيرٌ بدأ جهازاً يواصله ولو انتقلت
مسؤوليةُ العملية إلى فرعٍ لا يعمل فيه — قرارٌ صريحٌ قائم من قبل. أمّا أن
**يُسمَّى** خبيرٌ لعمليةٍ في فرعٍ لا يصلح له فادّعاءُ إسنادٍ لا يصحّ ويُردّ،
**ولو كان هو الخبيرَ الحاليَّ نفسَه** — والعقدُ القائم محفوظٌ بحرفه.
والتسميةُ تُتحقَّق دائماً، **وتُحوَّل فقط إن اختلفت** عن الحاليّ: فلا سطرُ
«من فلان إلى فلان» لتحويلٍ لم يقع.

### والكاتبُ القانونيّ يُعاد استعماله لا يُستنسَخ
`server/manufacturing/store.ts: reassignExpert` انقسمت إلى `reassignExpertTx`
**القانونية** + غلافٍ رقيق — **نفسُ نمط `startDeviceEpisodeTx` بحرفه** (٤.ك)،
ومُستدعي المسار القديم (`POST /api/manufacturing/orders/:id/reassign`) بلا
تغيير. فإتاحةُ الملفّ تغيّر الخبيرَ **بالكاتب عينه**: قفلُ الصفّ · رفضُ
المنتهي · مقارنةُ الخبير المتوقَّع بالمقفول · وسطرُ `reassigned` بتسمية
الخبيرين. **ولا `UPDATE` ثانٍ بملاحظةٍ منزلية الصنع** كما كان.

**وواقعتان تُقرآن على حدة**: النقلُ سطرُ `status_change` الخاصُّ به، وتغييرُ
الخبير سطرُ `reassigned` الخاصُّ به — لا سطرٌ واحد يخلطهما. وعمليةٌ بقيت
وتغيّر خبيرُها تحمل `reassigned` وحده، **بلا سطر نقلٍ لنقلٍ لم يقع**.

### والمتابعةُ تنتقل مع عمليتها هي
`moveLiveFollowupsToBranchTx` اكتسبت `followupIds` اختيارية — غيابُها يُبقي
السلوكَ القديم بحرفه (كلُّ متابعةٍ حيّةٍ للمريض)، وحضورُها يقصرها على
متابعات العمليات المنتقلة. فلا يُسحَب قرارٌ تجاريٌّ معلَّق إلى فرعٍ لا يملك
عمليتَه. **والمصفوفةُ الفارغة الصريحة «لا شيء ينتقل» لا تُقرأ «الكلّ»**.

### والصيغةُ المختصرة القديمة بحرفها
`moveOpenOperations`/`keepExpert`/`newExpertUserId` تُترجَم بـ`legacyDecisionsFor`
إلى قراراتٍ لكلّ عملية فتمرّ **بالمسار الواحد نفسِه** — لا فرعَ تنفيذٍ ثانٍ في
المخزن. **ولا يُخترَع «إبقاء» من السكوت**: «نعم» بلا قرارِ خبيرٍ كانت تُردّ
٤٠٠ وتبقى تُردّ. **والجمعُ بين الصيغتين في طلبٍ واحد التباسٌ يُردّ** ولا
يُرجَّح أحدُهما بصمت.

### والشاشة
بطاقةٌ لكلّ عملية باسمها وجزئها وخبيرها الحاليّ، وفيها منتقيان: **المسؤولية**
(تنتقل/تبقى) و**الخبير** (إبقاء/آخر) — والثاني يظهر متى تقرّر مصيرُها،
منتقلةً كانت أم باقية. وقائمةُ الخبراء تُجلَب **لفرع العملية بعد قرارها**
(`useQueries` على الفروع اللازمة فعلاً، بالنقطة القائمة `/branch-access/
:branchId/experts` — **بلا مسارِ خادمٍ جديد**)، وسطرٌ يقول من أيّ فرعٍ هم
ولماذا. **وبوّابةُ الحفظ هي حاكمُ الخادم نفسُه** (`resolveOperationDecisions`)
لا شرطٌ ثانٍ ينحرف عنه. وتبديلُ الفرع المضاف يُسقط خبيرَ **المنتقلة** وحدها —
والباقيةُ خبيرُها لا علاقةَ له بذلك التبديل.

### وما لم يُمَسّ
فرعُ التسجيل · الدفعاتُ والزياراتُ وقيودُ الكلف وحالاتُ `patient_cases` ·
الأوامرُ المنتهية (`completed`/`cancelled`) والمُبطَلةُ إدارياً · الحلقاتُ
المسلَّمةُ والملغاة · قفلُ المريض (٩١٩) والمعاملةُ الواحدة · سحبُ الإتاحة
وحارسُه (٤٠٩ عن فرعٍ يملك عملاً حيّاً) · الصلاحيةُ (المسؤولُ العام وحده) ·
`postMaintenanceFee` و`assignManufacturing` · العلاجُ الطبيعي.

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد، ولا مسٍّ لترحيلٍ من ٠٠١ إلى ٠٨١.

اختبارٌ دائم: `npm run test:branch-access-operations` (٣٠ تأكيداً بلا قاعدة
بيانات — الهويّةُ، فرعُ التحقّق في الاتجاهين، تبقى+يتغيّر خبيرُها، عمليتان
بقرارين والترتيبُ لا يغيّر شيئاً، الغيابُ والتكرارُ والشبحُ والمشوَّهُ
والخبيرُ لعمليةٍ بلا أمر، والمختصرةُ القديمة ومنها «نعم» بلا قرارِ خبير)،
والقسمُ **ل** (٨٣–٩٣) في `test:patient-branch-access` حيّاً على Postgres وعلى
النقاط الحقيقية. **وكلُّ حارسٍ مُتحقَّقٌ بالعكس**: التحقّقُ تجاه الفرع المضاف
دائماً يُسقط ٣ بنودٍ خالصة و٥ بنودٍ حيّة · وتطبيقُ قرارِ الأولى على الجميع
يُسقط ٨٩ و٩٠.

### تحسينٌ لاحق — الرأسُ يقول الفروع، لا فرعَ التسجيل وحده (٢٠٢٦-٠٩-١٦)

**رأسُ ملفّ المريض كان يعرض شارةَ فرعٍ واحدة عارية** — فرعَ التسجيل، بلا
تسمية. فملفٌّ أُتيح لفرعٍ آخر لا يقول ذلك في أيّ مكانٍ يُقرأ بالمرور، ولا
يعرفه إلّا مَن فتح نافذة الإتاحة. وصار الرأسُ يقول سطرين:

```
فرع التسجيل: ذي قار
متاح أيضاً: كربلاء
```

**وكلُّ فرعٍ أُتيح له** — لا الأوّلُ وحده («كربلاء، بغداد»)، وبترتيب المنح
(`ORDER BY granted_at ASC` من الخادم) لا بفرزٍ يُعاد. ولا تظهر الشارةُ
الثانية أصلاً حين لا إتاحة، فلا يُقرأ فراغٌ حالةً.

**ولا مسارَ خادمٍ جديد ولا حقلَ ولا ترحيل**: المصدرُ `GET /api/patients/:id/
branch-access` القائم (٠٨٠) **بمفتاح استعلامه القائم نفسِه**. و`usePatientBranchAccess`
تُصدَّر من `PatientBranchAccessDialog.tsx` فيستوردها الطرفان — فالنافذةُ
تُبطل **المفتاحَ بعينه** عند المنح والسحب، **ويتحدّث الرأسُ معها بلا وصلٍ
ثانٍ**. ولو نسخ الرأسُ المفتاحَ لنفسه لبقي يقول «متاح أيضاً» بعد سحبٍ وقع.

**وحارسُ النقطة هو حارسُ فتحِ الصفحة نفسُه** (`scopeReachesPatient`)، فلا
صلاحيةَ تتّسع ولا تضيق: مَن يقرأ الملفَّ يقرأ فروعَه — والفرعُ المضاف يقرأ
الرأسَ نفسَه لا رأساً أفقر.

**وفرعُ التسجيل من صفّ المريض لا من حمولة الإتاحة** — هو مصدرُه، والصفحةُ
تملكه أصلاً؛ فلو تأخّرت الإتاحةُ أو فشلت بقي السطرُ الأوّل صحيحاً. **ولا
يظهر في «متاح أيضاً» أبداً** ولو وصل في صفوف الإتاحة (الخادمُ يمنع منحَه،
وهذا حزامٌ ثانٍ لأن الكذبةَ هنا تُقرأ ولا تُكتشَف). واسمٌ مجهولٌ يُقال
`فرع #٣` — **لا شرطةٌ عارية ولا شارةٌ فارغة**.

**والقرارُ في دالّةٍ خالصة** `client/src/pages/patient_header_branches.ts`
لا في نصّ المكوّن: المشروع بلا مشغّل DOM، فقرارٌ داخل React لا يُختبَر إلّا
بقراءة نصِّه — وقراءةُ النصّ لا تُمسك انقلاباً في المعنى يعود بصياغةٍ أخرى
(درسُ ٤.u).

**وبلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد، ولا حرفَ في الخادم (الملفُّ
الوحيد المُعدَّل تحت `server/` اختبارٌ).

اختبارٌ دائم: `npm run test:patient-header-branches` (٢٦ تأكيداً بلا قاعدة
بيانات — سيناريو المالك بالحرف، الفروعُ المتعدّدة وترتيبُها، خلطُ التسجيل
بالإتاحة، الحدودُ والترجمة، وعقدُ الشاشة ومفتاحُ الإبطال المشترك وغيابُ أيّ
مسارِ خادمٍ جديد)، والقسمُ **ك** (٧٥–٨٢) في `test:patient-branch-access`
يُمرِّر **حمولةً حيّة** من النقطة نفسِها عبر دالّة الشاشة — فيُقفَل العقدُ
بين شكل الخادم ونصّ الرأس بدل وصفِه. **وكلُّ حارسٍ مُتحقَّقٌ بالعكس**:
اشتقاقُ فرع التسجيل من الإتاحة يُسقط ١٥ بنداً خالصاً و٤ بنودٍ حيّة · ومفتاحٌ
مختلف في الإبطال يُسقط البند ٢٥ وحده · وعرضُ الأوّل وحده يُسقط ٢١ · وحذفُ
الشارة يُسقط ٢١–٢٣.

### تصحيحٌ لاحق — لوحةُ التصنيع تتبع الإتاحة (٢٠٢٦-٠٩-٢٢)

**قاعدةُ المالك الصريحة**: «الفرعُ الذي يُتاح له المريض يرى هذا المريضَ —
كلُّ مَن في الفرع — كما في فرعه الأصليّ تماماً؛ فالغرضُ كأنّه نقلٌ كامل،
والفرقُ أن الملفَّ يبقى مشتركاً كي يسجّل كلُّ مركزٍ أحداثَه عنده.»

**والواقعة**: ملفٌّ سُجّل في ذي قار أُتيح لكربلاء وأُسنِد خبيرُه إلى خبير
كربلاء **والعمليةُ بقيت في فرعها**. فرآه الخبيرُ — قائمتُه
(`/api/manufacturing/my-orders`) ترشّح **برقم الخبير بلا شرط فرع** — ولم يرَه
مديرُ كربلاء: لوحتُه (`/api/manufacturing/orders`) كانت ترشّح
`prosthetic_work_orders.branch_id ∈ فروعه` **ولا تقرأ `patient_branch_access`
إطلاقاً**، بينما يفتح ملفَّ المريض طبيعياً (`scopeReachesPatient`).

**والشرطُ صار اتّحاداً**: `branchOrPatientAccessSql` في
`server/patients/branch_access.ts` — فرعُ الصفّ في النطاق **أو** الفرعُ يصل
ملفَّ المريض. **تعريفٌ واحد** يستعمله `orderConditions` و`getOverview` معاً،
فلا تقول اللوحةُ عدداً وتقول القائمةُ غيرَه.

**وأوسعُ دائماً لا أضيق**: صفٌّ في فرعي يبقى لي ولو سُحبت الإتاحةُ عن ملفّه،
أو كان فرعُ تسجيل صاحبه `NULL`.

**وما لم يُمَسّ**: `resolveActingBranchId` ونسبةُ كلّ حركةٍ لفرعها ·
`prosthetic_work_orders.branch_id` (لا يتغيّر بالإتاحة) · المحاسبةُ والتقاريرُ
والتقريرُ اليوميّ والإحصاءاتُ بالفرع · قرارُ نقل العمليات في نافذة الإتاحة ·
**وبقيّةُ الطوابير كما هي** (المعاينة · مراجعةُ الطبيب · بانتظار الحسم ·
المبالغُ المعلَّقة) — تبقى بفرع صفّها، ولم تدخل هذه التمريرة.

**وبلا ترحيل** — شرطُ قراءةٍ فقط، ولا عمودَ ولا جدولَ ولا قيد.

#### وصفحةُ الأمر تُفتَح — نصفُ الإصلاح أُكمِل في اليوم نفسِه

**شكوى المالك بعد النشر**: «ظهر في قائمة التصنيع ولكن حين يدخل عليه يظهر له
لا يمكنك رؤية الملف». **والسببُ أن القائمةَ وحدها وُسِّعت**: `GET
/api/manufacturing/orders/:id` كان يقيس بـ`branchInScope(s, raw.branchId)` —
فرعُ الأمر وحده — فيرى مديرُ الفرع المضاف الصفَّ ويُردّ ٤٠٣ على الضغطة
(«غير مصرح لك بعرض هذا الأمر»). **وصفٌّ يُعرَض ثمّ يُردّ فتحُه أسوأُ من
صفٍّ لا يُعرَض.**

**فصار `reachesOrderPatient` بجوار `reachesPatient` القائمة**: فرعُ الأمر في
النطاق **أو** الفرعُ يصل ملفَّ المريض. **واتّحادٌ لا استبدال** — فرعُ الأمر
يبقى كافياً وحدَه، **ولا يُقرأ صفُّ المريض إلّا حين لا يكفي**. والقياسُ
بالملفّ كاملاً (فرعُ التسجيل **أو** إتاحةٌ صريحة) لا بالإتاحة وحدها: أمرٌ
فُتح في الفرع المضاف يبقى مرئياً لفرع التسجيل. وملفٌّ في السلّة يُردّ كما
كان — `storage.getPatient` تُرجع الفعّالَ وحده.

**وثلاثةُ مواضعَ لا واحد**، وإلّا بقيت الشاشةُ تُفتَح وكلُّ زرٍّ فيها يُردّ:
`GET /api/manufacturing/orders/:id` (القراءة) · `loadWritable` (تقدّمُ
المرحلة والتوقّفُ والاستئنافُ والإلغاءُ وموعدُ التسليم) · وإعادةُ إسناد
الخبير — **والخبيرُ المسنَد قد يكون من الفرع المضاف نفسِه، فمديرُه هو مَن
يعيد إسنادَه**. والمسؤولُ العام والخبيرُ المسنَد كما كانا بحرفهما.

**وما لم يُمَسّ**: قائمةُ خبراء الفرع (`/api/manufacturing/experts`) تبقى
مقصورةً على فروع المنادي — سؤالٌ عن فرعٍ لا عن ملفّ · وإنشاءُ الأمر ·
`raw.branchId` نفسُه.

اختبارٌ دائم: `npm run test:manufacturing-share` (١٤ تأكيداً حيّاً على
Postgres — وعلى **النقطة الحقيقية** عبر Express حقيقيّ لصفحة الأمر: شكلُ
العطب بطرفيه (الخبيرُ يرى والمديرُ لا) · الظهورُ بعد الإتاحة · بقاءُ فرع
التسجيل يرى · فرعُ الأمر لم يتغيّر · اللوحةُ تعدّه · **وصفحةُ الأمر تُفتَح
للفرعين ٢٠٠ وتُردّ للثالث ٤٠٣** · وسحبُ الإتاحة يُخفي الصفَّ **ويُغلق
الصفحةَ معه** بينما يفتحها فرعُ التسجيل كما كان). **ومُتحقَّقٌ بالعكس، كلُّ
نصفٍ على حدة**: إعادةُ شرط القائمة تُسقط **تأكيدين بالضبط**، وإعادةُ شرط
صفحة الأمر تُسقط **واحداً بالضبط** — شكوى المالك بعينها — ولا شيءَ غيرها.

### تصحيحٌ لاحق — مراجعةُ الحركة تتبع الإتاحة (٢٠٢٦-٠٩-٢٢)

**قاعدةُ المالك**: «أما مراجعة الحركة فهنا من كل ولابد ان تظهر للكل لانها
مهمة تخبر الفرع الجديد ماذا فعل بالقديم وتخبر القديم ماذا سيفعل في الجديد
**بشرط ان تظهر بوضوح وتقسم احداث كل فرع بمعزل عن الاخر**».

**والواقعة**: صفحةُ `/medical-review` — «مراجعة حركة مرضى الأطراف والمساند»
(§4.b) — كانت ترشّح بفرع **الطلب** (`r.branch_id`) ولا تقرأ
`patient_branch_access` إطلاقاً. فملفٌّ سُجّل في ذي قار وأُتيح لكربلاء: تُقرأ
حركتُه في ذي قار وحدها، ومديرُ كربلاء لا يعرف ماذا جرى للمريض قبل أن يصله.
وتاريخُ طلبات المراجعة في ملفّ المريض (`listReviewsForPatient`) بالعيب نفسِه —
فالفرعُ المضاف يفتح الملفَّ ولا يرى طلباً قائماً، فيرسل ثانياً عنه.

**والشرطُ صار اتّحاداً**: `branchOrPatientAccessSql` نفسُها التي أرساها إصلاحُ
لوحة التصنيع — فرعُ الصفّ في النطاق **أو** الفرعُ يصل ملفَّ المريض. تُستعمل في
قارئَين اثنين: `listPendingReviews` (الطابور) و`listReviewsForPatient` (تاريخُ
الملفّ). **وأوسعُ دائماً لا أضيق**: صفٌّ في فرعي يبقى لي ولو لم يصل ملفُّ صاحبه.

**والفصلُ شرطٌ لا زينة**: `branchName` رُفع من `ReviewCard` إلى `ReviewRow`
فصار كلُّ صفٍّ يسمّي فرعَه — بطاقاتُ الطابور كانت تعرضه أصلاً، وسطرُ تاريخِ
الملفّ في `SendToDoctorReviewDialog` اكتسبه. فلا يُقرأ طلبُ فرعين سطراً واحداً.

**وما بقي بفرعه بقرار المالك**: «ان عاين في ذي قار فبالمعاينة تبقى ذي قار
وبانتظار الحسم بالتأكيد لذي قار». فـ`pendingFullRequestsFor` (طابورُ معاينة
الطبيب) و`listPendingFullRequests` (قسمُ المعاينات المنتظرة في سطح الإشراف)
**لم يُمَسّا بحرف** — والثاني فعلُه «إرجاع للاستعلامات»، ولا يُرجِع فرعٌ طلبَ
فرعٍ آخر. وكذلك طابورُ «بانتظار الحسم» (`decision_queue_store.ts`).

**واعتمادُ الخصومات يبقى بفرع الطلب — قرارٌ فوّضه المالكُ للمساعد.** والعلّة
أنه **طابورُ فعلٍ لا شاشةُ خبر**: الاعتمادُ يحرّك مالَ الفرع الذي طلبه، ومديرٌ
يعتمد خصمَ فرعٍ آخر يقرّر في إيرادٍ ليس له. والصفُّ يُستهلَك مرّةً واحدة، فلو
رآه مديران لَحسمه غيرُ صاحبه ولم يصل صاحبَه أبداً. **والخبرُ الذي أراده المالكُ
مُتاحٌ سلفاً** («كي يعرفوا ماذا دفع في ذي قار وكم باقي يدفع في كربلاء»):
`GET /api/patients/:id` يقيس بـ`scopeReachesPatient` منذ ٢٠٢٦-٠٩-١٦، فالفرعُ
المضاف يفتح الملفَّ كاملاً بدفعاته وزياراته وكلفته والمتبقّي عليه.

**وبلا ترحيل** — شرطا قراءةٍ وحقلُ عرض، ولا عمودَ ولا جدولَ ولا قيد. **ولا
كتابةَ تتغيّر**: `r.branch_id` يبقى فرعَ الطلب كما كُتب، وحُرّاسُ الإنشاء
والقرار والإرجاع (`createReviewRequest` · `decide` · `return`) لم تُمَسّ.

> **⚠ (٢٠٢٦-٠٩-٢٤) — حارسُ الإنشاء وحده تغيّر بعدها**: صار بالإتاحة، والطلبُ
> الجديد يُنسَب لفرع الحركة (التصحيحُ التالي). **والقرارُ والإرجاعُ باقيان بفرع
> الطلب** كما وُصفا هنا.

اختبارٌ دائم: `npm run test:review-share` (٢٣ تأكيداً حيّاً على Postgres وعلى
دوالّ المخزن الحقيقية: شكلُ العطب · الظهورُ بعد الإتاحة في الطابور وفي تاريخ
الملفّ · فرعُ التسجيل لا يخسر شيئاً · اسمُ الفرع على البطاقة وعلى سطر التاريخ ·
`r.branch_id` لم يتغيّر · مريضُ فرعٍ ثالث بلا إتاحة لا يُرى · **وما بقي بفرعه**
بطرفيه · والسحبُ يُخفيها · والمسؤولُ يرى الكلّ). **ومُتحقَّقٌ بالعكس**: إعادةُ
الشرط القديم تُسقط **خمسةَ تأكيداتٍ بالضبط** — كلَّها من قسمَي الإصلاح —
وأقسامُ العزل وما-لم-يُمَسّ تبقى خضراء.

**ومعه إصلاحُ فِكستشر**: اختبارا الإتاحة كانا يكتبان `patient_code` ملفَّقاً
(`WB-T…`)، وقيدُ ترحيل ٠٥٢ شكلُه `^WB-[0-9]+$` — فيُردّان على مخطَّطٍ كامل.
والرمزُ تولّده القاعدةُ بافتراضها، فتُرك لها.

### تصحيحٌ لاحق — الفرعُ المضاف يُرسل للطبيب ويفتح طلبَ الجهاز (٢٠٢٦-٠٩-٢٤)

**شكوى المالك**: «زهراء واثق مناحي ضيول — نقلتُها أو جعلتُها متاحةً لفرع بغداد مع
ذي قار، وحين دخل موظّفُ استعلامات بغداد عليها ظهر له: خطأ غير مصرح لك بهذا
الفرع».

**والسببُ نواةٌ واحدة لا خمسة أبواب**: `createReviewRequestTx`
(`server/medical_review/store.ts`) كانت تقيس `branchIds.includes(patient.branch_id)`
— **فرعُ التسجيل وحده**، النمطُ الذي يمنعه `patients/branch_access.ts` صراحةً —
وتكتب `patient.branch_id` فرعاً للطلب. وهي البابُ الواحد لكلّ طلبِ مراجعة، فأُعيد
إنتاجُ الأثر حيّاً على النقاط الحقيقية **قبل أيّ تعديل**، بجلسة استقبالِ الفرع
المُتاح:

| الفعل | قبل الإصلاح |
|---|---|
| إرسال لمراجعة الطبيب | ٤٠٣ «غير مصرح لك بهذا الفرع» — **الرسالةُ بعينها** |
| إضافة نوع حالة (جهاز) | ٥٠٠ بالرسالة نفسِها — **بعد** أن كُتبت الحالةُ وزيارةُ علامتها |
| طلب جهاز (بمعاينة) | ٥٠٠ «تعذّر بدء الجهاز» — **بعد** أن فُتحت الحلقة |
| تسجيل زيارة جهاز | **لا ردَّ إطلاقاً** — رفضٌ غيرُ ملتقَط بعد أن كُتبت الزيارة |
| «عاد للشراء» على جهازٍ فتحه الفرعُ المُتاح | ٤٠٣ من النواة نفسِها |

ومعها ثلاثةُ أبوابٍ أخرى بفرع التسجيل وحده: قائمةُ أجهزة المريض
(`GET …/device-episodes` — فتُبنى «ما سبب حضور المريض اليوم؟» على قائمةٍ فارغة) ·
وإلغاءُ طلب الجهاز · والملخّصُ الماليّ (`GET …/financial-summary`).

**والقاعدةُ الآن**:
- **الفحصُ `scopeReachesPatient`** — فرعُ التسجيل **أو** إتاحةٌ صريحة، وهي
  القاعدةُ التي تفتح الملفَّ نفسَه. **ولا تتّسع لأحدٍ لا يصل الملفّ**: كربلاء
  (بلا إتاحة) تُردّ في الأبواب كلِّها **بصفر كتابة**.
- **والطلبُ يُنسَب لفرع الحركة** (`resolveActingBranchId` بـ`sessionBranchId`
  الجديد) — لا لفرع التسجيل. فطلبُ الجهاز وحلقتُه وزيارتُه في بغداد معاً، ويقعان
  في طوابيرٍ واحدة. **وجلسةٌ بلا فرعٍ تبقى على فرع التسجيل كما كانت.**
- **و«عاد للشراء» يتبع عمليتَه** (`operationBranchId`، يُفحَص ولا يُصدَّق):
  مديرٌ يصل الفرعين وجلستُه على بغداد يُعيد جهازَ ذي قار ⟶ الطلبُ في ذي قار مع
  حلقته، لا في فرع جلسته.
- **و«إضافة نوع حالة» تفتح الحالةَ الجديدة وعلامتَها في فرع الحركة**
  (`addPatientCaseType` اكتسبت `actingBranchId`) — وهي من قائمة ٠٨٠ أصلاً
  («الحالةُ الجديدة إن لم تكن»)، فكانت تُفتَح في ذي قار وطلبُها في بغداد، فيراها
  طبيبُ فرعٍ ليست فيه المريضة. **والحالةُ القائمة لا يتحرّك فرعُها.**
- **والقراءتان وإلغاءُ طلب الجهاز** بـ`scopeReachesPatient`، والملخّصُ الماليّ
  بنطاق `GET /api/patients/:id` بحرفه. و`canReachBranch` حُذف من أبواب الحلقات
  — مساعِدٌ بفرع التسجيل بلا منادٍ يعود بنداءٍ واحدٍ بالسهو.

**وثغرةٌ أقدم في الطريق**: `POST /api/visits` **لم يكن يفحص الفرعَ إطلاقاً** —
موظّفُ أيّ فرعٍ يُنشئ زيارةً على أيّ ملفٍّ برقمه (فتُنسَب لفرع التسجيل)، ثمّ يرفض
توجيهُ المراجعة فيبقى الطلبُ بلا ردٍّ والزيارةُ مكتوبة. صار يردّ مَن لا يصل الملفّ
٤٠٣ **قبل أيّ كتابة**. **فحصُ فرعٍ وحده** — لا صلاحيةَ دورٍ تُضاف ولا تُسحب.

**وما بقي بفرعه عمداً** (قراراتٌ على عمليةِ فرعٍ آخر، بقرار المالك في هذا القسم):
قرارُ طلب المراجعة وإرجاعُه (`decide` · `return`) · «عاد للشراء» على جهازٍ يملكه
فرعٌ آخر (مُثبَتٌ ٤٠٣) · **وإلغاءُ طلبِ جهازٍ فتحه فرعٌ آخر** (تصحيحُ Codex أدناه) ·
حسمُ متابعةِ ما بعد المعاينة · طابورُ التذكير
(`/api/follow-ups`) — متّسقٌ بقائمته وكتابته. **⚠ وكان معلوماً لم يُصلَح**: طابورُ
«مراجعة الحركة» يعرض لمدير الفرع المُتاح طلباتِ ذي قار (§4.t أعلاه) **وأزرارُ
القرار عليها ظاهرة** (`canSupervise` للجلسة لا للصفّ) — فيُردّ ضاغطُها «غير مصرح
لك بهذا الفرع». **(٢٠٢٦-٠٩-٢٥) — أُصلح بقرار المالك: البطاقةُ تبقى للعلم بلا أزرار؛
اقرأ آخرَ القسم.**

**وبلا ترحيل** — ولا حرفَ في الواجهة ولا في القاعدة.

اختبارٌ دائم: `npm run test:branch-share-review` (٣٩ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية، بثلاثة فروع: ذي قار للتسجيل · بغداد مُتاحة · كربلاء لا تصل
الملفّ — والقراءتان · والإرسالُ اليدويّ **بالرسالة بعينها** · وطلبُ الجهاز بلا
نصفِ كتابة · وإضافةُ نوع الحالة وفرعُ حالتها وعلامتها وطلبها · والزيارةُ بردٍّ لا
تعليق · والإلغاء · و«عاد للشراء» بطرفيه · وفرعُ التسجيل كما كان · ومريضٌ لم
يُتَح لبغداد يبقى محجوباً · وحارسٌ معماريّ). **ومُتحقَّقٌ بالعكس ثماني مرّات،
كلُّ آليّةٍ على حدة**: إعادةُ حارس النواة القديم تُسقط **١٦** (أفعالُ بغداد كلُّها
والحارسان؛ وبنودُ الرفض خضراء) · ونسبةُ الطلب لفرع التسجيل **٦** · وإسقاطُ
`operationBranchId` **ز٥ وحدَه** · وقائمةُ الأجهزة **٢** · والإلغاءُ **٢** ·
والملخّصُ **١** · وفرعُ الحالة الجديدة **د٥ وحدَه** · وفحصُ الزيارة **٢**.
وأربعٌ وعشرون حزمةً مجاورة خضراء (منها `medical-review` · `return-to-purchase` ·
`device-episodes` · `patient-branch-access` · `review-share` · `exam-identity` ·
`patient-trash` · `permission-authority`)، **وثماني حزمٍ بإخفاقاتٍ متطابقةٍ حرفاً
على `main`** بشجرة عملٍ حقيقية — `service-path` (٢) · `returning-patient` (١٢) ·
`pending-charge` (٨) · `departments` · `component-sale` (٣) · `maintenance-simplify`
(٣) · `followup` (٤) · `decision-queue` (٤) — مسبَقةٌ لا منه. و`tsc` ٣٥ متطابقة،
و`check:tests` نجح، و`npm run build` نجح.

#### تصحيحُ مراجعة Codex على ٤٠٧ — طلبُ الجهاز يُلغيه فرعُه، وسطرُ التدقيق في فرع الحركة

**ملاحظتان (P2) على ٤٠٧، ثبتتا ومُعادتان حيّاً قبل أيّ تعديل — وكلتاهما من كتابتي
فيه.** **بلا ترحيلٍ ولا حرفٍ في الواجهة ولا في القاعدة.**

**١) الإلغاءُ كان يقيس بالملفّ لا بالعملية.** ٤٠٧ فتح إلغاءَ طلب الجهاز لكلّ فرعٍ
يصل الملفّ (`scopeReachesPatient`) **ولم يسأل لمن الطلبُ نفسُه**. فمريضٌ مسجَّلٌ في
ذي قار أُتيح لبغداد **وكربلاء معاً**، فتحت له بغداد طلبَ جهاز ⟵ ألغته كربلاء:
**٢٠٠، وسُحب معه طلبُ مراجعته المعلَّق**. وألغاه فرعُ التسجيل كذلك (٢٠٠).

**والقاعدةُ الآن قاعدةُ «عاد للشراء» بحرفها** (`executeReturnToPurchase`): **العمليةُ
يُلغيها فرعُها**. `cancelPreManufacturingDeviceEpisodeTx` اكتسبت `actorBranchScope`
— حين يُمرَّر يُشترط أن يكون **فرعُ هذه الحلقة بعينها** في نطاق الفاعل، وإلّا ٤٠٣
تسمّي الفرعَ المالك («طلبُ الجهاز هذا يخصّ فرع بغداد — يُلغيه ذلك الفرع») **بصفر
كتابة**. و`null` = المسؤولُ العام. والنقطةُ تمرّره من الجلسة، **وبابُها المبكّر على
الملفّ باقٍ كما هو** (فرعٌ بلا إتاحةٍ يُردّ قبله برسالته).
- **تحت قفل الحلقة لا قبله**: نقلُ مسؤولية العملية إلى فرعٍ آخر
  (`moveLiveEpisodeToBranchTx`) يكتب على صفّ الحلقة نفسِه فيتسلسل معنا، فلا يُحكَم
  على فرعٍ قُرئ قبل أن يتغيّر. **وقبل فحص الحالة**: مَن لا يملك العمليةَ يُقال له
  ذلك، لا تفاصيلُ حالِ عمليةٍ ليست له.
- **وفرعُ العملية** = فرعُ الحلقة، وإلّا فرعُ خيطها، وإلّا فرعُ التسجيل — ترتيبُ
  `startDeviceEpisodeTx` نفسُه حين يكتبه. فطلبٌ موروثٌ بلا فرعٍ مكتوب يُلغيه فرعُ
  خيطه، **لا يُحبَس عن الجميع إلّا المسؤول**.
- **وفي الاتّجاهين**: بغداد لا تُلغي طلبَ كربلاء، وفرعُ التسجيل لا يُلغي طلبَ بغداد.
  ومديرٌ نطاقُه يشمل الفرعَ المالك يُلغيه.
- **ومسارُ الطبيب لم يُمَسّ**: زرُّ «إلغاء المعاينة» في «معايناتي» (§4.s) لا يمرّر
  `actorBranchScope`، فيبقى على حكمه هو تحت قفله.

**٢) سطرُ تدقيق «إضافة نوع حالة» كان يكتب فرعَ التسجيل.** ٤٠٧ جعل الحالةَ الجديدة
وعلامتَها وطلبَها في فرع الحركة **وترك سطرَ `audit_log` على `patient.branchId`**،
فيُنسَب فعلُ بغداد إلى ذي قار في تقارير التدقيق. فصار فرعُ الحركة يُحسَب **مرّةً
واحدة** وتُكتب منه الحالةُ وسطرا التدقيق معاً، باحتياط الكاتب نفسِه
(`actingBranchId ?? patient.branchId`). **وكانت الشاذّةَ الوحيدة** (مفحوص): تدقيقُ
«خدمة جديدة» والدفعة والزيارة والحلقة وطلب المراجعة يكتب فرعَ صفّه أصلاً.

**وهذا ليس جواباً عن سؤال «مراجعة الحركة» المعلَّق**: الإلغاءُ صار على القاعدة
**القائمة اليوم** في `decide` و`return` و«عاد للشراء» (قرارُ العملية لفرعها). وإن
اختار المالكُ أن يقرّر الفرعُ المُتاحُ في عمليات غيره، تُرخى هذه معها.

اختبارٌ دائم: القسمان **ط** و**ي** في `npm run test:branch-share-review` (٦٢ تأكيداً
بعد الإضافة، مقابل ٣٩ قبلها — شكلُ الملاحظة بعينه بمريضٍ مُتاحٍ لفرعين · والرسالةُ
تسمّي المالك · وصفرُ كتابةٍ ببصمة الحلقة وطلبها وتدقيقها · وفرعُ التسجيل · والاتّجاهان ·
والمدير · **والسباقُ الحتميّ**: معاملةٌ تنقل الحلقةَ إلى كربلاء وتمسك صفَّها، وإلغاءُ
بغداد يقف على القفل فعلاً ثمّ يُحكَم بالفرع الذي التزم · والطلبُ الموروثُ بلا فرع ·
وسطرُ التدقيق في بغداد وفي ذي قار · وحارسان معماريّان). **ومُتحقَّقٌ بالعكس خمسَ
مرّات، كلُّ آليّةٍ على حدة**: إبطالُ الفحص في المخزن يُسقط **١٢** كلَّها من القسم ط ·
وإسقاطُ تمريره من النقطة **١٣** (ومعها الحارسُ ح٧) · **ونقلُه قبل القفل يُسقط ط١٤
وط١٥ وحدهما** — فالقفلُ شرطٌ لا زينة · وإسقاطُ احتياط الفرع **ط١٧ وحدَه** · وإعادةُ
التدقيق إلى فرع التسجيل **ح٦ وي١ وي٢**. واثنتا عشرةَ حزمةً مجاورة خضراء (منها
`device-episodes` بحارسه المعماريّ · `case-disposal` · `exam-request-cancel` ·
`exam-identity` · `episode-integration` · `medical-review` · `review-share` ·
`return-to-purchase` · `patient-branch-access` · `maintenance-service`)، **وأربعٌ
بإخفاقاتٍ متطابقةٍ حرفاً على `main`** بشجرة عملٍ حقيقية — `episode-flow` (١١) ·
`service-path` (٢) · `departments` · `returning-patient` (١٢) — مسبَقةٌ لا منه. و`tsc`
٣٥ متطابقةٌ سطراً بسطر، و`check:tests` نجح، و`npm run build` نجح.

### تراجعٌ عن ٤٠٩ و٤١٠ — العمليةُ في فرعها، ولا ينتقل شيءٌ بين الفروع (٢٠٢٦-٠٩-٢٤)

**حكمُ المالك**: «المريضُ الذي يتمّ مشاركتُه مع فرعٍ آخر لا يُشترَط نقلُ ملفّه،
لأنه مثلاً بدّل قالباً في بغداد بعد أن اشترى من ذي قار، ثمّ بعد بغداد عاد لذي قار
واشترى قدماً وهكذا… **إن اشترى أو دفع مريضٌ طرفاً أو جزءاً أو مسنداً في فرع، فعند
فتح المريض لفرعٍ آخر ممنوعٌ أن تُنقَل أموالُه من فرعه القديم إطلاقاً** — وإلّا صار
قصورٌ في قاصة الفرع الذي استلم والفرع الجديد، لأن الأموال فعلياً في قاصة والحسابَ
إلكترونياً في فرعٍ آخر.» ومعه: «ممنوعٌ التوسّعُ والافتراضُ والتخمين».

**وما فعله الطلبان من عندي — وأُرجع كلُّه**:
- نقلُ قرار ما بعد المعاينة وجهازِه إلى فرع الخبير المختار (حدثُ `sale_branch_moved`).
- قائمةُ خبراء البيع بملفّ المريض (`?patientId=`) بدل فرع القرار.
- أبوابُ القرار لكلّ فرعٍ يصل الملفّ بدل فرع القرار وحده.
- نسبةُ المعاينة بلا جهاز لفرع مَن أرسل المريض.
- وما بناه ٤١٠ فوقها (فرعُ البيع على طلب الخصم · إذنُ التصحيح الإداريّ بفرع العملية ·
  وسباقُ سحب الإتاحة مع البيع · والنافذةُ الموروثة).

**والإرجاعُ بحرفه لا بالتقدير**: `git revert` للطلبين، ثمّ مقارنةُ الشجرة بما قبل ٤٠٩
(`e76cc64`) — **متطابقةٌ تماماً** قبل أيّ سطرٍ جديد.

**فالقاعدةُ كما اتُّفق عليها**: كلُّ فرعٍ يسجّل عملياتِه عنده — العمليةُ في فرع
الجهاز الذي فُتحت فيه، وأمرُها وكلفتُها ودفعتُها هناك — والقرارُ لفرعه («إن عاين في
ذي قار… بانتظار الحسم لذي قار»)، وقائمةُ خبرائه خبراءُ ذلك الفرع. **ولا تنتقل متابعةٌ
ولا حلقةٌ ولا دفعةٌ ولا قيدُ كلفة من فرعٍ إلى آخر** إلّا بقرارٍ صريح من المسؤول في نافذة
الإتاحة (السؤالُ القائم أعلاه).

**وأمران بعد الإرجاع، كلاهما لا ينقل شيئاً**:
1. **جهازٌ فتحه الفرعُ المُتاح يُباع في فرعه.** `confirmPurchase` كانت تنادي
   `assignManufacturing` بلا `actingBranchId`، فيُفتَح الأمرُ بفرع التسجيل؛ وجهازٌ فتحته
   بغداد (٤٠٧) حلقتُه في بغداد، فيصطدم بحارس «لا أمرَ في فرعٍ وحلقتُه في آخر» ويُرمى
   `DeviceEpisodeError` من معالجٍ غير متزامن — **فلا يعود ردٌّ إطلاقاً** (مُعادٌ حيّاً على
   `e76cc64`). فصار فرعُ العملية **فرعَ الجهاز نفسِه** كما فُتح: الأمرُ والكلفةُ فيه،
   والدفعةُ في فرع القرار — وهو فرعُ الجهاز على مسار المعاينة. **وبلا جهاز** تُفتَح
   الحلقةُ الأولى في فرع الحالة كما كانت، فلا يتغيّر شيء. ومعه `fail()` يترجم
   `DeviceEpisodeError` و`ActiveAssignmentError`، و«إتمام البيع» يردّ ٥٠٠ عربياً لما لا
   يُتوقَّع بدل الرمي.
2. **نافذةُ المعاينة تفتح على ما سجّله الاستقبال** (§4.b، «ومصدرُ التعبئة»).

**وعنوانُ `sale_branch_moved` باقٍ للقراءة وحدها**: ٤٠٩ عمل على الإنتاج ساعات، فما
كُتب فيها من هذا الحدث يُقرأ بعنوانه لا «إجراءً مسجَّلاً» — **ولا شيفرةَ تكتبه بعد اليوم**.
وهل كُتب منه شيءٌ أصلاً يقوله `branch_share_facts.sql`.

**وزهراء وأيوب ينتظران البيانات**: القائمةُ خبراءُ فرع القرار (`getExpertsForBranch`:
فعّال، دورُه خبير أو `can_work_as_expert`، والفرعُ في `branch_ids` أو `branch_id`).
والمالكُ رأى عناد (بغداد) ولم يرَ أيوب — فالفرقُ في حسابَيهما أو في فرع القرار،
والاستعلامُ يقوله. **وما رآه استقبالُ بغداد** — نافذةٌ بلا خبراء — هو بعينه ما يقع حين
يكون القرارُ لفرعٍ آخر: الزرُّ يظهر بالدور، والقائمةُ تُردّ ٤٠٣ وتُبتلَع (§0، ينتظر القرار).

**بلا ترحيلٍ ولا حرفٍ في القاعدة.**

اختبارٌ دائم: `npm run test:shared-device-sale` (٤٨ تأكيداً حيّاً على Postgres وعلى النقاط
الحقيقية — **أ** نافذةُ المعاينة: البابُ القديم يردّ الطبيبين، وبابُ المعاينة يصلهما
بنوع البتر ونوع المسند وجهته، وسبعةُ حقولٍ لا غير، والنطاقُ باقٍ · **ب** جهازٌ فتحته
بغداد يُباع فيها: الأمرُ بأيوب والدفعةُ والكلفةُ في بغداد، **ولا ينتقل شيء**، وجهازُ فرع
التسجيل كما كان · **ج** ما أُرجع: بغدادُ لا تحسم قرارَ ذي قار بصفر كتابة، والقائمةُ
بفرع القرار، ولا قائمةَ بملفّ المريض · **د مثالُ المالك بعينه**: اشترى في ذي قار ودفع،
أُتيح لبغداد بجواب «لا» بالباب الحقيقيّ **فلا يتحرّك صفّ**، بدّل قالباً في بغداد بأيوب،
ثمّ اشترى قدماً في ذي قار — **دفعةُ ذي قار الأولى بحرفها**، وكلُّ دفعةٍ وكلفةٍ وأمرٍ في
فرعه، **وقاصةُ ذي قار ٦٠٠ ألف وقاصةُ بغداد ٢٠٠ ألف**). **ومُتحقَّقٌ بالعكس أربعَ
مرّات**: إسقاطُ فرع الجهاز يُسقط **ب٣–ب٧** (ب٣ بـ٤٠٩ مقولة) · ومعه إرجاعُ `fail()`
كما كان يُسقطها **بلا ردٍّ إطلاقاً** — العطبُ بعينه · وإرجاعُ النافذة إلى ملفّ المريض
يُسقط **أ٩–أ١١** · وحذفُ `registration` من الباب يُسقط **أ٤ وأ٦ وأ٧**. **والقسمُ د أخضرُ
في الأربع** — فهو يقيس النموذجَ المتّفق عليه كما كان قبل ٤٠٩. وتسعٌ وخمسون حزمةً مجاورة
**بمجموعةِ فشلٍ متطابقةٍ حرفاً** مع ما قبل ٤٠٩ بشجرة عملٍ حقيقية، **والستّون**
(`test:exam-identity-ui`) تغيّر فيها البندُ ٤ عن عمد (§4.b). و`tsc` ٣٥ متطابقةٌ سطراً
بسطر، و`check:tests` نجح، و`npm run build` نجح.

### القرارُ لمن أرسل المريض، والمنتظرُ لفرعه وحده (٢٠٢٦-٠٩-٢٥)

**قرارُ المالك**: «إذا تمّ تسجيلُ المريض في فرعٍ مثلاً ذي قار… وحين أعاينه فمنطقياً يظهر
لذي قار… بعدها مثلاً أتحته لبغداد أو كربلاء فهنا يكون محسوماً ولن يظهر قرارٌ لأيّ
فرع… ولكن إن احتاج معاينةً إضافية أو تمّ إرسالُه للمعاينة وهو متاحٌ لعدّة أفرع… فيجب
أن يعود القرارُ لهذا الفرع حصراً، فلا منطقَ من رؤية فرعٍ آخر القرار، لأن الفرعَ الآخر
لم ولن يعرف أن اشترى المريضُ أو لم يشترِ». **واعتمد القراءةَ قبل التنفيذ** («نعم»).

**والفحصُ قبل التعديل** — حيٌّ على النقاط الحقيقية: ملفٌّ في ذي قار أُتيح لبغداد،
أرسلته بغدادُ بـ«إرسال لمراجعة الطبيب» (طلبُها في بغداد منذ ٤٠٧)، ثمّ عُويِن بلا جهاز:

| | قبل |
|---|---|
| فرعُ القرار | ذي قار — فرعُ الحالة |
| ذي قار تحسمه | ٢٠٠ — وتبيع بخبيرها، فالأمرُ والكلفةُ عندها |
| بغدادُ المُرسِلة تحسمه | ٤٠٣ «غير مصرح لك بهذا الفرع» |
| قرارُ ذي قار المنتظر في ملفّ المريض | تراه بغدادُ |

**والمعاينةُ بجهاز كانت على القاعدة أصلاً**: طلبُ الجهاز هو الإرسال، وقرارُه لفرع
الجهاز منذ ٤٠٧.

**١) معاينةٌ بلا جهاز ⟵ القرارُ لفرع أوّل طلبٍ أغلقته** (`createExam`؛
و`closeRequestsAwaitingExam` صارت تُرجع ما أغلقته). و«الأوّلُ» ترتيبُ
`pendingFullRequestsFor` بحرفه (`created_at ASC`): **صفُّ «معايناتي» بلا جهاز يحمل أقدمَ
طلبٍ على مستوى الاختصاص وسببَ زيارته**، فهو الطلبُ الذي فتح منه الطبيبُ المعاينة. وشرطٌ
واحد: **أن يصل ذلك الفرعُ الملفَّ الآن** — فرعٌ أرسل ثمّ سُحبت إتاحتُه لا يفتح الملفَّ
أصلاً، فيبقى القرارُ لفرع الحالة. **ومعاينةٌ لم يُرسَل لها أحد على فرع الحالة كما
كانت. والمعاينةُ نفسُها على فرعها** — القرارُ وحده ما يتغيّر، ففحصُ تطابق الإرسال
(٠٧٤) لم يُمَسّ.

**ومعه ما يلزمه ليُباع في فرعه — وإلّا صار القرارُ لبغداد والبيعُ في ذي قار**: قرارٌ
بلا جهاز يُحسَم من الباب الموروث (`/confirm-purchase` و`/expert`)، وكان:
- **يتحقّق من الخبير بفرع التسجيل**، والبطاقةُ تعرض خبراءَ فرع القرار (`activeBranchId =
  f.branchId`) — فكان خبيرُ بغداد يُردّ ٤٠٠. صار بفرع القرار (`expertBranchOf`)، **وهو
  ما يفعله مسارُ المعاينة أصلاً** (`setCommercialFields` بـ`cur.branchId`).
- **يفتح الجهازَ الأوّل في فرع الحالة** (`ensureFirstDeviceEpisodeForSale`)، فيقع الأمرُ
  والكلفةُ في ذي قار. صار في فرع القرار.

**وكلاهما بشرط الوصول نفسِه** (`decisionBranchReachingPatient`): قرارٌ لا يصل فرعُه
الملفَّ يبقى على السلوك القائم بحرفه — فلا يتغيّر شيءٌ لصفٍّ قديمٍ خرج فرعُه من الملفّ.
**وقرارٌ نُقل بنافذة الإتاحة** («نعم» عن عمليته المفتوحة) صار يُباع في فرعه الجديد
كذلك — وهو معنى نقل المسؤولية.

**٢) القرارُ المنتظر في ملفّ المريض لفرعه وللمسؤول وحدهما**
(`GET /api/followups/patient/:patientId`): المنتظرُ لمن يصل فرعَه — `canReachBranch`،
**شرطُ `loadInScope` بعينه**، فما لا يُعرَض هو ما كان سيُردّ ٤٠٣ عند الضغط — **والمحسومُ
للجميع تاريخاً كما كان**. ومديرٌ نطاقُه يشمل الفرعَ يراه. **وبه زال بندٌ كان ينتظر
القرار**: زرُّ «إتمام البيع» لاستقبالِ فرعٍ مُتاحٍ على قرار فرعٍ آخر — القرارُ نفسُه لم
يعد يصله. (والبطاقةُ هي المستهلكُ الوحيد لهذه النقطة، وفهرسُ قدرات المساعد يناديها بجلسة
السائل فيتبعها.)

**وما لم يُمَسّ**: فرعُ التسجيل وفرعُ الحالة · المعاينةُ وفرعُها · طلبُ الجهاز وقرارُه ·
الدفعاتُ وكلُّ صفٍّ ماليٍّ قائم · طابورُ «بانتظار الحسم» (بفرع القرار أصلاً) ·
الصلاحيات · القاعدةُ والترحيلات.

**بلا ترحيلٍ ولا حرفٍ في الواجهة ولا في القاعدة.**

اختبارٌ دائم: `npm run test:decision-branch` (٦٤ تأكيداً حيّاً على Postgres وعلى النقاط
الحقيقية — **أ** بلا إتاحة ⟵ فرعُ التسجيل · **ب** سيناريو المالك: بغدادُ ترسل، القرارُ
لها، ذي قار لا تراه ولا تحسمه بصفر كتابة، وخبيرُ ذي قار يُردّ، وبغدادُ تبيع بخبيرها
**فالأمرُ والجهازُ والكلفةُ في بغداد**، والملفُّ لم يُنقَل، والمحسومُ تاريخٌ لذي قار،
ومعه بابُ اختيار الخبير · **ج** قرارُ ذي قار لا تراه بغدادُ، ويراه فرعُه والمسؤولُ ومديرُ
الفرعين، وبعد الحسم تقرؤه بغداد · **د** ما لا يتغيّر: إرسالُ فرع التسجيل · إتاحةٌ سُحبت
قبل المعاينة · طلبُ الجهاز ولو أرسلت بغدادُ معه · معاينةٌ بلا إرسال · **هـ** فرعان
أرسلا ⟵ أوّلُهما، بالترتيبين · **و** قرارٌ نقله المسؤولُ بـ«نعم» يُباع في فرعه الجديد).
**ومُتحقَّقٌ بالعكس سبعَ مرّات، كلُّ آليّةٍ على حدة**: إرجاعُ فرع القرار يُسقط **١٧**
(القسمُ ب وهـ٦ لبغداد) · وإسقاطُ شرط الوصول **د٥ وحدَه** · وعكسُ الترتيب **هـ٦ مرّتين** ·
وبابُ الاختيار **ب٢٢–ب٢٤** · وتحقّقُ «اشترى» **ثمانية** (ب١٠–ب١٣ وب٢٣–ب٢٤ وو٣–و٤) ·
وفرعُ الجهاز **خمسة** (ب١٣–ب١٥ وب٢٤ وو٤) · والإخفاءُ **ب٦ وج٢**. **والقسمُ و يسقط على
`main` كذلك** — قرارٌ نُقل بنافذة الإتاحة كان لا يُباع بخبير فرعه الجديد (٤٠٠)، فهو من
الآلية نفسِها لا بابٌ ثانٍ. وإحدى وأربعون حزمةً مجاورة: إحدى وثلاثون خضراء، وعشرٌ
**بسطورِ فشلٍ متطابقةٍ حرفاً على `main`** بشجرة عملٍ حقيقية (مسجَّلةٌ في آخر §4.ai). و`tsc`
٣٥ متطابقةٌ سطراً بسطر، و`check:tests` نجح، و`npm run build` نجح.

### بطاقةُ فرعٍ آخر في «مراجعة الحركة» تُقرأ ولا يُؤشَّر عليها (٢٠٢٦-٠٩-٢٥)

**قرارُ المالك**: «فيجب ان ترى الفروع المشتركة لمريض واحد ترى كل شيء يحدث لهذا المريض
سواء بفرعهم ام بغيره ليعلموا مافعل اما القرارات فبالتاكيد تحصر لصاحب الفرع هو من يقرر».

**والواقعة (مُعادةٌ حيّاً قبل أيّ تعديل)**: الطابورُ يعرض لكلّ فرعٍ يصل الملفَّ حركاتِه
كلَّها (قرارُ ٢٢/٩ أعلاه)، **والتأشيرُ لفرع الطلب وحده منذ نشأته**
(`decideReviewRequest` · `returnFullRequestToReception`). لكنّ الشاشةَ كانت تُظهر أزرارَ
التأشير بـ`canSupervise` — قدرةِ المستخدم العامّة — لا بفرع البطاقة. فمديرُ بغداد يرى
بطاقةَ ذي قار بأزرارها، ويضغط «تمت المراجعة» أو «إرجاع للاستعلامات» (وطبيبُ بغداد
«يتطلّب معاينة كاملة») فيُردّ «غير مصرح لك بهذا الفرع». **بلا كتابة** — زرٌّ مضلِّلٌ لا
فسادُ بيانات.

**والقاعدةُ الآن**:
- **`requestBranchInScope(scope, branchId)`** (`shared/medical_review.ts`) — **الحارسُ
  نفسُه** يُقرأ منه في الموضعين، **ومنه وحده** يُرسل الطابورُ `branchInScope` على كلّ
  صفّ. فلا يُعرَض زرٌّ يردّه الخادم ولا يُخفى زرٌّ يقبله. **ومطابقٌ للحارس القديم
  بحرفه** (مُثبَتٌ على ثلاثين حالة) — لا صلاحيةَ تتّسع ولا تضيق.
- **`reviewCardMode`** يقرّر منطقةَ الفعل خالصاً: `act` (الأزرارُ كما كانت) ·
  `other_branch` (سطرُ «هذه الحركة لفرع ذي قار — يراجعها ذلك الفرع» و«فتح ملف المريض»
  وحده) · `read_only` (كما كان لمن لا يملك الإشراف). **وغيابُ العلَم «لا»**: زرٌّ مخفيٌّ
  أهونُ من زرٍّ يُردّ.
- **ولماذا إخفاءٌ لا فتحُ التأشير لبغداد**: التأشيرُ يُخرج البطاقةَ من الطابور عند
  الجميع، فتأشيرُ بغداد كان سيُخفي حركةَ ذي قار عن مديرها قبل أن يراها.
- **ودليلُ الصفحة الذي يقرؤه المساعد** يقول الحالاتِ الثلاث، وسطرُ الفرع الآخر فيه من
  الدالّة نفسِها.

**وما لم يُمَسّ**: البطاقاتُ الظاهرة (للعلم كما قرّر المالك) · قسمُ «طلبات معاينة
كاملة بانتظار الطبيب» (بفرعك أصلاً) · حكمُ الخادم في كلّ تأشير · الصلاحيات · القاعدة —
**بلا ترحيل**.

اختبارٌ دائم: `npm run test:review-card-actions` (٢٠ تأكيداً حيّاً على النقاط الحقيقية:
البطاقةُ تُرى وتسمّي فرعَها · والأزرارُ الثلاثة تُردّ من غير فرعها بصفر كتابة · والعلَمُ
لستّة مشاهدين **يتبع فرعَ الطلب لا فرعَ التسجيل** · وحيث يقول «نعم» يقبل الخادمُ وحيث
يقول «لا» يردّ · والعزلُ كما كان) و`npm run test:review-card-ui` (خالصاً: الحارسُ مطابقٌ
للقديم · وقرارُ البطاقة · **والثابت: زرٌّ يُعرَض ⟺ الخادمُ يقبل ضغطَه، على ستّين حالة** ·
وعقدُ الشاشة والخادم). **ومُتحقَّقٌ بالعكس ثماني مرّات، كلُّ آليّةٍ على حدة**: العلَمُ
دائماً «نعم» يُسقط **٣ حيّة وواحداً خالصاً** · وحذفُه **٦ وواحداً** · والشرطُ القديم في
الشاشة **٧** · والقرارُ يتجاهل الفرع **٣** · وتوسيعُ الحارس **٣** · ونسخةٌ ثانية منه في
المخزن **٢** · وزرٌّ على بطاقة الفرع الآخر **٢** · وحذفُ بند الدليل **٢** — ولا شيءَ خارج
آليّتِه. وعشرون حزمةً (منها الجديدتان): تسعَ عشرةَ خضراء، و`pending-charge` بثمانيةِ
إخفاقاتٍ متطابقةٍ حرفاً على `main` بشجرة عملٍ حقيقية. و`tsc` ٣٥ متطابقةٌ سطراً بسطر،
و`check:tests` نجح، و`npm run build` نجح.

اختبارٌ دائم: `npm run test:patient-branch-access` (٧٥ تأكيداً حيّاً على
Postgres وعلى النقاط الحقيقية — **سيناريو المالك بالحرف**: دفعٌ في كربلاء ⟶
إتاحةٌ لذي قار ⟶ خدمةٌ ودفعةٌ في ذي قار ⟶ **حساباتُ كربلاء كما هي بالضبط**؛
ومعه: الفرعُ من الجلسة لا من جسم الطلب · تقاعدُ «نقل المريض» · مصفوفةُ
الصلاحية · رفضُ فرع التسجيل نفسِه · سؤالُ العملية المفتوحة بأجوبته الثلاثة ·
خبيرٌ من فرعٍ خاطئ يُردّ بصفر كتابة · السحبُ لا يمسّ صفّاً مالياً · وحذفُ
المريض الكامل مع صفّ الإتاحة · وعقدُ الشاشة · **وثلاثةُ أقسامٍ للثوابت
الثلاثة**: التدقيقُ في المعاملة — **بفاعلٍ لا وجود له يُفشل سطرَ التدقيق نفسَه
(مفتاحٌ أجنبيّ) فيسقط المنحُ كلُّه ويبقى السحبُ بلا أثر** · والأمرُ الحيُّ على
حلقةٍ مسلَّمة وبلا حلقةٍ إطلاقاً ⟶ سؤالٌ إلزاميّ ونقلٌ للأمر وحده، بلا ازدواج،
والمنتهي لا يُسأل عنه · والسحبُ عن فرعٍ يملك عملاً حيّاً ⟶ ٤٠٩ بصفر كتابة،
وعملٌ في فرعٍ آخر لا يمنع، واكتمالُ العملية يفكّ المنع). **وكلُّ حارسٍ من
الثلاثة مُتحقَّقٌ بالعكس**: أُزيل فسقطت تأكيداتُه وحدها، ثمّ أُعيد.

---

## 4.u العمليةُ المعلَّقة تُختار، لا تُنتقى صامتاً (٢٠٢٦-٠٩-١٥)

**استئنافُ بيعٍ بلا معاينة صار يسأل حين تتعدّد العملياتُ المعلَّقة.** فرعُ
`claude/no-exam-sale-resume-choice`. **بلا ترحيل، وبلا حرفٍ يتغيّر في الخادم
أو البيع أو المحاسبة** — قرارُ شاشةٍ وحده.

### الواقعة (أُعيد إنتاجُها على `2e8e47f` قبل أيّ تعديل)
ترحيلُ ٠٧٣ أسقط `uq_pde_case_open`، فصار للخيط الواحد أن يحمل **أكثر من
حلقةٍ مفتوحة** معاً. وبقيت `resumableNoExamSale` على `.find()` — أوّلِ
مطابقة — كما كُتبت يوم كان الفهرسُ يضمن واحدةً لا غير. فمريضٌ له عمليتان
معلَّقتان لنفس القسم بجزأين مختلفين (قالبٌ #١ وركبةٌ #٢) كانت الشاشةُ
**تستأنف الأولى صامتاً**: يأتي الموظّفُ ليُكمِل الركبة فيُفتَح النموذجُ
على القالب، فيُسجَّل السعرُ والخبيرُ وأمرُ العمل على **العملية الخطأ**.

**و«الأولى» لم تكن قراراً أصلاً** — بل ترتيبَ وصول الصفوف من الخادم: نفسُ
المدخلات معكوسةَ الترتيب كانت تُنتج استئنافاً مختلفاً. مُثبَتٌ حيّاً:
`[socket#1, knee#2]` ⟶ استؤنف القالب، والمعكوسُ ⟶ استؤنفت الركبة.

### القاعدة — نفسُ قاعدة سؤال الإلحاق حرفاً بحرف
`resumableNoExamSales` (جمعاً) تُرجع **كلَّ** المُرشَّحين مرتَّبين
بالتسلسل، و`resolveResumeTarget` تحسم:

| العددُ | السلوك |
|---|---|
| **صفر** | بيعٌ جديد **كما كان تماماً** — يُختار الجزءُ من القائمة |
| **واحدة** | تُستأنَف تلقائياً **كما كانت تماماً** — يُعرَض ما طُلب فيها |
| **أكثر** | **اختيارٌ صريح إلزاميّ** يمنع الحفظ حتى يقع، بعرض **الجزء ورقم العملية** |

**والمُختارُ يُطابَق بالمعرّف من القائمة الحيّة**: قيمةٌ بائتة (حُسمت
العمليةُ في تبويبٍ آخر فاختفت) تُقرأ «لم يُختَر» فتمنع الحفظ — لا «اختير
شيءٌ ما» فتُسجَّل على عمليةٍ لم تعد قائمة.

**والقرارُ دالّةٌ خالصة لا نصٌّ داخل مكوّن React**: المشروع بلا مشغّل DOM،
فقرارٌ يعيش في المكوّن لا يُختبَر إلّا بقراءة نصِّه — وقراءةُ النصّ **لا
تُمسك انتقاءً صامتاً يعود يوماً بصياغةٍ أخرى** (ثبت ذلك عملياً: عكسُ الإصلاح
داخل المكوّن مرّ على حرّاس النصّ كلِّهم). فأُخرِج إلى
`patient_service_launcher_logic.ts` ويُختبَر دخلاً وخرجاً.

### وعطبان أصغر أُغلقا معهما
- **سؤالُ الإلحاق كان يظهر على استئنافٍ لم يُحسَم**: شرطُه كان
  `!existingEpisodeId`، وذاك يصير صادقاً مع عملياتٍ معلَّقة لم تُختَر منها
  بعدُ واحدة. فصار `!resuming` — سؤالُ الإلحاق لبيعٍ **جديد** وحده.
- **`Number(null)` تساوي صفراً لا `NaN`**: حلقةٌ وصلت بلا تسلسل كانت
  تُقرأ «#٠» فتتصدّر القائمةَ بدل أن تُذيَّل — رقمٌ مخترَع يسبق الأرقامَ
  الحقيقية. فصار الفحصُ بـ`typeof` لا بالتحويل. (والنمطُ نفسُه قائمٌ في
  `inManufacturingFullDeviceEpisodes` — **لم يُمَسّ**، خارج نطاق هذه
  التمريرة.)

### وما لم يُمَسّ
`POST /api/no-exam/device-sale` بعقده وحُرّاسه وقفله ومعاملته · جسمُ
الطلب حرفاً بحرف (الاستئنافُ يرسل المعرّفَ والخبيرَ لا غير) · محاسبةُ
البيع · مُرشَّحو الإلحاق (دالّةٌ مستقلّة) · الصيانةُ · العلاجُ الطبيعي ·
كلُّ ترحيلٍ من ٠٠١ إلى ٠٨٠.

### الترحيل
**بلا ترحيل** — لا عمودَ جديداً ولا جدولَ جديداً ولا قيدَ جديد.

اختبارٌ دائم: `npm run test:no-exam-sale-resume` (٣٤ تأكيداً بلا قاعدة
بيانات: شكلُ العطب بعينه · التصفيةُ صفّاً صفّاً والمرفوضُ يسقط وحده ·
الترتيبُ الثابت والمجهولُ مُذيَّلاً · قرارُ النافذة **خالصاً** بحالاته
الثلاث والمُختارُ البائت · وعقدُ الشاشة). ومعه `test:service-launcher`
مُحدَّثاً للعقد الجمعيّ، و`test:reception-routing` و`test:patient-cache`
و`test:maintenance-device-target` بلا تغييرٍ وتمرّ كاملة.

---

## 4.v «إلغاء الحسم» — بابُ خروجٍ محايد من «بانتظار الحسم» (ترحيل ٠٨١ — ٢٠٢٦-٠٩-١٥)

**صفٌّ دخل الطابورَ بالخطأ يخرج منه بلا أن يُقال عن المريض شيء.** لا بيعٌ
يُنشأ · ولا «لم يشترِ» تُسجَّل · ولا بيعٌ سابقٌ يُعكَس · ولا دينارٌ يتحرّك.
فرعُ `claude/free-price-checkbox`.

### الواقعة
تشخيصٌ حيٌّ على الإنتاج (استعلامُ `decision_queue_diagnosis.sql`، قراءةٌ
فقط) أحصى **٥١** متابعةً في «بانتظار الحسم»: **٤١** ظهورُها صحيح — لا بيعَ
ولا أمرَ ولا مال — و**عشرٌ** قرّر المالكُ أنها دخلت بالخطأ.

ولم يكن لها مخرجٌ إلّا بابان كلاهما يكذب: **«اشترى»** يخترع بيعاً لم يقع،
و**«لم يشترِ»** يسجّل رفضاً لم يقله المريض ويكتب في تقارير «أسباب عدم
الشراء» سبباً ملفَّقاً. **والحذفُ ليس باباً** — صفُّ المتابعة تاريخٌ.

**ولا قاعدةَ عامّةً تُشتقّ من وجود دفعة** (قرارُ المالك صراحةً): مريضٌ له
دفعاتٌ قديمة ثمّ معاينةٌ جديدة صحيحة **يجب** أن يظهر في الطابور. فمنطقُ
عضويّةِ `decision_queue_store.ts` **لم يُمَسّ بحرف** — والعشرُ تُعالَج
بالمعرّف واحدةً واحدة بقرارِ إنسان.

### الطرفيّةُ السادسة — `closed_decision_cancelled`
**ولا واحدةٌ من الخمس تصفها**: `converted` بيعٌ وقع · `closed_without_purchase`
رفضٌ قاله المريض · `closed_exam_cancelled` معاينةٌ سقطت سريرياً ·
`closed_admin_void` صفقةٌ بطلت بقرارٍ إداريٍّ بعد بيع · `closed_request_cancelled`
طلبُ جهازٍ سُحب. وهذه **ليست شيئاً من ذلك**: الصفُّ نفسُه ما كان ينبغي أن
يصل الطابور، وسحبُه لا يقول عن المريض ولا عن جهازه ولا عن ماله شيئاً.

**والمفردةُ في مواضعها الثلاثة معاً** (درسُ ٠٦١ ثمّ ٠٦٤ ثمّ ٠٧٩): `shared/
followup.ts` (`FOLLOWUP_STATUSES` · `FOLLOWUP_STATUS_LABELS` · `TERMINAL_STATUSES`)
· `shared/schema.ts` (قيدُ `CHECK` + `uq_pef_active_episode` +
`uq_pef_active_legacy` + `POST_EXAM_FOLLOWUP_STATUSES`) · وترحيلُ ٠٨١.
نسيانُ واحدٍ يجعل المتقاعدةَ تُحسَب حيّةً فيُقفل الفهرسُ الفريدُ الملفَّ
إلى الأبد.

### الباب — `POST /api/followups/:id/cancel-decision`
`store.cancelDecision` — والكتابةُ **ثلاثةُ أعمدةٍ لا أكثر**:
`status` · `closed_at` · `updated_at`. ومعها صفٌّ في
`post_exam_followup_events` وسطرٌ في `audit_log`.

**ولا `last_note` ولا `last_contact_at`** (تصحيحُ مراجعةٍ مستقلّة،
٢٠٢٦-٠٩-١٥): السببُ يعيش في الحدث وفي التدقيق، **ولا يُكتب فوق آخر
ملاحظةٍ قالها زميلٌ عن المريض**؛ وإلغاءُ الحسم **ليس اتصالاً بالمريض**
فلا يحرّك ختمَ آخرِ تواصل — وإلّا قُرئ ملفٌّ لم يُكلَّم صاحبُه منذ شهر
«تُوبع اليوم».

**ولا `closed_reason` يُكتب**: ذاك العمودُ معناه «لماذا لم يشترِ المريض»،
وكتابةُ قيمةٍ فيه هنا تجعل التقاريرَ تقرأ رفضاً لم يقع. **ولا
`cancelPendingPriceRequestsTx`** كما تفعل `closeWithoutPurchase` — تلك
تُلغي طلبَ سعرٍ لأن الصفقةَ حُسمت، وهنا **لم يُحسَم شيء**.

**والنصُّ المعروض محايدٌ في الاتجاهين**: «أُلغي الحسم — أُغلقت مهمة الحسم
إدارياً». والصياغةُ الأولى («لا قرارَ شراءٍ مسجَّل») **كانت قد تكذب**:
النظامُ يقبل `purchase_decision = 'bought'` محفوظاً بينما البيعُ ينتظر
استكمالَ بياناته (٠٦٦)، و«إلغاء الحسم» **لا يمحو ذلك القرار** — وهذا
صوابُه — فكان الموظّفُ يقرأ نفياً لقرارٍ قائمٍ في الصفّ نفسِه.

**والسببُ إلزاميٌّ في المخزن نفسِه** لا في النقطة وحدها: حارسٌ في الشاشة
يسقط بأوّل عميلٍ آخر، وحارسٌ في النقطة يسقط بأوّل مُنادٍ داخليّ.

**والحيّةُ وحدها تُلغى**: `lockFollowup` بالحالات الخمس الحيّة ⟶ ٤٠٩ لكلّ
طرفيّة. فبيعٌ وقع لا يُعكَس من هذا الباب (بابُه التصحيحُ الإداريّ، ٠٦٤)،
و«لم يشترِ» لا تُمحى. **وشرطُ الحالة في `UPDATE` نفسِه** حارسُ السباق
الثاني بعد القفل: ضغطتان متزامنتان ⟶ `[200, 409]` وحدثٌ واحد.

### الصلاحية — سلطةٌ إدارية لا لكلّ موظّف
`shared/followup.ts: canCancelDecision(session)` — `isAdmin` تمرّ أوّلاً
وبلا قيد (بسلطتها لا بدور صاحبها)، وإلّا `role === 'branch_manager'`.
**والاستقبالُ والمحاسبُ والطبيبُ يُردّون ٤٠٣** — خلافاً لـ`/complete-sale`
و`/not-bought` اللتين يفتحهما الاستقبالُ والمحاسب: البيعُ فعلُ مَن يكلّم
المريض، وسحبُ صفٍّ من الطابور تصحيحٌ إداريّ.

**ودالّةٌ مستقلّة عمداً** رغم تطابق منطقها اليومَ مع `canSetCommercialPrice`
— نفسُ مبدأ ٤.ي و٤.ك: كلُّ بابٍ يتطوّر بلا أن يخشى تعديلُه كسرَ الآخر.

**والخادمُ يقول الحقيقةَ لا الشاشةُ وحدها**: `mayCancelDecision` تُشتقّ في
`/api/followups/patient/:patientId` وفي `/api/followups/decision-queue`
معاً من `canCancelDecision(s) && !isTerminal(status)` — فلا يظهر زرٌّ
سيردّه الخادمُ ٤٠٣ أو ٤٠٩.

**وخارجَ مصفوفة `actions` عمداً**: تلك مشروطةٌ بمالكية الحقول (٠٦٦)، وهذا
الفعلُ لا يلمس حقلاً مملوكاً — فلا يُقاس بمقياسها.

### الشاشة
زرٌّ ثالث في `ExamPathDecisionActions.tsx` (المكوّنُ المشترك بين بطاقة
المريض وطابور «بانتظار الحسم»، ٤.l) بجانب «إتمام البيع» و«لم يشترِ»:
نافذةُ تأكيدٍ تقول ما سيحدث وما لن يحدث، وحقلُ سببٍ **إلزاميّ**، وزرُّ حفظ.

#### تصحيحٌ لاحق — المسارُ الموروث كان يحمل الصلاحيةَ ولا يجد زرّاً (٢٠٢٦-٠٩-١٥)

**فجوةُ واجهةٍ محضة — بلا حرفٍ في الخادم ولا القاعدة ولا منطق الطابور ولا
ترحيل.** الخادمُ يرسل `mayCancelDecision` **لكلّ صفٍّ حيّ** من نقطتَي
البطاقة والطابور معاً (`server/followup/routes.ts`) بصرف النظر عن
`examPath`، لكنّ الشاشتين كانتا ترسمان الزرَّ في `ExamPathDecisionActions`
وحدها — وتلك لا تُركَّب إلّا حين `examPath === true`. **فالصفُّ الموروث**
(يتيمٌ بلا حلقة، أو حلقةٌ من ما قبل ٠٦٥) **كان يحمل الصلاحيةَ ولا يجد
زرّاً — وهو بعينه شكلُ الصفوف التي وُضع البابُ لأجلها**.

- **`LegacyDecisionActions.tsx`** اكتسب `mayCancelDecision` والزرَّ
  والنافذةَ **بسلوكٍ مطابقٍ حرفياً**: نفسُ النقطة
  `/api/followups/:id/cancel-decision` · نفسُ جسم الطلب (`{ reason }`
  مقلَّماً) · نفسُ حارس الإرسال (`!cancelReason.trim()`) · **ونفسُ
  `act`/`submit`/`invalidateAll` بلا طفرةٍ ثانية**. والزرُّ **خارجَ
  `actions`** كما في المكوّن الآخر — سلطتُه أضيقُ ولا تحرسها مالكيةُ حقلٍ
  تجاريّ. **وبلا وعاءِ تخطيطٍ** كبقيّة أزرار هذا المكوّن.
- **`PostExamFollowups.tsx`**: الحقلُ صار مطبوعاً في `WaitingRow` (كان
  يُقرأ `(row as any)`)، ويُمرَّر للمكوّنين معاً، **وشرطُ العرض صار
  «أفعالٌ عادية أو صلاحيةُ إلغاء»**. `actions.length > 0` وحدها كانت تحجب
  الصفَّ كلَّه حين تفرغ القائمة — **ومديرُ فرعٍ أمام صفٍّ موروثٍ في
  `price_approval_pending` يقع في ذلك بالضبط**: `canDecideLegacyPriceRequest`
  ليست له فتُرجع `allowedActions` **`[]`**، وهو مع ذلك يملك إخراجَ الصفّ.
- **`PostExamDecisionCard.tsx`** تمرّرها كذلك فتتطابق الشاشتان —
  **و`!examPath` شرطُ صحّةٍ لا زينة**: البطاقةُ تُركّب المكوّنَ الموروثَ بلا
  شرط (أفعالُه تفرغ على مسار المعاينة فلا يرسم شيئاً)، فبدونه كان الزرُّ
  يظهر **مرّتين** على صفّ مسارِ المعاينة.
- **ومربّعُ «مجاني» لم يُمَسّ**: المسارُ الموروث يملكه أصلاً داخل
  `ServiceDiscountFields`.

#### وحقولُ البيع تظهر من فتحِ النافذة لا بعد السعر (٢٠٢٦-٠٩-١٥)

**شرطٌ واحد حُذف، ولا حرفَ في الخادم ولا القاعدة ولا المال.** كتلةُ
`ServiceDiscountFields` في نافذة «اشترى» الموروثة كانت خلف
`{originalPrice > 0 && …}`، فتُفتَح النافذةُ على صفٍّ بلا سعرٍ محفوظ فلا
يرى الموظّفُ إلّا حقلَ السعر والخبير — **ولا يعرف أن فيها خصماً
ومجّانيّةً أصلاً** حتى يكتب رقماً. **ونافذةُ المسار الحديث**
(`ExamPathDecisionActions`) **ترسم حقولَها كلَّها دفعةً واحدة** ولا تُخفي
شيئاً خلف إدخالٍ سابق. فصارت تُرسَم دائماً.

**وأمانُه من القاعدة الخالصة لا من الحذر**: `hasDiscount(EMPTY_DISCOUNT, 0)`
تساوي `false` — فالكتلةُ **خاملةٌ تماماً** قبل السعر: لا ملخّصَ ولا سببَ
ولا حقلَ خصمٍ في الحمولة. و`purchaseBlocked` تعطّل الإرسالَ عند
`!(original > 0)` **كما كانت بحرفها** — فالعرضُ ليس إذناً. والسعرُ
المرجعيُّ يتبع ما يُكتب أعلاه حيّاً (`purchaseOriginalPrice` تُحسَب في كلّ
رسم)، فلا رقمَ بائتٌ في الكتلة.

اختبارٌ دائم — قسمٌ جديد في `npm run test:purchase-ui` (٤٥–٥٤): الخمولُ
قبل السعر · الإرسالُ معطَّلٌ كما كان · الحمولةُ بلا حقل خصم · ثمّ السلوكُ
كما كان بعد الإدخال · وعقدُ المصدر (لا `originalPrice > 0` **ولا شرطٌ آخر
مكانَه** — الفحصُ على ما يسبق الوسمَ مباشرةً، والتعليقاتُ تُزال قبله فلا
يمرّ الفحصُ على ذكرٍ بدل كود) · **والمرجعُ نافذةُ المسار الحديث نفسُها**
لا وصفٌ مكتوب: حقولُها الثلاثة تُفحَص أنها بلا شرطٍ سابق، فلو عادت يوماً
إلى الإخفاء سقط البند معها. **ومُتحقَّقٌ بالعكس**: أُعيد الشرطُ فسقط
البندان ٥٢ و٥٣ وحدهما.

> **⚠ فشلٌ مسبَقٌ في هذا الملفّ** (سابقٌ لهذه التمريرة، مُثبَتٌ بمقارنةٍ
> نظيفة على `main`): البنود ٣٦–٣٩ تفحص أن `PostExamDecisionCard.tsx` يستورد
> `purchase_dialog_ui` ويربط `purchaseBlocked`/`purchaseBody`/`purchaseGaps`
> — وذاك المنطقُ **انتقل إلى `LegacyDecisionActions.tsx`** في ٤.l، فبقيت
> الإشاراتُ على البطاقة. يستحقّ تحديثاً منفصلاً.

اختبارٌ دائم — قسما «ع» و«ف» في `npm run test:cancel-decision` (١٤٥ تأكيداً
بعد الإضافة، مقابل ١٣٤ قبلها): الصلاحيةُ الحيّة على صفٍّ موروثٍ حقيقيّ
بخمسة فاعلين (مسؤولٌ ومديرُ فرعٍ ⟵ نعم · استقبالٌ ومحاسبٌ وطبيبٌ ⟵ لا)،
ونطاقُ الفرع في الاتجاهين، **والأفعالُ العاديةُ فارغةً والصلاحيةُ قائمة**
مع إثباتِ أن الإلغاء يمضي فعلاً بذلك الشكل، والسببُ إلزاميّاً بأشكاله
الثلاثة بصفر كتابة، **ونفسُ النقطة** مُقابَلةً بـ`ExamPathDecisionActions.tsx`
نفسِه لا بنصٍّ مكتوبٍ في الاختبار، وعقدُ الوصل في الشاشتين معاً ومنعُ
الزرّ المزدوج. **وكلُّ إصلاحٍ من الثلاثة مُتحقَّقٌ بالعكس**: أُزيل فسقطت
تأكيداتُه وحدها، ثمّ أُعيد.

### الترحيل
**٠٨١ فقط** — إضافيّ، idempotent. **وثلاثةُ كائناتٍ تُعاد بناؤها فعلاً**،
ولا يُقال «بلا إسقاط» بإطلاق: قيدُ `post_exam_followups_status_check`
يُسقَط ويُعاد بمجموعةٍ **أوسع** (فكلُّ صفٍّ قائم يجتازه كما هو)، والفهرسان
الجزئيّان `uq_pef_active_episode` و`uq_pef_active_legacy` يُسقَطان ويُعادان
وشرطُهما يعرف الطرفيّةَ السادسة — وإعادةُ بناء فهرسٍ ليست حذفَ بيانات (مشتقٌّ
من الجدول يُبنى منه)، وهو `UNIQUE` على مجموعةٍ **أضيق** بعد التوسعة فلا يفشل
لتصادمٍ جديد.

**وبلا `DROP TABLE` ولا `DROP COLUMN` ولا عمودٍ جديد ولا `DELETE` ولا
`UPDATE` على أيّ صفّ قائم** — العشرُ على الإنتاج يحسمها إنسانٌ بالمسار
القانونيّ لا ترحيلٌ صامت. ولا مسٍّ لترحيلٍ من ٠٠١ إلى ٠٨٠.

### العشرُ على الإنتاج
**الأفضلُ الزرُّ نفسُه** بعد النشر — عشرُ ضغطات، المسارُ القانونيُّ بحرفه.
وبديلُه `cancel_decision_ten.sql` في جذر الريبو لمن أراد دفعةً واحدة:
مقصورٌ على المعرّفات العشرة (٦٠ · ٦٢ · ٦٣ · ٦٤ · ٦٥ · ٦٦ · ٦٧ · ٦٨ · ٦٩ ·
٧١)، **والمتابعةُ ٧٠ ليست منها** (WB-00798 — اسمٌ يتكرّر على ملفَّين).
يكتب ما يكتبه الزرُّ بالضبط — **الأعمدةَ الثلاثة وحدها** — ويأخذ **لقطةً
حقيقية** في أوّل معاملته فيقارنها في آخرها: أيُّ تغيُّرٍ على متابعةٍ خارج
العشر، أو على أيّ عمودٍ تجاريّ، أو على `last_note`/`last_contact_at` **لأيّ
صفٍّ بما فيها العشر**، أو أيُّ حركةٍ في الدفعات أو قيود الكلف أو `total_cost`
أو الحالات أو الحلقات أو أوامر التصنيع أو المعاينات ⟶ **استثناءٌ يُسقط كلَّ
شيء**.
والتشغيلُ الثاني لا يفعل شيئاً (شرطُ الحالة الحيّة في `WHERE` نفسِه).
**ويشترط أن يكون ترحيلُ ٠٨١ منشوراً** — وقبله يُردّ نظيفاً بلا نصفِ كتابة.

### وما لم يُمَسّ
عضويّةُ «بانتظار الحسم» (`decision_queue_store.ts` بحرفه) · `/complete-sale`
و`/not-bought` و`setCommercialFields` ومالكيةُ الحقول (٠٦٦) · إلغاءُ
المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) · تصحيحُ السعر بعد البيع ·
الصيانةُ وبيعُ الجزء (٤.ي، ٤.ك) · سحبُ نوع الحالة وإغلاقُها (٤.r) ·
العلاجُ الطبيعي · سلّةُ المرضى (٠٦٨) · واتساب وتلغرام.

اختبارٌ دائم: `npm run test:cancel-decision` (١٣٤ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية — **والبصمةُ هي الحَكَم**: كلُّ قسمٍ يلتقط الدفعاتِ
ومجموعَها و`total_cost` وقيودَ الدفتر والحالاتِ وكلفَها والحلقاتِ بحالاتها
وأسعارها وأوامرَ التصنيع بمراحلها والمعايناتِ بأختامها وشواهدَ الإلغاء
والزيارات، ويقارنها بعد الفعل حرفاً بحرف. ومنها: المفردةُ في القاعدة
والفهرسين · مصفوفةُ الصلاحية بخمسة فاعلين وصفرُ كتابةٍ على ٤٠٣ · السببُ
إلزاميّاً **من النقطة ومن المخزن مباشرةً** (فحارسُ المخزن لا تحجبه ٤٠٠
النقطة) · المسارُ الطبيعيُّ بلا قرارِ شراءٍ ولا سعرٍ ولا خبير · نطاقُ
الفرع · **العزل: متابعةٌ أخرى لنفس المريض لا تُمَسّ** · التكرارُ ⟶ ٤٠٩
والسببُ الأوّلُ يبقى وحدثٌ واحد · التزامنُ ⟶ `[200, 409]` · المنتهيةُ
والمبيعةُ تُردّان بصفر كتابة · **الصفُّ اليتيمُ (شكلُ العشر على الإنتاج)
يُلغى بصفر أثر** · عقدُ الشاشة على البطاقة والطابور معاً · وحارسٌ معماريٌّ
يقرأ جسمَ الدالّة: لا `payments` ولا `cost_entries` ولا `total_cost` ولا
`patient_cases` ولا `patient_device_episodes` ولا `prosthetic_work_orders`
ولا `medical_exams` ولا `assignManufacturing` ولا `purchase_decision` ولا
`not_bought_reason_text` ولا `approved_price` · **والتدقيقُ في المعاملة
نفسِها**: فاعلٌ لا وجودَ له في `system_users` يُفشل سطرَ التدقيق بالمفتاح
الأجنبيّ فيسقط الإلغاءُ كلُّه — لا صفٌّ يخرج من الطابور بلا شاهد).

**والحيادُ مُثبَتٌ لا موصوف** (قسما «ن» و«س»): صفٌّ يحمل ملاحظةً ووقتَ
تواصلٍ سابقين يُلغى حسمُه فيبقيان **حرفاً بحرف** · وصفٌّ يحمل
`purchase_decision = 'bought'` محفوظاً يبقى قرارُه كما هو **والنصُّ المعروض
لا ينفيه ولا يثبت شراءً**. وكلاهما مُتحقَّقٌ بالعكس.

**والملفُّ التشخيصيُّ صُحِّح معه**: الطرفيّةُ السادسة أُضيفت إلى نسختَي
الاستعلام (وبلا ذلك يُرجع صفوفاً أُلغي حسمُها ولم تعد في الطابور)، **ووجودُ
المال وحدَه صار فئةَ «يحتاج مراجعة» (`R1`) لا فئةَ خطأ** وخارجَ مجموع
«ظاهرون بالخطأ» — فمريضٌ له مالٌ قديم ثمّ معاينةٌ جديدة صحيحة ظهورُه صحيح،
والعشرُ حُسمت **بأعيانها** ولا تُستنتَج من مالٍ أبداً.

**والتدقيقُ داخل المعاملة لا بعدها** (درسُ ٤.t): `logAudit` بلا `tx` يبتلع
خطأه، فكان الصفُّ يخرج من الطابور والسطرُ يضيع صامتاً والردُّ ٢٠٠ —
مُثبَتٌ بالعكس (أُزيلت المعاملةُ فسقطت خمسُ تأكيدات، ثمّ أُعيدت).
**والفشلُ يُقال**: خطأٌ غيرُ متوقَّع يُردّ ٥٠٠ برسالةٍ عربية تقول إن شيئاً
لم يتغيّر، بدل `throw` من معالجٍ غيرِ متزامن لا يصل الطلبَ معه ردٌّ
إطلاقاً (درسُ ٤.c).

---

## 4.w متابعةٌ بلا هويّة جهاز تُباع ولو كان للمريض أجهزةٌ أخرى مفتوحة (٢٠٢٦-٠٩-١٥)

**البيعُ لا يُردّ، ولا يُختطَف له جهازٌ قائم — يُفتَح له جهازُه هو.** فرعُ
`claude/patient-event-communication-plan-y0x9e6`. **بلا ترحيل، وبلا قيدٍ في
القاعدة تغيّر.**

### الواقعة
ترحيلُ ٠٧٣ رفع `uq_pde_case_open` فصار **أيُّ عددٍ من عمليات الأجهزة
المستقلّة** لمريضٍ واحد في آنٍ واحد مشروعاً. لكنّ
`ensureFirstDeviceEpisodeForSale` — التي تُمادي هويّةَ الجهاز لحظةَ تأكيد
شراءِ متابعةٍ **بلا `device_episode_id`** — بقيت تقرأ أيَّ حلقةٍ ليست
`delivered`/`cancelled` على الخيط فتردّ ٤٠٩ «يوجد جهاز آخر قيد الإجراء لهذه
الحالة لكنه غير مرتبط بهذه المتابعة».

وقد كُتب ذلك الحارسُ يومَ كان تعدّدُ الحلقات المفتوحة **شذوذاً** يستحقّ
إنساناً يحسمه. وبعد ٠٧٣ صار **الحالَ المشروع**، فبات الحارسُ يحبس بيعاً
صحيحاً لمجرّد أن للمريض جهازاً آخر يُصنَع — **بلا بابٍ يفكّه**: لا الموظّفُ
يملك ربطَ المتابعة يدوياً، ولا يجوز إغلاقُ الجهاز الآخر ليمرّ هذا.

### القاعدة الآن
- **لا تُقرأ حلقةٌ قائمة في هذه الدالّة أصلاً** — لا تُقفَل ولا تُعدَّل ولا
  تُغلَق ولا تُلتقَط. **والاختطافُ الذي وُضع الحارسُ لمنعه ممنوعٌ كما كان،
  بالامتناع عن الالتقاط لا بالردّ**: ربطُ متابعةٍ بجهازٍ لا دليلَ يربطهما
  يُثبّت سعرَها على جهازٍ لم يُقصَد ويُنسَب أمرُ تصنيعه إليه.
- **ويُفتَح لهذه المتابعة جهازٌ جديدٌ مستقلّ** بتسلسله الخاصّ، تُربَط به،
  ثمّ يمضي البيعُ على مساره المعتاد (`assignManufacturing` بحرفها) — فسعرُها
  وأمرُ تصنيعها وقيدُ كلفتها تشير إليه وحده.
- **ولا خيطَ لهذا الاختصاص ⟶ `null` كما كان** فيمضي المُستدعي على مساره
  القديم بلا كسر.

### والذرّيةُ من القفل القائم لا من فهرس
قفلُ صفّ الخيط (`patient_cases … FOR UPDATE`) هو نقطةُ التسلسل نفسُها التي
يستعملها `startDeviceEpisodeTx`، فـ`MAX + 1` يُحسَب تحته ويحرسه
`uq_pde_case_seq`. وقفلُ المتابعة في `confirmPurchase` يمنع ضغطتين من إنتاج
حلقتين لنفس المتابعة (الثانيةُ تقرأ `converted` فتُردّ) — **مُثبَتٌ حيّاً
في القسم ج من الاختبار، بلا تغيير**.

### وما لم يُمَسّ
`confirmPurchase` نفسُها (كانت تربط المتابعة وتمرّر المعرّف أصلاً) ·
`assignManufacturing` ونصفاها · `startDeviceEpisodeTx` ·
`createReplacementEpisodeTx` · **«عاد للشراء»** (`return_to_purchase_store.ts`
بلا لمسة، واختبارُه أخضر) · إلغاءُ المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) ·
سلّةُ المرضى (٠٦٨) · العلاجُ الطبيعي · **وكلُّ قيدٍ وفهرسٍ في القاعدة كما هو**
(`uq_pde_case_seq` · `uq_pwo_one_open_build_per_episode` ·
`uq_pef_active_episode` وأخواتها).

### الترحيل
**بلا ترحيل** — لا عمودَ جديداً ولا جدولَ جديداً ولا قيدَ جديد، ولا مسٍّ
لترحيلٍ من ٠٠١ إلى ٠٨١.

### ⚠ وانعكسَ عقدُ قسمٍ قائم في الاختبار
`server/first_device_identity.test.ts` قسم **ب٢** (البنود ٥٣–٦٠) كان يثبت
الردَّ ٤٠٩. صار يثبت البيعَ الناجح والجهازَ الجديد — **بقرارِ المالك، لا
لتخضير اختبار**. **والبندُ ٥٨ هو نفسُه قبل وبعد**: الحلقةُ الغريبة تُبصَم
كاملةً وتُقارَن بايتاً بايت بعد البيع، فالمنعُ الأصليُّ (لا اختطاف، لا مساس)
ما زال مُثبَتاً حيّاً.

اختبارٌ دائم: `npm run test:first-device-identity` — القسمُ الجديد **ب٣**
(٦١–٧٠): **ثلاثةُ أجهزةٍ مفتوحةٍ معاً** بحالاتٍ متنوّعة (اثنان `awaiting_exam`
وواحدٌ `examined`) + متابعةٌ رابعةٌ بلا هويّة ⟹ البيعُ يمضي · جهازٌ رابعٌ
جديدٌ بتسلسلٍ رابع · المتابعةُ مربوطةٌ به لا بأيٍّ من الثلاثة · أمرُ التصنيع
وقيدُ الكلفة عليه وحده · **ولا أمرَ على أيٍّ من الثلاثة** · **والثلاثةُ
مبصومةٌ قبل وبعد ومتطابقةٌ بايتاً** (الحالة والسعر والتسلسل والمسار والفرع
والأختام) · والمالُ على الجديد وحده. **ومُتحقَّقٌ بالعكس**: أُعيد الحارسُ
فسقط ١١ بنداً — كلُّها من القسمين الجديدين ولا شيءَ غيرها.

---

## 4.x الطبيبُ يبدّل الاختصاصَ على طلبِ جهازٍ مُرسَل (٢٠٢٦-٠٩-١٥)

**مَن يفحص المريضَ هو مَن يحدّد اختصاصَه — ولو وصلت المعاينةُ من طلبِ جهازٍ
بعينه.** فرعُ `claude/patient-event-communication-plan-y0x9e6`. **نافذةُ
المعاينة الجديدة وحدها** — بلا خادمٍ ولا قاعدةٍ ولا ترحيلٍ ولا منطقٍ ماليّ.

### الواقعة
منتقي الاختصاص في `NewExamDialog` كان **معطَّلاً** متى وصل `deviceEpisodeId`
من صفّ «معايناتي» (`disabled={!isEdit && fixedEpisode !== null}`). والتعليلُ
المكتوب: «جهازٌ مُمرَّرٌ بعينه = اختصاصُه محسوم؛ تبديلُه كان يرسل حلقةَ
اختصاصٍ آخر فيُردّ برسالةٍ مضلِّلة».

**والتعليلُ صحيحٌ في نصفه**: إرسالُ حلقةِ مسندٍ على معاينةِ أطراف يُردّ فعلاً
٤٠٩ `device_episode_stale` (§4.p). **لكنّ العلاجَ كان قفلَ القرار السريريّ**
بدل إسقاط الهويّة: الاستعلاماتُ تُصنِّف بالتخمين، والطبيبُ يفحص فيجد الصوابَ
مقفلاً عليه — وتصحيحُ التصنيف **قرارٌ سريريّ** له مسارُه القائم منذ §4.b
(«تبديل النوع لا إضافته»).

### القاعدة الآن
- **المنتقي مفتوحٌ دائماً** — لا `disabled` عليه بحال.
- **واختصاصُ الجهاز المُمرَّر يُقرأ بدقّةٍ لا بتخمين**: `preferSpecialty` —
  صفُّ «معايناتي» يمرّره مع `deviceEpisodeId` من **الصفّ نفسِه** (صفٌّ واحد =
  حلقةٌ منتظرةٌ واحدة باختصاصها، §4.p)، والنافذةُ تُعاد تركيبُها لكلّ صفّ.
- **بقي الاختصاصُ كما أرسله الاستعلامات ⟶ لا حرفَ يتغيّر**: المعرّفُ يُرسَل
  والمعاينةُ تُختَم على جهازها كما كان تماماً.
- **بُدّل ⟶ الجهازُ القديم يسقط**: `activeFixedEpisode` تصير `null`، فلا
  يُرسَل معرّفُه ولا تُربَط به المعاينة. **ولا مسارَ موازٍ**: النافذةُ تعود
  إلى منطقها القائم نفسِه — أجهزةُ الاختصاص الجديد المنتظرة تُقرأ وتُحسَم كما
  لو فُتحت من صفحة المريض (واحدةٌ تلقائياً · أكثرُ بمنتقٍ إلزاميّ · صفرٌ بلا
  جهاز).
- **ويُقال للطبيب أن الجهازَ سقط** (`note-exam-device-dropped`) — لا يختفي
  السطرُ صامتاً فيظنّ أنه ما زال موقّعاً عليه. **والعبارةُ تقتصر على الربط:
  «لن تُربَط هذه المعاينة بطلب الجهاز الذي أرسله الاستعلامات» ولا تَعِد
  بمصير.**

### ومصيرُ الطلب القديم ليس واحداً — فلا يُوعَد به
**كانت العبارةُ تقول «يبقى ذلك الطلب كما هو بانتظار معاينته» — وهذا يكذب في
الشكل الأشيع**، وهو نفسُه شكلُ سيناريو المالك: خيطُ المسند كان **وحيداً**،
فمنطقُ التبديل القائم (`retireSupersededCase` ⟶ `storage.deleteCaseType`، §4.b)
سحبه عبر مسار السحب في §4.r — **فحلقتُه السقالية تُحذَف فيزيائياً، وطلبُ
مراجعتها يُلغى (`status='cancelled'`) ومرساتاه تُحرَّران، ويخرج المريضُ من
قائمة عمل الطبيب لذلك الاختصاص.** مُثبَتٌ حيّاً في بنود ٨٣.أ–ج، ومُتحقَّقٌ
بالعكس (أُكِّد بقاؤه `pending` فسقط البندان).

**ومريضٌ يحمل الخيطين معاً** (تركيبةٌ مشروعة يضيفها الاستعلامات) لا يُمَسّ:
`retireSupersededCase` تُرجع `switched: false`، فيبقى طلبُه معلَّقاً فعلاً.
**وكذلك حين يتعثّر حارسٌ** (أمرُ تصنيعٍ أو دفعةٌ موسومة) — تبقى الحالتان.

فمصيرُه يتبع حالَ الملفّ، **ووعدٌ بأحد المصيرين يكذب في نصف الحالات**. ولذلك
تقول الشاشةُ ما تعرفه يقيناً — **الربطَ وحده** — ولا تصف ما لم تقرأه.
- **وإغلاقُ النافذة على ٤٠٩ بائت صار بالهويّة الفعّالة**: مَن بدّل الاختصاصَ
  له بديلٌ يُحَلّ من القائمة، وإغلاقُها عليه كان يرمي معاينةً كتبها للتوّ.

### والتصحيحُ الفعليُّ من منطقٍ قائم لا جديد
الخادمُ يفعل ما كان يفعله: `retireSupersededCase` (`server/medical/store.ts`،
§4.b) تسحب خيطَ الاستعلامات حين كان **وحيداً** والطبيبُ اختار الآخر، عبر
`storage.deleteCaseType` بحُرّاسه — فمَن له تاريخٌ حقيقيّ لا يُمَسّ ويبقى
الخيطان. **ولا حرفَ في الخادم تغيّر.**

### وما لم يُمَسّ
نقطةُ التوقيع وحُرّاسُها (§4.p) · `retireSupersededCase` · القاعدةُ والترحيلات ·
المالُ بكلّ صوره · «معايناتي» و`MyExams.tsx` · الحلقاتُ وطلباتُ المراجعة ·
العلاجُ الطبيعي (تبديلٌ إليه يُسقط الجهازَ بالقاعدة نفسِها — ومعرّفُ جهازٍ
عليه يُردّ ٤٠٩ أصلاً).

### الترحيل
**بلا ترحيل** — ملفّان اثنان في الواجهة، ولا عمودَ ولا جدولَ ولا قيد.

### ⚠ وانعكسَ عقدُ بندٍ قائم
`client/src/components/medical/exam_device_identity_ui.test.ts` بندُ **٨** كان
يثبت أن المنتقيَ **مقفول** — صار يثبت أنه **مفتوحٌ دائماً**، بقرارِ المالك لا
لتخضير اختبار. ومعه خمسةُ بنودٍ جديدة (٨أ–٨هـ) — الأربعةُ الأولى للهويّة
الفعّالة و**٨هـ** يقفل صياغةَ السطر — و**١٣ب** يثبت أن النافذةَ لا تُغلَق على
مَن بدّل. وثلاثةُ بنودٍ (٤ · ٦ · ١٣) تتبع الاسمَ الجديد بلا تغيّرٍ في سلوكها.

اختبارٌ دائم: `npm run test:exam-identity` — القسمُ الجديد **غ** (٧٩–٨٥):
**سيناريو المالك بالحرف** (الاستعلاماتُ «مسند» ⟵ الطبيبُ «أطراف») ⟹ المعاينةُ
تُحفَظ أطرافاً · **ولا تُربَط بجهاز المسند** · والخيطُ صار أطرافاً بمنطق
التبديل القائم · ولا متابعةَ تشير إلى المسند · **ومصيرُ الطلب القديم صريحاً
(٨٣.أ–ج): حلقتُه حُذفت، وطلبُ مراجعته `cancelled` بمرساتين محرَّرتين، ولا صفَّ
مسندٍ يبقى في قائمة الطبيب** · ومعرّفُ جهازِ اختصاصٍ آخر مُرسَلٌ صراحةً يُردّ
٤٠٩ **بصفر كتابة** · **وبلا تبديلٍ المسارُ كما كان بحرفه**. و`npm run
test:exam-identity-ui` لعقد الشاشة، ومنه **٨هـ**: نصُّ السطر يقول الربطَ ولا
يذكر مصيراً — **والفحصُ على نصّ السطر وحده** لا الملفّ كلِّه، فتعليقٌ يشرح
المصيرَ لا يُقرأ وعداً للطبيب.

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات**: أُعيد القفلُ والهويّةُ القديمة فسقط
البندان ٨ و٨ب وحدهما · وأُعيدت الصياغةُ القديمة فسقط **٨هـ** وحدَه · وأُكِّد
بقاءُ الطلب القديم (`n: 1` و`pending`) فسقط **٨٣.أ و٨٣.ب** وحدهما — فالقاعدةُ
تُكذّب الوعدَ القديم صراحةً، لا قراءةُ كودٍ وحدها.

---

## 4.y تبديلُ النوع يصحّح الطلبَ نفسَه ولا يختطف طلباً مستقلّاً (٢٠٢٦-٠٩-١٦)

**الطبيبُ يبدّل نوعَ الطلب الذي وصله، فيُصحَّح ذلك الطلبُ بعينه بهويّته —
ولا تُربَط معاينتُه بأيّ طلبٍ آخر من النوع الجديد.** فرعُ
`claude/patient-event-communication-plan-y0x9e6`. **ترحيل ٠٨٧** (أُضيف في
الجولة الثانية أدناه — إثباتُ منشأ الهديّة).

### الواقعة (أُعيد إنتاجُها حيّاً قبل أيّ تعديل، للاتجاهين معاً)
§4.x جعلت النافذةَ **تُسقط معرّفَ الطلب** عند تبديل الاختصاص، لأن معرّفَ
حلقة المسند يُردّ ٤٠٩ على معاينةِ أطراف. فيصل التوقيعُ **بلا هويّة**، وقاعدةُ
التوافق في `resolveExamEpisode` («واحدةٌ منتظرة ⟵ هي»، §4.p) تلتقط عندئذٍ
**طلباً مستقلّاً آخر** إن وُجد على الخيط الهدف.

وأثرُها ليس تسميةً خاطئة — **الطلبُ الآخر يُبتلَع**: حلقتُه تنتقل
`awaiting_exam ⟶ examined` · وطلبُ مراجعته المعلَّق يُغلَق حاملاً رقمَ تلك
المعاينة · وتُولَد له متابعةُ قرارِ شراء. فطلبُ جهازٍ مستقلّ يختفي من قائمة
الطبيب بوصفه «مُعايَناً» بمعاينةٍ لم تنظر فيه، **والربطُ مختومٌ بترِكر ٠٢٨
فلا يُصحَّح**.

### القاعدة الآن — الهويّةُ تبقى، والطلبُ يُصحَّح
الشاشةُ ترسل **معرّفَ الطلب نفسِه** ومعه رايةٌ صريحة `retypeDeviceEpisode`،
فينقله الخادمُ إلى خيط الاختصاص الصحيح:

- **`retypeAwaitingEpisodeForExamTx`** (`server/device_episodes/store.ts` —
  كتابةُ الحلقات في طبقتها كما يوجب حارسُ `test:device-episodes`) تقفل
  **الخيطَ الهدف ثمّ الحلقة** (نفسُ ترتيب `startDeviceEpisodeTx`)، وتُعيد
  التحقّق من كلّ شرطٍ فحصته النقطة، ثمّ تُحدّث `case_id` و`sequence_number`
  وحدهما — **والرقمُ يُحسَب تحت قفل الخيط** لأنه رقمٌ داخله (`uq_pde_case_seq`).
- **والهويّةُ محفوظة**: المعرّفُ · المطلوبُ والجزءُ · الفرعُ · المسارُ ·
  `created_at` و`awaiting_since` · الكلفة — لا يتغيّر منها حرف.
- **وطلبُ المراجعة يتبعه**: `retagPendingRequestsForRetypedEpisode`
  (`server/medical_review/store.ts`) تُحدّث `service_type`/`case_id`
  **للمعلَّق وحده** — وقرارُ إنسانٍ (`examined`/`escalated`/`returned`/
  `cancelled`) لا يُعاد كتابتُه (درسُ §4.r). و**قبل** `closeRequestsAwaitingExam`
  لأنها تطابق بـ`service_type`، وإلّا بقي الطلبُ معلَّقاً في طابورٍ غادره
  **وحجز مرساتَه** (`uq_mrr_pending_episode`).
- **وكلُّه في معاملة التوقيع**: رفضٌ في أيّ خطوة = صفرُ كتابة.

### والرفضُ بصفر كتابة بدل اختيار طلبٍ آخر
«أ» لم يعد صالحاً لحظةَ الحفظ (سُحب · صار `examined` · تغيّر فرعُه · مساره
«بلا معاينة» · أو مطلوبُه لا يصحّ للنوع الجديد — «ركبة» لا تصير مسنداً) ⟶
**٤٠٩ بصفر كتابة**، **ولا سقوطَ إلى «الوحيدة المنتظرة»** التي قد تكون طلباً
مستقلّاً آخر.

### والنيّةُ صريحة — لا دلالةَ تتغيّر ضمناً
**بلا الراية يبقى معرّفُ خيطٍ آخر بائتاً ٤٠٩ كما كان بحرفه**، فلا عميلٌ قائم
يتغيّر سلوكُه. ورايةٌ **بلا معرّف** تُردّ ٤٠٠: رايةُ تصحيحٍ بلا هويّةِ الطلب
المقصود تفتح البابَ الذي جاءت لتغلقه. وقيمةٌ ليست بوليانياً تُردّ ٤٠٠.

### ⚠ وحدُّه القديم «خيطٌ هدفٌ قائم» — **رُفع، اقرأ التكملة الثانية أدناه**
كان التصحيحُ يُسقَط حين لا خيطَ للاختصاص الجديد، ويتولّاه مسارُ §4.b فيهدم
الطلبَ بدل أن ينقله. **وهذا هو شكلُ المالك الأشيع بعينه** — وقد عولج.

### والعلاجُ الطبيعي بلا حلقات
التبديلُ إليه **يُسقط الجهازَ حقّاً** (عبارةُ §4.x الكهرمانية كما هي)، والخادمُ
يردّ معرّفَ جهازٍ عليه ٤٠٩ كما كان. والتصحيحُ بين اختصاصَي الأجهزة وحدهما.

### وما لم يُمَسّ
ختمُ المعاينة (٠٢٨) · إلغاءُ المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) ·
`applyPrescription` وأعمدةُ المريض · محاسبةُ البيع · `markEpisodeExamined` ·
مسارُ §4.b · سلّةُ المرضى (٠٦٨) · واتساب · **وكلُّ ترحيلٍ من ٠٠١ إلى ٠٨١**.

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد. الأعمدةُ المكتوبة كلُّها قائمة.

اختبارٌ دائم: `npm run test:exam-identity` — القسمان **ف** (٨٦–٩٠.ب: سيناريو
المالك، «أ» مسندٌ و«ب» أطرافٌ مستقلّ) و**ق** (٩١–٩٨.أ: الاتجاهُ العكسيّ، ثمّ
الرفضُ بصفر كتابة والرايةُ بلا معرّف وبلا الراية إطلاقاً) — ومعهما
`npm run test:exam-identity-ui` لعقد الشاشة. **ومُتحقَّقٌ بالعكس**: إعادةُ
السقوط إلى «الوحيدة المنتظرة» تُسقط ٨٨/٨٩/٨٩.أ/٩٠ و٩٣/٩٤/٩٤.أ و٩٦ (العطبُ
بعينه عائداً)، وحذفُ فرع التصحيح وحده يُسقط سبعةَ بنودٍ كلُّها من القسمين
الجديدين، وحذفُ إعادة وسم الطلب يُسقط بندين اثنين لا غير.

### تكملةٌ لاحقة — «أ» تصير أطرافاً بالكامل، ولا يبقى أثرٌ تشغيليٌّ للمساند

**تصحيحُ المطلوب (قرارُ المالك)**: نقلُ الحلقة وحدَه يصحّح الطلبَ ولا يصحّح
**الملفّ**. فبعد أن يبدّل الطبيبُ «أ» من مساندٍ إلى أطراف كان يبقى على الصفحة
**أثرٌ تشغيليٌّ كامل لخطأ إدخالٍ لم يقع**: صفُّ `patient_cases` المساند قائمٌ
فارغاً · وعَلَمُ `is_medical_support` مرفوع · و`support_type` مكتوب · وشارةٌ
كهرمانية «بانتظار معاينة مساند» في `/api/medical/pending` وفي قائمة عمل
الطبيب. **فصارت «أ» أطرافاً بالكامل كأنّها سُجّلت أطرافاً من البداية.**

**والرفعُ بالكاتب القانونيّ نفسِه** — `storage.deleteCaseType` بحُرّاسه
الثمانية (§4.r)، **ولا مسارَ هدمٍ ثانٍ** يُكتب لهذه الحالة.
`retireRetypedSourceCase` (`server/medical/store.ts`، بجوار
`retireSupersededCase`) تُنادى من `applyDecision` **بعد `COMMIT`** لا داخل
معاملة التوقيع: `deleteCaseType` تفتح معاملتَها وتأخذ
`pg_advisory_xact_lock(919, …)`، فتعشيشُها خطأٌ لا اختصار.

**وثلاثةُ حُرّاسٍ قبلها، والأوّلُ هو المقصودُ بعينه:**
- ① **`caseHasAnyEpisode`** — أيُّ حلقةٍ باقيةٍ على الخيط المصدر = **عمليةٌ
  مستقلّةٌ حقيقية**، فلا يُمَسّ الخيطُ إطلاقاً. **وهذا وحدَه ما يمنع أن يُهدَم
  طلبُ مساندٍ حقيقيٌّ آخر**: `classifyCaseDisposal` تصنّف الحلقةَ
  `awaiting_exam` **سقالةً** (§4.r)، فنداءٌ أعمى لـ`deleteCaseType` كان
  سيمحو طلبَ المريض الثاني المشروع. **ومُثبَتٌ بالعكس**: إزالةُ هذا الحارس
  وحده تُسقط **ستّةَ بنودٍ كلُّها من القسم ش** ولا شيءَ غيرها.
- ② **كلفةٌ مسجَّلة ≠ صفر** ⟶ يُترَك الخيطُ ويُقال السبب. **تنظيفُ خطأ إدخالٍ
  لا يحرّك ديناراً**: لا `total_cost` ولا قيدَ `case_retired` ولا دفعة.
- ③ **وحُرّاسُ §4.r الثمانية** بنصّها — معاينةٌ موقّعة · متابعة · أمرُ عمل ·
  دفعةٌ موسومة · مبلغٌ معلَّق · طلبُ خصم · حلقةٌ حيّة · طلبٌ حسمه الطبيب.

**والفشلُ يُقال لا يُبتلَع**: `switchNote` نفسُها التي يستعملها تبديلُ §4.b
(«بقيت الحالة السابقة مفتوحة: …»). وخيطٌ رفعه التبديلُ سلفاً ⟶ **لا شيءَ
يُفعَل ولا شيءَ يُقال**.

**والشارةُ الباقيةُ للاختصاص الآخر صادقة**: «ب» ما زال ينتظر معاينتَه فعلاً،
فالمطلوبُ **غيابُ المساند** لا خلوُّ خريطة الانتظار.

اختبارٌ دائم: القسمُ **ف** اكتسب ٩٠.ب–٩٠.هـ (الخيطُ رُفع · ولا عَلَمَ ولا
عمودَ تفاصيل · ولا شارةَ مساند) والقسمُ **ق** ٩٤.ب–٩٤.د بالاتجاه العكسيّ،
**وقسمٌ جديد ش** (٩٩–١٠٠.د): الخيطُ المصدر يحمل «أ» **و«ج» مستقلّاً حقيقياً**
⟹ «ج» مبصومٌ ومتطابقٌ بايتاً بعد التبديل · وطلبُ مراجعته ما زال `pending`
وبـ`service_type = 'medical_support'` وبمرساته · والخيطُ والعَلَمُ قائمان.
**ومُتحقَّقٌ بالعكس**: تعطيلُ التنظيف يُسقط **أربعةَ بنودٍ بالضبط** (٩٠.ب ·
٩٠.ج · ٩٤.ب · ٩٤.ج)، وإزالةُ `caseHasAnyEpisode` تُسقط ستّةَ بنود القسم ش.

**وبلا ترحيل** — ولا عمودَ ولا جدولَ ولا قيد، ولا حرفَ في `deleteCaseType`
ولا في `classifyCaseDisposal`.

### تكملةٌ ثانية — الطلبُ يُصحَّح ولو لم يوجد خيطٌ هدفٌ بعد (٢٠٢٦-٠٩-١٦)

**شكلُ المالك الأشيع**: الاستعلاماتُ سجّلت «مساند» بالخطأ **وليس للمريض خيطُ
أطرافٍ أصلاً**، فبدّله الطبيبُ إلى «أطراف». وهذا ما كان الحدُّ أعلاه يُخرجه من
التصحيح.

**والمُعادُ إنتاجُه حيّاً قبل أيّ تعديل** (فحصٌ على النقاط الحقيقية): الرايةُ
تُسقَط، فيتولّاه مسارُ §4.b — الوصفةُ تُنشئ خيطَ الأطراف، و`retireSupersededCase`
تسحب خيطَ المساند الوحيد، **فتُحذَف حلقتُه سقالةً** (§4.r). فيخرج المريضُ من
التوقيع هكذا:

```
الحالات: ["prosthetic"] · الأعلام: مساندٌ مطفأ · pending: [] · worklist: []
الحلقات: []                      ⟵ **لا طلبَ جهازٍ إطلاقاً**
المعاينة: device_episode_id=null · المتابعة: device_episode_id=null
```

فالأثرُ التشغيليُّ للمساند **كان يختفي فعلاً** — لكنّ الطلبَ يختفي معه. وذاك
ليس «كأنّه سُجّل أطرافاً من البداية»: مريضٌ سُجّل أطرافاً يملك **طلبَ جهازٍ
مُعايَناً** بمعاينةٍ مختومةٍ عليه ومتابعةٍ مربوطةٍ به. والمتابعةُ اليتيمة هي
بعينها العطبُ الذي يسجّله §4.r («`/not-bought` عليها ٤٠٩»).

**فصار الخيطُ الهدفُ يُفتَح داخل معاملة التوقيع** بدل أن يُسقَط التصحيح:
- **`ensureActiveCaseTx`** (`server/patient_cases/reopen.ts` — طبقةُ الحالات
  نفسُها التي فيها `reopenClosedCaseTx`): يفتح الخيطَ بكلفةِ صفر، بـ
  `ON CONFLICT DO NOTHING` على `uq_patient_cases_patient_type` (نفسُ نمط
  `syncPatientCases` بحرفه)، ويفتح المغلقَ بالصفّ نفسِه.
- **وتُنادى من `createExam` داخل معاملتها** قبل نقل الطلب — فرفضٌ في أيّ خطوة
  بعدها **يتراجع عن الخيط معها**: ٤٠٩ بائتٌ لا يترك خيطاً مفتوحاً (مُثبَتٌ،
  البند ١٠٩.ب).
- **والترتيبُ صار الآمنَ دائماً**: `caseFirst` تصير صادقةً كلّما طُلب تصحيح،
  فيُوقَّع أوّلاً ثمّ تُطبَّق الوصفة. ولو سبقت `applyDecision` لسحبت الخيطَ
  القديم بما فيه الطلبُ نفسُه، فينقل التصحيحُ صفّاً لم يعد موجوداً.
- **و`retireSupersededCase` تنسحب من تلقائها**: هي تقرأ خيوطَ الجهاز **قبل**
  الوصفة، وقد صار للمريض خيطان حينئذٍ (المصدرُ والهدفُ المفتوحُ لتوّه) ⟶
  `switched: false`. فالرفعُ يقع من `retireRetypedSourceCase` وحدها بحُرّاسها
  الثلاثة — **ولا هادمَين على خيطٍ واحد**.
- **و`case_id` الطلب يتبع الخيطَ المحسوم** لا `values.caseId` الفارغة، فلا
  يُعاد وسمُ طلب المراجعة بـ`case_id = NULL`.
- **⚠ والمتابعةُ كذلك — أُغفلت أوّلاً وصُحِّحت (٢٠٢٦-٠٩-١٧)**: صفُّ المعاينة
  كان يأخذ الخيطَ المحسوم، **و`ensureFollowupForSignedExam` تبقى على
  `values.caseId`** — تلك لقطةٌ قُرئت **قبل** فتح الخيط الهدف فتصل `null`.
  فعلى **أشيع أشكال التصحيح** (مساندٌ وحده ⟶ أطراف، بلا خيطِ أطرافٍ سابق)
  تُولَد المتابعةُ بـ`case_id = NULL`، فيردّ `completeReceptionSale`
  **٤٠٩ «هوية الجهاز غير مكتملة»** على كلّ بيعٍ **بدفعةٍ فورية**
  (`server/followup/store.ts`: `sold.caseId === null`) — **فالملفُّ يُعايَن
  ولا يُباع**، وتصحيحُ السعر بعد البيع يتخطّى كلفةَ الحالة للسبب نفسِه.
  والإصلاحُ تمريرُ `targetCaseId` نفسِه — فالمتابعةُ والمعاينةُ على خيطٍ
  واحد دائماً. **ولا مسارَ آخر يتغيّر**: `targetCaseId` تبدأ `values.caseId`
  ولا تُعاد كتابتُها إلّا في فرع التصحيح-بلا-خيطٍ-هدف وحده.
  اختبارٌ حيّ في القسم **ت.١** من `test:exam-identity` (البنود ١٠٤.د–ز):
  المتابعةُ على خيط الأطراف وعلى عملية الجهاز نفسِها، **وإتمامُ البيع مع
  دفعةٍ فورية ينجح** بدفعةٍ واحدة على الخيط والحلقة ومتابعةٍ `converted`
  بأمرِ تصنيعٍ واحد. **ومُتحقَّقٌ بالعكس**: إعادةُ `values.caseId` تُسقط
  **أربعةَ بنودٍ بالضبط — كلُّها الجديدة** ولا شيءَ غيرها من الحزمة كلِّها.

**والنتيجةُ الآن** لنفس المدخلات بالضبط:

```
الحلقة #١: prosthetic · examined · نفسُ المعرّف والمطلوب والفرع والمسار
           والتواريخ والكلفة والتسلسل — بايتاً بايت
المعاينة: device_episode_id=1 · المتابعة: device_episode_id=1
طلبُ المراجعة: prosthetic · examined · بمعاينته وبخيطه الصحيح
الحالات: ["prosthetic"] · مساندٌ مطفأ · pending: [] · worklist: []
```

**وما لا يصحّ لا يُصحَّح**: طلبُ «قالب» (جزءُ أطراف) لا يصير مسنداً — المساندُ
بلا أجزاء (§4.e) — فيُردّ ٤٠٩ **ولا خيطَ مساندٍ يُفتَح له**.

**والعمليةُ المستقلّةُ الحقيقية لا تُمَسّ هنا أيضاً**: خيطُ مساندٍ يحمل الطلبَ
المصحَّح **وطلباً ثانياً حقيقياً** ⟶ يبقى الخيطُ والعَلَمُ والشارة، والطلبُ
الثاني مطابقٌ بايتاً بطلب مراجعته المعلَّق. يحرسه `caseHasAnyEpisode` نفسُه.

اختبارٌ دائم: القسمُ **ت** في `npm run test:exam-identity` (١٠١–١٠٩.ب —
سيناريو المالك بالحرف، والاتجاهُ العكسيّ، وحارسُ الجزء، والعمليةُ المستقلّة،
والرفضُ بصفر كتابةٍ بلا خيطٍ يُفتَح). **ومُتحقَّقٌ بالعكس**: إعادةُ الحدّ
القديم تُسقط **١٧ بنداً — كلُّها من القسم ت** ولا شيءَ غيرها؛ والأقسامُ ف وق
وش تبقى خضراء، فالتغييرُ مقصورٌ على مسار «بلا خيطٍ هدف».

**وبلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد.

**⚠ إخفاقاتٌ مسبَقة غيرُ متعلّقة** (كلُّها **بمجموعةِ فشلٍ متطابقة على الشجرة
النظيفة** — مُقارَنةٌ بـ`git stash`، ولم تُصلَح: خارجَ هذه المهمّة):
`test:exam-idempotency` ببندين (أ١/أ٢، تقاربُ سبع طلباتٍ متزامنة) ·
`test:decision-queue` بأربعة (٢٦–٢٩، إعادةُ فتحِ متابعةٍ محسومة) ·
`test:followup` بأربعة (٣٢ ومرفقاتُه، أمرُ بناءٍ فعّالٍ يمنع التأكيد).

### تكملةٌ ثالثة — التصحيحُ من صفحة المريض والسجلّ أيضاً (٢٠٢٦-٠٩-١٧)

**كان التصحيحُ يعمل من «معايناتي» وحدها.** والبابان الآخران — **صفحةُ المريض**
(`PatientMedicalExams.tsx`) و**سجلُّ المرضى** (`PatientsList.tsx`) — لا يمرّران
`deviceEpisodeId` أصلاً، فكان تبديلُ النوع منهما يقع **بلا هويّةٍ وبلا راية**.

### الواقعةُ المُعادُ إنتاجُها (حيّاً على النقاط الحقيقية قبل أيّ تعديل)
الاستعلاماتُ تسجّل «مساند» وتفتح طلبَ جهاز، فيوقّع الطبيبُ «أطراف» من صفحة
المريض. فيمضي التوقيعُ «بلا جهاز» ويتولّاه تبديلُ §4.b — **فتُحذَف الحلقةُ
سقالةً** (§4.r) ويخرج المريضُ هكذا:

```
الحالات: ["prosthetic"] · مساندٌ مطفأ · pending: [] · worklist: []
الحلقات: []                      ⟵ **لا طلبَ جهازٍ إطلاقاً**
exam.device_episode_id = null · followup.device_episode_id = null
```

فالأثرُ التشغيليُّ للمساند يختفي فعلاً (وذاك نصفُ المطلوب)، **لكنّ الطلبَ
يختفي معه** — ومَن سُجّل «أطرافاً» من البداية يملك طلبَ جهازٍ حيّاً ومعاينةً
مختومةً عليه ومتابعةً مربوطةً به. والمتابعةُ اليتيمةُ هي بعينها العطبُ الذي
يسجّله §4.r (`/not-bought` عليها ٤٠٩).

### القاعدةُ الآن — الشاشةُ تعلن النيّة كما تفعل «معايناتي»
حين **لا يكون للاختصاص المختار طلبٌ منتظر**، تكون أجهزةُ **الاختصاص الآخر**
المنتظرةُ هي الطلباتِ التي يصحّح الطبيبُ نوعَها: يُرسَل معرّفُها ومعه
`retypeDeviceEpisode` — **المسارُ القانونيُّ نفسُه** الذي بنته التكملتان
الأولى والثانية. فتصير النتيجة:

```
الحلقة #١: prosthetic · examined · بمعرّفها ومطلوبها وفرعها وتواريخها كما هي
طلبُ المراجعة: prosthetic · examined · بمعاينته وبخيطه الصحيح
exam.device_episode_id = 1 · followup.device_episode_id = 1
الحالات: ["prosthetic"] · مساندٌ مطفأ · pending: [] · worklist: []
```

**والخادمُ لم يُمَسّ بحرف** — لا نقطةُ التوقيع ولا `retypeAwaitingEpisodeForExamTx`
ولا `retireRetypedSourceCase` ولا قاعدةُ البيانات. التغييرُ **ما ترسله الشاشة**.

### والحُرّاسُ الثلاثة
- **ولا تصحيحَ حين للاختصاص المختار طلبُه هو**: ذاك طلبٌ مستقلٌّ حقيقيّ
  يُوقَّع عليه، **والآخرُ لا يُمَسّ ولا يُعرَض** (§4.y محفوظٌ بحرفه).
- **ولا تخمينَ بين طلبين**: أكثرُ من طلبٍ للاختصاص الآخر ⟵ **منتقٍ إلزاميّ**
  ولا حفظ قبله — نفسُ قاعدة المسار العاديّ، ونفسُ قاعدة الخادم.
- **ولا تصحيحَ إلى العلاج الطبيعي**: بلا حلقاتٍ أصلاً، فالجهازُ يسقط حقّاً
  وتبقى العبارةُ الكهرمانية كما هي.

**والنيّةُ تبقى صريحة**: بلا الراية يبقى معرّفُ خيطٍ آخر بائتاً ٤٠٩ في الخادم
كما كان — لا دلالةَ تتغيّر ضمناً، ولا عميلٌ قائم يتبدّل سلوكُه.

### والقرارُ خالصٌ خارجَ المكوّن
`client/src/components/medical/exam_episode_choice.ts` —
`resolveExamEpisodeChoice` تحسم **كلَّ** القرار: أيُّ طلبٍ، وأهو تصحيحُ نوعٍ أم
توقيعٌ عاديّ، وأيلزم اختيار، وأسقط الجهازُ المُمرَّر. والنافذةُ تفوّضها ولا
تحمل نسخةً ثانية من القاعدة.

**ولماذا**: درسُ §4.u بحرفه — المشروعُ بلا مشغّل DOM، فقرارٌ داخل React لا
يُختبَر إلّا بقراءة نصِّه، وقراءةُ النصّ **لا تُمسك انقلاباً في المعنى يعود
بصياغةٍ أخرى**. وخطأُ هذا القرار بعينه يُختَم بترِكر ٠٢٨ فلا يُصحَّح بعدها.

**و`fixedActive` لا تعتمد على القائمة إطلاقاً** — تقرؤها الشاشةُ **قبل** أن
تجلبها لتقرّر أتجلبها، فلو اعتمدت عليها لدار الشرطُ على نفسه.

**والتحريرُ خارجَ هذا كلِّه صراحةً**: النسخةُ الثانية لا تعيد ختمَ الجهاز ولا
تصحّح نوعاً — يُقال في الدالّة نفسِها لا يُترَك لكون الاستعلام معطَّلاً في
الشاشة (الاستثناءُ يُطلَب باسمه ولا يُنال بالسهو).

### والمرساةُ هي الاختصاصُ الذي فُتحت به النافذة، لا الجديد

**فُتحت النافذةُ على طلب مساندٍ «أ»، وللمريض طلبُ أطرافٍ مستقلٌّ «ب»، فبدّل
الطبيبُ إلى أطراف.** والقراءةُ بالاختصاص **الجديد** كانت تجد «ب» فتوقّع عليها
— **عمليةٌ مستقلّةٌ لم ينظر فيها الطبيب** — **وتبقى «أ» على خطئها**. وذاك
الاختطافُ بعينه الذي وُضع كلُّ هذا القسم لمنعه.

**فالمرشَّحون من أجهزة الاختصاص الأصليّ وحدها** (`openedSpecialty` =
`preferSpecialty`، تمرّره الأبوابُ الثلاثة): متى بُدّل عن اختصاصِ أجهزةٍ معلوم
إلى اختصاصِ أجهزةٍ آخر، فالمقصودُ يقيناً هو الطلبُ الذي فُتحت عليه النافذة —
**وأجهزةُ الاختصاص الجديد لا تُقرأ ولا تُعرَض ولا تُمَسّ**.

- **واحدٌ للاختصاص الأصليّ** ⟶ هو، بنيّةِ تصحيحٍ صريحة.
- **أكثرُ من واحد** ⟶ **منتقٍ إلزاميّ ولا حفظ قبله**، وخياراتُه من الاختصاص
  الأصليّ وحده. **واختيارُ «ب» لا يُقبَل** — ليست من مرشَّحي التصحيح أصلاً.
- **وصفرٌ** ⟶ **لا يُلتقَط بديل**: السكوتُ أصدقُ من توقيعٍ على طلبِ غيرها.

**وبلا تبديلٍ المرساةُ لا تغيّر شيئاً**: فُتحت على مساندٍ وبقي ⟶ طلبُ المساند
بلا راية؛ وعلى أطرافٍ وبقي ⟶ طلبُ الأطراف.

> **⚠ والفتحُ العامّ لم يعد يُقرأ تبديلاً — اقرأ «تكملة رابعة» أدناه
> (٢٠٢٦-٠٩-١٧).** كان هنا مخرجٌ ظُنّ ضيّقاً: نافذةٌ تُفتَح **بلا اختصاصٍ
> أصليٍّ معلوم** ثمّ لا يكون للاختصاص المختار طلبٌ منتظر ⟶ تُقرأ أجهزةُ
> الاختصاص الآخر «هي المقصودة». **وقد أُزيل بقرار المالك**: لا تصحيحَ
> تلقائياً إلّا عن مرساةٍ معلومة بُدّلت صراحةً.

**والخيطُ الهدفُ قد يحمل حلقةً أصلاً** — وهذا أوّلُ تصحيحٍ هدفُه خيطٌ غيرُ
فارغ. مُثبَتٌ حيّاً: «أ» تُنقَل إليه بتسلسلٍ جديد تحت القفل، **و«ب» مطابقةٌ
بايتاً بايت** بعد التصحيح بطلب مراجعتها المعلَّق وبلا معاينةٍ عليها، والخيطُ
يحمل الطلبين بتسلسلين متمايزين (`uq_pde_case_seq` محفوظ، و`uq_pde_case_open`
مرفوعٌ منذ ٠٧٣).

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد، **ولا حرفَ في الخادم**.

اختبارٌ دائم: `npm run test:exam-episode-choice` (٣٩ بنداً خالصاً بلا قاعدة
بيانات — شكلُ المالك، الاتجاهُ العكسيّ، **والمرساةُ بأقسامها الأربعة**: «أ»
تُصحَّح و«ب» لا تُعرَض · أكثرُ من طلبٍ أصليٍّ ⟶ اختيارٌ وخياراتُه بلا «ب»
واختيارُ «ب» مرفوض · صفرٌ ⟶ لا بديلَ يُلتقَط · وبلا تبديلٍ لا شيء يتغيّر —
والفتحُ العامّ، والعمليةُ المستقلّةُ لا تُمَسّ، والاختيارُ البائت، وما لا
يُصحَّح، ومسارُ «معايناتي» بحرفه، واستقلالُ `fixedActive` عن القائمة)، و`npm run test:exam-identity-ui`
(قسمٌ جديد ٨ز: البابان لا يمرّران معرّفاً · قراءةُ الاختصاص الآخر · حارسُ
العملية المستقلّة · النيّةُ من البابين · **وتفويضُ القرار للدالّة الخالصة بلا
نسخةٍ ثانية في المكوّن**). **ومُتحقَّقٌ بالعكس ثلاثَ مرّات**: إسقاطُ قراءة الاختصاص
الآخر يُسقط **٩ بنودٍ — كلُّها من الأقسام الجديدة** (وأقسامُ الحماية تبقى
خضراء) · وإسقاطُ حارس العملية المستقلّة يُسقط **٣ بالضبط** · **وإسقاطُ
المرساة يُسقط ١٠ بالضبط — كلُّها أقسامُ المرساة**، وقسمُ «بلا تبديل» وقسمُ
الفتح العامّ يبقيان خضراوين.

والمسارُ الحيُّ في `npm run test:exam-identity` (الأقسام ف · ق · ش · ت **و**
**ث الجديد**: التصحيحُ إلى خيطٍ يحمل طلباً مستقلّاً — «أ» بهويّتها ومعاينتُها
عليها، **و«ب» مطابقةٌ بايتاً** بطلبها المعلَّق، وتسلسلان متمايزان، وقائمةُ
الطبيب تُظهر «ب» وحدها، ولا أثرَ تشغيليٌّ للمساند).

### تكملةٌ رابعة — الفتحُ العامّ لا يختطف طلبَ الاختصاص الآخر (٢٠٢٦-٠٩-١٧)

**التصحيحُ التلقائيُّ بين الأطراف والمساند مشروطٌ بمرساةٍ معلومة بُدّلت
صراحةً — لا غير.** قرارُ المالك، **يضيّق** ما أتاحته التكملةُ الثالثة.
**بلا ترحيل، وبلا حرفٍ في الخادم** (المُعدَّلُ الوحيدُ تحت `server/` اختبار).

#### الواقعة
زرُّ «معاينة جديدة» في صفحة المريض (`PatientMedicalExams.tsx`) يمرّر
`preferSpecialty` من اختصاصٍ **منتظرٍ يملكه الطبيبُ نفسُه**
(`pending.find((p) => mySpecialties.includes(p))`)، **وإلّا `null`**. فطبيبُ
أطرافٍ أمام مريضٍ طلبُه المنتظر **مساندٌ** يفتح نافذةً بلا مرساة، ثمّ يستقرّ
الاختصاصُ على «أطراف» — اختصاصِه الوحيد. فكان المخرجُ القديم يقرأ ذلك
**تبديلاً**: يُرسَل معرّفُ طلب المساند ومعه رايةُ التصحيح، **فيُنقَل طلبٌ
مستقلٌّ لم يبدّله أحد** إلى خيط الأطراف، وتُختَم عليه معاينةٌ (ترِكر ٠٢٨) لا
تُصحَّح بعدها، ويُغلق طلبُ مراجعته، وتُولَد له متابعةُ شراء لجهازٍ لم ينظر فيه
أحد. **وذاك تخمينُ نيّةٍ لا قراءتُها.** (سجّله تدقيقُ Codex على ٣١٧ بوصفه P1.)
**والسجلُّ** (`PatientsList.tsx`) لا يفتح إلّا باختصاصٍ محدَّد، فلا يمرّ منه.

#### القاعدة الآن
- **فُتحت على اختصاص جهازٍ معلوم ثمّ بدّله الطبيبُ صراحةً إلى الآخر** ⟶
  التصحيحُ كما هو بحرفه (التكملةُ الثالثة، والقسمان ج و و في الاختبار الخالص).
- **فتحٌ عامّ وللاختصاص المختار طلبُه المنتظر** ⟶ يُوقَّع عليه كما كان.
- **فتحٌ عامّ ولا طلبَ له** ⟶ **معاينةٌ بلا جهاز**، ولا يُمَسّ طلبُ الاختصاص
  الآخر لا نوعاً ولا حالةً ولا طلبَ مراجعة، **ولا يُعرَض خياراً أصلاً**.

والتغييرُ سطرٌ واحد في `exam_episode_choice.ts`: مرشَّحو التصحيح
(`otherSpecialty`) صاروا من `switchedFromOpened` وحدها، والباقي مصفوفةٌ فارغة.
**ومسارُ «معايناتي» (`fixedEpisodeId`) لم يُمَسّ بحرف.**

#### والخادمُ لم يحتج حارساً
النيّةُ صريحةٌ أصلاً: `POST /api/medical/patients/:id/exams` لا يصحّح نوعاً
إلّا بـ`retypeDeviceEpisode: true` **مع** `deviceEpisodeId` (٤.y). فالإصلاحُ
**هو الحمولةُ التي ترسلها الشاشة** — والخادمُ ينفّذ نيّةً صريحة كما كان.

#### وثلاثُ طبقاتٍ تُثبته معاً
لأن الإصلاحَ في الشاشة، لا يكفي اختبارٌ حيٌّ وحده — الثلاثةُ سلسلةٌ واحدة:
- **القرارُ الخالص** (`test:exam-episode-choice`): الفتحُ العامّ يُنتج حمولةً
  **بلا معرّفٍ وبلا راية** وبلا خيارات — **وانقلب عقدُ ثلاثة أقسام** (أ ·
  ب/٦ · د) بقرار المالك، كلُّها موسومةٌ صراحةً. وتغطيةُ التصحيح المرسى **لم
  تنقص**: القسمان ج (٨–٢٤) و و (٣٤–٣٧) يبقيان بحرفهما.
- **عقدُ الشاشة** (`test:exam-identity-ui`، ٨ز.١ و٨ز.١أ): تعبيرُ
  `otherSpecialty` مطابقٌ نصّاً للصيغة المرساة وحدها، **والمخرجُ القديم مُزالٌ
  نصّاً** فلا يعود بصياغةٍ أخرى.
- **الأثرُ الحيّ** (`test:exam-identity` القسم **خ**، ١١٦–١٢٣.أ): شكلُ المالك
  بالحرف — خيطُ أطرافٍ وخيطُ مساند، وطلبُ مساندٍ مستقلٌّ منتظر، وتوقيعُ معاينةِ
  أطرافٍ بالحمولة الجديدة ⟶ المعاينةُ **مختومةٌ بلا جهاز**، وصفُّ طلب المساند
  **مطابقٌ بايتاً** نوعاً وحالةً، وطلبُ مراجعته **مطابقٌ بايتاً** معلَّقاً بلا
  معاينة، ولا متابعةَ وُلدت له، والخيطان والعَلَمان والشارةُ وصفُّ قائمة الطبيب
  كما كانت. **والبندان ١٢٣/١٢٣.أ يُثبتان أن السيناريو ليس فارغاً**: الحمولةُ
  القديمة نفسُها (معرّفٌ + راية) **تنقل الطلبَ فعلاً** إلى خيط الأطراف.

**ومُتحقَّقٌ بالعكس**: إعادةُ المخرج القديم تُسقط **٩ بنودٍ خالصة** (١ · ٢ ·
٣ · ٥ · ٦ · ٢٥ · ٢٦ · ٢٨ · ٢٩) و**بندَي عقد الشاشة** — ولا شيءَ غيرها؛
وأقسامُ التصحيح المرسى (ج · هـ · و · ز · ح) تبقى خضراء.

### تكملةٌ خامسة — والطلبُ **المُحال** يتبع عمليتَه كما يتبعها المعلَّق (٢٠٢٦-٠٩-١٧)

**الحيُّ يتبع عمليتَه، معلَّقاً كان أو مُحالاً.** فرعُ
`claude/retype-escalated-review-request`. **بلا ترحيل، وبلا حرفٍ في الواجهة.**

#### الواقعة (مُعادٌ إنتاجُها حيّاً على النقاط الحقيقية قبل أيّ تعديل)
`retagReviewRequestsForRetypedEpisode` كانت تقصر الاتّباعَ على `status =
'pending'` وحدها. فطلبُ مراجعةٍ **مُحال** (`escalated`) مرسىً إلى حلقةٍ يبدّل
الطبيبُ اختصاصَها كان يبقى على **اختصاصه وخيطه القديمين**، بينما تنتقل
عمليتُه إلى الخيط الجديد. وأثرُه المقروء:

```
الحلقة بعد التصحيح: case_type = prosthetic · status = examined
الطلبُ المُحال:      service_type = medical_support · status = escalated · exam_id = null
قائمةُ عمل الطبيب:   صفٌّ باقٍ في **المساند** لمريضٍ صارت عمليتُه أطرافاً
```

فلا يُغلقه توقيعُ المعاينة — `closeRequestsAwaitingExam` تطابق `service_type`
— ويبقى **صفّاً شبحاً حيّاً إلى الأبد في طابور الاختصاص الذي غادرته عمليتُه**.

#### القاعدة الآن — مجموعةٌ واحدة لا اثنتان
`pending` **و**`escalated`: وهما بعينهما الحالتان اللتان تقرؤهما أصلاً
`closeRequestsAwaitingExam` و`pendingFullRequestsFor` و
`lockSpecialtyLevelQueueRequestsTx` — أي **الطلبُ الذي ما زال ينتظر معاينتَه
الكاملة**. فمجموعةُ «يتبع» صارت مجموعةَ «يُغلَق» نفسَها، ولا تنحرف إحداهما عن
الأخرى يوماً.

**والمحسومُ نهائياً لا يتبع**: `examined` · `approved` · `returned` ·
`cancelled` — طلباتٌ انتهت، ونقلُها إلى اختصاصٍ آخر يزوّر تاريخاً.

#### والمتحرّكُ **مكانُ الطلب لا محتواه**
ثلاثةُ أعمدةٍ لا غير: `service_type` · `case_id` · `updated_at`.
**و`status` لا يُمَسّ في إعادة الوسم** — المُحالُ يبقى مُحالاً، ثمّ **يُغلقه
مسارُ الإغلاق القائم نفسُه** بالمعاينة الصحيحة (`examined` + `exam_id`).
**ولا `decision` ولا `decided_by` ولا `decided_at` ولا `doctor_note` ولا
`requested_path` ولا `review_kind`** — قرارُ الطبيب وسببُه وصاحبُه ووقتُه
شهادةٌ على ما جرى (درسُ ٤.r بحرفه).

**ولا قيدَ في القاعدة يضعف**: `medical_review_requests_decided_shape_check`
شرطُه على `status` وقرينَيه، وهذه لا تلمس أيّاً منها. **ولا فهرسَ تفرّدٍ
يتصادم**: الأربعةُ كلُّها `WHERE status = 'pending'`، والمُحالُ ليس منها.

#### والاسمُ لا يكذب
`retagPendingRequestsForRetypedEpisode` ⟶ `retagReviewRequestsForRetypedEpisode`
— دالّةٌ تُعيد وسمَ المُحال أيضاً لا يصحّ أن يقول اسمُها «المعلَّق». ثلاثةُ
مواضعَ لا غير (التعريفُ والاستيرادُ والنداء)، وبلا حرفٍ يتغيّر في سلوكها.

#### وما لم يُمَسّ
مسارُ الإغلاق (`closeRequestsAwaitingExam` بحرفه) · `retypeAwaitingEpisodeForExamTx`
· ختمُ المعاينة (٠٢٨) · `retireRetypedSourceCase` و`retireSupersededCase` ·
سحبُ نوع الحالة (٤.r) · إلغاءُ المعاينة (٠٦١) · التصحيحُ الإداريّ (٠٦٤) ·
المالُ بكلّ صوره · العلاجُ الطبيعي · الواجهةُ كلُّها · **وكلُّ ترحيلٍ من ٠٠١
إلى ٠٨١**. **وإعادةُ الإرسال المتزامنة لم تُعالَج في هذه التمريرة** (بطلب
المالك) — تبقى كما هي.

#### الاختبار
القسمُ **ذ** في `npm run test:exam-identity` (١٢ تأكيداً × اتجاهين — مساندٌ ⟶
أطراف وأطرافٌ ⟶ مساند): الفِكستشرُ **من النقاط الحقيقية وحدها** (إرجاعُ الطلب
الكامل ⟶ طلبٌ سريعٌ على الحلقة نفسِها ⟶ «يتطلّب معاينة كاملة» ⟶ `escalated`)،
**ولا صفَّ يُلفَّق بـSQL**؛ ثمّ العمليةُ تنتقل · والطلبُ يتبعها اختصاصاً
وخيطاً · ويُغلَق بالمعاينة الصحيحة بمعرّفها · والأعمدةُ الستّةُ مطابقةٌ بايتاً ·
**ولا طلبَ حيّاً يبقى في الاختصاص السابق** (القاعدةُ وقائمةُ عمل الطبيب وشارةُ
الانتظار، ثلاثتُها) · والمُرجَعُ (`returned`) لم يتبع.

**ومُتحقَّقٌ بالعكس**: إعادةُ `status = 'pending'` تُسقط **١٠ تأكيداتٍ بالضبط**
(١٢٨ · ١٢٩ · ١٣١ · ١٣٢ · ١٣٣ في كلا الاتجاهين) — **كلُّها من القسم الجديد**،
وبنودُ الفِكستشر (١٢٤–١٢٧ · ١٣٠ · ١٣٤ · ١٣٥) تبقى خضراء في الحالتين، فالاختبارُ
يقيس الإصلاحَ ولا يخلطه بإعداده. وحزمُ `exam-request-cancel` و
`return-to-purchase` و`medical-review` و`case-disposal` و`exam-idempotency` و
`exam-cancel-safety` و`reception-agreement` و`followup` **بمجموعة فشلٍ متطابقة
تماماً قبل التغيير وبعده** (`followup` بأربعةٍ سابقةٍ لهذه المهمّة)، و
`npx tsc --noEmit` ٣٥ خطأً قبل وبعد **متطابقةً سطراً بسطر**.

### تكملةٌ سادسة — إعادةُ الإرسال المتزامنة حين لا خيطَ للاختصاص الهدف (٢٠٢٦-٠٩-١٧)

**خاسرُ سباقِ مفتاح التطابق يُعيد معاينةَ الفائزة نفسَها — لا يردّ تعارضاً
لمجرّد أن معرّفَ الخيط المتوقَّع عند بدايته كان فارغاً ثمّ أنشأته الفائزة.**
فرعُ `claude/retype-race-replay`. **بلا ترحيل، وبلا حرفٍ في القاعدة ولا في
الشاشة** — الملفّان المُعدَّلان اثنان وملفُّ اختبار.

#### الواقعة (مُعادٌ إنتاجُها حيّاً على النقاط الحقيقية قبل أيّ تعديل)
الاستعلاماتُ فتحت طلبَ **مساند**، والطبيبُ يصحّحه إلى **أطراف** — ولا خيطَ
أطرافٍ على الملفّ بعد. فالخيطُ الهدفُ يُفتَح **داخل معاملة التوقيع**
(«تكملةٌ ثانية» أعلاه)، ومعرّفُه لا يوجد قبل بدء المعاملة: فطلبان متزامنان
بنفس المفتاح يبدآن **كلاهما** و`caseId = null` — لا لأن الطلبَ بلا خيط، بل
لأن خيطَه **لم يكن قد وُلد بعد** حين قرأه كلٌّ منهما.

فالفائزةُ تفتح الخيطَ وتنقل الطلبَ وتكتب المعاينةَ بـ`case_id` الجديد،
والخاسرةُ تعود إلى قاعدة إعادة الإرسال (٠٧٤) بهويّةٍ **بائتة**: `caseId`
عندها ما زال `null` بينما الصفُّ المحفوظ يحمل الخيطَ الذي وُلد بعدها. فتُقرأ
«هويّةٌ مختلفة» ويُردّ **٤٠٩ `idempotency_conflict`** على إعادةِ إرسالٍ
مشروعةٍ تماماً — والموظّفُ يقرأ «مفتاح الحفظ هذا استُعمل من قبل لمعاينةٍ
بمحتوًى مختلف» وهو لم يُرسل إلّا معاينتَه هو مرّتين.

**والبياناتُ كانت سليمةً في كلّ الأحوال** (معاينةٌ واحدة، متابعةٌ واحدة،
انتقالٌ واحد، بلا أثرٍ تجاريٍّ مكرَّر) — **الردُّ وحده كان كاذباً**.

#### والعلاجُ إعادةُ قراءةٍ ضيّقة بعد الخسارة، لا تخفيفُ قاعدة
`findReplayableExam` — القاعدةُ الصارمة — **لم يتغيّر فيها حرف**، ولا
`examIdentityMatches`، ولا صار الخيطُ الفارغ تطابقاً مفتوحاً. بل غلافٌ رقيق
`replayAfterLostRace` (`server/medical/store.ts`) لا يفعل شيئاً إطلاقاً إلّا
حين تَرمي القاعدةُ الصارمة تعارضاً — **وذاك هو إشارةُ الخسارة**: صفٌّ بهذا
المفتاح موجودٌ ولم تكتبه هذه المحاولة. وعندها وحدها:

① تُقرأ الهويّةُ القانونية الحالية **لنفس عملية الجهاز بعينها** من صفّها هي
(`episodeCaseOf` في `server/device_episodes/store.ts`، بجوار `episodeBranchOf`
ونمطِه) — **لا من صفّ المعاينة الفائزة**، فلا تدور المقارنةُ على نفسها:
المصدرُ مستقلٌّ عن الشيء الذي يُقارَن به. ② ثمّ تُعاد **القاعدةُ الصارمةُ
نفسُها** على تلك الهويّة — لا مقارنةٌ ثانيةٌ أليَن ولا نسخةٌ منها. ③ وإن
تعارضت ثانيةً صعد التعارضُ الأصليُّ كما هو.

**وثلاثةُ شروطٍ للشكل، مجتمعةً**: `caseId === null` (الهويّةُ التي قد تكون
بائتةً بهذا المعنى وحدها) · `retypeEpisode === true` (نيّةُ تصحيحٍ صريحة، لا
استنتاج) · و`deviceEpisodeId` حاضرٌ (**عمليةٌ بعينها بالمعرّف**، لا ترشيح).
وأيُّ شكلٍ آخر يمضي بهويّته كما هي حرفاً بحرف. **و`episodeCaseOf` مشروطةٌ
بنوع الخيط** فلا تُرجع خيطاً من اختصاصٍ آخر: اختلافُ النوع ⟶ `null` ⟶ تبقى
الهويّةُ كما هي ⟶ تعارضٌ صريح، وهو الصواب.

#### والصرامةُ لم تُمَسّ
نفسُ المفتاح مع **مريضٍ مختلف · طبيبٍ مختلف · فرعٍ مختلف · اختصاصٍ مختلف ·
عمليةِ جهازٍ مختلفة · محتوًى مختلف** يبقى تعارضاً كما هو اليوم — مُثبَتٌ
حيّاً بالستّة.

#### والمسارُ السريعُ في النقطة لم يُمَسّ
`POST /api/medical/patients/:id/exams` ما زال ينادي `findReplayableExam`
مباشرةً كما كان. الغلافُ يُنادى من **داخل `createExam` وحدها** — في فحصها
المسبَّق وفي مسارَي خسارة السباق (`ExamEpisodeStaleError` و`23505` على
`uq_medical_exams_idempotency_key`). فالطلبُ العاديّ لا يمرّ من هذا المسار
بحرف: **لا استعلامَ إضافيّ، ولا سلوكَ يتغيّر.**

**وقياسٌ حيٌّ لا تقدير**: ٢٠٠٠ سباقٍ حقيقيّ (طلبان متزامنان لكلٍّ، بقاعدةٍ
محلّية) ⟶ **٢٠٠٠ × (٢٠٠ | ٢٠٠)** وصفرُ شذوذٍ في البيانات. وقبل الإصلاح كان
الخاسرُ يُردّ ٤٠٩ بمعدّل ≈١/٦٠. والنافذةُ النظرية الباقية في المسار السريع
بالنقطة (لو التزمت الفائزةُ بين `findCaseFor` وفحصِ التطابق) **لم تقع مرّةً
واحدة** في تلك الألفين — وتُركت في هذه التمريرة بلا مساس، **وهي باقيةٌ كما هي
إلى اليوم**.

#### وما لم يُمَسّ
ختمُ المعاينة (٠٢٨) · `retypeAwaitingEpisodeForExamTx` و`ensureActiveCaseTx` ·
`retireRetypedSourceCase` · هويّةُ الجهاز في التوقيع (٤.p) · إغلاقُ طلبات
المراجعة · المالُ بكلّ صوره · الصيانة · العلاجُ الطبيعي · الشاشةُ كلُّها ·
**وكلُّ ترحيلٍ من ٠٠١ إلى ٠٨١**.

#### الاختبار
قسمان جديدان في `npm run test:exam-idempotency` (**ط**: السباقُ الحقيقيّ —
طلبان في اللحظة نفسِها، نفسُ العملية، نفسُ المفتاح، ولا خيطَ هدفٍ قبل البداية
⟹ الطلبان ينجحان · وكلاهما يُعيد المعرّفَ نفسَه · ومعاينةٌ واحدة · ومتابعةٌ
واحدة على الخيط والعملية الصحيحين · وخيطُ أطرافٍ **واحدٌ** وُلد · والعمليةُ
انتقلت **مرّةً واحدة** بمعرّفها وبتسلسلها · وسطرُ تدقيقٍ واحد · وصفرُ أثرٍ
ماليٍّ أو تصنيعيّ · وإعادةُ الإرسال بعده تعمل كما كانت. و**ي**: الستّةُ
تبقى تعارضاً، ولا صفَّ ثانٍ، والطلبُ المستقلُّ الثاني لم يُمَسّ).

**ومُتحقَّقٌ بالعكس**: تعطيلُ إعادة القراءة يُسقط **بندين بالضبط** (ط٢ وط٣)
ولا شيءَ غيرهما. و`npx tsc --noEmit` ٣٥ خطأً قبل وبعد **متطابقةً سطراً
بسطر**، و`npm run build` ينجح، و`git diff --check` نظيف. وإحدى وعشرون حزمةَ
اختبارٍ مجاورة بمجموعةِ فشلٍ متطابقة تماماً قبل التغيير وبعده.

> **⚠ فشلٌ مسبَقٌ كان في هذا الملفّ نفسِه — وقد عولج في التكملة السابعة
> أدناه.** البندان أ١/أ٢ (تقاربُ سبع طلباتٍ متزامنة) كانا يفشلان في **٦ من
> كلّ ١٤ تشغيلة على `main`** حين تلتزم الفائزةُ بين فحصِ التطابق في النقطة
> و`resolveExamEpisode` فيها — فيُردّ الخاسرُ **`ExamEpisodeStaleError`
> بالنقطة** قبل أن يبلغ `createExam` أصلاً، ولا فحصَ إعادةِ إرسالٍ هناك.
> **رمزٌ مختلف وشكلٌ مختلف**، ولذلك لم يُمَسّ في هذه التمريرة بطلب المالك —
> ثمّ أُغلق بتمريرةٍ مستقلّة كما يجب.

### تكملةٌ سابعة — البياتُ في النقطة يُعيد معاينةَ الفائزة (٢٠٢٦-٠٩-١٧)

**والمخرجُ الثاني أُغلق**: خاسرُ سباقٍ يُردّ **عند فحص حالة الجهاز في النقطة**
— قبل أن يبلغ `createExam` أصلاً — صار يُعيد معاينةَ الفائزة بدل ٤٠٩.
فرعُ `claude/route-stale-replay`. **بلا ترحيل، وبلا حرفٍ في القاعدة ولا في
الشاشة** — ملفُّ نقاطٍ واحد وملفُّ اختبار.

#### الواقعة — وهي **الفشلُ المسبَق أ١/أ٢ بعينه**
طلبان **متطابقان** بنفس المفتاح. الأوّلُ يلتزم **بين** فحصِ التطابق السريع في
النقطة (`routes.ts:558`) و`resolveExamEpisode` بعده (`routes.ts:601`). فيقرأ
الثاني `medical_exams` ولا يجد صفّاً (الفائزةُ لم تلتزم بعد)، ثمّ يقرأ الجهازَ
فيجده `examined` ⟶ **٤٠٩ `device_episode_stale`** («تغيّرت حالة طلب الجهاز»)
**قبل أن يبلغ `createExam`**، فلا يمرّ بحزام إعادة الإرسال الذي وُضع هناك في
التكملة السادسة.

وهو بعينه ما كان يُفشل البندين **أ١/أ٢** (تقاربُ سبع طلباتٍ متزامنة) في
`server/medical_exam_idempotency.test.ts` بمعدّل **٦ من كلّ ١٤ تشغيلة على
`main`** — وهو ما رصده تدقيقُ Codex على ٣٢٣ بوصفه P2.

#### والعلاجُ إعادةُ تحقّقٍ بالقاعدة الصارمة القائمة، لا تخفيفُها
`findReplayableExam` **لم يتغيّر فيها حرف**، ولا `examIdentityMatches`، ولا
`replayAfterLostRace` (٦)، ولا المسارُ السريعُ في النقطة. المُضافُ غلافٌ رقيق
`replayAfterStaleEpisode` في `server/medical/routes.ts` **لا يُنادى إلّا على
`ExamEpisodeStaleError` وحدها**: يُعيد التحقّق **بالمفتاح نفسِه وبمطابقة
الهويّة والمحتوى الصارمة نفسِها**، فإن وُجدت المعاينةُ المطابقة تُعاد كما هي
بـ`created: false` — **وبصفر كتابة**.

**وإلّا يبقى الرفضُ الأصليُّ بحرفه**: لا صفَّ بهذا المفتاح (`null`)، أو صفٌّ
بهويّةٍ أو محتوًى مختلف (**تعارض**) — كلاهما ⟶ يُردّ البياتُ كما كان.
**والالتباسُ** (`device_episode_ambiguous`) **وفرعُ الجهاز**
(`device_episode_branch`) لا يمرّان بهذا المسار إطلاقاً — ليسا شكلَ سباقٍ على
مفتاحٍ واحد. **وخطأٌ غيرُ متوقَّع يصعد كما هو** ولا يُبتلَع في رفضٍ يخفيه.

#### والتوقيتُ في الاختبار **حتميٌّ لا احتمال**
بوّابةٌ **اختباريةٌ على عميل القاعدة في ملفّ الاختبار وحده** (`pool.query`،
تُستعاد في `finally` مهما وقع — **ولا حرفَ في شيفرة الخادم**) تحجز **أوّل**
قراءةٍ للأجهزة المنتظرة بعد التسليح، وهي استعلامُ `awaitingExamEpisodesForCase`
الذي يناديه `resolveExamEpisode` — **يميّزه `ORDER BY sequence_number`** عن
قراءة فرع الجهاز (`episodeBranchOf`) التي تسبق الفحصَ السريع. فتقف الخاسرةُ
**بين الفحصين بالضبط**، وتمضي الفائزةُ حتى تلتزم، ثمّ تُحرَّر.

#### الاختبار
قسمان جديدان في `npm run test:exam-idempotency`:

- **ك** — السباقُ الحتميّ: الخاسرةُ تنجح · وتُعيد المعرّفَ نفسَه بـ
  `created:false` · **وبصمةُ الملفّ قبلها وبعدها سواء** (صفرُ كتابة) ·
  معاينةٌ واحدة · متابعةٌ واحدة · سطرُ تدقيقٍ واحد · **والجهازُ المستقلّ
  الثاني لم يُمَسّ** (منتظِرٌ بخيطه وتسلسله) · والمعاينةُ مختومةٌ على الجهاز
  المقصود وحده · وصفرُ أثرٍ ماليٍّ أو تصنيعيّ.
- **ل** — **وإلّا يبقى الرفض**: مفتاحٌ جديد على جهازٍ استُهلك ⟶ ٤٠٩
  `device_episode_stale` · **وسباقٌ بمحتوًى مختلف** يبلغ المخرجَ نفسَه ⟶ يُردّ
  ولا يُسرَّب معرّفُ معاينة الفائزة، وتبقى معاينةٌ واحدةٌ بتشخيص الفائزة ·
  ونفسُ المفتاح بمحتوًى مختلف، ومريضٌ مختلف بنفس المفتاح ⟶ تعارضٌ كما كان.

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات**: إزالةُ الإصلاح وحده تُسقط **ثلاثةَ بنودٍ
بالضبط** (ك٢ · ك٣ · «created:false») في كلّ تشغيلة — **والقسمُ ل يبقى أخضرَ
في الثلاث**، فهو يقيس ما لم يتغيّر لا ما تغيّر.

**والفشلُ المسبَق زال**: ١٨ تشغيلةً متتالية بالإصلاح ⟶ **صفرُ فشل** (بما فيها
أ١/أ٢)، مقابل ٦ فشلٍ من ١٤ على `main`. و`npx tsc --noEmit` ٣٥ خطأً قبل وبعد
**متطابقةً سطراً بسطر**، و`npm run build` ينجح، و`git diff --check` نظيف،
وأربعَ عشرةَ حزمةَ اختبارٍ مجاورة خضراءُ قبل وبعد.

#### وما لم يُمَسّ
القاعدةُ والترحيلات (٠٠١–٠٨٢) · الشاشةُ كلُّها · `findReplayableExam` و
`examIdentityMatches` و`replayAfterLostRace` · المسارُ السريعُ في النقطة ·
`createExam` وحُرّاسُها · `resolveExamEpisode` · ختمُ المعاينة (٠٢٨) ·
المالُ بكلّ صوره · العلاجُ الطبيعي · وطلبُ الدمج #318.

### تكملةٌ ثامنة — وبياتُ النقطة يُحدِّث هويّةَ الخيط أيضاً (٢٠٢٦-٠٩-١٨)

**المسارُ الذي فتحته التكملةُ السابعة كان يقرأ هويّةً بائتة.** فرعُ
`claude/replay-stale-episode-identity`. **بلا ترحيل، وبلا حرفٍ في الواجهة ولا
في القاعدة.**

#### الواقعة — وهي تقاطعُ التكملتين السادسة والسابعة
السادسةُ عالجت خسارةَ السباق **داخل `createExam`** حين لا خيطَ هدفَ للاختصاص
الجديد: الهويّةُ تُقرأ قبل أن يُولَد خيطُها، فتُحدَّث بـ`identityAfterLostRace`
ثمّ تُعاد `findReplayableExam` الصارمة. والسابعةُ عالجت خسارةً **في النقطة**
(`ExamEpisodeStaleError` قبل بلوغ `createExam`) لكنّها نادت
`findReplayableExam` **مباشرةً بالهويّة كما هي**.

**فتقاطعُهما ثغرةٌ ثالثة**: تصحيحُ نوعٍ إلى اختصاصٍ **لا خيطَ له بعد**،
والخاسرةُ تُردّ في النقطة لا في المخزن. فـ`replayContent.caseId` يبقى `null`
(قُرئ قبل السباق) بينما صفُّ الفائزة يحمل الخيطَ الذي وُلد في معاملتها ⟶
«هويّةٌ مختلفة» ⟶ **٤٠٩ `device_episode_stale` على إعادةِ إرسالٍ مشروعة**.
أُعيد إنتاجُه **حتميّاً** على `eb9b0f4` قبل أيّ تعديل (القسم م أدناه: ثلاثُ
تأكيداتٍ تسقط، وما عداها أخضر).

#### والعلاجُ إعادةُ استعمالٍ لا قاعدةٌ ثانية
`replayAfterLostRace` (`server/medical/store.ts`) **صُدِّرت، ولم يتغيّر فيها
حرفٌ آخر** — ولا في `identityAfterLostRace` ولا `examIdentityMatches` ولا
`findReplayableExam`. و`replayAfterStaleEpisode` في النقطة صارت تناديها بدل
`findReplayableExam` مباشرةً. **فمسارا الخسارة يتقاسمان قاعدةَ تحديث هويّةٍ
واحدة**، ولا نسخةَ موازية في النقطة تنحرف عنها يوماً.

**والنيّةُ تُمرَّر صريحةً**: `retypeEpisode: retypeDeviceEpisode` من راية
الطلب نفسِها — لا من المتغيّر المحلّي (لم يُسنَد: المحاولةُ رمت)، ولا
استنتاجاً. وهي أحدُ شروط إعادة القراءة الثلاثة الموروثة بحرفها: `caseId ===
null` · نيّةُ تصحيحٍ صريحة · **وعمليةُ جهازٍ بعينها بالمعرّف**. وأيُّ شكلٍ
آخر يمضي بهويّته كما هي.

**والصرامةُ لم تُمَسّ**: بعد إعادة القراءة تُطبَّق `findReplayableExam`
نفسُها — فمريضٌ آخر · طبيبٌ آخر · فرعٌ آخر · اختصاصٌ آخر · عمليةُ جهازٍ
أخرى · **أو محتوًى مختلف** يبقى تعارضاً كما هو اليوم. وغيرُ إعادةِ الإرسال
المطابقة تماماً يبقى ٤٠٩ بحرفه.

#### وما لم يُمَسّ
القاعدةُ والترحيلات (٠٠١–٠٨٢) · الواجهةُ كلُّها · الصيانةُ بكلّ مسارها (٤.ي،
٤.ز — **وصفرُ ملفٍّ منها في الفرق**) · تذكرةُ الإرسال · `createExam` وحُرّاسُها ·
`resolveExamEpisode` · `retypableEpisodeForExam` · المسارُ السريع قبل السباق ·
ختمُ المعاينة (٠٢٨) · المالُ بكلّ صوره · العلاجُ الطبيعي.

#### الاختبار
قسمٌ جديد **م** في `npm run test:exam-idempotency` (**١١٩ تأكيداً** بعد
الإضافة): البوّابةُ الحتميّة نفسُها من التكملة السابعة، **بنمطٍ ثانٍ** —
استعلامُ `retypableEpisodeForExam` وحده، وهو ما يقرؤه `resolveExamEpisode` في
هذا الفرع بالضبط (`awaitingExamEpisodesForCase` لا تُقرأ فيه إطلاقاً). فتقف
الخاسرةُ **بين** الفحص السريع وفحصِ صلاحية التصحيح، وتمضي الفائزةُ حتى تلتزم.

ويُثبت: الخيطُ الهدفُ **غيرُ موجودٍ قبل السباق** · والفائزةُ تلده في معاملتها ·
**والخاسرةُ تنجح وتُعيد المعاينةَ نفسَها** بـ`created:false` · **وبصمةُ الملفّ
قبلها وبعدها سواء** (صفرُ كتابة) · معاينةٌ واحدة · متابعةٌ واحدة · سطرُ تدقيقٍ
واحد · خيطُ أطرافٍ واحدٌ وُلد · وعمليةُ الجهاز صفٌّ واحدٌ بمعرّفه انتقل مرّةً
واحدة · ولا أثرَ ماليٍّ ولا تصنيعيّ. **ومعه الصرامةُ على الشكل نفسِه**: سباقٌ
بمحتوًى مختلف ⟶ ٤٠٩ ولا يُسرَّب معرّفُ معاينة الفائزة، ومريضٌ آخر بنفس المفتاح
⟶ تعارض.

**ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على حدة**: إعادةُ النداء إلى `findReplayableExam`
تُسقط **ثلاثَ تأكيداتٍ بالضبط** (م٣ · م٤ · `created:false`)، وإسقاطُ تمرير
`retypeEpisode` وحده يُسقط **الثلاثةَ عينها** — فكلُّ نصفٍ لازمٌ بذاته، ولا
شيءَ غيرها يسقط في الحالتين.

**والتذبذبُ زال**: ثمانُ تشغيلاتٍ متتالية على قواعد طازجة ⟶ **صفرُ فشل** (ومنها
أ١/أ٢). و`tsc` ٣٥ = ٣٥ متطابقةً، و`npm run build` ينجح، و`git diff --check`
نظيف. وخمسَ عشرةَ حزمةً مجاورة: ثلاثَ عشرةَ خضراء، و`followup`/`decision-queue`
بأربعةٍ لكلٍّ — **بمجموعةِ فشلٍ متطابقةٍ حرفياً** على الشجرة النظيفة (`diff`
آليّ بمقارنة `git stash`)، وهي الإخفاقاتُ المسبَقة المسجَّلة في التكملة الثانية.

---

## 4.z أوامرُ صيانةٍ متزامنة بلا حدّ (ترحيل ٠٨٢ — ٢٠٢٦-٠٩-١٧)

**أيُّ عددٍ من أوامر الصيانة مفتوحةٌ معاً — حتى لنفس الجهاز ونفس القطعة ونفس
الخبير.** المرحلةُ الأولى من تبسيط الصيانة، فرعُ
`claude/maintenance-concurrent-orders`. **ولا تنبيهَ ولا ضمانَ ولا حرفَ في
الواجهة** — قرارُ المالك: هذه المرحلةُ وحدها.

### الواقعة
ترحيلُ ٠٥١ قرّر «صيانةٌ مفتوحةٌ واحدة لكلّ جهاز»، وكان معقولاً يومَه. لكنّ
العملَ الحقيقيّ يفتح على الجهاز الواحد أكثرَ من أمرِ صيانةٍ في آنٍ واحد —
مريضٌ يكسر قالبَه مرّتين في أسبوع، أو يُصلَح جزءان من طرفه بأمرين منفصلين.
وكلٌّ منهما عملٌ حقيقيٌّ بأمره وزيارته وأجره وسجلّه ومراحله، ومنعُ الثاني كان
يجبر الاستقبالَ على تأجيل عملٍ وقع فعلاً أو حشرِه في أمرٍ لا يخصّه.

### ما رُفع — الصيانةُ وحدها
**فهرسان من ٠٥١، كلاهما `WHERE … purpose = 'maintenance'`**:
`uq_pwo_one_open_maint_per_episode` · `uq_pwo_one_open_legacy_maint`.
**وحارسُهما في منطق التطبيق حُذف معهما** — فلا حارسٌ بلا ضمان، ولا ضمانٌ بلا
حارس:
- `createMaintenanceOrderWithVisit` فقدت نداءَ `hasOpenOrderTx` بالكامل.
- **و`buildCompetitionFilter` فقدت فرعَ الصيانة، ولم يعد التوقيعُ يقبل النوع**
  (`purpose: "initial_build"` حصراً في `hasOpenOrder`/`hasOpenOrderTx`). فرعٌ
  ميّتٌ يبقى مكتوباً تعود به القاعدةُ يوماً بنداءٍ واحدٍ بالسهو.
- و`createWorkOrderForExisting` صار حارسُها مشروطاً `purpose === "initial_build"`
  — فالصيانةُ من البابِ العامّ نفسِه تمرّ بلا حدّ أيضاً.

### وبناءُ الجهاز الأوّليّ لم يُمَسّ بحرف
`uq_pwo_one_open_build_per_episode` و`uq_pwo_one_open_legacy_build` (٠٧٣)
قائمان، وشرطاهما في `buildCompetitionFilter` **بحرفهما**، وحارسُ
`createWorkOrderForExisting` كما كان.

> **ملاحظةُ تسمية**: سمّى المالكُ `uq_pwo_one_open_build_per_service` (٠٥١)
> اسماً للقيد الذي لا يُمَسّ — **وذاك أُسقط في ٠٧٣ وحلّ محلَّه هذان**.
> فالمحفوظُ هو الحمايةُ لا الاسم، وترحيلُ ٠٨٢ لا يذكرهما إلّا في تعليقه:
> لا يُنشئهما ولا يُسقطهما ولا يُعيد بناءهما.

### وما لم يتغيّر
المحاسبةُ بحرفها — `postMaintenanceFee` **لم تُمَسّ**، تُنادى من داخل
`createMaintenanceOrderWithVisit` كما كانت دائماً، فكلُّ أمرٍ يقيّد كلفةَ حالته
و`patients.total_cost` وقيدَ دفتره **في معاملته هو**: يقع معه أو يرتدّ معه، ولا
يُقيَّد مبلغُ أمرٍ مرّتين ولا يُحسَب أمرٌ على أمرٍ آخر. والشروطُ التجاريةُ
المُهيكَلة (٠٦٩) · الصلاحيةُ (`canCompleteMaintenance`) · حسمُ نوع الخدمة ·
حُرّاسُ `resolveDeviceTargetTx` (الجهازُ المسلَّم وحده يُصان) · بيعُ الجزء (٤.ك)
· مسارُ المعاينة · العلاجُ الطبيعي · واتساب · وكلُّ ترحيلٍ من ٠٠١ إلى ٠٨١.

### الترحيل
**٠٨٢ فقط** — إسقاطُ فهرسين لا أكثر، idempotent. **وإسقاطُ فهرسٍ ليس حذفَ
بيانات**: الفهرسُ مشتقٌّ من الجدول، وإسقاطُه يرفع **منعاً** ولا يمحو صفّاً —
فما كان مسموحاً يبقى مسموحاً وما كان ممنوعاً يصير مسموحاً. **وبلا `DROP TABLE`
ولا `DROP COLUMN` ولا `DELETE` ولا `UPDATE` ولا عمودٍ جديد**، ولا مسٍّ لترحيلٍ
من ٠٠١ إلى ٠٨١. ولا جدولَ جديداً، فلا شأنَ للقاعدة الملزمة في القسم ٨ هنا.

### ⚠ وانقلبت عقودُ خمسة أقسامٍ في اختباراتٍ قائمة
**بقرارِ المالك لا لتخضير اختبار** — كلُّها كانت تثبت القاعدةَ الملغاة نفسَها،
وكلُّها أُعيد توجيهُها لتثبت شيئاً حقيقياً بدل أن تُحذَف:
- `simplified_maintenance.test.ts` **ل** (كان «أمرٌ واحد بالضبط») و**ق٧** (كان
  «واحدٌ ينجح والآخر يرتدّ») ⟶ صارا يثبتان **عمليتين مستقلّتين كاملتين** وأنّ
  لا دفعةَ ولا قيدَ يُكتب مرّتين لعمليةٍ واحدة.
- `pending_charge.test.ts` **هـ٥/هـ٦** ⟶ كلتاهما تمرّ، ولكلٍّ أثرُها كاملاً.
  **و(هـ٣/هـ٤) للبيع لم تُمَسّا** ويبقيان «نجاحٌ واحد».
- `device_episode_integration.test.ts` (أربعُ محاولاتٍ على الجهاز نفسِه) ⟶
  أربعةُ أوامر مستقلّة، **وبلا تسريب خطأ قاعدةٍ خامّ** كما كان.
- `manufacturing/maintenance_service_type.test.ts` ⟶ صيانةٌ ثانية للخدمة نفسِها
  تمضي؛ **وموضوعُ الملفّ لم يتغيّر** — النوعُ يُحسَم بما طُلب صراحةً لا
  بتخمين، وهو ما تثبته الأوامرُ الثلاثة بأنواعها.
- `migration_051.test.ts` **لم يُعدَّل سلوكُه بحرف** — يُعيد تطبيق SQL ٠٥١ على
  قاعدته المعزولة فتبقى تأكيداتُه صحيحةً في عزلتها. أُضيف إليه رأسٌ يقول إنه
  **لقطةٌ تاريخية لا وصفٌ للمخطَّط اليوم** (٠٧٣ ثمّ ٠٨٢ غيّرا الصورة).

اختبارٌ دائم: `npm run test:maintenance-concurrent` (٣٤ تأكيداً حيّاً على
Postgres وعلى النقطة الحقيقية `/api/no-exam/maintenance`: حالةُ القاعدة
بفهارسها الأربعة وidempotency الترحيل · **سيناريو المالك بالحرف** — نفسُ الجهاز
ونفسُ القطعة ونفسُ الخبير ⟶ أمران مستقلّان مفتوحان · والمتزامنتان فعلاً ·
وجهازٌ قديم غير مسجَّل · **وبناءان ما زالا مرفوضين في القاعدة وفي منطق التطبيق
معاً** · وكلُّ أمرٍ بمبلغه والثابتُ المحاسبيّ · وحارسٌ معماريّ: لا فرعَ صيانةٍ
عاد إلى شرط المزاحمة ولا حارسَ أمرٍ مفتوحٍ عاد إلى كاتب الصيانة).
**والإصلاحان مُتحقَّقان بالعكس كلٌّ على حدة**: إعادةُ حارس التطبيق وحده تُسقط
١٨ تأكيداً (وتبقى أ١/أ٢ خضراوين)، وتعطيلُ الترحيل وحده يُسقط ١٨ أيضاً (وتبقى
ز١/ز٣ خضراوين) — فكلُّ نصفٍ لازمٌ بذاته.

> **ملاحظةٌ للتشغيل المحلّي**: قالبُ قاعدة الاختبار يجب أن يحمل ٠٨٢ (يُطبّقه
> المُشغِّل تلقائياً على الإنتاج وعند بدء الخادم). ومَن يشغّل على قالبٍ سابقٍ
> له سيرى `test:maintenance-simplify` و`test:maintenance-service` و
> `test:pending-charge` و`test:device-episode-integration` تفشل بالقاعدة
> القديمة — وذاك أثرُ القالب لا أثرُ الكود.

### تكملةٌ — المنعُ التقنيُّ للتكرار بتذكرةِ إرسال (بلا ترحيل — ٢٠٢٦-٠٩-١٨)

**رفعُ الحدّ أعلاه أزال ما كان يمسك التكرارَ صدفةً.** فأمرُ صيانةٍ ثانٍ لنفس
الجهاز كان يُردّ بالفهرس — لا لأن النظام يعرف أنها ضغطةٌ مكرَّرة، بل لأن
قاعدةً تجاريةً خاطئةً كانت تمنع عملاً مشروعاً. ولمّا رُفعت، صارت ضغطتان على
«حفظ» — أو إعادةُ إرسالٍ بعد انقطاع شبكة — تُنتجان **عمليتَي صيانةٍ حقيقيتين**:
أمران وزيارتان وقيدا كلفةٍ ودفعتان، **ومالٌ قُيِّد مرّتين لا يُصحَّح بعد وقوعه**.

**والعلاجُ تذكرةٌ لا قيدٌ تجاريّ يعود.** `submission_tokens` (ترحيل ٠٤٠، قائمٌ
منذ حادثة «انضافت مرتين» ٢٠٢٦-٠٦-٢٠) هو التفريقُ الدقيق: **نفسُ الضغطة** تحمل
الرمزَ نفسَه دائماً، و**عمليةٌ جديدة** تفتح النافذةَ فتسكّ رمزاً جديداً.
**فعمليتان متطابقتان تماماً — نفسُ الجهاز والقطعة والخبير والسعر — برمزين
مختلفين تنجحان كلتاهما، ولو تزامنتا.** ولا تنبيهَ «صيانةٌ متشابهة» ولا ضمانَ
ولا قيدَ تجاريٍّ واحد أُعيد.

**والحجزُ أوّلُ شيءٍ داخل معاملة `createMaintenanceOperation`** — قبل قفل
الحالة، وقبل إعادة التحقّق من الخبير، وقبل أمر العمل والزيارة وقيد الكلفة
والدفعة. فالتكرارُ **لا يبلغ المالَ أصلاً**، ويُردّ بـ٢٠٠ `duplicate: true`
ورسالةٍ تقول إن العملية مسجَّلةٌ سلفاً — **نجاحٌ آمنٌ لا فشل**: ما طلبه الموظّفُ
وقع فعلاً، والذي لم يقع تكرارُه. والنقطةُ تعود **قبل** `logAudit` وقبل
`createJournalForPayment` وقبل تدقيق الدفعة، فلا سطرَ ثانٍ في أيٍّ منها.

**ويرتدّ الحجزُ مع المعاملة.** فشلٌ في أيّ خطوةٍ بعده — حالةٌ غيرُ نشطة، خبيرٌ
خرج من الفرع، جهازٌ لم يعد مؤهَّلاً — يُرجع صفَّ التذكرة نفسَه، فتُعاد المحاولةُ
**بالرمز عينه** وتنجح. ولو التُزم الحجزُ خارج المعاملة لحُبس الرمزُ إلى الأبد
وقُرئت إعادةُ المحاولة «مسجَّلة سابقاً» **كذباً** على عمليةٍ لم تقع قطّ — وهو
بعينه درسُ `new_service` (`server/routes.ts`) الذي وقع فيه ثمّ صُحِّح.

**والتزامنُ من القاعدة لا من الشيفرة**: `token` مفتاحٌ أساسيّ، فالمحاولةُ
الثانية تنتظر الأولى على قفل الفهرس ثمّ تقرأ نتيجتَها — التزمت ⟶ صفرُ صفوف ⟶
تكرار، وارتدّت ⟶ تُدرِج هي وتمضي. **ولا قفلَ آخر بيدها حينئذٍ** (الحجزُ أوّلُ
عبارة)، فلا جمود.

**والشاشةُ تعيد استعمال الدالّتين المُختبَرتين** `nextSubmissionToken`/
`mintSubmissionToken` (`client/src/components/patient_service_launcher_logic.ts`)
اللتين تستعملهما «خدمة جديدة» — **لا نسخةَ ثانية**. والأثرُ يتبع **حالةَ**
`open` لا حدثَ فتحها (هذه النافذةُ قد تُركَّب مفتوحةً أصلاً فلا `onOpenChange`
يقع)، فنافذةٌ مفتوحة ⟶ رمزٌ واحدٌ يثبت، وإغلاقُها يصفّره.

**واختياريةٌ عمداً**: عقدُ `new_service` بحرفه — عميلٌ لا يرسل الرمز يبقى يعمل
كما كان بالضبط (بلا حجزٍ وبلا منع)، فلا نقطةٌ قائمة ولا اختبارٌ قائم ينكسر.
**وبيعُ الجزء (٤.ك) خارج هذه التمريرة** — لم يُمَسّ بحرف.

**بلا ترحيل** — الجدولُ قائمٌ منذ ٠٤٠، ولا عمودَ ولا قيدَ ولا فهرسَ جديد، ولا
حرفَ تغيّر في ترحيل ٠٨٢ نفسِه.

اختبارٌ دائم — ثلاثةُ أقسامٍ جديدة في `npm run test:maintenance-concurrent`
(٦٥ تأكيداً بعد الإضافة، مقابل ٣٧ قبلها): **ح** نفسُ الرمز + إرسالان متزامنان
⟶ ٢٠١ و٢٠٠، وأمرٌ واحد وزيارةٌ واحدة وقيدُ كلفةٍ واحد **ودفعةٌ واحدة** وسطرا
تدقيقٍ واحدان، وإرسالٌ ثالثٌ متتالٍ لا يحرّك رقماً · **ط** رمزان مختلفان + نفسُ
البيانات + إرسالان متزامنان ⟶ ٢٠١ و٢٠١، وعمليتان مستقلّتان بكلّ آثارهما
(**الثابتُ الذي لا يجوز أن يكسره منعُ التكرار**) · **ي** فشلُ المعاملة يُرجع
الحجزَ، فتنجح إعادةُ المحاولة بالرمز عينه.

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ نصفٍ على حدة**: تعطيلُ الحجز يُسقط **١٣
تأكيداً** كلُّها من الأقسام الجديدة (وأ–ز وط١–ط٨ تبقى خضراء) · ونقلُ الحجز
**خارج** المعاملة يُسقط **ي٢/ي٤/ي٥ بالضبط** — ذاك بعينه العطبُ الموصوف أعلاه —
وح وط تبقيان خضراوين.

#### والتذكرةُ صارت **إلزاميةً** على النقطة العامّة (٢٠٢٦-٠٩-١٨)

**اختياريّتُها كانت تُبقي البابَ مفتوحاً لمن لم يحدّث صفحته.** الحارسُ أعلاه
يمنع التكرارَ **متى وصل الرمز**؛ وعميلٌ سابقٌ لهذه المرحلة لا يرسله، فتمرّ
ضغطتاه عمليتين حقيقيتين ويُقيَّد مالُه مرّتين — **وهو بعينه ما رفعُ حدّ ٠٨٢
جعله ممكناً**. فصار `POST /api/no-exam/maintenance` **يشترط** رمزاً غيرَ فارغ.

- **والردُّ ٤٠٠ يقول المخرجَ لا اللومَ**: `MAINTENANCE_TOKEN_REQUIRED_MESSAGE`
  («انتهت صلاحية هذه الصفحة — حدّث الصفحة وأعد تسجيل الصيانة. لم تُسجَّل أيّ
  عملية ولم يُقيَّد أيّ مبلغ.»). **خمسةُ أشكالٍ للغياب تُقرأ غياباً** — المفتاحُ
  غائبٌ · نصٌّ فارغ · بياضٌ وحده · رقمٌ لا نصّ · `null` صريحة — **ولا يُصحَّح
  مشوَّهٌ بصمت**.
- **والحارسُ أوّلُ شيءٍ بعد الصلاحية والعقد المتقاعد**: قبل قراءة المريض وقبل
  فحص الفرع وقبل الخبير وقبل السعر وقبل أيّ قفل. فالردُّ **صفرُ كتابةٍ تشغيلية
  أو مالية** — لا أمرَ ولا زيارةَ ولا قيدَ كلفةٍ ولا دفعةَ ولا سطرَ تدقيق.
- **والحجزُ نفسُه لم يتحرّك**: يبقى **داخل معاملة** `createMaintenanceOperation`
  قبل أيّ كتابة، فيرتدّ معها ويُعاد بالرمز عينه. وهذا **حارسُ عقدٍ يسبقه** لا
  بديلٌ عنه.
- **والحقلُ يبقى اختيارياً في المخزن** (`submissionToken?: string | null`) —
  لمُنادٍ داخليّ يتجاوز النقطة (قسمُ «س» في `test:maintenance-simplify` ينادي
  المخزنَ مباشرةً)؛ **والإلزامُ عقدُ البابِ العامّ وحده**.
- **والشاشةُ لا تُجهِّز زرَّ الحفظ قبلها**: `maintenanceTokenUnready` في شرط
  `ready` — **للصيانة وحدها** (بيعُ الجزء لا يرسلها أصلاً، ولم يُمَسّ بحرف).
  و`useEffect` يسكّها **بعد** أوّل رسم، فثمّة لحظةٌ يكون فيها الزرُّ ظاهراً
  والتذكرةُ فارغة — والخادمُ يبقى الحارسَ الحقيقيّ على كلّ حال.
- **ولا قيدَ تجاريّ عاد**: صيانتان متطابقتان برمزين مختلفين تنجحان كلتاهما —
  الثابتُ الذي لا يجوز أن يكسره منعُ التكرار، وما زال مُثبَتاً حيّاً.

**وتسعةُ ملفّاتِ اختبارٍ تنادي النقطةَ** صارت تمرّر **رمزاً فريداً لكلّ نداء**
(`maintTok()`، بطابعٍ زمنيٍّ في بادئته فتنجح إعادةُ التشغيل على القاعدة نفسِها):
`simplified_maintenance` · `pending_charge` · `payment_history_description` ·
`departments` · `medical_review` · `daily_review` · `device_episode_integration` ·
`returning_patient` · `manufacturing/maintenance_service_type`. **ورمزٌ فريدٌ
لكلّ نداء هو بالضبط ما ترسله ضغطتان مستقلّتان** — فالحقنُ يحاكي الواقعَ ولا
يخفّف حارساً.

**وبلا ترحيل** — ولا عمودَ ولا جدولَ ولا قيد، ولا حرفَ في ترحيل ٠٨٢ نفسِه، ولا
في `createMaintenanceOperation` أكثرَ ممّا يلزم (توقيعُها كما هو).

اختبارٌ دائم — قسمٌ جديد **ك** في `npm run test:maintenance-concurrent` (٨٥
تأكيداً بعد الإضافة، مقابل ٦٥ قبلها): الأشكالُ الخمسة ⟶ ٤٠٠ بالرسالة عينها ·
**وصفرُ كتابةٍ تشغيلية أو مالية وصفرُ تدقيقٍ وصفرُ تذكرةٍ محجوزة** · **والجسمُ
عينه بتذكرةٍ صالحة ينجح ٢٠١** (فالردُّ كان للتذكرة وحدها لا لعيبٍ آخر) · وعقدُ
الشاشة الثلاثة. **والقسمان ح وط باقيان بحرفهما**.

**ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على حدة**: حذفُ حارس الخادم يُسقط **١٣ تأكيداً
كلُّها من القسم ك** (وأ–ي تبقى خضراء) · وفصلُ حارس الشاشة عن `ready` يُسقط
**ك١١ وحده**. **وعشرُ حزمٍ مجاورة بمجموعةِ فشلٍ متطابقةٍ حرفياً قبل وبعد**
(`diff` آليّ على سطور الفشل: maintenance-concurrent · maintenance-simplify ·
pending-charge · payment-description · departments · medical-review ·
daily-review · episode-integration · returning-patient · maintenance-service)،
وستُّ حزمٍ عميلية خضراء، و`tsc` ٣٥ = ٣٥ متطابقةً، و`npm run build` ينجح.


## 4.aa إكمالُ ملفّ الصيانة — تنبيهُ التشابه والضمان (ترحيل ٠٨٣ — ٢٠٢٦-٠٩-١٨)

**بقي في ملفّ الصيانة شيئان، وقد تمّا معاً: تنبيهٌ معلوماتيٌّ عند وجود صيانةٍ
مفتوحةٍ مشابهة، و«ضمن الضمان» حالةً مهيكلةً مستقلّة.** فرعُ
`claude/maintenance-warranty-and-similar-alert`. **ولا ملفَّ خارج الصيانة
يُمَسّ** (عدا مرساةَ حدودٍ في اختبارٍ قائم، آخرَ هذا القسم).

### ① التنبيهُ المعلوماتيّ — يقول ولا يمنع

**الواقعة**: ترحيلُ ٠٨٢ رفع «صيانةٌ مفتوحةٌ واحدة لكلّ جهاز» **عن قصد** —
مريضٌ يكسر قالبَه مرّتين في أسبوع عملان حقيقيّان. لكنّ رفعَ الحدّ أزال معه ما
كان **يُعلِم** الموظّفَ صدفةً: زميلٌ في شفتٍ آخر فتح أمراً لنفس الجزء، فيُفتَح
ثانٍ بلا أن يعرف أحدُهما بالآخر.

**فالعلاجُ تنبيهٌ لا حارس**: `POST /api/no-exam/maintenance/similar` —
**قراءةٌ فقط**، تُسأل قبل الحفظ فتقول ما هو قائم، ثمّ **القرارُ للموظّف**.

**والتشابهُ أربعةٌ لا أكثر**: المريضُ · نوعُ الخدمة · الجهازُ **بهويّته**
(أو **غيابُها** حين يكون الاختيارُ «جهاز قديم غير مسجَّل» — فيُقابَل بما لا
هويّةَ له، **ولا تُخترَع هويّة**) · والجزءُ في الأطراف.
**ولا الخبيرُ ولا السعرُ ولا الخصمُ ولا المبلغُ المدفوعُ ولا الفرع**: هذه
تفاصيلُ العملية لا هويّةُ العمل — وأمرُ صيانةِ الركبة نفسِها مفتوحٌ **في فرعٍ
آخر بخبيرٍ آخر وبسعرٍ آخر** هو بالضبط ما يستحقّ التنبيه.

**و«مفتوحة» = ما لم ينتهِ ولم يُبطَل**: `status NOT IN ('completed',
'cancelled')` — نفسُ تعريف `hasOpenOrder` بحرفه — ومعه استبعادُ المُبطَل
إدارياً (٠٦٤).

**وخياران لا ثالث**: **رجوع** ⟶ **صفرُ كتابة** (لم يُرسَل شيءٌ أصلاً)،
و**متابعة** ⟶ عمليةُ صيانةٍ مستقلّةٌ كاملة بكلّ آثارها المعتادة.

**ولا قيدَ يعود**: لا فهرسَ فريداً، ولا حارسَ أمرٍ مفتوح، **ولا حرفَ يتغيّر في
ترحيل ٠٨٢**. ومَن تخطّى الفحصَ — عميلٌ قديم أو نداءٌ مباشر — يُسجَّل كما كان
تماماً: التنبيهُ لا تناديه نقطةُ التسجيل ولا يقرأ نتيجتَه حارس.

**ولا يلمس تذكرةَ الإرسال**: لا يحجزها ولا يقرؤها ولا يطلبها أصلاً — **والمتابعةُ
تمضي بالتذكرة الحالية نفسِها**، فضغطةٌ واحدة تبقى عمليةً واحدة كما في ٠٨٢.

**وفشلُ الفحص لا يحجب عملاً مشروعاً**: تنبيهٌ تعذّر ليس سبباً لتعطيل صيانةٍ
يريدها الموظّف — فتتخطّاه الشاشةُ بصمتٍ ويمضي الحفظ.

**والصلاحيةُ والنطاقُ كما في التسجيل بالضبط** (`canCompleteMaintenance` +
`scopeReachesPatient`): مَن لا يسجّل صيانةً لا يقرأ صيانةَ مريضٍ ليس في نطاقه،
**والطبيبُ يُردّ ٤٠٣** كما على نقطة التسجيل.

**وهويّةُ العملية من مصدرٍ واحد**: `maintenanceContext` و`maintenanceItem`
(`server/pending_charges/routes.ts`) يخدمان البابين معاً — فلا يُنبَّه على خيطٍ
غيرِ الذي سيُكتب فيه فعلاً. **والرسائلُ والرموزُ والترتيبُ على نقطة التسجيل كما
كانت بحرفها** (فحصُ الخبير في موضعه القائم بالضبط، فلا يتبدّل خطأٌ يُردّ به
طلبٌ ناقصٌ من وجهين).

### ② «ضمن الضمان» — حالةٌ مهيكلةٌ مستقلّة لا «مجّانيّ» بثوبٍ آخر

**الواقعة**: صيانةُ الضمان وصيانةٌ مجّانيةٌ عادية تنتهيان معاً إلى **أجرٍ صفر**،
لكنّ الأولى التزامٌ سبق أن قطعه المركز والثانية قرارُ منحٍ جديد. فلو دُلَّ
عليهما بـ`maintenance_price_kind = 'free'` **وحدَه** لصارتا صفّاً واحداً لا
يُفرَّق بينه بعد اليوم: لا تقريرُ ضمانٍ يُبنى، ولا تكلفةُ الالتزام تُعرَف.

**فالعمودُ الجديد `prosthetic_work_orders.maintenance_under_warranty`** يقول
**لماذا** لا يُدفَع، والنوعُ يبقى يقول **كم** يُدفَع (`free`، والرقمان متّسقان
معه كما كانا). **ولماذا عمودٌ لا قيمةُ نوعٍ رابعة**: `maintenance_price_kind`
يصف العلاقةَ بين الأصليّ والنهائيّ (٠٦٩) ويحرسه
`maintenance_commercial_kind_check` بثلاث قيمٍ مُحكَمة — وإقحامُ «ضمان» فيه
يخلط سؤالين، ويجرّ معه إعادةَ بناء ذلك القيد وكلَّ قارئٍ للنوع (ومنه المراجعةُ
اليومية، §4.m) بلا حاجة. **فالعلامةُ عمودٌ صريحٌ بجواره، والتفريقُ في القاعدة لا
في تخمين قارئ.**

**وعند اختيار الضمان**: الأصليُّ **يبقى محفوظاً كما أدخله الموظّف** (القيمةُ
الاسمية للصيانة) · والنهائيُّ صفر · **ولا قيدَ كلفةٍ ولا دَينَ ولا دفعةَ ولا
مبلغَ مدفوعاً الآن ولا خصمَ ولا اعتماداً لاحقاً** · **وأمرُ العمل والزيارةُ
يُنشآن بشكلٍ طبيعي** (عملٌ حقيقيٌّ بقيمة صفر — `postMaintenanceFee` لا تُنادى
أصلاً عند `cost = 0` كما كانت دائماً) · والعلمُ يبقى محفوظاً على الأمر.

**ولا خصمَ مع الضمان**: يُردّ ٤٠٠ برسالةٍ تدلّ على المخرج — الأجرُ صفرٌ بقرار
التزامٍ سابق لا بخصمٍ يُمنَح، وقبولُ الاثنين معاً يُنتج صفّاً يقول شيئين
متناقضين عن السبب نفسِه. **وعلمٌ غيرُ بوليان يُردّ ٤٠٠ ولا يُصحَّح بصمت.**

**ولا شرطَ أهليّةٍ محسوب إطلاقاً**: لا مدّةَ ولا تاريخَ شراءٍ ولا عدَّ مرّاتٍ
ولا نوعَ جهاز. **الموظّفُ هو مَن يقرّر**، **وصيانةُ ضمانٍ سابقة لا تمنع لاحقة**.

**والشاشة**: مربّعٌ صريح في نافذة الصيانة؛ وعند تأشيره **يختفي حقلُ الخصم وحقلُ
المبلغ المدفوع الآن** (ويُصفَّران، فلا بقايا من قرارٍ سابق)، **ويبقى السعرُ
الأصليُّ مطلوباً**، ويقول صندوقُ السعر «ضمن الضمان — السعر النهائي: 0 د.ع
(القيمة الاسمية X)». **ولا مبلغَ يُفترَض في أيّ موضع.**

### وما لم يتغيّر
دلالاتُ **العاديّ والخصم والمجّانيّ** بحرفها (`normal` / `discount` / `free`
بأرقامها ونوعها و`no_exam_no_charge` كما هو) · تذكرةُ الإرسال وإلزاميّتُها
(§4.z) · `createMaintenanceOrderWithVisit` و`postMaintenanceFee` (الأولى
اكتسبت معامِلاً اختيارياً لا أكثر، و`undefined` تبقى `NULL`) · اعتمادُ الخصم
الموروث (`server/discounts/store.ts` يناديها بلا العلم فتبقى `NULL`) · **بيعُ
الجزء (§4.k) بلا حرف** · المعايناتُ · العلاجُ الطبيعي · سلّةُ المرضى ·
وترحيلاتٌ من ٠٠١ إلى ٠٨٢.

### الترحيل
**٠٨٣ فقط** — إضافيّ، idempotent: عمودٌ واحد **بلا `DEFAULT`** (`NULL` غيابُ
سؤالٍ لا نفي — درسُ ٠٣٨/٠٣٥/٠٦٩/٠٦٥ بحرفه، **ولا تعبئةَ رجعية**) وقيدان:
الضمانُ على أمرِ صيانةٍ وحدَه · وضمانٌ بأجرٍ تناقضٌ (النهائيُّ صفرٌ والأصليُّ
موجب). وكلاهما **توسيعٌ لا تضييق** فكلُّ صفٍّ قائم يجتازه كما هو. **بلا `DROP`
ولا `DELETE` ولا `UPDATE`**، ولا مسٍّ لترحيلٍ من ٠٠١ إلى ٠٨٢. وطُبِّقت القيودُ
حيّةً بمصفوفة `SAVEPOINT` (ثلاثةٌ تُردّ وثلاثةٌ تمرّ) وأُعيد الترحيلُ مرّتين قبل
حزمة الاختبارات.

اختبارٌ دائم: `npm run test:maintenance-warranty` (٩٦ تأكيداً حيّاً على Postgres
وعلى النقطتين الحقيقيتين: حالةُ القاعدة · لا مشابهَ ⟶ حفظٌ مباشر · مشابهٌ ⟶
تنبيهٌ **وبصمةُ الملفّ كما هي بايتاً** · الرجوعُ بصفر كتابة · المتابعةُ بعمليةٍ
ثانيةٍ كاملة · اختلافُ الجهاز والجزء ⟶ لا تنبيه · اختلافُ الخبير والسعر
والفرع ⟶ **يبقى** التنبيه · المكتملُ والملغى يخرجان · جهازٌ غير مسجَّل **بلا
اختراع هويّة** · صيانةُ ضمانٍ بكلّ ثوابتها وسطرِ تدقيقها · والمجّانيُّ العاديُّ
**مميَّزاً عنها في القاعدة** · تذكرتان ⟶ ضمانان مستقلّان وتذكرةٌ واحدة ⟶ لا
تكرار · الخصمُ والعلمُ الفاسد ⟶ ٤٠٠ بصفر كتابة · الصلاحيةُ والنطاق · وعقدُ
الشاشة وحارسٌ معماريّ: لا `INSERT/UPDATE/DELETE` في الفحص ولا ذكرَ لتذكرة).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات**: حذفُ كتابة علم الضمان يُسقط **٦ تأكيداتٍ
بالضبط** كلَّها عن العلم · وحذفُ شرط الجزء يُسقط **واحداً** (ز١) · وحذفُ شرط
الجهاز يُسقط **اثنين** (و١ · ز٢) — ولا شيءَ غيرها في الثلاث.

**ومرساةُ حدودٍ في اختبارٍ قائم**: `client/src/lib/patient_cache.test.ts` كان
يحدّ دالّةَ `invalidate` بالنصّ الحرفيّ `"const offer = deriveOfferFromDiscount"`،
وقد صار الاشتقاقُ يتفرّع على الضمان — فضاقت المرساةُ إلى `"const offer = "`.
**وموضوعُ الاختبار لم يتغيّر بحرف** (٢٢ و٢٣ كما هما).

---

## 4.ab المحذوفُ خارجَ منعِ تكرار التسجيل (٢٠٢٦-٠٩-١٨)

**المريضُ المحذوف لا يدخل منعَ التكرار إطلاقاً.** اسمُه لا يمنع، ورقمُه لا
يمنع، **وملفٌّ جديدٌ مطابقٌ له في كلّ بياناته يُسجَّل بصورةٍ طبيعية.** فرعُ
`claude/duplicate-check-ignores-deleted`. **بلا ترحيل.**

### الواقعة
تصحيحُ ٢٠٢٦-٠٩-٠٨ جعل السلّةَ **تحجز الهويّة** عند التسجيل: اسمٌ أو هاتفٌ
يطابق صفّاً محذوفاً يُردّ ٤٠٩ برسالة السلّة الآمنة («يوجد ملف مطابق يحتاج
مراجعة الإدارة قبل التسجيل»)، حتى يُستعاد الأصلُ أو يُبَتّ فيه إدارياً. وأثرُه
العمليّ أن موظّفَ الاستقبال يُردّ **برسالةٍ لا تسمّي شيئاً** أمام مريضٍ حاضر،
ولا يرى الملفَّ (المحذوفُ غائبٌ عن السجلّ والبحث)، ولا يملك بابَ تجاوزٍ —
فيتعطّل تسجيلٌ مشروعٌ لسببٍ لا يستطيع رؤيته ولا حلَّه.

### القاعدةُ الآن — الفعّالون وحدهم
`POST /api/patients` يفحص **الفعّالين حصراً**: الاسمُ بادئةً، والهاتفُ مطابقةً
تامّة — **بحرفهما، لم يتغيّر منهما شيء** (`duplicate_guard.ts` القسمان ١ و٢).
وحُذف من مسار التسجيل وحده:
- `hasTrashedNamePrefixConflict` و`PatientNameTrashConflictError` — **بلا
  مُستدعٍ آخر، فحُذفا لا عُطِّلا**: فرعٌ ميّتٌ يبقى مكتوباً تعود به القاعدةُ
  يوماً بنداءٍ واحدٍ بالسهو (درسُ ٤.ز).
- `checkTrash` من نداء `createPatient` للهاتف.
- معالجا الخطأين في `POST /api/patients`.

**ولا تأكيدَ إداريّ ولا تجاوزَ ولا نافذةَ ثانية** — البابُ يمضي كما لو لم يكن
للمحذوف وجود.

### وفحصُ توفّر الاسم في الشاشة يتبعها
`GET /api/patients/name-availability` ⟵ `checkNameAvailability` صارت تقرأ
الفعّالين وحدهم، فسقطت `reason: "trash_conflict"` من نوع الجواب. **فلا يصير
المحذوفُ حدّاً أحمرَ في نموذج التسجيل ولا يمنع الحفظ** — ولا نسخةَ ثانية من
القاعدة في العميل: `CreatePatient.tsx` يعرض ما يرسله الخادم كما كان.

### وما لم يُمَسّ
- **الهاتفُ عند التعديل** (`PUT /api/patients/:id` ⟵ `assertPhoneAvailable`
  بـ`checkTrash: true`) **باقٍ بحرفه**: مريضٌ فعّالٌ قائم لا ينتقل إلى رقم
  مريضٍ محذوف. **منعُ انتقالٍ لا منعُ تسجيل** — وهو ليس مسار التسجيل.
- **قاعدةُ الفعّالين** بكلّ تفاصيلها: البادئةُ واتجاهُها الواحد، والتطبيعُ
  الكنسيّ، وعالميّةُ المنع عبر الفروع، ورسالتُه المعتمَدة بالحرف.
- **الحذفُ والاستعادةُ** (٠٦٨) — لا كتابةَ على صفٍّ محذوف، ولا استعادةَ ضمنية.
- **قفلُ الهويّة المشترك** بين التسجيل وتعديل الهاتف والاستعادة: باقٍ ويعمل.
  لم يعد للتسجيل فحصان تتسلّل الاستعادةُ بينهما، **لكنّ الاستعادةَ تُصيّر
  الهويّةَ فعّالة** — فتسجيلٌ بلا تسلسلٍ معها كان يُدرِج على قراءةٍ سبقت
  التزامَها فينكسر **شرطُ الفعّالين نفسُه**.
- `lookup-by-name` بحرفها (تنبيهُ تكرار التسجيل القائم، ٤.ز) · الدفعاتُ
  والزياراتُ والمعايناتُ والتصنيعُ والعلاجُ الطبيعي وواتساب.

### والعاقبةُ مقالةٌ لا مخبوءة
ملفٌّ جديد فُتح بهويّة محذوفٍ ثمّ استُعيد الأصلُ ⟶ **ملفّان فعّالان بالهويّة
نفسِها**. هذا ما طُلب حرفياً، ومعالجتُه قرارٌ إداريّ (دمجٌ أو حذف) لا حارسٌ في
النظام — **ويُثبَت في الاختبار صراحةً** (٢٦.١٣) كي يُقرأ الأثرُ كما هو لا كما
يُظَنّ.

### و`lookup-by-name` تبعتها — تنبيهُ السلّة سقط (تكملةٌ في اليوم نفسه)

**كان قد بقي تناقضٌ واحد**: `GET /api/patients/lookup-by-name` ما زال يقرأ
صفوفَ السلّة ويُخرجها في `inTrash`/`inTrashCount`/`trashNotice` — تنبيهاً على
«ملفٍّ مطابقٍ محذوف» قبل التسجيل (٤.ز). **وذاك غرضُ تسجيلٍ بعينه**، والمحذوفُ
خرج منه كلّياً. فصار:

- **المطابقاتُ الفعّالة وحدها** تُقرأ وتُعاد، و**الحقولُ الثلاثة غابت من الردّ
  أصلاً** — لا تنبيهَ ولا عدّاد ولا صفٌّ يُكشَف. والردُّ `{ matches }` وحده.
- **والتصفيةُ في SQL قبل `LIMIT`** (`activePatientDrizzle()` القانونية، وهي
  مستورَدةٌ في الملفّ أصلاً): الحدُّ عشرون صفّاً، فصفوفُ السلّة كانت تزاحم
  المطابقاتِ الفعّالة عليه ثمّ تُسقَط بعد القراءة — **فقد يُحجَب نظيرٌ فعّالٌ
  في فرعٍ آخر لأن محذوفاً سبقه** (نفسُ درس التصفية قبل الحجز في ٤.ج).
- **وكشفُ المريض الفعّال عبر الفروع لم يتغيّر بحرف**: «ما لا يراه السائلُ في
  فرعه» كما كان، والمسؤولُ العام يرى كلَّ شيءٍ أصلاً فلا تنبيهَ له.
- **والسلّةُ نفسُها لم تُمَسّ**: صفحتُها والاستعادةُ والحذفُ النهائيّ
  و`IN_TRASH_HINT`/`IN_TRASH_ESCALATION` في `shared/patient_trash.ts` كما هي
  — سقط **استعمالُها في هذا المسار وحده**، ومعه استيرادُها في `routes.ts`
  (بقي `PATIENT_IN_TRASH_ERROR` لمُستدعِيه). **ولا واجهةَ تتأثّر**: لا ملفَّ
  عميلٍ واحد ينادي هذا المسار اليوم.

**⚠ وانقلب عقدُ قسمين في اختبارين قائمين** — بقرارِ المالك لا لتخضير اختبار:
القسمُ «ح» في `test:patient-trash` (كان يثبت التنبيهَ بوجهيه: صفٌّ مكشوفٌ
لمن يملك السلّة، ورسالةٌ آمنة لمن لا يملكها) والقسمُ «ل» في
`test:patient-duplicate-guard` (كان يثبت المفاتيحَ الأربعة) — صارا يثبتان
**مفتاحاً واحداً** وغيابَ المحذوف. ومعهما إثباتٌ حيٌّ جديد (٢٧.١–٢٧.٤):
نظيرٌ **فعّالٌ** في فرعٍ آخر **يظهر**، ومحذوفٌ مثلُه في الفرع نفسِه **لا
يظهر** — فالفارقُ الوحيد بينهما حالةُ الحذف.

**ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على حدة**: إعادةُ الحقول الثلاثة تُسقط ٤
تأكيداتٍ (٢٧ · ٢٧.٤ · ح١ · ح٣)، وإسقاطُ شرط SQL وحده يُسقط **٢٧.٣ بالضبط** —
وهو اليوم الحارسُ الوحيد لغياب المحذوف عن `matches`.

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيدَ ولا فهرس، ولا مسٍّ لترحيلٍ من ٠٠١
إلى ٠٨٣.

اختبارٌ دائم: `npm run test:patient-duplicate-guard` (حيّاً على Postgres وعلى
النقاط الحقيقية — **والقسمُ ك انقلب عقدُه بقرار المالك لا لتخضير اختبار**:
اسمٌ مطابقٌ لمحذوف ⟶ يُسجَّل · هاتفٌ مطابقٌ ⟶ يُسجَّل · الاثنان معاً ⟶ يُسجَّل
**وصفُّ المحذوف مطابقٌ بايتاً قبل وبعد** · والفعّالُ يبقى مانعاً اسماً وهاتفاً ·
وفحصُ توفّر الاسم `{available:true}` للمحذوف ورسالتُه المعتمَدة للفعّال ·
والاستعادةُ تنجح والعاقبةُ مُثبَتة) و`npm run test:create-patient-duplicate-guard`
(عقدُ الشاشة: لا منطقَ سلّةٍ فيها إطلاقاً). **ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على
حدة**: إعادةُ حجز الاسم تُسقط ١١ تأكيداً، وإعادةُ `checkTrash` للهاتف تُسقط ٩ —
**كلُّها من القسم الجديد**، ولا شيءَ خارجه يسقط في الحالتين.

### تصحيحٌ لاحق — الرسالةُ تقول الحقيقةَ بدل أن تدلّ على التكرار (٢٠٢٦-٠٩-٢٢)

**المنعُ كان يعمل، والكلامُ وحده كان يضلّل.** حادثةُ المريضة «لمياء حسين علي»:
ملفٌّ في بغداد وملفٌّ ثانٍ في الموصل بالاسم نفسِه حرفاً بحرف.

**والتحقيقُ أثبت أنّ الحارسَ بريءٌ من تلك الحادثة بعينها**: الموصلُ سُجّل
١٩/٠٨/٢٠٢٦، والحجزُ الصلب أُضيف ٠٨/٠٩/٢٠٢٦ — بعده بعشرين يوماً. الذي كان
قائماً يومها **تنبيهُ ٠٦/٠٨/٢٠٢٦** (طلبُ الدمج ٢٠٢)، ونصُّ قراره: «**ولا يمنع
الحفظ أبداً — يُعلم ولا يحجب**». فرأته الموظّفةُ وحفظت.

**لكنّ التحقيق كشف ثغرةً حيّةً اليوم**: الرسالةُ الواحدة «يوجد اسم مسجل يبدأ
بهذا الاسم، **أكمل كتابة الاسم**» تأمر الموظّفةَ بإطالة الاسم — **والاسمُ
الأطول مقبولٌ بالقاعدة عمداً** (الاتجاهُ واحدٌ لا اثنان). فالنظامُ يحجب ثمّ
**يدلّ على الالتفاف**، ولا يقول للموظّفة أصلاً أنّ المريضَ مسجَّلٌ عندنا ولا في
أيّ فرع. وزاد الأمرَ أنّ طلبَ الدمج ٢٧٩ استبدل التنبيهَ بالحجز **فأزال معه
اسمَ الفرع**: `lookup-by-name` باقيةٌ في الخادم **بلا مُنادٍ واحد** منذئذٍ.

**فصارت رسالتين، كلٌّ في موضعها:**

| الحالة | الرسالة |
|---|---|
| **مطابقةٌ تامّة** بعد التطبيع (نفسُ الشخص) | «هذا المريض مسجَّل مسبقاً **في فرع كذا** — لا تفتح له ملفاً جديداً. راجع المسؤول لإتاحة ملفّه لفرعك.» |
| **بادئةٌ فقط** (القائمُ أطول — قد يكون شخصاً آخر) | `NAME_PREFIX_CONFLICT_MESSAGE` **بحرفها، لم تتغيّر** |

**ولا قاعدةَ تغيّرت بحرف**: الشرطُ المانع هو هو (`hasActiveNamePrefixConflict`
**بنصّها وخطّةِ تنفيذها**)، فمَن كان يُحجَب يبقى محجوباً ومَن كان يمرّ يمرّ —
**والاسمُ الأطول يُسجَّل كما كان**. المتبدِّلُ نصُّ الرسالة في حالةٍ واحدة.

**والتصنيفُ لا يُبطئ الكتابة**: `findActiveNameConflict` تفحص التطابقَ التامّ
**بمساواةٍ على العمود المفهرَس** (`ix_patients_name_norm_prefix`) فتُحسَم
بمسبارٍ واحد، ثمّ تسقط إلى استعلام البادئة القائم بلا تعديل. فالكتابةُ بحرفٍ
واحد تبقى على مسارها السابق. **والفراغُ يُستثنى في الاثنين معاً**
(`patient_search_norm(…) <> ''`) — بدونه كان اسمٌ يطبَّع إلى فراغ «يطابق تماماً»
صفّاً اسمُه فراغ، وذاك تغييرُ سلوكٍ لا تغييرُ رسالة. **والأقدمُ أوّلاً**
(`ORDER BY ap.id`) فالقرارُ حتميّ. **وبياضُ اسم الفرع يُقرأ غياباً** فلا تُكتب
«في فرع  —» بلا معنى.

**وبلا حرفٍ في الواجهة** (مُتحقَّقٌ): الشاشةُ تعرض رسالةَ الخادم كما تصلها
أصلاً (`nameCheck.message`)، ونقطةُ التسجيل تردّ `err.message` — فالرسالتان
تصلان الموظّفةَ بلا لمسِ ملفِّ التسجيل. **وشكلُ الردّ لم يتغيّر**: نفسُ
`reason: "active_conflict"` ونفسُ المفاتيح الثلاثة.

**وبلا ترحيل** — ولا عمودَ ولا جدولَ ولا قيدَ ولا فهرس. والمُعدَّلُ ملفّان
اثنان: الحارسُ واختبارُه.

**وما لا يُصلحه بصدق**: موظّفةٌ تُطيل الاسمَ **عمداً** ما زالت تستطيع فتح ملفٍّ
ثانٍ. وسدُّ ذلك يعني منعَ الاسم الأطول — وهو يكسر تسجيلاً مشروعاً (مريضٌ جديد
اسمُه يمتدّ على اسمٍ قائم)، **فلم يُفعَل بقرارٍ صريح**.

اختبارٌ دائم: القسمُ **م** في `npm run test:patient-duplicate-guard` (شكلُ لمياء
بعينه حيّاً: استقبالُ فرعٍ آخر يكتب الاسمَ نفسَه ⟶ الفحصُ الحيُّ يحجب والرسالةُ
تسمّي بغداد · والتسجيلُ الفعليُّ يُردّ ٤٠٩ بالرسالة نفسِها وصفٌّ واحدٌ بالاسم ·
**والبادئةُ الأقصر تبقى على رسالتها القديمة** · والاسمُ الأطول يُسجَّل كما كان ·
وبياضُ اسم الفرع). ومعه **سبعةُ تأكيداتٍ قائمة انقلب عقدُها** (٣ · ١٠ · ١١ ·
١٢ · ١٣ · ٢٦.٨ · ٢٦.١٠ — كلُّها مطابقاتٌ تامّة) إلى الرسالة الجديدة، **وأربعةُ
تأكيدات البادئة لم تُمَسّ** (١ · ٢ · ٧ · ٨).

**ومُتحقَّقٌ بالعكس**: تعطيلُ التصنيف يُسقط **تسعةَ تأكيداتٍ بالضبط** — كلُّها
مطابقاتٌ تامّة — **وتبقى أقسامُ البادئة والتطبيع والتزامن والسلّة خضراء**.

#### تصحيحٌ لاحق — الإرشادُ محايدٌ لأنّ نطاقَ السائل غيرُ معلومٍ للحارس (٢٠٢٦-٠٩-٢٢)

**مراجعةٌ آلية على الطلب ٣٨٢ أمسكت أن الشقَّ الثاني من الرسالة خاطئٌ في أشيع
حالاته.** كانت تأمر صراحةً: «راجع المسؤول لإتاحة ملفّه لفرعك» — **والمنعُ
عالميٌّ عبر الفروع**، فأكثرُ ما يقع أن يكون المريضُ في **فرع السائل نفسِه**
(ملفٌّ قائم لم تجده الموظّفةُ في البحث)، فلا إتاحةَ تلزم أصلاً وتُرسَل إلى
المسؤول بلا موجب. وكذلك ملفٌّ أُتيح لفرعها سلفاً (٠٨٠)، **وكذلك المسؤولُ
العام نفسُه** — يقرأ «راجع المسؤول» وهو هو.

**ولا نطاقَ يُقرأ في الحارس**: `checkNameAvailability(db, name)` و
`assertNameAvailableForRegistration(tx, name)` — بلا جلسةٍ ولا فرعٍ ولا معرّف
مريض. وتمريرُ النطاق يعني تغييرَ توقيعَي دالّتين ومسارَ `storage.createPatient`
كلَّه **لأجل نصِّ رسالة** — توسّعٌ لا يستحقّه.

**فصار الإرشادُ يصف فعلاً يصحّ في الحالات الأربع**: «ابحث عنه في سجل المرضى،
وإن لم يظهر لك فراجع المسؤول لإتاحة ملفّه لفرعك». وسجلُّ المرضى يُظهر لغير
المسؤول **فرعَ تسجيله أو ما أُتيح لفرعه** (`patientVisibleToScopeSql`)
وللمسؤول كلَّ شيء — فمَن يملك الملفَّ يجده، ومَن لا يملكه لا يجده فيراجع.
**والإرشادُ لم يسقط، صار مشروطاً.**

**والشرطُ المانعُ واسمُ الفرع لم يتغيّرا بحرف** — مَن كان يُحجَب يبقى محجوباً،
ومَن كان يمرّ يمرّ، ورسالةُ البادئة كما هي. **وبلا ترحيلٍ ولا حرفٍ في الواجهة
ولا في القاعدة.**

**ومعه إصلاحُ فِكستشر**: الاختبارُ كان يقارن باسم «بغداد» حرفياً بينما يُدرج
الفرعَ بـ`ON CONFLICT DO NOTHING` — فقاعدةٌ يوجد فيها الفرعُ ١ باسمٍ آخر
تُفشل إحدى عشرةَ تأكيداً **بسبب بيئةٍ لا بسبب شيفرة**. فصار الاسمُ يُقرأ من
القاعدة بعد الإدراج: يقيس الاختبارُ **صياغةَ الرسالة** لا اسمَ فرعٍ في قاعدةٍ
بعينها. **وحرفا قسمين مكرَّران صُحِّحا** (كان في الملفّ «م» و«ن» مرّتين).

اختبارٌ دائم: القسمُ **ع** في `npm run test:patient-duplicate-guard` — **النقطةُ
الحقيقية** `/api/patients/registry` بحُرّاسها لا وصفٌ مكتوب: مطابقةٌ تامّة داخل
فرع السائل ⟶ محجوبةٌ كما كانت **والسجلُّ يُظهره لها فعلاً** · ومُتاحٌ لفرعها
سلفاً ⟶ كذلك · والمسؤولُ كذلك · **وبلا إتاحةٍ يصحّ الشقُّ الثاني فعلاً**
(السجلُّ لا يُظهره، والرسالةُ تدلّه على المسؤول). **والفحصُ على العلاقة لا على
نصٍّ حرفيّ**: لا يجوز أن تأمر الرسالةُ بالإتاحة أمراً غيرَ مشروطٍ سائلاً
يُظهر له السجلُّ المريضَ.

**ومُتحقَّقٌ بالعكس**: إعادةُ الصياغة القديمة تُسقط **ثلاثةَ تأكيداتٍ بالضبط**
— واحداً لكلّ حالةٍ من الثلاث — **وتبقى تأكيداتُ السجلّ والمنع خضراء** لأنها
تقيس البياناتِ لا القرار.
وستُّ حزمٍ مجاورة خضراء (`create-patient-duplicate-guard` · `patient-trash` ·
`patient-code` · `diagnose-create-patient` · `patient-required` ·
`search-surfaces`)، و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` نجح،
و`npm run build` نجح.

---

## 4.ac عدُّ المبيعات يُفصَّل بالأصناف — لا رقمٌ إجماليٌّ واحد (٢٠٢٦-٠٩-٢١)

**سؤالُ صنفٍ حقيقيٍّ له جوابٌ في القاعدة لا يُعتذَر عنه.** فرعُ
`claude/device-sales-breakdown`. **بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد.

### الواقعة (لقطةُ شاشةٍ من الإنتاج)
سأل المالكُ: «كم إصبعاً سليكونياً بِيع؟» — و«الأصابع السليكونية» تصنيفٌ حقيقيّ
داخل «اطراف سليكونية تعويضية» (`SILICONE_PARTS` في `shared/case_fields.ts`،
القسم ٤.b). فأجاب المساعد: «أداةُ عدّ الأطراف تُرجع **عدداً إجمالياً فقط**
للأطراف الصناعية الكاملة المباعة ولا تُفصّل حسب نوع الطرف أو حسب أجزائه…
أعتذر عن عدم توفّر هذا المستوى من التفصيل».

**والاعتذارُ كان صادقاً عن الأداة** — `device_sales_summary` تحسب
`COUNT(*)` واحداً بشرطين يُسقطان كلَّ صنفٍ آخر:
`ep.requested_item = 'full_device'` **يُسقط الأجزاء الثمانية** (قالبٌ · ركبةٌ ·
قدمٌ …)، و`wo.service_type = 'prosthetic'` **يُسقط المساندَ الطبية كلَّها**.
ولا يُقرأ نوعُ بترٍ إطلاقاً، فالأطرافُ الكاملة نفسُها رقمٌ أعمى.

### القاعدة الآن — `byCategory` + `totals`، بلا مسٍّ لِـ`totalSold`
`totalSold` يبقى **الأطرافَ الكاملة وحدها بحرفه** (نفسُ الاستعلام ونفسُ
الحُرّاس)، ويُضاف بجانبه تفصيلٌ من **استعلامٍ ثانٍ** بلا شرطَي الإسقاط:

| الصنف | المفتاح | العنوان |
|---|---|---|
| طرفٌ كاملٌ سليكونيٌّ بجزئه | `prosthetic_full:silicone:اصبع` | «اطراف سليكونية تعويضية — اصبع» |
| طرفٌ كاملٌ احاديٌّ/ثنائيّ | `prosthetic_full:single` · `:double` | «طرف صناعي كامل — احادي» |
| **نوعٌ غير مسجَّل** | `prosthetic_full:unrecorded` | «طرف صناعي كامل — نوعٌ غير مسجَّل» |
| جزءٌ من الثمانية | `prosthetic_part:socket` … | «القالب» … |
| مسندٌ طبيٌّ كامل | `support_full` | «مسند طبي كامل» |

ومعها `totals` بثلاثة مجاميعَ جاهزة (`prostheticFullDevices` ·
`prostheticComponents` · `medicalSupportDevices`) — **فلا يجمع النموذجُ
بنفسه** (قاعدةُ «لا تحسب رقماً بنفسك» من ٤.n).

### والتصنيفُ دالّةٌ خالصة لا SQL ثانية
`shared/device_sale_category.ts: classifyDeviceSale` / `tallyDeviceSales`.
والاستعلامُ يجلب **الوصفةَ خامّاً** عبر `activeExamSql` نفسِها التي يستعملها
`effectiveExamForEpisode` — فقاعدةُ «مواصفاتُ الجهاز من معاينته هو» (٤.q)
**لا تُكتَب بـSQL ثانيةً فتنحرف عنها**، والقرارُ يُختبَر دخلاً وخرجاً لا
بقراءة نصِّه (درسُ ٤.u). والمعاجمُ قانونية: `COMPONENT_LABELS` و
`FULL_DEVICE_LABELS` و`AMPUTATION_TYPE_OPTIONS` — **لا معجمَ ثانٍ**.
وترتيبُ الصفّ الفرعيّ هو ترتيبُ فهرس ٠٧٨ بالضبط.

### **ولا يُخمَّن تصنيفٌ أبداً**
حلقةٌ بلا معاينةٍ فعّالة · أو **معاينةٌ ملغاة** (٠٦١، فشاهدتُها تُسقط سلطتَها) ·
أو وصفةٌ لم يُلمَس فيها بانيَ البتر ⟶ «نوعٌ غير مسجَّل» **صراحةً**: لا تُنسَب
إلى «احادي» لأنه الأشيع، ولا تُسقَط فيَنقص المجموعُ بلا أن يعلم أحد.
وسليكونيٌّ بلا جزءٍ مسجَّل ⟶ «جزءٌ غير مسجَّل» لا يُخلَط بالإصبع.

### وحُرّاسُ «بِيع» لم يُرخَ منها حارس
الصيانةُ (`purpose = 'maintenance'`) · والمُبطَلُ إدارياً (٠٦٤، بوسم الأمر أو
الحلقة) · وخارجُ المدى · وخارجُ النطاق — كلُّها خارج التفصيل كما هي خارج
العدّ. **ومُثبَتٌ بوصفاتٍ سليمة**: الفرعُ المُختبَر يحمل تسعةَ أوامر، ثلاثةٌ
منها تقول وصفتُها «إصبع سليكوني» وتُستبعَد — فلو أُرخيَ حارسٌ لقفز العدُّ من
٢ إلى ٤. **ولا مبلغَ ولا اسمَ مريضٍ يتسرّب مع التفصيل**، والصلاحيةُ
(`canViewReports`) والنطاقُ كما هما بالحرف.

### الترحيل
**بلا ترحيل** — قراءةُ أعمدةٍ قائمة لا غير.

اختبارٌ دائم: `npm run test:device-sale-category` (٣٩ تأكيداً خالصاً بلا
قاعدة بيانات) و`npm run test:ai-device-sales` — القسمُ الجديد **ص**
(سؤالُ المالك بعينه · الكفُّ لا يُخلَط بالإصبع · المسندُ والقالبُ يظهران ·
المعاينةُ الملغاة لا تحكم · المجاميعُ تتصالح مع `totalSold` · الحُرّاسُ
الثلاثة بوصفاتٍ سليمة · لا مبلغَ ولا اسم · وعقدُ الإرشاد). **والأقسامُ
القائمة كلُّها بحرفها** — فِكستشراتُ التفصيل في **فرعٍ رابع خاصٍّ بها**
فلا تُزيح رقماً في تأكيدٍ قائم. **ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على حدة**:
إعادةُ شرط «الطرفُ الكامل وحده» تُسقط تسعةَ تأكيداتٍ كلَّها من القسم
الجديد، وإسقاطُ قراءة الوصفة يُسقط ستّةً — وكلاهما لا يمسّ شيئاً خارجه.

---

## 4.ad بطاقةُ «معايناتي» على الهاتف — وشارةٌ كانت تختفي (٢٠٢٦-٠٩-٢١)

**المالكُ يتصفّح من شاشة هاتف، وصفُّ قائمة عمل الطبيب لم يكن يُعرَض صحيحاً.**
فرعُ `claude/my-exams-phone`. **بلا خادمٍ ولا قاعدةٍ ولا ترحيل** — ملفُّ شاشةٍ
واحد وملفُّ اختبار.

### الواقعة — **مقيسةٌ بمتصفّحٍ حقيقيّ على CSS المبنيّ، لا موصوفة**
```
<CardContent className="p-3 flex items-center justify-between gap-3 flex-wrap">
  <div className="min-w-0"> … </div>
  <div className="flex items-center gap-1 shrink-0">   ⟵ أربعةُ أزرار
```
أربعةُ أزرارٍ (الملف · إرجاع للاستعلامات · إلغاء المعاينة · كتابة معاينة) في
كتلةٍ **لا تلتفّ ولا تنكمش** ≈ ٤٢٠ بكسل. والقياسُ الفعليّ:

| العرض | فيضُ البطاقة قبل | بعد |
|---|---|---|
| ٣٦٠ (هاتف) | **٣٩px** | **٠** |
| ٣٩٠ (هاتف) | **٩px** | **٠** |
| ٧٦٨ · ١٢٨٠ | ٠ | ٠ (**وارتفاعُ الحاسوب ٧٠px قبل وبعد**) |

**ومعه عطبٌ ثانٍ أخطرُ من التخطيط**: `truncate` كانت على **حاوية `flex`**.
و`text-overflow` لا يسري على عنصرٍ `display:flex`، فالقياسُ يقول:

| | «…» تسري؟ | شارةُ «عاد للشراء» | فيضُ السطر |
|---|---|---|---|
| قبل | **لا** (`display:flex`) | **غيرُ ظاهرة** | ١١٢px |
| بعد | نعم (`display:block`) | **ظاهرة** | ٠ |

فطبيبٌ يقرأ اسماً طويلاً **كان يفقد الشارةَ التي تقول إن المريض عاد للشراء**
— خسارةُ معلومةٍ لا مجرّدُ شكل.

### الإصلاحُ أربعةُ أجزاء، **والحاسوبُ كما كان بالحرف**
- **الصفُّ يتكوّم تحت `sm`**: `flex flex-col gap-2 sm:flex-row sm:flex-wrap
  sm:items-center sm:justify-between sm:gap-3` — كلُّ صنفٍ كان قائماً يعود
  مشروطاً بـ`sm`، فمحاذاةُ الحاسوب وتوزيعُه وتباعدُه وحشوتُه لا تتغيّر.
- **وكتلةُ الأزرار تلتفّ**: `flex-wrap sm:flex-nowrap sm:shrink-0` —
  سطران على الهاتف، وصفٌّ واحد غيرُ منكمش من `sm`.
- **ولكتلة البيانات أساسُ نموّ**: `min-w-0 flex-1` — بلا أساسٍ تنكمش إلى
  عرض محتواها الأدنى فيُسحَق الاسم.
- **والقصُّ على النصّ نفسِه**: `<span className="truncate">{r.patientName}</span>`
  داخل حاويةٍ `min-w-0`.

### وما لم يُمَسّ
الخادمُ كلُّه · القاعدةُ والترحيلات · منطقُ الترتيب والفلترة والصفحات ·
الأزرارُ الأربعة بوسومها وحُرّاسها (ومنها `isDeviceServiceKind` لإلغاء
المعاينة، ٤.s) · سطرُ هويّة الجهاز (٤.p) · النوافذُ · بطاقةُ «مرضاي الذين
اشتروا مؤخراً» · الصفحاتُ الأخرى كلُّها.

اختبارٌ دائم: `npm run test:my-exams-phone` (عقدُ الشاشة على **صفّ قائمة
العمل وحده** لا الملفّ كلّه، والتعليقاتُ تُزال قبل الفحص فلا يمرّ شرحٌ بدل
كود). **ومُتحقَّقٌ بالعكس، كلُّ إصلاحٍ على حدة**: إعادةُ شكل الصفّ القديم
تُسقط القسم أ وحده · وإعادةُ `shrink-0` بلا التفاف تُسقط ب وحده · وإعادةُ
`truncate` إلى الحاوية تُسقط د وحده · وإزالةُ `flex-1` تُسقط ج وحده.

---

## 4.ae مسوّدةُ المساعد تُقاس لحظةَ تركيب المربّع، لا عند تغيّر المحتوى وحده (٢٠٢٦-٠٩-٢١)

**أيُّ إعادة تركيبٍ للمربّع تُقاس — مهما كان الشرطُ الذي فكّكه.** فرعُ
`claude/ai-chat-input-remeasure`. **بلا خادمٍ ولا قاعدةٍ ولا ترحيل** — ملفُّ
شاشةٍ واحد وملفُّ اختبار.

### الواقعة (أمسكتها مراجعةُ Codex على #٣٦٧ بعد دمجه)
مربّعُ السؤال في `AiChatDrawer.tsx` يعيش داخل `{!trainingOpen && (…)}`، فيُفكَّك
حين تُفتَح لوحةُ التدريب ويُعاد تركيبُه حين تُغلَق. وأثرُ القياس اعتمادياتُه
`[draft, open]` — **وكلاهما لا يتغيّر عبر ذلك التبديل**، فلا يُعاد تشغيلُه
للعنصر الجديد. فمسوّدةٌ لم تُرسَل بعدُ تعود إلى مربّعٍ ارتفاعُه سطرٌ واحد
(`rows={1}`) وتبقى كذلك **حتى يكتب الموظّفُ حرفاً آخر**.

**مقيسٌ بمتصفّحٍ حقيقيّ على إعادةِ تركيبٍ فعلية** (لا موصوف) — مسوّدةُ ثلاثة
أسطر:

| | بعد الكتابة | **بعد العودة من التدريب** | مخفيٌّ بالتمرير |
|---|---|---|---|
| **قبل** | ٨٨px | **٤٢px** | **٤٨px** (سطران من ثلاثة) |
| **بعد** | ٨٨px | **٨٨px** | ٢px (تقريبُ قياس) |

### والعلاجُ عند **الربط** لا في قائمة الاعتماديات
إضافةُ `trainingOpen` إلى الاعتماديات تُغلق هذا الشرطَ بعينه وتترك القاعدةَ
مربوطةً بـ**سببِ** التفكيك: شرطٌ ثانٍ يُضاف يوماً فوق المربّع يُعيد العطبَ
صامتاً. فالقياسُ صار يقع لحظةَ **ربط العنصر** — فأيُّ إعادة تركيبٍ تُقاس بلا
أن يُعدَّد ما يفكّكها.

- **`measureAiChatInput(el)`** — **الموضعُ الوحيد للقاعدة**، فوق المكوّن.
  يُصفَّر الارتفاعُ ثمّ يُقاس (وإلّا لم ينكمش المربّعُ بعد الحذف:
  `scrollHeight` لا ينقص تحت ارتفاعٍ مفروضٍ سلفاً) — **منقولٌ من جسم الأثر
  بحرفه، لا مكتوبٌ من جديد**.
- **بابان ينادِيانه لا واحد**: الأثرُ التخطيطيُّ لتغيّر المحتوى
  (`[draft, open]` **كما كانت**)، و`attachInput` لإعادة التركيب.
- **و`useCallback([])` شرطُ صحّةٍ لا تحسين**: ربطٌ غيرُ ثابت يُفكّ ويُعاد في
  كلّ رسم فيُعاد القياسُ بلا داعٍ في كلّ حرف.
- **والمرجعُ يُكتب كما كان** (`inputRef.current = el`) فإعادةُ المؤشّر في
  `send` بعد الإرسال تبقى تعمل بحرفها.

### وما لم يُمَسّ
الخادمُ كلُّه · القاعدةُ والترحيلات · `ai_chat_input.ts` (الدالّتان الخالصتان
`grownInputHeight`/`shouldSendOnKey` بلا لمسة) · قاعدةُ المفاتيح وتركيبُ
الإدخال · غيابُ `disabled` عن المربّع وتعطيلُ الزرّ وحده · لوحةُ التدريب
ومحتواها · «اقترح تصحيحاً» · عرضُ Markdown · الرسائلُ والتزويد.

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد.

اختبارٌ دائم: `npm run test:ai-chat-input` (القسمُ هـ توسّع: دالّةُ القياس
وترتيبُها، **وغيابُ نسخةٍ ثانية منها في الشاشة**، وأن المربّعَ مشروطٌ فعلاً
بإغلاق لوحة التدريب — **فالتفكيكُ واقعٌ لا مفترَض وبدونه كان القسمُ يحرس لا
شيء** — والربطُ دالّةً تقيس، وثباتُها، وكتابتُها المرجع). **ومُتحقَّقٌ بالعكس
أربعَ مرّات، كلُّ آليّةٍ على حدة**: إزالةُ القياس عند الربط تُسقط هـ.١٠ج
وحدَه · وإعادةُ `ref={inputRef}` تُسقط هـ.١٠أ وحدَه · وإزالةُ `[]` تُسقط
هـ.١٠د وحدَه · وإعادةُ نسخة القاعدة داخل الأثر تُسقط هـ.٩أ٢ وهـ.٩ج لا غير.

---

## 4.af الإشعاراتُ على الهاتف — موضعٌ لا يحجب زرّاً، ومهلةٌ تنتهي (٢٠٢٦-٠٩-٢١)

**الإشعارُ لا يقف فوق زرٍّ أبداً، ويختفي وحدَه، وله زرُّ إغلاقٍ يُرى ويُضغَط.**
فرعُ `claude/toast-mobile-behavior`. **بلا خادمٍ ولا قاعدةٍ ولا ترحيل**، وبلا
لمسِ واحدٍ من **٢٥٥ موضعَ نداءٍ** لـ`toast({...})` في المستودع.

### الواقعة — **مقيسةٌ بمتصفّحٍ حقيقيّ على شيفرة المشروع وCSS المبنيّ**
شكوى المالك من شاشة هاتف: «الإشعارات تبقى فترة طويلة… وغير واضحة… ولا أتمكّن
من فتح القائمة الجانبية حينما أضغط لأن الإشعار موجود ويتأخر ليذهب».

القياسُ على ٣٩٠×٨٤٤ (بـ`meta viewport` الحقيقيّ للتطبيق):

| | **قبل** | **بعد** |
|---|---|---|
| زرّ القائمة (`12..48px`) | **محجوب** | **حرٌّ دائماً** |
| موضعُ الإشعار | `16..110px` — فوقه تماماً | `646..740px` أسفلَ الشاشة |
| زرّ المساعد العائم | حرّ | **حرّ** — لم تنتقل المشكلة إليه |
| يتحرّر الزرُّ بعد | **٥١٨٥ms** | **لا حجبَ أصلاً** |
| **وبلمسةٍ واحدة عليه** | **❌ ≥٣٠ ثانية** | لا أثر |
| زرّ الإغلاق ✕ | **شفافية ٠** · ٢٤px | **شفافية ١** · ٤٠px |
| المهلة | ٥٢٠٠ms (افتراضُ Radix الصامت) | ٤٢٢٠ms (صريحة) |

**وثلاثةُ أسباب مستقلّة، لا واحد:**

**١) الموضع.** `ToastViewport` كان `fixed top-0` على الهاتف، ورأسُ الهاتف
(`Sidebar.tsx`) ثابتٌ في `top-0` بـ`z-30` — والإشعارُ `z-[100]` فوقه
بـ`pointer-events: auto`. فزرُّ القائمة **مغطّى كاملاً** ما دام ظاهراً.
(والعارضُ الفارغ `pointer-events: none` — فحصتُه، لا يحجب شيئاً.)

**٢) المصيدة: لمسةٌ تُجمِّده.** Radix يوقف مؤقّتَ الإشعار عند دخول المؤشّر
ويستأنفه عند خروجه — **ولا «خروجَ» على شاشة لمس**. فالمالكُ يضغط ليفتح
القائمة، فتقع ضغطتُه على الإشعار فتُجمِّده، ثمّ يضغط ثانيةً فيُجمِّده أكثر.
مُقيسٌ: **باقٍ بعد ١٥ ثانية**، ولا يتحرّر إلّا بلمسةٍ بعيدةٍ ثمّ ٥ ثوانٍ.
و**زرُّ الإغلاق كان `opacity-0` يكشفه `group-hover` وحدَه** — فلا مخرجَ منه.

**٣) `TOAST_REMOVE_DELAY = 1000000`** — ستَّ عشرةَ دقيقةً وأربعين ثانية،
افتراضُ `shadcn/ui` المنقول كما هو. لا يُبقي شيئاً على الشاشة (Radix يفكّك
عنصرَه بانتهاء حركة الخروج) لكنّه يترك مؤقّتاً حيّاً وصفَّ حالةٍ ميّتاً.

### الإصلاح — أربعةُ أجزاء، **وسطحُ المكتب كما كان بالحرف**
- **أسفلَ الشاشة لا فوق رأسها**: `fixed inset-x-0 bottom-[5.5rem]`.
  و`bottom-[5.5rem]` **لا `bottom-0`**: زرُّ المساعد العائم
  (`AiChatDrawer.tsx`) في `bottom-5` بحجم `h-14` — يشغل `20..76px` من
  الأسفل، **فإشعارٌ ملتصقٌ بالحافّة كان ينقل المشكلة إلى زرٍّ آخر لا
  يحلّها**. ٨٨px + حشوةُ ١٦px ⟶ حافّةُ الإشعار عند ١٠٤px، فوقه بـ٢٨px.
- **مهلةٌ صريحة**: `toast_timing.ts` — أربعُ ثوانٍ للعاديّ و**سبعٌ للخطأ**
  (رسالةُ الفشل تُقرأ ويُتصرَّف بها لا تُلمَح). **ومهلةٌ صريحة من موضع
  النداء تعلو دائماً**، والفاسدةُ (صفرٌ · سالبٌ · `NaN` · لا نهائيّ) تُقرأ
  **غياباً لا أمراً** — `setTimeout` بفاسدةٍ يُطلق فوراً فيومض الإشعار.
- **`TOAST_REMOVE_DELAY` ⟶ ١٠٠٠ms** — أطولُ بكثير من حركة الخروج (١٥٠ms)
  فلا تُقتطَع، وليست ستَّ عشرةَ دقيقة.
- **زرُّ الإغلاق يُرى ويُضغَط**: `opacity-100` و`p-3` (هدفُ لمسٍ ٤٠px) على
  الهاتف، **و`sm:` تعيد سطحَ المكتب حرفياً** — شفافيةٌ صفرية وكشفٌ بالـhover
  وحشوةٌ صغيرة. وجسمُ الإشعار `pr-12 sm:pr-8` ليتّسع للزرّ الأكبر.

**والقرارُ في دالّةٍ خالصة** (`toast_timing.ts`) لا في نصّ المكوّن — المشروع
بلا مشغّل DOM، فقرارٌ داخل React لا يُختبَر إلّا بقراءة نصِّه، وقراءةُ النصّ
لا تُمسك انقلاباً يعود بصياغةٍ أخرى (درسُ ٤.u).

### وما لم يُمَسّ
الخادمُ كلُّه · القاعدةُ والترحيلات · **٢٥٥ موضعَ نداءٍ لـ`toast({...})`**
(ولا واحدٌ منها يرسل `duration` اليوم) · ألوانُ الإشعار ومتغيّراتُه
(`toast_tone.test.ts` أخضرُ بلا تعديل) · `TOAST_LIMIT = 1` (قاعدةُ Material:
الجديدُ يحلّ محلّ القديم) · اتّجاهُ السحب · بطاقةُ PWA · الشريطُ الجانبيّ.

### الترحيل
**بلا ترحيل** — لا عمودَ ولا جدولَ ولا قيد.

اختبارٌ دائم: `npm run test:toast-behavior` (قرارُ المهلة خالصاً بالفاسد
والصريح، والثوابتُ العاقلة وغيابُ `1000000`، والموضعُ أسفلَ الشاشة،
**وحافّةُ الإشعار تعلو زرَّ المساعد محسوبةً من أصناف `AiChatDrawer.tsx`
نفسِها** — فلو تحرّك ذلك الزرُّ أو كبر سقط البند — وسطحُ المكتب بحرفه، وزرُّ
الإغلاق، وتمريرُ المهلة من الدالّة القانونية بعد النشر لا قبله).
**ومُتحقَّقٌ بالعكس خمسَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ `top-0` تُسقط
د.١ ود.٢ · وإلصاقُه بالحافّة فوق زرّ المساعد يُسقط **د.٤ ود.٥ وحدهما** ·
وإخفاءُ زرّ الإغلاق يُسقط و.٢–و.٥ · وإعادةُ `1000000` تُسقط ج.٥ · وإسقاطُ
تمرير المهلة يُسقط ز.١–ز.٣.

---

## 4.ag سجلُّ محادثات المساعد — **يُحفَظ ويُقرأ** (ترحيل ٠٨٤ — ٢٠٢٦-٠٩-٢١)

**⚠ هذا القسم يعكس قاعدةً موثَّقة في القسم ٤.n بقرارِ المالك الصريح — لا
سهواً يُعاد.** فرعُ `claude/ai-chat-conversations`.

### الواقعة
القسمُ ٤.n يقول بالحرف: «**ولا محادثةٌ عادية تُحفَظ أبداً** (كما كانت دائماً)
— فقط لحظةَ تقديم اقتراحٍ صريح تُحفَظ لقطةُ ذلك التبادل بعينه». وكان ذلك
قراراً مقصوداً يومَه: خصوصيّةُ محتوى العيادة، وسطرُ `audit_log` للمساءلة بلا
نصّ. **وسأل المالكُ صراحةً**: «اين المشكلة ان حفظناها بقاعدة البيانات واقرا
مايكتبه الموظفون؟» — ثمّ قرّر الحفظ. فالمساعدُ صار جزءاً من العمل اليوميّ،
ومَن يديره يحتاج أن يعرف **ما يسأله موظّفوه وما يُجابون به**: أين يضلّون،
وأيُّ معرفةٍ ناقصة، وأيُّ جوابٍ خاطئ يتكرّر.

> **⚠ تصحيحُ اعتراضٍ كنتُ أخطأتُ فيه**: قلتُ للمالك أوّلاً إن جدولاً جديداً
> سيُرسَل في النسخة الاحتياطية اليومية إلى بريده. فسأل، فقُرئ `server/backup.ts`
> فعلاً: النسخةُ **قائمةُ أعمدةٍ مكتوبةٌ يدوياً** من خمسة جداول
> (`patients` · `branches` · `payments` · `expenses` · `systemSettings`) **لا
> تفريغٌ للمخطَّط كلِّه** — فجدولٌ جديد لا يبلغ بريداً إطلاقاً. الاعتراضُ كان
> خطأً، والقاعدةُ التي بُني عليها هذا القسم هي القراءةُ لا الافتراض.

### الجدولُ — صفٌّ لكلّ **تبادل**، لا لكلّ محادثة
نقطةُ `/api/ai/chat` تستقبل التاريخَ كاملاً في كلّ طلب (`messages.slice(-10)`)،
فحفظُه كلِّه يكرّر الرسائلَ القديمة صفّاً بعد صفّ. فيُحفَظ **التبادلُ الواحد**:
`lastUserQuestion(history)` — آخرُ ما كتبه المستخدم — وجوابُه.

| | |
|---|---|
| مَن يقرأ محادثاتِه | **صاحبُها** — كلُّ موظّفٍ له رقمُ مستخدمٍ حقيقيّ |
| مَن يقرأ الجميع | **المسؤولُ العام وحده** — `canReadAllConversations` |
| المدّة | **تسعون يوماً**، ثمّ تُمحى |
| النسخةُ الليلية | **لا تُمَسّ بحرف** — والجدولُ خارجَها أصلاً |

**ولا `branch_manager` في «كلُّ المحادثات»** — قياسٌ على معرفة المساعد (٤.n)
لا اختراع: «قرارُ معرفة المساعد للمسؤول العام حصراً»، وهذه أحسَسُ منها لأن
فيها **نصَّ ما كتبه الموظّف**. ومديرُ الفرع سلطتُه تشغيليةٌ في نطاقه، وقراءةُ
ما يكتبه فريقُه للمساعد ليست من ذلك النطاق. **ولا يُقاس هذا البابُ بالفرع
إطلاقاً**: نطاقُ الفرع يحرس بياناتِ المرضى، وهذا حرسُ محتوى موظّفين.

**والقرارُ في `shared/ai_conversations.ts`** لا في معالج النقطة — درسُ ٤.u
بحرفه: المشروعُ بلا مشغّل DOM، فقرارٌ مدفونٌ في معالجٍ لا يُختبَر إلّا بقراءة
نصِّه، وقراءةُ النصّ لا تُمسك انقلاباً في المعنى يعود بصياغةٍ أخرى. ويستورده
الخادمُ والشاشةُ معاً — **ولا نسخةَ ثانية** (درسُ ٤.l: شريطٌ جانبيٌّ كان يعيد
كتابة القاعدة يدوياً فينحرف صامتاً).

### والحفظُ لا يُضيّع جواباً أبداً
`recordExchange` تُنادى **بعد** بناء الردّ، **بلا `await`، وبمصيدةٍ صريحة** —
نفسُ حدّ `logAudit` في النقطة عينها. فعطلُ القاعدة أو امتلاءُ قرصٍ لا يعني أن
الموظّفَ يفقد إجابةً بين يديه. **ومحروسةٌ بـ`result.ok && access.userId`**:
جوابٌ لم يقع لا يُحفَظ له صفّ (مُثبَتٌ حيّاً عبر النقطة الحقيقية بلا مفتاح
مزوّد — ٥٠٣ وصفرُ صفوف).

**ولا يُحفَظ تبادلٌ فارغ**: سؤالٌ أو جوابٌ خالٍ يعني أن لا شيءَ يُقرأ.
**واسمٌ غائب يُقال «مستخدم #رقم»** لا يُترَك فارغاً — العمودُ `NOT NULL`،
وصفٌّ بلا صاحبٍ يُقرأ ليس سجلّاً. **والأسماءُ والأدوارُ لقطاتٌ لا `join`**
(درسُ `medical_exams.doctor_name`): يبقى السجلُّ مقروءاً بعد تغيير الحساب أو
حذفه.

### ومعرّفُ المحادثة **تجميعٌ لا هويّة ولا إذن**
تسكّه الشاشةُ عند فتح الدرج بـ`mintSubmissionToken`/`nextSubmissionToken`
القائمتين — **لا مُولِّدَ معرّفاتٍ ثانٍ في المستودع** — ويُصفَّر عند الإغلاق،
فكلُّ فتحةٍ خيطٌ واحد. **والقراءةُ تُرشَّح بـ`userId` من الجلسة دائماً**:
معرّفُ خيطِ زميلٍ يُرجع **مصفوفةً فارغة** لا صفوفَه (مُثبَتٌ). فمعرّفٌ ملفَّق
من عميلٍ خبيث أقصى أثره أن يخلط خيوطَ صاحبه هو. **ونصٌّ غريبٌ يُقرأ «بلا
تجميع» ولا يُكتب كما وصل** (`sanitizeConversationId`) — والطويلُ يُردّ لا
يُقصّ.

### ونافذةُ التسعين يوماً تُفرَض **عند القراءة** لا بالمحو وحده
وعدُ الاحتفاظ لا يُبنى على نجاح كرونٍ ليليّ قد يتأخّر أو يتعطّل: **كلُّ قراءةٍ
مرشَّحةٌ بالنافذة نفسِها**، فصفٌّ تجاوزها **لا يُعرَض ولو بقي في الجدول** —
للموظّف وللمسؤول وللخيط سواءً. والمحوُ الفيزيائيُّ كرونٌ يوميّ (٠٦:٣٠ بغداد)
يحرّر المساحة ويصدق الوعدَ في القاعدة أيضاً.

**وبلا حجزٍ يوميّ** خلافاً لكرون النسخة الليلية: إرسالُ بريدٍ مرّتين **يُرى**،
أمّا `DELETE` بشرطٍ ثابت فمثيلان يشغّلانه معاً ⟶ الأوّلُ يمحو والثاني يمحو
صفراً، والنتيجةُ واحدة. فحجزٌ هنا تعقيدٌ بلا ثمرة، **ونسخةٌ ثانية من بدائيّة
تزامنٍ قائمة** أسوأُ من غيابها. **ولا `DELETE` بلا `WHERE` أبداً** (القسم ٨).

### وملفُّ النقاط **قراءةٌ محضة**
`server/ai/conversations/routes.ts` بلا `POST` ولا `PATCH` ولا `DELETE` ولا
SQL كتابةٍ إطلاقاً — **سجلٌّ يُقرأ ولا يُنقَّح**، وإلّا صار مَن يُقرأ عليه
قادراً على تنقيحه. **والكتابةُ الوحيدة** `recordExchange` داخل `/api/ai/chat`
وحدها (يحرسه فحصٌ معماريّ).

| النقطة | مَن يفتحها |
|---|---|
| `GET /api/ai/conversations/mine` | كلُّ موظّفٍ مصادَق — صفوفُه هو وحدها |
| `GET /api/ai/conversations` | المسؤولُ العام حصراً (بترشيحٍ اختياريٍّ بموظّف) |
| `GET /api/ai/conversations/users` | المسؤولُ العام حصراً — لمرشِّح الشاشة |
| `GET /api/ai/conversations/thread/:id` | صاحبُ الخيط، أو المسؤولُ العام |

**والمشوَّهُ يُردّ ولا يُقرأ «الكلّ»**: رقمُ مستخدمٍ غيرُ صحيح ⟶ ٤٠٠، بينما
**الفراغُ الصريح ⟶ «الكلّ» صحيحاً** (نفسُ تمييز ٤.n بين الغياب والتشويه).
وحجمُ صفحةٍ هائل يُقصَر على سقف الخادم لا يُؤخَذ من العميل.

### الشاشتان
- **«محادثاتي»** — لوحةٌ ثالثة في درج المساعد بجوار «التدريب» (وفتحُ إحداهما
  يُغلق الأخرى: جسمُ الدرج واحد). أسئلتُه السابقة وأجوبتُها، **قراءةٌ محضة**.
- **«سجلّ المحادثات»** — تبويبٌ في لوحة المسؤول بجوار «معرفة المساعد»، مع
  مرشِّحٍ بالموظّف. **بلا زرِّ تعديلٍ أو حذف.**

**وتنظيفُ الدرج عند الإغلاق يبقى كما هو بحرفه** — شأنُه ذاكرةُ المتصفّح: لا
سياقَ قديم يُرسَل إلى النموذج في فتحةٍ جديدة، ولا نصَّ مريضٍ يبقى في الصفحة.
**والعبارةُ «لا شيء يُحفظ أصلاً» في تعليقه صُحِّحت** — لم تعد صحيحة.

### وما لم يُمَسّ
**سطرُ `audit_log` بـ`entity_type = 'ai_chat'` بحرفه** — يبقى للمساءلة بلا
نصّ سؤالٍ ولا جواب (أرقامُ أدواتٍ ومقالاتٍ فقط)، وهذا السجلُّ الجديد شيءٌ آخر
بجواره لا بديلٌ عنه · **و`server/backup.ts` بلا حرفٍ واحدٍ يتغيّر** (مُثبَتٌ
في الفرق وفي الاختبار) · ولا تغييرَ في معرفة المساعد ولا في اقتراحات الموظّفين
(٤.n) ولا في التدريب ولا في الأدوات السبع ولا في `resolveAiAccess` ولا في
عزل الفرع ولا في عزل المال · ولا حرفَ في العلاج الطبيعي ولا المرضى ولا المال ·
وواتساب وتلغرام كما هما.

### الترحيل
**٠٨٤ فقط** — إضافيّ، idempotent: جدولٌ واحد وثلاثةُ فهارس. **بلا `DROP` ولا
`DELETE` ولا `UPDATE`**، ولا مسٍّ لترحيلٍ من ٠٠١ إلى ٠٨٣. **ومفاتيحُه إلى
`system_users`/`branches` لا إلى `patients` ولا أحد توابعها** — فالقاعدةُ
الملزمة في القسم ٨ لا تنطبق، **وهذا مُثبَتٌ باستعلام `pg_constraint` في
الاختبار لا مفترَض**.

اختبارٌ دائم: `npm run test:ai-conversations` (حيّاً على Postgres وعلى النقاط
الحقيقية: حالةُ القاعدة وفهارسُها وidempotency الترحيل **وغيابُ مفتاحِ
`patients`** وغيابُ الجدول عن النسخة البريدية · الكاتبُ القانونيّ وما يرفضه ·
«محادثاتي» لأربعة أدوار · **«كلُّ المحادثات» للمسؤول وحده و٤٠٣ للأربعة
الباقين ومنهم مديرُ الفرع** · الخيطُ ليس إذناً · النافذةُ عند القراءة والصفُّ
باقٍ في الجدول · المحوُ بشرطه وidempotency · المدخلاتُ المشوَّهة · **وجوابٌ
لم يقع لا يُحفَظ له صفّ عبر النقطة الحقيقية** · وحارسٌ معماريّ) و
`npm run test:ai-conversations-pure` (القرارُ الخالص: البوّابتان، معرّفُ
التجميع، القصُّ، آخرُ سؤال، النافذة، سقفُ الصفحة — بلا قاعدة بيانات).

**ومُتحقَّقٌ بالعكس ستَّ مرّات، كلُّ آليّةٍ على حدة**: فتحُ «كلّ المحادثات»
لمدير الفرع يُسقط **د٤.mgr و د٥.mgr** وحدهما · وإسقاطُ ترشيح صاحب الخيط يُسقط
**هـ٢** وحدَه · ورفعُ النافذة عن «محادثاتي» يُسقط **و١** وحدَه · وقلبُ شرط
المحو يُسقط **ز٣** وحدَه · وإسقاطُ حارس `result.ok` يُسقط **ط٢ وي٤** ·
وحفظُ التاريخ كلِّه بدل آخرِ سؤال يُسقط **ي٥** وحدَه.

> **⚠ وأمسك العكسُ ضعفاً في الاختبار نفسِه**: البندُ **ط٢** («صفرُ صفوفٍ
> محفوظة») كان يمرّ **صدفةً** — الكتابةُ «أطلق وانسَ» فلم تكن قد وقعت لحظةَ
> عودة الردّ، لا أن الحارسَ منعها. فأُضيفت مهلةُ استقرارٍ قبل القياس،
> وأُعيد العكسُ فسقط البندان معاً كما يجب. **فحصٌ يمرّ لسببٍ خاطئ ليس فحصاً.**

### تصحيحٌ لاحق — مراجعةٌ آلية على #٣٧٢ (٢٠٢٦-٠٩-٢١)

المعماريّةُ أعلاه ثابتة؛ ثغرتان بلّغتهما مراجعةٌ آلية قبل الدمج، **وثالثةٌ
أمسكها الاختبارُ الحيُّ أثناء إصلاح الثانية**. عولجت الثلاثُ في الفرع نفسِه.

**١) حذفُ فرعٍ كان يُحبَس بهذا السجلّ.** `branch_id` مفتاحٌ بـ`NO ACTION`،
و`storage.deleteBranch` يحذف تابعيه الستّة ثمّ الفرعَ نفسَه — **ولا يعرف هذا
الجدول ولا يلتقط ٢٣٥٠٣**. فحذفُ فرعٍ تحادث فيه أحدٌ يوماً كان **يفشل بنصّ
Postgres خامّ على وجه المستخدم**. مُعادٌ إنتاجُه حيّاً قبل أيّ تعديل.

**والحلُّ `ON DELETE SET NULL` لا `CASCADE` ولا إضافةٌ إلى قائمة الحذف**:
`branch_name` **لقطةُ نصٍّ** أصلاً بحكم تصميم هذا الجدول، فالصفُّ يبقى مقروءاً
كما كُتب ويسقط الرقمُ وحده. و`CASCADE` كان سيمحو سجلّاً وُضع ليُقرأ، وإضافتُه
إلى `deleteBranch` كذلك. ومعه كتلةٌ idempotent في الترحيل نفسِه تقرأ
`pg_constraint.confdeltype` فتُعيد بناءَ **المفتاح وحده** لقاعدةٍ طُبّق عليها
الترحيلُ قبل التصحيح (`CREATE TABLE IF NOT EXISTS` لا تصلحه) — **ولا صفَّ
يُمَسّ ولا عمودَ ولا جدول**.

> **⚠ و`user_id` لم يُمَسّ عمداً**: عمودٌ `NOT NULL`، فـ`SET NULL` مستحيلةٌ
> عليه و`CASCADE` كانت ستمحو سجلّاً بحذف حساب. ونقطةُ «حذف الموظّف نهائياً»
> تلتقط ٢٣٥٠٣ **أصلاً** وتردّ برسالةٍ عربية تدلّ على البديل («يمكنك إبقاء
> الحساب معطلاً») — فلا عطبَ هناك يُصلَح.

**٢) السقفُ خمسون صفّاً بلا مؤشّر.** `boundedPageSize` يقصر القائمة على
خمسين، **ولا مؤشّرَ في النقطتين ولا زرَّ في الشاشتين**. فموظّفٌ تجاوز الخمسين
— أو عيادةٌ كاملةٌ في شاشة المسؤول — يبقى أقدمُه محفوظاً تسعين يوماً **ولا
سبيلَ إلى قراءته من الشاشة التي وُعد بها**.

**والحلُّ مؤشّرُ مفتاحٍ لا `OFFSET`**: الصفوفُ تُضاف في الرأس، فـ`OFFSET 50`
بعد تبادلين يعيد صفَّين قُرئا ويقفز عن غيرهما. والشرطُ مقارنةُ صفٍّ مركَّبة
`(created_at, id) <` — **ترتيبُ الفهرسين القائمين حرفاً بحرف** فيُخدَم بلا
فرز. والجلبُ بـ`limit + 1`: الصفُّ الزائد هو الدليلُ على وجود تالٍ، فلا
`COUNT(*)` ثانٍ على كلّ نداء ولا ادّعاءَ «لا مزيد» لصفحةٍ امتلأت بالمصادفة.

**والمشوَّهُ يُقرأ غياباً لا خطأً** — مؤشّرٌ بائتٌ من تبويبٍ قديم يُعيد الصفحةَ
الأولى، وذاك أهونُ من شاشةٍ فارغة بخطأ. **والعزلُ لم يضعف**: الترشيحُ
بـ`user_id` من الجلسة يسبق المؤشّرَ دائماً، فمؤشّرُ زميلٍ لا يُسرّب صفّاً.

**والشاشتان `useInfiniteQuery` لا تراكمٌ يدويٌّ في `useState`**: مفتاحُ
الاستعلام هو نفسُه الذي يُبطِله `askMutation.onSuccess` **بحرفه**، فتُعاد
الصفحاتُ المحمَّلةُ كلُّها ويُشتقّ مؤشّرُ كلٍّ من سابقتها — فيظهر السؤالُ
الجديد في الرأس بلا أن تُصفَّر الصفحاتُ التي فتحها الموظّف. وتراكمٌ في حالةٍ
محليّة كان سيبقى بائتاً بعد ذلك الإبطال بلا أن ينتبه أحد. **والرابطُ من دالّةٍ
مشتركة** (`conversationsPageUrl`): الافتراضيُّ في `queryClient.ts` يبنيه من
`queryKey.join("/")` فلا موضعَ فيه لسلسلة استعلام — **وهو بعينه سببُ أن
المؤشّرَ لم يكن ليبلغ الخادمَ لو تُرك للافتراضيّ**.

**٣) والمؤشّرُ بالميكروثانية لا بالملّي ثانية — أمسكه الاختبارُ الحيّ.**
`created_at` عمودُ `TIMESTAMPTZ` دقّتُه ميكروثانية، و`Date` في جافاسكربت تقف
عند الملّي ثانية. فمؤشّرٌ من `Date.getTime()` **يقصّ الكسرَ صامتاً**، فيصير
الحدُّ أقدمَ من الصفّ الذي جاء منه: صفٌّ في الملّي ثانية نفسِها بميكروثانيةٍ
أكبر **يسقط من الصفحتين معاً** — لا الأولى تعرضه (هو بعدها في الترتيب) ولا
الثانية (ليس «أقدمَ» من الحدّ المقصوص).

**وقع فعلاً**: اثنا عشرَ صفّاً كُتبت في المصفوفة نفسِها، فبلغت القراءةُ أحدَ
عشر — **وصفٌّ واحدٌ اختفى بلا أن يقول أحدٌ شيئاً**. فالقيمةُ تُقرأ من SQL
(`(EXTRACT(EPOCH FROM created_at)::numeric * 1000000)::bigint`) لا من `Date`،
ويُبنى الحدُّ بحسابٍ صحيحٍ دقيق (`TIMESTAMPTZ 'epoch' + N * INTERVAL '1
microsecond'`) — **بلا عائمٍ ومهما كانت نسخةُ Postgres**. وقيمةُ المؤشّر
الداخلية **لا تُسلَّم مع الصفوف** (`toRow` يبني حقولَه صريحةً): المؤشّرُ
يُسلَّم مرّةً واحدة في `nextCursor` مبهماً.

**وثابتٌ للاختبار نفسِه**: البنودُ الأولى كتبت صفوفَها بـ`NOW()`، فوقوعُها في
الملّي ثانية نفسِها **مصادفةٌ تتبع سرعةَ الآلة**. فأُضيف قسمٌ يثبّت الأختامَ
صراحةً — ثلاثةُ صفوفٍ في ملّي ثانيةٍ واحدة بميكروثانياتٍ متمايزة — **فصار
الحارسُ حتميّاً لا تُسقطه آلةٌ أسرع.**

### الترحيل (التصحيح)
**بلا ترحيلٍ جديد** — ٠٨٤ نفسُه عُدِّل (لم يُدمَج بعد) فحمل `ON DELETE SET
NULL` ومعه كتلةُ الإصلاح idempotent. والمؤشّرُ **قراءةٌ محضة**: لا عمودَ
جديداً ولا فهرسَ جديداً — الفهرسان القائمان هما ترتيبُ المقارنة بعينه.

اختبارٌ دائم — ثلاثةُ أقسامٍ جديدة في `npm run test:ai-conversations`
(**ك**: اثنا عشرَ صفّاً في ثلاث صفحات، الصفوفُ كلُّها بلغتها القراءة بأعيانها
وبترتيبها بلا تكرارٍ ولا قفز · والصفحةُ الأولى بلا مؤشّرٍ كما كانت بحرفها ·
والمشوَّهُ يُعيدها · واثنا عشرَ بسقف اثني عشر ⟶ لا مزيد · **وثابتٌ تحت إدراجٍ
بين الصفحتين** · والنقطتان الحقيقيتان ترفعان المؤشّرَ وتقبلانه · **ومؤشّرُ
زميلٍ لا يُسرّب صفّاً** · والمرشِّحُ يبقى مطبَّقاً مع المؤشّر · **وثلاثةُ صفوفٍ
في ملّي ثانيةٍ واحدة تُقرأ بأعيانها** والمؤشّرُ يحمل كسرَها. **ل**: حذفُ فرعٍ
ينجح، والصفُّ باقٍ، والرقمُ وحده سقط، ولقطةُ الاسم كما كُتبت، والمفتاحُ
`SET NULL` في القاعدة. **م**: عقدُ الشاشتين — الرابطُ من الدالّة المشتركة،
و«هل من مزيد؟» من قول الخادم، وزرٌّ مشروطٌ به، ومفتاحُ الإبطال كما كان، ولا
تراكمَ يدويّ). ومعه أقسامٌ جديدة في `npm run test:ai-conversations-pure`
(الترميزُ والفكُّ بالميكروثانية، وثلاثَ عشرةَ صورةً مشوَّهة، وصفحةُ
`limit + 1`، ورابطُ الصفحة بسبع حالات، و«لا مزيد» بستّ صور).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ المؤشّر إلى
الملّي ثانية تُسقط **ك٢٥–ك٢٨ بالضبط** (أربعة، كلُّها من قسم الدقّة الحتميّ) ·
وإسقاطُ المؤشّر إلى سقفٍ نهائيٍّ وحده يُسقط **١١ بنداً كلُّها من القسم ك** ·
وإعادةُ المفتاح إلى `NO ACTION` تُنتج **٢٣٥٠٣ خامّاً من `deleteBranch`** —
العطبُ المُبلَّغ بعينه — بعد أن يمرّ ل١ وحدَه.

### تصحيحٌ ثانٍ — مراجعةٌ آلية ثانية على #٣٧٢ (٢٠٢٦-٠٩-٢١)

ثلاثُ ثغراتٍ أخرى على الفرع نفسِه. المعماريّةُ أعلاه ثابتة؛ **وبلا ترحيلٍ
جديد** ولا عمودٍ ولا جدولٍ ولا حرفٍ في القاعدة.

**١) محتوى المحادثة كان يُسكَب في سجلّ الطلبات.**
وسيطُ التسجيل في `server/index.ts` يلتقط **جسمَ كلّ استجابة** `/api` ويكتبه
نصّاً بعد المرور بـ`redactForLog` — **وقائمةُ الحجب لم تكن تعرف `question`
ولا `answer`**. فصفحةٌ واحدة من «كلّ المحادثات» تسكب **خمسين تبادلاً**
لموظّفين كثيرين في سجلّ العملية: نسخةٌ **خارج نافذة التسعين يوماً** وخارج
حارسِ «المسؤولُ العام وحده»، يقرؤها كلُّ مَن يصل السجلّ. والصفحةُ تُجلَب عند
كلّ فتحٍ وكلّ «عرض المزيد».

**والحجبُ بالاسم لا بالمسار** — قاعدةُ `server/log_redaction.ts` المكتوبة
فيه بالحرف: «قائمةُ مساراتٍ كانت ستنسى أوّل وافدٍ عليها». فأُضيف `question`
و`answer` إلى `REDACTED_LOG_KEYS`، **ومعهما `reply`**: هو **النصُّ عينُه**
من `POST /api/ai/chat` (يُحفَظ `answer` في الجدول ويُرسَل `reply` في الردّ)،
فحجبُ أحدِهما دون الآخر يترك المحتوى نفسَه يتسرّب صفّاً صفّاً من النقطة التي
**تُنشئ** الصفوف. وفُحص المستودعُ قبل الإضافة: لا نقطةَ أخرى تُعيد حقلاً
بهذه الأسماء (الاستبياناتُ تستعمل `questionText`/`textValue`، و`question`
الوحيدةُ الأخرى تعيش داخل حمولة نموذجٍ لا تبلغ استجابةً).

**والاستجابةُ إلى العميل لم تتغيّر بحرف** — الحجبُ نسخةٌ للسجلّ وحده كما
يقول الملفُّ أصلاً، **ومُثبَتٌ حيّاً** (البند ن٢) لا مفترَضاً. **وما يبقى في
السجلّ هو ما يبقى في `audit_log` عمداً**: مَن سأل ومن أيّ فرعٍ وبأيّ وضعٍ
ومتى — يكفي للتشخيص بلا محتوى العيادة. **وسطرُ `audit_log` نفسُه لم يُمَسّ**.

**٢) رفضُ وعدٍ عارٍ كان يترك الطلبَ بلا استجابة أبداً.**
المعالِجاتُ الأربعة في `server/ai/conversations/routes.ts` غيرُ متزامنة بلا
`try/catch`. وExpress 4 لا يلتقط رفضَ الوعود، **ومعالِجُ `unhandledRejection`
في `index.ts` لا يملك `res` أصلاً** فلا يستطيع الردّ — يُبقي العمليةَ حيّةً
ويترك **ذلك الطلبَ معلَّقاً إلى الأبد**. فعطلُ قاعدةٍ عابر (مهلةُ قفل،
انقطاعُ اتّصال) كان يجعل شاشةَ السجلّ تدور بلا نهاية بدل أن تقول «تعذّر» —
وهو بعينه عطبُ حذف المستخدم ومقالة المعرفة المسجَّل في تعليق `index.ts`.

**والمخرجُ هو القائم في `server/ai/knowledge/routes.ts` بحرفه**: `try/catch`
حول كلّ معالجٍ يلمس القاعدة و`next(err)` يوصله إلى وسيط الأخطاء العامّ.
**والحُرّاسُ قبله كما هي**: ٤٠٣ و٤٠٠ تبقى ردوداً مبكّرة لا استثناءات.

**٣) أسماءُ الأدوات الخامّة كانت تُسلَّم للمتصفّح.**
`toolNames` (`patient_lookup`، `financial_summary`…) أسماءٌ تقنيةٌ داخلية،
و`POST /api/ai/chat` **يُسقطها عمداً** من ردّه (`const { tools: _rawTools,
...clientSafe }`) فلا يصل العميلَ إلّا `toolsUsed` المُترجَمة — «والاسمُ
الخامّ لا يصل العميل إطلاقاً بعد اليوم» (القسم ٤.n). وسجلُّ المحادثات كان
يسلّمها في كلّ صفّ **ولا شاشةَ من الاثنتين تقرؤها**، فصار البابَ الخلفيَّ لما
أُغلق هناك. فأُسقطت من `ConversationRow` ومن `toRow`. **والعمودُ يبقى في
القاعدة** ويكتبه `recordExchange` كما كان للتدقيق (مُثبَتٌ، البند ع١) —
و`knowledgeIds` تبقى كما هي: أرقامُ مقالاتٍ يرسل `/api/ai/chat` عناوينَها
للعميل أصلاً في سطر «اعتمدتُ على: …»، فليست من هذا الصنف.

اختبارٌ دائم — ثلاثةُ أقسامٍ جديدة في `npm run test:ai-conversations`
(**ن**: الجسمُ الحقيقيُّ من النقاط الثلاث يمرّ بتعبير `index.ts` بعينه
(`JSON.stringify(redactForLog(...))`) فلا نصَّ سؤالٍ ولا جواب · والحجبُ وقع
فعلاً لا أنّ الجسمَ فارغ · وما يُشخَّص به باقٍ · و`reply` محجوبٌ كذلك ·
**والاستجابةُ إلى العميل كاملةٌ كما هي** · وحارسٌ معماريٌّ على وسيط التسجيل.
**س**: عطلُ قاعدةٍ حقيقيّ (بحقنٍ على `pool.query` لهذا الجدول وحده، يُستعاد
في `finally`) على النقاط الأربع ⟶ **ردٌّ حقيقيّ ٥٠٠ ضمن مهلةٍ صريحة** لا
تعليقٌ، والأخطاءُ الأربعة بلغت وسيطَ الأخطاء، **ولا رفضٌ عارٍ واحد**،
والقاعدةُ تعمل بعدها. **ع**: العمودُ محفوظٌ في القاعدة · ولا مفتاحَ
`toolNames` في أيٍّ من ستّة أسطح (ثلاثُ نقاطٍ وثلاثُ دوالّ مخزن) · ولا اسمُ
أداةٍ في أيّ موضعٍ من الصفّ · وما تقرؤه الشاشةُ باقٍ · وحارسٌ معماريّ على
بناء `toRow` الصريح).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: رفعُ الأسماء الثلاثة
من قائمة الحجب يُسقط **١٠ بنودٍ كلُّها من القسم ن** · وإعادةُ المعالِجات إلى
رفضٍ عارٍ تُسقط **١٠ بنودٍ كلُّها من القسم س** (والنقاطُ الأربع تنتهي بالمهلة
فعلاً، والرفضُ يتسرّب إلى `unhandledRejection` — العطبُ بعينه) · وإعادةُ
`toolNames` إلى `toRow` تُسقط **١٣ بنداً كلُّها من القسم ع**. ولا شيءَ خارج
قسمِه يسقط في أيٍّ من الثلاث.

### تصحيحٌ ثالث — مراجعةٌ آلية ثالثة على #٣٧٢ (٢٠٢٦-٠٩-٢١)

ثغرتان أخريان، **وبلا ترحيلٍ ولا حرفٍ في القاعدة**.

**١) مربّعُ السؤال كان ظاهراً فوق «محادثاتي»، والإرسالُ منه يختفي.**
جسمُ الدرج واحد، ففتحُ «محادثاتي» يُخفي لوحةَ المحادثة. وكان المربّعُ
محروساً بـ`!trainingOpen` **وحده** فيبقى ظاهراً فوقها — فالإرسالُ منه يُلحق
السؤالَ والجوابَ ومؤشّرَ الانتظار باللوحة **المخفيّة**: لا يرى الموظّفُ
شيئاً يحدث فيظنّ الإرسالَ فاشلاً ويعيده، والجوابُ ينتظره خلف زرٍّ لا يعرف
أنّ عليه ضغطَه. **و«محادثاتي» قراءةٌ محضة** بحكم تصميمها، **ولوحةُ التدريب
تُخفيه أصلاً** — فالاتّساقُ أن تُخفيه كلتاهما، والعودةُ ضغطةٌ واحدة على زرٍّ
معنونٍ «المحادثة». (شرطٌ واحد تغيّر، ولا حرفَ في `send` ولا في الطفرة.)

**٢) ومرشِّحُ المسؤول كان يسمّي الموظّفَ بالأكبر أبجدياً لا بالأحدث.**
`MAX(user_name)` تختار الأكبرَ **أبجدياً** بين لقطات النافذة — لا علاقةَ له
بالزمن. فموظّفٌ تغيّر اسمُه خلال التسعين يوماً كان المرشِّحُ يسمّيه باسمه
**القديم** بينما تسمّيه صفوفُه الأحدثُ بالجديد، فيبدو المرشِّحُ وقائمتُه عن
شخصين. فصار الاسمُ **أحدثَ لقطة** بترتيب القراءة نفسِه `(created_at, id)`.

**ولا `join` إلى `system_users`**: مبدأُ هذا الجدول أنّ الأسماء لقطاتٌ تبقى
مقروءةً بعد تغيير الحساب أو حذفه — فالصوابُ أحدثُ لقطة لا حسابٌ حيٌّ قد لا
يوجد أصلاً.

اختبارٌ دائم — قسمان جديدان في `npm run test:ai-conversations` (**ف**:
اسمان يسبق فيهما الجديدُ القديمَ أبجدياً وختمان صريحان — فلو عادت `MAX`
لأعطت القديم؛ والمرشِّحُ لا يخالف أحدثَ صفوفه؛ ولا يُقرأ الاسمُ من الحساب
الحيّ. **ص**: عقدُ الشاشة — المربّعُ محجوبٌ عن اللوحتين معاً، ولوحتان
مفتوحتان معاً حالةٌ لا تقع، والعودةُ معنونة).

**ومُتحقَّقٌ بالعكس مرّتين**: إعادةُ `MAX(user_name)` تُسقط **ف٢ وف٤ بالضبط**،
وإعادةُ الشرط إلى `!trainingOpen` وحده تُسقط **ص١ وحدَه**.

**ومرساةُ حدودٍ في اختبارٍ قائم**: `client/src/components/ai_chat_input.test.ts`
بندُ **هـ.١٠** (من القسم ٤.ae) كان يثبّت الحارسَ بالنصّ الحرفيّ
`"{!trainingOpen && ("`، وقد اكتسب شرطاً ثانياً. فصارت المرساةُ تطابق **بنية**
الحارس لا نصَّ شروطه — وهذا ما يقوله البندُ المجاور هـ.١٠ج أصلاً: «أيُّ إعادة
تركيبٍ تُقاس **مهما كان الشرطُ الذي فكّكه**». **وموضوعُ البند لم يتغيّر
بحرف**، ومُتحقَّقٌ أنه ما زال يسقط حين يُزال الحارسُ كلُّه.

---

## 4.ah تعريفُ صاحب المراكز — مقالةُ معرفةٍ واحدة (ترحيل ٠٨٥ — ٢٠٢٦-٠٩-٢١)

**المساعدُ صار يعرف مَن هو الدكتور ياسر الساعدي — من جدولٍ يحرّره المسؤولُ
العام بلا نشر كود، لا من سطرٍ مدفونٍ في نصّ النظام.** فرعُ
`claude/assistant-owner-profile`.

### الواقعة
الموظّف يسأل «من هو الدكتور ياسر الساعدي؟» أو «من صاحب المراكز؟» فلا تطابق
سؤالَه مقالةٌ واحدة من الثماني عشرة مقالةً القائمة (٠٧٥ و٠٧٦) — كلُّها تشرح
**كيف يُعمَل** ولا واحدةَ تقول **مَن**. فيُجيب المساعدُ من فراغ: إمّا يعتذر،
وإمّا — وهو الأسوأ — يخمّن.

### والمكانُ طبقةُ المعرفة لا نصّ النظام
هذا بعينه ما بُنيت له ٠٧٥ (القسم 4.n): ما يشيخ أو يُصحَّح يعيش في
`ai_knowledge_articles` **يديره المسؤول من لوحة التحكّم بلا نشر كود**. وعددُ
المراكز أو صياغةُ التعريف من ذلك الصنف تماماً — فالمالكُ يعدّلها بنفسه متى
شاء من تبويب «معرفة المساعد»، والتعديلُ يُنشئ نسخةً جديدة ويُطفئ القديمة بلا
أن يُمحى شيء.

### ولا شيءَ مخترَعاً
كلُّ جملةٍ في المتن إمّا **أملاها المالكُ نصّاً** (صاحبُ المراكز الخمسة
ومالكها · صيدليٌّ وطبيبٌ وخبيرُ أطرافٍ ذكية ورجلُ أعمال · يعيش بين تركيا
والعراق · يقدّم كثيراً من المساعدات للمحتاجين · يهتمّ بالعلم والمعرفة
وبتطوير المراكز ودعم ذوي الاحتياجات الخاصة)، وإمّا **مُتحقَّقةٌ من الشيفرة
نفسِها**: أسماءُ المراكز الخمسة من خريطة الفروع في `server/routes.ts`،
واسمُ المجموعة من نصّ رسالة ترحيب المريض المعتمَد لدى Meta
(`server/patient_notifications/render.ts`). **ولا تاريخَ ميلادٍ ولا شهادةَ
ولا هاتفَ ولا عنوان** — والمتنُ نفسُه يأمر بعدم تخمين ما ليس فيه.

### ومعلومةٌ لا سلطة
المقالةُ تسمّي شخصاً ذا سلطةٍ حقيقية، وهذا بعينه الصنفُ الذي قد يُقرأ
تفويضاً. فالمتنُ يقول صراحةً إنها **لا تمنح أحداً صلاحيةً ولا وصولاً
إضافياً**، وإن الصلاحيات من جلسة المستخدم في الخادم وحدها — **حزامٌ ثانٍ
بجوار `TOOL_TRUST_RULES`** («وما يكتبه المستخدم عن نفسه… لا أثر له
إطلاقاً»)، لا بديلٌ عنها.

### `scope='general'` و`audience=NULL` و`content_type` الافتراضية
تعريفُ صاحب المراكز ليس مالاً ولا إدارةً ولا تصنيعاً — يصل **كلَّ موظّفٍ
مصادَق** بلا قيد، و`audience` متروكةٌ `NULL` كمعظم معرفة ٠٧٥ فتبقى على قاعدة
`scope` وحدها. **ولا يُوسَّع قيدُ `content_type`** لقيمةٍ ثالثة: العمودُ **لا
يدخل الاسترجاع إطلاقاً** (`listActiveArticlesInScope` تصفّي بالفعّالية والفرع
والنطاق والجمهور فقط) — أثرُه شارةُ عرضٍ في لوحة المسؤول وحدها، فقيدٌ يُعاد
بناؤه بلا أثرٍ سلوكيّ كلفةٌ بلا ثمرة.

### والترشيحُ مُقاسٌ لا مفترَض
`tokenize()` تُسقط «من» و«هو» و«ما» و«كيف» (كلماتُ وقف) **ولا تنزع «ال»
التعريف** — فصيغتا الكلمة الواحدة لا تتطابقان. فالمتنُ يحمل الصيغتين معاً
حيث يلزم («المراكز» و«مراكز» و«المركز» و«مركز» · «الدكتور» و«دكتور» ·
«بايونك» و«بايونيك»)، **وقيس ذلك على المجموعة الكاملة (١٩ مقالة) لا بالتقدير**:
اثنا عشر سؤالَ هويّةٍ تُرجعها **أوّلاً**، وثمانيةُ أسئلة مسارِ عملٍ **لا
تُرجعها إطلاقاً**.

### وما لم يُمَسّ
`GENERAL_SYSTEM_PROMPT` و`SYSTEM_PROMPT` · `TOOL_TRUST_RULES` · خوارزميةُ
الترشيح وبوّابةُ النيّة (`shared/ai_knowledge_retrieval.ts` بلا حرف) ·
الأدواتُ السبع · طبقةُ التدريب (٠٧٦) · سجلُّ المحادثات · الصلاحياتُ وعزلُ
الفرع وعزلُ المال · واتساب وتلغرام · **وكلُّ ترحيلٍ من ٠٠١ إلى ٠٨٤**.

### الترحيل
**٠٨٥ فقط** — صفٌّ واحد بـ`ON CONFLICT (seed_key) DO NOTHING`. **بلا `CREATE`
ولا `ALTER` ولا `DROP` ولا `DELETE` ولا `UPDATE` ولا عمودٍ ولا جدولٍ جديد**
— ولا مفتاحَ أجنبيّاً إلى `patients` ولا أحد توابعها، فالقاعدةُ الملزمة في
القسم ٨ لا تنطبق.

اختبارٌ دائم: `npm run test:owner-profile` (٤٧ تأكيداً حيّاً على Postgres وعلى
`retrieveKnowledge` نفسِها التي يناديها `chat.ts`، خلف بوّابة النيّة عينها:
شكلُ الصفّ · إعادةُ الزرع لا تكرّر **ولا تكتب فوق تحريرٍ قام به المسؤول** ·
**الحقائقُ العشر التي أملاها المالك لا تسقط منها واحدة** · **أسماءُ المراكز
الخمسة مطابقةٌ لخريطة الفروع الحيّة المقروءة من مصدر `routes.ts`** فإن فُتح
سادسٌ أو تغيّر اسمٌ سقط البند · نفيُ منح الصلاحية ومنعُ التخمين وخلوُّ المتن
من رقمٍ شخصيّ · اثنا عشر سؤالَ هويّةٍ تُصيبها **لموظّف استقبالٍ عاديّ لا
مسؤول** · ثمانيةُ أسئلة مسارِ عملٍ لا تُصيبها · وحارسٌ معماريّ: الترحيلُ زرعٌ
لا بناء).

**ومُتحقَّقٌ بالعكس خمسَ مرّات، كلُّ آليّةٍ على حدة**: إزالةُ الزرع تُسقط كلَّ
شيءٍ برسالةٍ صريحة لا بانهيار · و`scope='finance'` تُسقط **أ٢ والاثني عشر
سؤالَ هويّةٍ وحدها** (فالقسمُ «و» يقيس وصولَ موظّفٍ عاديّ لا وجودَ صفّ) ·
واسمُ مركزٍ لا يطابق الخريطة يُسقط **د٢ وحدَه** · وحقيقةٌ ساقطة تُسقط **بندَها
وحدَه** · وتجريدُ العنوان يُسقط **أ٨ وحدَه** — فالمتنُ وحده يكفي اليوم
لإصابة كلّ أسئلة الهويّة، وبندُ العنوان يحمي الترتيبَ حين تكبر المجموعة لا
يتوقّف عليه الاسترجاعُ الآن (وهذا مكتوبٌ في الاختبار نفسِه بدل أن يُدَّعى).

### تصحيحٌ لاحق — نطاقُها شخصُه وحده (ترحيل ٠٨٦ — ٢٠٢٦-٠٩-٢١)

مراجعةٌ آلية على #٣٧٣ **بعد دمجه**، فالإصلاحُ فرعٌ وترحيلٌ جديدان لا مسٌّ
لِـ٠٨٥.

**الواقعة**: التوجيهُ النافي في المقالة عدّد أمثلةً صريحة — «(هاتف، عنوان،
تواريخ، تفاصيل عائلية)». وهي تسمّي المراكزَ الخمسةَ ومدنَها بالضرورة، فسؤالٌ
عن **المركز** لا عن شخصه («ما عنوان فرع بغداد؟» · «رقم هاتف مركز كربلاء؟» ·
«وين موقع مركز ذي قار؟») يستدعيها — **وهي المقالةُ الوحيدة التي تُستدعى** في
الأخيرين (قيسَ حيّاً على `retrieveKnowledge` نفسِها). فيصير توجيهُها النافي —
المقصودُ به شخصُه — التوجيهَ الوحيدَ في السياق، فيُقرأ نفياً عن **المركز**.

**وذاك نفيٌ قد يكون كاذباً**: `branches.location` عمودٌ قائم، فالنظامُ قد
يحمل موقعَ الفرع فعلاً. وأسوأُ من «لا أعرف» أن يُقال «غيرُ متاحٍ في معرفة
النظام» عن شيءٍ لم تكن هذه المقالةُ مصدرَه أصلاً.

**ولا يُعالَج بمطاردة الاسترجاع — قيسَ ذلك ولم يُفترَض**: حذفُ «هاتف»
و«عنوان» من المتن **لا يوقف المطابقة** (هي من «مركز»/«فرع» وأسماء المدن، وهي
ممّا **يجب** أن تحمله مقالةٌ تقول مَن يملك أيَّ مركز)؛ وبوّابةُ هويّةٍ قبل
الاسترجاع كانت ستُسقط أسئلةً مشروعة («كم عدد المراكز؟» · «ما اسم مجموعة
المراكز؟» · «من يملك مركز كربلاء؟») وكلُّها تُصيبها اليوم بحقّ.

**فالعلاجُ أن تصير المطابقةُ غيرَ ضارّة**: التوجيهُ النافي صار مقصوراً على
شخصه بلا أمثلةٍ تُوهِم، وأُضيفت ملاحظةٌ صريحة تقول إن المقالةَ تعريفٌ بشخصه
وحده وليست مصدرَ سؤالٍ عن مركز، **وإن سكوتَها لا يُبنى عليه نفيٌ عن المركز**،
ويُحال السائلُ إلى إدارة فرعه.

**والمعرفةُ تُنسَخ ولا تُمحى** (٤.n): صفٌّ جديد بـ`supersedes_id` إلى القديم
والقديمُ يُطفأ — **وهو يبقى بنصّه كاملاً** يُقرأ للتدقيق. والكتلةُ **مشروطة**:
لا تعمل إلّا إن كان صفُّ ٠٨٥ **فعّالاً ولم يُنسَخ بعد** — فإن حرّره المسؤولُ
من لوحة التحكّم (فأنشأ نسخته وأطفأ الأصل) أو أطفأه عمداً **لا يُمَسّ شيء**.
وهي idempotent بذلك نفسِه.

### الترحيل
**٠٨٦ فقط** — إدراجُ صفٍّ وإطفاءُ رايةٍ على صفٍّ واحدٍ **بمعرّفه**. بلا
`CREATE` ولا `ALTER` ولا `DROP` ولا `DELETE` ولا `TRUNCATE`، ولا مسٍّ لترحيلٍ
من ٠٠١ إلى ٠٨٥.

اختبارٌ دائم: `npm run test:owner-profile` اكتسب القسم **ط** (المتنُ بلا
الأمثلةِ المُوهِمة · والنفيُ مقصورٌ على شخصه · وتقول صراحةً إنها ليست مصدرَ
سؤالٍ عن مركز ولا يُبنى على سكوتها نفيٌ عنه · وتدلّ على إدارة الفرع ·
**والقديمةُ كانت تعدّدهما فعلاً** فالإصلاحُ يقابل عطباً واقعاً · **والمطابقةُ
باقيةٌ عمداً** ويُقال ذلك صراحةً لا يُدَّعى خلافُه) وبنودَ ٠٨٦ في القسمين أ
(نسخةٌ ثانية تخلُف الأولى · والأولى مُطفأةٌ **ونصُّها باقٍ بحرفه**) وب
(**ولا يُنشئ شيئاً إن كان الأصلُ منسوخاً سلفاً** — معزولاً عن حارس «مُطفأ»)
وح (`UPDATE` واحدٌ على صفٍّ بمعرّفه، ولا هدمَ فيه).

**والبنودُ الموضوعية تُقاس على المقالة الفعّالة** لا على مفتاحٍ بعينه — فما
يصل النموذجَ هو ما يُختبَر. **والحزمةُ تُعاد على القاعدة نفسِها بلا فشل**:
لا بندَ فيها يقارن «قبل وبعد» في تشغيلٍ واحد (بندُ عدمِ الفراغ يقرأ مصدرَ
٠٨٥ بدل ذلك — وهو أصدقُ وأدومُ).

**ومُتحقَّقٌ بالعكس**: تعطيلُ ٠٨٦ يُسقط **١٠ بنودٍ بالضبط** — أ٣ وأ٤ وط١–ط٥
وح٣–ح٥ — **وتبقى أسئلةُ الهويّة الاثنا عشر وعزلُ مسارِ العمل خضراء**، فالعطبُ
يقع في موضعه ولا يُقرأ في غيره.

#### تصحيحٌ ثانٍ — القراءةُ تحت القفل (مراجعةٌ آلية على #٣٧٤)

**٠٨٦ كان يقرأ الأصلَ حرّاً ثمّ يقرّر.** والقرارُ مركَّب — «أفعّالةٌ هي؟» ثمّ
«أَلَها خَلَفٌ؟» ثمّ كتابتان — فبين السؤال والجواب نافذةٌ يقع فيها فعلُ غيرِنا.

**وسباقُ الترحيل مع نفسِه مستحيلٌ أصلاً** (نُسخته تحمل `seed_key` فريداً،
فالخاسرُ يرتدّ بـ23505). **والسباقُ الواقع فعلاً مع تحريرِ المسؤول**: نسخةُ
`editArticleTx` تُولَد بـ`seed_key = NULL` و`supersedes_id` بلا قيدِ تفرّد —
**فلا شيءَ في القاعدة يمنع خَلَفَين فعّالَين لأصلٍ واحد**، وهو بعينه ما يمنعه
القسمُ ٤.n صراحةً. وبالترتيب المعاكس أسوأ: مسؤولٌ يُطفئ المقالةَ **عمداً**
فيمرّ الترحيلُ على قراءةٍ سبقت إطفاءَه ويُنشئ خَلَفاً فعّالاً لم يُرِده أحد.

**فالمصدرُ يُقفَل قبل أن يُقرأ** (`FOR UPDATE`) — **نفسُ ما يفعله مسارُ
التحرير بحرفه**. وهو وحده يكفي: كلُّ مسارٍ مشروعٍ يُنشئ خَلَفاً يقفل الأصلَ
أوّلاً، فقفلُنا يُسلسِلنا معه ويبقى حتى `COMMIT` فيغطّي الفحصَ والكتابتين.
ومعه `lock_timeout` لأن هذا يجري عند الإقلاع: انقضاؤها يُفشل الترحيلَ فلا
يُسجَّل في `_migrations` **فيُعاد في الإقلاع التالي** — تأجيلُ التصحيح خيرٌ من
وقوعه على قراءةٍ بائتة.

**وبلا ترحيلٍ جديد** — ٠٨٦ نفسُه عُدِّل (لم يُدمَج بعد)، ولا عمودَ ولا قيدَ
ولا حرفَ في `store.ts`.

اختبارٌ دائم: القسمُ **ي** في `npm run test:owner-profile` — معاملةٌ تفعل ما
يفعله `editArticleTx` بالضبط (تقفل، تُدرج خَلَفاً فعّالاً، تُطفئ الأصل) بينما
يُشغَّل ٠٨٦ **وهي معلَّقة**: خَلَفٌ فعّالٌ واحدٌ بالضبط · وهو نسخةُ المسؤول ·
ولا صفَّ ٠٨٦ إطلاقاً · والحالةُ القانونية تعود فتُعاد الحزمةُ على القاعدة
نفسِها بلا فشل (مُثبَتٌ بتشغيلين متتاليين).

**ومُتحقَّقٌ بالعكس**: رفعُ `FOR UPDATE` وحدَه يُسقط **ي٣ وي٤ وي٥ بالضبط** ولا
شيءَ غيرها.

> **⚠ وي٢ شرطُ صحّةٍ لا دليلُ إصلاح — ويقول ذلك عن نفسِه.** أوّلُ صياغةٍ له
> ادّعت أنها تُثبت القفل، **والعكسُ كذّبها**: الترحيلُ يُحجَز في الحالتين —
> بالقفل **قبل أن يقرّر**، وبلا قفلٍ تمضي قراءتُه (فالقارئُ في MVCC لا يحجزه
> شيء) ويُدرج ثمّ يُحجَز عند `UPDATE` الأخير **بعد فوات الأوان**. فلا يفرّق
> بينهما إلّا الأثرُ. وقيمتُه أنه يمنع سيناريو فارغاً: لو انتهى الترحيلُ قبل
> أن يفعل «المسؤول» شيئاً لَما كان سباقٌ أصلاً ولمرّت ي٣–ي٥ بلا معنى.
> **وفحصٌ يمرّ لسببٍ خاطئ ليس فحصاً** — فأُعيدت تسميتُه ليقول ما يقيسه.

---

## 4.ai الجلسةُ المُهداة تُحتسب، والمالُ لا يتحرّك (ترحيل ٠٨٧ — ٢٠٢٦-٠٩-٢١)

**قاعدةُ المالك**: مَن يُدخل الجلسات — استقبالٌ أو مديرٌ أو مديرُ فرع —
**بلا تأشير «مجاني»** يُحتسب المالُ وتُسجَّل الجلسات؛ **وبتأشير «مجاني»**
تُسجَّل الجلسات ولا يُحتسب المال. فرعُ
`claude/patient-event-communication-plan-y0x9e6`. **ترحيل ٠٨٧** (استوجبته مراجعةُ Codex الثانية — إثباتُ منشأ الهديّة).

### الواقعة (المريضة «انتصار حبيب محمد»، WB-01688)
مُنحت ستَّ جلسات «أجهزة علاج طبيعي» مجّانية، **ولم تنزل في العدّاد إطلاقاً**.
والتشخيصُ الحيّ كشف سببين متراكبين، كلاهما مُثبَتٌ على النقاط الحقيقية:

**① الرقمُ الذي كان يعرضه العدّاد (٢٦) لم يكن مسجَّلاً في أيّ مكان.**
ملفُّها أُنشئ ٢٠٢٦-٠٦-٣٠، **قبل ترحيل ٠٣٦** الذي أضاف `physio_plan`. وقتها
كانت «الكلفة والجلسات» **تحسب العددَ وتنساه**: الطبيبُ وصف ٢٦ جلسة روبوت على
الورق، فأدخلتها ريام، فحسب النظامُ ٢٦ × ٥٠,٠٠٠ = ١,٣٠٠,٠٠٠ وكتب **المال واسمَ
النوع فقط** — ونصُّها بقي «روبوت» عارياً بلا عدد، وخطتُها فارغة. فصار العدّادُ
**يعيد بناء العدد بالقسمة** (`caseCost ÷ price`) عند كلّ عرض.

**② والاشتقاقُ يَغلب سجلَّ الدفعات كلَّه.** الحارسُ `derived > paymentsTotal`
وُضع ليحمي مريضَ المفرد من كلفةٍ **أصغر**؛ وهنا ٢٦ > ٨ فيفوز الاشتقاقُ ويُسقط
الدفعاتِ — **والهديّةُ الستّ تختفي**. فالعدّادُ عندها رقمٌ ثابت لا يتحرّك
مهما مُنحت جلسات.

**وتُثبَت هويّةُ الـ ١,٣٠٠,٠٠٠ بالدليل لا بالترجيح**: دفعتاها (٥٠,٠٠٠ × ٢)
ملاحظتُهما «روبوت (1 جلسة)» بلا بادئةِ خدمة — وهي صيغةُ **نافذة الدفعات**
حصراً، وتلك النقطةُ **لا تكتب `total_cost` إطلاقاً** (تقرؤه فقط لتمنع دفعةً
تفوق المتبقّي). فالمبلغُ كلُّه من فعلٍ واحد سبقهما، و١,٣٠٠,٠٠٠ = ٢٦ × ٥٠,٠٠٠
بالضبط.

### ومصفوفةُ الأبواب الأربعة — مقيسةً حيّاً لا موصوفة
| الباب | صاحبُ خطةٍ محفوظة | مريضُ المفرد (بلا خطة) |
|---|---|---|
| «خدمة جديدة» — مدفوعة | كلفة+ · مقبوض+ · **جلسات ✅** | كلفة+ · مقبوض+ · **جلسات ✅** |
| «خدمة جديدة» — مجانية | بلا مال ✅ · **جلسات ✅** | بلا مال ✅ · **جلسات ✅** |
| نافذة الدفعات — مدفوعة | مقبوض+ · جلسات كما هي *(تحصيلٌ لا بيع)* | تُردّ ٤٠٠ وتدلّ على «خدمة جديدة» |
| **نافذة الدفعات — مجانية** | **❌ كانت تختفي** ⟵ صارت ✅ | ✅ (عبر الدفعات) |

**فالقاعدةُ كانت مطبَّقةً في «خدمة جديدة» بالكامل**، والخللُ في **نافذة
الدفعات وحدها**.

### الإصلاح — نصفان، ولا دينارَ يتحرّك في أيٍّ منهما

**① نافذة الدفعات ترفع الخطةَ كما ترفعها «خدمة جديدة» بالضبط.**
`POST /api/payments` عند `isFreeSessions` يُدخل الجلساتِ في `physio_plan`
بالدالّة القانونية نفسِها (`mergePhysioPlan`) — **ولا يُنشئ خطةً لمن لا خطةَ
له**: مريضُ المفرد تاريخُه كلُّه على دفعاته، وخطةٌ من جلسةٍ واحدة تطمسه وتقلب
عدّادَه سالباً (**حادثةُ ذي قار ٢٠٢٦-٠٧-٢٩**)، وعدّادُه يقرأ الدفعاتِ أصلاً
فيرى الهديّةَ بلا خطة. **وأنواعُ العلاج الطبيعي وحدها** تدخل الخطة — دفعةُ
طرفٍ أو مسندٍ ليست جلسة. **ولا كلفةَ ولا قيدَ دفتر.**

**ولماذا المجّانيُّ وحده**: الدفعةُ المدفوعة في هذه النافذة **تحصيلٌ لا بيع**
— يحرسه ردُّها ٤٠٠ «لا يمكن تسجيل دفعة جديدة لعدم وجود مبلغ متبقي» — فإضافةُ
جلساتها للخطة تعدُّ الشراءَ مرّتين. أمّا المجّانيُّ فليس تحصيلاً: قيمتُه صفر،
وهو **منحٌ خالص** يجب أن يبلغ العدّاد.

**② والمُهدى يُضاف فوق الاشتقاق من الكلفة.** `resolvePurchasedSessions` صارت
تفرّق المدفوعَ من المُهدى (`isFree`)، فتقيس الحارسَ بـ**المدفوع وحده** وتضيف
المُهدى **فوق** الناتج. فملفٌّ قديمٌ اشتُقّ عدّادُه من المال يظلّ يرى هديّتَه
(٢٦ + ٦ = ٣٢). **والخطةُ المحفوظة لا تُمَسّ**: هي تُكتب من البابين معاً
(«خدمة جديدة» تكتب في الخطة والدفعة)، فجمعُها مع الدفعات يعدّ الهديّةَ مرّتين.
و`summarizePaymentSessions` صارت تجمع بالمفتاح (النوع، أمجّانيّةٌ هي) بدل
النوع وحده — **ولا مبلغَ ولا تاريخَ ولا معرّفَ دفعةٍ يُضاف**، فالحدُّ الماليُّ
الذي وُضع له ذلك الملخّص (٢٠٢٦-٠٩-٠٢) باقٍ بحرفه.

**وحين لا هديّةَ إطلاقاً، الشيفرةُ مطابقةٌ بايتاً لما كانت**: `freeTotal = 0`
و`paidTotal = paymentsTotal`.

### وما لم يُمَسّ
سعرُ الجلسة وجدولُ الأسعار · «الكلفة والجلسات» · «خدمة جديدة» بشقّيها ·
`PUT /api/patients/:id/physio-plan` (تصحيحُ الأعداد بلا مال) · حارسُ «لا دفعة
فوق المتبقّي» · دفترُ الكلف · التصنيعُ والمعايناتُ والأجهزة · العلاجُ
الطبيعي فيما عدا ما ذُكر.

### الترحيل
**أوّلُ تنفيذٍ كان بلا ترحيل**، ثمّ استوجبت مراجعةُ Codex الثانية عمودَ
إثباتِ منشأ (**ترحيل ٠٨٧**) — التفصيل في قسمها أدناه.

اختبارٌ دائم: `npm run test:physio-free` (٢٨ تأكيداً حيّاً على Postgres وعلى
النقاط الحقيقية — الأبوابُ الأربعة بشقَّي المال والجلسات، وشكلُ «انتصار»
بالاشتقاق ثمّ بعد تصحيح الكلفة، وحمايةُ ذي قار، ومنعُ الازدواج، ودفعةُ الجهاز
لا تدخل الخطة). **ومُتحقَّقٌ بالعكس، كلُّ نصفٍ على حدة**: تعطيلُ رفع الخطة
يُسقط **أربعةَ بنودٍ بالضبط** (أ٣ · أ٦ · و١ · و٢)، وتعطيلُ حمل المُهدى فوق
الاشتقاق يُسقط **هـ٢ وحدَه**.

### تصحيحُ مراجعة Codex — والفرعُ الذي لم تكن الشاشةُ تبلغه (٢٠٢٦-٠٩-٢١)

مراجعةٌ آلية على #٣٧٥ كشفت ثلاثَ ثغرات، **أُعيد إنتاجُ الأولى حيّاً قبل أيّ
تعديل** وكانت تُبطل الإصلاح كلَّه.

**① العلمُ لكلّ بند، لا علماً علوياً** (P1 — العطبُ الحاكم). `PaymentModal`
يبني البندَ المُهدى بـ`isFree` **عليه هو** ويرسل `treatmentEntries` **بلا
`isFreeSessions` علويّ إطلاقاً**. وحلقةُ الإدراج كانت `if (entry.cost > 0 ||
isFreeSessions)` — فبندٌ بكلفةِ صفرٍ وعلمٍ علويٍّ مُطفأ **يُتخطّى، ولا
يُسجَّل شيءٌ البتّة، والردُّ ٢٠١**. مُثبَتٌ بإعادة إنتاجٍ حيّة: **صفرُ صفوف**.

فصار `entryIsFree(e) = mayGrantFree && (e.isFree === true || isFreeSessions)`
— **ونفسُ بوّابة الصلاحية للاثنين**، فمَن لا يملك المنحَ المجّانيّ لا يصنعه
من بابٍ خلفيّ. والعلمُ العلويُّ يبقى مقبولاً (عميلٌ سابق) فيُعمَّم على البنود.
والبنودُ المختلطة تعمل: مدفوعٌ بمبلغه ومُهدى بصفرٍ في إرسالٍ واحد.

**وحارسُ «لا متبقّي» يُتخطّى حين لا شيءَ يُقبض**: حمولةٌ كلُّها هدايا لا
تقبض ديناراً، وكان يردّها ٤٠٠ لصاحب ملفٍّ سُدِّد بالكامل فيتعذّر منحُه أصلاً.

**② والقفلُ شرطُ صحّةٍ لا تحسين** (P2). كتابةُ الخطة قراءةٌ‑تعديلٌ‑كتابةٌ على
عمود `jsonb`. فمنحان متزامنان كانا يقرآن الخطةَ عينها ويكتب الأخيرُ فوق
الأوّل — **فتضيع هديّةٌ صحيحة**. صار `SELECT … FOR UPDATE` على صفّ المريض
داخل المعاملة.

**③ وتصحيحُ الهديّة يُصحِّح الخطةَ معها** (P2). تصحيحُ ستٍّ إلى ثلاث — أو
حذفُ الهديّة — كان يُبقي الستَّ محسوبةً **إلى الأبد**. فصارت الكتابةُ كلُّها
تمرّ بدالّةٍ واحدة `storage.adjustPhysioPlanForGift(patientId, deltas, tx?)`
**بالدلتا لا بالكتابة الكاملة**، يناديها: الإنشاءُ · و`applyCorrectionWriteTx`
(الكاتبُ الواحد للحذف وللتعديل المحميّ، مباشراً كان أو باعتمادِ طلب) ·
و`storage.updatePayment`/`updatePaymentSessionInfo`. وتشمل الأشكال الثلاثة:
تغيُّرَ العدد، وقلبَ العلم في الاتجاهين، وإعادةَ وسم النوع (نقصٌ من القديم
وزيادةٌ للجديد).

**والسباقُ في الاختبار حتميٌّ لا احتمال**: `Promise.all` وحدها مرّت **حتى
بلا قفل** — فحصٌ يمرّ لسببٍ خاطئ ليس فحصاً. فبوّابةٌ على العميل المُستعار من
المجمَّع (**في ملفّ الاختبار وحده**، وتُستعاد في `finally`) تحجز أوّلَ كتابةٍ
للخطة داخل معاملتها، ويمضي الثاني بكامله، ثمّ تُحرَّر.

اختبارٌ دائم: `npm run test:physio-free` توسّع إلى الأقسام **ز** (بنودٌ
مختلطة) و**ح** (السباقُ الحتميّ) و**ط** (التصحيحُ والحذف) و**ي** (الاستقبالُ
لا يمنح)، والقسمُ **أ** صار بحمولة النافذة الحقيقية. **ومُتحقَّقٌ بالعكس،
كلُّ إصلاحٍ على حدة**: إسقاطُ علم البند يُسقط سبعةَ بنود · ورفعُ القفل يُسقط
**ح٣ وحدَه** · وإسقاطُ المصالحة يُسقط **ط٣ وط٥ وحدهما**.

### تصحيحُ مراجعة Codex الثانية — إثباتُ المنشأ والذرّية (ترحيل ٠٨٧)

أربعُ ثغراتٍ أخرى على `77f7cfb`، **أُعيد إنتاجُ الأمنيّة منها حيّاً**.

**① ولا علمَ مجّانيّةٍ من العميل يُخزَّن** (P1 — التفافٌ على الصلاحية).
`input` مبنيٌّ من جسم الطلب، والمسارُ المفرد (بلا `treatmentEntries`) كان
يكتب `{ ...input }` كما هو. فموظّفُ استقبالٍ يملك `canAddPayments` يرسل
`isFreeSessions: true` بعددِ جلسات ⟶ **تُخزَّن الهديّةُ وترتفع خطتُه**.
مُثبَتٌ حيّاً قبل الإصلاح: **١٠ ⟶ ١٥** من حساب استقبال. فالقيمةُ المُصرَّح
بها تُكتب فوقها **قبل أيّ إدراج**، فلا يبقى للعميل أثرٌ في العمود.

**② والطرحُ لا يقع إلّا على ما قُيِّد فعلاً** (P1 — ترحيل ٠٨٧). الهدايا
**السابقة لهذا النشر لم تدخل الخطةَ قطّ** (وذاك العطبُ نفسُه)، ولا تعبئةَ
رجعية لها. فطرحٌ أعمى عند الحذف **يُفسد خطةً مشروعة**: عشرُ جلسات روبوت
وهديّةٌ قديمةٌ بستٍّ ⟶ حذفُها يُنزل الخطةَ إلى **أربع**.

فعمودٌ واحد `payments.plan_credited` يقول الحقيقةَ عن الصفّ نفسِه بثلاثِ
حالاتٍ صادقة: `NULL` **لم يُسأل** (سابقٌ للترحيل، لا يُطرَح منه شيء) ·
`FALSE` مُنح ولم يُقيَّد (مريضُ المفرد بلا خطة، وعدّادُه يقرأ الدفعات) ·
`TRUE` قُيِّد فعلاً ⟶ وحدَه يُطرَح. **بلا `DEFAULT` وبلا تعبئةٍ رجعية.**

**③ والصفُّ ورصيدُه في الخطة يقعان معاً أو لا يقع شيء** (P2). كان الإدراجُ
يُلتزَم ثمّ تُفتَح معاملةٌ ثانية — ففشلٌ بينهما يترك هديّةً مخزَّنةً وعدّادَ
صاحب الخطة ساكناً، وإعادةُ المحاولة تُنتج هديّةً ثانية. فصارا في معاملةٍ
واحدة (`createPaymentAttributed` اكتسبت معاملَ معاملةٍ اختيارياً)، والوسمُ
يُكتب فيها فلا يُقيَّد الصفُّ مرّتين.

**④ وقفلُ صفّ الدفعة قبل اشتقاق الدلتا** (P2). `updatePayment` و
`updatePaymentSessionInfo` كانتا تقرآن `before` بلا قفل — فتعديلان
متزامنان (٦⟶٣ و٦⟶٤) يطرحان ٥ بينما الصفُّ يقول ٤. صارتا معاملةً واحدة
بـ`FOR UPDATE`. **ومسارُ التصحيح المحميّ كان مقفولاً أصلاً** فلم يُمَسّ.

**وفشلٌ غيرُ متوقَّع يُقال، ولا يبقى الطلبُ معلَّقاً** (كشفه حقنُ العطل في
اختبار الذرّية): معالِجُ `POST /api/payments` غيرُ متزامن وExpress 4 لا
يلتقط رفضَ الوعود — فخطأٌ عابر كان يخرج رفضاً غيرَ ملتقَط **فلا يصل الطلبَ
ردٌّ إطلاقاً**. صار يُردّ ٥٠٠ برسالةٍ عربية تقول إن شيئاً لم يُحفَظ.
(نفسُ درس القسم ٤.ag.)

اختبارٌ دائم: `test:physio-free` توسّع إلى **ك** (الالتفاف) و**ل** (الهديّةُ
القديمة) و**م** (وسمُ المنشأ) و**ن** (تعديلان متزامنان) و**س** (الذرّيةُ
بحقن عطلٍ حقيقيّ في كتابة الخطة). **ومُتحقَّقٌ بالعكس، كلٌّ على حدة**:
إسقاطُ تثبيت العلم يُسقط **ك١ وك٢** · وإسقاطُ إثبات المنشأ يُسقط **ل٢ ول٤** ·
ونقلُ القيد إلى معاملةٍ ثانية يُسقط **س٣** · ورفعُ قفل الدفعة يُسقط **ن٢** —
لا شيءَ غيرها في الأربع.

### الترحيل
**٠٨٧** — إضافيّ، idempotent (شُغّل مرّتين): عمودٌ واحد **بلا `DEFAULT`**
وفهرسٌ جزئيّ. بلا `DROP` ولا `DELETE` ولا `UPDATE` على أيّ صفّ قائم، ولا
مسٍّ لترحيلٍ من ٠٠١ إلى ٠٨٦.

### تصحيحُ مراجعة Codex الثالثة — حارسُ المال، وتسلسلُ كتّاب الخطة

**١) حارسُ «لا متبقّي» كان يسقط ببنودٍ صفرية — ثغرةٌ أدخلتُها أنا.**
إصلاحُ «المجّانيُّ بالبنود يتخطّى الحارس» (أعلاه) حسب `payableTotal` من
**كلف البنود وحدها**. وحمولةُ الطرف/المسند اليدوية تصل ببنودٍ كلفتُها صفرٌ
ومبلغٍ في الأعلى — وهي الحالةُ التي تخدمها «شبكةُ الأمان» بإنشاء دفعةٍ واحدة
به. فكان المجموعُ يخرج صفراً، **فيُتخطّى الحارس**، ثمّ تُدرَج الدفعةُ الموجبة
وقيدُها على ملفٍّ سُدِّد بالكامل.

**والقاعدةُ الآن**: يُتخطّى الحارسُ حين **لا يُقبَض دينارٌ أصلاً** — أي حين
تكون البنودُ **كلُّها مُهداةً** — وإلّا فالمقياسُ `max(مجموعُ كلف البنود غير
المُهداة، المبلغُ العلويّ)`. **والهديّةُ الصريحة لم تُكسَر**: تمضي على الملفّ
المُسدَّد نفسِه كما جاء الاستثناءُ لأجله.

**٢) وكلُّ كاتبٍ للخطة يقفل صفَّ المريض أوّلاً.** `adjustPhysioPlanForGift`
تقفل (`FOR UPDATE`)، وكاتبا الخطة الآخران كانا يقرآنها **بلا قفل** ثمّ يكتبان
فوقها بعد أسطر — فهديّةٌ تُقيَّد في تلك الفجوة تضيع: يُكتب «القديمُ + الجديد»
**وصفُّ الهديّة باقٍ يقول إنها قُيِّدت**، والعدّادُ يقرأ الخطةَ وحدها فلا
يراها. فصار القفلُ في الاثنين:
- **`storage.pricePhysiotherapy`** — `.for("update")` على قراءتها الأولى.
- **`server/new_service/store.ts: executeNewService`** — قفلٌ إرشاديّ (٩١٩)
  ثمّ صفُّ المريض، **بهذا الترتيب بعينه**: هي تنادي `ensurePhysiotherapyCase`
  بعد أسطر، وتلك تأخذ الإرشاديَّ أوّلاً ثمّ صفَّ المريض — فلو عُكس الترتيبُ
  هنا لصار الجمودُ ممكناً بينهما. والترتيبُ العامّ صار واحداً في الثلاثة:
  **إرشاديّ ⟵ صفُّ المريض ⟵ صفُّ الحالة**.

**٣) ولا قفلَ ثالثٌ في `creditGiftToPlanTx` — أُضيف ثمّ أُزيل.** سؤالُها «أله
خطة؟» قرارٌ، وقراءتُه على حالةٍ بائتة كانت ستَسِم الصفَّ `planCredited = false`
بينما تسعيرٌ يُنشئ الخطةَ في اللحظة عينها. **لكنّ المراجعةَ العكسية أثبتت أن
إزالة القفل لا تُسقط تأكيداً واحداً** — وهذا ليس ثغرةً في الاختبار: الدالّةُ
تُنادى **دائماً بعد إدراج صفّ الدفعة في المعاملة نفسِها** (ناديها واحد)،
والإدراجُ يأخذ `FOR KEY SHARE` على صفّ المريض بمفتاحه الأجنبيّ، وهو يتعارض
مع `FOR UPDATE` الذي يأخذه الكاتبان — فالتسلسلُ واقعٌ قبل أن تصل. **ولا
يُشحَن قفلٌ لا يُثبته اختبار**، والسببُ مكتوبٌ في موضعه كي لا يُعاد.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (٧٨ تأكيداً بعد الإضافة):
**ع** حارسُ المتبقّي — بنودٌ صفريةٌ غيرُ مُهداة + مبلغٌ موجب ⟶ ٤٠٠ وصفرُ
صفوفٍ أُدرجت، **والمجّانيُّ الصريح يمضي على الملفّ نفسِه** · **ف** سباقُ
التسعير مع الهديّة ⟶ ١٠ + ٦ + ٥ = ٢١ · **ص** و«خدمة جديدة» كذلك · **ق**
مريضٌ **بلا خطة**: التسعيرُ يلدها والهديّةُ تدخلها ⟶ ٥ + ٤ = ٩ والصفُّ موسومٌ
مقيَّداً. **والسباقاتُ الثلاثة حتميّةٌ لا احتمال** — بالبوّابة نفسِها من
القسم ح على عميل القاعدة (في ملفّ الاختبار وحده، تُستعاد في `finally`).

**ومُتحقَّقٌ بالعكس، كلُّ إصلاحٍ على حدة**: ردُّ `payableTotal` إلى كلف البنود
وحدها يُسقط **٥ تأكيداتٍ كلَّها من القسم ع** · ورفعُ قفل `pricePhysiotherapy`
يُسقط **ف٣ وق٣ وق٤** · ورفعُ قفلَي «خدمة جديدة» يُسقط **ص٣ وحدَه** — ولا شيءَ
خارج الأقسام الجديدة في الثلاث.

### تصحيحُ مراجعة Codex الرابعة — إثباتُ منشأ «خدمة جديدة»، وذرّيةُ البنود

ثغرتان أخريان على الفرع نفسِه، **وبلا ترحيلٍ جديد** ولا عمودٍ ولا قيدٍ ولا
حرفٍ في القاعدة.

**١) وهديّةُ «خدمة جديدة» لم تكن تقول أقُيِّدت أم لا** (P2). عمودُ
`plan_credited` (٠٨٧) يكتبه `creditGiftToPlanTx` وحده — **و«خدمة جديدة» لا
تمرّ به إطلاقاً**: هي تكتب صفَّ الدفعة بنفسها وترفع الخطةَ بـ`planPatch` في
معاملتها. فكانت هديّتُها تُخزَّن بـ`NULL` («لم يُسأل»، أي **سابقةٌ للترحيل**)
بينما هي مُقيَّدةٌ في الخطة فعلاً — فحذفُها أو تصحيحُها **لا يطرح منها شيء**،
والجلساتُ تبقى خالدةً في العدّاد. مُثبَتٌ حيّاً: خطةٌ ١٠ + هديّةُ ٦ ⟶ ١٦، ثمّ
حذفُ الهديّة ⟶ **١٦** لا ١٠.

**والوسمُ يُكتب صريحاً في الإدراج نفسِه**، بقاعدةٍ تقول الحقيقةَ عن **ذلك
الصفّ بعينه**: `planMerged` (أَرُفعت الخطةُ في هذا النداء أصلاً؟ — للمريض
خطةٌ **و**وصلت بنودٌ) **و**`entersPhysioPlan(type, n)` **و**أن يكون النوعُ
من `PHYSIO_TREATMENT_TYPES`. والفرعُ المفرد (بلا `entries`) يكتب `false`
صراحةً — هديّةٌ مُنحت ولم تُقيَّد، **وهو ما يقوله `false` بصدق** خلافاً
لـ`NULL` التي تعني «سابقٌ للترحيل». **والمدفوعةُ تبقى `NULL`** كما في
`creditGiftToPlanTx` بالضبط.

**ولا قاعدةَ ثانية تنحرف**: شرطُ دخول الخطة كان مكتوباً داخل حلقة
`mergePhysioPlan`، فاستُخرج إلى `entersPhysioPlan` في `shared/pricing.ts`
ويقرؤه الاثنان — **والشرطُ حرفُه حرفُه**، فلا تغيُّرَ في سلوك الدمج.

**٢) وبنودُ الطلب الواحد تُلتزَم معاً أو لا يقع منها شيء** (P2). كلُّ بندٍ
كان معاملةً مستقلّة، فبندٌ يلتزم وآخرُ يفشل يترك **دفعةً محفوظة ورصيدَها في
الخطة** بينما يقول الردُّ «تعذّر… لم يُحفَظ شيء» — فيُعيد الموظّفُ الإرسالَ
**فتتضاعف الدفعةُ والهديّةُ معاً**. فصار الإدراجُ كلُّه (ومعه شبكةُ الأمان)
معاملةً واحدة.

**واليوميةُ والتدقيقُ يبقيان بعدها خارجها كما كانا** — لا تتغيّر معامَليّتُهما
في هذه التمريرة. **والردُّ لا يقول «لم يُحفَظ شيء» بعد التزام**: عدّادٌ
(`committedPayments`) يفرّق الفشلَ **قبل** الالتزام (صفرُ كتابةٍ فعلاً، فيُقال
«أعد المحاولة») من الفشل **بعده** (الدفعةُ محفوظة، فيُقال «لا تُعِد التسجيل —
أبلغ المسؤول»). ولولاه لَدلّت الرسالةُ الموظّفَ على تكرارٍ يضاعف المال.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (٩٦ تأكيداً بعد الإضافة،
مقابل ٧٨ قبلها): **ر** هديّةُ «خدمة جديدة» موسومةٌ مقيَّدة · وحذفُها يُعيد
الخطةَ ١٦ ⟶ ١٠ · **ومريضُ المفرد يُوسَم `false` ولا خطةَ تُولَد له** ·
**ش** الذرّية — حقنُ عطلٍ حقيقيّ على **إدراج البند الثاني** ⟶ **صفرُ صفوفٍ
بقيت** (لا البندُ الأوّل) والرسالةُ تقول «لم يُحفَظ شيء» والخطةُ كما كانت،
ثمّ الطلبُ نفسُه بلا حقنٍ ⟶ صفّان معاً والخطةُ ١٠ + ٤ + ٢ = ١٦.

**ومُتحقَّقٌ بالعكس، كلُّ إصلاحٍ على حدة**: إسقاطُ وسمَي «خدمة جديدة» يُسقط
**٣ تأكيداتٍ بالضبط** (ر٣ · ر٥ · ر٨) · وردُّ البنود إلى معاملاتٍ مستقلّة
يُسقط **٥ بالضبط** (ش٣ · ش٤ · ش٥ · ش٧ · ش٨) — ولا شيءَ خارج قسمِه في
الاثنين.

### تصحيحُ مراجعة Codex الخامسة — معنى الوسم، وترتيبُ القفل

ثلاثُ ثغراتٍ بلّغتها المراجعة، **ورابعةٌ كشفها اختبارُ الثالثة وهي تُكتب**.
**وبلا ترحيلٍ جديد** ولا عمودٍ ولا قيدٍ ولا حرفٍ في القاعدة.

**١) وهديّتان متزامنتان كانتا تتجمّدان** (P2). إدراجُ صفّ الدفعة يأخذ
`FOR KEY SHARE` على صفّ المريض بحكم مفتاحه الأجنبيّ — **وهو متوافقٌ مع
نفسِه**. فهديّتان تُدرِجان معاً، ثمّ تطلب كلٌّ ترقيةَ قفلِها إلى `FOR UPDATE`
في `creditGiftToPlanTx` فتنتظر الأخرى ⟶ **جمودٌ حقيقيّ** تقتل فيه Postgres
إحداهما فيُردّ «لم يُحفَظ شيء» على منحٍ مشروع. **وهذا يُكذّب حجّةَ المراجعة
الثالثة** التي رفعت القفل بدعوى أن المفتاح الأجنبيّ يُسلسل — لا يُسلسل:
`KEY SHARE` متوافقةٌ مع نفسِها.

**فالتصعيدُ صار قبل الإدراج** (`lockPatientForGiftTx` في المخزن، يناديها
`writePaymentTx`): الأولى تمسك `FOR UPDATE` فتنتظرها الثانية قبل أن تُدرج
شيئاً. **ولا يُقفَل إلّا ما سيُقيَّد** — شرطُ `creditGiftToPlanTx` بحرفه،
فدفعةٌ عادية لا تُسلسَل بلا سبب. وقراءةُ «أله خطة؟» عادت تحت القفل: **هو
القفلُ نفسُه**، فإعادةُ أخذه في المعاملة عينها لا تفعل شيئاً.

**والقسمُ «ح» لا يمسك هذا**: بوّابتُه تُمهل الأوّلَ حتى يملك `FOR UPDATE`
قبل أن يبدأ الثاني أصلاً. **فالبوّابةُ الجديدة ذاتُ وجهين** فتكون حتميّةً في
الشكلين: تُمسك **بعد** `insert into payments` (شكلُ ما قبل الإصلاح) و**قبل**
`… patients … for update` (شكلُ الإصلاح). ولولا الوجهان لَمرّ الفحصُ في أحد
الشكلين لسببٍ خاطئ.

**٢) وصفٌّ مدفوع يُصحَّح إلى «مجاني» لم يكن يدخل الخطة** (P2). وسمُه `null`
(«لم يُسأل») يصف **ماضيه لا حاضره**، فكان `reconcileGiftPlanTx` يقرؤه «غيرَ
مقيَّد» فينصرف: الصفُّ يصير هديّةً بجلسات **والعدّادُ لا يتحرّك**. والمدفوعُ
لا تاريخَ له في الخطة بحكم التعريف، فيُعامَل معاملةَ الهديّة الجديدة
بالكاتب القانونيّ نفسِه.

**٣) والتسعيرُ يَسِم ما استورده** (P2). بذرةُ `pricePhysiotherapy` تسحب
**كلَّ** دفعةٍ حاملةٍ لجلسات حين لا خطةَ للمريض — ومنها هديّةُ مريض المفرد
الموسومةُ `false` بصدقٍ يومَها. فتصير في الخطة والوسمُ ما زال `false`،
فحذفُها لا يطرح شيئاً **وجلساتُها تبقى في العدّاد إلى الأبد**. فصار الوسمُ
يُكتب في معاملة التسعير نفسِها، **وعلى أنواع العلاج الطبيعي وحدها**: نوعٌ
آخر تضعه البذرةُ في دلو «غير محدد» فلا يُطابقه الطرحُ لاحقاً، والصمتُ أصدقُ
من وسمٍ لا يُوصِل إلى مفتاحٍ في الخطة.

**٤) والمدفوعُ يُوسَم كالمُهدى، ومعنى الوسم واحد** (كشفه اختبارُ ٣ وهو
يُكتب). وسمُ المُهدى وحده كان يترك صفّاً **مدفوعاً** مستورَداً يُقرأ «ليس في
الخطة» — فتصحيحُه إلى «مجاني» يُضيف جلساته **مرّةً ثانية وهي فيها أصلاً**
(١٢ ⟶ ١٤). فالبذرةُ لا تفرّق بينهما، وكلاهما صار في الخطة حقّاً.

**ومعه صار `planCredited === true` يعني «جلساتُ هذا الصفّ في الخطة الآن»** —
مُهدىً كان أم مدفوعاً — فقياسُ مساهمته بـ«أهو هديّة؟» (`giftOf`) كان يقرأ
المدفوعَ المستورَد صفراً ويقرأ حالتَه بعد التصحيح جلستين فيضيفهما.
فصار `inPlanOf` يقيسها من **عدد الجلسات وحده** متى كان الصفُّ موسوماً.
وأثرُه أن حذفَ صفٍّ مستورَدٍ يُنقص الخطةَ بمقداره — وهو الصواب: الدفعةُ لم
تقع، فجلساتُها لا تُعَدّ.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (١٢٢ تأكيداً بعد
الإضافة، مقابل ٩٦ قبلها): **ت** مدفوعٌ يُصحَّح إلى «مجاني» ⟶ ١٠ ⟶ ١٢ وموسومٌ
مقيَّداً، **والحذفُ يُنقص بالمقدار عينه** فالوسمُ ليس زينة · **ث** هديّتان
متزامنتان ⟶ الطلبان ينجحان ولا جمود، ١٠ + ٣ + ٢ = ١٥، وصفّان موسومان ·
**خ** هديّةُ مريض المفرد `false` بصدق، ثمّ التسعيرُ يبذر منها ٤ + ١٠ = ١٤
**ويَسِمها مقيَّدة**، فحذفُها يُنقص أربعاً · **ذ** الوجهُ المقابل: مدفوعٌ
استوردته البذرةُ ⟶ التصحيحُ إلى «مجاني» **يُبقي الخطةَ ١٢** ولا يعدّ جلستين
مرّتين.

**ومُتحقَّقٌ بالعكس، كلُّ إصلاحٍ على حدة**: إسقاطُ التصعيد قبل الإدراج يُسقط
**٣ بالضبط** (ث٢ · ث٣ · ث٤) **والردُّ ٥٠٠ «لم يُحفَظ شيء» يظهر حيّاً** —
الجمودُ بعينه · وإسقاطُ معاملة المدفوع-الصائر-هديّة يُسقط **٢** (ت٥ · ت٦) ·
وإسقاطُ وسمِ التسعير يُسقط **٤** (خ٥ · خ٧ · ذ٣ · ذ٥) · وردُّ قياس المساهمة
إلى `giftOf` يُسقط **١** (ذ٥) — ولا شيءَ خارج قسمِه في الأربع.

### تصحيحُ مراجعة Codex السادسة — الاستشارةُ لا تدخل الخطة، والتصنيفُ الماليُّ
لا يخرج من مسارٍ يحجب المال

ثغرتان، **كلتاهما من كتابتي في هذه التمريرة**، وبلا ترحيلٍ ولا عمودٍ ولا حرفٍ
في القاعدة.

**١) «استشارة طبية» كانت تُقيَّد في الخطة لو أُهديت بجلسات** (P2).
`PHYSIO_TREATMENT_TYPES` تضمّ الاستشارةَ (بسعر صفر)، بينما `mergePhysioPlan`
تُسقطها دائماً — زيارةٌ واحدة لا دورةُ علاج. وحارسُ `creditGiftToPlanTx`
كان يقيس **العضويةَ في تلك القائمة وحدها**، فهديّةُ استشارةٍ بثلاث جلسات
تمرّ: `adjustPhysioPlanForGift` — بالقياس نفسِه — **تُدخلها الخطةَ فعلاً**،
والصفُّ يُوسَم «قُيِّد». مُعادٌ إنتاجُه حيّاً: خطةُ عشرٍ صارت ثلاثَ عشرة.

**والشرطُ صار في موضعٍ واحد**: `shared/pricing.ts: physioSessionsEnterPlan`
— النوعُ من أنواع العلاج الطبيعي المعروفة **و**يصلح سطراً في الخطة
(`entersPhysioPlan` نفسُها) **و**العددُ موجب. ومعه `physioPlanEligibleType`
(الشقُّ النوعيُّ وحده، **مُعرَّفٌ بالدالّة عينها**) لمن يعدّل بالدلتا —
فالطرحُ سالبٌ ولا يُقاس بـ`n > 0`. ويقرؤهما أربعةُ مواضع: الحارسان في
`creditGiftToPlanTx` و`lockPatientForGiftTx` · ومرشِّحُ
`adjustPhysioPlanForGift` · و`giftCreditedToPlan` في «خدمة جديدة» (كان
التركيبُ مكتوباً فيها بشقَّيه، **فانحرف عنه الحارسان**). وبذرةُ
`pricePhysiotherapy` تَسِم `PHYSIO_PLAN_TYPES` لا القائمةَ العريضة —
فلا تُوسَم استشارةٌ «قُيِّدت» وهي ليست في الخطة أصلاً.

**٢) الملخّصُ غيرُ الماليّ كان يكشف «كم أُهدي مقابل كم دُفع»** (P1).
`summarizePaymentSessions` هو ما يصل مَن لا يملك `canViewPayments` بدل صفوف
الدفعات (إصلاحُ ٢٠٢٦-٠٩-٠٣). وأضافت تمريرةٌ سابقة `isFree` إلى كلّ سطر
وفصلت المدفوعَ عن المُهدى — كي تصل الرايةُ إلى `resolvePurchasedSessions`
في العميل. **وذاك تسريب**: التبرّعُ قرارٌ ماليّ، وهذا المسارُ بعينه يحجب
المال. **وكسَر معه عقداً مكتوباً في المستودع**: `permission_authority.test.ts`
بندُ م٦ يشترط أن يحمل كلُّ سطرٍ **حقلين لا غير** — وكان يفشل فعلاً.

> **⚠ وهذا خطأٌ في منهجي لا في الاختبار**: قيَّدتُ فشلَ
> `test:permission-authority` في التمريرات السابقة «مسبَقاً غيرَ متعلّق»
> استناداً إلى مقارنةٍ بـ`git stash` — **وتلك تقارن بـ`HEAD` الذي يحمل
> تغييراتي المُلتزَمة سلفاً، لا بـ`main`**. فأُعيدت المقارنةُ بشجرة عملٍ
> حقيقية على `main` (`git worktree`)، والسطورُ متطابقةٌ حرفاً للستّ الباقية
> — أمّا م٦ فكان منّي، وقد زال.

**فالشكلُ عاد حقلين** (`treatmentType`, `sessionCount`) مُجمَّعَين بالنوع
كما كان على `main` بحرفه، **والعدّادُ الصحيح يُحسَب في الخادم**:
`physioSessionsResolved` — نتيجةُ `resolvePurchasedSessions` نفسِها
(`test:physio-sessions`) بالمدخلات التي كان العميلُ يمرّرها (الخطةُ
المخزَّنة ونصُّها وكلفةُ حالة العلاج الطبيعي، وإلّا مجموعُ المريض كما تفعل
الشاشة). تصل **مجموعةً بلا تصنيف**: كم جلسةً لكلّ نوع، وهي حقيقةٌ سريريّة.
**ولا تُرسَل لمن يرى الدفعاتِ أصلاً** — يحسبها من صفوفه كما كان دوماً.

**والفرقُ حقيقيٌّ لا صوريّ**: ملفٌّ كلفتُه ٥٠٠,٠٠٠ «روبوت» (اشتقاقٌ ١٠)
بدفعتين (٢) وهديّةٍ (٦) ⟶ الصوابُ **١٦**؛ ومن الملخّص المدموج وحده **١٠**.
يُثبته الاختبارُ بحسابٍ حيٍّ على الردّ نفسِه لا بوصف.

**وما لم يُمَسّ**: دلالاتُ العمود `plan_credited` الثلاث (ترحيل ٠٨٧) ·
`mergePhysioPlan` و`entersPhysioPlan` بحرفهما · `resolvePurchasedSessions` ·
بادجُ الرأس (يجمع الملخّصَ كما كان) · حارسُ `canViewPayments` · الدفعاتُ
الخام لمن يملكها · المالُ بكلّ صوره.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (١٤٤ تأكيداً بعد
الإضافة، مقابل ١٢٢ قبلها): **ض** هديّةُ استشارةٍ بجلسات ⟶ الخطةُ مطابقةٌ
بايتاً ولا وسمَ «قُيِّد» ولا دينار، **وهديّةُ «روبوت» تمضي ١٠ ⟶ ١٢** فالحارسُ
ضيّقٌ لا شامل · **ظ** بذرةُ التسعير لا تَسِم استشارةً وتَسِم «روبوت» ·
**غ** المحجوبُ عنه المال: حقلان لا غير · **ولا كلمةَ `isFree` في نصّ الردّ
كلِّه** · والعدّادُ ١٦ من الخادم بمصدره · **ولولا حسابُ الخادم لقرأ ١٠** ·
ومَن يرى الدفعاتِ يأخذ الثلاثةَ خاماً بلا حقلٍ محسوب. و`npm run
test:permission-authority` عاد أخضرَ كاملاً.

**ومُتحقَّقٌ بالعكس أربعَ مرّات، كلُّ آليّةٍ على حدة**: ردُّ الحرّاس الأربعة
إلى القائمة العريضة يُسقط **٥ بالضبط** (ض٣ · ض٤ · ض٥ · ض٧ · ظ٣) **ويُدخل
الاستشارةَ الخطةَ فعلاً** — العطبُ بعينه · وردُّ بذرة التسعير وحدها يُسقط
**ظ٣** وحدَه · وإسقاطُ حساب الخادم يُسقط **غ٦ وغ٧** · وردُّ الملخّص إلى
التصنيف يُسقط **غ٣ وغ٤ وغ٨** ومعها **م٦** في `test:permission-authority`.

### تصحيحُ مراجعة Codex السابعة — ترتيبُ قفلٍ واحد، ووسمٌ يطابق ما استُورد

ثلاثُ ثغراتٍ بلّغتها المراجعة، **ورابعةٌ كشفها اختبارُ الثالثة وهو يُكتب**.
**وبلا ترحيلٍ جديد** ولا عمودٍ ولا قيدٍ ولا حرفٍ في القاعدة.

**١) بذرةُ التسعير وتصحيحُ الدفعة كانا يقفلان بترتيبين متعاكسين.**
البذرةُ تقفل صفَّ المريض (إصلاحُ المراجعة الثالثة) ثمّ تكتب على صفوف دفعاته
(وسمُ ما استوردته)، ومسارا التعديل والحذف يقفلان **صفَّ الدفعة أوّلاً** ثمّ
يطلبان صفَّ المريض من `adjustPhysioPlanForGift` ⟶ **ABBA**.

**وأثرُه شكلان مُعادان حيّاً**، لا واحد: تشابكٌ يُنتج **تصحيحاً ضائعاً**
(الصفُّ لم يُوسَم بعدُ حين يقرؤه التصحيح، فتنصرف `reconcileGiftPlanTx` بلا
طرح، ثمّ تبذر البذرةُ من العدد القديم — فيمضي الطلبان بنجاحٍ ظاهر وتضيع
الأربعُ ⟶ اثنتان)؛ وآخرُ يُنتج **`deadlock detected`** حقيقياً حين يكون
الصفُّ موسوماً سلفاً.

فصار صفُّ المريض هو **الخارجيَّ في المسارين معاً** عبر
`storage.lockPatientForPaymentWriteTx` — وهو الترتيبُ نفسُه الذي أرساه
`lockPatientForGiftTx` على مسار الإدراج في المراجعة الخامسة. يُنادى في
`updatePayment` و`updatePaymentSessionInfo` وفي `correction_store` ثلاثَ
مرّات (التصحيحُ المباشر، والطلبُ المعلَّق، واعتمادُه).

**٢) وخطةٌ كتبها الموظّفُ بيده كانت تُهدَم بتصحيحٍ لاحق.**
`PUT /api/patients/:id/physio-plan` **يستبدل** الخطةَ بما يكتبه الموظّف، ولم
يكن يمسّ وسمَ `plan_credited` على صفوفه. فخطةُ ستَّ عشرةَ (عشرٌ مسعَّرة +
هديّةُ ستٍّ موسومة) تُستبدَل بعشر، ثمّ تُحذف الهديّة ⟶ **أربع**: تصحيحُ
الموظّف يُهدَم بلا أن يعلم.

فصار `clearPhysioPlanProvenanceTx` يرفع الأوسمةَ القائمة **في معاملة
الاستبدال نفسِها** — الرقمُ المؤلَّف هو الحقيقة، ولا تحرّكه تصحيحاتُ
الدفعات بعده. **و`NULL` تبقى `NULL`** (لا نَدَّعي أننا سألنا صفّاً سابقاً
للترحيل)، **وهديّةٌ جديدة بعده تُقيَّد وتُوسَم كالمعتاد** — هي دخلت الخطةَ
القائمة فعلاً.

**٣) والبذرةُ كانت تَسِم أقلَّ ممّا استوردت.**
هي تسحب **كلَّ** دفعةٍ حاملةٍ لجلسات وتضع ما لا نوعَ له في دلو «غير محدد»،
بينما الوسمُ كان يقيس بقائمة الأنواع المعروفة. فصفٌّ بلا نوعٍ مسجَّل يدخل
الخطةَ ويبقى `NULL` — فحذفُه لا يطرح منه شيئاً وجلساتُه تبقى في العدّاد إلى
الأبد.

فصار الوسمُ **بالمعرّفات**: الصفوفُ تُحَلّ مرّةً واحدة بـ
`shared/pricing.ts: physioPlanKeyForPayment` — **قاعدةُ البذرة نفسُها**
(النوعُ كما كُتب، وإن كان فارغاً فدلوُ «غير محدد»، ثمّ `entersPhysioPlan`
تحسم) — فما دخل الخطةَ هو بالضبط ما يُوسَم، **ولا شرطَ SQL ثانٍ ينحرف
عنها**. و`adjustPhysioPlanForGift` تَحُلّ **المفتاحَ نفسَه** عند الطرح، فصفٌّ
في دلو «غير محدد» يُطرَح من دلوه لا يُهمَل. (وحلّت هذه الدالّةُ محلَّ
`physioPlanEligibleType` و`PHYSIO_PLAN_TYPES` من المراجعة السادسة، فلا
تبقى قاعدةٌ ميّتة.)

**٤) ومصيدةُ `PATCH /api/payments/:id` كانت تُعيد الرميَ عارياً.**
كشفه اختبارُ الجمود: `throw err` في معالجٍ غيرِ متزامن يصير رفضاً غيرَ
ملتقَط في Express 4 — **فلا يصل الطلبَ ردٌّ إطلاقاً**، وتخرج العمليةُ نفسُها
حيث لا معالجَ لـ`unhandledRejection`. فخطأُ قاعدةٍ عابر (`deadlock detected`)
كان يُسقط الخادم بدل أن يُردّ ٥٠٠.

فصار يُردّ ٥٠٠ برسالةٍ عربية. **و«لم يُحفَظ شيء» صادقةٌ هنا بلا شرط**: كلُّ
كاتبٍ في المسار معاملةٌ واحدة ترتدّ بكاملها، ولا خطوةَ بعد الالتزام تصل تلك
المصيدة — `logAudit` تبتلع خطأها بحكم تصميمها فلا تُفشل الطلبَ أصلاً
(**مُثبَتٌ حيّاً**، لا مفترَضاً؛ وأوّلُ صياغةٍ فرّقت الرسالتين كما في
`POST /api/payments` ثمّ أثبت الاختبارُ أن الفرعَ الثاني لا يقع هنا، فأُزيل).

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**١٦٩ تأكيداً** بعد
الإضافة، مقابل ١٤٤ قبلها): **أأ** دفعةٌ بلا نوعٍ مسجَّل ⟶ دلوُ «غير محدد»
وعدّادٌ ١٧، **والصفُّ موسومٌ**، وحذفُه ١٧ ⟶ ١٢ · **بب** استبدالُ الخطة يرفع
الأوسمة، فحذفُ الهديّة يُبقي العشرَ لا أربعاً، **وهديّةٌ جديدة بعده تُقيَّد** ·
**جج** تسعيرٌ يزامن تصحيحاً ⟶ لا تصحيحَ يضيع · **دد** الشكلُ الصريحُ للجمود
⟶ الطلبان ينجحان ولا `deadlock detected` · **هه** حقنُ عطلٍ حقيقيّ: قبل
الالتزام ⟶ ٥٠٠ و«لم يُحفَظ شيء» وصفرُ كتابة، وعطلُ التدقيق ⟶ **لا يُفشل
الطلبَ** والتعديلُ وقع، ثمّ القاعدةُ تعمل بعدهما.

> **⚠ وحاقنُ العطل يحترم شكلَ النداء**: `pg-pool` ينادي
> `client.query(text, vals, cb)` بدالّة ردّ، فرميٌ مباشر يُنتج وعداً مرفوضاً
> لا يلتقطه أحد — **فيسقط المُشغِّل نفسُه بدل أن يصل العطلُ من حُقن**. وقع
> فعلاً ثمّ صُحِّح.

**ومُتحقَّقٌ بالعكس خمسَ مرّات، كلُّ آليّةٍ على حدة**: رفعُ ترتيب القفل يُسقط
**٣ بالضبط** (جج٣ · دد٢ · دد٣) **ويُظهر `deadlock detected` حيّاً** · ورفعُ
مسح الأوسمة يُسقط **٣** (بب٣ · بب٥ · بب٧) · وردُّ الوسم إلى شرط النوع يُسقط
**٢** (أأ٤ · أأ٦) · وردُّ مفتاح الطرح وحده يُسقط **١** (أأ٦) · وإعادةُ
`throw err` **تُسقط المُشغِّل بالرفض غير الملتقَط** — العطبُ بعينه.

### تصحيحُ مراجعة Codex الثامنة — الوسمُ يتبع ما رفعته «خدمة جديدة»، وعدّادٌ واحد في الشاشة

ثغرتان، **كلتاهما من كتابتي في هذه التمريرة**، وبلا ترحيلٍ ولا عمودٍ ولا حرفٍ
في القاعدة.

**١) وبندٌ مدفوعٌ رفعته «خدمة جديدة» كان يبقى `NULL`** (P2). `planPatch` في
منسّق الخدمة **لا يسأل عن المجّانيّة إطلاقاً**: كلُّ بندٍ يدخل `mergePhysioPlan`.
وكان الوسمُ يقتصر على الهديّة، فيبقى صفُّ البند المدفوع «غيرَ مقيَّد» وجلساتُه
في الخطة فعلاً. فتصحيحُه لاحقاً من «مدفوع» إلى «مجاني» يقرؤه `reconcileGiftPlanTx`
صفّاً جديداً **فيضيف جلساتِه ثانيةً**: خطةُ عشرٍ + خدمةُ خمسٍ = ١٥، ثمّ **٢٠**
بعد التصحيح. مُعادٌ إنتاجُه حيّاً قبل أيّ تعديل.

**والوسمُ يتبع ما فعله `planPatch` لا وصفَ المال** — وهو معناه الواحد منذ
المراجعة الخامسة: «جلساتُ هذا الصفّ في الخطة الآن»، مُهدىً كان أم مدفوعاً.
فصار `creditedToPlan(entry…)` يُكتب للبندين معاً، والشرطُ `physioSessionsEnterPlan`
نفسُه بلا حرفٍ يتغيّر. **والفرعُ المفرد (بلا `entries`) لم يُمَسّ**: `planPatch`
لا تلمس الخطةَ فيه إطلاقاً، فالمدفوعُ يبقى `NULL` بصدق ويُقيَّد حين يصير هديّةً.

**٢) وعدّادُ الجلسات كان رقمين في شاشةٍ واحدة** (P2). `patient.payments` تغيب
عمّن لا يملك `canViewPayments`، فكلُّ حسابٍ محلّيٍّ من صفوفها يقرأ **صفراً** له.
فكانت بطاقةُ «ملخّص الجلسات» تعرض رقمَ الخادم (١٦ = عشرٌ من الكلفة + ستٌّ
مُهداة) بينما يعرض تبويبُ الزيارات — في الشاشة نفسِها — **١٠**، وكذلك عمودُ
«المتبقي» لكلّ زيارة.

**فصار المصدرُ واحداً**: `resolveSessionsFor` في الصفحة تُنادى من المواضع
الثلاثة، و**`resolvePurchasedSessions` تُنادى مرّةً واحدة في الملفّ كلِّه** —
فلا حسابٌ ثانٍ أعمى عن الحجب يعود بالسهو. ولكلّ موضعٍ مدخلاتُه هو كما كانت
(البطاقةُ دفعاتِ المريض كلَّها، وتبويبُ الزيارات دفعاتِ الحالة المختارة)، فلا
تتبدّل دلالةُ موضعٍ قائم.

**والمشيُ الزمنيُّ يحتاج صفوفاً بتواريخها**: مَن حُجبت عنه لا تصله واحدة،
وبلا زرعِ المجموع يقرأ كلُّ صفٍّ «المتبقي = −عدد الزيارات». فصار الزرعُ يقع
متى غابت الصفوفُ **مهما كان المصدر** — والمزروعُ رقمُ الخادم نفسُه الذي تعرضه
البطاقةُ فوقه.

**والقراران خالصان** في `client/src/pages/physio_sessions_source.ts`
(`pickPhysioSessions` · `seedPurchasedUpFront`) لا في نصّ المكوّن: المشروعُ بلا
مشغّل DOM، فقرارٌ داخل React لا يُختبَر إلّا بقراءة نصِّه، وقراءةُ النصّ لا
تُمسك انقلاباً في المعنى يعود بصياغةٍ أخرى (درسُ ٤.u).

**ولا تسريبَ ماليّ**: الردُّ لم يكتسب حقلاً، والملخّصُ يبقى حقلين بلا رايةِ
مجّانيّة، و`physioSessionsResolved` رقمٌ مجموعٌ بلا تصنيف — كما أرسته المراجعةُ
السادسة بحرفها.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**١٩٣ تأكيداً** بعد
الإضافة، مقابل ١٦٩ قبلها): **وو** خدمةٌ مدفوعة بخمس جلسات على خطةِ عشر ⟶ ١٥
والصفُّ موسومٌ مقيَّداً، ثمّ تصحيحُه إلى «مجاني» ⟶ **ما زالت ١٥ لا ٢٠**،
وحذفُه ⟶ ١٠ · **زز** القراران خالصين بحالاتهما السبع · وعقدُ الشاشة (نداءٌ
واحد داخل المُحلّل الواحد، وقرارُ الزرع من الدالّة لا من شرطٍ في الشاشة) ·
**والفرقُ حقيقيٌّ لا صوريّ**: الخادمُ يرسل ١٦ بينما الحسابُ من صفوفٍ لا
يملكها يُعطي ١٠، والقرارُ يُعطي ١٦ للموضعين معاً · ومفردٌ محجوبٌ مصدرُه
«الدفعات» يُزرَع له المجموعُ رغم ذلك.

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: قصرُ الوسم على الهديّة
يُسقط **٣ بالضبط** (وو٤ · وو٦ · وو٩) **ويُظهر ١٥ ⟶ ٢٠ حيّاً** — العطبُ بعينه ·
وإسقاطُ `pickPhysioSessions` يُسقط **٢** (زز١ · زز١٣) · وردُّ `seedPurchasedUpFront`
إلى شرطها القديم يُسقط **٢** (زز٦ · زز١٥) — ولا شيءَ خارج قسمِه في الثلاث.

### تصحيحُ مراجعة Codex التاسعة — ترتيبُ قفلٍ واحد في المسارات كلِّها، ومفتاحٌ لا يدخل خطةً ليست له

ثغرتان، **كلتاهما من كتابتي في هذه التمريرة**، وبلا ترحيلٍ ولا عمودٍ ولا حرفٍ
في القاعدة.

**١) إعادةُ وسمِ نوع العلاج كانت تتجمّد مع مزامنةٍ متزامنة** (P2).
`syncPatientCases` تأخذ `pg_advisory_xact_lock(919, patientId)` **أوّلاً** ثمّ
تكتب على صفوف دفعات المريض. وكانت معاملةُ التعديل — بعد أقفال المراجعتين
الخامسة والسابعة — تمسك صفَّ المريض وصفَّ الدفعة **ثمّ** تطلب الإرشاديَّ عبر
`reattachPaymentCase ⟶ syncPatientCases`. فمزامنةٌ أخرى تمسك الإرشاديَّ وتنتظر
صفَّ الدفعة، وتعديلُنا يمسك الدفعةَ وينتظر الإرشاديّ ⟶ **`deadlock detected`
حقيقيّ** تقتل فيه Postgres إحداهما، فيُردّ تعديلٌ مشروع ٥٠٠. مُعادٌ إنتاجُه
حيّاً قبل أيّ تعديل.

**فالترتيبُ صار واحداً في المسارات كلِّها**: **إرشاديّ ⟶ صفُّ المريض ⟶ صفُّ
الدفعة** — وهو ترتيبُ `syncPatientCases` و`executeNewService` نفسُه. والمكانُ
`lockPatientForPaymentWriteTx` وحدها، فتُغلق **خمسةَ مواضع نداءٍ معاً**
(`updatePayment` · `updatePaymentSessionInfo` · وثلاثةٌ في
`payments/correction_store.ts`)؛ ومعها `lockPatientForGiftTx` على مسار الإدراج
بالترتيب عينه. **والإرشاديُّ خاصٌّ بالمعاملة ويُعاد أخذُه فيها بلا أثر**، فلا
يضرّ مُنادياً يملكه سلفاً. **ورقمُ المريض يُقرأ مرّةً واحدة** ويُقفَل به
الاثنان معاً، فلا يقع إرشاديٌّ على مريضٍ وصفٌّ على آخر.

**٢) وإعادةُ الوسم إلى «أطراف صناعية» كانت تُبقي الجلساتِ في خطة العلاج
الطبيعي** (P2). نافذةُ تعديل الدفعة تعرض النوعين الجهازيّين صراحةً، وإعادةُ
الوسم إليهما تنقل الدفعةَ إلى حالة الجهاز (`reattachPaymentCase`). وكانت
إضافةُ `reconcileGiftPlanTx` تمضي بلا فحص، و`physioPlanKeyForPayment` تقبل كلَّ
نصٍّ غيرِ الاستشارة — **فتبقى الستُّ في `physio_plan` تحت اسم الجهاز**: عدّادُ
علاجٍ طبيعيّ يحمل مفتاحاً لا يخصّه، ودفعتُه في حالةٍ أخرى.

**فالإضافةُ تُفحَص كما تُفحَص الهديّةُ الجديدة** (`physioSessionsEnterPlan`
نفسُها)، **والطرحُ يبقى بالمفتاح الذي قُيِّد به فعلاً** — وإلّا بقيت بقيّةٌ في
الخطة لا يطرحها أحد. **والمفتاحُ الذي لم يتغيّر يُعدَّل في مكانه**: صفٌّ
استوردته بذرةُ التسعير بوسمٍ غريب موجودٌ في الخطة فعلاً (وذاك سلوكُها منذ ما
قبل هذه التمريرة)، وتعديلُ عدده وحده ليس إدخالاً لمفتاحٍ جديد — وطرحُه كان
يمحو تاريخاً بتعديلٍ لا علاقةَ له به. والمقارنةُ **بالمفتاح لا بالنصّ الخام**،
فالفراغُ والبياضُ يُقرآن دلواً واحداً.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**٢٠٨ تأكيداً** بعد
الإضافة، مقابل ١٩٣ قبلها): **حح** هديّةٌ مقيَّدة تُعاد وسمَها إلى «أطراف
صناعية» ⟶ لا مفتاحَ جهازٍ في الخطة، والعدّادُ ١٦ ⟶ ١٠، والصفُّ لم يعد مقيَّداً ·
**وإعادةُ الوسم إلى نوعٍ فيزيويٍّ آخر تنتقل كما كانت بحرفها** (الستُّ إلى «أبر
صينية» والعدّادُ ١٦ والصفُّ مقيَّد) · **طط** الجمودُ بشكله الصريح: معاملةٌ تمسك
الإرشاديَّ، ثمّ يُنتظَر حتى يقف التعديلُ على قفلٍ فعلاً (لا مهلةً عمياء) ثمّ
تكتب على صفوف الدفعات ⟶ **الطرفان ينجحان ولا جمود**، والوسمُ الجديد محفوظ.

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ آليّةٍ على حدة**: رفعُ الإرشاديّ يُسقط **٢
بالضبط** (طط٤ · طط٥) **ويُظهر `deadlock detected` حيّاً** — العطبُ بعينه ·
وإسقاطُ فحص الإضافة يُسقط **٣** (حح٤ · حح٥ · حح٦) **والخطةُ تُطبَع فيها
`{"sessionCount":6,"treatmentType":"أطراف صناعية"}`** — العطبُ بعينه أيضاً.
ولا شيءَ خارج قسمِه في الاثنين.

### تصحيحُ مراجعة Codex العاشرة — قفلٌ واحد قبل أوّل إدراج، ورقمٌ لا يُعرَض بلا تاريخه

ثغرتان، **كلتاهما من كتابتي في هذه التمريرة**، وبلا ترحيلٍ ولا عمودٍ ولا حرفٍ
في القاعدة.

**١) دفعتان مختلطتان (بندٌ مدفوعٌ ثمّ هديّة) كانتا تتجمّدان** (P2).
إدراجُ صفّ دفعةٍ يأخذ `FOR KEY SHARE` على صفّ المريض بمفتاحه الأجنبيّ —
**وهي متوافقةٌ مع نفسِها**. وقفلُ الهديّة (المراجعةُ الخامسة) كان يُنادى
**لكلّ بند**، فبندٌ مدفوعٌ يسبق هديّةً يُدرَج أوّلاً ثمّ تُطلَب ترقيةُ القفل
إلى `FOR UPDATE` عند بند الهديّة. فطلبان متزامنان بالشكل نفسِه يمسكان
`KEY SHARE` معاً ثمّ ينتظر كلٌّ ترقيةَ الآخر ⟶ **`deadlock detected`
(`40P01`) حقيقيّ** يُردّ به طلبٌ مشروع بـ٥٠٠ «لم يُحفَظ شيء». مُعادٌ إنتاجُه
حيّاً قبل أيّ تعديل، بنصّ Postgres: «Process A waits for ShareLock on
transaction …; Process B waits for ExclusiveLock on advisory lock […,919,…]»
`while locking tuple … in relation "patients"`.

**فالقفلُ يُؤخَذ مرّةً واحدة قبل أوّل إدراج**: النقطةُ تسأل
`storage.giftEntersPlan` عن **بنود الدفعة كلِّها** فإن حملت واحدةً تدخل
الخطةَ نادت `lockPatientPlanRowTx` (إرشاديّ ⟶ صفُّ المريض، الترتيبُ الواحد
نفسُه) **قبل أن تُدرج شيئاً**. **والبوّابةُ هي بوّابةُ القيد بحرفها** —
`giftEntersPlan` دالّةٌ واحدة يقرؤها القفلُ الدفعيُّ وقفلُ البند وقيدُ الخطة
معاً، فلا شرطَ ثالثٌ ينحرف — **فدفعةٌ كلُّها مدفوعة لا تُسلسَل بلا سبب**.
**والحارسُ لكلّ بندٍ يبقى حزاماً ثانياً بلا كلفة**: الإرشاديُّ معدودٌ
و`FOR UPDATE` على صفٍّ مقفولٍ من المعاملة نفسِها لا ينتظر.

**٢) وعمودُ «المتبقي» لكلّ زيارة كان يُقدِّم شراءً لاحقاً على زياراتٍ سبقته**
(P2). زرعُ المجموع لمن حُجبت عنه صفوفُ الدفعات (المراجعةُ الثامنة) كان
يشمل **مريضَ المفرد** — ومصدرُ عدّاده الدفعاتُ نفسُها. فمريضٌ اشترى عشراً
وحضر ثمّ اشترى عشراً أخرى: يقرأ المخوَّلُ على الزيارة الأولى **٩** ويقرأ
المحجوبُ عنه **١٩** — على الزيارة عينها في الشاشة عينها.

**فالقرارُ صار ثلاثياً** (`perVisitSessionMode`): **`seed`** حين يكون المصدرُ
غيرَ الدفعات (خطةٌ أو نصُّ أعدادٍ أو اشتقاقٌ من الكلفة — الدورةُ اشتُريت
سلفاً فعلاً، **ولمن حُجبت عنه الصفوفُ كما لمن يراها**) · **`walk`** للمشي
الزمنيّ كما كان بحرفه · **`hidden`** حين يكون المصدرُ الدفعاتِ والصفوفُ
محجوبة: **لا رقمَ يُعرَض** — لا تواريخَ للمشي ولا مجموعَ اشتُري سلفاً يُزرَع،
وتركُه بلا زرعٍ أسوأ («المتبقي = −عدد الزيارات»). **والمجموعُ الصحيحُ يبقى
في بطاقة «ملخّص الجلسات» فوقه** ولا يضيع. و«لا يُعرَض» تُرسَم **بالعلامة
القائمة نفسِها** (`-999` ⟶ «-») التي تستعملها الاستشارةُ وزيارةُ الخدمة —
لا مسارَ عرضٍ ثانٍ.

**⚠ وانقلب عقدُ بندين من المراجعة الثامنة** (زز٦ وزز١٥) بقرارِ التصحيح لا
لتخضير اختبار، وكلاهما موسومٌ صراحةً في الاختبار.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**٢٢٦ تأكيداً** بعد
الإضافة، مقابل ٢٠٨ قبلها): **يي** دفعتان مختلطتان متزامنتان ببوّابةٍ حتميّة
على قفل الخطة ⟶ الطلبان ينجحان · **ولا `deadlock detected` في سجلّ الخادم**
(لا في نصّ الردّ — المعالِجُ يُخفي `40P01` خلف رسالةٍ عربية، فاختبارٌ يقرأ
الجسمَ وحده يمرّ لسببٍ خاطئ) · أربعةُ صفوف · الهديّتان موسومتان مقيَّدتين ·
والعدّادُ ١٠ ⟶ ١٢ والمقبوضُ بالمدفوعَين. **كك** مفردٌ بشراءين وزيارتين
بتواريخ صريحة ⟶ الخادمُ يرسل ٢٠ بمصدر «الدفعات» · والمخوَّلُ يرى الصفَّين
بتاريخيهما والمحجوبُ لا يصله واحد · **وحتى الزيارة الأولى اشتُري ١٠ فقط**
فالمخوَّلُ يقرأ ٩ والزرعُ كان يقرأ ١٩ · فلا رقمَ للمحجوب عنه والمشيُ
للمخوَّل · وبطاقةُ الملخّص تبقى ٢٠.

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: إسقاطُ القفل الدفعيّ
يُسقط **٦ بنودٍ كلَّها من القسم يي** **ويُظهر `deadlock detected` حيّاً** —
العطبُ بعينه · وردُّ الزرع إلى شموله المحجوبَ يُسقط **٣ بالضبط** (زز٦ ·
زز١٥ · كك٦) · وحذفُ الحارس من الشاشة يُسقط **زز١٠أ وحدَه**. ولا شيءَ خارج
قسمِه في الثلاث — **وكك٤/كك٥ (إثباتُ أن الفرقَ حقيقيّ) تبقيان خضراوين في
الحالتين**، فهما يقيسان البياناتِ لا القرار.

### تصحيحُ مراجعة Codex الحادية عشرة — الصفُّ الموسومُ مجّانياً لا يحمل مالاً

**ثابتٌ واحد**: صفُّ `payments` الموسومُ `is_free_sessions` مبلغُه صفرٌ حتماً
— وهو قاعدةُ المالك حرفاً: «وان اشر مجاني فتحسب جلسات لكن **اموال لاتحسب**».
وكان البابان يكسرانه. **وبلا ترحيلٍ ولا عمودٍ ولا حرفٍ في القاعدة.**

**١) نقطةُ الإنشاء — المسارُ المفرد** (P2، ما بلّغته المراجعة).
`POST /api/payments` بلا `treatmentEntries` كان يكتب `input.amount` كما وصل.
فطلبٌ من مسؤولٍ أو مديرِ فرع بـ`isFreeSessions: true` ومبلغٍ موجب:
**يتخطّى حارسَ «لا متبقّي»** (شرطُه `!isFreeSessions`) · ثمّ يُخزَّن المالُ
مقبوضاً فيرتفع مدفوعُ المريض · **ولا قيدَ يومية يُنشأ له** (شرطُ القيد
`!isFreeSessions` أيضاً). فيصير في «الوارد» مالٌ لا سطرَ له في الدفتر.
مُعادٌ إنتاجُه حيّاً قبل أيّ تعديل: ملفٌّ سُدِّد بالكامل، دفعةٌ عادية تُردّ
٤٠٠، ثمّ «مجّانيّ ٣٠٠,٠٠٠» يمضي ٢٠١ ويُخزَّن بمبلغه.

**والتصحيحُ عند نقطة الخنق نفسِها** التي تختم العلمَ (`input.isFreeSessions
= isFreeSessions`): يُصفَّر المبلغُ معه. **والبنودُ لم تكن تُصاب أصلاً** —
علمٌ علويٌّ مرفوع يجعل `entryIsFree` صادقةً لكلّ بند فتُكتب أصفاراً،
و«شبكةُ الأمان» محروسةٌ بـ`!isFreeSessions`.

**٢) وتوأمُه في مسار التصحيح** — وُجد أثناء التحقّق من الأوّل، لا مذكوراً في
البلاغ. تحويلُ دفعةٍ مقبوضة إلى «مجّانيّة» **بلا إرسال مبلغ**: `changed.amount`
غائبة فيبقى المالُ في `payments.amount`، بينما `touchesJournal` يفيره تغيُّرُ
العلم فيُعكَس القيدُ **ولا يُعاد** (`!updated.isFreeSessions`). فيقول الدفترُ
إن المالَ رُدّ ويقول جدولُ الدفعات إنه ما زال مقبوضاً — **والصفُّ يبقى في
«الوارد»**. وهذا المسارُ مدقَّقٌ بسببٍ إلزاميّ، فالعيبُ ليس فقدانَ أثرٍ بل
تناقضَ رقمين.

**فيُصفَّر المبلغُ حين يكون الناتجُ مجّانياً**، في `applyCorrectionWriteTx`.
**ولا يُلمَس صفٌّ متّسقٌ أصلاً**: الشرطُ على **الناتج** (`willBeFree &&
resultingAmount !== 0`)، فتعديلُ ملاحظةٍ على مجّانيٍّ صفريٍّ لا يضيف حقلاً
إلى `setFields` ولا يُنتج تحديثاً لم يُطلَب.

**وما لم يُمَسّ**: الدفعةُ العادية تُخزَّن بمبلغها كاملاً · حارسُ «لا
متبقّي» بحرفه · بوّابةُ `mayGrantFree` · القيدُ واليوميةُ والتدقيق ·
`reconcileGiftPlanTx` ووسمُ `plan_credited` · **وهديّةٌ تبقى تمضي على ملفٍّ
سُدِّد بالكامل** كما أرسته المراجعةُ السادسة.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**٢٤٠ تأكيداً** بعد
الإضافة، مقابل ٢٢٦ قبلها): **لل** الحارسُ مسلَّحٌ فعلاً (دفعةٌ عادية ⟶ ٤٠٠)
· والهديّةُ تمضي ٢٠١ · **والمبلغُ المخزَّن صفرٌ ولو أُرسل ٣٠٠,٠٠٠** ·
والمقبوضُ لم يتحرّك · والجلستان حُسبتا والصفُّ موسومٌ مقيَّداً · **والصفُّ
والدفترُ متّفقان** · والدفعةُ العادية قبله بمبلغها. **مم** دفعةٌ مقبوضة
٢٠٠,٠٠٠ ثمّ قلبُ العلم بلا مبلغ ⟶ **المالُ تبع العلمَ إلى الصفر** ·
والمقبوضُ نزل بمقداره · ولا قيدَ حيٌّ باقٍ · **وتعديلُ ملاحظةٍ بعده يمضي
والصفُّ كما هو**.

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ بابٍ على حدة**: إسقاطُ تصفير الإنشاء يُسقط
**٣ بالضبط** (لل٣ · لل٤ · لل٦) · وإسقاطُ تصفير التصحيح يُسقط **٤ بالضبط**
(مم٣ · مم٤ · مم٥ · مم٧) — ولا شيءَ خارج قسمِه في الاثنين.

### تصحيحُ مراجعة Codex الثانية عشرة — دمجُ ملفَّين: الوسمُ يتبع خطتَه أو يُرفَع

**ثغرةٌ واحدة، وهي من كتابتي في هذه التمريرة.** **وبلا ترحيلٍ ولا عمودٍ ولا
حرفٍ في القاعدة.**

**الواقعة** (مُعادٌ إنتاجُها حيّاً على النقطة الحقيقية قبل أيّ تعديل):
`plan_credited = true` معناه «جلساتُ هذا الصفّ في الخطة **الآن**»، و
`mergePatients` ينقل صفوفَ الدفعات إلى الهدف **ويُسقط خطةَ المصدر مع صفّه**
(`physio_plan` ليس في `fillable` ولا في الرقعة). فيصير الوسمُ يشير إلى خطةٍ
لم تعد موجودة، ويُقاس بعدها على خطةِ الهدف التي لم تُبنَ منه قطّ:

```
هدفٌ خطتُه ١٠ روبوت · مصدرٌ خطتُه ١٦ (١٠ مسعَّرة + هديّةُ ٦ مقيَّدة)
⟶ الدمج ⟶ خطةُ الهدف ١٠ (وخطةُ المصدر ضاعت)
⟶ حذفُ الهديّة المنقولة ⟶ **٤**
```

**والعلاجُ سؤالٌ واحد: أانتقلت خطةُ المصدر أم لا؟**
`carryPhysioPlanOnMergeTx` (في `storage.ts` بجوار أخواتها) **داخل معاملة
الدمج**، بعد رقعة صفّ الهدف وقبل حذف المصدر — والقراءةُ تحت القفل **بترتيب
الكتابة نفسِه** (الهدفُ ثمّ المصدر) فلا ترتيبَ ثانٍ:

- **الطرفان يملكان خطة ⟶ تُجمعان** بالكاتب القانونيّ `adjustPhysioPlanForGift`
  (لا SQL خطةٍ منسوخ). كلتاهما **رقمٌ مؤلَّف** كتبه موظّف، والمريضُ واحدٌ
  اشترى في الملفّين — تماماً كما يجمع الدمجُ `total_cost` والدفعاتِ والزياراتِ
  وقيودَ الكلف. **والوسمُ يبقى صادقاً**: جلساتُ الصفّ انتقلت مع خطتها.
- **وإلّا يُرفَع الوسم** عن **المعرّفات المنقولة بأعيانها**: لا خطةَ تحمل
  جلساتِها، فلا يُطرَح منها لاحقاً. **و`NULL` تبقى `NULL`** (صفٌّ سابقٌ
  للترحيل، لا نَدَّعي أننا سألناه)، **و`false` تبقى `false`**، **وصفوفُ الهدف
  نفسِه لا تُمَسّ**.

**ولا تُخترَع خطةٌ لمن لا خطةَ له** (حادثةُ ذي قار): هدفٌ بلا خطة عدّادُه يقرأ
دفعاتِه، وإنشاءُ خطةٍ له من خطة المصدر يقلبه إلى القراءة من الخطة وحدها
**فتختفي جلساتُ دفعاته هو**. والحارسُ قائمٌ في `adjustPhysioPlanForGift` نفسِها
(تنصرف حين لا خطةَ للهدف)، **فيُنادى الكاتبُ القانونيّ ولا يُكتب فرعٌ ثانٍ**.

**ولا دينارَ يتحرّك**: لا كلفةَ ولا قيدَ دفتر ولا دفعة — الدمجُ يجمع
`total_cost` كما كان بحرفه.

اختبارٌ دائمٌ مُحدَّث ضمن `npm run test:physio-free` (**٢٥٤ تأكيداً** بعد
الإضافة، مقابل ٢٤٠ قبلها): **نن** شكلُ العطب بعينه ⟶ الخطةُ ١٠ + ١٦ = **٢٦**،
والوسمُ صادق، وحذفُ الهديّة المنقولة **٢٦ ⟶ ٢٠** لا ١٠ ⟶ ٤ · **سس** هدفٌ بلا
خطة ⟶ **لا خطةَ تُخترَع**، والوسمُ مرفوع، وحذفُ الهديّة بعده لا يُنشئ خطةً ولا
يطرح من شيء.

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ نصفٍ على حدة**: إسقاطُ جمع الخطّتين يُسقط **٣
بالضبط** (نن٦ · نن٧ · نن٩) **ويُظهر ١٠ ⟶ ٤ حيّاً** — العطبُ بعينه · وإسقاطُ رفع
الوسم يُسقط **١** (سس٤) — ولا شيءَ خارج القسمين الجديدين في الاثنين.

> **⚠ إخفاقاتٌ مسبَقة غيرُ متعلّقة** (مُثبَتٌ هذه المرّة بشجرة عملٍ على
> **`main`** لا بـ`git stash`، والسطورُ متطابقةٌ حرفاً قبل التغيير وبعده،
> ولم تُصلَح — خارجَ هذه المهمّة، وكلُّها عيوبُ فِكستشر لا سلوكِ إنتاج):
> `test:pending-charge` (٨) · `test:discount` (١٢) · `test:departments` (٤) ·
> `test:returning-patient` (١٢) · `test:payment-guard` و
> `test:cost-ledger-parity` (تعطُّلُ فِكستشر، بصفر سطر فشل).
> **و`test:permission-authority` لم يعد منها** — كان عطبي، وقد زال.
> **(٢٠٢٦-٠٩-٢٤) وتُضاف**: `test:episode-flow` (١١ — أغلبُها ينتظر ردَّ حلقةٍ
> ثانيةٍ مفتوحة بـ٤٠٩، وقد رفعه ترحيل ٠٧٣) و`test:service-path` (٢) — مُثبَتٌ
> تطابقُهما حرفاً على `main` (`9bbfef0`) بشجرة عملٍ حقيقية.
> **(٢٠٢٦-٠٩-٢٥) وتُضاف**، مُثبَتةً حرفاً على `main` (`471ec1d`) بشجرة عملٍ حقيقية:
> `test:followup` (٤) · `test:decision-queue` (٤) · `test:device-prescription` (١) ·
> و`test:merge-exams` و`test:parallel-device-operations` (تعطُّلُ فِكستشر: رقمُ هاتفٍ
> مكرَّر، ومفتاحُ `patient_branch_access` عند التنظيف). و`test:discount` صار **٩** لا
> ١٢، متطابقاً على `main` كذلك.

> **⚠ ملاحظةٌ تشغيلية عن ملفّ «انتصار» نفسِه**: كلفتُها صُحِّحت يدوياً إلى
> ١٠٠,٠٠٠ (ما دفعته فعلاً)، فسقط الاشتقاقُ وعاد عدّادُها إلى سجلّ الدفعات:
> **٨ = جلستان مدفوعتان + ستٌّ مهداة**. **ولا تُنشأ لها خطةٌ بـ٢٦** — الخطةُ
> تَغلب كلَّ شيء، فتُخفي هديّتَها من جديد. ووصفةُ الطبيب بـ٢٦ تبقى على الورق.

---

## 4.aj تحريرُ كلفة الحالة على الهاتف — أهدافُ لمسٍ حقيقية (٢٠٢٦-٠٩-٢١)

**شكوى المالك**: في بطاقة المريض، القلمُ بجانب «التكلفة» صغير، والمربّعُ
الذي يفتح ضيّقٌ جداً، وزرّا ✓/✗ بالكاد يمكن ضغطهما.

### الفحص — مقيسٌ بمتصفّحٍ حقيقيّ على CSS المبنيّ، لا موصوف
`PatientCasesTabs.tsx`: المحرِّرُ يعيش داخل **خليةِ ثلثِ الشبكة** (التكلفة /
المدفوع / المتبقّي). عند ٣٩٠px:

| العنصر | قبل | بعد | الحدّ المعتمَد |
|---|---|---|---|
| خليةُ التكلفة | ١٠٦px | ٣٥٨px | — |
| مربّعُ الإدخال | **٤٧×٣٢** | **٢٣٠×٤٤** | — |
| زرُّ ✓ | **١٦×١٦** | **٤٤×٤٤** | ٤٤ (Apple) / ٤٨ (Material) |
| زرُّ ✗ | **١٦×١٦** | **٤٤×٤٤** | ٤٤ / ٤٨ |
| هدفُ لمس القلم | **١٢×١٢** | **٥٠×٥٠** | ٤٤ / ٤٨ |

### الإصلاح — على الهاتف وحده
المحرِّرُ يأخذ **عرضَ الصفّ كلِّه** أثناء التحرير (`col-span-3 md:col-span-1`،
مشروطاً بوجود الأعمدة الثلاثة) · والإدخالُ `h-11` بخطٍّ أكبر · والزرّان
`h-11 w-11` بخلفيّةٍ خفيفة تجعلهما يُقرآن زرّين · **والقلمُ يكبر هدفُه بعنصرٍ
زائف** (`before:-inset-2.5`) فيصير ٥٠×٥٠ **بلا أثرٍ في التخطيط** — وزيادةُ
مقاسه الفعليّ كانت ستُطيل سطرَ العنوان لكلّ مريض، محرِّراً كان أو لا.

**وسطحُ المكتب لم يتغيّر بحرف** — مُقارَنٌ آلياً عند ١٢٨٠px قبل وبعد،
**متطابقٌ حرفياً**: الخليةُ ٤٠٨×٧٦ · الإدخالُ ٣٤٤×٣٢ · القلمُ ١٢×١٢ ·
الزرّان ١٦×١٦ · بلا فيض. فكلُّ مقاسٍ جديد مشروطٌ بـ`md:` يعيده إلى قيمته
السابقة، ومنها `md:h-auto md:w-auto` للزرّين (لم يكن لهما مقاسٌ مفروضٌ أصلاً)
و`md:before:hidden` للعنصر الزائف.

### وما لم يُمَسّ
عرضُ الكلفة غيرَ محرَّرة · شبكةُ الصناديق الثلاثة · صندوقا المدفوع والمتبقّي ·
منطقُ الحفظ والصلاحية (`canEditCost`) · ولا حرفَ في الخادم ولا القاعدة.

### الترحيل
**بلا ترحيل** — تغييرُ أصنافِ عرضٍ في ملفٍّ واحد.

اختبارٌ دائم: `npm run test:case-cost-edit-phone` (عقدُ الشاشة، بلا قاعدة
بيانات؛ التعليقاتُ تُزال قبل الفحص، والفحصُ على كتلة التحرير وزرّ القلم
وحدهما لا الملفّ كلِّه). **ومُتحقَّقٌ بالعكس**: إعادةُ المقاسات القديمة تُسقط
**ثمانيةَ بنود** كلَّها من أقسام الإصلاح، وأقسامُ «ما لم يُمَسّ» تبقى خضراء.

---

---

## 4.ak قدراتُ القراءة — المساعدُ يسأل التطبيقَ بجلسة السائل (٢٠٢٦-٠٩-٢٢)

**«يعلم كلَّ شيء» و«يحترم حدَّ كلّ مستخدم» نتيجتان لآليةٍ واحدة**: إن سأل
المساعدُ **التطبيقَ** لا قاعدةَ البيانات، وسأله بجلسة السائل، جاء الشمولُ
مجّاناً وجاء الحدُّ مجّاناً — **ولا سطرَ صلاحيةٍ واحد يُكتب من جديد**. فرعُ
`claude/ai-capability-layer`. **بلا ترحيل.**

### الواقعة (لقطةُ شاشةٍ من الإنتاج)
سأل المالكُ: «مَن أكثرُ خبير أطرافٍ في كلّ الفروع لديه مرضى متأخّرون بلا عذر
— بلون أحمر؟» فأجاب المساعد: «أنا مساعدٌ محاسبيّ وليس لديّ أداةٌ تقدّم إحصاءً
عن الخبراء والأوامر المتأخّرة بالألوان… استعرض التنبيهات بنفسك».

**والجوابُ كان محسوباً في القاعدة أصلاً**:
`server/manufacturing/store.ts:1738` ⟵ `e.overdue++` لكلّ خبير، وتمييزُ
الأحمر عن الكهرمانيّ في `holdReasonCode` بنقطة التنبيهات. **والمعرفةُ عملت
فعلاً** (وصفُ الشاشة في الردّ صحيح، من دليل الصفحة) — فالساقطُ كان **الوصولَ
إلى البيانات وحده**: ١٣٢ نقطةَ قراءة في التطبيق، والمساعدُ يصل ثمانياً.

**وأداةٌ جديدة لكلّ سؤالٍ لا تُغلق الفجوة أبداً**: القائمةُ ثابتة والتطبيقُ
تحرّك ثلاثمئةً وتسعةً وسبعين طلبَ دمج، وكلُّ شاشةٍ جديدة ثغرةٌ جديدة. ومع كلّ
أداةٍ تُضاف يكبر نصُّ النظام في **كلّ** طلب ويضطرب اختيارُ النموذج بينها.

### الآليّة — أربعةُ ملفّات، `server/ai/capabilities/`
| الملفّ | ماذا يفعل |
|---|---|
| `catalog.ts` | يقرأ **جدول مسارات Express** ويبني الفهرس؛ ومعه قرارٌ صريح لكلّ نقطة |
| `match.ts` | يرشّح بالكلمات بعد تطبيعٍ عربيّ — دالّةٌ خالصة |
| `invoke.ts` | ينفّذ النقطةَ **داخل العملية** بجلسة السائل |
| `shape.ts` | يقصّ ويجمّع ويعدّ **في الخادم** — دالّةٌ خالصة |

وأداتان في السجلّ: `list_capabilities(topic)` تبحث، و`read_capability(name, …)`
تنادي. **وخطوتان لا واحدة عمداً** — مئةٌ وتسعَ عشرةَ قدرةً في نصّ النظام
تُرسَل مع كلّ سؤال.

### والجلسةُ هي هي — لا تُبنى ولا تُوسَّع
الكائنُ المُمرَّر هو `req.session` الحقيقيُّ بعينه. فوسيطُ «تحديث الصلاحيات
حيّاً» يُعيد قراءتَها من القاعدة كما يفعل لكلّ طلب، وحسابٌ عُطِّل يُردّ ٤٠١
هنا أيضاً. **ولا حقلَ واحدٌ يُضاف أو يُبدَّل.**

**وهذا أأمنُ من القائم لا أخطر**: كلُّ أداةٍ في `tools/registry.ts` تستعلم
القاعدةَ بحارسٍ كتبناه **لها وحدها** — وقد انكشفت ثغراتٌ حقيقية في ثلاثةٍ منها
(§4.n). وهذه الطبقةُ **لا تفتح باب بياناتٍ جديداً إطلاقاً**، فلا نسخةَ ثانية
من الصلاحية تنحرف بعد شهرين: لا نسخةَ أصلاً.

### ⚠ و«الطريقةُ قراءةٌ» ليست ضماناً — مُثبَتٌ لا مفترَض
**`GET /api/logout` يُنهي الجلسة**، و`GET /api/cron/daily-income` يحسب ويرسل.
فلو قيل «كلُّ قراءةٍ آمنة» لَسجّل المساعدُ خروجَ الموظّف وهو يحاول الإجابة —
**وهذا مُثبَتٌ بالمراجعة العكسية**: برفع قائمة السماح يُنادى المعالِجُ فعلاً
وتُنهى الجلسة.

فثلاثةُ أصنافٍ لا صنفان: **موصوفةٌ** (تُنادى) · **ممنوعةٌ بسببٍ مكتوب** ·
وما ليس فيهما **يُفشل الحارسَ المعماريّ** — فنقطةٌ جديدة تُجبر صاحبَها على
قرارٍ صريح. **والاكتشافُ من جدول المسارات أمسك إحدى عشرةَ نقطةً لم يجدها
بحثٌ نصّيّ** (منها `/api/patients` و`/api/patients/:id`) لأنها سُجّلت بأنماطٍ
أخرى — وذاك بعينه الفرقُ بين فهرسٍ يُكتب باليد وفهرسٍ يُشتَقّ.

### والوسمُ الماليُّ لغرض النقطة لا لوجود مبلغ
`financial: true` حين يكون **غرضُ النقطة نفسِه مالاً** (محاسبةٌ · مصاريفُ ·
فواتيرُ · تقريرُ إيراد) — فتُحجب عن الوضع العامّ عرضاً ونداءً. أمّا شاشةٌ
تشغيليةٌ تحمل مبلغاً **تحجبه هي بنفسها** عمّن لا يملك `canViewPayments`
(سجلُّ المرضى · ملفُّ المريض) فلا تُوسَم: وسمُها كان يمنع موظّفَ الاستقبال
من قراءة ما يراه على شاشته بعينه، **وذاك نقضُ الغرض**.

### والحسابُ في الخادم لا في النموذج
`aggregate` يرشّح (`where` بـ`equals`/`isNull`/`notNull`) ويجمّع (`groupBy`)
ويجمع (`sum`) ويرتّب — **فقاعدة «لا تحسب رقماً بنفسك» محفوظةٌ بحرفها**.
**والقصُّ يُعلَن ولا يُخفى**: `truncated` مع العدد الحقيقيّ، فيقول النموذج
«أعلى عشرة من أربعين» لا «أربعون» كذباً.

### وما لم يُمَسّ
الأدواتُ الإحدى عشرة القائمة **بحرفها** (ومنها أدواتُ التقارير الثلاث —
تحسب حدودَ يوم بغداد والفروقَ والنسبَ وتقسيمَ الأقسام، ولا نقطةَ تفعل ذلك،
فلا تُستبدَل) · المعرفةُ الموثوقة واسترجاعُها · التدريبُ · `resolveAiAccess`
وعزلُ الفرع وعزلُ المال · سجلُّ المحادثات · القاعدةُ وكلُّ ترحيلٍ من ٠٠١ إلى
٠٨٧ · الواجهةُ · واتساب.

### والتقاعدُ لاحقاً بالقياس لا بالرأي
سجلُّ المحادثات (٠٨٤) يسجّل الأدواتِ المستعمَلة في كلّ تبادل. فبعد أسبوعين
تُقرأ: أيُّ أداةِ مريضٍ ما زال النموذجُ يختارها. **وأدواتُ التقارير والتدريب
تبقى قطعاً.**

اختبارٌ دائم: `npm run test:ai-capabilities` (٣٤ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية عبر Express حقيقيّ: **سؤالُ المالك بعينه** ⟶ الخبيرُ
الأكثرُ تأخّراً بلا عذر بثلاثةٍ، وبأربعةٍ حين لا يُستبعَد الكهرمانيّ ·
**والحدُّ من النقطة نفسِها**: مديرُ الفرع الآخر والاستقبالُ يريان خبيرَهما
وحده، ومَن لا يملك عرضَ المرضى يُردّ برسالة النقطة · والمالُ خلف وضعه عرضاً
ونداءً · و`/api/logout` لا يبلغ معالِجَه **والجلسةُ لا تُنهى** · والسرُّ لا
يُقرأ · والفهرسُ بلا نقطةٍ غيرِ مقرَّرة ولا وصفٍ شاخّ · وصفرُ كتابة) و
`npm run test:ai-capabilities-pure` (٣٢ تأكيداً خالصاً بلا قاعدة بيانات:
التجميعُ والقصُّ والمطابقةُ العربية بعد تجريد السوابق، وترميزُ معامِل
المسار فلا يُحقَن، وإسقاطُ الفارغ من الاستعلام).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ حارسٍ على حدة**: رفعُ بوّابة الوضع
الماليّ يُسقط تأكيداً واحداً بالضبط · ورفعُ قائمة السماح يُسقط **اثنين**
(والجلسةُ تُنهى فعلاً — العطبُ بعينه) · وإسقاطُ شرط `isNull` يُسقط واحداً
(ثلاثةٌ تصير أربعة). **وأمسك العكسُ ضعفاً في الاختبار نفسِه**: مصيدةُ
`destroy` الصامتة كانت تجعل فحصَ «الجلسةُ لم تُمَسّ» يمرّ **لسببٍ خاطئ** —
يُردّ النداءُ لأنه إعادةُ توجيهٍ لا لأنه مُنع. فصارت تُسجَّل.

**⚠ وإخفاقٌ مسبَقٌ غيرُ متعلّق**: `test:ai-tools` يفشل بتأكيدٍ واحد («بالرمز
الحالي والاسم والفرع») — **مُثبَتٌ متطابقاً على `main`** بشجرة عملٍ حقيقية.

### تصحيحٌ لاحق — ترويساتُ الجسم لا تُورَّث (٢٠٢٦-٠٩-٢٢، أوّلُ اختبارٍ على الإنتاج)

**الفهرسُ عمل والشاشةُ وُجدت — والنداءُ وحده انهار.** أوّلُ سؤالٍ جرّبه
المالكُ بعد النشر («كم مريضاً ينتظر المعاينة الآن، وفي أيّ اختصاص») ردّ:
«خادمُ شاشة انتظار المعاينة تأخّر في الاستجابة». **وهو تشخيصٌ صادقٌ لعطبٍ
في مكانٍ آخر**: المساعدُ سمّى الشاشةَ الصحيحة بالضبط، فالاكتشافُ والمطابقةُ
والصلاحيةُ كلُّها عملت.

**والسببُ الجذريُّ مُعادٌ إنتاجُه حيّاً قبل أيّ تعديل**: الطلبُ الصناعيُّ كان
يرث ترويساتِ مصدره بالكامل — ومصدرُه `POST /api/ai/chat` بجسمٍ حقيقيّ. فيرث
`content-type: application/json` و`content-length`، فيرى `express.json()`
**جسماً موعوداً** فيسلّم الكائنَ إلى قارئ الجسم، وذاك يعامله معاملةَ مجرى
قراءةٍ حقيقيّ فينهار:

```
TypeError: Cannot read properties of undefined (reading 'Symbol(kState)')
    at Readable.removeListener (node:internal/streams/readable:1174:11)
    at cleanup (node_modules/raw-body/index.js:295:12)
```

**والانهيارُ في دورةٍ لاحقة فلا تلتقطه مصيدةُ النداء**، فيبقى الوعدُ معلَّقاً
حتى المهلة (اثنتا عشرةَ ثانية) ثمّ يُردّ ٥٠٤ — وهو ما تُرجمه الشاشةُ «تأخّر
الخادم».

**والإصلاحُ أن الطلبَ الصناعيَّ قراءةٌ بلا جسم فلا يُعلن جسماً**:
`inheritHeaders` تُسقط `content-type` · `content-length` ·
`transfer-encoding` · `content-encoding`، **وتورّث ما عداها كما هو** (وكيلُ
المتصفّح واللغةُ والمُحيلُ — يقرؤها التدقيقُ في نقاطٍ قائمة). والوسمُ
الداخليُّ يُكتب **أخيراً** فلا يُزيحه مصدرٌ يحمل اسمَه.

**ولماذا لم يمسكه الاختبارُ الأصليّ**: مصدرُه كان **جلسةً بلا ترويسةٍ
واحدة**، فلا يرى قارئُ الجسم جسماً موعوداً فلا يُنادى أصلاً — **فمرّ لسببٍ
خاطئ**. فصار الملفُّ يمرّر **ترويسات متصفّحٍ حقيقية** في كلّ نداءٍ فيه.

**وبلا ترحيل** — ولا حرفَ في القاعدة ولا في الفهرس ولا في المطابقة ولا في
التجميع ولا في قائمتَي المنع والوصف.

اختبارٌ دائم: القسمُ **ز** في `npm run test:ai-capabilities` (٤١ تأكيداً بعد
الإضافة، مقابل ٣٤ قبلها): الترويساتُ الأربعُ تسقط · وما ليس جسماً يبقى ·
والوسمُ لا يُزاح · **والنداءُ بترويسات المتصفّح يمضي ٢٠٠ بمهلةٍ قصيرة عمداً**
(أربعُ ثوانٍ — فانحدارٌ يُقرأ عطباً لا بطئاً) · وجوابُه هو جوابُ المصدر
العاري نفسُه.

**ومُتحقَّقٌ بالعكس**: برفع التصفية **تنهار الحزمةُ كلُّها عند سؤال المالك
بعينه** (القسم ب، أوّلُ نداءِ قراءة) بنفس الاستثناء المطبوع أعلاه — لا
تأكيدٌ يسقط بل تشغيلةٌ تموت، وذاك أصدقُ من عدّ.

---

---

## 4.am أرضيّةُ الأجهزة الباقية — التصحيحُ لا يسحب مالَ جهازٍ لم يُلغَ (٢٠٢٦-٠٩-٢٣)

**بلا ترحيل** — ولا عمودَ ولا جدولَ ولا قيد، ولا حرفَ في الواجهة.

### الواقعة (الملفّ WB-01982، أُمسكت قبل التأكيد)
مريضٌ له جهازان على خيطٍ واحد (`#257` أمرُ ٣٦٢، و`#258` أمرُ ٣٧٢)، بِيع كلٌّ
منهما بـ٢,٧٠٠,٠٠٠ فقيّد الدفترُ **+٢,٧٠٠,٠٠٠ مرّتين**. ثمّ خفّض المسؤولُ كلفةَ
الخيط يدوياً بـ**−٢,٧٠٠,٠٠٠** (`case_cost_edit`) لأن أحدهما مكرَّر — فصارت
الدفاتر تحمل جهازاً واحداً، **ومالُ المكرَّر خرج منها سلفاً**، والثابتُ سليم
(مجموعُ القيود = `total_cost` = ٢,٧٠٠,٠٠٠).

ثمّ فتح نافذةَ «إلغاء العملية بالكامل» على المكرَّر. و`standingCostOf` تسأل
«كم على دفاتر المريض؟» لا «كم وضع هذا الجهاز بعينه؟»، فقرأت `min(٢.٧ سعرُ
البيع، ٢.٧ المجموع، ٢.٧ كلفةُ الخيط) = ٢,٧٠٠,٠٠٠` — **وهي كلفةُ الجهاز
الباقي**. فكان التأكيدُ سيُنزل المجموعَ إلى **صفر**، ويقرأ الملفُّ **رصيداً
للمريض ٢,٠٠٠,٠٠٠** بدل متبقٍّ ٧٠٠,٠٠٠، **ويبقى الجهازُ الحيُّ قيد التصنيع بلا
كلفةٍ على الدفاتر**.

**والحدُّ القائم لم يكن ليمسكها**: كُتب ليمنع عكسَ أكثرَ ممّا على الدفاتر بعد
تخفيضٍ يدويّ — وهو يرى ٢,٧٠٠,٠٠٠ فيظنّها كلفةَ هذا الجهاز، وهي كلفةُ الآخر.

### القاعدة الآن — حدٌّ ثالثٌ بجوار الاثنين
**لا تنزل الدفاتر تحت ما تحمله للأجهزة الحيّة الباقية.**

```
العكس = max(0, min( سعرُ البيع ،
                    مجموعُ المريض − أرضيّةُ أجهزته الباقية ،
                    كلفةُ الخيط   − أرضيّةُ أجهزة هذا الخيط ))
```

**والأرضيّةُ من الدفتر لا من السعر** — وهذا جوهرُ الإصلاح: `agreed_cost` سعرُ
بيعٍ على الحلقة **وليس دليلاً على أن مبلغاً قُيِّد**. ترحيلا ٠٥٠ و٠٦٣ ملآ
حلقاتٍ تاريخية بأسعارها **بلا مسِّ `total_cost` ولا كلفةِ الخيط** بنصّهما،
فمريضٌ عائدٌ بجهازٍ قديمٍ مسلَّم كانت أرضيّةٌ مبنيّةٌ على السعر تُلغي تصحيحَ
جهازه الجديد كلَّه — **عطبٌ أسوأُ من الأوّل، أمسكه الاختبارُ الحيُّ قبل الدمج**.

فالأرضيّةُ **مجموعُ ما قيّده كلُّ جهازٍ باقٍ في `cost_entries`** عبر
`device_episode_id` الذي يكتبه `applyDeviceSaleFinancialsTx` لكلّ بيعِ جهازٍ
حيّ — **صافياً** (فتصحيحُ سعرٍ منسوبٌ إلى جهازه يُنقص أرضيّتَه)،
و`GREATEST(0, …)` لكلّ جهازٍ على حدة كي لا يوسّع صافٍ سالبٌ شاذٌّ أرضيّةَ غيره.
**وما لا أثرَ له في الدفتر لا أرضيّةَ له** — والحدُّ القديم يبقى حارسَه.

**والباقي هو المُباعُ الحيُّ وحده**: `in_manufacturing` و`delivered` — فما دون
البيع لم يُقيَّد له شيء، والمُبطَلُ إدارياً عُكست كلفتُه سلفاً وقيدُه المعاكس
منسوبٌ إليه فيصفّر صافيه. **وبلا هويّةِ حلقةٍ** (الموروثُ) تُحسَب الأجهزةُ
الباقية كلُّها أرضيّةً — وهو الصدق: لا قيدَ منسوبٌ إلى هذه العملية بعينها.

**والحدّان لازمان كلٌّ بذاته**: خيطٌ ثانٍ بمالٍ حقيقيّ (علاجٌ طبيعي) يجعل
مجموعَ المريض واسعاً بينما خيطُ الأجهزة قصير — فلا يمسكها إلّا الحدُّ الخيطيّ؛
وتخفيضُ المجموع وحده من «تعديل مريض» يترك كلفةَ الخيط أعلى — فلا يمسكها إلّا
الحدُّ المريضيّ. **ومُثبَتٌ بالعكس لكلٍّ على حدة.**

### وفي البيانات السليمة لا يفعل شيئاً
أضاف كلُّ بيعٍ سعرَه إلى الدفاتر وقيّده باسم جهازه، فالفارقُ يبقى ≥ سعرِ هذا
الجهاز ولا يبيت الحدُّ أصلاً — **حدٌّ أضيقُ لا أوسع**، ولا تصحيحٌ مشروعٌ
يُمنَع.

### وبلا قفلٍ جديد ولا ترتيبٍ ثانٍ
القراءةُ **بعد** قفل صفّ المريض، فبيعٌ متزامن إمّا التزم قبله فيُرى في الطرفين
أو يقف عليه فلا يُرى في أيّهما. **والانحرافُ بين المعاينة والتنفيذ يحرسه
الختمُ نفسُه** (`stampOf` يحمل المقدار) فيُردّ البائتُ ٤٠٩ بلا كتابةِ حرف.
**و`reversedCost = 0` مسارٌ قائمٌ أصلاً**: `if (reversedCost > 0)` تحرس
الكتابةَ الماليّة كلَّها، فالعمليةُ تُلغى ولا يتحرّك دينار، وصفُّ التصحيح يقول
`financial_delta = 0` بصدق.

### تصحيحٌ لاحق — قيدُ المريض على الوصل (مراجعةٌ آلية على ٣٨٦)
`cost_entries` مفهرسٌ على `patient_id` و`case_id` و`(branch_id, created_at)`
— **ولا فهرسَ على `device_episode_id`**. فوصلُ الأرضيّة به وحده كان يُجبر
القارئَ على **مسح الدفتر كلِّه** في كلّ معاينةِ تصحيحٍ وكلّ تنفيذ، مهما قلّت
قيودُ هذا المريض. مقيسٌ حيّاً على دفترٍ بستّين ألف قيد:

| | قبل | بعد |
|---|---|---|
| خطّةُ القراءة | `Seq Scan` على `cost_entries` | `Bitmap Index Scan` على `idx_cost_entries_patient` |
| الصفوفُ المقروءة | **٦٠٬٠٠٠** | **٣٠٠** |
| الصفحات | ٦٨٣ | ١٥٢ |
| الزمن | ٩٫١ مللي ثانية | **١٫١** |

**ولا يغيّر الناتجَ بحرف**: القيدُ المركَّب `cost_entries_patient_episode_fk`
**مُتحقَّقٌ منه في القاعدة**، فمريضُ القيد هو مريضُ حلقته حتماً — والشرطُ صادقٌ
أصلاً لكلّ صفٍّ كان سيُطابَق (مُثبَتٌ بتطابق الناتج على المجموعة نفسِها).
**وموضعُه `ON` لا `WHERE`**: الوصلُ يساريٌّ عمداً — حلقةٌ بلا قيدٍ أرضيّتُها
صفرٌ ويجب أن تبقى، وشرطٌ على الطرف الأيمن في `WHERE` يقلبه وصلاً داخلياً
فتسقط. **وبلا فهرسٍ جديد ولا ترحيل.**

### وما لم يُمَسّ
سؤالُ إرجاع المبلغ وعقدُه · إبطالُ الأمر وإلغاءُ الحلقة وتقاعدُ المتابعة ·
سحبُ سلطة المعاينة (٠٦١) · الاستبدالُ · «تراجع عن الشراء فقط» · الدفعاتُ
والتسليمُ وسجلُّ التصنيع · `applyDeviceSaleFinancialsTx` و`updateCaseCost` ·
العلاجُ الطبيعي · وكلُّ ترحيلٍ من ٠٠١ إلى ٠٨٧.

اختبارٌ دائم: القسمُ **ص** في `npm run test:administrative-reversal` (٣٦ تأكيداً
حيّاً على Postgres وعلى النقاط الحقيقية: بيعان سليمان ⟵ العكسُ كما كان والجهازُ
الباقي مطابقٌ بايتاً · **شكلُ WB-01982 بالحرف** — تخفيضٌ يدويٌّ من بابه ثمّ
إلغاءٌ يعكس **صفراً**، ولا سطرَ «عكس كلفة» في الملخّص، ولا قيدَ معاكس، وصفُّ
التصحيح يقول صفراً، والملفُّ يقرأ متبقّياً ٧٠٠,٠٠٠ لا رصيداً · جهازٌ وحيدٌ
يُعكَس كاملاً · وعزلُ الحدّين: المريضيُّ وحده، والخيطيُّ وحده).
**ومُتحقَّقٌ بالعكس أربعَ مرّات**: إسقاطُ الأرضيّة كلِّها يُسقط **٨ بنودٍ
كلَّها من القسم الجديد** (ومنها عودةُ العطب: رصيدٌ بدل متبقٍّ) · وإسقاطُ الحدّ
الخيطيّ وحده يُسقط **٢ بالضبط** · والمريضيّ وحده **٢ بالضبط** · وإسقاطُ قيد
المريض على الوصل يُسقط **واحداً بالضبط** (ص٣٧) **وتبقى بنودُ الصحّة خضراء** —
فهو مسارُ قراءةٍ لا مجموعةُ نتائج. ولا شيءَ خارج القسم في الأربع. وستُّ حزمٍ مجاورة خضراء (`case-closure` · `case-disposal` ·
`component-sale` · `first-device-identity` · `device-episodes` ·
`exam-price-correction`)، و`tsc` ٣٥ = خطُّ الأساس،
و`check:tests` صفر، و`npm run build` ينجح.

---

## 4.an التحديثُ الحيّ — قانونٌ واحد بدل ٢٣٤ قائمة (٢٠٢٦-٠٩-٢٣)

**كلُّ كتابةٍ تنجح على الخادم ⟶ تُحدَّث كلُّ شاشةٍ مفتوحة.** بلا ترحيلٍ ولا
حرفٍ في الخادم ولا في القاعدة — والمُعدَّلُ ملفَّان: `client/src/lib/queryClient.ts`
واختبارُه.

### الواقعة (شكوى المالك)
«حين تضيف شيئاً أو تحذف أو تغيّر، **بعضُ نوافذ التطبيق لا تُظهر التعديل إلّا
بعد إعادة تحميل الصفحة** — وهذا غير صحيح ولا نظاميّ ولا يشبه التطبيقات
العالمية.»

**والسببُ بنيويّ لا عرَضيّ.** مقيسٌ على الواجهة كلِّها:

| | العدد |
|---|---|
| استعلاماتُ قراءة (`useQuery`) | ١٦٧ |
| عملياتُ كتابة (`useMutation`) | ١٠٧ في ٤٦ ملفاً |
| **قوائمُ تنظيفٍ مكتوبةٌ باليد** (`invalidateQueries`) | **٢٣٤** في ٤١ ملفاً |
| **ملفّاتٌ تكتب ولا تنظّف شيئاً إطلاقاً** | **٨** |

فمَن نسي مفتاحاً — أو كتب مفتاحاً من عائلةٍ أخرى — بقيت شاشتُه قديمة. وملفُّ
`queryClient.ts` نفسُه يسجّل العطبَ **ثلاث مرّات** بأسماء حوادثه: كلفةٌ حُفظت
ولم تظهر **فأُدخلت الخدمةُ مرّتين** (٢٠٢٦-٠٨-٠٦) · ومريضٌ محذوفٌ بقي ظاهراً
فيُضغط «حذف» ثانيةً ويُقرأ «فشل الحذف» وقد نجح · ومفتاحٌ يحمل رقمَه في جسمه
(§4.al). **وثلاثتُها أُصلحت بقائمةٍ رابعة** — والقائمةُ التالية ستُنسى.

### ولماذا عند `fetch` لا عند `useMutation`
لأنها **نقطةُ الخنق الحقيقية الوحيدة**. الواجهةُ تكتب بثلاثة أشكال: `useMutation`
(١٠٧) · و`apiRequest` مباشرةً (٤١) · و`fetch` خاماً (٧١) — وكلُّها تنتهي إلى
`fetch`. فحارسٌ عند `MutationCache` وحده يترك الشكلين الآخرين، وحارسٌ عند
`apiRequest` وحده يترك الثالث — **وما لا يُغطّى كلُّه يعود العطبُ منه**.
فالغلافُ يُركَّب مرّةً واحدة مع الوحدة (`Symbol.for` يمنع تضاعفَه في التطوير).

### وما لا يُحدِّث
القراءةُ (`GET`/`HEAD`) · وما لم ينجح (`res.ok === false`) فردٌّ بخطأ لم يغيّر
شيئاً · وما ليس من نقاط هذا التطبيق (`/api/…` على أصله) فلا نداءَ لطرفٍ ثالث
يحرّك الشاشة · **وثلاثُ نقاطٍ تُنادى بـ`POST` وهي لا تغيّر بياناتِ عمل**:
رسالةُ المساعد `/api/ai/chat` · وتلميحاتُ نموذج المصروف `/api/guidance/expense` ·
وأثرُ التصحيح قبل وقوعه `/api/admin/operation-reversal/preview`. **والقائمةُ
محروسةٌ لا مكتوبةٌ باليد** — اقرأ «تصحيحُ مراجعة Codex» أدناه.

### والتأخيرُ الصغير شرطُ صحّةٍ لا تحسين
`LIVE_REFRESH_DELAY_MS = 50`. و`MutationCache` تنادي حرّاسَها **قبل**
`onSuccess` الخاصّ بالعملية، وبعضُ العمليات **تنزع** مفاتيحَ صفٍّ لم يعد له
وجود (`removeQueries` عند حذف مريض — فصفحتُه لو أُعيد جلبُها ارتدّت ٤٠٤ وعرضت
خطأً). فلو وقع التحديثُ فوراً لسبق النزعَ. **والنافذةُ تُجمِّع كذلك دفعةَ
كتاباتٍ متتابعة في تمريرةٍ واحدة**، فلا عاصفةَ طلباتٍ من حلقةٍ تكتب مراراً.

**و`invalidateQueries()` بلا مفتاح تُعيد جلبَ الفعّال وحده** — ما هو مركَّبٌ
على الشاشة الآن؛ والمخبّأُ غيرُ المعروض يُوسَم قديماً ويُعاد جلبُه عند أوّل
عرض. فلا طلباتٍ لشاشاتٍ لا يراها أحد.

### والعودةُ إلى النافذة تُحدِّث
`refetchOnWindowFocus` كان **مطفأً**، فكانت الشاشةُ تشيخ بصمت. صار
**`"always"`**: التحديثُ الحيُّ يغطّي كتابتَك أنت في الحال، وهذا يغطّي كتابةَ
زميلك على جهازٍ آخر لحظةَ عودتك إلى التبويب. **و`staleTime` باقٍ ٦٠ ثانية**
لعمله الآخر (لا يُعاد الجلبُ مع كلّ تركيب) — والتركيزُ خارجَه. (وكان `true`
أوّلاً، وهي تُعيد جلبَ القديم وحده فيسقط الوعدُ في أشيع حالاته — أدناه.)

### ولا استطلاعَ دوريّ — قرارُ المالك
سُئل صراحةً أيريد أن يظهر عملُ زميله على شاشته وهو واقفٌ أمامها بلا أن يلمس
شيئاً، فأجاب: «إن كان يثقل النظام فبلاه». فـ`refetchInterval` يبقى `false`،
ولا ويب سوكِت — **ولا يُضاف بلا طلبٍ صريح**.

### والقوائمُ الـ٢٣٤ تبقى كما هي
لم تُحذَف ولم تُمَسّ: هي **أسرعُ وأضيق** (تعمل في الحال وعلى مفاتيحها وحدها)،
والقانونُ الجديد شبكةُ أمانٍ تحتها لا بديلٌ عنها. وحذفُها فرقٌ ضخمٌ بلا ثمرة.

### الترحيل
**بلا ترحيل** — ولا عمودَ ولا جدولَ ولا قيد، ولا حرفَ في الخادم.

### تصحيحُ مراجعة Codex — القراءةُ بـ`POST` حلقةٌ لا تنتهي، والتركيزُ لا يكفيه «القديم»

ملاحظتان على الطلب ٣٨٧، **كلتاهما ثبتت بإعادةِ إنتاجٍ حيّة قبل أيّ تعديل**،
وعولجتا في فرعٍ واحد. **وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة.**

#### ١) `POST` بوصفه `queryFn` — حلقةُ طلباتٍ لا تنتهي (P1)
الواجهةُ تحمل نقطتين تُنادَيان بـ`POST` **وهما قراءةٌ محضة**، وتُستعملان
`queryFn` لاستعلامٍ **فعّال**:

| الشاشة | النقطة |
|---|---|
| بطاقةُ تلميحات المصروف (`Accounting.tsx`) | `POST /api/guidance/expense` |
| نافذةُ التصحيح الإداريّ (`AdministrativeReversalDialog.tsx`) | `POST /api/admin/operation-reversal/preview` |

والقانونُ الجديد يقرأ `POST` كتابةً، فيجدول تحديثاً ⟵ يُبطل الاستعلامَ ⟵
يُعاد جلبُه ⟵ يُنادى `POST` ثانيةً ⟵ يجدول تحديثاً… **بلا نهاية كلَّ ٥٠ مللي
ثانية ما دامت الشاشةُ مفتوحة**. ولا يحميها `staleTime`: الإبطالُ يُوسِم
الاستعلامَ قديماً مهما كانت طزاجتُه.

**ومُعادٌ إنتاجُه حيّاً** بـ`QueryObserver` حقيقيّ على شيفرة المشروع:
**٢٤ طلباً في ١٢٠٠ مللي ثانية** لبطاقة المصروف، و**٢٠ في ١٠٠٠** لنافذة
التصحيح. وشرطُ بطاقة المصروف `!debounced.category` صادقٌ على نموذجٍ فارغ —
**ففتحُ النافذة وحدَه يُطلق الحلقة**. (وCodex سمّى الأولى؛ والثانيةُ وُجدت
بالفحص، وهي النافذةُ التي استعملها المالكُ على الإنتاج قبل يومين.)

**والعلاجُ استثناءٌ بالمسار** — وقد فُحصت النقطتان في الخادم أوّلاً: لا
`INSERT` ولا `UPDATE` ولا `DELETE` في أيٍّ منهما (`previewReversal` قراءةٌ
محضة، والكتابةُ في `execute` وحدها).

**والقائمةُ محروسةٌ لا مكتوبةٌ باليد** — وهذا جوهرُ الإصلاح، وإلّا كان الحلُّ
هو العطبَ الذي جاء §4.an ليغلقه (قائمةٌ تُنسى). **فحصٌ معماريّ** في
`test:live-refresh` يقرأ **مصادرَ الواجهة كلَّها**، ويستخرج كتلَ `queryFn`
بموازنة الأقواس (والمُحالَ إليها باسمها في الملفّ نفسِه، فلا بقعةَ عمياء)،
فأيُّ كتلةٍ تنادي بفعلِ كتابة يجب أن يكون **كلُّ** عنوانٍ فيها مستثنى — وإلّا
سقطت الحزمةُ **وسمّت الملفَّ والنقطةَ بعينهما**. فنقطةٌ جديدة لا تعود بالحلقة
صامتة. (نفسُ مبدأ حارس فهرس القدرات في §4.ak.)

#### ٢) والتركيزُ لا يكفيه «القديم» (P2)
`refetchOnWindowFocus: true` تُعيد جلبَ **ما صار قديماً وحده**، و`staleTime`
ستّون ثانية. فزميلٌ يكتب بينما تبويبُك مخفيّ ثمّ تعود **خلال دقيقة** ⟵ لا
طلبَ إطلاقاً؛ ولا حدثَ تركيزٍ ثانياً يقع بعد انقضائها وأنت واقفٌ أمام الشاشة.
**فالوعدُ كان يسقط في أشيع حالاته.** صار **`"always"`**.

**والكلفةُ محدودةٌ بفعل إنسان** — حدثُ تركيزٍ عند العودة إلى التبويب، لا
مؤقّتٌ يدقّ من تلقائه. **و`refetchInterval` يبقى `false`** بقرار المالك.

اختبارٌ دائم: `npm run test:live-refresh` (٣٤ تأكيداً على `QueryClient` حقيقيّ
وبـ`fetch` ملفوفٍ حقيقيّ، بلا قاعدة بيانات ولا شبكة: القرارُ خالصاً بعشر صور ·
**والعطبُ بعينه** — كتابةٌ تُحدِّث مفتاحاً من عائلةٍ سباعيةٍ لم تعرفها قائمةٌ
قطّ ومفتاحاً يحمل رقمَه في جسمه ومفتاحاً لا علاقةَ له بما كُتب · والقراءةُ
والفاشلةُ والمساعدُ لا تُحدِّث · وستُّ كتاباتٍ ⟵ تمريرةٌ واحدة · **والمنزوعُ
لا يُعاد جلبُه** · والافتراضاتُ وعدمُ تضاعف الغلاف وعقدُ الوصل في المصدر).
· **والحلقةُ**: استعلامان فعّالان حقيقيّان على النقطتين ⟵ **نداءٌ واحد لكلٍّ**،
ومعهما شاهدُ عدم-فراغ (نقطةٌ غيرُ مستثناةٍ بالشكل نفسِه **تدور فعلاً** —
فالاستثناءُ وحدَه هو ما أوقفها) · **والحارسُ المعماريّ** يمسح المصادر ويثبت
أن لا نقطةَ كتابةٍ داخل `queryFn` خارج الاستثناء).
**ومُتحقَّقٌ بالعكس خمسَ مرّات، كلُّ آليّةٍ على حدة**: إسقاطُ تركيب الحارس
يُسقط **٦ بنودٍ بالضبط** · وإسقاطُ تحديثِ العودة للنافذة **واحداً** · ورفعُ
استثناء تلميحات المصروف يُسقط **اثنين** (الحلقةُ تعود: ١٤ نداءً، والماسحُ
يسمّي `Accounting.tsx`) · ورفعُ استثناء معاينة التصحيح **اثنين** (ويسمّي
`AdministrativeReversalDialog.tsx`) · وردُّ التركيز إلى `true` **واحداً**.
ولا شيءَ خارج الملفّ في الخمس. وأربعون حزمةً عميلية أخرى خضراء (عدا `purchase-ui` و`add-case-ui`
بأربعةِ إخفاقاتٍ لكلٍّ — **مُثبَتةٌ متطابقةً حرفاً على `main`** بشجرة عملٍ
حقيقية، وهي إخفاقاتٌ مسبَقة)، و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر،
و`npm run build` ينجح.

### تصحيحُ مراجعة Codex الثانية — الحارسُ المعماريُّ كان له بقعةٌ عمياء

**العطبُ في الحارس نفسِه لا في القانون.** والحارسُ هو ما وُضع كي لا تعود
الحلقةُ صامتة، فبقعةٌ عمياء فيه تُعيد بالضبط ما أُغلق. **وبلا ترحيلٍ ولا حرفٍ
في الخادم ولا في القاعدة ولا في `queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار
وحده.

#### الواقعة (مُعادٌ إنتاجُها حيّاً قبل أيّ تعديل)
الماسحُ كان يقرّر «أهذا سهمٌ بجسم؟» بسؤالٍ فضفاض: **أفي جوار `queryFn` سهمٌ
`=>`؟** ثمّ يأخذ **أوّلَ `{` بعده**. فسهمٌ **بتعبيرٍ بلا أقواس** — وهو شكلٌ
قائمٌ في المستودع: `queryFn: () => fetchPatientBranchAccess(patientId)` في
`PatientBranchAccessDialog.tsx` — يجعله يقفز إلى أوّل `{` **لا علاقةَ له
بالاستعلام**، وهو هناك كائنُ التسميات الذي يليه:

```
[arrow] "{ prosthetic: \"طرف صناعي\", medical_support: \"مسند طبي\", … }"
```

وبما أن الكتلةَ غيرُ فارغة تمضي `continue` — **فلا يُحَلّ اسمُ الدالّة
أبداً**. فلو كتبت تلك الدالّةُ يوماً بـ`POST` على نقطةٍ غير مستثناة **لمرّت
من الحارس وعادت الحلقة**.

ومعها شكلٌ ثانٍ وُجد بالفحص ولم تسمّه المراجعة: **نداءُ كتابةٍ مكتوبٌ في
التعبير نفسِه** — `queryFn: () => apiRequest("POST", "/api/x").then(…)` —
يفلت للسبب عينه.

#### القاعدة الآن — «سهمٌ بجسم» يُعرَف من رأسه لا من جواره
`ARROW_HEAD` تُطابق رأسَ دالّة السهم **في أوّل القيمة** لا في جوارها، ثمّ:
- **يليه `{`** ⟶ جسمٌ بأقواس، يُقرأ كما كان بحرفه.
- **وإلّا فتعبير** ⟶ يُقرأ **التعبيرُ نفسُه** (فنداءُ كتابةٍ مكتوبٌ فيه لا
  يفلت) **وتُحَلّ أسماؤه** في الملفّ نفسِه (فما خلف الاسم لا يبقى بقعةً
  عمياء).
- **وإحالةٌ باسمٍ مجرَّد** (`queryFn: fetchUser`) كما كانت تعمل بحرفها.

و`expressionAt` تقرأ قيمةَ الخاصّية بموازنة الأقواس فتقف عند فاصلتها أو عند
قوس كائنها، **وبحدٍّ أعلى أربعمئة حرف**: انفلاتُها يجعل الماسحَ يقرأ ملفّاً
كاملاً بوصفه «استعلاماً».

#### والحارسُ يُقاس على شكلٍ يمسكه، لا على مستودعٍ نظيفٍ اليوم
بندُ «لا مخالفةَ في المستودع» **يبقى أخضرَ ولو كان الماسحُ أعمى تماماً** —
وهو ما وقع فعلاً: بالماسح القديم كان أخضرَ والبقعةُ قائمة. فصارت الأشكالُ
تُعرَض عليه صراحةً بمصدرٍ مُركَّب يحاكي ترتيبَ الملفّ الحقيقيّ (الكائنُ ذو
الأقواس **بعد** الاستعلام لا قبله، وإلّا لم يُعَد إنتاجُ العطب أصلاً).

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٤٠ تأكيداً** بعد
الإضافة، مقابل ٣٤ قبلها — والستّةُ الجديدة: سهمٌ بتعبيرٍ يُحيل إلى دالّةٍ
تكتب · وإحالةٌ باسمٍ مجرَّد · ونداءُ كتابةٍ في التعبير نفسِه · وجسمٌ بأقواس
كما كان يُمسَك · **والمستثنى لا يُبلَّغ عنه** · ولا قراءةٌ عادية).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ الماسح القديم
تُسقط **بندَي «التعبير» بالضبط** — العطبُ بعينه — **ويبقى بندُ المستودع
أخضر** فيُقرأ الماسحُ الأعمى «سليماً» · وإسقاطُ قراءةِ التعبير وحدها تُسقط
**بندَ النداء المكتوب فيه** · وإسقاطُ حلِّ الأسماء وحدَه يُسقط **بندَي
الإحالة**. ولا شيءَ خارج القسم في الثلاث. و`tsc` ٣٥ = خطُّ الأساس،
و`check:tests` صفر، و`npm run build` ينجح، وخمسُ حزمٍ عميلية مجاورة خضراء.

### تصحيحُ مراجعة Codex الثالثة — والمساعِدُ يُقرأ من مُهيّئه

**الحارسُ أُغلقت بقعتُه الثانية.** المراجعةُ السابقة أصلحت قراءةَ **قيمةِ
`queryFn`**، وبقيت **قراءةُ تعريفِ الدالّة المساعِدة** على أخذِ **أوّلِ `{`
بعد الاسم**. **وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة ولا في
`queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار وحده.

#### الواقعة (مُعادٌ إنتاجُها حيّاً قبل أيّ تعديل)
مساعِدٌ بسهمٍ **بلا أقواس** — وهو أشيعُ أشكال المساعِد القصير:

```ts
const helper = (id) => apiRequest("POST", "/api/x", { id });
```

فأوّلُ `{` بعد اسمه هو **كائنُ حمولة النداء** `{ id }` لا مُهيّئه. فالكتلةُ
المقروءة `{ id }` — **لا فعلَ كتابةٍ فيها ولا عنوان** — فيمرّ من الحارس،
ولو كانت نقطتُه قراءةً بـ`POST` لعادت الحلقةُ صامتة. مُثبَتٌ بمسبارٍ يحاكي
الماسحَ القديم: `كتلة: "{ id }"` ⟵ **لا يُمسَك**، بينما الدالّةُ المُعلَنة
بجسمٍ بأقواس تُمسَك.

#### القاعدة الآن — قاعدةٌ واحدة للطرفين
`valueBody(code, at)` تقرأ **قيمةً** أينما كانت — قيمةَ `queryFn` وتعريفَ
المساعِد سواءً — فلا تنحرف إحداهما عن الأخرى:
- **سهمٌ بجسمٍ بأقواس** ⟶ الجسم.
- **سهمٌ بتعبير** ⟶ التعبيرُ نفسُه.
- **`function`** ⟶ جسمُها **بعد قائمة معامِلاتها** — فقيمةٌ افتراضية تحمل
  `{` (`opts = { retry: false }`) لم تعد تُقرأ جسماً.
- **وإلّا** ⟶ تعبير (إحالةٌ باسمٍ مجرَّد أو نداء).

**والتعريفُ يُقرأ بعد علامة الإسناد لا عند `const`**، **ونوعٌ مكتوبٌ يحمل
`=>` لا تُقرأ علامتُه إسناداً** (`const h: (id: number) => Promise<X> = …`).
**وتُلاحَق السلسلةُ عبر التعابير**: تعبيرٌ يُحيل إلى تعبيرٍ يكتب يُمسَك،
و`seen` تمنع الدورانَ بين مساعِدَين يُحيل كلٌّ منهما للآخر.

#### وحدُّه مقيسٌ لا مُقدَّر — ولا يُتّهم بريء
جُرِّبت ملاحقةُ الأسماء **من الأجسام بأقواس أيضاً**، فأنتجت في هذا المستودع
النظيف **٩٢ اتّهاماً باطلاً في تسعة ملفّات** (`Accounting.tsx` وأخواتها):
جسمُ الاستعلام الكبير يجرّ معظمَ الوحدة فتُنسَب إليه كتاباتُ الشاشة كلُّها.
**وحارسٌ يُسقط الحزمةَ بلا مخالفةٍ يُعطَّل بعد أوّل مرّة فلا يحرس شيئاً** —
فبقيت الملاحقةُ عبر التعابير وحدها، ويحرسها بندٌ صريح.

**فحدُّه معلومٌ ومقال**: استعلامٌ بجسمٍ بأقواس ينادي مساعِداً يكتب لا يُمسَك
— **وهو حدُّه قبل هذه التمريرة أيضاً، لا انحدارٌ فيها**.

**والتغطيةُ على المستودع لم تتغيّر بحرف**: ١٣٤ كتلة · ٢ بكتابة · صفرُ
مخالفات — قبل وبعد، على المصادر نفسِها. فالمتبدِّلُ **ما يستطيع الماسحُ
إمساكَه**، لا ما يقوله عن مستودع اليوم.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٤٧ تأكيداً** بعد
الإضافة، مقابل ٤٠ قبلها — والسبعةُ الجديدة: مساعِدٌ بسهمٍ بلا أقواس ·
وبمعامِلٍ عارٍ · وبنوعٍ مكتوبٍ يحمل `=>` · وسلسلةٌ من تعبيرين · وسهمٌ بجسمٍ
بأقواس يبقى ممسوكاً · ودالّةٌ مُعلَنة بقيمةٍ افتراضية تحمل `{` · **ولا
يُتّهم بريء**).

**ومُتحقَّقٌ بالعكس أربعَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ جانب التعريف
كما كان تُسقط **٥ بالضبط** (ط١٠ · ط١١ · ط١٢ · ط١٣ · ط١٥) · وإسقاطُ الملاحقة
عبر التعابير يُسقط **ط١٣ وحدَه** · وإسقاطُ دعم النوع المكتوب **ط١٢ وحدَه** ·
وإسقاطُ القفز عن قائمة المعامِلات **ط١٥ وحدَه**. **وط١–ط٩ تبقى خضراء في
الأربع**، فلا شيءَ خارج الإصلاح يتأثّر.
و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build` ينجح،
و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.

### تصحيحُ مراجعة Codex الرابعة — ما يُمرَّر قيمةً لا يُنادى

**البقعةُ الثالثة في الحارس أُغلقت — وهذه في الاتّجاه المعاكس**: السابقتان
كانتا تُفلتان مخالفاً، وهذه **تتّهم بريئاً**. **وبلا ترحيلٍ ولا حرفٍ في
الخادم ولا في القاعدة ولا في `queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار
وحده.

#### الواقعتان (مُعادتان حيّاً قبل أيّ تعديل)
ملاحقةُ الأسماء كانت تَحُلّ **كلَّ اسمٍ يُذكَر** في تعبير الاستعلام، لا ما
يُنادى منه. فاسمٌ يُمرَّر **قيمةً** يُقرأ كتابةً يفعلها الاستعلامُ وهو لا
يفعلها:

| الشكل | ما كان يُبلَّغ عنه |
|---|---|
| `queryFn: () => ({ saveThing })` | `/api/save` |
| `queryFn: () => readThing(9, { onDone: saveThing })` | `/api/save` **و**`/api/read/` |

**والثاني كشف عيباً ثانياً لم تسمّه المراجعة**: `expressionAt` كانت تقف عند
الفاصلة وقوسِ الحاوي **ولا تقف عند الفاصلة المنقوطة**. وقيمةُ خاصّيةٍ تنتهي
بفاصلة، أمّا قيمةُ **تعريفٍ** (`const h = …;`) فتنتهي بها وحدها — فكان
المساعِدُ يبتلع الأسطرَ التالية إلى حدّ الأربعمئة **فتُنسَب إليه عناوينُ
جيرانه**. وأثرُه الأسوأ **مقيسٌ حيّاً**: استعلامٌ ينادي مستثنىً حقيقياً
(`/api/ai/chat`) بجواره دالّةُ قراءةٍ عادية ⟶ **اتّهامٌ باطلٌ بعنوان الجارة**:

```
④ مستثنى يبتلع جارَه: ["/api/reports/"]
```

**والاتّهامُ الباطلُ أخطرُ من الإفلات**: حارسٌ يُسقط الحزمةَ بلا مخالفة
يُعطَّل بعد أوّل مرّة فلا يحرس شيئاً — وهو المبدأ نفسُه الذي ردّ ملاحقةَ
الأجسام بأقواس (٩٢ اتّهاماً باطلاً).

#### القاعدة الآن
- **`calledNames`** — تُحَلّ الأسماءُ التي **تُنادى** وحدها (اسمٌ يليه `(`)،
  **وقيمةٌ هي اسمٌ مجرَّدٌ وحدَه** (`queryFn: helper`) مُنادَاةٌ **بحكم
  موضعها** — تنادِيها مكتبةُ الاستعلام — فتُحَلّ. **ولولا هذا الاستثناء لسقط
  شكلُ الإحالة باسم**، وهو مُثبَتٌ بالعكس.
- **و`expressionAt` تقف عند `;`** كما تقف عند `,`.

**وكلاهما تضييقٌ لا توسيع**: ما كان يُمسَك يبقى ممسوكاً — النداءُ المباشر
والسلسلةُ والسهمُ بلا أقواس والنوعُ المكتوب والدالّةُ المُعلَنة — **والتغطيةُ
على المستودع لم تتغيّر بحرف**: ١٣٤ كتلة · ٢ بكتابة · صفرُ مخالفات، قبل وبعد
على المصادر نفسِها.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٥١ تأكيداً** بعد
الإضافة، مقابل ٤٧ قبلها — والأربعةُ الجديدة: اسمٌ يُمرَّر قيمةً في كائن ·
ومُمرَّراً في كائن إعدادٍ داخل نداءِ قراءة · **وشاهدُ عدم الفراغ** (الفِكستشرُ
نفسُه لو نُوديَ فعلاً يُمسَك، فلا يمرّ البندُ لسببٍ خاطئ) · ونداءٌ مستثنىً لا
يبتلع عنوانَ جاره).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ الملاحقة إلى
كلّ اسمٍ تُسقط **ط١٧ وط١٨ بالضبط** · وإسقاطُ الوقوف عند `;` يُسقط **ط١٩
وحدَه** · وإسقاطُ قاعدة الاسم المجرَّد يُسقط **ط٥ وحدَه**. ولا شيءَ خارج
آليّتِه يسقط في الثلاث. و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر،
و`npm run build` ينجح، و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة
خضراء.

### تصحيحُ مراجعة Codex الخامسة — والوسيطُ يُنادِيه مُستقبِلُه

**البقعةُ الرابعة في الحارس أُغلقت — وهذه من كتابتي في الطلب ٣٩٢.** تضييقُ
الملاحقة إلى «ما يُنادى» كان صحيحاً في مبدئه وضيّقاً في تطبيقه: قصرَها على
**رأس النداء** وحدَه، **والوسيطُ المجرَّد موضعُ استدعاءٍ أيضاً** — مَن يستقبله
يناديه. **وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة ولا في
`queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار وحده.

#### الواقعة (مُعادٌ إنتاجُها حيّاً قبل أيّ تعديل)
```
queryFn: () => Promise.resolve().then(saveThing)
```
`saveThing` يكتب بـ`POST`، والوعدُ يناديه **أثناء الاستعلام**. وكانت
`calledNames` تُرجع `["resolve","then"]` وحدَهما، فلا يُحَلّ اسمُه ولا يُمسَح
فعلُ كتابته: **صفرُ مخالفات** — والحلقةُ تعود صامتة. والشكلُ العامّ معه:
`runIt(saveThing)` · `list.map(saveThing)` · كلُّ ردّ نداءٍ يُمرَّر مجرَّداً.

#### القاعدة الآن — **ثلاثةُ مواضعِ نداءٍ لا واحد**
**رأسُ النداء** (`saveThing(`) · **والوسيطُ المجرَّد** (`‎.then(saveThing)`) ·
**والقيمةُ التي هي اسمٌ وحدَه** (`queryFn: helper` — تنادِيها مكتبةُ الاستعلام
بحكم موضعها).

**والوسيطُ يُقرأ على مستوى قائمته وحدها**، فتصحيحا ٣٩١ يبقيان بحرفهما:
`() => ({ saveThing })` قيمةٌ في كائن، و`readThing(9, { onDone: saveThing })`
وسيطُه الثاني **كائنٌ لا اسم** — فكلاهما خارجَ الملاحقة كما كان، ولا يعود
الاتّهامُ الباطل.

**والقوسُ الذي لا يسبقه نداءٌ ليس قائمةَ وسائط** — وهذا شرطُ صحّةٍ **مقيسٌ لا
مُقدَّر**: `(row) =>` قائمةُ **معامِلات**، والماسحُ بلا نطاقٍ يعرفه؛ فبلا هذا
الشرط يُحَلّ اسمُ المعامِل إلى مساعِدٍ يشاركه الاسمَ في الوحدة — **فيُتَّهم
بريء** (مُثبَتٌ حيّاً بالشكل نفسِه: `‎.then((row) => row)` وفي الوحدة
`const row = () => apiRequest("POST", …)`).

**وتضييقٌ وتوسيعٌ معاً، والحدُّ مقيس**: التغطيةُ على المستودع **لم تتغيّر
بحرف** — ١٣٤ كتلة · ٢ بكتابة · **صفرُ مخالفات**، قبل وبعد على المصادر نفسِها.
فالمتبدِّلُ **ما يستطيع الماسحُ إمساكَه** لا ما يقوله عن مستودع اليوم.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٥٨ تأكيداً** بعد
الإضافة، مقابل ٥١ قبلها — والسبعةُ الجديدة: الوسيطُ في `‎.then` يُمسَك
وعنوانُه عنوانُ ما يكتبه · ووسيطٌ مُمرَّرٌ لدالّةٍ تنادِيه · **وشاهدا عدم
فراغ** (الوسيطُ نفسُه في كائنٍ يبقى خاملاً، والمساعِدُ نفسُه لو مُرِّر وسيطاً
لَأُمسِك) · ومعامِلُ سهمٍ يشارك اسمَ مساعِدٍ يكتب لا يُتَّهم به).

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ آليّةٍ على حدة**: إعادةُ قاعدة ٣٩٢ (رأسُ
النداء وحدَه) تُسقط **٤ بالضبط** (ط٢٠ · ط٢٠أ · ط٢١ · ط٢٢أ) **وتبقى ط١٧–ط١٩
خضراء** فلا ينكسر تصحيحُ ٣٩١ · وإسقاطُ شرط «القوسُ نداءٌ» يُسقط **ط٢٢ وحدَه**
**ويُظهر الاتّهامَ الباطل حيّاً**. ولا شيءَ خارج آليّتِه يسقط في الاثنين.
و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build` ينجح،
و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.

### تصحيحُ مراجعة Codex السادسة — الوسيطُ المجرَّد ليس رَدَّ نداءٍ بذاته

**البقعةُ الخامسة في الحارس أُغلقت — وهذه من كتابتي في الطلب ٣٩٣.** توسيعُ
الملاحقة إلى الوسيط المجرَّد كان صحيحاً في مبدئه وواسعاً في تطبيقه: قرأ **كلَّ**
وسيطٍ مجرَّدٍ نداءً، **ومَن يستقبله قد يخزّنه أو يقارنه أو يُرجعه ولا يناديه**.
**وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة ولا في `queryClient.ts`** —
المُعدَّلُ ملفُّ الاختبار وحده.

#### الواقعة (مُعادٌ إنتاجُها حيّاً قبل أيّ تعديل)
```
const cacheValue = (f) => { store.push(f); return 1; };
queryFn: () => cacheValue(saveThing)
```
`saveThing` يكتب بـ`POST`، **والاستعلامُ لا يكتب شيئاً** — المُستقبِلُ يخزّنه
ولا يناديه. وكانت القاعدةُ تقرؤه نداءً فتَحُلّ تعريفَه وتمسح فعلَ كتابته:
**اتّهامٌ باطل** يُسقط الحزمةَ بلا مخالفة، **وهو صنفُ العطب نفسُه الذي أصلحه
٣٩١** — ولم يفلت منه هناك إلّا لأن الاسمَ كان ملفوفاً في كائن.

مُثبَتٌ بمسبارٍ يحاكي الماسحَ قبل التغيير: `["cache.tsx ⟵ /api/__probe__"]`.

#### القاعدة الآن — **دليلُ نداءٍ لا احتمالُه**
| الحالة | السلوك |
|---|---|
| طريقةٌ تنادي **بحكم عقدها** (`‎.then` · `‎.catch` · `‎.map` · `‎.forEach` … ومؤقّتاتُ المتصفّح) | يُلاحَق |
| **تعريفٌ في الملفّ يُثبت أن معامِلَه في ذلك الموضع يُنادى** | يُلاحَق |
| طريقةٌ ليست منها · أو دالّةٌ مستورَدة لا يُقرأ تعريفُها · أو معامِلٌ لا يُنادى | **لا يُلاحَق — والصمتُ أصدقُ** |

**والمطابقةُ بموضع الوسيط لا بوجوده**: مساعِدٌ ينادي معامِلَه الثاني لا يجعل
الأوّلَ منادىً. **والقراءةُ بقاعدة «ما يُنادى» عينها**، فالمعامِلُ يُمسَك سواءٌ
نُوديَ مباشرةً (`f()`) أو مُرِّر إلى مَن يناديه (`‎.then(f)`) — و`seen` تمنع
الدورانَ بين مساعِدَين يُمرِّر كلٌّ منهما إلى الآخر.

#### ورأسُ السهم صار يوازن الأقواس
`ARROW_HEAD` كانت `\([^)]*\)` — تقف عند أوّل `)` فتبتر قائمةَ معامِلاتٍ تحمل
نوعاً بأقواس: `const runIt = (f: () => void) => f()` يُقرأ جسمُه
` Promise<…>` بدل `f()`، **فلا يُعرَف أن المعامِلَ يُنادى**. فصارت
`arrowHeadEnd` بموازنة الأقواس. **وهي شرطُ صحّةٍ للقاعدة أعلاه لا تحسين**:
بدونها يسقط الطرفُ الثاني كلُّه (مقيسٌ بالعكس).

**والتغطيةُ على المستودع لم تتغيّر بحرف**: ١٣٤ كتلة · ٢ بكتابة · **صفرُ
مخالفات**، قبل وبعد على المصادر نفسِها. فالمتبدِّلُ **ما يستطيع الماسحُ
إمساكَه** لا ما يقوله عن مستودع اليوم — وتصحيحا ٣٩١ و٣٩٢ يبقيان بحرفهما.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٦٤ تأكيداً** بعد الإضافة،
مقابل ٥٧ قبلها — والسبعةُ الجديدة: المُخزَّنُ لا يُتَّهم به · والمُستقبِلُ
المستورَد لا يُلاحَق · والموضعُ الذي لا يُنادى · ويُمسَك المُنادى مباشرةً
وبالتمرير وفي موضعه الصحيح · **وثلاثةُ شواهدِ عدم فراغ** يقابل كلٌّ منها حالةَ
سكوتٍ بحالةِ إمساكٍ بالشكل نفسِه).

**ومُتحقَّقٌ بالعكس ثلاثَ مرّات، كلُّ آليّةٍ على حدة**: رفعُ اشتراط دليل النداء
يُسقط **٣ بالضبط** (ط٢٣ · ط٢٥ · ط٢٦أ) **ويُظهر الاتّهامَ الباطل حيّاً**
(`cache.tsx ⟵ /api/__probe__`) · وإسقاطُ قراءة تعريف المُستقبِل يُسقط **٥**
(ط٢١ · ط٢٣أ · ط٢٤ · ط٢٥أ · ط٢٦) · وردُّ رأس السهم إلى القوس الأوّل يُسقط
**الخمسةَ عينها** — فهو شرطُ صحّةٍ لتلك القراءة لا آليّةٌ ثالثة مستقلّة،
**ويُقال كما هو لا يُدَّعى خلافُه**. **وط١–ط٢٢ تبقى خضراء في الثلاث**، فلا شيءَ
خارج الإصلاح يتأثّر. و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر،
و`npm run build` ينجح، و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.

### تصحيحُ مراجعة Codex السابعة — المعامِلُ الاختياريّ، والصنفُ المغلق

**بقعتان أخريان في الحارس، وكلتاهما من كتابتي في ٣٩٣ و٣٩٤**، وكلتاهما في
اتّجاه **الإفلات** لا الاتّهام. **وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في
القاعدة ولا في `queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار وحده.

#### الواقعتان (مُعادتان حيّاً قبل أيّ تعديل)
| الشكل | قبل | والمطابقُ له |
|---|---|---|
| `const runIt = (f?: () => Promise<unknown>) => { if (f) return f(); }` ثمّ `queryFn: () => runIt(saveThing)` | **يفلت** | المطابقُ له **بلا `?`** يُمسَك |
| `queryFn: () => rows.findLast(saveThing)` · و`findLastIndex` | **يفلت** | `‎.map` و`‎.find` يُمسَكان |

**① و`?` علامةُ اختيارٍ لا فاصلَ اسم.** تعبيرُ قراءة المعامِل كان يشترط أن
يلي الاسمَ `:` أو `=` أو نهايةُ النصّ، فيردّ `f?` **ويكتب فراغاً** — فلا
يُطابَق الوسيطُ في موضعه ولا يُلاحَق ما يكتبه. **والفارقُ الوحيد بين
الشكلين محرفٌ واحد**، فالملاحظةُ دقيقة.

**② ونقصانُ طريقةٍ من الصنف مخرجٌ صامت.** `findLast` و`findLastIndex`
تنادِيان وسيطَهما بحكم عقدهما كأخواتهما، **وكانتا تُمسَكان قبل ٣٩٣** (يوم
كان كلُّ وسيطٍ مجرَّدٍ يُلاحَق) — فتضييقُ ٣٩٣ أسقطهما بلا قصد.

#### القاعدة الآن — مجموعةٌ مغلقةٌ على **الصنف** لا على ما صادفنا منه
أُكملت الطرائقُ المعياريةُ كلُّها التي تنادي وسيطَها الدالّيّ: `findLast` ·
`findLastIndex` · `toSorted` · `groupBy`؛ ومعها المؤقّتان `setImmediate` و
`requestIdleCallback`. **فلا يبقى في الصنف نقصٌ يُصلَح حالةً بحالة.**

**والمُلتبِسُ متروكٌ بقرارٍ مقيس**: `Array.from(rows, fn)` تنادي معامِلَها
الثاني حقّاً، **لكنّ `‎.from(` تقع في سلاسلَ كثيرةٍ لا تنادي شيئاً** (تواريخُ
وبناةُ استعلامات)، فإدراجُها يُلاحق وسيطاً بريئاً — **والاتّهامُ الباطل
يُعطِّل الحارسَ فلا يحرس شيئاً**، بينما المخرجُ المتروك شكلٌ لا يقع (دالّةُ
كتابةٍ مسمّاةٌ مُمرَّرةً مُحوِّلاً لـ`Array.from` داخل استعلام). **ويُقال كما
هو ولا يُدَّعى خلافُه** — ويحرسه بندٌ صريح.

#### وتضييقا ٣٩٣ يبقيان بحرفهما
**المطابقةُ بالموضع**: معامِلٌ اختياريٌّ **لا يُنادى** لا يُمسَك ولو كان
الوسيطُ كاتباً. **والمُخزَّنُ لا يُتَّهم به**: مُستقبِلٌ يدفع معامِلَه في
مصفوفةٍ ولا يناديه يبقى صامتاً. **والتغطيةُ على المستودع لم تتغيّر بحرف**:
١٣٤ كتلة · ٢ بكتابة · **صفرُ مخالفات**، قبل وبعد على المصادر نفسِها.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٧٥ تأكيداً** بعد
الإضافة، مقابل ٦٤ قبلها — والعشرةُ الجديدة: المعامِلُ الاختياريُّ يُمسَك
بنوعٍ مكتوبٍ وبلاه · **وشاهدُ عدم الفراغ** (المطابقُ له بلا `?`) · والموضعُ
لا يزال يحكم · والمُخزَّنُ يبقى صامتاً · والأربعةُ الناقصةُ تُمسَك ·
والأربعَ عشرةَ الباقيةُ كما كانت · والمؤقّتاتُ الستّة · **و`Array.from`
متروكةٌ عمداً** · وطريقةٌ ليست من الصنف لا تُلاحَق).

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ إصلاحٍ على حدة**: ردُّ تعبير المعامِل كما
كان يُسقط **ط٢٧ وط٢٧أ بالضبط** (وشاهدُ الفراغ والموضعُ والمُخزَّنُ تبقى
خضراء) · وردُّ القائمتين كما كانتا يُسقط **ط٢٨ وط٢٨ب بالضبط**، **ورسالةُ
الفشل تسمّي الأربعَ** — العطبُ بعينه — وتبقى ط٢٨أ (الباقيةُ) وط٢٨ج
(المتروكة) خضراوين. ولا شيءَ خارج آليّتِه يسقط في الاثنين.
و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build` ينجح،
و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.

### تصحيحُ مراجعة Codex الثامنة — الموضعُ جزءٌ من العقد، والساكنةُ بناقلتها

**بقعتان أخريان في الحارس، وكلتاهما اتّهامٌ باطل لا إفلات** — وهو الأخطر:
حارسٌ يُسقط الحزمةَ بلا مخالفةٍ يُعطَّل فلا يحرس شيئاً. **وبلا ترحيلٍ ولا حرفٍ
في الخادم ولا في القاعدة ولا في `queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار
وحده.

#### الواقعة (مُعادةٌ حيّاً قبل أيّ تعديل، بمسبارٍ يحاكي الماسح)
`callbackArguments` كانت تقرأ **كلَّ** وسيطٍ مجرَّدٍ لطريقةٍ في المجموعة رَدَّ
نداء، **بلا نظرٍ إلى موضعه** — و**كلُّ** طريقةٍ معياريةٍ فيها موضعُ ردٍّ معلوم
وما عداه بيانات:

| الشكل | الحقيقة | قبل |
|---|---|---|
| `Object.groupBy(fns, keyFn)` | الأوّلُ مصفوفةُ بياناتٍ لا تُنفَّذ | **يُتَّهم** |
| `Map.groupBy(fns, keyFn)` | كذلك | **يُتَّهم** |
| `rows.reduce(combine, seedFns)` | الثاني **قيمةٌ ابتدائية** | **يُتَّهم** |
| `rows.map(pick, holder)` | الثاني **`thisArg`** | **يُتَّهم** |

**وCodex سمّى الأوّلَين؛ والأخيران وُجدا بالفحص** — فالعطبُ صنفٌ لا حالتان.
**ومعه خطأٌ في ٣٩٤ نفسِه**: `groupBy` أُدرجت طريقةَ مصفوفة، **ولا
`Array.prototype.groupBy` في اللغة إطلاقاً** — بل `Object.groupBy(items, cb)`
و`Map.groupBy(items, cb)`، **ساكنتان ردُّهما في الموضع الثاني**. فالفِكستشرُ
الذي كتبتُه (`rows.groupBy(saveThing)`) كان يختبر طريقةً لا وجودَ لها.

#### القاعدة الآن — الموضعُ والناقلة
`INVOKING_METHODS` صارت **خريطةَ مواضع** لا مجموعةَ أسماء: `‎.then` تنادي
**الأوّلَ والثاني**، وكلُّ طرائق المصفوفة **الأوّلَ وحدَه** (وما بعده `thisArg`
أو قيمةٌ ابتدائية)، والمؤقّتاتُ **الأوّلَ**. فيُطابَق الوسيطُ **بموضعه لا
بوجوده** — وهي القاعدةُ التي كان مسارُ `invokesParam` يطبّقها أصلاً، فصار
المساران على قاعدةٍ واحدة.

**والساكنةُ بناقلتها** (`INVOKING_STATICS`): `Object.groupBy` و`Map.groupBy` في
الموضع الثاني. **واسمُ الطريقة وحدَه ليس عقداً** — `‎.groupBy(` مجرَّدةً قد
تكون بانيَ استعلامٍ لا يَعِد بشيء، فقراءتُها عقداً تُلاحق وسيطاً بريئاً.

**وبالناقلة زال سببُ استثناء `Array.from`** (٣٩٤): استُثنيت لأن `‎.from(` تقع
في سلاسلَ لا تنادي شيئاً — ولم تعد تُقرأ إلّا بناقلتها وفي موضعها، **فدخلت**،
ومعها `JSON.parse` (المُحيي في موضعه الثاني).

**وشرطُ الدخول أن يكون الموضعُ دالّةً في توقيعه** — وهذا ما يُبقي المُلتبِسَ
خارجاً ويُقال صراحةً: `JSON.stringify` موضعُها الثاني «دالّةٌ **أو** مصفوفةُ
مفاتيح»، و`String.replace` الثاني «نصٌّ **أو** دالّة» — فتُتركان، لأن الاتّهامَ
الباطل أخطرُ من مخرجٍ لا يقع (رَدُّ استبدالٍ يكتب على الخادم).

**والتغطيةُ على المستودع لم تتغيّر بحرف**: ١٣٤ كتلة · ٢ بكتابة · **صفرُ
مخالفات**، قبل وبعد على المصادر نفسِها. فالمتبدِّلُ **ما يستطيع الماسحُ
إمساكَه** لا ما يقوله عن مستودع اليوم — وتصحيحاتُ ٣٩١ و٣٩٢ و٣٩٣ و٣٩٤ تبقى
بحرفها.

#### ⚠ وانقلب عقدُ بندٍ واحد
**ط٢٨ج** كان يُثبت أن `Array.from` **متروكة**؛ صار يُثبت أنها **دخلت بناقلتها
وموضعها**، وأن `d.from(a, cb)` على ناقلةٍ أخرى تبقى صامتة. **بقرارِ التصحيح لا
لتخضير اختبار**، وموسومٌ صراحةً في الاختبار. ومعه فِكستشرُ `groupBy` في **ط٢٨**
انتقل إلى موضعه الحقيقيّ.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٨٢ تأكيداً** بعد الإضافة،
مقابل ٧٥ قبلها — والسبعةُ الجديدة: موضعُ البيانات لا يُلاحَق في `thisArg` وفي
القيمة الابتدائية · **وشاهدُ عدم الفراغ بالتصريحات عينِها والمتبدِّلُ ترتيبُ
الوسيطين** · و`then` تنادي موضعين · والساكنةُ الثلاثُ لا تُلاحق موضعَها الأوّل ·
وتُلاحق ردَّها في الثاني بأربعة أشكال · والناقلةُ شرطٌ · والموضعُ الذي يقبل
غيرَ الدالّة متروكٌ عمداً).

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ آليّةٍ على حدة**: ردُّ الموضع إلى العمى يُسقط
**بندين بالضبط** (ط٢٩ · ط٣٠) **ورسالةُ الفشل تسمّي شكلَ Codex بعينه** · وإسقاطُ
الناقلة يُسقط **أربعةً بالضبط** (ط٢٨ج · ط٣٠ · ط٣٠أ · ط٣٠ب). **وط١–ط٢٨ب وط٢٨د
تبقى خضراء في الاثنين**، فلا شيءَ خارج الإصلاح يتأثّر.
و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build` ينجح،
و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.
### تصحيحُ مراجعة Codex التاسعة — اسمُ المُنادى لا تُقتطَع منه ناقلة

**بقعةٌ سادسة في الحارس، وهي من كتابتي في ٣٩٦** — وأثرُها **إفلاتٌ** يُعيد
الحلقةَ صامتة. **وبلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة ولا في
`queryClient.ts`** — المُعدَّلُ ملفُّ الاختبار وحده.

#### الواقعة (مُعادةٌ حيّاً قبل أيّ تعديل، بمسبارٍ يحاكي الماسح)
قراءةُ ٣٩٦ جعلت الناقلةَ مجموعةً **اختياريةً منفردة** قبل النقطة الاختيارية:
```
/([A-Za-z_$][\w$]*)?\s*(\?\.|\.)?\s*([A-Za-z_$][\w$]*)\s*$/
```
فحين لا يسبق اسمَ المُنادى فاصلٌ تبتلعه الناقلةُ إلّا حرفَه الأخير:
`queryFn:()=>setTimeout(saveThing)` تُقرأ ناقلتُها `setTimeou` ومُنادَاها `t`،
فلا يُعرَف المؤقّتُ ولا يُلاحَق ما يكتبه — **والشكلُ يكتبه كلُّ مُنسِّقٍ يضغط
المسافات**.

| الشكل | قبل ٣٩٦ (٥٣١٤c6e) | بعد ٣٩٦ |
|---|---|---|
| `queryFn:()=>setTimeout(saveThing)` | يُمسَك | **يفلت** |
| `queryFn:()=>runIt(saveThing)` (مساعِدٌ محلّيّ) | يُمسَك | **يفلت** |
| `queryFn: () => setTimeout(saveThing)` (بفراغ) | يُمسَك | يُمسَك |

**وCodex سمّى الأوّل؛ والثاني وُجد بالفحص** — فالعطبُ صنفٌ (كلُّ نداءٍ مجرَّدٍ
بلا فاصلٍ قبله) لا حالةٌ واحدة. **ومقيسٌ أنه انحدارُ ٣٩٦**: المسبارُ نفسُه على
ماسح ٥٣١٤c6e يُمسك الشكلين.

#### والاقتراحُ الحرفيُّ مرفوضٌ بالقياس لا بالرأي
اقترحت المراجعةُ أن تُربَط الناقلةُ بالنقطة في مجموعةٍ واحدة. **ونُفِّذ
وقيس**: يُصلح الشكلين، **ويكسر السلسلةَ** — ناقلةُ `rows.filter(x).map`
و`Promise.resolve().then` قوسٌ لا اسم، فتُقرأ المجموعةُ فارغةً **فتُقرأ
الطريقةُ نداءً مجرَّداً** ولا تُسأل `INVOKING_METHODS` فيفلت ردُّها. وإسقاطُه
الحيُّ **خمسةُ بنود**: ط٢٠ · ط٢٠أ · ط٢٢أ · ط٢٤ (وهي شكلُ ٣٩٢ بعينه) · وط٣٢.

#### القاعدة الآن — الاسمُ أوّلاً، والناقلةُ خطوةٌ ثانية بعد النقطة
`callee`/`isMethod` يُقرآن **بتعبير ما قبل ٣٩٦ بحرفه** (فلا يُمَسّ شيءٌ ثبت
قبله)، ثمّ تُقرأ الناقلةُ **ممّا قبل النقطة وحده**:
- **نداءٌ مجرَّد** ⟶ لا ناقلةَ تُقرأ أصلاً، فلا تُقتطَع من الاسم.
- **ناقلةٌ اسمٌ مجرَّد** (`Object.groupBy`) ⟶ تُقرأ، فتبلغ الساكنةَ موضعَها.
- **ناقلةٌ ليست اسماً** (`rows.filter(x).map`) ⟶ `undefined` **والطريقةُ تبقى
  طريقة** فتُسأل `INVOKING_METHODS`.

**والتغطيةُ على المستودع لم تتغيّر بحرف**: ١٣٤ كتلة · ٢ بكتابة · **صفرُ
مخالفات**، قبل وبعد على المصادر نفسِها.

#### وحدُّه مقالٌ لا مخبوء
`queryFn: () => { return setTimeout(saveThing); }` — **جسمٌ بأقواس** — يفلت
**قبل هذه التمريرة وبعدها سواء**: ملاحقةُ الأسماء من الأجسام بأقواس متروكةٌ
بقرارٍ مقيس منذ ٣٩٠ (٩٢ اتّهاماً باطلاً في مستودعٍ نظيف). فليس انحداراً هنا،
**ولم يُوسَّع النطاقُ لأجله**.

اختبارٌ دائمٌ مُحدَّث: `npm run test:live-refresh` (**٨٧ تأكيداً** بعد الإضافة،
مقابل ٨٢ قبلها — والخمسةُ الجديدة: ثلاثةُ نداءاتٍ مجرَّدةٍ بلا فراغ · والمساعِدُ
المحلّيُّ كذلك · **وشاهدُ عدم الفراغ** (الشكلُ عينُه بردٍّ يقرأ ولا يكتب يبقى
صامتاً، فالمُمسَكُ هو الكتابةُ لا ضيقُ المسافات) · والسلسلةُ تبقى طريقةً بأربعة
أشكال · والساكنةُ تبلغ ناقلتَها وموضعَها بلا فراغٍ أيضاً).

**ومُتحقَّقٌ بالعكس مرّتين، كلُّ قراءةٍ على حدة**: إعادةُ ناقلة ٣٩٦ تُسقط **ط٣١
وط٣١أ بالضبط** **ورسالةُ الفشل تسمّي شكلَ Codex بعينه** · واقتراحُه الحرفيُّ
يُسقط **الخمسةَ أعلاه**. **وط١–ط٣٠ج تبقى خضراء في الأولى**، فلا شيءَ خارج
الإصلاح يتأثّر. و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build`
ينجح، و`git diff --check` نظيف، وستُّ حزمٍ عميلية مجاورة خضراء.

---

## 4.ao شرائطُ لوحة التصنيع تُضغَط فتُرشِّح (٢٠٢٦-٠٩-٢٤)

**عرضٌ وترشيحٌ فقط.** بلا ترحيل · ولا حرفَ في الخادم ولا في القاعدة · ولا
صلاحيةَ تتّسع أو تضيق.

### الواقعة (شكوى المالك من شاشة هاتف)
الشرائطُ التسعة أسفلَ المرشِّحات كانت **تعدّ ولا تُضغَط**: يقرأ «متأخرون ٢١»
ثمّ لا يجد باباً يفتحهم، فيقلّب أربعمئة وستّةً وستّين أمراً بيده. وتعليقٌ في
الشيفرة يقول «click to filter» بينما العنصرُ `div` لا يستقبل ضغطة.

### القاعدة الآن
أزرارٌ تُضغَط فتُرشِّح، **والضغطةُ الثانية على الشريط نفسِه تُلغي اختيارَه**
فلا يبحث المستخدمُ عن زرِّ إلغاءٍ ثالث.

**والتصفيةُ فوق مرشِّحات الخادم لا بدلاً منها** — وهذا ما يحقّق طلبَ المالك
الثاني حرفياً («اختار عناد واختار المتأخر فيظهرون»): مرشِّحُ الخبير معامِلٌ في
الخادم، فالقائمةُ التي تصل الشاشةَ مُرشَّحةٌ به سلفاً، والتصنيفُ يعمل فوقها.
**ولا معامِلَ جديد في الخادم ولا استعلامَ ثانٍ ينحرف عن الأوّل.**

**ويصحّ ذلك لأن نقطةَ الأوامر بلا حدٍّ ولا صفحات** (`listOrders` تُرجع كلَّ
المطابق مرتَّباً، بلا `LIMIT` — مفحوصٌ حيّاً) فالقائمةُ التي بين يدي الشاشة هي
المجموعةُ الكاملة. **ولو صُفِّحت النقطةُ يوماً لوجب أن ينتقل الترشيحُ إلى
الخادم** — وإلّا صار الشريطُ يعدّ صفحةً ويدّعي لوحة.

**والعددُ هو ما يظهر بالضبط**: الشريطُ والقائمةُ من المصفوفة نفسِها بالشرط
نفسِه، فلا رقمٌ يقول شيئاً وقائمةٌ تقول غيره.

### والقرارُ دالّةٌ خالصة
`client/src/pages/manufacturing_buckets.ts` — التعريفاتُ التسعة وشروطُها
وعدُّها وترشيحُها وقاعدةُ التبديل. **ولماذا خارج المكوّن**: المشروعُ بلا مشغّل
DOM، فقرارٌ داخل React لا يُختبَر إلّا بقراءة نصِّه، وقراءةُ النصّ لا تُمسك
انقلاباً في المعنى يعود بصياغةٍ أخرى (درسُ §4.u).

**والشروطُ التسعة منقولةٌ بحرفها** من الصفحة قبل هذه التمريرة، وترتيبُها
ترتيبُها — فلا رقمٌ يتحرّك على شاشة المالك ولا شريطٌ يزيح مكانه.

**والمفتاحُ داخليٌّ ثابت** (`overdue` لا «متأخرون»): تبديلُ صياغةٍ عربية لا
يُفقد المستخدمَ تصنيفَه. **ومفتاحٌ لا نعرفه يُقرأ «بلا تصنيف» لا «صفر
نتائج»** — بائتٌ من تبويبٍ قديم يجب ألّا يُفرغ الشاشة بلا سببٍ يراه أحد.

### والشكلُ مقيسٌ بمتصفّحٍ حقيقيّ على التنسيق المبنيّ، لا موصوف
| | قبل | بعد |
|---|---|---|
| الخلفية | أبيضُ للجميع | ملوّنةٌ بمعنى الحالة (أزرق · كهرمانيّ · أحمر · أخضر) |
| الشكل | دائريّ (`9999px`) | **مستطيلٌ** (`9px`) |
| الارتفاع على الهاتف | ٣٠ بكسل | **٣٨** (ويعود مضغوطاً على الحاسوب) |
| المختار | لا علامةَ عليه | حلقةٌ ظاهرة و✕ |
| فيضٌ أفقيّ عند ٣٩٠ | ٠ | ٠ |

**وسلاسلُ الأصناف حرفيةٌ كاملة لا مركَّبة**: ماسحُ أدوات التنسيق يقرأ النصَّ،
فاسمٌ يُبنى بالدمج لا يصل الحزمةَ فيخرج الشريطُ بلا لون — **ومُثبَتٌ بقراءة
ملفّ التنسيق المبنيّ نفسِه**.

### ورسالةُ الفراغ تقول أيُّ تصنيفٍ أفرغ الشاشة
ومعها بابُ خروجٍ ظاهر — وإلّا قرأ الموظّفُ «لا توجد أوامر» وظنّ أن العمل اختفى.

### وما لم يُمَسّ
الخادمُ كلُّه · القاعدةُ والترحيلات · صفُّ الأمر ولونُ حالته (`statusTone`
**كما هو منذ أوّل نسخة**) · بطاقاتُ الأرقام الخمس · المرشِّحاتُ الستّة ·
مقارنةُ الخبراء والفروع · نقطتا `/orders` و`my-orders` · عزلُ الفرع والصلاحية.

اختبارٌ دائم: `npm run test:manufacturing-buckets` (٥٤ تأكيداً خالصاً بلا قاعدة
بيانات: الشروطُ التسعة بحرفها · والعددُ يساوي طولَ ما يظهر لكلّ تصنيف ·
**وطلبُ المالك**: متأخرو عناد وحدهم، ومتأخرو فاضل وحدهم، ومجموعُهما متأخرو
اللوحة فلا صفَّ يسقط ولا يُعدّ مرّتين · والمفتاحُ البائت والفراغُ الصريح ·
وقاعدةُ التبديل · وشهرُ بغداد يُمرَّر ولا يُقرأ من ساعة الجهاز · وعقدُ الشاشة).

**ومُتحقَّقٌ بالعكس أربعَ مرّات، كلُّ آليّةٍ على حدة**: ردُّ القائمة إلى كلّ
الأوامر يُسقط **واحداً بالضبط** · وردُّ الشريط إلى `div` يُسقط **اثنين** ·
وإسقاطُ قاعدة التبديل يُسقط **واحداً** · وجعلُ المفتاح البائت يُفرغ الشاشة
يُسقط **واحداً** — ولا شيءَ خارج آليّتِه في الأربع.

### تكملةٌ — صفُّ الأمر يقول حالتَه بلونه، ويعرض عذرَه كاملاً (٢٠٢٦-٠٩-٢٤)

**قاعدةُ المالك بحرفها**: «المتأخرون هم الذين ليس لديهم أي عذر للتأخير — أي لم
يكتب الخبير أي عذر لتأخيرهم — لذلك يكونوا **أحمر**. أما من لديه عذر: مثلاً
المريض تأخّر (بانتظار المريض) أو المواد متأخّرة **أو أي عذر** فيجب أن يكون
**أصفر**. وطبعاً المكتمل **أخضر**.» ثمّ: «**انظر كيف الأصفر يعرض السبب
كاملاً**.»

**عرضٌ فقط.** بلا ترحيل · ولا حرفَ في الخادم ولا في القاعدة · ولا صلاحيةَ
تتّسع أو تضيق.

#### والاتّهامُ رُفع بشهادة المالك نفسِه
سأل أوّلاً: «كانت تظهر مستطيل كامل أخضر أو أصفر أو أحمر لكل حالة — أين ذهب
ولماذا مسسته وأنا لم أطلب ذلك منك؟» ثمّ **تذكّر وصحّح**: «الصورة التي أرسلتها
لك مع الألوان هي من **التنبيهات** وليس من التصنيع، لذلك لا بأس أن صار التصنيع
يشبه التنبيهات — يصبح أجمل.» **وهو ما أثبته الفحصُ قبل شهادته**:
`git diff --name-only 7845164..f7ef352` لا يحمل `Manufacturing.tsx` إطلاقاً،
وآخرُ تعديلٍ عليه كان الطلبَ ٣٤٣، و`statusTone` مطابقةٌ بايتاً لأوّل نسخة
(الطلب ٩٠) إلّا إعادةَ تسميةِ حالةٍ في ٢٠٥. **فلم يُحذَف شيء** — الصفُّ الملوّن
كان في `Notifications.tsx` وحدها، وهذه التمريرةُ تنقل قاعدتَه إلى اللوحة.

#### و«العذرُ المكتوب» واقعةٌ في القاعدة لا استنتاج
`holdReasonCode` **إلزاميّ** على كلّ توقّف: `holdOrder` تشترطه للحالات الأربع
(`waiting_patient` · `waiting_materials` · `medical_hold` · `technical_rework`)،
وإعادةُ العمل الفنّي تكتبه كذلك، والإلغاءُ والتسليمُ يُصفّرانه (**والاستئنافُ لم
يعد يُصفّره** — «تكملةٌ ثالثة» أدناه). فحضورُه **هو
بعينه** «كتب الخبيرُ عذراً»، وغيابُه «لم يكتب» — **ولا يُخمَّن عذرٌ من حالةٍ
ولا من مرحلة**. **والخادمُ يرسله أصلاً** منذ ٢٠٢٦-٠٨-٣١ (`listOrders` تُرجع
`holdReasonCode` و`holdNote`)، **وكانت الشاشةُ تُسقطهما من عقدها** فلا تعرف
متأخّراً بعذرٍ من متأخّرٍ بلا عذر. فلا حرفَ في الخادم.

#### القاعدة — والمنتهي يسبق، والعذرُ يسبق التأخّر
| الحال | اللون |
|---|---|
| `completed` | **أخضر** |
| `cancelled` | رماديّ |
| عذرٌ مكتوب (متأخّراً كان أو في موعده — «أو أيّ عذر») | **أصفر** |
| متأخّرٌ بلا عذر | **أحمر** |
| يعمل في موعده بلا توقّف | محايد |

**والترتيبُ مقصود**: المنتهي أوّلاً (مكتملٌ يحمل رمزَ توقّفٍ قديم يبقى أخضر،
ولا يُعرَض عليه «سببُ توقّفه» — انتهى ظرفُه)، ثمّ **العذرُ قبل التأخّر** لأن
«متأخّرٌ بعذر» أصفرُ لا أحمر، وهو نصُّ القرار. **وشارةُ «متأخر» تتبعه**:
كهرمانيّةٌ لمن له عذر، حمراءُ لمن لا عذرَ له — نفسُ ما تفعله شاشةُ التنبيهات.

**والفراغُ ليس عذراً**: نصٌّ فارغ أو بياضٌ وحده يُقرأ «لم يكتب» فيبقى الصفُّ
أحمر — ولا عذرٌ يُلفَّق. **ورمزٌ خارج المعجم يُقال كما هو** لا يُعرَض فراغاً،
بالمعجم القانونيّ نفسِه (`REASON_CODE_LABELS`) الذي يترجم به الخادمُ في نقطة
التنبيهات — **لا معجمَ ثانٍ**.

#### والسببُ كاملاً — العنوانُ وما كتبه الخبيرُ بيده
«سببُ التأخير: تأخّر وصول مكوّن — المفصل من تركيا» في صندوقٍ كهرمانيٍّ داخل
الصفّ، **بنفس صياغة شاشة التنبيهات وصندوقها**. **ومَن توقّف في موعده يُقال
«سببُ التوقّف»** لا «سببُ التأخير» — فلا يُدَّعى تأخّرٌ لم يقع.

#### والاسمُ سطرٌ قائمٌ بذاته
كان محشوراً في سطرٍ واحد مع الشارات فينضغط («اسم المريض كان يظهر **كله** بشكل
مميّز أكثر من الآن»). **مقيسٌ بمتصفّحٍ حقيقيّ على التنسيق المبنيّ** عند ٣٩٠
بكسل: عرضُ الاسم **٢٢٠ ⟶ ٢٧٤ بكسل** ووزنُه **٦٠٠ ⟶ ٧٠٠**، وبلا اقتطاعٍ ولا
فيضٍ أفقيّ (٠ قبل وبعد).

#### والبطاقةُ الممتلئة تصل بلا علامة — لأن `cn` تُسقط `bg-card`
`Card` تمرّر صنفَها عبر `cn` (`twMerge`)، و`twMerge` **تُسقط `bg-card` حين
يصلها لونُ خلفيّةٍ آخر** — فلا يجتمع الصنفان على العنصر، ولا ترتيبَ في الحزمة
يُحسَم به شيء. وهي الصيغةُ نفسُها (`border-amber-300 bg-amber-50`) التي تلبسها
شاشةُ التنبيهات منذ نشأتها، فالشاشتان على سلسلةٍ واحدة حرفاً بحرف.
**والشرطُ الوحيد أن يمرّ الصنفُ عبر `Card`**: لو لُبس على عنصرٍ يحمل `bg-card`
بلا `cn` لخسر الكهرمانيُّ حقّاً (يسبق `card` في الحزمة) — ويحرسه بندٌ صريح.

> **⚠ تصحيحُ قياسٍ (بعد ٣٩٩، بطلب المالك «أصلحها في التنبيهات»)**: كان هذا
> القسمُ يقول إن الكهرمانيَّ «يخسر صامتةً… مقيسٌ لا مُقدَّر» وإن شاشةَ
> التنبيهات «فيها العطبُ نفسُه»، ووُضعت علامةُ `!` على خلفيّات اللوحة الأربع.
> **ولم يقع شيءٌ من ذلك**: القياسُ جرى على صفحةٍ كُتبت باليد جمعت `bg-card`
> واللونَ على عنصرٍ واحد **متخطّيةً `cn`**، فقاست ما لا تُنتجه البطاقةُ
> الحقيقيّة. وقبل أيّ تعديلٍ في التنبيهات قيست **الصفحتان الحقيقيتان** مبنيّتين
> بـVite في متصفّح: الألوانُ كلُّها ممتلئة و`bg-card` غائبٌ عن كلّ بطاقةٍ ملوّنة
> — أحمر `rgb(254,242,242)` · أصفر `rgb(255,251,235)` · أخضر
> `rgb(240,253,244)` · رماديّ `rgb(248,250,252)` · برتقاليّ `rgb(255,247,237)` ·
> أصفرُ «بعد يومين» `rgb(254,252,232)` — **والصفحةُ المكتوبة باليد تُعيد البياضَ
> في البناء نفسِه**، فهي مصدرُ الوهم. **فلم تُمَسّ `Notifications.tsx`**،
> وأُزيلت العلامةُ من اللوحة (والبناءُ لم يعد يُصدِر قاعدتَها). **وصورةُ المالك
> كانت الشاهد**: المستطيلُ الأصفرُ الكامل الذي أشار إليه كان من التنبيهات.

#### والقرارُ دالّةٌ خالصة
`client/src/pages/manufacturing_row_tone.ts: rowToneOf` — اللونُ وصنفُ البطاقة
وصنفُ الشارة والسببُ بعنوانه وملاحظته، كلُّها خرجاً واحداً. **ولماذا خارج
المكوّن**: المشروعُ بلا مشغّل DOM، فقرارٌ داخل React لا يُختبَر إلّا بقراءة
نصِّه، وقراءةُ النصّ لا تُمسك انقلاباً في المعنى يعود بصياغةٍ أخرى (درسُ §4.u)
— **وانقلابُ هذا القرار بعينه خطِر**: أمرٌ متأخّرٌ بلا عذرٍ يُعرَض أصفرَ يُقرأ
«معذور» فلا يُسأل عنه أحد.

#### وما لم يُمَسّ
الخادمُ كلُّه · القاعدةُ والترحيلات · **`Notifications.tsx` وسويتُها**
(`test:delivery-hold-tone` خضراءُ بلا تعديل) · شارةُ الحالة `statusTone`
**كما هي منذ أوّل نسخة** · الشرائطُ التسعة وقاعدةُ ترشيحها · بطاقاتُ الأرقام
الخمس · المرشِّحاتُ الستّة · نقطتا `/orders` و`my-orders` · عزلُ الفرع
والصلاحية.

اختبارٌ دائم: `npm run test:manufacturing-row-tone` (٧٧ تأكيداً خالصاً بلا
قاعدة بيانات: قرارُ المالك الثلاثيّ · «أو أيّ عذر» على الحالات الأربع متأخّرةً
وفي موعدها · المحايد · المنتهي يسبق · **السببُ كاملاً** بعنوانه من المعجم
القانونيّ وملاحظته وبادئته المختلفة للمتأخّر وللمتوقّف · الفراغُ ليس عذراً ·
المجهولُ يُقال **والخادمُ يفعل الشيءَ نفسَه** · الشارةُ · عقدُ الشاشة · **وأن
الشاشتين على سلسلةٍ واحدة حرفاً بحرف** · ونقاءُ القرار من الساعة وReact والمعجم
الثاني · **وأن البطاقةَ الحقيقيّة تلبس اللون**: `cn` الحقيقيّة على قاعدة `Card`
المقروءة من ملفّها تُسقط `bg-card` لكلّ لونٍ في الشاشتين العشرة، ومعها شاهدُ
عدم الفراغ — الجمعُ باليد يُبقي الصنفين معاً).

**ومُتحقَّقٌ بالعكس ثماني مرّات، كلُّ آليّةٍ على حدة** (أُعيدت الخمسُ الأولى على
الاختبار بعد التصحيح): تقديمُ التأخّر على العذر يُسقط **ستّةً** (عودةُ العطب:
متأخّرٌ بعذرٍ يصير أحمر) · وقراءةُ الفراغ عذراً تُسقط **ثلاثة** · وحذفُ سطر
السبب من الصفحة يُسقط **اثنين** · وإعادةُ الاسم محشوراً يُسقط **واحداً** ·
وإعادةُ الحاشية الحمراء القديمة بدل بطاقة القرار تُسقط **ثلاثة** · **وقَصرُ `cn`
على `clsx` بلا دمج يُسقط عشرةً** — كلَّ لونٍ في الشاشتين، وتبقى بنودُ القراءة
خضراء · وإعادةُ العلامة على الكهرمانيّ وحده تُسقط **أربعة** · ونقلُ لون
التنبيهات عن `Card` يُسقط **واحداً**. ولا شيءَ خارج آليّتِه في الثماني.

### تكملةٌ ثانية — «متأخرون بدون عذر» و«متأخرون بعذر»: الأحمرُ لمن لا عذرَ له وحدَه، في كلّ عدّاد (٢٠٢٦-٠٩-٢٤)

**شكوى المالك من شاشة هاتف**: «حين أضغط المتأخرون ١٨ يظهر لي متأخرون بعذر،
وهؤلاء أوضحتُ لك ألّا تحسبهم متأخرين، وإنما نقول عنهم متأخرون بعذر، فلا
يُعرَضون بالمتأخرون بدون عذر. **الأحمرُ فقط وفقط لمن متأخرٌ وليس لديه عذر**.»

**والتكملةُ الأولى صحّحت اللونَ وحده، وبقي العدُّ على التعريف القديم**
(«مضى موعدُه» وحدَه، بلا نظرٍ إلى العذر) — **في أربعة مواضع لا واحد**:

| الموضع | مصدرُه |
|---|---|
| شريطُ «متأخرون» | الشاشة (`manufacturing_buckets`) |
| مربّعُ «متأخرة» | الخادم (`getOverview` ⟵ `totals.overdue`) |
| عمودُ «متأخر» في مقارنة الخبراء | الخادم (`experts[].overdue`) |
| «متأخر» في مقارنة الفروع | الخادم (`branches[].overdue`) |

فإصلاحُ الشريط وحده كان سيُبقي المربّعَ فوقه يقول «متأخرة ١٨» بالأحمر بينما
الشريطُ تحته يقول غيرَه — والقاعدةُ عامّة: «لا تحسبهم متأخرين».

#### تعريفٌ واحد، يقرؤه الطرفان
`shared/manufacturing.ts`: `writtenHoldExcuse` (العذرُ المكتوب مقصوصاً، والبياضُ
`null`) و`latenessOf({ isOverdue, holdReasonCode })` ⟶ `late` (متأخرٌ بدون عذر) ·
`late_excused` (متأخرٌ بعذر) · `not_late`. تقرؤه **الشرائطُ ولونُ الصفّ وشارتُه
ولوحةُ الأداء في الخادم** — ولم يبقَ في `manufacturing_row_tone.ts` نسخةٌ محليّة
(`writtenExcuse` حُذفت). و`isOverdue` يبقى ما يحسبه الخادم («مضى موعدُه ولم
ينتهِ») لا يُعاد حسابُه في التعريف.

#### والانقسامُ لا يُسقط صفّاً
كلُّ رقمٍ قديمٍ صار اثنين **مجموعُهما هو بالضبط**:
- **الشريط**: الأحمرُ **«متأخرون بدون عذر»** (المفتاحُ `overdue` باقٍ) + شريطٌ
  كهرمانيٌّ جديد **«متأخرون بعذر»** (`overdue_excused`) آخرَ الشرائط.
- **المربّع**: «متأخرة بدون عذر» (أحمر) + «متأخرة بعذر» (كهرمانيّ) — ستّةُ
  مربّعات: على الهاتف ثلاثةُ صفوفٍ متوازنة (كان الخامسُ يتيماً)، وعلى الحاسوب
  صفٌّ واحد.
- **عمودا الخبراء** «متأخر بدون عذر» (أحمر) و«متأخر بعذر» (كهرمانيّ).
- **سطرُ الفرع** كذلك.
- **وشارةُ الصفّ تقول الكلمتين** — «متأخر بدون عذر» حمراء و«متأخر بعذر»
  كهرمانيّة — بكلمات المالك، من القرار (`overdueBadgeLabel`) لا مكتوبةً في
  الصفحة.

**والخادمُ**: `overdue` صار **بدون عذرٍ وحدَه**، و`overdueExcused` جديدٌ بجواره
في `totals` و`experts[]` و`branches[]`، و`getOverview` صار يقرأ
`hold_reason_code` (لم يكن يقرؤه). ووصفُ القدرة في فهرس المساعد
(`catalog.ts`) يقول الحقلين. **ولا حرفَ في القاعدة ولا ترحيل.**

**مقيسٌ في متصفّحٍ حقيقيّ على الصفحة الحقيقيّة** (مبنيّةٌ بـVite بلمسٍ مُحاكى
على ٣٩٠ بكسل، وبجلسة مسؤول): ضغطُ الأحمر يعرض الصفَّ الأحمرَ وحدَه، وضغطُ
«متأخرون بعذر» يعرض صفوفَ صورة المالك الثلاثة بسببها كاملاً، والشاراتُ بكلمتيها،
والمربّعان واللونان، **ولا فيضَ أفقيّ على ٣٩٠ و٧٦٨ و١٢٨٠**.

#### وما لم يُمَسّ
**شاشةُ التنبيهات** — تبقى تجمع الاثنين تحت «متأخرة عن موعد التسليم»
بلونين (وهي ما أشار إليه المالكُ بإعجاب)؛ **ولا تُغيَّر بلا طلبٍ صريح** · الشرائطُ
الثمانيةُ الأولى بشروطها · `listOrders` وحقلُ `isOverdue` · `holdOrder` و
`resumeOrder` · نقطةُ التنبيهات · القاعدةُ والترحيلات · عزلُ الفرع والصلاحية.

#### ⚠ وثغرةٌ سابقة وُجدت بالقياس ولم تُصلَح
عمودا **«إعادة قالب»** و**«إعادة سوكت»** في مقارنة الخبراء **فارغان دائماً**:
الشاشةُ تقرأ `e.recasts` و`e.resockets` **ولا يحسبهما `getOverview` إطلاقاً**
(يحسب `reworks` وحدَه). ظهرا فارغين في المتصفّح نفسِه. سابقٌ لهذه التمريرة،
**ولا يُصلَح بلا طلب**.

اختبارٌ دائم: `npm run test:manufacturing-overview-lateness` (٢٦ تأكيداً حيّاً
على Postgres وعلى النقطتين الحقيقيتين: الأحمرُ اثنان والكهرمانيُّ اثنان والبياضُ
ليس عذراً · **ومجموعُهما الرقمُ القديم** (أربعة) · عمودُ كلّ خبيرٍ وسطرُ الفرع ·
بقيّةُ المربّعات لم يتحرّك منها رقم · **والشريطُ = المربّعُ** من القائمة ومن
النقطتين · «الأحمرُ فقط وفقط» صفّاً صفّاً · صفُّ صورة المالك بعينه · والعزلُ ·
**والعذرُ يُكتب بـ`holdOrder` فيتحوّل الصفّ، والاستئنافُ لا يُسقطه** — انقلب في
«تكملةٌ ثالثة» أدناه)، و
`npm run test:manufacturing-buckets` (٧٢)، و`npm run test:manufacturing-row-tone` (٨٢).

**ومُتحقَّقٌ بالعكس تسعَ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ الشريط الأحمر إلى
«مضى موعدُه» تُسقط **٩ بنودٍ خالصة و٦ حيّة** — العطبُ بعينه · وعدُّ الكلّ في
مربّع الخادم **١٠ حيّة** · وفي عمود الخبراء **٢** · وفي سطر الفروع **١** ·
وشارةٌ بكلمةٍ واحدة للاثنين **٣ و٢ حيّة** · والشارةُ مكتوبةً في الصفحة **١** ·
**وقراءةُ البياض عذراً ٨ في الشرائط و٣ في اللون و١٠ حيّة** — فالتعريفُ واحدٌ
فعلاً · والخادمُ لا يقرأ العذرَ **١٣ حيّة** · والمربّعُ أو العمودُ أو سطرُ
الفروع برقمٍ واحدٍ يخلطهما **٢ لكلٍّ**. ولا شيءَ خارج آليّتِه في التسع.

#### تصحيحُ مراجعة Codex — دليلُ الشاشة الذي يقرؤه المساعدُ يقول الانقسامَ نفسَه

**الشاشةُ انقسمت والدليلُ لم ينقسم.** `pageGuideFor` يحقن `manufacturingGuide`
في نصّ النظام حين يسأل المستخدمُ من لوحة التصنيع، وكان الدليلُ بعد ٤٠١ ما زال
يصف شريطاً واحداً «متأخرون» ومربّعاً واحداً «متأخر» — فيشرح المساعدُ عدّاداً لم
يعد موجوداً، بالتعريف القديم الذي ألغاه المالك. **مُثبَتٌ على الشيفرة الحيّة
قبل أيّ تعديل.**

فصار الدليلُ (`server/ai/page_guides.ts`) يقول:
- **المتأخرُ قسمان والفارقُ عذرٌ مكتوب** — سببُ التوقّف على الأمر، لا يُستنتَج
  من حالةٍ ولا مرحلة — **والأحمرُ لمن لا عذرَ له وحدَه**، ومعنى لون الصفّ
  (أحمر · كهرمانيّ · أخضر · رماديّ) وشارتاه.
- **الشريطان** بأسمائهما، **وأن الشريطَ يُضغَط فيُرشِّح** (كان الدليلُ يصف
  الشرائطَ عدّاداتٍ فقط منذ ٣٩٨)، ومجموعُهما كلُّ المتأخّرين في القائمة.
- **المربّعان وعمودا الخبراء وسطرُ الفروع** بالتقسيم نفسِه، ولماذا قد يختلف
  رقمُ المربّع عن رقم الشريط (الملخّصُ يتبع مرشّحَ الفرع وحده).

**والحارسُ يقرأ الأسماءَ من مصادرها لا من نصٍّ يُكتب في الاختبار**: الشرائطُ
من `BUCKET_DEFS`، والمربّعاتُ وأعمدةُ الخبراء وسطرُ الفروع من نصّ
`Manufacturing.tsx`، والشارتان من `rowToneOf` نفسِها — فكلُّ اسمٍ للمتأخّرين في
الشاشة يجب أن يُذكر في الدليل بحرفه، **ولا اسمَ في الدليل ليس في الشاشة**.
فتسميةٌ تتغيّر في أحد الطرفين تُسقطه حتى يلحقها الآخر.

**وشاشةُ التنبيهات ودليلُها لم يُمَسّا** — ما زالت تجمع الاثنين بلونين، ودليلُها
يقول ذلك بصدق. **بلا ترحيلٍ ولا حرفٍ في الواجهة ولا في الخادم ولا في القاعدة**
— المُعدَّلُ نصُّ الدليل واختبارُه.

اختبارٌ دائم: القسمُ **ي.٧ب–ي.٧ز** في `npm run test:ai-page-guide` (٥٣١ تأكيداً
بعد الإضافة، مقابل ٥٢٥ قبلها). **ومُتحقَّقٌ بالعكس خمسَ مرّات**: الدليلُ القديم
يُسقط **خمسةً بالضبط** ورسالةُ الفشل تسمّي «متأخرون» — الملاحظةُ بعينها ·
وتغييرُ اسم الشريط الأحمر في الشاشة وحدها يُسقط **اثنين** · وضمُّ المربّعين
مربّعاً واحداً **ثلاثة** · وتغييرُ اسم الشارة **واحداً** · وسطرُ فروعٍ لا يطابق
أعمدةَ الخبراء **واحداً**. ولا شيءَ خارج القسم في الخمس.

#### تكملةٌ ثالثة — العذرُ المكتوب لا يضيع، ويُكتب في مكانٍ واحد (٢٠٢٦-٠٩-٢٤)

**شكوى المالك**: «أخبرني الخبير مصطفى شرف أن وهج كتب لها عذراً — تأخّر
البرلون — ولا أعلم لماذا لم يظهر». ثمّ قرارُه: «أريد أن تبقى منطقياً بحيث لا
يضيع عذرٌ مهما كان موقعُه أو كتبه الخبير، أو تُلزم الخبيرَ بمكان كتابة عذرٍ
واحد وليس في عدّة أماكن، وتجعل التطبيقَ يقرأ فقط من هذا المكان».

**والواقعُ من الشيفرة لا من التخمين**: يكتب الخبيرُ نصّاً على الأمر في ثلاثة
مواضع — «توقّف / مشكلة» (سببٌ من قائمة + ملاحظة) · «ملاحظات فنّية» عند الانتقال
للمرحلة التالية · «سبب التغيير» عند تعديل الموعد (ومثالُه المكتوب داخل الحقل
«تأخّر وصول مفصل الركبة من المورّد» — نصُّ عذرٍ بحرفه). والتطبيقُ يقرأ العذرَ
**من الأوّل وحدَه** (`holdReasonCode`)، **ثمّ كان يمحوه** `resumeOrder` («إلغاء
التوقّف ومتابعة العمل») ويمحوه التقدّمُ (`clearHold`)، فيعود أمرٌ متأخّرٌ كتب
خبيرُه سببَه أحمرَ كأن شيئاً لم يُكتب.

**ولماذا لا يُقرأ العذرُ من كلّ المواضع**: سببُ تغيير الموعد **إلزاميّ** فهو
على كلّ أمرٍ تغيّر موعدُه، والملاحظاتُ الفنّية تُكتب في كلّ مرحلة — فلو عُدّت
أعذاراً لصار كلُّ متأخّرٍ تقريباً أصفر وفقد الأحمرُ معناه. ولا يفرّق التطبيقُ
بين «تم أخذ القياس» و«تأخّر البرلون». **فالخيارُ الثاني**: مكانٌ واحد يُقرأ منه.

**القاعدةُ الآن**:
- **العذرُ يبقى** بعد الاستئناف وبعد التقدّم وبعد تغيير الموعد، ولا يزول إلّا
  بالتسليم أو إنجاز الصيانة أو الإلغاء، أو بعذرٍ أحدث من المكان نفسِه يحلّ محلّه
  (والقديمُ باقٍ في الخطّ الزمني). `resumeOrder` لا يمسّ العمودين، و`clearHold`
  حُذف من `updateStage` ومن نقطة التقدّم، **والإكمالُ يُصفّرهما** (التسليمُ كما
  كان، وإنجازُ الصيانة صار يصرّح بذلك بعد أن كان يرثه من `clearHold`).
- **و«أهو متوقّفٌ الآن؟» تقوله `status` وحدها** — حضورُ العذر لم يعد يعنيه.
  فالأصفرُ في `rowToneOf` صار لعذرٍ **يعمل الآن**: متوقّفٌ (`isHoldStatus`) أو
  متأخّر؛ وعاملٌ في موعده بعذرٍ باقٍ **بلا لون ولا سبب** (عذرٌ لا يعذر شيئاً
  الآن)، ويعود أصفرَ بعذره نفسِه إن تأخّر. و`latenessOf` لم يتغيّر بحرف.
- **والمكانُ الواحد تقوله الصفحة** (`orderLatenessNotice`، قرارٌ خالص): في صفحة
  كلّ أمرٍ عاملٍ متأخّر تنبيهٌ — «متأخر بعذر» بسببه وأنه باقٍ، أو «متأخر بدون
  عذر» يدلّ على «توقّف / مشكلة» بوصفه **المكانَ الوحيد**. وبجانب «ملاحظات فنّية»
  و«سبب التغيير» جملةٌ أنهما ليسا عذراً، ونافذةُ «توقّف / مشكلة» تقول إنها وحدَها
  مكانُه وإنه يبقى. **ولا تنبيهَ على متوقّفٍ بسببٍ مكتوب** (بطاقتُه تعرض سببَه
  أصلاً) — والمتوقّفُ **بلا** سببٍ مكتوب له تنبيهُه، في التصحيح أدناه.
- و`isOrderOverdue` **تعريفٌ واحد** في المخزن تقرؤه القائمةُ واللوحةُ وصفحةُ
  الأمر (`getOrderDetail` صار يرسل `isOverdue`) — لا حسابٌ ثالث في المتصفّح.

**وما مُحي قبل النشر لم يُسترجَع**: ينتظر قرارَ المالك — يعيد الخبيرُ كتابتَه
مرّةً، أو يُسترجَع آليّاً من سطر «توقّف» الأخير في الخطّ الزمني. **ولا يُسترجَع
بلا طلبٍ صريح**: كتابةٌ على صفوف الإنتاج بأثرٍ رجعيّ. **(٢٠٢٦-٠٩-٢٥) — طلبه المالكُ،
والسكربتان جاهزان: «تكملةٌ رابعة» آخرَ القسم.**

**وما لم يُمَسّ**: القاعدةُ والترحيلات · `latenessOf` · شاشةُ التنبيهات (تلوّن
المتأخّرَ بسببه أصلاً، فصار الاستئنافُ لا يقلبه أحمر) · نقطةُ التوقّف وأسبابُها ·
تغييرُ الموعد وسجلُّه · الإلغاءُ.

**⚠ وانقلب عقدُ أربعة بنودٍ قائمة بقرار المالك** (لا لتخضير اختبار): في
`test:workflow` «والسبب أُفرغ» بعد الاستئناف (ثلاثُ صور) وبعد التقدّم ⟵ صار
«باقٍ»؛ وفي `test:manufacturing-overview-lateness` القسمُ د (كان يثبت أن
الاستئنافَ يقلب الصفَّ أحمر) ⟵ صار يثبت بقاءَه كهرمانيّاً. **وفِكستشران** في
`test:manufacturing-row-tone` (هـ٨ · ز) كانا على «عاملٍ بعذرٍ في موعده» — حالٌ
لم تكن ممكنة وصارت شكلَ ما بعد الاستئناف — فنُقلا إلى حالة توقّفٍ حقيقية.

اختبارٌ دائم: `npm run test:manufacturing-excuse` (٣٥ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية: شكلُ وهج — توقّفٌ ثمّ استئنافٌ فيبقى العذرُ بحرفه، والقائمةُ
واللوحةُ والتنبيهاتُ وصفحةُ الأمر تقوله كهرمانيّاً · التقدّمُ لا يمحوه · تغييرُ
الموعد لا يمحوه، وفي الموعد بلا لون، ويعود بعذره إن تأخّر · الأحدثُ يحلّ محلّه
والاثنان في الخطّ الزمني · ينتهي بالتسليم وبإنجاز الصيانة وبالإلغاء · ومَن لم
يُكتب له شيءٌ أحمرُ يُدَلّ على المكان الواحد، **وملاحظةٌ فنّية لا تصير عذراً** ·
وإعادةُ العمل الفنّي كذلك)، و`test:manufacturing-row-tone` (١١٠ — القسمان س وع).

**ومُتحقَّقٌ بالعكس ستَّ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ محو الاستئناف —
عطبُ وهج بعينه — تُسقط **١٥ حيّة و٣ في `workflow` و٤ في اللوحة** · وإعادةُ محو
التقدّم **٣ و١** · وإبقاءُ العذر بعد إنجاز الصيانة **١** · و«كلُّ عذرٍ أصفر» **٢
خالصة و١ حيّة** · وحذفُ `isOverdue` من صفحة الأمر **٣** · وحذفُ التنبيه من
الصفحة **١**. ولا شيءَ خارج آليّتِه في الستّ.

#### تصحيحُ مراجعة Codex — المتوقّفُ بلا سببٍ مكتوب ليس معذوراً، وصفحتُه لا تصمت

**ملاحظةٌ واحدة على ٤٠٣، ثبتت على الشيفرة الحيّة وأُعيد إنتاجُها قبل أيّ تعديل.**
**بلا ترحيلٍ ولا حرفٍ في الخادم ولا في القاعدة** — المُعدَّلُ القرارُ الخالص
(`manufacturing_row_tone.ts`) وصفحةُ الأمر ودليلُها واختباراتُها.

**الواقعة**: ترحيلا ٠٤٥ و٠٤٦ حوّلا `waiting_components` ⟶ `waiting_materials`
و`needs_recast`/`needs_resocket` ⟶ `technical_rework` **ولم يكتبا
`hold_reason_code`** (العمودُ نفسُه وُلد في ٠٤٥). فأمرٌ متوقّفٌ موروثٌ قد لا يحمل
عذراً مكتوباً — واللوحةُ تعدّه «متأخر بدون عذر» بالأحمر (`latenessOf` لا يقرأ
الحالة)، **بينما صفحتُه صامتةٌ من ثلاث جهات**: `orderLatenessNotice` تُسقط
التنبيهَ لأنه «متوقّف» · وبطاقةُ التوقّف مشروطةٌ بالسبب فتُخفى · و«توقّف /
مشكلة» مشروطٌ بـ`!onHold` فيُخفى — فلا يبقى إلّا «إلغاء التوقّف». **فالمكانُ
الواحد للعذر لا يُبلَغ من هذه الصفحة أصلاً.** مُعادٌ حيّاً: البندان ط٣ وط٥
يسقطان على الشيفرة قبل الإصلاح (التنبيهُ `null`)، والخادمُ يقبل كتابةَ السبب
على الأمر المتوقّف نفسِه أصلاً (`holdOrder` لا يردّ المتوقّف) — فالعطبُ في
الواجهة وحدها.

**القاعدةُ الآن**:
- **`heldExcuseOf`** — سببٌ مكتوبٌ على أمرٍ متوقّف، أو `null` — **شرطٌ واحد**
  للبطاقة وللتنبيه: البطاقةُ تقوله ولا تنبيهَ يكرّره؛ وإلّا فالتنبيه. **فلكلّ
  متوقّفٍ متأخّر أحدُهما بالضبط** — لا يجتمعان ولا يغيبان معاً (مُثبَتٌ بالحالات
  الأربع × خمسة أشكالٍ للسبب: غيابٌ · فراغٌ · بياضٌ · رمزٌ معروف · رمزٌ مجهول).
- **وتنبيهُ المتوقّف بلا سبب يقول لماذا** (`EXCUSE_PLACE_HINT_RED_HELD`): «متوقّفٌ
  بلا سببٍ مكتوب، فيُحسب متأخراً بدون عذر» ويدلّ على «توقّف / مشكلة» — **وجملةُ
  العامل بلا عذر كما هي بحرفها**.
- **`holdButtonShown`** — «توقّف / مشكلة» لكلّ أمرٍ حيٍّ **إلّا** متوقّفاً كُتب
  سببُ توقّفه (ذاك يُستأنَف ثمّ يُكتب الأحدث، كما كان). فالمتوقّفُ بلا سببٍ
  مكتوب يرى الزرَّ بجانب «إلغاء التوقّف» ويكتب سببَه وهو متوقّف.
- **والصفحةُ تقرأ القرارين ولا تعيد كتابتَهما**: البطاقةُ بـ`heldExcuse` لا
  بـ`order.holdReasonCode` خاماً، والزرُّ بـ`holdButtonShown(holdShape)` لا
  بـ`!onHold`.
- **والدليلُ الذي يقرؤه المساعد يقول الحالَ نفسَه**، وحارسُه يقيس الجملةَ
  **على القرارين الخالصين** لا على وصفٍ مكتوب.

**وما لم يُمَسّ**: الخادمُ كلُّه (`holdOrder` وحُرّاسُه · `latenessOf` ·
`isOrderOverdue` · اللوحةُ والقائمةُ والتنبيهات) · القاعدةُ والترحيلات ·
**ولا يُعبَّأ سببٌ للأوامر الموروثة بأثرٍ رجعيّ** — لا عذرَ يُخترَع لأمرٍ لم يكتب
خبيرُه سببَه. · نافذةُ «توقّف / مشكلة» بحرفها — **⚠ وكان هذا عطباً لا حدّاً
مقبولاً**: كُتب هنا أن إعادةَ العمل فيها تسأل عن مرحلةٍ سابقة «فيختار الخبيرُ النوعَ
الذي يصف تأخّرَه»، والنوعُ الذي يصفه على أمرٍ موروثٍ في `technical_rework` هو «إعادة
عمل فني» بعينه — فيُرجَع مرّةً ثانية. صُحِّح في التصحيح التالي.

اختبارٌ دائم: القسمُ **ط** في `npm run test:manufacturing-excuse` (٤٨ تأكيداً
بعد الإضافة، مقابل ٣٥ قبلها — الشكلُ الموروث بعينه كما تتركه الترحيلتان ·
اللوحةُ تعدّه أحمر · **وصفحتُه تقوله بتنبيهٍ أحمر يقول لماذا** · ولا بطاقةَ ·
**والزرُّ ظاهر** · وإعادةُ العمل الموروثة كذلك · ثمّ يُكتب السببُ على المتوقّف
نفسِه فتقوله البطاقةُ ولا تنبيهَ ثانٍ ويختفي الزرّ ويصير الصفُّ كهرمانيّاً
واللوحةُ تنقله)، والقسمُ **ف** في `npm run test:manufacturing-row-tone` (١٣٠)،
و**ي.١٩أ–ي.١٩ب** في `npm run test:ai-page-guide` (٥٣٢).

**ومُتحقَّقٌ بالعكس ستَّ مرّات، كلُّ آليّةٍ على حدة**: إعادةُ الإسقاط على كلّ
متوقّف — العطبُ بعينه — تُسقط **٦ خالصة وط٣ وط٥ حيّةً** · وإعادةُ الزرّ إلى
`!onHold` في القرار تُسقط **ف١١ وط٤أ وط٥** · وجملةُ العامل على المتوقّف **٤
خالصة وط٣** · والبطاقةُ بـ`order.holdReasonCode` خاماً في الصفحة **ف١٥** ·
والزرُّ بـ`!onHold` في الصفحة **ف١٦** · وحذفُ فقرة الدليل **ي.١٩أ وي.١٩ب**.
ولا شيءَ خارج آليّتِه في الستّ. وثلاثَ عشرةَ حزمةً مجاورة خضراءُ كلُّها، و`tsc`
٣٥ = خطُّ الأساس، و`check:tests` صفر، و`npm run build` ينجح.

#### تصحيحُ مراجعة Codex على ٤٠٤ — سببُ التوقّف القائم يُكتب، ولا يُرجِع الأمرَ بمرحلة

**ملاحظةٌ واحدة (P1) على ٤٠٤، ثبتت ومُعادةٌ حيّاً قبل أيّ تعديل — وهي من كتابتي
في ٤٠٤**: سطرُ «وما لم يُمَسّ» أعلاه وصفها حدّاً مقبولاً، وهي عطب. **بلا ترحيلٍ
ولا عمودٍ ولا قيد.**

**الواقعة**: ٤٠٤ أظهر «توقّف / مشكلة» للمتوقّف الموروث بلا سبب، لكنّ نافذتَه نافذةُ
**توقّفٍ جديد**: «إعادة عمل فني» فيها لا تُكتب إلّا برجوعٍ إلى مرحلةٍ سابقة، و`/hold`
ينادي عندها `reworkToStage`. وأمرٌ موروثٌ في `technical_rework` (كان `needs_recast`/
`needs_resocket` — **رجع فعلاً في النظام القديم**، وصفُّ إعادة عمله باقٍ كما تركه
٠٤٥) نوعُه الذي يصفه هو «إعادة عمل فني» بعينه. مُعادٌ على النقاط الحقيقية:

| الأمرُ الموروث | ما تُتيحه النافذة | الأثر |
|---|---|---|
| في «التصنيع» | الرجوعُ إلى القياسات أو القالب إلزاميّ | **رجع مرّةً ثانية** · **صفُّ إعادة عملٍ ثانٍ** · **حدثُ مرحلةٍ يصل المريض** |
| في «القياسات» | لا مرحلةَ سابقة | **لا يُكتب السببُ أصلاً** (٤٠٠) |
| صيانةٌ في أوّل مراحلها | لا مرحلةَ سابقة | كذلك |

**القاعدةُ الآن — لسبب التوقّف القائم بابُه، والنوعُ نوعُه والمرحلةُ مرحلتُه**:
- **`POST /api/manufacturing/orders/:id/hold-reason`** ⟵ `documentHoldReason` في
  المخزن. **تحت القفل** يُشترط أن يكون الأمرُ ما زال متوقّفاً **بالنوع الذي رآه
  الخبير** ولا سببَ مكتوباً عليه — وإلّا ٤٠٩، ولا يُكتب فوق سببٍ كتبه غيرُه. ويكتب
  السببَ والملاحظةَ **وحدهما**: لا الحالةَ ولا المرحلةَ ولا صفوفَ إعادة العمل، ولا
  حدثَ للمريض. وسطرٌ واحد في الخطّ الزمني بنوعه الخاصّ **`hold_reason`** — «كتابة
  سبب التوقّف» في صفحة الأمر وفي المراجعة اليومية — لا «تغيير حالة» لحالةٍ لم
  تتغيّر. و`LockedOrder` اكتسب `holdReasonCode` ليُحكَم تحت القفل.
- **والنقطةُ تردّ مبكّراً برسالةٍ تقول ما جرى**: عاملٌ ⟵ ٤٠٩ «لا توقّفَ قائم» ·
  سببٌ مكتوبٌ سلفاً ⟵ ٤٠٩ تدلّ على «إلغاء التوقّف» ثمّ «توقّف / مشكلة». وخطأٌ غيرُ
  متوقَّع ⟵ ٥٠٠ «لم يتغيّر شيء» — لا رميٌ من معالجٍ غير متزامن (درسُ §4.ag).
- **`holdDialogKind`** (القرارُ الخالص) يختار النافذةَ **لحظةَ الضغط**: متوقّفٌ بلا
  سببٍ مكتوب ⟵ **نافذةُ سبب التوقّف القائم** (`HoldReasonDialog`: النوعُ مقروءٌ لا
  يُختار، ولا سؤالَ عن مرحلة، والسببُ من قائمة نوعه) · عاملٌ ⟵ نافذةُ التوقّف الجديد
  **كما هي بحرفها**. والنوعُ يُلتقَط عند الضغط، فلا تتبدّل النافذةُ تحت يد الخبير إن
  حُدِّث الأمرُ وهي مفتوحة. **ولتغيير نوع التوقّف**: يُستأنَف ثمّ «توقّف / مشكلة» —
  القاعدةُ نفسُها للمتوقّف بسببه.

**ولماذا لم يُقيَّد `/hold` في الخادم**: إعادةُ العمل على أمرٍ متوقّفٍ أصلاً
**مشروعةٌ ومُختبَرة** — «إعادةُ عملٍ ثانية ترجع أبعد» (السيناريو ٢٠ في
`test:manufacturing`). العطبُ أن النافذةَ لم تعرض غيرَها لمن أراد كتابةَ السبب، لا
أن الخادمَ يسمح بها. فالإصلاحُ بابٌ جديد وخيارُ نافذة، و`/hold` و`holdOrder` و
`reworkToStage` **بحرفها**. (وتبويبٌ فُتح قبل النشر يحمل النافذةَ القديمة حتى
يُحدَّث — نافذةٌ قصيرة: قالبُ التطبيق يُجلَب من الشبكة أوّلاً عند كلّ فتح.)

**مقيسٌ في متصفّحٍ حقيقيّ على الصفحة الحقيقيّة** — الخادمُ الحقيقيّ في وضع التطوير
على قاعدةٍ محلّية، ودخولُ خبيرٍ عبر نقطة الدخول نفسِها، ولمسٌ على ٣٩٠ بكسل: الأمرُ
الموروث يعرض التنبيهَ الأحمر · والضغطُ يفتح «هذا الأمر متوقّفٌ أصلاً: إعادة عمل فني
— اكتب سبب توقّفه» **بلا منتقي نوعٍ ولا منتقي مرحلة**، والحفظُ معطّلٌ حتى يُختار
السبب · والكتابةُ الوحيدة `POST …/hold-reason` · ثمّ تقول بطاقةُ «متوقّف» سببَه،
ويزول التنبيهُ والزرّ، ويظهر سطرُ «كتابة سبب التوقّف»، ولا فيضَ أفقيّ. والقاعدةُ
بعدها: المرحلةُ «التصنيع» كما هي · صفُّ إعادة عملٍ واحد · صفرُ أحداثِ مريض.

**وما لم يُمَسّ**: `/hold` و`holdOrder` و`reworkToStage` وإعادةُ العمل الثانية ·
`latenessOf` و`isOrderOverdue` واللوحةُ والتنبيهات · القاعدةُ والترحيلات · **ولا
يُعبَّأ سببٌ للأوامر الموروثة بأثرٍ رجعيّ** — الخبيرُ يكتبه من البابِ الجديد.

اختبارٌ دائم: القسمُ **ي** في `npm run test:manufacturing-excuse` (٧٥ تأكيداً بعد
الإضافة، مقابل ٤٨ قبلها — شكلُ Codex بعينه بصفّ إعادة العمل القديم · النوعُ
والمرحلةُ كما هما · لا صفَّ ثانٍ ولا حدثَ للمريض · سطرٌ واحدٌ بنوعه · «القياسات»
والصيانةُ وقد كانا مستحيلين · والمردودُ بلا كتابة: سببٌ مكتوب · عامل · نوعٌ بائت ·
سببٌ لا يخصّ النوع · منتهٍ · خبيرٌ غيرُ مسنَد · **ولقطةٌ بائتة يردّها القفلُ نفسُه**
بنداء المخزن مباشرةً)، والقسمُ **ص** في `npm run test:manufacturing-row-tone`
(١٤٧)، و**ي.١٩ج** في `npm run test:ai-page-guide` (٥٣٣).

**ومُتحقَّقٌ بالعكس تسعَ مرّات، كلُّ آليّةٍ على حدة**: رفعُ حارس القفل يُسقط ما يتبع
الكتابةَ غيرَ المحروسة وحدَه (ي١٧ · ي١٨أ · ي٢٢ · ي٢٥) · وإعادةُ الأثر القديم (رجوعٌ
وصفُّ إعادة عمل) تُسقط **ي٥ وي٦** ومعهما ي١٣أ وي١٣ب وي٢٣ (تقيس المرحلةَ والصفوف) ·
و«نافذةُ التوقّف الجديد دائماً» تُسقط **ص٣ وص٤ وي١ وي.١٩ج** · والزرُّ إلى نافذة
التوقّف الجديد **ص٩** · ونافذةُ السبب إلى `/hold` **ص١٢** · وحذفُ الفحوص المبكّرة
**ي١٥** · وتسميةُ المراجعة اليومية **ي١٠** · وتسميةُ الخطّ الزمني **ص١٧** · وفقرةُ
الدليل **ي.١٩ج**. وثماني عشرةَ حزمةً مجاورة خضراءُ كلُّها (منها `test:manufacturing`
بإعادة العمل الثانية، و`test:ai-capabilities` بالنقطة الجديدة)، و`tsc` ٣٥ = خطُّ
الأساس، و`check:tests` نجح، و`npm run build` ينجح.

#### تصحيحُ مراجعة Codex على ٤٠٥ — إذنُ الخبير هو الإسنادُ نفسُه، ويُحكَم عليه تحت القفل

**ملاحظةٌ واحدة (P2) على ٤٠٥، ثبتت ومُعادةٌ حيّاً قبل أيّ تعديل — وهي صنفٌ لا حالة.**
**بلا ترحيلٍ ولا حرفٍ في الواجهة ولا في القاعدة.**

**الواقعة**: `loadWritable` تأذن بالكتابة لثلاثة: المسؤولِ، ومديرِ الفرع في نطاقه،
والخبيرِ المسنَد. والثالثُ إذنُه **هو الإسنادُ نفسُه** (`raw.expertUserId ===
s.userId`) — ويُقرأ **قبل** القفل، ولم يكن كاتبٌ واحد يُعيد فحصَه على الصفّ المقفول.
فمديرٌ يُعيد إسنادَ الأمر إلى خبيرٍ آخر بين فحص الطلب وقفله ⟵ طلبُ الأوّل يأخذ القفلَ
**ويكتب بعد أن فقد الأمر**. سمّت المراجعةُ `documentHoldReason` وحدَه، **ومُعادٌ
حتميّاً على `main` في الأبواب الستّة المتاحة للخبير** — كلُّها ردّت ٢٠٠ وكتبت بيد مَن
لم يعد صاحبَ الأمر:

| الباب | ما كتبه الخبيرُ القديم |
|---|---|
| كتابةُ سبب التوقّف | السببُ وسطرٌ في الخطّ الزمني |
| التوقّف | أوقفه بسببه وملاحظته |
| إعادةُ العمل الفنّي | أرجعه من «التصنيع» إلى «القالب» |
| الاستئناف | أعاده إلى العمل |
| الانتقال للمرحلة التالية | نقله والتزم بموعد تسليم |
| تغييرُ الموعد | حرّك الوعدَ الذي تُقاس عليه دقّةُ التسليم |

**القاعدةُ الآن — أساسُ الإذن يُحمَل إلى الكاتب، ويُحكَم عليه تحت القفل**:
- **`WriteAuthority`** في المخزن: `{ via: "role" }` للمسؤول والمدير، و
  `{ via: "assignment", expertUserId }` للخبير. تبنيه **`loadWritable` وحدها** وتُرجعه
  مع الأمر، ويُمرَّر **مطلوباً لا اختيارياً** إلى الكتّاب السبعة: `updateStage` ·
  `updateDeliveryDate` · `holdOrder` · `documentHoldReason` · `resumeOrder` ·
  `reworkToStage` · `cancelOrder`. فكاتبٌ بلا إذنٍ لا يُترجَم (`tsc` و`check:tests`).
- **`assertWriteAuthority(live, params.authority)` أوّلَ ما بعد القفل** في كلّ واحد —
  قبل حارس المنتهي وحارس الانحراف: مَن لم يعد مأذوناً يُقال له ذلك، لا تفاصيلُ حالٍ لم
  يعد له.
- **والفحصُ على الإسناد الحاليّ لا على «أتغيّر؟»**: أمرٌ أُسنِد إلى غيره ثمّ أُعيد إليه
  قبل القفل هو أمرُه لحظةَ الكتابة — فيكتب.
- **`WorkOrderReassignedError` صنفٌ فرعيٌّ من `WorkOrderConflictError`** عمداً: كلُّ
  نقطةٍ تلتقط التعارضَ تلتقطه فلا يبقى طلبٌ بلا ردّ (Express 4)، و`handledConflict`
  يقرؤه **قبل** أبيه فيردّ **٤٠٣** «أُسنِد هذا الأمرُ إلى خبيرٍ آخر أثناء طلبك — لم
  يُكتب شيء.» — لا «حدّث وأعد المحاولة» لمحاولةٍ مردودةٍ أصلاً. والواجهةُ تعرض نصَّ
  الخادم وتُعيد الجلب أصلاً، فلا حرفَ فيها.
- **والمديرُ والمسؤولُ لا يُردّان لأن الخبيرَ تغيّر**: سلطتُهما دورُهما ونطاقُهما. والبديلُ
  الأبسط — ردُّ كلّ كاتبٍ تغيّر خبيرُ أمره منذ اللقطة — كان يُضيّق عليهما بلا سبب،
  **ومُثبَتٌ أن الاختبارَ يُمسكه** (المراجعةُ العكسية أدناه).

**ولماذا لا غير هذه**: إعادةُ الإسناد نفسُها (`reassignExpertTx`) تُطابق الخبيرَ المتوقَّع
تحت القفل أصلاً، ولا يُعيد الخبيرُ إسنادَ شيء · والإبطالُ الإداريّ
(`voidOrderAdministratively`) إذنُه نطاقُ المسؤول لا الإسناد · والقراءةُ
(`GET …/orders/:id`) لا تكتب شيئاً. **وكلُّ كتابةٍ يُؤذَن فيها بالإسناد تمرّ بـ
`loadWritable`** (مفحوصٌ في الملفّ كلِّه).

**وما لم يُمَسّ**: القاعدةُ والترحيلات · الواجهةُ · مَن يُؤذَن له في `loadWritable`
بحرفه · حُرّاسُ المنتهي والانحراف والموعد · إعادةُ الإسناد والإبطالُ الإداريّ.

اختبارٌ دائم: `npm run test:manufacturing-write-authority` (٤٠ تأكيداً حيّاً على Postgres
وعلى النقاط الحقيقية — **والسباقُ حتميٌّ لا احتمال**: بوّابةٌ على العميل المُستعار من
المجمَّع تحجز أوّلَ قفلٍ لصفّ أمر العمل، فتلتزم إعادةُ الإسناد كاملةً ثمّ تُحرَّر؛ وهي
في ملفّ الاختبار وحده. **الأبوابُ الستّة**: السباقُ وقع · ٤٠٣ برسالته · **وصفرُ كتابة
ببصمةٍ كاملة** (الصفُّ وخطُّه الزمني وإعادةُ العمل وأحداثُ المريض والتدقيق) · والخبيرُ
الجديد يكتب والقديمُ يُردّ عند الباب · **والمديرُ لا يُردّ** توقّفاً وإلغاءً · والإسنادُ
المُعاد قبل القفل يكتب · **وعقدُ المخزن للكتّاب السبعة**: بإذن الخبير القديم يُردّ بلا
كتابة، **وبإذن صاحبه يمضي** — شاهدُ أن الحالَ صالحةٌ والردَّ للإذن وحده · وحارسان
معماريّان: كلُّ قفلٍ لأمرٍ يتلوه فحصُ الإذن، ولا إذنَ يُبنى خارج `loadWritable`). ومعه
**ستّةَ عشرَ نداءً مباشراً** في خمسة ملفّات اختبار صارت تمرّر إذنَها — `role` حيث
المديرُ أو لا أحد، و`assignment` حيث الخبيرُ المسنَد.

**ومُتحقَّقٌ بالعكس ستَّ مرّات، كلُّ آليّةٍ على حدة**: إبطالُ الفحص تحت القفل يُسقط
**٢٦** — السباقاتُ الستّة (٤٠٣ وصفرُ الكتابة) وعقدُ المخزن، ومعها ب١ وبنودُ «هـNأ»
أثراً لا عطباً مستقلّاً (كتب القديمُ أوّلاً فتغيّر الحالُ أمام الثاني) · وتمريرُ `role`
للخبير من `loadWritable` يُسقط **السباقاتِ وحدها** وعقدُ المخزن أخضر — فالنصفان لازمان
كلٌّ بذاته · وإسقاطُ ٤٠٣ الخاصّة يُسقط **ستّةً** (الرسالةُ وحدها، وصفرُ الكتابة باقٍ) ·
ونزعُ الفحص من `holdOrder` وحدَه يُسقط **أربعةً** (سباقُه وعقدُه والحارسُ و١) ·
**والبديلُ المفرط** (المديرُ يُردّ كذلك) يُسقط **ج١ وج٢ بالضبط** · وتمريرُ `role` باليد
في بابٍ واحد يُسقط سباقَه **والحارسَ و٢**. وأربعٌ وعشرون حزمةً مجاورة خضراءُ كلُّها
(تسعَ عشرةَ على القاعدة وخمسٌ خالصة — منها `test:workflow` و`test:episode-manufacturing`
و`test:episode-integration` و`test:manufacturing-excuse` و`test:manufacturing-overview-lateness`
بنداءاتها المباشرة، و`test:patient-branch-access` بإعادة الإسناد)، و`tsc` ٣٥ = خطُّ
الأساس، و`check:tests` نجح، و`npm run build` ينجح.

#### تكملةٌ رابعة — استرجاعُ الأعذار التي مُحيت قبل ٤٠٣ (٢٠٢٦-٠٩-٢٥)

**قاعدةُ المالك بحرفها**: «جلُّ ما أريده هو أن الخبير الذي يكتب عذراً في أيّ موضعٍ خاصٍّ
للعذر يجب ألّا يكون مريضه أحمر وإنما أصفر لأنه بعذر، وباقي الأمور… من عدم محو العذر».
**بلا ترحيلٍ ولا حرفٍ في شيفرة التطبيق** — ملفّا SQL في جذر الريبو واختبارُهما.

**والفحصُ قبل البناء — الجديدُ سليم**: كلُّ شاشةٍ تعرض التأخير تقرأ العذرَ المكتوب —
لوحةُ التصنيع (`latenessOf`/`rowToneOf`) وصفحةُ الأمر (`orderLatenessNotice`) والتنبيهات
(`holdReasonLabel`) — **ولا كاتبَ يمحوه من أمرٍ حيٍّ بعد ٤٠٣**: كلُّ `holdReasonCode: null`
في المخزن على طريقٍ يُنهي الأمر (التسليم · إنجازُ الصيانة · الإلغاء · الإبطالُ الإداريّ).
**فالباقي أوامرُ مُحي عذرُها قبل ٤٠٣ وحدها** — مقروءٌ من الشيفرة (`git show 2477990`: كان
`resumeOrder` يكتب `holdReasonCode: null, holdNote: null`، والتقدّمُ يمرّر `clearHold: true`).

**والعذرُ لم يضع — وهذا مُثبَتٌ لا مفترَض**: كلُّ كاتبٍ للعذر يكتب في المعاملة نفسِها سطراً
في `prosthetic_work_history` يحمل الرمزَ والملاحظةَ بصيغةٍ ثابتة **منذ وُلد العذرُ نفسُه**
(ترحيل ٠٤٥ والصيغةُ في طلب الدمج ٢٠٥ نفسِه، ٢٠٢٦-٠٨-١١ — `git log -S`): «توقّف: {النوع} —
السبب: {الرمز} — {الملاحظة}» (`holdOrder`) · «إعادة عمل فني — رجوع من … — السبب: …»
(`reworkToStage`) · «سبب التوقّف: …» (`documentHoldReason`، ٤٠٥). وسطورُ ما قبل ٢٠٥
(«الحالة: …» و«إعادة عمل: …») لا تطابقها — ولم يكن قبلها عذرٌ مكتوبٌ أصلاً.

**الملفّان** — يلصقهما المالكُ في Neon، **ولا يُشغَّل أحدُهما تلقائياً** (ليسا ترحيلاً):
- **`restore_erased_excuses_preview.sql`** — استعلامٌ واحد يقرأ ولا يكتب. كلُّ صفٍّ أمرٌ
  **حيٌّ** بلا عذرٍ الآن وفي سجلّه عذرٌ كتبه خبيرٌ بيده: رقمُه · المريض · الفرع · الخبير ·
  الحالة · الموعد · أمتأخّرٌ الآن · **العذرُ الذي سيعود** باسمه العربيّ وملاحظتِه · **مَن
  كتبه ومتى** · أفي السلّة · والعددُ الكلّيّ.
- **`restore_erased_excuses_apply.sql`** — كتلةُ `DO` واحدة (تتراجع كلُّها عند أيّ خطأ).
  يعدّل المالكُ سطرين: **أرقامُ الأوامر المستبعَدة** (`v_excluded`) و**مَن ينفّذ**
  (`v_operator_id`، صفرٌ = «تدخّلٌ يدويّ على قاعدة البيانات» بلا رقمٍ يُلفَّق).

**والقاعدةُ واحدة في الملفّين حرفاً بحرف** — كتلةُ «تعريف المرشَّحين» بين علامتين، يطابقها
الاختبارُ نصّاً فلا تعرض المعاينةُ شيئاً ويكتب السكربتُ غيرَه:
- **آخرُ** سطرِ عذرٍ على الأمر (`DISTINCT ON … created_at DESC, id DESC`) — عذرٌ أحدث يحلّ
  محلَّ الأقدم، كما في القاعدة اليوم. **فالمُعادُ هو بعينه ما كان سيبقى لو طُبّق ٤٠٣ من
  البداية** — لا يُخترَع عذر، ولا يُعاد عذرٌ حلّ محلَّه أحدث.
- **حيٌّ فقط**: لا `completed` ولا `cancelled` — التسليمُ والإلغاءُ يُنهيان العذرَ بحقّ،
  والمُبطَلُ إدارياً حالتُه إحداهما.
- **بلا عذرٍ الآن** (`NULLIF(btrim(hold_reason_code), '') IS NULL`).
- **ومكانُ العذر وحده**: ملاحظاتُ الانتقال الفنّية وسببُ تغيير الموعد **ليست أعذاراً**
  (قرارُ ٢٠٢٦-٠٩-٢٤) — والتصفيةُ بنوع السطر لا بكلمة «السبب»، فتغييرُ موعدٍ **بعد** العذر لا
  يحجبه.
- **والأسماءُ من المعجم القانونيّ نفسِه** (`REASON_CODE_LABELS` · `STATUS_LABELS`) — يطابقها
  الاختبارُ زوجاً زوجاً.

**وحُرّاسُ الكتابة**:
- **رقمٌ مستبعَدٌ ليس في القائمة ⟶ يتوقّف قبل أيّ كتابة** — خطأُ كتابةٍ لا يمرّ فيُعاد عذرُ
  أمرٍ أراد المالكُ استبعادَه. **ومنفِّذٌ بلا حسابٍ قائم** كذلك.
- **الشرطُ يُعاد على الصفّ الحيّ لحظةَ الكتابة**: أمرٌ كتب له خبيرٌ عذراً أثناء التشغيل
  يُترك ويُقال باسمه — ولا يُكتب فوقه العذرُ القديم.
- **ولا يكتب إلّا عمودَي العذر** (وختمَ التحديث): لا حالةَ ولا مرحلةَ ولا موعدَ ولا خبيرَ.
- **والأثرُ مكتوب**: سطرٌ في الخطّ الزمني بنوع «كتابة سبب التوقّف» يقول ما أُعيد ومَن كتبه
  أصلاً ومتى ومَن نفّذ، وسطرُ تدقيقٍ بالقيمتين ورقمِ السطر الذي أُعيد منه.
- **وتحقّقٌ في آخره يُرجع كلَّ شيء**: كلُّ أمرٍ أُعيد يطابق سطرَه، وسطرٌ واحدٌ بالضبط في
  الخطّ الزمني والتدقيق لكلّ أمر.
- **وتشغيلُه ثانيةً لا يفعل شيئاً**: ما عاد عذرُه خرج من القائمة. والمستبعَدُ يبقى فيها.
- **`lock_timeout` خمسُ ثوانٍ**: صفٌّ مقفولٌ في طلبٍ طويل لا يُنتظَر بلا نهاية — يتراجع كلُّ
  شيء ويُعاد التشغيل.

**ولم تُشغَّل الكتابةُ على الإنتاج** — تنتظر أن يشغّل المالكُ المعاينةَ ويراجعها.

**ومُلاحَظٌ في الطريق ولم يُصلَح**: مربّعاتُ لوحة التصنيع (`getOverview`) **تعدّ أوامرَ مرضى
السلّة**، والقائمةُ والشرائطُ والتنبيهاتُ (`listOrders` ⟵ `activePatientDrizzle`) لا تعدّها —
فقد يزيد المربّعُ على الشريط بعددهم. أظهره الاختبار: ملفٌّ في السلّة بفرع اللوحة نفسِه حرّك
المربّعَين. **سابقٌ لهذه التمريرة، ولا يُصلَح بلا طلب.**

اختبارٌ دائم: `npm run test:restore-excuses` (٧٠ تأكيداً حيّاً على Postgres وعلى النقاط
الحقيقية، **والسكربتان بنصّهما كما يُلصقان**: الأشكالُ تُبنى بـ`/hold` و`/resume` و`/advance`
و`/hold-reason` و`/delivery-date` و`/cancel`، ثمّ يُمحى العذرُ بما كان يفعله ما قبل ٤٠٣ بحرفه ·
**أ** النصّ: الكتلةُ واحدة · المعجمان · صيغُ الكتّاب الثلاثة مقروءةً من `store.ts` · المعاينةُ
بلا فعلِ كتابة · والكتابةُ بلا حذفٍ ولا إسقاطٍ وتحديثٌ واحد لعمودَي العذر · **ب–ج** «وهج»
وأخواتُها حمراءُ قبله · **د** المعاينةُ داخل `BEGIN READ ONLY` وبصمةٌ لا تتغيّر، والقائمةُ هي
المحوّةُ بعينها: الأحدثُ من عذرين · ملاحظةٌ بسطرين وشرطة · إعادةُ العمل · «كتابة سبب التوقّف»
· تغييرُ موعدٍ بعد العذر · السلّة — **لا** الباقي عذرُه ولا مَن لم يُكتب له ولا الملاحظةُ الفنّية
وسببُ الموعد ولا الملغى ولا الموروث · **هـ** الأخطاءُ الثلاثة بصفر كتابة · **و** الكتابةُ
**وسباقٌ حقيقيّ**: معاملةٌ تكتب عذراً جديداً وتمسك الصفّ، والسكربتُ يقف على قفله فعلاً
(`pg_stat_activity` برقم اتّصاله) ثمّ يتركه · وما عداه مطابقٌ بايتاً · **ز** الخطُّ الزمني
والتدقيق · **ح** **الأصفرُ**: اللوحةُ والمربّعان وصفحةُ الأمر والتنبيهات، والمستبعَدُ ومَن لم
يُكتب له أحمرُ · **ط** التشغيلُ الثاني بصفر كتابة · **ي** المنفِّذُ باسمه ورقمه).
**ومُتحقَّقٌ بالعكس اثنتي عشرةَ مرّة، كلُّ حارسٍ على حدة**: رفعُ فحص الصفّ الحيّ لحظةَ
الكتابة يُسقط **خمسةً** — السباقُ بعينه (و١٢ · و١٣ · و١٦ · ز١ · ز٣) · وقلبُ «الأحدث» إلى
الأقدم **د٥ وو٦ وحدهما** · ورفعُ فحص الاستبعاد يُسقط هـ١ وهـ٣ **فتقع الكتابةُ فعلاً** وما بعدها
· ورفعُ شرط الحيّ **د٣ وو١٦ وط٣** (والتحديثُ يحرسه ثانيةً فلا يُكتب على الملغى) · وإسقاطُ سطر
إعادة العمل أو «كتابة سبب التوقّف» يُسقط بندَه وما يتبعه · وإسقاطُ بادئة «توقّف:» **يُسقط
الاسترجاعَ كلَّه** (يصير سطرُ الاستئناف «الأحدث») · وقصُّ الملاحظة عند أوّل شرطة **د٦ وو٧** ·
وانحرافُ الكتلة في ملفٍّ واحد **أ١ وأ٢** · وتغييرُ اسمٍ في الملفّين **أ٢ وحده** · وإسقاطُ سطرِ
خطٍّ زمنيٍّ واحد **يُفشل تحقّقَ السكربت فيتراجع كلُّه** · ورفعُ فحص المنفِّذ **هـ٥ وحده**. **وشرطٌ
حُذف لأنه لا يُسقط شيئاً**: «للسطر رمز» — لا كاتبَ يكتب سطرَ عذرٍ بلا رمز. وأربعُ حزمٍ مجاورة
خضراء (`manufacturing-excuse` · `manufacturing-overview-lateness` · `manufacturing-row-tone` ·
`manufacturing-buckets`)، و`tsc` ٣٥ = خطُّ الأساس، و`check:tests` نجح، و`npm run build` ينجح.

---

## 5. ثغرات معروفة لم تُعالج بعد

- **`req.ip` يُظهر حافة Cloudflare**: نحتاج `app.set('trust proxy', N)`
  مع N متوافق مع Cloudflare وقراءة `X-Forwarded-For`.
- **عناصر ريبلت**: `.replit`, `server/replit_integrations/auth/`
  لا تزال موجودة. الكود يعمل لكن التسمية مضلّلة بعد الانتقال إلى
  Render.
- ~~**`deletePatient` cascade ضخم وصامت سابقاً**~~ — **عولج في ترحيل ٠٦٨**:
  الحذف العادي صار سلّةً بمهلة استعادة ثلاثين يوماً، والكاسكيد بقي كما هو
  خلف باب «حذف نهائي» للمسؤول العام وحده. القسم 4.g.
- **توحيد الجلسات والمصادقة بين Render وأي مثيل سابق**: حالياً
  لا يوجد فرض على عدد المثيلات، فإذا تعدّدت عمّال Render قد ترى
  تضارباً (نظرياً).
- **مهلة إعادة النشر**: PR #74 لا يحوي اختبارات؛ كل التحقّق يدوي
  عبر استعلامات Neon.

---

## 6. الأماكن المهمّة في الريبو

```
server/
  index.ts               # نقطة البدء؛ يشغّل migrations ثم registerRoutes ثم cron
  routes.ts              # كل نقاط REST. ضخم (~4900 سطراً).
  storage.ts             # جميع عمليات Drizzle. ضخم. يحوي deletePatient cascade.
  db.ts                  # pg pool + Drizzle client + معالج الأخطاء.
  backup.ts              # كرون النسخة اليومية بالبريد.
  accounting/
    ledger.ts            # logAudit() يكتب في audit_log.
    routes.ts            # نقاط محاسبية.
  anomalies/
    detector.ts          # كشف الشذوذ (قراءة فقط).
  patients/
    active_patient.ts    # «المريض الفعّال» = deleted_at IS NULL — تعريفٌ واحد يُستورَد.
    trash_store.ts       # الحذف الناعم والاستعادة والحذف النهائي (٠٦٨).
    trash_routes.ts      # /api/patient-trash — السلّة والاستعادة والهدم.
  medical/
    store.ts             # طبقة بيانات المعاينات (إدراج فقط، لا تعديل ولا حذف).
    routes.ts            # نقاط المعاينة + الملاحق + قائمة الانتظار.
  ai/
    provider.ts          # مزوّد Claude، aiComplete/aiToolStep، بلا صلاحياتٍ يعرفها.
    access.ts             # resolveAiAccess — من الجلسة وحدها، بلا شبكة.
    chat.ts               # حلقة المحادثة (وضعان: عام/ماليّ) + الاسترجاع.
    tools/
      registry.ts          # السجلّ المقفَل — سبعُ أدوات قراءةٍ فقط.
      reports.ts           # operational_summary/financial_summary — أرقامٌ محسوبةٌ في الخادم.
    knowledge/
      store.ts             # المعرفة الموثوقة + اقتراحات الموظّفين (٤.n).
      retrieval.ts          # الاسترجاعُ الحيّ فوق shared/ai_knowledge_retrieval.ts.
      routes.ts             # /api/ai/knowledge/* — تقديمٌ للجميع، إدارةٌ للمسؤول العام.
    conversations/
      store.ts             # سجلُّ المحادثات (٠٨٤، ٤.ag) — الكتابةُ الوحيدة recordExchange.
      routes.ts            # /api/ai/conversations/* — **قراءةٌ محضة**، لا كتابةَ فيه.
      cleanup.ts           # كرونُ محو التسعين يوماً — مستقلٌّ عن backup.ts.
    semantics.ts          # تسمياتٌ عربية للأدوات — فوق shared/manufacturing.ts، لا تسميةٌ ثانية.
  sessions_module/
    routes.ts            # تتبّع الجلسات اليومية لكل جهاز.
  replit_integrations/
    auth/                # المصادقة (مُعاد استخدامها بعد الانتقال).
  migrations/
    runner.ts            # يشغّل الـ migrations المرتّبة على البدء.
    001_..010_..., 011_visit_soft_delete_and_forensic.ts
shared/
  schema.ts              # Drizzle schemas + zod inserts. صدر الحقيقة لجميع الجداول.
client/src/
  hooks/use-patients.ts  # mutations: addVisit / updateVisit / deleteVisit
  pages/PatientDetails.tsx # صفحة المريض (تعرض الجلسات والمتبقّي).
.replit                  # إعداد ريبلت (المشروع قديماً عليه).
```

---

## 7. أوامر مفيدة

```bash
# تحقّق الأنواع (شيفرة التطبيق — خطّ الأساس ٣٥ خطأً معروفاً)
npx tsc --noEmit

# تحقّق أنواع ملفّات الاختبار — `tsconfig.json` يستثنيها فلا يراها الأمر
# أعلاه، وكسرٌ في ملفّ اختبار يجعل حزمتَه لا تعمل أصلاً بينما يبقى
# `npm run check` أخضر (وقع فعلاً من PR #358 إلى #364).
npm run check:tests

# تشغيل التطوير
npm run dev

# دمج تحديثات schema للقاعدة المحلية
npm run db:push

# عند ملاحظة سلوك غير مفهوم على الإنتاج، الاستعلامات الأولى:
# (تنفّذ في Neon Console > SQL Editor)
SELECT entity_type, action, COUNT(*) AS n, MAX(created_at) AS last
FROM audit_log
GROUP BY entity_type, action
ORDER BY entity_type, action;

SELECT * FROM visits_forensic_log
ORDER BY logged_at DESC LIMIT 50;
```

---

## 8. ملاحظات للمساعد التالي

- المالك يكتب بالعربية ويفضّل الردود بالعربية مع اقتباس أسماء التقنية
  بالإنجليزية. الأسلوب مباشر وموجز.
- **سياسة الدمج (أمر دائم من المالك 2026-06-08)**: عند الانتهاء من أي
  PR طلبه المالك، **ادمجه فوراً** بدون انتظار موافقة صريحة لكل مرة.
  المالك أعطى تفويضاً عاماً ولا يريد سؤاله في كل دمج. وأبلغ هذا
  للمساعدين القادمين عبر هذا الملف.
- **لا تنفّذ شيئاً مدمّراً على الإنتاج بدون إذن صريح**: مثل
  drop column, drop table, DELETE بدون WHERE.
- **قاعدة ملزمة (بعد حادثة «فشل في حذف المريض» 2026-07-26)**: أي جدول
  جديد يحمل مفتاحاً أجنبياً إلى `patients` أو إلى أحد توابعها
  (`patient_cases` وغيرها) **يجب** قبل الدمج: (١) إضافته إلى كاسكيد
  `storage.deletePatient` بترتيب المفاتيح الصحيح، و(٢) اختبار حذف مريض
  كامل يحمل صفوفاً في الجدول الجديد على Postgres محلي. النسيان يكسر
  الحذف لكل المستخدمين بمن فيهم المسؤول.
- إن طلب المالك تشخيص اختفاء بيانات: ابدأ بـ
  `audit_log` و `visits_forensic_log`. هما المصدران الرسميان الآن.
- الـ migrations تُشغّل تلقائياً عند بدء الخادم. لا حاجة لخطوة يدوية
  على Render.
- النشر يحدث تلقائياً عند الدمج إلى `main`، لكن **يمكن يدوياً** عبر
  Render Dashboard → Manual Deploy → Deploy latest commit.
- المالك ليس له شيل على Neon ولا psql محلياً (لاحظ Neon Console فقط).
  السكربتات المقترحة يجب أن تكون لصقاً مباشراً في SQL Editor.
- **قاعدةٌ ملزمة من المالك (٢٠٢٦-٠٩-٢٥) — ما يلصقه المالكُ يصله داخل صندوق
  شيفرةٍ في المحادثة، لا ملفّاً مرفقاً**: «لاترسل لي اوامر نيون بهذا الشكل اطلاقا
  التزم بارسالها داخل صندوق قابل للنسخ فقط وتذكر ذلك الامر جيدا لكل شي مستقبلا».
  فكلُّ SQL لـ Neon — وكلُّ أمرٍ أو سكربتٍ يُطلَب من المالك لصقُه أو تنفيذُه — يُكتب
  نصُّه **كاملاً** داخل كتلة شيفرةٍ مُسيَّجة في الردّ نفسِه، وكلُّ خطوةٍ في صندوقها،
  **ولا يُرسَل بـ`SendUserFile` ولا مرفقاً ولا رابطاً إلى ملفّ**. (وقعت المخالفةُ فعلاً
  مع سكربتَي استرجاع الأعذار فرفضها.) والملفُّ في الريبو يبقى مرجعاً يُقرأ من `main`
  قبل اللصق، **والصندوقُ هو ما يُسلَّم** — ولا يُختصَر سكربتٌ لطوله.

---

## 4.al ملفُّ المريض يقول أيُّ عمليةٍ وأيُّ مال (٢٠٢٦-٠٩-٢٣)

**عرضٌ فقط.** لا ترحيل · ولا عمودَ جديد في القاعدة · ولا حرفَ في أيّ حساب
أو صلاحية. المعلوماتُ كانت كلُّها في القاعدة، والناقصُ إظهارُها.

### الواقعة
مريضٌ له أمرا تصنيعٍ بنفس النوع ونفس الخبير (شكلُ «عبد الله فتحي عون»).
فملفُّه يعرض بطاقتين نصُّهما واحد — «أطراف صناعية — الخبير: سيف موصل» —
لا يفرّق بينهما إلّا المرحلةُ والتاريخ. **ولا رقمَ أمرٍ على أيّ منهما ولا
رقمَ جهاز**، فمَن يُكلَّم عن «أمر ٣٦٢» لا يعرف أيَّ بطاقةٍ يضغط. وجدولُ
الدفعات يعرض المبلغَ والتاريخ والملاحظة **بلا عمودٍ يقول لأيّ جهازٍ هي**،
فنقلُ دفعةٍ بين جهازين لا يُرى أثرُه إطلاقاً. ونافذةُ التصحيح تعرض «الكلفة
المسجّلة» **ولا تعرض المدفوع** — وهو وحدَه ما يقرّر أيُطرَح سؤالُ إرجاع
المبلغ وكم يُردّ، فيُفاجأ به المسؤولُ ويجيب بلا علم (وقع فعلاً).

### الأربعة
- **بطاقةُ الأمر**: «الجهاز الأول · طرف صناعي كامل» و«أمر #٣٦٢».
- **ومالُها**: كلفةُ هذا الجهاز · المدفوع عليه · المتبقّي. وللصيانة أجرُها
  ووسمُ «ضمن الضمان» حين يكون.
- **عمودُ «الجهاز» في جدول الدفعات** — يظهر لمريض الأجهزة وحده.
- **«المدفوع على هذه العملية» في نافذة التصحيح** — بجوار الكلفة المسجّلة،
  **قبل** اختيار نوع التصحيح لا بعده، وخارجَ قسم «تفاصيل ما سيحدث» المطويّ.

### والعبارةُ واحدةٌ في الموضعين
`shared/device_label.ts: deviceOrdinalLabel` — «الجهاز الأول» في البطاقة
وفي صفّ الدفعة **من الدالّة نفسِها**. وبها وحدها يربط القارئُ الدفعةَ
بعمليتها، فلو كُتبت في كلّ شاشةٍ على حدة لانحرفت إحداهما فانقطع الربط.
**وترتيبٌ غيرُ صالح ⟶ `null` لا «الجهاز الأول»**: لا يُخترَع ترتيبٌ لدفعةِ
علاجٍ طبيعي ولا لرصيدٍ غير مخصَّص.

### والمالُ خلف بوّابته
نقطةُ `GET /api/manufacturing/patient/:id/orders` محروسةٌ بـ`canViewPatients`
لا بـ`canViewPayments`، فـ`getAllOrdersForPatient` صارت تأخذ `includeMoney`
**يقرّرها المنادي من الجلسة**. **والحقولُ تُحذَف ولا تُصفَّر** (درسُ إصلاح
٢٠٢٦-٠٩-٠٢): صفرٌ يُقرأ «لم يدفع شيئاً» — كذبةٌ ماليةٌ لا حالةَ صلاحية.
والشاشةُ تفحص `typeof === "number"` لا القيمة. ودفعاتُ الملفّ كما كانت:
محجوبةٌ كاملةً عمّن لا يملك عرضَها، فالحقلان الجديدان عليها لا يفتحان باباً.
**والخبيرُ لا يرى هذه البطاقة أصلاً** — الخادمُ يردّه ٤٠٣ كما كان.

### وما لم يُمَسّ
أيُّ حسابٍ ماليّ · أيُّ صلاحية · القاعدةُ والترحيلات · مسارُ التصحيح
الإداريّ نفسُه · محاسبةُ البيع والصيانة · العلاجُ الطبيعي.

اختبارٌ دائم: `npm run test:patient-file-money` (٣١ تأكيداً حيّاً على
Postgres وعلى النقاط الحقيقية — عمليتان بنفس النوع والخبير، الهويّةُ على
كلّ بطاقة، المالُ لمن يملكه **ومحذوفٌ لمن لا يملكه مع خلوّ نصّ الردّ من أيّ
رقم**، دفعةُ كلّ جهازٍ تقول جهازَها، **ونقلُ دفعةٍ بين الجهازين يُقرأ في
الملفّ** — شكوى المالك بعينها — ومعاينةُ التصحيح تقول المدفوع، وعقدُ
الشاشات الثلاث) و`npm run test:device-label` (الدالّةُ الخالصة، بلا قاعدة).
**ومُتحقَّقٌ بالعكس مرّتين**: رفعُ بوّابة المال يُسقط تأكيدَي الحجب وحدهما،
وإسقاطُ هويّة الجهاز عن الدفعات يُسقط ثلاثةً كلَّها من قسمها.

### تصحيحٌ لاحق — مراجعةٌ آلية على ٣٨٤ (٢٠٢٦-٠٩-٢٣)

خمسُ ملاحظاتٍ فُحصت كلُّها على الكود الحيّ وثبتت، **وكلُّها من كتابتي في
الطلب ٣٨٤**. **وبلا ترحيلٍ ولا حرفٍ في القاعدة.**

#### ١) الذاكرةُ المخبَّأة كانت تُبقي المبلغَ بعد سحب الصلاحية

`App.tsx` يُبطل عائلاتِ المفاتيح عند تغيّر لقطة الصلاحيات (إصلاحُ
٢٠٢٦-٠٩-٠٣) **بقائمةٍ مكتوبةٍ حرفاً** — وذاك يكفي لمفتاحٍ ثابت لأن المطابقةَ
بالبادئة تصيب كلّ توليفاته. أمّا مفتاحُ أوامر المريض فنصٌّ واحدٌ يحمل رقمَه
في **جسمه** (`` [`/api/manufacturing/patient/${id}/orders`] ``)، فلا بادئةَ
تصيبه ولا يعرف `App.tsx` أرقامَ المرضى المخبَّأة أصلاً.

وردُّ تلك النقطة **صار يتبع الصلاحية** في ٣٨٤. فسحبُ `canViewPayments`
وصفحةُ المريض مفتوحةٌ كان يُبقي `agreedCost`/`paidOnDevice` معروضَين من ردٍّ
مخبَّأٍ سابق، ومنحُها يُبقيهما محجوبَين — **والخادمُ يبقى الحارسَ الحقيقيّ**،
لكنّ الشاشةَ تكذب حتى تُعيد الجلب.

**فالمطابقةُ بمُسنِدٍ لا ببادئة** (`invalidatePermissionShapedQueries` في
`client/src/lib/queryClient.ts`) — وهو الشيءُ الوحيد الذي يبلغ مفتاحاً يحمل
رقماً في جسمه. **وإبطالٌ لا نزع** (الصفحةُ مشروعة، والمطلوبُ إعادةُ جلب)،
**ومصوَّبٌ لا كاسح** (`…/summary` و`…/followups` و`…/exams` لا تُمَسّ)،
**وداخل فرع `patientOrPaymentViewChanged` نفسِه** لا فرعاً ثانياً.

**والقرارُ دالّةٌ خالصة** (`isPermissionShapedOrdersKey`) لا شرطٌ مدفونٌ في
`useEffect`: المشروعُ بلا مشغّل DOM، فقرارٌ داخل React لا يُختبَر إلّا بقراءة
نصِّه (درسُ ٤.u).

#### ٢–٥) سكربتُ التراجع: ما لا يحمله التدقيقُ يقف عنده ولا يلفّقه

سطرُ التدقيق يحمل `orderStatus`/`episodeStatus`/`followupStatus` **وحدها**:
لا وقتَ إغلاق المتابعة ولا سببَه، ولا ختمَ إلغاء الحلقة ولا سببَه. والإلغاءُ
الإداريّ **يكتب فوق الزوجين** حين يكون الصفُّ مغلقاً/ملغىً قبله (ولا حارسَ في
`server/admin_reversal/store.ts` يمنع ذلك — مفحوصٌ حيّاً). فكان السكربتُ يُرجع
الحالةَ ويُصفّرهما، فيُنتج صفّاً يدّعي «أُغلقت بلا شراء» بلا وقتٍ ولا سبب.

- **متابعةٌ كانت `closed_%`** ⟶ **يقف** برسالةٍ تقول لماذا، **بصفر كتابة**.
- **حلقةٌ كانت `cancelled`** ⟶ كذلك. (نفسُ منطق حالات التوقّف القائم فيه.)
- **وشهادةُ إلغاء المعاينة تُحذَف بالعلامة لا بنصّ السبب**: الإلغاءُ **يتخطّى**
  كتابتَها حين تكون المعاينةُ ملغاةً سلفاً، فحذفٌ بمطابقة `'إلغاء إداري
  للعملية — %'` كان يمحو شهادةً **سابقة** فيُعيد سلطةَ معاينةٍ سُحبت قبل
  الضغطة الخاطئة. والعلامةُ الدقيقة في التدقيق: `new_values.examCancelled`
  (رقمُ المعاينة حين أنشأها، و`null` حين تخطّاها)، و`exam_id` فريدٌ في
  الجدول. وغيابُ المفتاح من التدقيق ⟶ **يقف** ولا يُخمَّن.
- **والفعلُ يُنسَب لمن نفّذه**: كان يكتب `r.created_by`/`r.created_by_name` —
  **مَن أوقع الإلغاءَ الذي نتراجع عنه** — في سطر التدقيق وفي سجلّ الأمر معاً.
  فصار `v_operator_id` صريحاً في الرأس: رقمٌ **قائمٌ في `system_users`**
  (يُفحَص قبل أوّل كتابة ويُقرأ اسمُه منه)، أو صفرٌ ⟶ **لا رقمَ يُلفَّق**
  واسمٌ يقول «تدخّلٌ يدويّ على قاعدة البيانات». ومَن أوقع الإلغاءَ الأصليّ
  يبقى مسمّى في `new_values.originalReversalBy`.

اختبارٌ دائم: `npm run test:permission-cache` (على `QueryClient` حقيقيّ بلا
React — **العطبُ نفسُه** بالقائمة القديمة، ثمّ الإبطالُ بالمُسنِد، وعزلُ ستّة
مفاتيح لا تتبع الصلاحية، والقرارُ الخالصُ بعشر صورٍ، **وعقدُ الوصل** في
`App.tsx`)، والأقسامُ ز/ح/ط/ي في `npm run test:undo-admin-reversal` (٦٦
تأكيداً بعد الإضافة، مقابل ٤٠ قبلها — **وكلُّ شكلٍ خطِرٍ يُبنى حيّاً**: يُغلَق
الصفُّ فعلاً ثمّ يقع الإلغاءُ فوقه، ويُثبَت أن الإلغاءَ كتب فوق السبب، ثمّ
يُسأل السكربت).

**ومُتحقَّقٌ بالعكس خمسَ مرّات، كلُّ حارسٍ على حدة**: حذفُ نداء `App.tsx` يُسقط
**تأكيداً واحداً** · وتوسيعُ المُسنِد إلى بادئة يُسقط **ستّة** · ورفعُ حارس
المتابعة المغلقة يُسقط **اثنين** · ورفعُ حارس الحلقة الملغاة يُسقط **اثنين** ·
والعودةُ إلى الحذف بنصّ السبب تُسقط **واحداً** · والعودةُ إلى نسبة الفعل لمن
ألغى تُسقط **ستّة**. ولا شيءَ خارج قسمِه في أيٍّ منها.
