# bionic-centers-manager — مرجع للمساعد الذكي

> **هذا الملفُّ يُحمَّل كاملاً في بداية كلّ محادثة وبعد كلّ ضغط — فيبقى قصيراً عمداً** (§8).
>
> **ابدأ بالقسم ٠ أدناه**: هو وحدَه يقول أين وصل العمل اليوم، وما هو مفتوحٌ، وما هو مؤجَّلٌ
> بقرار المالك، وخطُّ الأساس الذي تُقاس عليه أيّ دعوى كسر.
>
> **وتاريخُ كلّ ميزةٍ وقراراتُها** — الأقسامُ ٣ و٤ و4.b … 4.ao و4.al — في `docs/claude/`،
> ملفٌّ لكلّ قسم، **والفهرسُ بعد القسم ٢**. اقرأ ملفَّ القسم قبل أن تعمل في مجاله.

---

## 0. حالة المشروع الآن — ابدأ من هنا

> **آخرُ تحديثٍ لهذا القسم: ٢٠٢٦-٠٩-٢٥.**
>
> الأقسامُ التي تقول **«كيف يعمل النظام»** — وهي مرجعٌ دائم — في `docs/claude/` (الفهرسُ بعد
> القسم ٢). وهذا القسمُ وحدَه يقول **«أين وصلنا اليوم»**. **ومَن أنهى عملاً يحدّثه قبل أن يغلق
> مهمّته**: قسمُ حالةٍ لا يُحدَّث يشيخ فيصير يكذب، وقسمٌ كاذبٌ أسوأُ من غيابه.

### آخرُ ما دُمج
**مريضُ السلّة خارجَ مربّعات لوحة التصنيع وعدّاداتِ المساعد** (٢٠٢٦-٠٩-٢٥، §4.ao «تكملةٌ
سادسة»): `getOverview` كانت تعدّ أوامرَ مرضى السلّة والشرائطُ لا تعدّها، فصار الشرطُ
(`belongsToActivePatientSql`) عليها وعلى عدّادات التصنيع الثلاثة في المساعد. الاستعادةُ تُعيد
كلَّ رقم. **بلا ترحيل.**

وقبله **التنبيهاتُ تنقسم كاللوحة، وعمودُ «إعادات العمل»** (٢٠٢٦-٠٩-٢٥، §4.ao «تكملةٌ خامسة»):
شاشةُ التنبيهات صارت قسمين «متأخرة بدون عذر» (أحمر) و«متأخرة بعذر» (كهرمانيّ بسببه) بتعريف
اللوحة نفسِه (`latenessOf` عبر `notifications_sections.ts`)، وعمودا «إعادة قالب/سوكت» الفارغان
صارا عموداً واحداً «إعادات العمل» من `reworks`. **بلا ترحيل**، وعقدُ نقطة التنبيهات لم يتغيّر
إلّا أن اسمَ السبب صار من `writtenHoldExcuse`.

وقبلهما **تخفيفُ هذا الملفّ** (٢٠٢٦-٠٩-٢٥، §8): كان ١٠٬٦٦٩ سطراً (~٩٧٨ كيلوبايت) يُحمَّل كاملاً في
بداية كلّ محادثة وبعد كلّ ضغط. **مقيسٌ من سجلّ الجلسة**: الضغطُ وقع عند نحو ٩٣٢ ألف رمز من مليون، وأوّلُ
طلبٍ بعده بدأ عند نحو ٥٠٢ ألف قبل أيّ عمل — وأغلبُها هذا الملفّ. **ووكيلٌ فرعيٌّ على نموذجٍ أصغر
عجز عن البدء أصلاً** («Prompt is too long»). فنُقلت الأقسامُ ٣ و٤ و4.b … 4.ao و4.al **بحرفها**
إلى `docs/claude/` — ملفٌّ لكلّ قسمٍ باسمه — وسجلُّ «آخرُ ما دُمج» القديم إلى
`docs/claude/changelog.md`. **لا سطرَ حُذف**: مُتحقَّقٌ آلياً أن كلَّ سطرٍ من الملفّ القديم موجودٌ
في الجديد أو في ملفّات المجلّد، إلّا أسطرَ الحالة التي تغيّرت. **بلا حرفٍ في الشيفرة.**

والأقدمُ كلُّه — من استرجاع الأعذار إلى ما قبله — في `docs/claude/changelog.md`.

**وآخرُ ترحيل: ٠٨٧** — فالتالي **٠٨٨**، ولا يُشغَّل ما لم يُسجَّل في
`server/migrations/runner.ts` (استيرادٌ **ومصفوفة** `migrations`، الاثنان معاً).

### ما هو مفتوحٌ الآن
لا فرعَ عملٍ قائم، ولا PR مفتوح، ولا مهمّةً نصفَ منجَزة.

**زهراء وأيوب — حسمها المالكُ بنفسه** (٢٠٢٦-٠٩-٢٥): الاستعلامُ أثبت أن حسابَ أيوب
بلا ذي قار (`branch_ids` = بغداد، الموصل، كركوك) وحسابَ عناد بلا الموصل، فأعاد المالكُ
فروعَهما وحوّل أمرَ زهراء من زياد إلى أيوب. **والمالكُ متأكّدٌ أنه لم يُزِل تلك الفروع،
والسببُ لم يُثبَت**: الكاتبُ الوحيد لـ`branch_ids` نافذةُ تعديل المستخدم
(`PATCH /api/admin/users/:id`، للمسؤول وحده) **ولا تكتب سطرَ تدقيق**، فلا أثرَ يقول
مَن ومتى (آخرُ حفظٍ لأيوب ٢٠٢٦-٠٧-١٦ ٠٦:٠٢، ولعناد ٢٠٢٦-٠٨-٢٢ ١٦:٣٦). وتدقيقُها لم
يُطلَب.

**وقرارٌ قائم — أسبابُ الأوامر الموروثة لا تُعبَّأ** (المالك ٢٠٢٦-٠٩-٢٤/٢٥): يكتبها
الخبيرُ من «توقّف / مشكلة»، ولا سكربتَ يملؤها.

**وقرارُ المالك ٢٠٢٦-٠٩-٢٣ — لا استطلاعَ دوريّ**: سُئل أيريد أن يظهر عملُ
زميله على شاشته وهو واقفٌ أمامها بلا أن يلمس شيئاً، فأجاب أنّ ذلك يعجبه
**لكن إن أثقل النظامَ فلا**. فبقي التحديثُ عند الكتابة وعند العودة إلى
النافذة (§4.an)، **ولا `refetchInterval` ولا ويب سوكِت** — ولا يُضاف بلا
طلبٍ صريح.

### وينتظر المالكَ لا المساعد — واتساب المرضى
الشيفرةُ كاملةٌ ومدموجة منذ ٢٠٢٦-٠٨-٢٢، **ولم تُرسَل رسالةٌ واحدة قطّ**:
بيئةُ Render لا تحمل أيّاً من متغيّرات `PATIENT_WHATSAPP_*`. والقالبان
معتمَدان في Meta (`bionic_patient_welcome` · `bionic_patient_update`،
والثاني بمعامِلين — مطابقٌ لعقد الشيفرة)، والمتبقّي خطوةُ `Create app`
لأخذ `Phone Number ID` والتوكن الدائم. **التفصيلُ كلُّه في §4.c**، ومنه
الأربعةُ التي تُضبَط في حفظةٍ واحدة. **ولا يُطلَب التوكنُ في محادثةٍ
أبداً** — يُلصَق في Render مباشرةً.

### المؤجَّلُ بقرار المالك — لا يُبدَأ بلا طلبٍ صريح منه
- **`PUT /api/patients/:id` لا تكتب سطرَ `audit_log` واحداً**: تغييرُ كلفة
  مريضٍ لا يخلّف أثراً يقول مَن ومتى. قرارُ المالك ٢٠٢٦-٠٩-٢١: «لا حاجة…
  لا أحد يعدّلها إلّا أنا المدير المسؤول».
- **المراحلُ ٤–١٠ من قطار إصلاح تدقيق ٢٠٢٦-٠٩-١٢**: لم تُبدأ. ومنها **INT-05**
  الموصوفُ بتفصيله في آخر **§4.q** — انقلابُ ترتيب القفل بين إلغاء المعاينة
  وإتمام البيع. أثرُه **ردٌّ خاطئ وخروجُ عملية، لا فسادَ بيانات** (مقيسٌ
  حيّاً)، وهو **سابقٌ** لتلك المرحلة لا ناتجٌ عنها.

### ما أُنجز ولا يُعاد فتحه
**الملفُّ المكرَّر WB-01686** — دُمج في **WB-01688** (انتصار حبيب محمد) يوم
٢٠٢٦-٠٩-٢١ بنجاح: الثابتان المحاسبيّان متطابقان (١,٨٥٠,٠٠٠ = مساند
١,٧٥٠,٠٠٠ + علاج طبيعي ١٠٠,٠٠٠)، والرمزُ القديم محفوظٌ اسماً بديلاً،
وعدّادُ جلساتها ٨ كما كان، **ولا شارةَ انتظارٍ ظهرت** (حلقتُها `delivered`
فالخيطُ مستقرٌّ بحكم حارس `noEpisodes`). **لا عملَ باقٍ عليه.**

**والمتراكمُ الأوّل لواتساب** — ٨٤٦ صفّاً `pending` صارت `skipped` يوم
٢٠٢٦-٠٩-٢٢ بقرار المالك الصريح («نتخطّاهم ونبدأ بالجدد»): ٤٢١ ترحيباً
قديماً و٤٢٥ تحديثَ تصنيعٍ كانت ستصل خارجَ تسلسلها. `UPDATE 846` ثمّ
`بقي_معلقا = 0` ثمّ `COMMIT`، بصورةٍ طبق الأصل من `markSkipped` ولا صفَّ
`sent` ولا ديناراً مُسّ. **و`skipped` طرفيّةٌ لا تُحجَز ثانيةً** —
**ولا تُعاد هذه الصفوفُ إلى `pending` بحال**، فذاك يُطلق بالضبط ما قرّر
المالكُ تفاديه. (§4.c)

### خطُّ الأساس — قِسْ عليه قبل أن تدّعي كسراً
| الأمر | المتوقَّع |
|---|---|
| `npx tsc --noEmit` | **٣٥ خطأً معروفاً** — قارن بالعدد لا بالصفر |
| `npm run check:tests` | ٠ (وهو **لا** يُغطّيه `npm run check`) |
| `npm run build` | ينجح |

**وإخفاقاتُ الاختبار المسبَقة مسجَّلةٌ في آخر §4.ai**، ومعها `server/ai_tools_reports.test.ts`
(١٥ سطرَ إخفاق، متطابقةً على `main` — آخرُ §4.ao) — كلُّها عيوبُ فِكستشر
لا سلوكِ إنتاج، ومُثبَتةٌ متطابقةً على `main`. **ولا تُصلَح بتخفيف اختبارٍ
ولا بتعطيله.**

**والمقارنةُ بـ`main` تكون بشجرة عملٍ حقيقية** — `git worktree add /tmp/mainwt main`
— **لا بـ`git stash`**: تلك تقارن بـ`HEAD` الذي يحمل التزاماتِك أنت، فتُريك
عطبَك «مسبَقاً» وهو منك. (وقع فعلاً وصُحِّح.)

**والقياسُ في المتصفّح يكون على المكوّن الحقيقيّ لا على صفحةٍ تُكتب باليد** —
ما يصل العنصرَ هو ما تُخرجه دوالُّ المكوّن (`Card` ⟵ `cn` ⟵ `twMerge`)،
وصفحةٌ تجمع الأصنافَ باليد تقيس شيئاً لا تُنتجه الشاشة. (وقع فعلاً وصُحِّح،
§4.ao.)

### وحارسُ فهرس القدرات
`npm run test:ai-capabilities` يُفشل الحزمةَ حين تُضاف **نقطةُ قراءةٍ جديدة**
لم يُقرَّر فيها: توصَف في `DESCRIBED` أو تُمنَع في `BLOCKED` بسببٍ مكتوب
(§4.ak). فنقطةٌ جديدة لا تنفتح صامتةً ولا تختفي صامتة.

### ولا CI في هذا الريبو
لا ملفَّ workflow واحد. المراجعةُ الآلية الوحيدة **Codex Code Review** —
بلغت حدَّ استعمالها يوم ٢٠٢٦-٠٩-٢١ ثمّ **عادت تعمل في ٢٠٢٦-٠٩-٢٢**
(راجعت الطلب ٣٨١ وخرجت بلا ملاحظة)، **ثمّ بلغته ثانيةً يوم ٢٠٢٦-٠٩-٢٥** فلم تراجع
الطلب ٤١٢. **فالتحقّقُ مسؤوليتُك
أنت**: اختبارٌ حيٌّ على Postgres محلّي على **النقاط الحقيقية**، ومعه
**مراجعةٌ عكسية لكلّ حارسٍ جديد** — أزِله فتسقط تأكيداتُه **وحدها**، ثمّ
أعِده. وحارسٌ لا يُسقط شيئاً بإزالته لا يحرس شيئاً.

### قاعدةُ الاختبار المحلّية
تذهب مع إعادة تشغيل الحاوية، وتُبنى من الصفر: أنشئ قاعدةً ⟵
`DATABASE_URL=… npm run db:push` ⟵ شغّل الخادمَ مرّةً ليُطبِّق الترحيلات ⟵
ثمّ استعملها **قالباً** (`CREATE DATABASE x TEMPLATE …`) لكلّ حزمة اختبار.
**ولا تُلمَس قاعدةُ الإنتاج من بيئة التطوير إطلاقاً** (القسم ٨).

**وعلى قاعدةٍ فارغة يتعثّر `db:push` مرّتين، والمخرجُ معروف** (٢٠٢٦-٠٩-٢٤):
(١) `function patient_search_norm(text) does not exist` — فهرسٌ تعبيريٌّ يسبق
الدالّة، فأنشئ الدالّتين (`patient_search_norm` و`patient_digits_only`) من SQL
الترحيل ٠٥٤ أوّلاً؛ (٢) `no unique constraint matching given keys for
referenced table "patient_device_episodes"` — المفاتيحُ المركّبة تسبق قيدَي
`uq_pde_id_patient` و`uq_patient_cases_id_patient`، **والإعادةُ لا تُصلحه**:
خذ جملَ `drizzle-kit push --force --verbose` وطبّق ما فيه `UNIQUE(` قبل
غيره. ثمّ `runMigrations()` مرّةً (سكربتٌ صغير يستورد
`server/migrations/runner.ts`) — فتُسجَّل الترحيلاتُ كلُّها إلّا
`backfill_all_cases_v4` الذي يكتبه إقلاعُ الخادم وحده ولا تحتاجه الاختبارات.

---

## 1. ما هو المشروع

نظام إدارة مراكز الأطراف الصناعية والعلاج الطبيعي (Bionic Centers Manager).
- المالك: Dr Yasir (yasir.s81@gmail.com)
- اللغة الأساسية للواجهة: العربية (RTL).
- المنطقة الزمنية: بغداد (UTC+3).
- المستخدمون الفعليون: موظفو استقبال، مدراء فروع، ومسؤول واحد (admin).

### الوحدات الأساسية
- **المرضى** (`patients`) — بيانات شخصية، نوع الإصابة، الفرع، إجمالي التكلفة.
- **الزيارات** (`visits`) — كل جلسة علاج. مرتبطة بمريض وفرع. تحوي
  `treatment_type`، `session_count`، `cost`، `shift`، `created_by`.
- **الدفعات** (`payments`) — مدفوعات المريض، مع
  `payment_treatment_type` و `session_count`.
- **الفواتير** (`invoices`) + `invoice_items` — للجانب المحاسبي.
- **خطط التقسيط** (`installment_plans`).
- **المستخدمون** (`system_users`) — حسابات الموظفين بأدوار:
  `reception` / `branch_manager` / `admin`. كل حساب يحمل أعلام صلاحية
  دقيقة (مثل `can_delete_visits`, `can_edit_visits`, `can_delete_patients`،
  وغيرها).
- **سجل التدقيق** (`audit_log`) — يستقبل أحداث (create / update / delete)
  مع `entity_type`، `entity_id`، `user_id`، `ip_address`، `old_values`،
  `new_values`. ليست كل النقاط النهائية تكتب فيه — راجع "ثغرات معروفة".
- **تتبّع الجلسات اليومية** (`daily_sessions`, `session_counts`,
  `monthly_targets`) — إحصاءات الأجهزة الـ15 في كل فرع/شفت/يوم.
- **الكشف عن الشذوذ** (`anomaly_decisions` + `server/anomalies/detector.ts`)
  — يكشف فواتير متأخّرة، مرضى بلا دفعات، إلخ. قراءة فقط.
- **النسخ الاحتياطي البريدي** (`server/backup.ts`) — كرون يومي 23:55
  بغداد يرسل
  Excel بكل البيانات لبريد المالك.
- **تنبيهات تلغرام** (`server/notifications/telegram.ts`) — رسالة فورية
  للمالك عند تسجيل كل مريض جديد (الاسم/الفرع/النوع/المسجِّل، بلا هاتف).
  التوكن و chat id في `system_settings` تُدار من لوحة المسؤول (بطاقة
  «تنبيهات تلغرام» + زر رسالة تجريبية يستخرج chat id تلقائياً من
  getUpdates). الإرسال «أطلق وانسَ»: فشله لا يعطّل تسجيل المريض أبداً.
  **وهذا شأنٌ داخليّ للإدارة لا علاقة له بتواصل المرضى** (القسم 4.c):
  تلغرامُ المرضى تقاعد، وهذه باقيةٌ كما هي.
- **عملياتُ «بلا معاينة»** (`server/pending_charges/`, ترحيل ٠٦٧) —
  الاستقبالُ يُنجز البيعَ أو الصيانةَ **ويقيّد مبلغَها معها بحفظٍ واحد**
  (٠٦٩/٠٧٠)، **وصفرُ سلطةٍ للطبيب**: لا معاينةَ يُوقّعها، ولا مراجعةَ
  إشرافيةً تصله عن أيٍّ منهما بعد اليوم (كانت تصله استرجاعياً، تقاعدت
  للصيانة في القسم 4.j ولبيع الجزء في القسم 4.k). وجدولُ
  `pending_service_charges` **تاريخٌ لا مسار**: لا صفَّ جديد يدخله — يبقى
  مقروءاً وقابلاً للحسم بمساره القائم وحده. التفصيل في الأقسام 4.e، 4.j، 4.k.
- **سلّة المرضى** (`server/patients/`, ترحيل ٠٦٨) — **الحذفُ العاديّ لم يعد
  يهدم شيئاً**: حالةُ حذفٍ على صفّ المريض ومهلةُ استعادةٍ ثلاثين يوماً، ولا
  صفَّ تابعاً يُمَسّ. والكاسكيدُ الهادمُ بابُه الوحيد «حذف نهائي» للمسؤول
  العام. التفصيل في القسم 4.g.
- **تواصل المريض** (`server/patient_whatsapp/`) — **واتساب حصراً**: حفظُ
  المريض يستحقّ رسالةَ ترحيبٍ على رقمه المسجَّل، وتحديثاتُ التصنيع تتبعها.
  التفصيل في القسم 4.c.

### حماية بيانات الزيارات (مهمّة بعد حادثة 2026-06-08)
- **حذف ناعم**: `visits.deleted_at`. كل القراءات تصفّي
  `IS NULL`. `storage.deleteVisit` يحدّث العمود بدل
  `DELETE FROM`، فالضغطة الخاطئة لا تمحو الصف.
- **جدول جنائي**: `visits_forensic_log` يستقبل صورة كاملة من كل صف
  يُحذف فعلياً من `visits` (الكاسكيد عند حذف مريض مثلاً) مع معلومات
  جلسة Postgres (current_user, application_name, client_addr).
- **ترِكر**: `trg_log_visit_delete BEFORE DELETE ON visits` يكتب في
  الجدول الجنائي مهما كان مصدر الحذف (تطبيق، SQL مباشر من Neon Console،
  أي شيء).
- **تسجيل تدقيق لحذف المريض**: `DELETE /api/patients/:id` يكتب الآن في
  `audit_log` قبل تنفيذ الكاسكيد. قبل اليوم كان الحذف صامتاً تماماً.

---

## 2. البنية التقنية

- **الخلفية**: Node 20 + Express + TypeScript + Drizzle ORM.
- **قاعدة البيانات**: PostgreSQL على Neon (production branch).
- **الواجهة**: React + Vite + TanStack Query + Radix UI + Tailwind.
- **النشر**: Render Web Service (auto-deploy على كل دمج إلى `main`).
- **التحقّق من المخطّط**: Drizzle Kit (`npm run db:push` للتطوير).
- **الـ migrations**: ملفات في `server/migrations/`، يشغّلها
  `server/migrations/runner.ts` عند بدء الخادم. كل migration **يجب**
  أن يكون idempotent (يستخدم `IF NOT EXISTS` ونحوه).
- **الجلسات**: `connect-pg-simple` على نفس قاعدة البيانات
  (`sessions` table).
- **المصادقة**: جلسة `branchSession` على `req.session` تحوي
  `userId`, `branchId`, `displayName`, `isAdmin`, `permissions`.

### بيئة عمل وحيدة
- Render خلف Cloudflare، لذلك `req.ip` يُظهر حافة Cloudflare لا IP
  المستخدم الحقيقي. (لم نفعّل قراءة `X-Forwarded-For` بعد).
- ريبلت سابقاً كان يستضيف المشروع، الآن **متوقّف** (يتطلّب تجديد).
  ملف `.replit` ومجلّد `server/replit_integrations/auth/` لا يزالان
  في الريبو لأن المصادقة بُنيت فوقهما؛ يمكن إعادة تسميتها لاحقاً.

---

## فهرسُ الأقسام المنقولة — `docs/claude/`

> **لا تُحمَّل تلقائياً** — كي تبقى كلُّ محادثةٍ خفيفة. **اقرأ ملفَّ القسم قبل أن تعمل في
> مجاله**، والطويلُ منها يُقرأ الجزءُ الذي تحتاجه (عمودُ «الأسطر»). وكلُّ إشارةٍ هنا أو في
> الشيفرة أو في ملفّات المجلّد إلى «§4.x» أو «القسم 4.x» تعني `docs/claude/4.x.md`. ولموضوعٍ
> لا تعرف قسمَه: `grep -rn "الكلمة" docs/claude/`.

| القسم | الموضوع | الأسطر | الملفّ |
|---|---|---|---|
| 3 | أحداث جلسة 2026-06-08 | 59 | `docs/claude/3.md` |
| 4 | حادثة فقدان زيارات حميد ذياب سبع | 53 | `docs/claude/4.md` |
| 4.b | معاينة الطبيب (migration 028) | 961 | `docs/claude/4.b.md` |
| 4.c | تواصل المريض — واتساب، وحدها، وتلقائياً (2026-08-22) | 230 | `docs/claude/4.c.md` |
| 4.d | مسارُ العملية — والسؤالُ الذي كان يُجاب عنه بالخطأ (ترحيل ٠٦٥ — 2026-08-24) | 96 | `docs/claude/4.d.md` |
| 4.f | المرحلةُ الثانية — تفاصيلُ البيع ومالكُ كلّ حقل (ترحيل ٠٦٦ — 2026-08-24) | 122 | `docs/claude/4.f.md` |
| 4.e | مسارُ «بلا معاينة» — العمليةُ والمالُ من الاستعلامات (ترحيل ٠٦٧ — 2026-08-24، والقاعدةُ صُحِّحت 2026-08-26) | 307 | `docs/claude/4.e.md` |
| 4.g | سلّةُ المرضى واستعادتُهم خلال ثلاثين يوماً (ترحيل ٠٦٨ — 2026-08-24) | 156 | `docs/claude/4.g.md` |
| 4.h | معاينةٌ طبّيةٌ محضة — بلا مسؤوليةٍ تجارية على الطبيب (2026-08-28) | 106 | `docs/claude/4.h.md` |
| 4.i | تبسيطُ بيع الاستعلامات — بابٌ واحد «إتمام البيع» (2026-08-28) | 211 | `docs/claude/4.i.md` |
| 4.j | تبسيطُ الصيانة — عمليةٌ وحيدة، حفظٌ وحيد، بلا طبيب (ترحيل ٠٦٩ — 2026-08-28) | 218 | `docs/claude/4.j.md` |
| 4.k | تبسيطُ بيع الجزء — حفظٌ واحد يفتح الحلقةَ ويبيعها معاً، بلا طبيب (ترحيل ٠٧٠ — 2026-08-28) | 172 | `docs/claude/4.k.md` |
| 4.l | طابورُ «بانتظار الحسم / تم الحسم» — تبسيطُ الشريط الجانبي (٢٠٢٦-٠٨-٢٨) | 244 | `docs/claude/4.l.md` |
| 4.m | المراجعةُ اليومية — سردٌ إشرافيٌّ مُشتَقّ، لا مصدرَ حقيقةٍ جديد (٢٠٢٦-٠٨-٣٠) | 56 | `docs/claude/4.m.md` |
| 4.n | المساعد الذكي — نسخةٌ ثانية موثوقة: معرفةٌ يديرها المسؤول، وتقاريرُ مُحسَّبة في الخادم (٢٠٢٦-٠٩-٠٩) | 445 | `docs/claude/4.n.md` |
| 4.o | التحقّق الإنتاجيُّ الأوّل لِـ AI Assistant v2 — أربعةُ إصلاحات (٢٠٢٦-٠٩-٠٩) | 117 | `docs/claude/4.o.md` |
| 4.p | هويّةُ الجهاز في توقيع المعاينة — المرحلة الأولى من قطار الإصلاح (ترحيل ٠٧٧ — ٢٠٢٦-٠٩-١٢) | 101 | `docs/claude/4.p.md` |
| 4.q | مواصفاتُ كلّ جهازٍ من معاينته هو، وإلغاءُ القديمة لا يُرجع جهازاً تحكمه أحدث — المرحلة الثانية من قطار الإصلاح (٢٠٢٦-٠٩-١٢) | 145 | `docs/claude/4.q.md` |
| 4.r | السقالةُ تُسحَب والتاريخُ يُحمى — المرحلة الثالثة من قطار الإصلاح (ترحيل ٠٧٩ — ٢٠٢٦-٠٩-١٣) | 299 | `docs/claude/4.r.md` |
| 4.s | «إلغاء المعاينة» — طلبٌ لم تبدأ معاينتُه يُسحَب من «معايناتي» (٢٠٢٦-٠٩-١٣) | 138 | `docs/claude/4.s.md` |
| 4.t | إتاحةُ الملفّ لفروعٍ إضافية — بديلُ «نقل المريض» (ترحيل ٠٨٠ — ٢٠٢٦-٠٩-١٤) | 709 | `docs/claude/4.t.md` |
| 4.u | العمليةُ المعلَّقة تُختار، لا تُنتقى صامتاً (٢٠٢٦-٠٩-١٥) | 63 | `docs/claude/4.u.md` |
| 4.v | «إلغاء الحسم» — بابُ خروجٍ محايد من «بانتظار الحسم» (ترحيل ٠٨١ — ٢٠٢٦-٠٩-١٥) | 229 | `docs/claude/4.v.md` |
| 4.w | متابعةٌ بلا هويّة جهاز تُباع ولو كان للمريض أجهزةٌ أخرى مفتوحة (٢٠٢٦-٠٩-١٥) | 65 | `docs/claude/4.w.md` |
| 4.x | الطبيبُ يبدّل الاختصاصَ على طلبِ جهازٍ مُرسَل (٢٠٢٦-٠٩-١٥) | 90 | `docs/claude/4.x.md` |
| 4.y | تبديلُ النوع يصحّح الطلبَ نفسَه ولا يختطف طلباً مستقلّاً (٢٠٢٦-٠٩-١٦) | 684 | `docs/claude/4.y.md` |
| 4.z | أوامرُ صيانةٍ متزامنة بلا حدّ (ترحيل ٠٨٢ — ٢٠٢٦-٠٩-١٧) | 197 | `docs/claude/4.z.md` |
| 4.aa | إكمالُ ملفّ الصيانة — تنبيهُ التشابه والضمان (ترحيل ٠٨٣ — ٢٠٢٦-٠٩-١٨) | 121 | `docs/claude/4.aa.md` |
| 4.ab | المحذوفُ خارجَ منعِ تكرار التسجيل (٢٠٢٦-٠٩-١٨) | 205 | `docs/claude/4.ab.md` |
| 4.ac | عدُّ المبيعات يُفصَّل بالأصناف — لا رقمٌ إجماليٌّ واحد (٢٠٢٦-٠٩-٢١) | 69 | `docs/claude/4.ac.md` |
| 4.ad | بطاقةُ «معايناتي» على الهاتف — وشارةٌ كانت تختفي (٢٠٢٦-٠٩-٢١) | 54 | `docs/claude/4.ad.md` |
| 4.ae | مسوّدةُ المساعد تُقاس لحظةَ تركيب المربّع، لا عند تغيّر المحتوى وحده (٢٠٢٦-٠٩-٢١) | 54 | `docs/claude/4.ae.md` |
| 4.af | الإشعاراتُ على الهاتف — موضعٌ لا يحجب زرّاً، ومهلةٌ تنتهي (٢٠٢٦-٠٩-٢١) | 77 | `docs/claude/4.af.md` |
| 4.ag | سجلُّ محادثات المساعد — يُحفَظ ويُقرأ (ترحيل ٠٨٤ — ٢٠٢٦-٠٩-٢١) | 342 | `docs/claude/4.ag.md` |
| 4.ah | تعريفُ صاحب المراكز — مقالةُ معرفةٍ واحدة (ترحيل ٠٨٥ — ٢٠٢٦-٠٩-٢١) | 176 | `docs/claude/4.ah.md` |
| 4.ai | الجلسةُ المُهداة تُحتسب، والمالُ لا يتحرّك (ترحيل ٠٨٧ — ٢٠٢٦-٠٩-٢١) | 785 | `docs/claude/4.ai.md` |
| 4.aj | تحريرُ كلفة الحالة على الهاتف — أهدافُ لمسٍ حقيقية (٢٠٢٦-٠٩-٢١) | 41 | `docs/claude/4.aj.md` |
| 4.ak | قدراتُ القراءة — المساعدُ يسأل التطبيقَ بجلسة السائل (٢٠٢٦-٠٩-٢٢) | 148 | `docs/claude/4.ak.md` |
| 4.am | أرضيّةُ الأجهزة الباقية — التصحيحُ لا يسحب مالَ جهازٍ لم يُلغَ (٢٠٢٦-٠٩-٢٣) | 105 | `docs/claude/4.am.md` |
| 4.an | التحديثُ الحيّ — قانونٌ واحد بدل ٢٣٤ قائمة (٢٠٢٦-٠٩-٢٣) | 604 | `docs/claude/4.an.md` |
| 4.ao | شرائطُ لوحة التصنيع تُضغَط فتُرشِّح (٢٠٢٦-٠٩-٢٤) — وتكملتُها: التنبيهاتُ تنقسم كاللوحة وعمودُ «إعادات العمل» (٢٠٢٦-٠٩-٢٥) — ومريضُ السلّة خارجَ مربّعاتها | 767 | `docs/claude/4.ao.md` |
| 4.al | ملفُّ المريض يقول أيُّ عمليةٍ وأيُّ مال (٢٠٢٦-٠٩-٢٣) | 116 | `docs/claude/4.al.md` |
| — | سجلُّ «آخرُ ما دُمج» الأقدم | 344 | `docs/claude/changelog.md` |

---

## 5. ثغرات معروفة لم تُعالج بعد

- **`req.ip` يُظهر حافة Cloudflare**: نحتاج `app.set('trust proxy', N)`
  مع N متوافق مع Cloudflare وقراءة `X-Forwarded-For`.
- **عناصر ريبلت**: `.replit`, `server/replit_integrations/auth/`
  لا تزال موجودة. الكود يعمل لكن التسمية مضلّلة بعد الانتقال إلى
  Render.
- ~~**`deletePatient` cascade ضخم وصامت سابقاً**~~ — **عولج في ترحيل ٠٦٨**:
  الحذف العادي صار سلّةً بمهلة استعادة ثلاثين يوماً، والكاسكيد بقي كما هو
  خلف باب «حذف نهائي» للمسؤول العام وحده. القسم 4.g.
- **توحيد الجلسات والمصادقة بين Render وأي مثيل سابق**: حالياً
  لا يوجد فرض على عدد المثيلات، فإذا تعدّدت عمّال Render قد ترى
  تضارباً (نظرياً).
- **مهلة إعادة النشر**: PR #74 لا يحوي اختبارات؛ كل التحقّق يدوي
  عبر استعلامات Neon.

---

## 6. الأماكن المهمّة في الريبو

```
docs/claude/             # تاريخُ الأقسام ٣ و٤ — ملفٌّ لكلّ قسم، والفهرسُ بعد القسم ٢ هنا.
server/
  index.ts               # نقطة البدء؛ يشغّل migrations ثم registerRoutes ثم cron
  routes.ts              # كل نقاط REST. ضخم (~4900 سطراً).
  storage.ts             # جميع عمليات Drizzle. ضخم. يحوي deletePatient cascade.
  db.ts                  # pg pool + Drizzle client + معالج الأخطاء.
  backup.ts              # كرون النسخة اليومية بالبريد.
  accounting/
    ledger.ts            # logAudit() يكتب في audit_log.
    routes.ts            # نقاط محاسبية.
  anomalies/
    detector.ts          # كشف الشذوذ (قراءة فقط).
  patients/
    active_patient.ts    # «المريض الفعّال» = deleted_at IS NULL — تعريفٌ واحد يُستورَد.
    trash_store.ts       # الحذف الناعم والاستعادة والحذف النهائي (٠٦٨).
    trash_routes.ts      # /api/patient-trash — السلّة والاستعادة والهدم.
  medical/
    store.ts             # طبقة بيانات المعاينات (إدراج فقط، لا تعديل ولا حذف).
    routes.ts            # نقاط المعاينة + الملاحق + قائمة الانتظار.
  ai/
    provider.ts          # مزوّد Claude، aiComplete/aiToolStep، بلا صلاحياتٍ يعرفها.
    access.ts             # resolveAiAccess — من الجلسة وحدها، بلا شبكة.
    chat.ts               # حلقة المحادثة (وضعان: عام/ماليّ) + الاسترجاع.
    tools/
      registry.ts          # السجلّ المقفَل — سبعُ أدوات قراءةٍ فقط.
      reports.ts           # operational_summary/financial_summary — أرقامٌ محسوبةٌ في الخادم.
    knowledge/
      store.ts             # المعرفة الموثوقة + اقتراحات الموظّفين (٤.n).
      retrieval.ts          # الاسترجاعُ الحيّ فوق shared/ai_knowledge_retrieval.ts.
      routes.ts             # /api/ai/knowledge/* — تقديمٌ للجميع، إدارةٌ للمسؤول العام.
    conversations/
      store.ts             # سجلُّ المحادثات (٠٨٤، ٤.ag) — الكتابةُ الوحيدة recordExchange.
      routes.ts            # /api/ai/conversations/* — **قراءةٌ محضة**، لا كتابةَ فيه.
      cleanup.ts           # كرونُ محو التسعين يوماً — مستقلٌّ عن backup.ts.
    semantics.ts          # تسمياتٌ عربية للأدوات — فوق shared/manufacturing.ts، لا تسميةٌ ثانية.
  sessions_module/
    routes.ts            # تتبّع الجلسات اليومية لكل جهاز.
  replit_integrations/
    auth/                # المصادقة (مُعاد استخدامها بعد الانتقال).
  migrations/
    runner.ts            # يشغّل الـ migrations المرتّبة على البدء.
    001_..010_..., 011_visit_soft_delete_and_forensic.ts
shared/
  schema.ts              # Drizzle schemas + zod inserts. صدر الحقيقة لجميع الجداول.
client/src/
  hooks/use-patients.ts  # mutations: addVisit / updateVisit / deleteVisit
  pages/PatientDetails.tsx # صفحة المريض (تعرض الجلسات والمتبقّي).
.replit                  # إعداد ريبلت (المشروع قديماً عليه).
```

---

## 7. أوامر مفيدة

```bash
# تحقّق الأنواع (شيفرة التطبيق — خطّ الأساس ٣٥ خطأً معروفاً)
npx tsc --noEmit

# تحقّق أنواع ملفّات الاختبار — `tsconfig.json` يستثنيها فلا يراها الأمر
# أعلاه، وكسرٌ في ملفّ اختبار يجعل حزمتَه لا تعمل أصلاً بينما يبقى
# `npm run check` أخضر (وقع فعلاً من PR #358 إلى #364).
npm run check:tests

# تشغيل التطوير
npm run dev

# دمج تحديثات schema للقاعدة المحلية
npm run db:push

# عند ملاحظة سلوك غير مفهوم على الإنتاج، الاستعلامات الأولى:
# (تنفّذ في Neon Console > SQL Editor)
SELECT entity_type, action, COUNT(*) AS n, MAX(created_at) AS last
FROM audit_log
GROUP BY entity_type, action
ORDER BY entity_type, action;

SELECT * FROM visits_forensic_log
ORDER BY logged_at DESC LIMIT 50;
```

---

## 8. ملاحظات للمساعد التالي

- المالك يكتب بالعربية ويفضّل الردود بالعربية مع اقتباس أسماء التقنية
  بالإنجليزية. الأسلوب مباشر وموجز.
- **سياسة الدمج (أمر دائم من المالك 2026-06-08)**: عند الانتهاء من أي
  PR طلبه المالك، **ادمجه فوراً** بدون انتظار موافقة صريحة لكل مرة.
  المالك أعطى تفويضاً عاماً ولا يريد سؤاله في كل دمج. وأبلغ هذا
  للمساعدين القادمين عبر هذا الملف.
- **لا تنفّذ شيئاً مدمّراً على الإنتاج بدون إذن صريح**: مثل
  drop column, drop table, DELETE بدون WHERE.
- **قاعدة ملزمة (بعد حادثة «فشل في حذف المريض» 2026-07-26)**: أي جدول
  جديد يحمل مفتاحاً أجنبياً إلى `patients` أو إلى أحد توابعها
  (`patient_cases` وغيرها) **يجب** قبل الدمج: (١) إضافته إلى كاسكيد
  `storage.deletePatient` بترتيب المفاتيح الصحيح، و(٢) اختبار حذف مريض
  كامل يحمل صفوفاً في الجدول الجديد على Postgres محلي. النسيان يكسر
  الحذف لكل المستخدمين بمن فيهم المسؤول.
- إن طلب المالك تشخيص اختفاء بيانات: ابدأ بـ
  `audit_log` و `visits_forensic_log`. هما المصدران الرسميان الآن.
- الـ migrations تُشغّل تلقائياً عند بدء الخادم. لا حاجة لخطوة يدوية
  على Render.
- النشر يحدث تلقائياً عند الدمج إلى `main`، لكن **يمكن يدوياً** عبر
  Render Dashboard → Manual Deploy → Deploy latest commit.
- المالك ليس له شيل على Neon ولا psql محلياً (لاحظ Neon Console فقط).
  السكربتات المقترحة يجب أن تكون لصقاً مباشراً في SQL Editor.
- **قاعدةٌ ملزمة من المالك (٢٠٢٦-٠٩-٢٥) — ما يلصقه المالكُ يصله داخل صندوق
  شيفرةٍ في المحادثة، لا ملفّاً مرفقاً**: «لاترسل لي اوامر نيون بهذا الشكل اطلاقا
  التزم بارسالها داخل صندوق قابل للنسخ فقط وتذكر ذلك الامر جيدا لكل شي مستقبلا».
  فكلُّ SQL لـ Neon — وكلُّ أمرٍ أو سكربتٍ يُطلَب من المالك لصقُه أو تنفيذُه — يُكتب
  نصُّه **كاملاً** داخل كتلة شيفرةٍ مُسيَّجة في الردّ نفسِه، وكلُّ خطوةٍ في صندوقها،
  **ولا يُرسَل بـ`SendUserFile` ولا مرفقاً ولا رابطاً إلى ملفّ**. (وقعت المخالفةُ فعلاً
  مع سكربتَي استرجاع الأعذار فرفضها.) والملفُّ في الريبو يبقى مرجعاً يُقرأ من `main`
  قبل اللصق، **والصندوقُ هو ما يُسلَّم** — ولا يُختصَر سكربتٌ لطوله.
- **هذا الملفُّ يبقى قصيراً — قرارُ المالك ٢٠٢٦-٠٩-٢٥ («باقلّ توكنات ممكنة»)**: يُحمَّل كاملاً
  في بداية كلّ محادثة وبعد كلّ ضغط، فكلُّ سطرٍ فيه يُدفع ثمنُه في كلّ محادثة. فـ(١) **القسمُ
  الجديد يُكتب في ملفٍّ جديد** `docs/claude/4.xx.md` ويُضاف سطرُه إلى الفهرس — لا هنا؛
  و(٢) **تكملةُ قسمٍ قائم تُكتب في ملفّه هناك**؛ و(٣) **«آخرُ ما دُمج» في القسم ٠ مدخلان أو
  ثلاثة**، والأقدمُ يُنقل بحرفه إلى **أعلى** `docs/claude/changelog.md`. **ولا `@import`**
  لملفّات المجلّد: الاستيرادُ يُحمَّل مع هذا الملفّ فيُعيد المشكلةَ نفسَها. (المرجع:
  `code.claude.com/docs/en/memory` — «target under 200 lines per CLAUDE.md file» و«imported
  files still load and enter the context»، والملفُّ يُحمَّل كاملاً حتى 4 MiB.)
- **«سلّم» — تسليمُ العمل إلى محادثةٍ جديدة**: المحادثةُ الجديدة لا تبدأ بما دار في سابقتها بل
  بهذا الملفّ. فإذا كتب المالكُ «سلّم»: حدِّث القسمَ ٠ بما وصل إليه العمل — **ومنه ما لم يُدمَج
  وما ينتظر ردَّه** (سكربتٌ أُعطي له ولم يُشغَّل، سؤالٌ ينتظر جوابَه) — ثمّ ادمجه، ثمّ أخبره أنه
  يستطيع فتح المحادثة الجديدة.
