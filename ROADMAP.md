# خطة الطريق — Bionic Centers Manager

> هذا الملف هو المرجع الرسمي لتطوير النظام المحاسبي ومميزات الذكاء الاصطناعي.
> يُحدَّث مع كل تغيير كبير لضمان استمرارية العمل بين الجلسات.

---

## الوضع الحالي (2026-04-24)

النظام يعمل على الإنتاج من فرع `main` عبر
`Render`،
وقاعدة البيانات على
`Neon PostgreSQL`.

الإطلاق الفعلي للعمل: صباح السبت 2026-04-25.

---

## ما تم إنجازه

### المرحلة 1 — الأساس المحاسبي (مكتملة)

- **شجرة الحسابات** — 52 حساب مُصنَّف دولياً
  (أصول / خصوم / حقوق ملكية / إيرادات / مصاريف)
  مع حساب صندوق نقدي مخصّص لكل فرع
  `1111XX`
- **نظام القيود المزدوجة** —
  `journal_entries`
  +
  `journal_lines`
  مع تحقّق إلزامي من التوازن
- **الفترات المحاسبية** —
  `accounting_periods`
  مع إغلاق الفترة
- **سجل التدقيق** —
  `audit_log`
- **قيود تلقائية عند:**
  - تسجيل دفعة مريض → مدين صندوق / دائن إيراد (موزَّع حسب نوع الخدمة)
  - تسجيل مصروف → مدين مصروف / دائن صندوق
  - حذف أو تعديل دفعة/مصروف → قيد عكسي
- **تقارير كخدمات
  API:**
  ميزان المراجعة، قائمة الدخل، الميزانية العمومية
- **خريطة ربط الخدمات بالإيرادات:**
  - علاج طبيعي →
    `4100`
  - طرف صناعي →
    `4200`
  - مسند / مساند طبية →
    `4300`
  - خدمات إضافية →
    `4400`
  - غير ذلك →
    `4900`

### المرحلة 2.3 — دور المحاسب (مكتملة 2026-04-25)

- خيار "محاسب" مُضاف إلى قائمة الأدوار في إعدادات المسؤول
- صلاحيات افتراضية: عرض مرضى/تقارير، إضافة دفعات، إدارة محاسبة
- المدير يُعدّل أي صلاحية يدوياً عبر المبدّلات
- `branch_id` يُعيّن المحاسب لفرع واحد (الواجهة موجودة سابقاً)
- كل مسارات `/api/accounting/*` و `/api/expenses/*` فُتحت لمن لديه `canManageAccounting`
- الواجهة تُجبر فلتر الفرع تلقائياً لغير المسؤول

### الطبقة الثالثة — الذكاء الاصطناعي

#### 3.0 — البنية التحتية (مكتملة 2026-04-25)

- تثبيت `@anthropic-ai/sdk`
- وحدة `server/ai/provider.ts` فيها:
  - تهيئة كسولة (lazy) لعميل Claude
  - `isAiEnabled()` للتحقق من تواجد المفتاح
  - `aiComplete()` بنمط prompt caching على الـ system prompt
  - `safeAiComplete()` يُرجع نتيجة موسومة (success/disabled/rate_limit/api_error)
  - النموذج الافتراضي Haiku 4.5، مع إمكانية رفع لـ Sonnet 4.6
- مسار `GET /api/ai/status` للواجهة لفحص توفر الخدمة
- يحتاج المستخدم إضافة `ANTHROPIC_API_KEY` كمتغيّر بيئة في Render لتفعيل الميزات

#### 3.2 — كشف الشذوذ المحاسبي (مكتملة 2026-04-27)

- وحدة `server/anomalies/detector.ts` بمنطق قائم على القواعد (مجاني، سريع):
  - مصروف بمبلغ شاذ مقارنة بمتوسّط الفئة في الفرع (>5x median)
  - مصاريف مكرَّرة في نفس اليوم بنفس المبلغ والفئة
  - فواتير معلَّقة لأكثر من 30 يوماً
  - مرضى لديهم زيارات لكن لا دفعات مسجَّلة
- مسار `GET /api/anomalies` — يعمل بدون LLM، رخيص
- مسار `POST /api/ai/explain-anomaly` — يستدعي Haiku عند ضغط "اشرح بالذكاء" فقط
- تبويب "تنبيهات" في صفحة المحاسبة مع شارة عددية
- تصنيف بصري حسب الخطورة (أحمر/أصفر/أزرق)
- شرح
  AI
  مُخزَّن مؤقتاً بحيث لا يُعاد طلبه

#### الجولة الثالثة — أداء + تقييم موظفين (2026-04-28)

**أداء التطبيق**:
- هجرة `005_performance_indexes` تضيف فهارس على
  `branch_id + date`
  لكل من
  expenses / invoices / payments / purchases،
  وعلى
  patient_id
  للزيارات والدفعات. يقطع الاستعلامات الثقيلة من ثوانٍ إلى عشرات
  المللي ثانية.
- إصلاح
  N+1
  في كاشف الشذوذ:
  `detectPatientsWithoutPayments`
  كانت تستخدم استعلاماً منفصلاً لكل مريض (200 مريض = 400
  round-trip).
  الآن استعلامان مجمّعان فقط بصرف النظر عن العدد.
- ضبط
  TanStack Query
  `staleTime` من Infinity إلى 60 ثانية — يقلّل التحديثات المكرّرة.
- تقسيم الكود (code splitting) بـ
  `React.lazy`
  لصفحات
  Accounting / Statistics / Surveys / AdminSettings.
  الـ bundle الأساسي انخفض من 2.9MB إلى 842KB (gzip: 232KB).
  الصفحات الثقيلة تُحمَّل فقط عند زيارتها.

**نظام تقييم الموظفين**:
- استخدام جدول
  `audit_log`
  الموجود أصلاً
  (entityType / action / userId / branchId / oldValues / newValues)
  بدل إنشاء جدول جديد.
- إضافة حدث
  `login`
  إلى الجدول عبر
  `logAudit`
  لتتبّع نشاط الدخول.
- توسيع
  `getEmployeeAccuracy`
  بمقاييس جديدة:
  عدد التعديلات
  (editCount)،
  عدد عمليّات الحذف
  (deleteCount)،
  عدد تسجيلات الدخول
  (loginCount)،
  آخر نشاط
  (lastActivityAt)،
  ودرجة محسوبة (0-100).
- معادلة الدرجة:
  `النشاط (60 نقطة، لوغاريتمي) + الجودة (30 نقطة، تنخفض مع زيادة الأخطاء) + الانضباط (10 نقاط من تسجيلات الدخول)`.
- واجهة جديدة "دقّة وحركة الموظفين":
  بطاقة لكل موظّف بنقاطه واسمه ودوره وآخر نشاط، مع 8 صناديق
  مقاييس
  (مصاريف/فواتير/مشتريات/إجمالي/تعديلات/حذف/تنبيهات/دخول).
  ألوان دلاليّة بحسب الدرجة (ممتاز/جيّد/مقبول/يحتاج متابعة).
  قسم تفسيري شفّاف يشرح كيف تُحسَب النقاط.

#### إصلاحات الجولة الثانية (2026-04-28)

- **المساعد الذكي يعرف الفرع**: كل فاتورة في
  `outstandingInvoices.sample`
  داخل الـ
  snapshot
  أصبحت تحوي
  `branchName`
  صراحةً، مع تعليمة في
  system prompt
  تشير إلى الحقل. الآن السؤال "في أيّ فرع هذا المريض؟" يُجاب عليه فوراً.
- **التدقيق الذكي بدون "تعذّر"**:
  - تحوّل من
    Sonnet 4.6
    إلى
    Haiku 4.5
    (أكثر ثباتاً للمخرجات المنظّمة).
  - استخدام تقنية
    "prefilling"
    عبر إضافة دور
    assistant
    بقيمة
    `{`
    لإجبار النموذج على البدء بـ JSON مباشرة.
  - رفع
    `maxTokens`
    إلى 2000 لتفادي القطع.
  - تسجيل الإخراج الخام عند فشل
    parse
    لأوّل ١٠٠٠ حرف لتسهيل التشخيص لاحقاً عبر سجلّات Render.
- **تطوير شامل لنظام تنبيهات المراجعة**:
  - إعادة تسمية الأزرار إلى الأكثر وضوحاً:
    - "مراجَع" → "صحيح، أخفه شهراً" بأيقونة خضراء وحدّ سفلي أخضر.
    - "ليس خطأ" → "ليس خطأ، أخفه نهائياً" بأيقونة
      X
      تشير للحذف الدائم.
  - تحديث الواجهة فوريّ
    (optimistic update)
    عبر
    `optimisticallyDismissed`:
    البطاقة تختفي والشارة تنقص فوراً عند الضغط، دون انتظار الخادم.
    عند فشل الحفظ يُعاد كل شيء.
  - قسم جديد قابل للطيّ بعنوان "قرارات سابقة على التنبيهات":
    - يعرض كل القرارات الفعّالة (لم تنته صلاحيّتها).
    - لكل قرار:
      نوع التنبيه + المصدر + من اتّخذ القرار + تاريخ الانتهاء + السبب
      المكتوب (إن وُجد).
    - زرّ "تراجع" يحذف القرار فيظهر التنبيه مجدّداً.
  - نقاط نهاية جديدة:
    `GET /api/anomalies/decisions`
    و
    `DELETE /api/anomalies/decisions/:id`
    مع عزل فروع صارم على كليهما.

#### إصلاحات ما بعد الإطلاق (2026-04-28)

- **شفافية الطبقات العائمة**: متغيّرات
  `--card`
  و
  `--popover`
  لم تكن معرّفة في
  `index.css`،
  فكان كل
  `bg-card`
  و
  `bg-popover`
  يُحلّ إلى
  `hsl( / 1)`
  أي شفّاف. أُضيفت تعريفات بيضاء صريحة فأصبح
  درج المساعد الذكي وقائمة بحث المريض يظهران بخلفية صلبة.
- **بطاقة المريض في الفاتورة**: عند اختيار المريض تظهر بطاقة
  بارزة كاملة المعلومات (اسم، رقم، هاتف، عمر، فرع، عنوان،
  أنواع علاج)، مسحوبة من سجلّ المريض مباشرة، غير قابلة للتعديل
  ومُلوّنة بإطار
  primary
  لتؤكّد أنّها رسميّة.
- **تحليل ردّ التدقيق المحاسبي**: parser الجديد يحاول
  parse مرّتين (نصّ كامل، ثمّ شريحة بين أوّل
  `{`
  وآخر
  `}`).
  عند الفشل يعيد summary رسالة لطيفة بدل خطأ 502 كي لا تنكسر الواجهة.
  system prompt شُدّد لإلزام النموذج بـ JSON فقط.
- **رسائل خطأ المساعد على المصاريف**: تتعامل مع 503/502
  وتعرض رسالة عربية واضحة بدل "خطأ في الاتصال".
- **مدير الفرع =
  admin
  داخل فرعه**: عند تسجيل دخول مستخدم بدور
  `branch_manager`
  يحصل تلقائياً على كل
  permissions
  كاملة (canManageAccounting، canManageUsers، canDeletePayments…)
  بصرف النظر عمّا في صفّه. علم
  `isAdmin`
  يبقى
  `false`
  فلا يستطيع رؤية فروع أخرى أو تعديل إعدادات نظام عامة، لكنّ كل
  العمليات داخل فرعه مفتوحة له. النموذج في إعدادات المسؤول
  يُظهر القيم الافتراضيّة كاملة.

#### 3.8 — الردّ التلقائي على الاستبيانات (مكتملة 2026-04-28)

- وحدة `server/ai/survey_reply.ts` تجمع استجابة استبيان (الإجابات +
  أسئلتها + النوتات + النسبة المئوية) وترسلها لـ
  Haiku 4.5
  مع system prompt
  يُلزم النموذج بالردّ كـ
  JSON صلب
  يحوي `summary` و `draftReply`.
- النبرة المُطلوبة:
  عربية فصحى بسيطة، 4-6 جمل، شكر صادق + اعتذار
  محدّد + إجراء ملموس + ترحيب باستفسارات.
- defensive parsing مع رسالة خطأ واضحة عند فشل النموذج.
- نقطة نهاية:
  `POST /api/ai/survey-reply` تتطلّب
  `canManageSurveys`
  أو
  `canManageAccounting`؛
  عزل فروع صارم — المحاسب لا يقدر يطلب ردّاً لاستبيان من خارج فرعه.
- في حوار تفاصيل الاستبيان (تبويب "النتائج" في صفحة الاستبيانات):
  زرّ "اقترح ردّاً" يولّد المسوّدة، ثمّ
  Textarea
  قابل للتحرير مع زرّ نسخ.
  زرّ "أعد التوليد" بعد أوّل توليد.
- المسوّدة دائماً تُراجَع بشريّاً قبل الإرسال — لا إرسال تلقائي.
- التكلفة: <$0.10/شهر (50 استبيان سلبي/شهر تقدير).

#### 3.7 — مراقبة دقّة الموظفين (مكتملة 2026-04-28)

- إصلاح نظام تتبّع المُنشِئ:
  POST
  endpoints
  للمصاريف والفواتير والمشتريات الآن تستخدم
  `branchSession.userId.toString()`
  بدل
  `user.claims.sub`
  (الذي كان فارغاً في معظم الحالات).
- منهج بدون درجة دقّة موحّدة (هذا يكون مضلّلاً بدون سجلّ تعديلات/حذف).
  بدلاً عنه، نعرض البيانات الخام للمسؤول ليحكم:
  - عدد المصاريف وإجمالي مبالغها
  - عدد الفواتير وإجمالي مبالغها
  - عدد المشتريات وإجمالي مبالغها
  - عدد قرارات التنبيه التي اتّخذها (مؤشّر حِمل عمل)
- نقطة نهاية:
  `GET /api/admin/employee-accuracy?days=30&branchId=…`
  - مسؤول فقط
  - تجمع ٧/٣٠/٦٠/٩٠/١٨٠ يوماً
- تبويب جديد في إعدادات المسؤول: "دقّة الموظفين"
  - جدول مرتّب تنازلياً بإجمالي الإدخالات
  - تحذير صريح أن البيانات قبل هذا التحديث ستظهر تحت "غير معروف"
- التكلفة: $0/شهر (rule-based بالكامل في v1).
  AI ملخّصات سرديّة شهرية يمكن إضافتها لاحقاً مقابل ~$0.10/شهر.

#### 3.6 — تدقيق محاسبي ذكي (مكتملة 2026-04-28)

- وحدة `server/ai/smart_audit.ts` تكمّل (لا تكرّر) كاشف الشذوذ المباشر:
  - 3.2: قواعد لكل معاملة، فوريّ على لوحة القيادة، مجاني.
  - 3.6: تحليل توزيعات وأنماط عبر نافذة 30/60/90 يوماً، مرّة دوريّة.
- snapshot شامل يحتوي:
  - الإجماليات اليوميّة (إيرادات/مصاريف/صافي)
  - توزيع المصاريف على الفئات + حصّة كل فئة
  - متوسط الفاتورة، نسبة التحصيل، معدّل الخصم
  - شيخوخة الذمم في 3 شرائح: 0-30 / 31-60 / 61+ يوم
  - تركّز الموردين (أعلى 5)
  - توزيع توقيت الدفعات (فجر / مساء متأخّر / عطلة)
- يستدعي
  Sonnet 4.6
  مع system prompt يطلب
  JSON
  منظّم لـ
  findings: [{severity, category, title, description, suggestedAction}]
- التحليل الدفاعي: parsing مع fallback لـ
  findings فارغة عند فشل التحليل.
- caching بـ
  6 ساعات
  لكل (فرع، نافذة) لتفادي الاستدعاء المتكرّر.
- بطاقة `SmartAuditCard` في تبويب "التحليلات" بألوان دلاليّة
  (عالية=أحمر، متوسطة=كهرماني، منخفضة=أزرق)
  وزرّ "إجراء مقترح" واضح لكل ملاحظة.

#### 3.5 — تقارير سردية شهرية (مكتملة 2026-04-28)

- وحدة `server/ai/monthly_report.ts` تبني snapshot رقمياً غنياً لشهر
  محدّد لفرع معيّن (أو لكل الفروع للمسؤول): إيرادات/مصاريف/فواتير/مرضى
  جدد + مقارنة كاملة بالشهر السابق مع نسب التغيّر.
- نقطة نهاية `POST /api/ai/monthly-report` تستدعي
  Sonnet 4.6
  (جودة سرديّة عالية مهمّة هنا) لتوليد تقرير منظّم بالعربية الفصحى:
  ملخّص تنفيذي → أداء مالي → فواتير وذمم → أكبر بنود مصاريف → حركة عامّة
  → ملاحظات وتوصيات.
- caching داخلي على الخادم (LRU، 50 إدخال) مفتاحه
  `branchId:month` —
  الشهر المغلق ثابت، فالتوليد لمرّة واحدة لكل تركيب.
- منع توليد تقارير لشهر مستقبلي.
- بطاقة `MonthlyNarrativeCard` في تبويب التقارير:
  منتقي شهر + منتقي فرع (للمسؤول فقط) + زرّ "توليد التقرير".
- عرض النصّ بتنسيق منظّم
  (NarrativeBody)
  مع زرّ نسخ.
- التكلفة المتوقّعة: ~$0.10-0.20/شهر لـ 5-10 تقارير.

#### 3.3 — إرشاد الموظف لحظياً (مكتملة 2026-04-28)

- نقطة نهاية `GET /api/patients/:id/financial-summary` تعيد لكل مريض:
  عدد الفواتير غير المدفوعة، إجمالي المتبقّي، عدد المتأخّرة (>30 يوماً)،
  وتاريخ أقدم فاتورة غير مدفوعة. تطبّق عزل الفروع.
- عند اختيار المريض في حوار إنشاء الفاتورة، تظهر بطاقة تحذير
  `PatientFinancialChip`
  تخبر المحاسب بوجود ذمم مستحقّة وتقترح تسجيل الدفعة على الفاتورة
  القديمة بدل إنشاء فاتورة جديدة. يُخفى تلقائياً عند عدم وجود ذمم.
- نقطة نهاية `POST /api/guidance/expense` تعيد قائمة ملاحظات قصيرة
  للوصف/المبلغ/الفئة المُدخَلة. القواعد الافتراضية مجاناً (rule-based)
  تغطّي:
  وصف خدمات دورية بدون رقم فاتورة، مبالغ كبيرة بوصف قصير، فئة فارغة،
  وصف مشتريات يفترض تسجيله في تبويب المشتريات.
- نمط هجين: `useAi: true` يُفعّل استدعاء Haiku خفيف لاقتراحات إضافية
  لكنّه مُعطَّل افتراضياً للحفاظ على التكلفة عند صفر.
- لوحة `ExpenseHintsPanel` تحت حقل الوصف تستهلك النتائج مع
  debounce 350ms. تختفي تماماً عند انعدام الملاحظات.

#### 3.1 — مساعد دردشة ذكي (مكتملة 2026-04-28)

- نقطة نهاية جديدة `POST /api/ai/chat` تستقبل سجلّ المحادثة وتعيد ردّاً
  بالعربية مبنياً على لقطة مالية فورية للفرع.
- اللقطة (snapshot) تُبنى من بيانات حقيقية: إيرادات ومصاريف آخر 30 يوماً،
  دفعات آخر 7 أيام، فواتير مستحقة، أكبر فئات المصاريف، رصيد القاصة اليوم.
- اعتماد بنية snapshot-in-prompt بدلاً من tool-use — أبسط وأرخص وأقل
  تأخّراً، ومناسب لأسئلة المدير المعتادة.
- عزل الفروع: المحاسب لا يرى إلا بيانات فرعه؛ المسؤول يرى الكل.
- زرّ عائم في كل صفحات النظام (بعد بوّابة الفرع) يفتح درجاً جانبياً
  للمحادثة، يخفي نفسه تلقائياً عند غياب مفتاح
  `ANTHROPIC_API_KEY`
  أو غياب صلاحية المحاسبة.
- اقتراحات أسئلة جاهزة في الواجهة لتقليل الاحتكاك
  ("كم بلغت الإيرادات هذا الشهر؟"، "من المرضى الذين عليهم ذمم متأخّرة؟").
- caching للـ system + snapshot لتقليل التكلفة على الأسئلة المتتابعة.

#### 3.2.1 — ذاكرة الذكاء الاصطناعي (مكتملة 2026-04-27)

- هجرة جديدة `004_ai_memory` تُنشئ جدولَي:
  - `anomaly_decisions` — قرارات المحاسب على التنبيهات
  - `ai_memory_notes` — ملاحظات سياقية يكتبها المسؤول
- المُكتشف يتجاهل تلقائياً التنبيهات المُقرَّر بشأنها
- المفسِّر يقرأ ملاحظات الذاكرة المتعلّقة ويمرّرها للنموذج كسياق
- زرّان جديدان على كل تنبيه: "✓ مراجَع" و "ليس خطأ"
- "مراجَع" يُخفي لمدة 30 يوماً، "ليس خطأ" يُخفي دائماً
- "ليس خطأ" يفتح حواراً لكتابة سبب اختياري
- تبويب جديد في إعدادات المسؤول: **"ذاكرة الذكاء"**
- إدارة ملاحظات بنطاق (عام/مصاريف/فواتير/مرضى) وفرع وفئة
- المسؤول فقط يُضيف/يعدّل/يحذف؛ المحاسب يقرأ
- النتيجة: نظام يتعلّم سياق العمل من المسؤول دون أيّ
  ML حقيقي

#### 3.4 — التصنيف التلقائي للمصاريف (مكتملة 2026-04-25)

- وحدة `server/ai/categorize.ts` تُصنّف وصف المصروف لإحدى 12 فئة محدّدة
- نظام prompt محكم بالعربية الفصحى مع إرشادات للهجة العراقية
- مسار `POST /api/ai/categorize-expense` (محاسب أو مدير)
- زر "اقتراح بالذكاء" في نموذج إضافة المصروف بجوار قائمة الفئات
- يظهر فقط عند تفعيل الـ AI server-side
- توسيع قائمة `EXPENSE_CATEGORIES` في الواجهة من 6 إلى 12 فئة لتطابق الباك إند

### المرحلة 2.5 — نظام الموردين والمشتريات الآجلة (مكتملة 2026-04-25)

- جدولان جديدان: `vendors` (إدارة الموردين) و `purchases` (المشتريات)
- مسارات
  `CRUD`
  للموردين والمشتريات + تسجيل دفعات للموردين
- قيود محاسبية تلقائية:
  - شراء آجل: مدين مصروف / دائن ذمم دائنة
    `2110`
  - شراء نقدي: مدين مصروف / دائن صندوق
  - دفعة لمورد: مدين ذمم دائنة / دائن صندوق
- تبويب جديد "الموردون" في صفحة المحاسبة
- جدول بسيط للموردين مع إضافة/تعديل/إلغاء تفعيل
- جدول للمشتريات مع: إضافة، دفع جزئي/كامل، حالة (معلق/جزئي/مدفوع)
- المحاسب يستطيع إضافة وتسجيل الدفعات؛ التعديل والحذف للمسؤول فقط
- تصفية الفرع تلقائياً للمحاسب على مستوى الخادم

### المرحلة 2.4 — الملخّص اليومي للقاصة + تصدير `PDF` (مكتملة 2026-04-25)

- مسار `GET /api/accounting/daily-summary?date=&branchId=` يحسب:
  - وارد اليوم (مع توزيع حسب نوع الخدمة)
  - مصاريف اليوم (مع توزيع حسب الفئة)
  - صافي اليوم
  - رصيد القاصة من أمس (محسوب تلقائياً = إجمالي المدفوعات قبل اليوم − إجمالي المصاريف قبل اليوم)
  - رصيد القاصة في نهاية اليوم (= قاصة أمس + صافي اليوم)
- زر "ملخص يومي" في صفحة المحاسبة يفتح حواراً لاختيار التاريخ
- توليد `PDF` احترافي بالعربية (خط `Amiri`) يحوي:
  - رأسية مع اسم المركز والفرع والتاريخ
  - جدول الإيراد بالتفصيل + الإجمالي
  - جدول المصاريف بالتفصيل + الإجمالي
  - جدول ملخص القاصة (5 أسطر) مع تمييز رصيد الإقفال
  - مكان لتوقيع المحاسب والمسؤول
- المحاسب مُقيَّد بفرعه تلقائياً (دفاع على الخادم بالإضافة للواجهة)

### المرحلة 2.1 — دمج الفواتير (مكتملة 2026-04-24)

- جدولا
  `invoices`
  و
  `invoice_items`
- قيود تلقائية عند الإصدار:
  مدين ذمم مدينة
  `1130`
  / دائن إيراد (موزَّع حسب البنود)
  / دائن خصومات ممنوحة
  `4990`
  (إذا وُجد خصم)
- قيود تلقائية عند الدفع:
  مدين صندوق / دائن ذمم مدينة
- قيود عكسية عند التعديل والحذف

### إصلاحات حرجة

- إصلاح تعطّل الإنتاج بسبب
  `import.meta.url`
  في حِزَم
  `CJS`
- تحويل ملفات
  `SQL`
  إلى وحدات
  `TypeScript`
  لتُحزَّم مع التطبيق
- توحيد فروع النشر على
  `main`

---

## الطبقة الأولى — الليلة (2026-04-24)

تحسينات صغيرة آمنة قبل الإطلاق الصباحي.

### 2.2.1 — توسيع خريطة أنواع العلاج

قبول التنوّع الإملائي والصيغ المختلفة (فيزيائي، تأهيل، برسثيس، إلخ) لضمان وصول الإيراد لحسابه الصحيح حتى لو اختلفت الصياغة.

### 2.2.2 — تشديد صلاحيات الحذف والتعديل

مسارات حذف/تعديل الدفعات والمصروفات والفواتير تتطلّب دور "مدير" صراحةً. تمهيد لدور المحاسب.

### 2.2.3 — عرض نطاق التاريخ مع عدد الأيام

أسفل كل مجموع في الصفحة المحاسبية (وارد، مصاريف، ديون):
`من [تاريخ أول سجل] إلى [اليوم] — [عدد] يوماً`

مع قائمة اختيار نطاق مخصّص (من / إلى).

### 2.2.4 — ملف خطة الطريق في المستودع

هذا الملف الذي تقرأه الآن.

---

## الطبقة الثانية — خلال 2 إلى 3 أيام بعد الإطلاق

### 2.3 — دور المحاسب

- حقل
  `role`
  جديد:
  `admin` / `accountant` / `employee`
- حقل
  `branch_id`
  يربط المحاسب بفرع واحد محدد
- واجهة للمدير لإضافة محاسب
  (اسم، كلمة مرور، الفرع)
- تعديل
  `BranchGate`
  لقبول المحاسبين
- وسيط صلاحيات يُطبّق قيود:
  - المحاسب يرى بيانات فرعه فقط
  - يستطيع إدخال دفعات ومصاريف وفواتير وزيارات
  - لا يحذف ولا يعدّل أي شيء
  - المرضى للقراءة فقط
- تصفية القوائم حسب الفرع تلقائياً

### 2.4 — الملخّص اليومي وتصدير
`PDF`

- جدول
  `daily_cash_closings`
  يحفظ لكل يوم ولكل فرع:
  رصيد الافتتاح، الوارد، المصاريف، رصيد الإقفال
- معادلة:
  `قاصة اليوم = قاصة أمس + (وارد اليوم − مصاريف اليوم)`
- مسار:
  `GET /api/accounting/daily-summary?date=&branchId=`
- زر تصدير
  `PDF`
  منظّم:
  - رأسية (اسم المركز، الفرع، التاريخ)
  - وارد اليوم مُفصَّلاً حسب نوع الخدمة
  - مصاريف اليوم مُفصَّلة حسب الفئة
  - صافي اليوم
  - رصيد القاصة (أمس + اليوم)
  - مكان لتوقيع المحاسب والمدير

### 2.5 — نظام الموردين والمشتريات الآجلة

- جدول
  `vendors`
  (اسم، اتصال، عنوان، عملة)
- جدول
  `purchases`
  (مورد، تاريخ، مبلغ، حالة، فاتورة المورد، تاريخ الاستحقاق)
- واجهات
  `CRUD`
- قيود تلقائية:
  مدين مصروف أو مخزون / دائن ذمم دائنة
  `2110`
- قيد الدفع للمورد:
  مدين ذمم دائنة / دائن صندوق

---

## الطبقة الثالثة — الذكاء الاصطناعي

مبني على
`Claude Haiku 4.5`
(أساس) مع
`prompt caching`
+ رفع النداءات المعقدة إلى
`Claude Sonnet 4.6`.
طبقة تجريد
`aiProvider`
تسمح بتبديل المزوّد إعدادياً.

التكلفة المقدَّرة:
`$5-15`
شهرياً مع
`caching`
لنظام 5 فروع و10-15 موظف.

### 3.1 — مساعد محادثة ذكي

أيقونة ثابتة في كل صفحة تفهم البيانات الحالية وتجيب أسئلة مثل:
- "كم صرف فرع بغداد هذا الشهر مقارنة بالشهر الماضي؟"
- "أعطني قائمة المرضى الذين لم يدفعوا منذ أسبوعين"
- "ما متوسط قيمة الفاتورة في فرع الموصل؟"

### 3.2 — كشف الشذوذ والأخطاء التلقائي

- فاتورة بقيمة غير منطقية
- مصروف مكرر في نفس اليوم
- موظف سجّل زيارة بدون دفعة والمبلغ مطلوب
- قيد محاسبي غير متوازن (نظرياً محظور لكن احتياط)
- فروقات مشبوهة في القاصة اليومية

### 3.3 — إرشاد الموظف أثناء الإدخال

تنبيهات فورية على الشاشة:
- "لاحظت إدخال مصروف كهرباء بدون رقم فاتورة — هل تريد إضافته؟"
- "المريض لديه ذمم متأخّرة، هل تريد تسجيل هذه الدفعة عليها أم كدفعة جديدة؟"

### 3.4 — تصنيف تلقائي للمصاريف والخدمات

عند كتابة وصف المصروف، اقتراح الفئة تلقائياً بدلاً من انتظار الموظف.

### 3.5 — تقارير سردية شهرية

في نهاية كل شهر، توليد ملخّص بالعربية الفصحى عن أداء كل فرع:
- الإيرادات والمصاريف
- أبرز التغيّرات مقارنة بالشهر السابق
- ملاحظات وتوصيات

### 3.6 — تدقيق محاسبي ذكي

مراجعة دورية لقيود اليومية مع اقتراح تصحيحات أو تنبيهات عن أنماط غير معتادة.

### 3.7 — مراقبة دقة عمل الموظفين

تتبُّع نسبة الأخطاء لكل موظف واقتراح تدريب عند الحاجة.

### 3.8 — الرد التلقائي على الاستبيانات السلبية

عند استبيان سلبي، توليد مسودّة رد لطيف للمراجعة قبل الإرسال.

---

## الطبقة الرابعة — تحسينات بنيوية لاحقة

### 4.1 — أداة الترحيل التاريخي
`(Backfill)`

تحويل كل الزيارات والدفعات والمصاريف القديمة إلى قيود محاسبية بأثر رجعي، حتى تبدأ الدفاتر من تاريخ نشأة المركز لا من تاريخ نشر النظام.

### 4.2 — ربط خطط الأقساط بالذمم المدينة

المرضى الذين يدفعون بأقساط لا يظهرون في الدفاتر حالياً.

### 4.3 — تحسين سلامة المعاملات

لفّ كل عملية مالية في
`database transaction`
واحدة لمنع تلف الدفاتر عند فشل القيد.

### 4.4 — واجهات التقارير الكاملة

صفحات منظّمة لميزان المراجعة، قائمة الدخل، الميزانية العمومية
(الخدمات موجودة كـ
`API`
لكن لا واجهة لها).

### 4.5 — إغلاق الفترات من الواجهة

زر إغلاق الشهر/السنة مع منع القيود في الفترات المغلقة.

---

## الطبقة الخامسة — أنظمة متقدّمة (مستقبلية)

### 5.1 — إدارة المخزون

المستلزمات الطبية، الأطراف الصناعية، المساند. تتبُّع كميات، تكاليف، حركات.

### 5.2 — نظام الرواتب الكامل
`(Payroll)`

موظفون، عقود، رواتب شهرية، بدلات، خصومات، سُلَف.

### 5.3 — الأصول الثابتة والإهلاك

الأجهزة الطبية، السيارات، الأثاث — مع إهلاك شهري تلقائي وفقاً للمعايير.

### 5.4 — ضريبة القيمة المضافة
`(VAT)`

البنية جاهزة (حساب
`2130`
موجود). التفعيل عند اعتماد ضريبة عراقية رسمية.

### 5.5 — مطابقة البنك
`(Bank Reconciliation)`

استيراد كشوف الحسابات البنكية ومطابقتها مع
`1120`.

### 5.6 — تقرير التدفّقات النقدية
`(Cash Flow Statement)`

### 5.7 — تصدير
`Excel`
لكل التقارير

---

## مبادئ العمل

هذه الشروط ملتزَم بها في كل تعديل:

- **الحفاظ على البيانات** — لا حذف، لا
  `drizzle-kit push`
- **هجرات إضافية فقط** — جداول وأعمدة جديدة، بلا تعديل أو حذف موجود
- **كل
  `migration`
  قابل للـ
  `rollback`
  نظرياً**
- **الاختبار المحلي قبل الدفع**
- **فروع عمل مستقلة ثم
  `PR`
  إلى
  `main`**
- **لا
  `push`
  مباشرة على
  `main`**

---

## مراجع تقنية

- **المستودع:**
  `yasirsaadi/bionic-centers-manager`
- **النشر:**
  `Render`
  من فرع
  `main`
  مع
  `Auto-Deploy: On Commit`
- **قاعدة البيانات:**
  `Neon PostgreSQL`
- **مصدر الإيرادات الرئيسي:**
  `server/accounting/auto_journal.ts`
- **منطق القيود:**
  `server/accounting/ledger.ts`
- **الهجرات:**
  `server/migrations/*.ts`
- **مخطط قاعدة البيانات:**
  `shared/schema.ts`
